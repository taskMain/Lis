# 检验检查结果互认平台 MVP 交接文档

## 接手目标

继续梳理 `E:\MedSync\MedicalRecognition` 中检验检查结果互认平台的 MVP 需求。目前处于业务流程和系统边界澄清阶段，尚未进入代码实现。下一步重点是确认 MVP 主流程，再据此编写需求规约。

## 项目目标

建设一个检验检查结果互认平台，连接医院 HIS、LIS、PACS、超声等系统，接收已经完成标准编码对照的检查检验报告，并向医院 HIS 提供患者可互认报告查询、报告/PDF/影像调阅、处理结果留痕和统计能力。

平台不控制医生的临床行为。弹窗展示、避免或撤销开单、病历引用、不互认原因录入等属于医院 HIS 和医生的职责，平台通过接口提供数据并接收反馈。

## 当前已确认的 MVP 决策

1. MVP 不导入平台上线前的历史报告，只处理上线后新产生并审核的报告。
2. 平台只接收医院侧已经完成标准编码对照的数据，未对码数据不上传。
3. 报告接收成功后即视为可互认数据，不再按有效期、质控资格或互认范围进行二次业务资格筛选。
4. 基础字段、格式、患者标识、重复数据、报告更新或作废状态等检查仍然需要保留。这些属于数据有效性控制，不属于互认资格筛选。
5. 医院上传的数据包括必要的患者/就诊标识、结构化报告、PDF 报告和影像资料或影像索引。
6. 在线查询由医院 HIS 在接诊或开单场景发起，平台按患者身份和标准项目编码精确匹配。
7. 平台返回候选报告，但最终采纳、不采纳或未处理由医生在医院 HIS 中决定。
8. 医院 HIS 应向平台反馈提醒、调阅、采纳、不采纳和未处理等行为，平台负责审计与统计。

## 已落地文档

- 主流程图：[docs/流程图.md](E:/MedSync/MedicalRecognition/docs/流程图.md)
  - 已包含标准目录与对码、仅上传已对码新报告、基础校验、患者关联、存储索引、在线精确匹配、医生/HIS 外部行为和结果统计。
  - 当前为工作区未提交修改，不要覆盖或回退。
- 需求规约：[docs/需求规约SRS.md](E:/MedSync/MedicalRecognition/docs/需求规约SRS.md)
  - 当前为空，尚未开始编写。
- 参考资料：[docs/参考资料](E:/MedSync/MedicalRecognition/docs/参考资料)
  - 包含甘肃省数据交换规范、互认平台对接技术方案、影像共享实施方案、325 项标准目录、楚雄系统截图和一份调研报告。
  - 不要在新文档中复制其中的示例身份信息、账号、密码或接口密钥。

## 资料阅读所得关键事实

- 甘肃方案的业务接口包括授权、近期可互认项目查询、互认结果提交、检验结果引用和检查结果引用。
- 医院数据通道包括结构化报告、PDF 报告和 DICOM 影像。
- 数据关联依赖医院代码、就诊流水号、检查/检验流水号、影像号或 Study UID 等标识。
- 标准目录工作簿共有 325 项：检验 140 项、放射影像 145 项、超声影像 40 项；“是否对码”列为空，因此它是待对码标准目录，不是已完成的医院映射成果。
- 楚雄截图展示了提醒记录、调阅状态、互认/不互认/未选择、院内/院外记录，以及机构、项目、医生等统计维度。

## 待确认问题

1. “接收成功即可以互认”是否意味着报告自上线后永久参与查询，还是仍需限定查询时间窗口。当前流程图按“不执行有效期筛选”处理，但该决定会直接影响医疗风险和查询结果数量。
2. 医院报告发生修改、撤销或作废时，平台采用覆盖、版本保留还是停止查询，需要明确状态规则。
3. 影像资料在 MVP 中是全量存储 DICOM、只存 PDF，还是仅保存院端阅片地址/影像索引。
4. 患者精确关联使用身份证号、电子健康卡、院内卡号还是组合键；无证件患者是否暂不支持。
5. 医院侧负责对码转换，平台是否仍需维护和查看医院对码关系，以及谁负责审核发布。
6. HIS 如何反馈“提醒已展示”“报告已调阅”“未选择”，需要明确接口调用时点和统计口径。
7. `docs/流程图.md` 的“后续拆分图”中仍有“规则过滤流程”字样，与 MVP 不做二次资格筛选的决定不完全一致，后续应改为“精确匹配与查询流程”。

## 推荐下一步

1. 与用户逐项确认上述待确认问题，尤其是查询时间窗口、作废处理和影像存储范围。
2. 根据确认结果微调 `docs/流程图.md`，不要重新引入有效期、质控资格或互认范围筛选，除非用户改变 MVP 决策。
3. 用户确认主流程后，在 `docs/需求规约SRS.md` 编写 MVP SRS，优先覆盖参与者、范围边界、主事件流、备选流、业务规则、关键数据和外部接口。
4. 后续再拆分对码、数据采集、查询调阅、结果反馈和统计口径子流程，不要提前进入代码设计。

## 工作区状态与保护事项

- 当前分支：`main`，跟踪 `origin/main`。
- 已修改：`AGENTS.md`、`docs/流程图.md`。
- 未跟踪：`.claude/`、`.codex/`、`.cursor/`、`docs/参考资料/`。
- `AGENTS.md` 当前是 0 字节。用户已明确说明此前提供的 AGENTS.md 指令不再适用。
- 上述修改和未跟踪内容都应视为用户或前序工作的成果，不要清理、覆盖或回退。
- 当前没有代码改动、测试结果或提交记录。

## Suggested Skills

- `dyskills:dy-srs`：主流程确认后编写或审查需求规约时使用；若当前环境未安装，则按标准 SRS 用例结构继续。
- `dyskills:dy-puml`：后续进行领域建模、聚合/命令/事件设计时使用；当前 Mermaid 业务流程阶段不必提前进入领域模型。
- `pdf:pdf`：需要重新核对调研 PDF 或正式 PDF 资料时使用。
- `spreadsheets:Spreadsheets`：需要重新分析 325 项标准项目目录时使用。
- `gitnexus-cli`：进入代码开发前先为当前仓库建立或校验 GitNexus 索引。现有可用索引指向其他仓库，不能用于本项目变更分析。
