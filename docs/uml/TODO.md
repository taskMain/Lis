# UML TODO

- [ ] 重新验证当前六个 UML 文件，更新交接文档中的过期验证证据
  - 当前工作区文件与交接验证副本已不一致。
- [ ] 补齐或明确当前 UML 的本地 ddd_lib.puml 验证依赖
  - 当前 docs/uml 文件引用该依赖，但目录中没有本地文件。
- [ ] 修正命令契约文件“仅包含 Request/DTO”的描述
  - 文件实际还包含报告提交状态枚举。
- [ ] 决定是否将医院 HIS PDF 下载能力作为独立 Query 明确建模
  - 当前主要通过候选/引用 DTO 和规则文字表达。
