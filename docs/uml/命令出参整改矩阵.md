# 命令出参整改矩阵

> 用途：记录 UML 命令契约中同步出参的审查结果，作为后续 UML 和 DTO 参考图修改清单。
>
> 当前状态：参考草稿/设计登记；代码、OpenAPI、API Client 和运行验证尚未开始，本文不是正式业务契约，也不作为源码完成证据。

## 统一判断原则

| 原则 | 当前结论 | 状态 |
| --- | --- | --- |
| 领域事件不能替代同步接口出参 | 外部调用方需要的即时结果必须在命令出参或响应 DTO 中表达 | 已确认 |
| 匹配查询返回业务数据 | RequestRecognitionMatchesCommand 必须返回匹配记录、匹配项及匹配内容；成功无匹配时返回空集合 | 已确认 |
| 写入/提交类命令的成功结果 | 成功或幂等成功返回 Boolean=true，失败抛业务异常，不以 false 表示失败 | DesignConfirmed |
| 内部实体命令 | 不是医院接入契约，可由 Facade 内部使用 ID、数量或 Ack；不要求对外响应 DTO | 建议 |
| 空成功与 Boolean=true 的取舍 | 当前版本统一采用 Boolean=true，暂不引入 OperationSuccessResponse | DesignConfirmed |

## A. 医院 HIS / 医院采集服务外部命令

| 命令 | 调用场景 | 当前问题 | 建议出参 | 业务决策 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| RequestRecognitionMatchesCommand | HIS 接诊或开单前查询互认匹配 | 已在正式查询 UML 补齐响应模型 | RecognitionMatchesResponseReadModel：互认匹配记录 ID、匹配项 ID、报告/项目匹配内容、PDF/影像入口；无匹配返回空集合 | 正式模型位于 `3-HospitalIntegrationApiQuery.wsd`；本矩阵仅登记 | P0 | DesignRegistered |
| SubmitCompleteLaboratoryReportCommand | 采集服务提交完整检验报告 | 未说明同步成功结果 | Boolean=true | 每次成功保存返回 true 并形成新版本；失败抛业务异常 | P1 | DesignConfirmed |
| SubmitCompleteExaminationReportCommand | 采集服务提交完整检查报告 | 未说明同步成功结果 | Boolean=true | 与检验报告提交保持一致，不提供报告提交幂等语义 | P1 | DesignConfirmed |
| VoidLaboratoryReportCommand | 采集服务作废检验报告 | 未说明作废成功后的同步确认 | Boolean=true；失败抛业务异常 | 不返回最终生命周期状态 | P2 | DesignConfirmed |
| VoidExaminationReportCommand | 采集服务作废检查报告 | 未说明作废成功后的同步确认 | Boolean=true；失败抛业务异常 | 与检验报告作废保持一致 | P2 | DesignConfirmed |
| SubmitRecognitionProcessingResultsCommand | HIS 提交整组采纳/不采纳结果 | 未说明成功 Ack | Boolean=true；失败/冲突抛业务异常 | 不返回首次处理时间或原结果 | P1 | DesignConfirmed |
| SubmitRecognitionReferencesCommand | HIS 提交已写入病历的引用事实 | 未说明成功 Ack | Boolean=true；失败/冲突抛业务异常 | 不返回已记录数量 | P1 | DesignConfirmed |

## B. 管理端外部命令

| 命令 | 调用场景 | 当前问题 | 建议出参 | 业务决策 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| CreateMutualRecognitionItemCommand | 新增互认项目 | 未说明保存成功后的确认 | Boolean=true | 不返回新建 ID | P2 | DesignConfirmed |
| UpdateMutualRecognitionItemConfigurationCommand | 修改互认项目配置 | 未说明最终保存结果 | Boolean=true | 不返回最终配置状态 | P2 | DesignConfirmed |
| EnableMutualRecognitionItemCommand | 启用互认项目 | 未说明成功结果 | Boolean=true | 统一 Boolean | P2 | DesignConfirmed |
| DisableMutualRecognitionItemCommand | 停用互认项目 | 未说明成功结果 | Boolean=true | 统一 Boolean | P2 | DesignConfirmed |
| CreateMedicalStandardCategoryCommand | 新增标准项目分类 | 未说明保存成功后的确认 | Boolean=true | 不返回新建 ID | P2 | DesignConfirmed |
| UpdateMedicalStandardCategoryCommand | 修改标准项目分类资料 | 未说明最终保存结果 | Boolean=true | 不修改启停状态 | P2 | DesignConfirmed |
| EnableMedicalStandardCategoryCommand | 启用标准项目分类 | 未说明成功结果 | Boolean=true | 独立状态入口 | P2 | DesignConfirmed |
| DisableMedicalStandardCategoryCommand | 停用标准项目分类 | 未说明成功结果 | Boolean=true | 独立状态入口 | P2 | DesignConfirmed |
| CreateMedicalStandardGroupCommand | 新增标准项目分组 | 未说明保存成功后的确认 | Boolean=true | 不返回新建 ID | P2 | DesignConfirmed |
| UpdateMedicalStandardGroupCommand | 修改标准项目分组资料 | 未说明最终保存结果 | Boolean=true | 不修改启停状态 | P2 | DesignConfirmed |
| EnableMedicalStandardGroupCommand | 启用标准项目分组 | 未说明成功结果 | Boolean=true | 独立状态入口 | P2 | DesignConfirmed |
| DisableMedicalStandardGroupCommand | 停用标准项目分组 | 未说明成功结果 | Boolean=true | 独立状态入口 | P2 | DesignConfirmed |
| CreateMedicalStandardItemCommand | 新增标准医疗项目 | 未说明保存成功后的确认 | Boolean=true | 不返回新建 ID；创建后默认启用 | P2 | DesignConfirmed |
| ChangeMedicalStandardItemRemarkCommand | 修改标准医疗项目备注 | 未说明最终保存结果 | Boolean=true | 仅允许修改备注，不修改编码、名称、分类、分组或启停状态 | P2 | DesignConfirmed |
| EnableMedicalStandardItemCommand | 启用标准医疗项目 | 未说明成功结果 | Boolean=true | 独立状态入口 | P2 | DesignConfirmed |
| DisableMedicalStandardItemCommand | 停用标准医疗项目 | 未说明成功结果 | Boolean=true | 独立状态入口；编码不可复用 | P2 | DesignConfirmed |
| SaveOrganizationHospitalBranchRecognitionAmountCommand | 保存组织/医院/院区项目金额 | 未说明保存成功后的确认 | Boolean=true | 只返回成功 Ack | P2 | DesignConfirmed |

## C. 聚合内部实体命令

这些命令由 Facade 协调或属于聚合内部操作，不直接作为医院接入契约。它们仍需在实现设计中约定内部返回值，但不必为每个命令创建对外 Response DTO。

| 命令 | 所属外部入口/用途 | 建议内部结果 | 状态 |
| --- | --- | --- | --- |
| CreateOrGetMedicalRecognitionReportCommand | 完整检验/检查报告 Facade 内部 | ReportId 或内部 Ack | 已调整命名 |
| AppendMedicalReportVersionCommand | 每次成功完整上传追加报告版本 | ReportVersionId、版本号或内部 Ack | 已调整命名 |
| CreateLaboratoryReportContentCommand | 创建检验报告内容 | 内部 Ack | 待确认 |
| CreateLaboratoryResultItemsCommand | 创建普通检验结果 | 创建数量或内部 Ack | 待确认 |
| CreateLaboratoryBacteriaResultsCommand | 创建细菌鉴定结果 | 创建数量或内部 Ack | 待确认 |
| CreateLaboratoryAntimicrobialSusceptibilityResultsCommand | 创建药敏结果 | 创建数量或内部 Ack | 待确认 |
| CreateExaminationReportContentCommand | 创建检查报告内容 | 内部 Ack | 待确认 |
| CreateExaminationItemsCommand | 创建检查项目 | 创建数量或内部 Ack | 待确认 |
| CreateExaminationSitesCommand | 创建检查部位 | 创建数量或内部 Ack | 待确认 |
| CreateRecognitionMatchRecordCommand | 匹配查询 Facade 内部 | RecognitionMatchRecordId、匹配记录创建时间 | 待确认 |
| CreateRecognitionMatchItemsCommand | 匹配查询 Facade 内部 | RecognitionMatchItemId[] 或创建数量 | 待确认 |

## 待确认决策清单

| 编号 | 决策问题 | 影响范围 | 当前倾向 | 状态 |
| --- | --- | --- | --- | --- |
| D1 | 写入/提交类外部命令是否统一成功返回 Boolean=true，失败抛业务异常？ | 报告提交、作废、互认处理、引用、配置 | 采用裸 Boolean，不增加统一响应对象 | DesignConfirmed |
| D2 | 完整报告提交是否需要返回 ReportId、当前 ReportVersionId、版本号和处理类型？ | 两个完整报告提交 Facade | 不返回平台内部标识，只返回 Boolean=true | DesignConfirmed |
| D3 | 完整报告提交重试是否形成新版本？ | 报告提交 | 每次成功上传形成新版本；报告提交不定义幂等确认 | DesignConfirmed |
| D4 | 配置命令是否需要返回新建实体 ID 或最终状态？ | F01/F02 配置命令 | 当前统一只返回 Boolean=true | DesignConfirmed |
| D5 | RecognitionMatchesReturnedEvent 是否仅在非空匹配生成时触发？ | 匹配查询 | 依据当前 SRS，非空匹配形成提醒；空结果不创建匹配记录 | DesignConfirmed |

## 实施顺序

1. 已登记 D1-D4 的统一成功响应原则，待实现阶段核对。
2. 已登记 RequestRecognitionMatchesCommand 的响应 DTO 及 Meta DTO 关系，待正式接口契约确认。
3. 已登记两个完整报告提交命令的 Boolean 响应，待实现阶段核对。
4. 已登记其他外部命令的 Boolean 成功响应表达，待实现阶段核对。
5. UML 静态校验已完成；PlantUML 渲染工具未在当前环境中发现，待工具可用后补做。

## 交接信息

- 总协调智能体：/root
- 主责智能体：/root
- 当前阶段：需求/领域模型整改登记
- 任务目标：登记所有命令出参缺口，供用户审查后再实施
- 负责范围：docs/uml/命令出参整改矩阵.md
- 关键登记：匹配查询必须有丰富响应；其他外部命令暂按 Boolean=true 登记，正式接口契约仍待确认
- 修改文件：仅新增本矩阵文件
- 已记录设计检查验证：命令清单与 SRS/Meta DTO 核对；五个可导入 UML 文件通过静态兼容检查；git diff --check 通过
- 未执行或受阻验证：真实生成器目录缺失，校验保留 C001；PlantUML 可执行工具/JAR 未在当前环境中发现，渲染验证 NotRun
- 遗留风险：需在真实 Dy.DevToolkit 和 PlantUML 环境可用后补做导入、生成和渲染验证；复杂匹配响应 DTO 的最终接口字段仍需技术设计确认
- 建议下一主责智能体：API Platform Engineer 负责将匹配响应字段落实为正式接口契约
- 下一决策门：确认匹配响应 DTO 的字段粒度及正式 API 报文结构
