# 05: 引用详情（F06）

**What to build:** 医院 HIS 按患者证件类型与号码、本次来源就诊类型与流水号获取引用详情，平台按该组处理结果保存时间计算有效期，逐项校验绑定报告版本的有效性，返回报告公共上下文、项目级内容、检查所见与检查结论、PDF 与影像入口；部分项目不可用时按设计返回可用部分（全部不可用时按设计失败）。有效期时长参数按平台级编码读取，取值缺失、非法或参数服务失败一律整次失败。

**Blocked by:** 04

**Status:** developed-and-reviewed

**外部前置：** 四张表已由负责人建成；该组处理结果由票 04 保存（有效期起点为其保存时间）；有效期时长参数已在平台系统参数系统配置（编码 `citation_detail_valid_hours`，当前取值 24 小时），其宿主运行面归票 08。

- [x] 引用详情基本返回：含匹配项标识、报告与版本标识、项目内容、文件信息与报告公共上下文（来源医疗机构名称由来源三值经组织路径解析点批量回填）（V46）（证据：`Stage5QueryTests` 的基本返回用例与读取次数计数断言，见阶段根 `testReport.md` 的「批次 4」段；真实数据库面归票 08）
- [x] 未匹配到采纳记录时整次失败（V47）（证据：同文件的拒绝用例，断言文案不含其他患者证件号与报告标识）
- [x] 有效期边界按包含处理，起点为该组处理结果保存时间（V48）（证据：同文件的边界用例，覆盖边界两侧）
- [x] 有效期时长参数不存在、为非正整数、参数服务读取失败时整次失败，不使用兜底值（V49、V50、V51）（证据：三组用例，均断言未发起任何候选读取）
- [x] 参数调整后立即生效（V52）（证据：同一条记录两次不同参数值的用例，断言每次请求都重新读取参数）
- [x] 绑定报告版本已非当前有效版本、报告已作废时不返回该项目（V53、V54）（证据：版本失效与报告作废两组用例）
- [x] 不采纳项目不返回（V55）
- [x] 自然超过可互认时间但仍在有效期内的项目按设计返回（V56）（证据：该用例断言未读取互认配置）
- [x] 同版本同项目存在多条采纳时按设计处理（V57）（证据：只返回保存时间最近的一条，同一保存时间时以匹配项标识升序兜底，用例侧独立复算期望值）
- [x] 同次就诊中部分组超期或失效时按部分可用语义返回（V58）
- [x] 已提交引用结果的项目在有效期内仍可获取详情（V58b）
- [x] 引用详情含检查所见与检查结论（V84 的引用详情面；匹配响应不含该两项由票 01 承接）（证据：检查侧的字段断言；引用侧语句投影该两项）
- [x] 引用详情链不产生任何写入、事件或状态变化；不形成引用结果、不形成引用次数、不形成新的互认处理（V88 的查询链面，与票 07 共同承接）（证据：查询应用服务与查询仓储的引用详情分片内无事务、写入与事件登记符号，另以成功与失败各一次的用例断言零写入与零事件）
- [x] 新增查询入口后，既有查询入口契约方法集合等值断言、查询契约固定方法名清单与查询侧端口与实现的分部文件数量断言同批同步（无矩阵编号：既有断言同步面）（证据：`Stage4EndpointTests` 的查询入口方法集合、`Stage1ArchitectureTests` 的查询契约固定方法名清单由十一条改为十二条并仍为等值断言、`Stage4ConstraintTests` 的查询侧分部文件数量由三改为四、各语句清单与列一致性守卫同步；未放宽任何既有断言）
- [x] `git diff --check` 干净（无矩阵编号）（证据：命令 `git diff --check` 无输出，见阶段根 `testReport.md` 的「批次 4」段）

**口径：** 本票覆盖查询与读模型面（`Query`、`Contract` 的 V84 引用详情面、`Static/Domain` 的 V88 查询链面）；V84 的匹配响应面由票 01 承接，V88 的静态核对面由票 07 承接；真实数据库与真实工作单元面由票 08 取证。
