# 06: 引用结果提交（F07）

**What to build:** 医院把已经写入病历的引用事实提交回来：平台逐项反查匹配项与对应处理结果、校验归属一致性与时间序、识别幂等（完全一致的重试按幂等处理，相同匹配项但事实字段不同返回冲突）→ 整批原子保存，只增加引用次数（采纳次数、来源医院被互认次数与预计节省金额不变）→ 登记引用结果已记录事件。引用结果不校验提交时报告版本是否有效，也不受引用详情有效期限制；并发重复写入由引用事实表的唯一索引兜底并翻译为业务拒绝。

**Blocked by:** 05

**Status:** ready-for-agent

**外部前置：** 四张表由负责人建成；该组处理结果已由票 04 保存

- [ ] 引用结果基本提交：每项反查通过后保存引用科室与医生的标识与名称、实际引用时间，登记事件（V59）
- [ ] 一次请求包含不同匹配记录的项目时整批校验并原子保存（V60）
- [ ] 匹配项不存在、匹配项对应处理结果不存在、匹配项对应决定为不采纳时整次失败（V61、V62、V63）
- [ ] 当前可信三值与匹配记录接收三值不一致时整次失败（V64）
- [ ] 引用科室或医生标识与名称不成对时整次失败（V65）
- [ ] 实际引用时间早于已保存的互认时间、晚于请求接收时间时整次失败；等于两端边界时接受（V66、V67、V68）
- [ ] 幂等重试完全一致时按幂等处理；相同匹配项但事实字段不同时返回冲突不登记；相同业务键仅引用科室或医生名称不同时按设计处理（V69、V70、V70b）
- [ ] 提交时报告已更正或作废时仍接受医院明确反馈的既有引用事实（V71）
- [ ] 引用只增加引用次数；采纳次数、来源医院被互认次数与预计节省金额不变（V72）
- [ ] 超过引用详情有效期仍可提交引用结果（V72b）
- [ ] 不得以调用账号替代引用科室与引用医生（V95 的引用侧面，与票 04 共同承接）
- [ ] 新增写入口后，既有写入口契约方法集合等值断言与写侧分部文件逐文件等值断言同批同步（无矩阵编号：既有断言同步面；查询入口的同步见票 05）
- [ ] 写路径上的真实入口断言「请求校验先于领域写入」（无矩阵编号：取证方式见 README 约定 13）
- [ ] `git diff --check` 干净（无矩阵编号）

**口径：** 本票覆盖契约与领域校验层（`Contract/Domain`、`Domain`、`Domain/Repository`）；V95 在本票承接引用结果侧的负向面，处理结果侧由票 04 承接；整批原子性与真实数据库面由票 08 取证。
