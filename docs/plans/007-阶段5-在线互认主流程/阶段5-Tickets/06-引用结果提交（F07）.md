# 06: 引用结果提交（F07）

**What to build:** 医院把已经写入病历的引用事实提交回来：平台逐项反查匹配项与对应处理结果、校验归属一致性与时间序、识别幂等（完全一致的重试按幂等处理，相同匹配项但事实字段不同返回冲突）→ 整批原子保存，只增加引用次数（采纳次数、来源医院被互认次数与预计节省金额不变）→ 登记引用结果已记录事件。引用结果不校验提交时报告版本是否有效，也不受引用详情有效期限制；并发重复写入由引用事实表的唯一索引兜底，撞键时数据库异常按原样向外传播。

**Blocked by:** 05

**Status:** developed-and-reviewed

**外部前置：** 四张表已由负责人建成；该组处理结果由票 04 保存（测试以替身构造）。

- [x] 引用结果基本提交：每项反查通过后保存引用科室与医生的标识与名称、实际引用时间，登记事件（V59）（证据：`Stage5WritePathTests` 的引用结果用例，见阶段根 `testReport.md` 的「批次 5」段；整批零提交面归票 08）
- [x] 一次请求包含不同匹配记录的项目时整批校验并原子保存（V60）
- [x] 匹配项不存在、匹配项对应处理结果不存在、匹配项对应决定为不采纳时整次失败（V61、V62、V63）（证据：三条用例按不存在、无处理结果、非采纳的顺序判定，异常文案带相关匹配项标识）
- [x] 当前可信三值与匹配记录接收三值不一致时整次失败（V64）
- [x] 引用科室或医生标识与名称不成对时整次失败（V65）（证据：请求校验面与领域面两侧分别取证）
- [x] 实际引用时间早于已保存的互认时间、晚于请求接收时间时整次失败；等于两端边界时接受（V66、V67、V68）（证据：三条用例；边界用例另带边界外一档必须失败的反向断言）
- [x] 幂等重试完全一致时按幂等处理；相同匹配项但事实字段不同时返回冲突不登记；相同业务键仅引用科室或医生名称不同时按设计处理（V69、V70、V70b）（证据：幂等命中不新增不重复统计不登记事件；冲突不覆盖且原值保留；名称确实不参与一致性判断）
- [x] 提交时报告已更正或作废时仍接受医院明确反馈的既有引用事实（V71）（证据：以断言未发起版本有效性读取取证「不校验」）
- [x] 引用只增加引用次数；采纳次数、来源医院被互认次数与预计节省金额不变（V72）（证据：断言未读取金额、处理结果条数与该金额不变、未更新处理结果保存时间）
- [x] 超过引用详情有效期仍可提交引用结果（V72b）（证据：把处理结果保存时间推到一年前并断言未读取系统参数）
- [x] 不得以调用账号替代引用科室与引用医生（V95 的引用侧面，与票 04 共同承接）（证据：断言引用科室与医生标识不等于操作人、权限系统零调用）
- [x] 新增写入口后，既有写入口契约方法集合等值断言与写侧分部文件逐文件等值断言同批同步（无矩阵编号：既有断言同步面；查询入口的同步见票 05）（证据：`Stage4EndpointTests` 的写入口方法集合与事务声明清单、`Stage4ConstraintTests` 的分部文件清单与数量断言与调用点扫描面、各语句清单与列一致性守卫；未放宽任何既有断言）
- [x] 写路径上的真实入口断言「请求校验先于领域写入」（无矩阵编号：取证方式见 README 约定 13）
- [x] `git diff --check` 干净（无矩阵编号）（证据：命令 `git diff --check` 无输出，见阶段根 `testReport.md` 的「批次 5」段）

**口径：** 本票覆盖契约与领域校验层（`Contract/Domain`、`Domain`、`Domain/Repository`）；V95 在本票承接引用结果侧的负向面，处理结果侧由票 04 承接；整批原子性与真实数据库面由票 08 取证。
