# 07: 静态收尾与实现约束核对

**What to build:** 让阶段 5 的跨接口静态面收口：不新增异常中间件、过滤器、响应转换器或错误包装模型，不新增角色门控；事务声明只加在匹配查询、处理结果提交与引用结果提交三个入口，引用详情不声明，且既有事务声明分布口径不变；引用详情链不产生任何写入、事件或状态变化。同时完成构建、静态检查与差异检查，并按约定 13 复核请求校验取证的落地情况。

**Blocked by:** 03、04、05、06

**Status:** ready-for-agent

**外部前置：** 无（本票为静态核对面，不需要建表与参数配置）

- [ ] 不新增异常中间件、过滤器、响应转换器或错误包装模型；不新增角色门控（V86）
- [ ] 事务声明：匹配查询、处理结果提交、引用结果提交三个入口声明显式事务，引用详情不声明；既有声明分布与设计口径一致（V87）
- [ ] 引用详情链不产生任何写入、事件或状态变化；不形成引用结果、不形成引用次数、不形成新的互认处理（V88 的静态核对面，与票 05 共同承接）
- [ ] 实现约束核对：不新增手写控制器、不重建聚合与管理器、不重复声明共享语法助手、不重生成 API Client、不重跑后端代码生成器、不覆盖再生成保护清单中的配置与启动文件；新增源码守卫若复用共享语法工具，须登记 `Architecture/SourceGuardReuseTests.cs` 的受治理清单并保持精确一致（无矩阵编号：按 README 约定 12 的静态核对）
- [ ] 四个入口的请求校验取证已落地：四个请求类型经校验器断言必填与标识类型，写路径上以真实入口断言「请求校验先于领域写入」（无矩阵编号：按 README 约定 13）
- [ ] 构建、静态检查与 `git diff --check` 干净（无矩阵编号）
- [ ] 阶段根 `testReport.md` 已按执行结果更新，残余风险登记与设计一致（无矩阵编号）

**口径：** 本票覆盖跨接口静态层（`Static`、`Static/Domain`）；V88 在本票承接静态核对面（查询链行为面由票 05 承接）；真实数据库与宿主面由票 08 取证。
