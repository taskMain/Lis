# 08: 真实宿主链路验收与阶段报告

**What to build:** 让四个接口在真实宿主、真实数据库与真实工作单元上取得证据：三个写用例的整批零提交经失败注入取证，参数三类前置经共享桩取证（见第 12 行勾选项与 `阶段5-规格.md` 的测试替身决策），未认证请求被拒绝，成功后可经接口回读验证；四张表与索引在目标库上核对，引用事实的唯一索引在同一匹配项的重复写入下表现为业务拒绝。结果、证据与残余风险写入阶段根 `testReport.md`，无证据不写通过性状态。

**Blocked by:** 07

**Status:** evidence-collected

**外部前置：** 四张表由负责人建成；三个系统参数在平台系统参数系统配置；宿主可用且可经宿主入口发起请求；部署宿主的外部服务代理映射（组织、用户、系统参数）已就位

- [ ] 三个写用例的整批原子性经失败注入取证：失败时零提交、不登记事件（V1、V23、V59 的整批零提交面）
- [x] 参数严格失败语义经共享桩取证：取值缺失、取值非法、参数服务异常三类前置在匹配查询与引用详情两侧均整次失败（V89、V49、V50、V51；取证方式见 `阶段5-规格.md` 的测试替身决策）
- [x] 不带认证凭据的请求被拒绝（V92）
- [x] 四个接口成功后可经接口回读验证，返回内容与设计读模型一致（V1、V46；两个写入口的回读经匹配查询与引用详情实现）
- [x] 四张表与四条索引在目标库上与脚本一致；引用事实表按互认匹配项标识的唯一索引使重复写入表现为业务拒绝（V73、V75、V76 的数据库面）
- [ ] 矩阵「真实数据库」列记「是」的 71 条（覆盖票 02–06 声明的编号，逐行判定）逐条取得证据；无上述证据的受影响运行项记 `Blocked`，其余未执行项记 `NotRun`（无矩阵编号：取证口径，判据见 `Server/design.md` 的测试数据边界段）
- [x] 阶段根 `testReport.md` 更新完成：执行结果、证据出处、残余风险与重审条件（无矩阵编号）
- [x] `git diff --check` 干净（无矩阵编号）

**结果与未覆盖面。** 第一项：处理结果（V23）与引用结果（V59）的整批原子性面按项目口径记 `N/A`——失败时整批不保存由框架的显式事务执行，属框架生命周期，业务侧不重复验证、也不做故障注入；业务侧该侧的三项保证（事务声明、校验先于写入、影响行数守卫）分别由 V87 的静态判据、V26-V36 等应用层零写入用例与领域实现承接。匹配查询（V1）的受控注入作为附加证据保留。该项按原文口径（三个写用例都经失败注入取证）未满足。第六项按「逐行判定」已完成：71 条逐条给出状态与理由，其中相当一部分按条目构造口径记 `NotRun` 或 `N/A`，原文要求「逐条取得证据」，因此该项按原文口径同样未满足。上述两项的未覆盖面与理由即为本票的收口结论，不构成遗留动作。

**收口。** 阶段 5 已由负责人于 2026-09-20 裁定收口；本票两项未覆盖面（框架回滚面记 `N/A`、逐条证据面记 `NotRun`）与两处证据边界（V48、V64）均不阻塞收口。交付物尚未提交。

**残余证据边界（负责人已接受现状）。** V48（引用详情有效期边界）未做等待式取证，重审条件是为该就诊构造一条独占的较早决定后按参数值两侧取样；V64（可信三值与记录接收三值不一致）在公开入口不可达，重审条件是出现接收三值与可信三值不一致的真实数据。两条均记 `NotRun` 并登记在阶段根 `testReport.md` 的残余风险表。

**口径：** 本票覆盖真实数据库与宿主面（矩阵「真实数据库」列为「是」的条目，含 V1、V23、V59 的整批零提交面）；本票不新增矩阵编号，只承接前序各票已声明编号的运行时面；测试状态只用 `testing-baseline.md` 的词汇并只写在阶段根 `testReport.md`。
