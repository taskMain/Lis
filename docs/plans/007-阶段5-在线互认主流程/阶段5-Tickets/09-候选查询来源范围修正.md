# 09: 候选查询来源范围修正

**What to build:** 修正候选报告查询语句的来源范围：检验与检查两个报告类型分支各只保留 `r.organization_code = $OrganizationCode` 一条来源范围谓词，删除 `r.hospital_code` 与 `r.branch_code` 两条过滤谓词，使同组织跨医院报告与本院跨院区报告能够进入候选；来源医院列继续投影给域层完成本院报告判定（S5-D25）。语句功能注释与同一行谓词同步改写，消除「判定在应用层完成」与谓词自行过滤的自相矛盾。域层管理器与读模型不变：候选读模型本就只承载来源医院编码，本院报告判定语义（不区分院区、开关与排除时长）原样承接。

**Blocked by:** 03

**Status:** evidence-collected

**外部前置：** 无新增。复用票 03 的外部前置：四张表已由负责人建成；三个系统参数已在平台系统参数系统配置（本院报告匹配开关取值 1、本院报告排除时长取值 4 小时）；候选语句涉及的目标库与 schema 已由票 08 确认。

- [x] 候选语句两个分支各只保留来源组织一条来源范围谓词，来源医院与来源院区不作为过滤条件出现（V79b）（证据：`Stage5SqlMapTests.Candidate_query_limits_source_scope_to_organization_only`，修复前失败（`r.hospital_code =` 出现 2 次、期望 0）、修正语句后通过，带重新引入过滤谓词的变异证据；见阶段根 `testReport.md` 的票 09 段）
- [x] 同组织跨医院报告作为候选参与匹配，不受本院报告开关与排除时长约束；来源组织不一致的仍不参与（V16b）（证据：真实链路双向取证——`lisadmin`（CSYY2）查询命中 `0101` 报告 `MR-ST5-09-CROSS-0101-002`、`yangkj`（0101）查询命中 `CSYY2` 报告 `MR-ST5-09-CROSS-CSYY2-002`，两次均伴随本院未达 4 小时排除窗的最新报告被排除；请求响应原文见 `var/st5-09/`，见阶段根 `testReport.md` 的票 09 段）
- [x] 本院跨院区候选可达：候选查询不以来源院区过滤，域层按本院报告开关与排除时长判断（不区分院区）；真实链路面受测试身份每医院单一院区限制记 `Blocked`，语义面由 V17-V20 既有用例组承接（V16c）（证据：V79b 静态守卫冻结院区不作为过滤条件；候选读模型不含来源院区字段，「不区分院区」由结构成立；V17-V20 本院报告用例组见票 03，真实链路面阻塞理由见阶段根 `testReport.md` 的票 09 段）
- [x] 语句功能注释与谓词一致，不再出现「判定在应用层完成」与谓词自行过滤并存的矛盾写法（无矩阵编号：注释与代码一致性核对，随 V79b 同批验证）（证据：V79b 守卫用例的语句文本判定覆盖谓词本身；语句头部功能注释已改写为「来源范围只按来源组织限定（S5-D25）」）
- [x] `git diff --check` 干净（无矩阵编号）（证据：命令 `git diff --check` 无输出，见阶段根 `testReport.md` 的票 09 段）

**口径：** 本票覆盖映射静态面（`Repository/Static`，V79b）与候选来源范围的行为面（`Domain/Repository`，V16b、V16c）；不动域层管理器、读模型契约、端点清单与分部文件清单（无新增 symbol 与文件）。
