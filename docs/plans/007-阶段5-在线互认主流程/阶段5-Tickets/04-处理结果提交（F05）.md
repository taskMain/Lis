# 04: 处理结果提交（F05）

**What to build:** 医院 HIS 提交整组采纳与不采纳结果：平台校验组内项目与匹配记录属于同一组、时间序合法、决定与不采纳原因成对且值域正确、科室与医生标识与名称成对 → 识别幂等（命中完全相同的既有事实时返回成功且不重复登记；相同业务键不同内容返回冲突不登记）→ 读取采纳项目的当前金额 → 整批原子保存并写入处理结果保存时间、按设计形成采纳次数与来源医院被互认次数的观察口径 → 登记处理结果已保存事件。任一校验或写入失败整批不保存、不登记。

**Blocked by:** 03

**Status:** developed-and-reviewed

**外部前置：** 四张表已由负责人建成；该组匹配记录由票 03 形成（测试以替身构造）。

- [x] 整组采纳提交：全部匹配项保存采纳决定、形成预计节省金额、形成采纳次数与来源医院被互认次数各一次、写入处理结果保存时间、登记事件（V23）（证据：`Stage5WritePathTests` 的整组采纳用例，见阶段根 `testReport.md` 的「批次 3」段；预计节省金额只写入互认处理结果）
- [x] 整组不采纳提交按设计保存不采纳原因与补充说明（V24）（证据：同文件的整组不采纳用例，断言未读取金额、未形成预计节省金额）
- [x] 组内项目分别作出不同决定时按各自决定保存（V25）
- [x] 项目数组缺少组内匹配项、重复提交同一匹配项、夹带其他匹配记录的匹配项时整次失败（V26、V27、V28）（证据：三条用例的拒绝文案分别指出缺少、重复与非法的互认匹配项标识）
- [x] 互认时间早于匹配记录生成时间、晚于请求接收时间时整次失败；等于两端边界时接受（V29、V30、V31）
- [x] 不采纳未提供原因、原因非法、选择其他原因但缺少补充说明、采纳时带有不采纳原因时整次失败（V32、V33、V34、V35）（**分层说明**：V32、V34、V35 在领域层取得证据；V33 的领域分支为防御性约束、经公开入口不可达（请求契约的枚举值域校验先拦截），本票只取得契约层拒绝证据，该条不随本条勾选，见阶段根 `testReport.md` 的「批次 3」段）
- [x] 处理结果值域非法时整次失败（V35b）（证据：请求校验面拒绝；该条的层级列含 `Contract`，契约面证据成立）
- [x] 互认科室或医生标识与名称不成对时整次失败；提交的组织、医院、院区与匹配记录接收三值不一致时整次失败（V36、V36b）（**分层说明**：V36b 在领域层取得证据；V36 的领域层成对判定已按设计补入管理器，但其为防御性约束、经公开入口不可达（请求契约的非空校验先拦截），本票只取得契约层拒绝证据，该条的层级列为 `Domain`，其领域面无行为证据，见阶段根 `testReport.md` 的「批次 3」段）
- [x] 幂等重试命中完全相同既有事实时返回成功且不重复登记；命中时绑定报告版本已失效不改变幂等结论（V37、V38）（证据：两条用例；V37 的前置包含金额已变化，断言未读取金额且处理结果保存时间不变）
- [x] 相同业务键但互认时间不同、任一项目决定不同、科室或医生标识不同、仅名称不同、项目数组顺序不同但内容一致时，按设计的冲突与幂等口径分别处理（V39、V40、V41、V42、V43）（证据：五条用例；冲突文案含「互认处理结果冲突」，不覆盖已保存决定）
- [x] 未命中幂等时绑定报告版本已失效时整次失败；采纳项目未配置当前金额、配置停用但绑定报告版本仍有效时按设计处理（V44、V45、V45b）（证据：三条用例；V44 断言未读取金额，证明版本校验先于金额读取）
- [x] 业务科室与人员的负向要求成立：不向权限系统补查名称、不因权限系统中不存在或已停用而拒绝、不校验医生当前所属科室与提交科室一致、不得以调用账号替代（V95）（证据：权限系统替身零调用断言与成对字段齐全的请求）
- [x] 新增写入口后，既有写入口契约方法集合等值断言与写侧分部文件逐文件等值断言同批同步（无矩阵编号：既有断言同步面；查询入口的同步见票 05）（证据：`Stage4EndpointTests` 的写入口方法集合与新增事务声明用例、`Stage4ConstraintTests` 的分部文件清单与数量断言与调用点扫描面、`Stage4SqlMapTests` 与 `Stage1SqlMapProbeTests` 与 `Stage5SqlMapTests` 的语句清单与列一致性守卫；未放宽任何既有断言）
- [x] 写路径上的真实入口断言「请求校验先于领域写入」（无矩阵编号：取证方式见 README 约定 13）（证据：同文件的请求校验用例，断言零领域读取与零写入）
- [x] `git diff --check` 干净（无矩阵编号）（证据：命令 `git diff --check` 无输出，见阶段根 `testReport.md` 的「批次 3」段）

**口径：** 本票覆盖契约、领域校验与写入层（`Contract/Domain`、`Domain`、`Domain/Repository`）；V95 在本票承接处理结果侧的负向面（引用结果侧由票 06 承接）；整批原子性与真实数据库面由票 08 取证。
