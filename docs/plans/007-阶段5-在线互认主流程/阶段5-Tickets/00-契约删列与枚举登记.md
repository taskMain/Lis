# 00: 契约删列与枚举登记

**What to build:** 让阶段 5 的契约层不再携带项目编码，并让两个枚举具备元数据登记：两个领域实体与两个对外 DTO 删去项目编码属性；三个系统参数的编码以常量声明在使用处；互认结果与不采纳原因两个枚举补齐成员中文说明与描述器特性并登记描述器注册表，**同批删除既有枚举元数据守卫用例排除清单中的对应两项**——该用例同时断言「排除清单与已登记枚举的交集为空」与「排除清单每一项都出现在对外契约上」，只登记不删排除项、只删排除项不登记，两者都会使既有用例失败。

**Blocked by:** None（可立即开始）

**Status:** developed-and-reviewed

**外部前置：** 三个系统参数的正式编码取值已确认，为 `own_hospital_match_switch`（本院报告匹配开关，取值为字符串 1 或 0，1 表示开启、0 表示关闭）、`own_hospital_exclude_hours`（本院报告排除时长，非负整数小时，允许零）、`citation_detail_valid_hours`（引用详情有效时长，正整数小时），已按小写下划线形态在使用处声明常量。三个参数已在平台系统参数系统配置、四张表已由负责人建成，属票 03、票 05 与票 08 的运行前置。

- [x] 两个领域实体与两个对外 DTO 均不再含项目编码属性，且与上游已删列的 UML 口径一致（无矩阵编号：属 S5-D14 的本阶段产物面，其 DDL 面由票 02 承接；证据：`Stage5ParameterAndEnumContractTests.Processing_result_and_reference_contracts_no_longer_carry_the_project_code`，见阶段根 `testReport.md` 的「批次 0」段）
- [ ] 三个系统参数以常量声明于使用处，按平台级编码读取、不传组织与医院与院区维度，且不引入参数解析 helper（V90）（本票已交付**常量声明面**：三个编码以 `private const string` 精确取值声明在各自的读取入口类内，未引入参数解析 helper、未新建共享常量类或只装常量的分部文件；**按级读取与读取时不传组织、医院、院区维度的行为面**由票 03 与票 05 承接，故本条不勾选）
- [x] 互认结果与不采纳原因两个枚举的每个成员具备中文说明与描述器特性，并已登记描述器注册表（V85）（证据：`Stage5ParameterAndEnumContractTests.Stage5_enums_declare_descriptor_attribute_and_chinese_member_descriptions` 与 `Stage5ParameterAndEnumContractTests.Stage5_enums_are_registered_in_the_descriptor_registry`，见阶段根 `testReport.md` 的「批次 0」段）
- [x] 既有枚举元数据守卫用例的排除清单同批删除对应两项，该用例的「排除清单与已登记枚举交集为空」与「排除清单每项出现在对外契约上」两侧断言同时通过（V85）（证据：`Stage2EnumMetadataQueryTests.Stage2_contract_enums_match_the_registry_whitelist` 与 `Stage2EnumContractTests` 的枚举取值与说明冻结用例；两侧断言均保留、判据未放宽，见阶段根 `testReport.md` 的「批次 0」段）
- [ ] 枚举文本在读模型上以计算属性暴露（V85 的读模型面，与票 01 共同承接）（本票承接其元数据与登记面，读模型计算属性面由票 01 承接，本条不勾选）
- [x] `git diff --check` 干净（无矩阵编号）（证据：命令 `git diff --check` 无输出，见阶段根 `testReport.md` 的「批次 0」段）

**口径：** 本票覆盖契约与静态面（矩阵层级 `Contract/Static`、`Static/Contract`），不产生运行期证据；V85 的枚举文本读模型面由票 01 承接，本票承接其元数据与登记面。
