# 02: 建表脚本与 SqlMap 映射语句

**What to build:** 让阶段 5 的四张表与四个映射在物理层就位：四张表按项目现行规范重建脚本（统一前缀、全量中文表列注释、显式索引、列序按既定规则、两个业务时间列精度不低于秒、处理结果表与引用事实表不再含项目编码列、四张表不含计数列且不新增报告使用关系载体）；四个映射在既有实体作用域下补齐写入与读取语句（语句标识与仓储方法名对应、作用域逐调用显式传入、不调用上下文设置方法、映射文件留在程序集根下一级目录），候选查询使用集合参数写法与两个左连接合并取值且无方言特征；既有语句清单断言与映射位置守卫同步通过。

**Blocked by:** 00

**Status:** developed-and-reviewed

**外部前置：** 四份建表脚本已交付，四张表已由负责人按脚本在 `DysoftHIS` 的 `public` schema 建成；三个系统参数已由负责人在平台系统参数系统配置，票 03 起的集成验证继续。

- [x] 四张表脚本具备统一前缀、全量中文表列注释、显式索引与既定列序；互认时间与实际引用时间精度不低于秒（V73）（静态面证据：`Stage5SqlMapTests` 的脚本用例；脚本为 `Repository/Scripts/mrec_recognition_match_record.sql`、`mrec_recognition_match_item.sql`、`mrec_recognition_processing_result.sql`、`mrec_recognition_reference.sql`；目标库结构与注释核对归票 08）
- [x] 未新增报告使用关系表、实体或事件；采纳与报告版本的对应关系只由互认匹配项反查派生（V73b）（静态面证据：脚本集合、领域实体定义与事件声明的扫描用例）
- [x] 处理结果表不含项目编码列；项目编码由匹配项标识反查取得（V74）（静态面证据：脚本用例按列清单断言；目标库核对归票 08）
- [x] 引用事实表不含项目编码列，与处理结果表同口径（V74b）（静态面证据同上）
- [x] 四条索引按设计建立：匹配记录表按接收三值与患者证件、匹配生成时间；匹配项表按所属匹配记录与标准项目编码；处理结果表与引用事实表各按互认匹配项标识唯一（V75）（静态面证据：索引名称、定义、唯一性与注释的逐条断言；目标库核对归票 08）
- [ ] 四个映射的作用域为各自实体名，物理表映射与既有约定一致（V76）（**部分完成**：作用域为各自实体名、语句注册在同一实体作用域下、不调用上下文设置方法、物理表全部指向 `mrec_` 前缀表，四者已取得静态证据；「语句标识与仓储方法名对应」与「逐调用显式传作用域」在本票没有观察对象——仓储方法与端口方法归票 03 至票 06，届时由既有 `Stage4ConstraintTests.Repository_call_sites_derive_statement_id_from_method_name` 用例按「调用方法名去 `Async`」推导并核对；本条不勾选）
- [x] 映射文件与仓储类同处程序集根下一级目录，未新建分组子目录（无矩阵编号：改动范围约束，由守卫用例 `Stage1SqlMapProbeTests.Sql_map_resources_stay_directly_under_one_folder` 覆盖，设计见 `Server/design.md` 的映射与作用域段）（证据：该守卫用例通过，映射文件位置未变）
- [x] 既有语句清单断言按补齐结果同步，未放宽为宽松形式（V77）（证据：`Stage1SqlMapProbeTests` 的四条作用域语句标识清单按补齐结果升级为逐条完整键断言、占位语句断言改为负向断言；`Stage4SqlMapTests` 的查询语句清单仍为逐项等值断言；`SourceGuardReuseTests` 的受治理清单精确等值，见阶段根 `testReport.md` 的「批次 1」段）
- [x] 候选查询以集合参数写法按一组标准项目编码过滤（V78）（静态面证据：参数名直接跟在 `in` 之后、不带括号；仓储侧传只读集合的形态归票 03）
- [x] 候选查询以两个左连接合并取值取得统一排序键，且查询语句无数据库方言特征（V79、V80）（证据：按报告类型分别以左连接取检验检测完成时间或检查实际检查时间并合并为统一排序键，排序为匹配基准时间倒序、报告时间倒序、报告版本标识升序；`Stage5SqlMapTests` 的跨文件列一致性守卫与枚举取值守卫逐列核对语句引用的物理列与数值字面量，见阶段根 `testReport.md` 的「批次 1」段）
- [x] 四张表脚本已交付且索引与列注释声明完整，并已注明建表暂停点（无矩阵编号：脚本交付暂停点检查；表结构、列注释与索引的目标库核对由票 08 承接，未建成时相关运行项记 `Blocked`）（证据：四份脚本在 `Repository/Scripts/`，旧的无前缀脚本已删除；暂停点见本票「外部前置」行与 `Server/impl.md`）
- [x] `git diff --check` 干净（无矩阵编号）（证据：命令 `git diff --check` 无输出，见阶段根 `testReport.md` 的「批次 1」段）

**口径：** 本票覆盖 DDL 静态面与 SqlMap 映射面（`Repository/Static`、`Static`、`Static/DB`、`Static/Domain`）；V73、V75 与 V76 的「真实数据库」列结论由票 08 在目标库上取证，本票只交付脚本与静态面。
