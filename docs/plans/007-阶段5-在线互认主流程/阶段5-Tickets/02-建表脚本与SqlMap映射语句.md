# 02: 建表脚本与 SqlMap 映射语句

**What to build:** 让阶段 5 的四张表与四个映射在物理层就位：四张表按项目现行规范重建脚本（统一前缀、全量中文表列注释、显式索引、列序按既定规则、两个业务时间列精度不低于秒、处理结果表与引用事实表不再含项目编码列、四张表不含计数列且不新增报告使用关系载体）；四个映射在既有实体作用域下补齐写入与读取语句（语句标识与仓储方法名对应、作用域逐调用显式传入、不调用上下文设置方法、映射文件留在程序集根下一级目录），候选查询使用集合参数写法与两个左连接合并取值且无方言特征；既有语句清单断言与映射位置守卫同步通过。

**Blocked by:** 00

**Status:** ready-for-agent

**外部前置：** 无（脚本交付后暂停；四张表由负责人建成后再由票 03 起的集成项继续）

- [ ] 四张表脚本具备统一前缀、全量中文表列注释、显式索引与既定列序；互认时间与实际引用时间精度不低于秒（V73）
- [ ] 未新增报告使用关系表、实体或事件；采纳与报告版本的对应关系只由互认匹配项反查派生（V73b）
- [ ] 处理结果表不含项目编码列；项目编码由匹配项标识反查取得（V74）
- [ ] 引用事实表不含项目编码列，与处理结果表同口径（V74b）
- [ ] 四条索引按设计建立：匹配记录表按接收三值与患者证件、匹配生成时间；匹配项表按所属匹配记录与标准项目编码；处理结果表与引用事实表各按互认匹配项标识唯一（V75）
- [ ] 四个映射的作用域为各自实体名，物理表映射与既有约定一致（V76）
- [ ] 映射文件与仓储类同处程序集根下一级目录，未新建分组子目录（无矩阵编号：改动范围约束，由守卫用例 `Stage1SqlMapProbeTests.Sql_map_resources_stay_directly_under_one_folder` 覆盖，设计见 `Server/design.md` 的映射与作用域段）
- [ ] 既有语句清单断言按补齐结果同步，未放宽为宽松形式（V77）
- [ ] 候选查询以集合参数写法按一组标准项目编码过滤（V78）
- [ ] 候选查询以两个左连接合并取值取得统一排序键，且查询语句无数据库方言特征（V79、V80）
- [ ] 四张表脚本已交付且索引与列注释声明完整，并已注明建表暂停点（无矩阵编号：脚本交付暂停点检查；表结构、列注释与索引的目标库核对由票 08 承接，未建成时相关运行项记 `Blocked`）
- [ ] `git diff --check` 干净（无矩阵编号）

**口径：** 本票覆盖 DDL 静态面与 SqlMap 映射面（`Repository/Static`、`Static`、`Static/DB`、`Static/Domain`）；V73、V75 与 V76 的「真实数据库」列结论由票 08 在目标库上取证，本票只交付脚本与静态面。
