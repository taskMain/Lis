# 阶段 5 实施

设计见 [design.md](design.md)；后端批次与矩阵编号见 [Server/impl.md](Server/impl.md)；结果见 [testReport.md](testReport.md)。当前进度、环境与数据现状与下一会话的起点见本文「待办与交接」段。

## 批次与依赖

| 批次 | 范围 | 主责 | 文件所有权 | 前置条件 | 矩阵编号 | 当前状态 |
|---|---|---|---|---|---|---|
| 0 | 契约准备：S5-D14 的两个领域实体与两个对外 DTO 删列、参数键名登记、两个枚举补中文说明与描述器特性并登记**并同步既有枚举元数据守卫用例的排除清单**、四个请求类型与三个事件的声明、读模型重建 | 后端主责 | Contracts、Domain.Share、Tests、文档 | 设计确认 | V81-V85、V90 | 票 00、票 01 交付并独立审查完成；V90 的读取维度面属批次 2、4；V85 的读模型计算属性面在本阶段无承载读模型 |
| 1 | 四张表建表脚本交付与语句补齐：四张表按现行规范重建脚本（统一前缀与索引，含按设计删除项目编码列，UML 侧已完成）、四个映射在既有实体作用域下补齐语句、既有语句清单断言同步 | 后端主责 | Repository、Tests | 批次 0 | V73-V80、V73b、V74b | 票 02 交付并独立审查完成；四张表已建成，V73、V75、V76 的目标库面归票 08 |
| 2 | 匹配查询实现：管理器分步、候选查询、参数读取与严格失败语义、匹配记录与匹配项写入、事件登记 | 后端主责 | Domain、Repository、Application | 批次 1 脚本交付且负责人建表完成 | V1-V22（含 V20b、V22b）、V89、V91-V94 | 票 03 交付并独立审查完成；V14-V16 的行为面、V91 的领域层分支与 V92 的宿主面归票 08 |
| 3 | 处理结果实现：幂等识别、整批校验、金额读取、原子保存、处理结果保存时间写入、事件登记 | 后端主责 | Domain、Repository、Application | 批次 2 | V23-V45（含 V35b、V36b、V45b）、V95 | 票 04 交付并独立审查完成；V33 与 V36 的领域面为不可达防御分支、整批原子性与真实数据库面归票 08 |
| 4 | 引用详情实现：参数读取、有效期计算、版本有效性逐项校验、部分可用与全不可用语义、读模型映射 | 后端主责 | Application、Domain、Repository | 批次 3 | V46-V58（含 V58b） | 票 05 交付并独立审查完成；V88 的静态核对面与真实数据库面分别归票 07 与票 08 |
| 5 | 引用结果实现：逐项反查、归属一致性、时间序、幂等与冲突、唯一索引兜底、事件登记 | 后端主责 | Domain、Repository、Application | 批次 4 | V59-V72（含 V70b、V72b） | 票 06 交付并独立审查完成；整批原子性与真实数据库面归票 08，并发撞键按设计不适用登记 |
| 6 | 阶段收尾：V86-V88 的跨接口静态核对、适用测试与构建与静态检查与差异检查、阶段报告更新 | 后端主责 | 全部 | 批次 0-5 | V86-V88 | 票 07 交付并独立审查完成；真实数据库与宿主面归票 08 |
| 7 | 真实链路取证：四个接口在真实数据库与真实宿主 token 上的成功链路与零写入面、整批零提交的受控失败注入、参数严格失败语义与参数取值面、两条写链的拒绝幂等与冲突面、候选选优与联合报告、四表与四索引核对、只在真实 Provider 上成立的缺陷修复与守卫 | 后端主责 | Repository、Domain、Application、Tests、文档 | 批次 0-6 | V1-V80、V82-V84、V88-V95 的运行时面 | 票 08 取证完成并按条目构造口径定案：四个接口的成功与拒绝面、V1 的受控注入、参数三类前置与取值面、处理结果与引用结果两条链、V7、V15、V28、V36b、V57、V72 等 `Passed`；其余按业务不可达、已由静态判据冻结、本轮未构造三类记 `NotRun`，V23／V59 的整批原子性面记 `N/A`（回滚属框架行为）。逐条理由见 `testReport.md` 的票 08 段 |
| 8 | 候选查询来源范围修正：候选语句只按来源组织限定（删除来源医院与来源院区谓词）、相关守卫与真实链路核验 | 后端主责 | Repository、Tests、文档 | 设计确认（S5-D25） | V16b、V16c、V79b | 票 09 取证完成：V79b、V16b `Passed`，V16c 真实链路面 `Blocked`（测试身份每医院单院区，真实链路面构造不了；语义由读模型无院区字段与 V17-V20 既有用例承接）；见 `testReport.md` 的票 09 段 |

**矩阵编号的批次归属**：V1-V22（含 V20b、V22b）与 V89 与 V91-V94 归批次 2（匹配查询）；V23-V45（含 V35b、V36b、V45b）与 V95 归批次 3；V46-V58（含 V58b）归批次 4；V59-V72（含 V70b、V72b）归批次 5；V73-V80（含 V73b、V74b）归批次 1；**V81-V85 与 V90 归批次 0**；V86-V88 归批次 6；V16b、V16c、V79b 归批次 8（候选来源范围修正，V16b 与 V16c 为就近增补的行为条目，V79b 为就近增补的静态条目）。各编号只归一个批次，**不重复归属**。

**S5-D14 的 UML 侧已同步**（处理结果实体与引用事实实体已删列）；批次 1 只需把两份建表脚本的项目编码列按设计删除后再交付，否则会建成与设计冲突的表。

**端点与分部文件清单的同步**：既有断言分三种形态——方法集合等值断言三处（写入口契约、查询入口契约与查询契约固定清单）、分部文件逐文件等值断言三处（管理器、写侧端口与写侧实现各按文件名清单逐项相等）、分部文件数量断言两处（查询侧端口与实现各按声明数量相等）。新增三个写入口与一个查询入口、并按既有惯例新增分部文件时，这三类清单须在同一批次内按新增结果同步；各批次不得只加端点而留下清单断言失败。

## 执行记录

- [ ] 完成适用设计及确认。
- [ ] 建立目标失败证据或等价基线，完成适用影响分析。
- [ ] 完成当前批次实现与直接影响验证。
- [ ] 完成适用最终检查并更新根测试报告。

## 待办与交接

**当前进度。** 批次 0 至批次 6 的七张票已交付并通过独立审查；批次 7（票 08「真实宿主链路验收与阶段报告」）取证完成，票面 `Status` 为 `evidence-collected`，四个接口的成功与附带拒绝面、整批零提交的受控失败注入（V1）、参数严格失败语义与参数取值面、处理结果与引用结果两条链的拒绝幂等与冲突面、候选选优与时间边界、四张表与四条索引核对均已取得真实证据，逐条状态与覆盖边界见 `testReport.md` 的票 08 段。构建 0 错误，全量测试 631 条通过、0 跳过，`git diff --check` 无输出。**V48 与 V64 两处证据边界按负责人已接受的现状记 `NotRun` 并登记重审条件，不阻塞收口。阶段 5 已由负责人于 2026-09-20 裁定收口；交付物当时尚未提交（该状态已被 2026-09-22 票 09 批次 8 交付的提交取代）。**

**本轮发现并修复的缺陷（三处，均先取得失败证据再修复）。** 报告主体未关联平台患者导致匹配查询恒空；查询侧来源三列与检查所见、检查结论未按投影类型属性名取别名导致匹配响应组装整次失败、引用详情缺少两项；集合参数语句在空集合上落入语句执行导致真实 Provider 报错。修复位置与回归守卫见 `testReport.md` 的票 08 段。新增守卫用例 5 条（含一条理论用例的两行数据）。

**环境与数据现状（已核实）。** 后端端口 `15014` 空闲；宿主 `183.224.180.166:35000` 的登录页返回 HTTP 200；外部服务 `183.224.180.166:35001` 可达；系统参数、组织、用户与 PDF 四项代理映射均在 `server/Dy.MedicalRecognition/appsettings.Development.json` 内（该文件不纳入版本控制，其他宿主部署需确认就位）；四张表已建成，本轮取证后匹配记录 4 行、匹配项 4 行、处理结果 1 行、引用事实 1 行。三个系统参数已配置（本院报告匹配开关 `1`、本院报告排除时长 `4` 小时、引用详情有效时长 `24` 小时）。既有业务数据为报告 30 份、报告版本 33 条、平台患者 21 个、标准项目 6 个、互认配置 3 条、金额 11 条；其中组织 `01` 下可用互认项目编码为 `123` 与 `S1-D29-ITEM-20260915-001`，编码 `222` 的配置已停用。上游同步均已完成（S5-D4 的参数口径、S5-D14 的两个实体删列、S5-D6 的子命令合并），报告采集与生命周期能力的前置已就绪。

**票 08 的取证范围与口径（已执行）。**

- 逐条取证验证矩阵中「真实数据库」列记「是」的 71 条；其中 V1、V23、V59 三条承担三个写用例的整批零提交失败注入（失败时零写入、不登记事件），参数严格失败语义在真实参数服务上复核，未认证请求的对外响应在宿主上复核，四个接口成功后的回读分别经匹配查询与引用详情完成，四张表与四条索引在目标库上与脚本核对。
- 数据只经正常业务路径形成：报告数据用阶段 4 的两个提交接口构造，本阶段的匹配、处理与引用事实用本阶段四个接口自身构造；不直接写库、不使用脚本注入；dbx 只做只读核对。
- 测试身份只用测试环境规范登记的两个账号（组织 `01`，医院分别为 `0101` 与 `CSYY2`），跨医院归属用例用两者的医院范围构造，不索取其他账号。
- 接口测试按负责人指定使用 Playwright 对应工具（`playwright cli`），宿主页面交互按浏览器调试与控制技能执行；实际会话、请求与响应原文记录在阶段根 `testReport.md` 的票 08 段。
- 结果、证据出处、未执行项与残余风险写入阶段根 `testReport.md`；无实际证据不写通过性状态；票 08 票面勾选与实施文档的批次状态由编排方同步。

**记录在案、无需等待确认即可开工的事项**（详见阶段根 `testReport.md` 的残余风险表）：V33 与 V36 的领域面为不可达防御分支记 `NotRun`；票 05 的断言级失败证据面记 `NotRun`；来源组织名称不在上游接口模型；两个参数读模型已按 S5-D5 删除而上游 UML 仍声明；无时区业务时间列的时钟口径；引用详情检验结果的行级取舍；引用详情链查询侧映射语句集合的只读性未纳入自动判据；客户端路径判据已把阶段 1 至阶段 4 的既有异常处理位置显式登记；工作区存在早于本轮的额外 worktree。

**票 08 记录的条目构造口径**：矩阵条目按「业务可达且静态判据盖不住则取证」「正常业务不可达记 `NotRun`」「业务可达但已由静态判据冻结、运行期无独立增量记 `NotRun`」三类处置，不为不可达前置硬造数据；逐条理由见 `testReport.md` 的票 08 段。其余按口径记 `NotRun` 的条目与两处证据边界（处理结果与引用结果的整批原子性、引用详情有效期边界与 V64）同样登记在该段的残余风险表。

**工作区与授权边界。** 阶段 5 的全部改动尚未提交（当前分支领先远端一个既有文档提交）；本轮未创建提交、分支或 worktree。阶段 5 已由负责人于 2026-09-20 裁定收口；需要提交时须取得负责人明确授权，并按 GitNexus 工作流先执行变更检查。本轮取证辅助脚本与请求、响应原文留存在 `var/st5/`，该目录是否纳入版本控制由负责人裁定。（本节所述「尚未提交」状态截至票 08 轮次为真；2026-09-22 票 09 批次 8 交付已按同一授权流程完成提交，提交号不登记、以仓库历史为准。）

**批次 8（票 09）交付记录（2026-09-22 追加）。** 应负责人重启阶段 5 处理候选查询来源范围问题：按决策 S5-D25，候选语句两个分支只保留来源组织等值谓词，删除来源医院与来源院区谓词；仓储签名不动。先红后绿新增静态守卫用例 `Candidate_query_limits_source_scope_to_organization_only`（V79b），真实链路在同一组织两个医院（`0101`/`CSYY2`）身份下双向取证通过（V16b `Passed`）；V16c 真实链路面因测试身份每医院单院区记 `Blocked`，语义由读模型无院区字段与 V17-V20 既有用例承接。全量测试 768 条通过、构建 0 错误。逐条状态与证据原文见 `testReport.md` 的票 09 段；票面、矩阵与批次状态均已同步。本批次交付已由负责人于 2026-09-22 授权提交入库（提交号不登记，以仓库历史为准），提交前已完成 GitNexus 变更检查（low）。
