# 阶段 5 实施

设计见 [design.md](design.md)；后端批次与矩阵编号见 [Server/impl.md](Server/impl.md)；结果见 [testReport.md](testReport.md)。

## 批次与依赖

| 批次 | 范围 | 主责 | 文件所有权 | 前置条件 | 矩阵编号 | 当前状态 |
|---|---|---|---|---|---|---|
| 0 | 契约准备：S5-D14 的两个领域实体与两个对外 DTO 删列、参数键名登记、两个枚举补中文说明与描述器特性并登记**并同步既有枚举元数据守卫用例的排除清单**、四个请求类型与三个事件的声明、读模型重建 | 后端主责 | Contracts、Domain.Share、Tests、文档 | 设计确认 | V81-V85、V90 | 未开始 |
| 1 | 四张表建表脚本交付与语句补齐：四张表按现行规范重建脚本（统一前缀与索引，含按设计删除项目编码列，UML 侧已完成）、四个映射在既有实体作用域下补齐语句、既有语句清单断言同步 | 后端主责 | Repository、Tests | 批次 0 | V73-V80、V73b、V74b | 未开始 |
| 2 | 匹配查询实现：管理器分步、候选查询、参数读取与严格失败语义、匹配记录与匹配项写入、事件登记 | 后端主责 | Domain、Repository、Application | 批次 1 脚本交付且负责人建表完成 | V1-V22（含 V20b、V22b）、V89、V91-V94 | 未开始 |
| 3 | 处理结果实现：幂等识别、整批校验、金额读取、原子保存、处理结果保存时间写入、事件登记 | 后端主责 | Domain、Repository、Application | 批次 2 | V23-V45（含 V35b、V36b、V45b）、V95 | 未开始 |
| 4 | 引用详情实现：参数读取、有效期计算、版本有效性逐项校验、部分可用与全不可用语义、读模型映射 | 后端主责 | Application、Domain、Repository | 批次 3 | V46-V58（含 V58b） | 未开始 |
| 5 | 引用结果实现：逐项反查、归属一致性、时间序、幂等与冲突、唯一索引兜底、事件登记 | 后端主责 | Domain、Repository、Application | 批次 4 | V59-V72（含 V70b、V72b） | 未开始 |
| 6 | 阶段收尾：V86-V88 的跨接口静态核对、适用测试与构建与静态检查与差异检查、阶段报告更新 | 后端主责 | 全部 | 批次 0-5 | V86-V88 | 未开始 |

**矩阵编号的批次归属**：V1-V22（含 V20b、V22b）与 V89 与 V91-V94 归批次 2（匹配查询）；V23-V45（含 V35b、V36b、V45b）与 V95 归批次 3；V46-V58（含 V58b）归批次 4；V59-V72（含 V70b、V72b）归批次 5；V73-V80（含 V73b、V74b）归批次 1；**V81-V85 与 V90 归批次 0**；V86-V88 归批次 6。各编号只归一个批次，**不重复归属**。

**S5-D14 的 UML 侧已同步**（处理结果实体与引用事实实体已删列）；批次 1 只需把两份建表脚本的项目编码列按设计删除后再交付，否则会建成与设计冲突的表。

**端点与分部文件清单的同步**：既有断言分三种形态——方法集合等值断言三处（写入口契约、查询入口契约与查询契约固定清单）、分部文件逐文件等值断言三处（管理器三文件、写侧端口五文件、写侧实现五文件）、分部文件数量断言两处（查询侧端口与实现各二）。新增三个写入口与一个查询入口、并按既有惯例新增分部文件时，这三类清单须在同一批次内按新增结果同步；各批次不得只加端点而留下清单断言失败。

## 执行记录

- [ ] 完成适用设计及确认。
- [ ] 建立目标失败证据或等价基线，完成适用影响分析。
- [ ] 完成当前批次实现与直接影响验证。
- [ ] 完成适用最终检查并更新根测试报告。

## 待办与交接

**上游同步已完成。** S5-D4 的参数口径（改动范围为 SRS F04 业务规则 8、9 与 F06 业务规则 9、SRS 设计约束 17、`CONTEXT.md`「本院报告匹配开关」段与「引用详情」段，以及两份 UML 的参数表述）已落盘；S5-D14 的处理结果实体与引用事实实体已在 `docs/uml/1-MedicalRecognitionReport.wsd` 删列；S5-D6 的匹配查询两个创建子命令及其事件的合并方式已在 UML 登记。本阶段的实施前置只剩四张表建成与三个系统参数配置；本阶段自身产物的删列（两个领域实体、两个对外 DTO 与两份建表脚本）随批次 0 与批次 1 交付。

**建表由项目负责人执行。** 批次 1 交付四张表的最终建表脚本后暂停，收到负责人建表完成确认后继续依赖新表的集成验证；未确认前相关条目记 `Blocked`。

**三个系统参数由项目负责人配置。** 参数未在平台系统参数系统配置前，依赖参数的运行项记 `Blocked`。参数服务的代理映射位于宿主开发环境配置 `server/Dy.MedicalRecognition/appsettings.Development.json`：该文件被 `server/.gitignore` 覆盖、未纳入版本控制，当前工作区已含该代理映射与 PDF 存储根目录；部署到其他宿主时须确认该配置包含本服务。

**报告采集能力的前置已就绪。** 该能力已交付：其批次 0–5 完成、10 张报告表已按三批建成、目标库与 schema 已确认；匹配查询与引用详情所依赖的报告侧读路径与两条索引（`ix_mrec_medical_recognition_report_scope`、`ux_mrec_medical_recognition_report_business_key`）包含在其中，报告侧十个实体映射的语句与作用域（仓库映射现共 20 份：聚合目录 19 份实体映射与 1 份独立查询映射，均自阶段 1 起纳入版本控制）、报告主体的报告时间列与 PDF 下载动作均在版本控制内。前置成立。该能力后续若调整报告侧契约，需回改本阶段相应已确认内容。本阶段不实现也不修改该能力范围内任何对象。总体计划设计 §4.7 与实施方案 §8 规定的推进关系已成立，总体计划实施进度表本阶段一行的矩阵规模与前置表述已按其完成状态回写。
