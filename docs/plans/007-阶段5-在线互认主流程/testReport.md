# 阶段 5 测试报告

**当前状态：未执行。** 本阶段处于设计阶段，尚未进入实施，因此没有任何条目具备实际执行证据。全部条目按定义记 `NotRun`，依赖负责人建表或参数配置的条目在相应前置满足前记 `Blocked`。

测试前矩阵见 [后端设计](Server/design.md) 的验证矩阵章节（V1-V95 主编号，另含 V20b、V22b、V35b、V36b、V45b、V58b、V70b、V72b、V73b、V74b 十个子编号，共 105 行）；批次与矩阵编号的对应见 [实施](impl.md) 与 [后端实施](Server/impl.md)。

## 执行边界

- 报告状态：后端矩阵 105 条（V1-V95 主编号，另含 V20b、V22b、V35b、V36b、V45b、V58b、V70b、V72b、V73b、V74b 十个子编号，共 105 行）全部未执行。
- 前置条件：**参数口径的上游同步已完成**（S5-D4 的改动已落盘）；未满足的前置为——四张表尚未建成、三个系统参数尚未在平台系统参数系统配置、S5-D14 与 S5-D6 的上游同步尚未执行。报告侧前置已就绪：报告采集与生命周期能力的批次 0–5 已完成，10 张报告表已按三批建成，目标库与 schema 已确认。
- 数据形成口径：验收数据只通过平台接口或页面形成；报告数据由阶段 4 的两个提交接口构造，本阶段的匹配、处理与引用事实由本阶段四个接口自身构造；不直接写库、不使用脚本注入数据。
- 既有断言的同步：四个映射作用域已是各自实体名，既有断言已按原严格口径断言实体作用域键存在、旧聚合作用域键不存在；本阶段补齐语句后，同一用例的语句标识清单需按补齐结果同步。两个枚举登记描述器注册表后，既有枚举元数据守卫用例的排除清单需同批删除对应两项。此外新增端点与分部文件时，既有端点方法集合断言与分部文件清单断言须同批同步。上述分别属实施批次 0 与批次 1 的交付内容，尚未执行。
- **事务与事件取证口径**：三个写用例均为多条独立语句且要求全成全败，其整批零提交属回滚面，需以真实数据库、真实入口与失败注入取证（V1、V23、V59 三个主条目）。其余失败路径条目按响应与调用记录取证，不重复取证整批原子性。**事件可见性子面**（事件对外投递与顺序）属框架生命周期，本阶段无专用观察点，该子面记 `N/A` 并说明框架边界；该记录只作用于可见性子面，不豁免整批原子性的取证。事件**登记**面由框架可读的事件集合在测试侧观察。
- 不测试范围：不做并发压测；文件补偿类用例不属本阶段。
- 不构造的前置：不设置需要空组织编码、空医院编码或空院区编码账号的条目；代码中的防御性校验保留。

## 结果

尚无任何条目具备实际执行证据，不逐条列出。

## 问题与残余风险

| 风险 | 说明 | 重审条件 |
|---|---|---|
| 报告侧契约变化导致返工 | 本阶段以报告采集与生命周期能力的设计契约为上游依据；该能力后续若调整报告侧契约，本阶段需回改相应已确认内容 | 报告采集与生命周期能力收口后核对报告侧契约差异 |
| 参数严格失败语义无本项目内先例 | 本项目 `server/` 下当前没有任何系统参数调用代码，严格失败语义的实现与取证无既有先例 | 实施阶段以取值缺失、取值非法与参数服务异常三类前置分别取证 |
| 参数严格失败语义的部署影响 | 三个参数不设默认值，任一参数未配置将使匹配查询或引用详情整体不可用；参数与代理映射均需在部署环境就位 | 部署清单确认三个参数已配置、代理映射已就位后消解 |
| **脏归属落库** | 可信身份的组织、医院、院区不再向组织服务核对存在、启用与父子归属；令牌中已停用或已撤销的归属仍会落库为接收三值。该口径无上游条款支持，属技术理由 | 出现可复现的归属不一致数据，或组织服务提供轻量校验能力时 |
| 参数服务代理映射不可见 | 参数服务的代理映射位于未纳入版本控制的开发环境配置，本工作树不可见 | 实施前在目标宿主确认该配置包含本服务 |
| 阶段 5 与报告采集能力的推进关系 | 总体计划设计 §4.7 与实施方案 §8 规定前者完成后推进；该能力已交付，总体计划实施进度表本阶段一行的矩阵规模与前置表述已按其完成状态回写 | 已消解；总体计划若再次回写导致两处表述分歧时复核 |
| 上游要求「提醒统计形成」的计数口径待归一 | 提醒事实即互认匹配项记录，该口径由 V93 在本阶段承接；**提醒次数的读取与报表口径归阶段 6 统计设计** | 阶段 6 统计设计时复核计数口径与 V93 一致 |

## 结论

本阶段尚未进入实施，无可交付结论。记录存在不代表负责人已批准收口或验收。
