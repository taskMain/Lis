# 阶段 5 阶段文档审查结果（glm）

- 审查时间：2026-09-18 23:41 起（GMT+8）
- 审查对象（实际读取，全文）：
  - `docs/plans/007-阶段5-在线互认主流程/design.md`（94 行）
  - `docs/plans/007-阶段5-在线互认主流程/impl.md`（36 行）
  - `docs/plans/007-阶段5-在线互认主流程/testReport.md`（35 行）
  - `docs/plans/007-阶段5-在线互认主流程/Server/design.md`（258 行）
  - `docs/plans/007-阶段5-在线互认主流程/Server/impl.md`（17 行）
  - `docs/plans/007-阶段5-在线互认主流程/阶段5-规格.md`（401 行）
  - `docs/plans/007-阶段5-在线互认主流程/阶段5-审查相关/阶段5-已整改条目清单.md`（复核输入，不作为阶段依据）
- 核对依据（实际读取）：
  - `docs/需求规约SRS.md`：F04、F05、F06、F07 全文（含前置条件、后置条件、主事件流、备选流、业务规则、关键属性），「医院接入接口」表及其共同边界 1–6，术语段「引用详情有效时长」，设计约束 13、17、21、22
  - `CONTEXT.md`：「组织、医院和院区」「医院 HIS 互认接入边界」「批量互认匹配查询」「互认匹配记录」「最近一次互认匹配」「本院报告匹配开关」「互认匹配返回内容」「互认处理结果」「引用详情」「引用结果」「互认次数与引用次数」「提醒次数」「项目级互认处理结果」「互认匹配记录与匹配项标识」「互认处理结果不可更改」「医院业务科室及人员标识接入」
  - `docs/uml/1-MedicalRecognitionReport.wsd`（互认四命令、五事件、四实体段），`docs/uml/3-HospitalIntegrationApiQuery.wsd`（全文，十个读模型）
  - `.agents/instructions/`：`agent-conduct.md`、`backend-architecture.md`、`backend-command-query-event.md`、`dy-framework-workunit.md`、`testing-baseline.md`、`testing.md`、`planning-and-evidence.md`
  - `docs/plans/001-总体计划/design.md` §4.7、`docs/plans/001-总体计划/impl.md` §8 与进度表
  - `docs/plans/004-阶段2-标准项目互认配置/Server/design.md`、`docs/plans/005-阶段3-互认项目金额维护/Server/design.md`（实体名作用域先例）
  - 主工作区（只读）：`E:\MedSync\Dy.MedicalRecognition\docs\plans\006-阶段4-报告采集与生命周期\design.md、impl.md、testReport.md`；`E:\MedSync\Dy.MedicalRecognition\server\Dy.MedicalRecognition.Repository\Scripts\mrec_medical_recognition_report.sql`、`recognition_processing_result.sql`、`recognition_reference.sql`
  - 本工作树代码事实：`server/` 下应用服务源文件、Domain.Share 枚举、Contracts 读模型、Repository SqlMap、测试目录结构
- 未读取到的材料：
  - `.agents/instructions/pagination.md` 在本工作树不存在（主工作区存在、未纳入版本控制）；本轮核对未依赖其内容
  - 宿主开发环境配置（参数服务代理映射）：两工作树均不可见，只能按文档口径核对
  - 任何运行时行为证据（审查为只读文档审查，未执行测试）
- 写入前确认：已清空该文档全部历史内容（是）
- 已整改条目复核：见下节

## 已整改条目复核

第一部分 36 条按清单建议抽样 04、07、12、25、27、34、36，第二轮 15 条抽样 P-04、P-06、P-11、P-15，均回到被改文件与段落实际核对；第三部分 16 条逐条复核。

| 编号 | 结论 | 核对说明 |
|---|---|---|
| R-01 | 已消失 | 规格决策 15 现为「三个参数均按平台级编码读取，不传组织、医院、院区维度」，与 `design.md` 参数段、`Server/design.md` 系统参数集成节、V90 三处一致 |
| R-02 | 部分生效 | 清单与逐行取值已完全一致（「是」15/15、「否」40/40、其余 46 行 N/A，脚本比对无差异项）；但档位「适用情形」描述与部分成员语义不符、同型条目跨段取值不一致，见问题 P1 |
| R-03 | 已消失 | 四处门禁句均为「S5-D4 已完成；S5-D14 与 S5-D6 未完成前不进入实施」口径：`design.md` 验收条件、`design.md` 参数段、`impl.md` 待办、`testReport.md` 执行边界；扫描无残留旧表述 |
| R-04 | 已消失 | 规格决策 68 现为「本项目首例、以映射语句文本静态取证」，与 `Server/design.md` 映射与作用域节同口径；决策 14 已无「同组织项目惯例」表述。该「首例」断言经代码核实成立（20 个 SqlMap 文件无任何 `in` 集合参数与 `foreach` 用法） |
| R-05 | 已消失 | V74b 存在且紧随 V74，S5-D14 待验证面为「V74 与 V74b」，两份 impl 批次 1 均含 V74b |
| R-06 | 已消失 | V82、V83 为读模型字段清单，V95 为业务科室与人员的负向要求（不补查名称、不停用拒绝、不校验科室一致），三者主题不同、无包含关系 |
| R-07 | 已消失 | V35b、V58b、V72b 均存在且紧随主编号；上游出处真实（分别对应 SRS F05 业务规则 1、F06 业务规则 14、F07 业务规则 11，已打开原文核对）；与 V33、V56、V72 主题不同；分段说明与两份 impl 批次 3/4/5 均已同步 |
| R-08 | 已消失 | V92 前置改为「不带认证凭据的请求」，正常业务可构造；规格测试替身段改为「仅构造令牌三层齐全一种身份」 |
| R-09 | 已消失 | V23 表行含「并形成采纳次数与来源医院被互认次数各一次」；V88 表行含「不形成引用结果、不形成引用次数、不形成新的互认处理」 |
| R-10 | 已消失 | V36b 在批次 3 单元格、V70b 在批次 5 单元格，两份 impl 与归属段一致 |
| R-11 | 已消失 | 规格 Further Notes「上游待改项」为客观现状陈述（参数口径已完成同步、冗余列同步属批次 1 硬前置、术语待落），无讨论痕迹 |
| R-12 | 已消失 | `testReport.md` 开头与执行边界均为「101 行（V1–V95 主编号 + 六个字母后缀条目）」，与矩阵实际行数一致 |
| R-13 | 已消失 | S5-D15 依据指向 SRS F06 业务规则 10 与 F07 主事件流 2、4 及 `CONTEXT.md`「引用结果」段，逐字核对方向一致（有效期起点取平台保存时间、引用时间下界取已保存互认时间） |
| R-14 | 已消失 | `Server/design.md` 复用行含「该解析只覆盖组织与医院两层；院区层取可信身份为本阶段新增口径，不经该解析器」 |
| R-15 | 已消失 | 分段说明为编号区段式并含「区段名只表编号范围」限定；区段与表内编号经脚本核对一一对应（V1–V22、V23–V45 含 V35b/V36b、V46–V58 含 V58b、V59–V72 含 V70b/V72b、V73–V80 含 V74b、V81、V82–V95） |
| R-16 | 已消失 | 风险表末条为「该口径由 V93 承接；提醒次数的读取与报表口径归阶段 6 统计设计」，与 V93 表行一致 |
| 抽样：一轮 04 | 部分生效 | 整列 N/A 已改为三分法；残余见 P1（清单一致但档位描述与部分取值仍不符） |
| 抽样：一轮 07 | 已消失 | V74b 已建（同 R-05） |
| 抽样：一轮 12 | 已消失 | 两份 impl 批次与矩阵双向覆盖 101 编号、无重复归属（脚本区间展开核对） |
| 抽样：一轮 25 | 已消失 | V37 表行含「处理结果保存时间不变」断言 |
| 抽样：一轮 27 | 已消失 | V36b 表行存在且承接 F05 主事件流 2 的归属校验 |
| 抽样：一轮 34 | 已消失 | V3、V5、V6、V26–V28 表行均含「指出相关标准项目编码/互认匹配项标识」粒度断言 |
| 抽样：一轮 36 | 已消失 | V91、V92 表行存在 |
| 抽样：二轮 P-04 | 部分生效 | 同 R-02，见 P1 |
| 抽样：二轮 P-06 | 已消失 | 决策 8、11、14、15、16、18、24、42、59、61、68 与故事 78 逐条核对，均与阶段文档同口径；「本项目此前没有系统参数调用点」经代码核实成立（Application 层无任何系统参数服务调用，仅两个已生成且无消费方的读模型） |
| 抽样：二轮 P-11 | 已消失 | 规格决策 4 与 `Server/design.md` 衔接节均为「组织、医院、院区与报告时间的组合索引 + 业务键唯一索引」，无「患者标识的索引」表述；主工作区脚本实际索引与其一致 |
| 抽样：二轮 P-15 | 已消失 | 风险表现存 7 条（报告侧契约变化、参数语义无先例、参数部署影响、脏归属落库、代理映射不可见、推进关系、提醒统计口径），均为现行有效项 |

## 一、已核对且未发现问题

| 项 | 文档与段落 | 核对依据 | 结论 |
|---|---|---|---|
| S5-D4 上游同步完整性 | `design.md` 参数段与 S5-D4；规格决策 16；`testReport.md` 执行边界 | SRS F04 业务规则 8、9 与 F06 业务规则 9、术语「引用详情有效时长」、设计约束 13、17、21、22；`CONTEXT.md`「本院报告匹配开关」「引用详情」段；`docs/uml/1-…wsd` 规则 5；`docs/uml/4-…wsd` 参数注记 | 逐处打开核对，默认值表述已全部删去；脚本扫描「默认关闭／默认四十八／默认七百二十」在六份文档与上游修改区均未命中；SRS 中已无该三参数的默认值残留 |
| 台账依据可回查 | `design.md` 关键决策 S5-D1–S5-D24 | SRS/CONTEXT/UML 原文与规范原文逐条回查 | 24 条依据均真实存在且方向一致；S5-D3、S5-D9、S5-D14、S5-D18 的技术理由均已显式声明上游状态；事务判断口径与 `backend-command-query-event.md` §4.1 逐字一致（依据业务原子边界、独立数据库语句数量与并发要求，不依据涉及表数量） |
| 台账代码事实断言 | S5-D5、S5-D18、S5-D21、S5-D24 与「当前代码事实」段 | 仓库实际代码 | 均成立：`MedicalRecognitionReportAppService.cs` 恰有 18 个公开方法、2 处 `[WorkUnit(` 声明；两个参数读模型存在、用值类型（`int`）承载、无任何消费方；`TrustedScopeResolver`、`OrganizationPathResolver` 存在；三个枚举在 Domain.Share、`MedicalRecognitionEnumDescriptorRegistry` 存在；四个互认实体已生成、三个事件类不存在、四个映射文件为聚合作用域占位形态 |
| 集合参数「本项目首例」 | `Server/design.md` 映射与作用域节；规格决策 68 | 20 个 SqlMap 全文扫描 | 无任何既有映射使用 `in` 集合参数或 `foreach` 展开，断言成立 |
| 读模型字段与 UML 一致 | `Server/design.md` V82、V83、V84；规格决策 51 | `docs/uml/3-HospitalIntegrationApiQuery.wsd` 十个读模型逐字段核对 | V82 的检验明细六字段、检查部位、整体异常标识、来源影像状态、影像调阅地址、PDF 三字段、「不含标本类型编码与院内标本号」，V83 的公共上下文六字段与文件信息，V84 的所见结论边界，均与 UML 一致 |
| UML 元素覆盖与合并口径 | `design.md` 事务与事件段；`Server/design.md` 事件表；规格决策 61 | `docs/uml/1-…wsd` | 四命令、四实体、十个读模型全部有落点；UML 现状确为匹配查询编排两个创建子命令并各对应一个事件，设计声明的合并形态与「尚未同步」自述均与实况一致；主工作区 `recognition_processing_result.sql`、`recognition_reference.sql` 仍含 `standard_project_code` 列，S5-D14「尚未同步」自述属实 |
| 报告侧上游主张 | `design.md` 筛选与选优段；`Server/design.md` 候选查询与衔接节；规格决策 4、24 | 主工作区 `mrec_medical_recognition_report.sql` | 该脚本确含 `report_time` 列（非空）、`(organization_code, hospital_code, branch_code, report_time desc, id desc)` 组合索引与 `ux_mrec_medical_recognition_report_business_key` 业务键唯一索引，文档三处主张与上游交付一致，无回归 |
| 报告侧前置状态 | `design.md` 前置段；`impl.md` 待办；`testReport.md` 执行边界 | 主工作区阶段 4 `testReport.md`、`impl.md` | 「10 张报告表已按三批建成、目标库与 schema 已确认、PDF 存储已配置」与阶段 4 报告一致；阶段 4 的批次 0–5 完成声明见无法确认项 1 |
| 批次依赖顺序与粒度 | 两份 impl | 矩阵编号与设计 | 批次 0（契约与上游同步）→ 1（表与映射，S5-D14 为硬前置）→ 2 → 3 → 4 → 5 → 6；引用详情（批次 4）依赖批次 3 的保存时间事实；各批次可独立验证；无被丢弃的工作 |
| 测试报告纪律 | `testReport.md` 全文 | `testing-baseline.md` §3 | 仅用 `NotRun`/`Blocked` 与允许标签；无任何通过性表述；数据形成口径写明「只通过平台接口或页面形成、不直接写库」；环境就绪（建表、参数配置）均表述为外部前置，未写成阶段交付；结论明确「无可交付结论」 |
| 禁止性内容与失效引用 | 六份文档全文 | 脚本扫描 + 人工核对 | 无未取证的 `Passed`/`Complete`/`Ready`/「验收通过」；无「批注」「负责人裁定」「回复原文」「确认来源」类对话痕迹；无审查方编号（`I-`/`U-`/`K-` 等）；无审查相关目录引用；所有相对链接指向的文件均存在 |
| 设计不含实施细节 | 两份设计文档与规格 | 脚本扫描 | 无 SQL 语句文本、无物理类型与宽度、无代码骨架（`Guid)`/`String)`/`Decimal(` 等命中均为对 UML 原文形态的描述，非设计自定实现细节；设计文档自身无此类内容） |
| 与既有阶段先例一致 | S5-D18、S5-D21；文档结构 | 阶段 2/3 Server 设计、`planning-and-evidence.md`、`stage-template` | 实体名作用域先例真实（阶段 2 `MutualRecognitionItem`、阶段 3 实体名作用域均有原文）；纯后端阶段未创建前端文档；事务声明规则、枚举文本派生、异常边界、权限边界均与既有口径一致；未触碰阶段 4 范围内任何对象 |
| 矩阵规模表述 | `testReport.md` 开头与执行边界 | 矩阵实际行数 | 两处均为 101 行，与实际一致；`testReport.md` 之外无其他行文位置声明总数 |

## 二、问题清单

| 编号 | 严重度 | 位置 | 问题描述 | 对照依据 | 建议处理 | 是否阻塞实施 |
|---|---|---|---|---|---|---|
| P1 | medium | `Server/design.md` 验证矩阵「"真实 WorkUnit"列的取值口径（三分法）」段与矩阵表行 | 三分法的档位定义与部分表行取值不符，同型条目跨段取值不一致。具体：①「否」档适用情形写「在进入工作单元写入前即终止的纯校验拒绝（其失败在入口校验阶段即可判定）」，但归入「否」的 V37、V38、V42、V43、V58b、V70b 为幂等与有效期成功路径（非拒绝），V39–V41 的冲突判定需先读取既有处理结果（在入口校验阶段不可判定）；②引用详情查询链内 V46–V57 记 N/A、V58 记「是」、V58b 记「否」，同链三种取值且无分层说明；③V31（F05 时间边界接受）记「是」而同型的 V68（F07 时间边界接受）记 N/A；V37（F05 幂等重试）记「否」而同型的 V69 记 N/A；V7、V71、V72 的断言需真实入口观察写入成功（按「是」档定义），却记 N/A；V93、V94 含非空匹配写入却记「否」；④「真实数据库」列同族取值不一：V3「项目编码不存在」记「否」而 V4–V6 记「是」（判断编码同样需要查询标准目录与互认配置库），V26、V27 记「否」而同族的 V28 记「是」（三者前置均为已存在的匹配记录） | `Server/design.md` 三分法主定义「该列按"本条目是否需要真实工作单元入口才能取得其证据"取值」；V1/V23/V59 记「是」的同类断言形态；`testing-baseline.md` §1.8 | 在口径段补一段可操作的取值判定步骤（例如：断言写入结果或零写入的条目记「是」；仅凭请求校验即可判定且无需数据库前置的拒绝记「否」；其余 N/A），按该步骤重推上列条目取值并逐一改表行：至少消除 V31/V68、V37/V69 两组同型矛盾与引用详情链内混值，并把 V3 的「真实数据库」改为「是」或写明无需数据库的理由；重推结果与两份 impl 批次归属无关，无需改批次 | 否（影响实施期测试归类与取证面，按现表实施会产生口径混乱与返工） |
| P2 | medium | `Server/design.md` 矩阵 V1–V22、V89–V94 区段 | SRS F04 后置条件 5「查询本身不形成互认处理结果、引用结果或调阅状态」无矩阵条目承接。V88 只覆盖引用详情链的同类断言；匹配查询侧（含 F05 业务规则 2、F07 业务规则 2 的匹配返回与 PDF 访问入口侧）没有任何条目断言负向不形成 | SRS F04 后置条件 5；F05 业务规则 2；F07 业务规则 2；`CONTEXT.md`「互认处理结果」「引用结果」段「查询、匹配返回和报告调阅也不自动生成…」 | 在批次 2 区段新增一条（建议编号 V22b，紧随 V22）：用例「匹配查询不形成处理结果、引用结果或调阅状态」，层级 Domain/Repository，预期「空匹配与非空匹配后，互认处理结果、引用事实与调阅状态记录均不存在，登记的事件仅为匹配已返回」，真实数据库「是」、真实 WorkUnit「是」；同步矩阵分段说明与两份 impl 批次 2 单元格 | 否 |
| P3 | medium | `docs/plans/001-总体计划/impl.md` 进度表 007 行（本轮回写内容） | 该行仍写「后端验证矩阵 V1-V88 已定义」，与现行矩阵（V1–V95 主编号 + 六个后缀，共 101 行）不符；同一行「阶段 4 的报告侧契约与索引尚未交付」与 `design.md` 前置段、`impl.md` 待办、`testReport.md` 执行边界的「报告侧前置已就绪（10 张表已建成、目标库与 schema 已确认）」直接矛盾；同表 006 行仍为「未开始」 | `testReport.md` 执行边界；主工作区阶段 4 `testReport.md`「10 张表已由负责人按三批执行建表」；本矩阵实际 101 行 | 更新 001 总体计划 impl.md 进度表 007 行：矩阵规模改为「V1-V95 主编号，另含 V35b、V36b、V58b、V70b、V72b、V74b，共 101 行」；报告侧前置表述改为与 `testReport.md` 一致的「报告侧读路径、索引与建表前置已就绪」；006 行状态按阶段 4 实际进度改写 | 否（回写滞后会误导总体计划读者与阶段收口判断） |
| P4 | low | `Server/design.md` 矩阵 V17–V20 区段 | SRS F04 业务规则 11「排除时长可以等于或大于某些项目的可互认时间，不作为配置错误；大于时该项目不会返回本院报告」无条目取证。V19 只覆盖满足侧（经过时间在排除时长与可互认时间之间），V20 只覆盖零值；「排除时长大于可互认时间」这一非错误配置的行为没有前置数据组 | SRS F04 业务规则 11；`CONTEXT.md`「本院报告匹配开关」段「排除时长大于项目可互认时间时该项目不返回本院报告，两种配置均不作为业务配置错误」 | 新增一条（建议编号 V20b，紧随 V20，归批次 2）：前置「排除时长配置值大于某项目可互认时间，且存在该项目的本院报告」，预期「该项目不返回本院报告，查询整体成功，不作为配置错误」；真实数据库「是」、真实 WorkUnit N/A（与 V17–V20 同档）；同步分段说明与两份 impl | 否 |
| P5 | low | `Server/design.md` 矩阵 V23–V45 区段 | SRS F05 业务规则 14 后半「互认项目停用或标准目录不可用时不得形成新的匹配；已经返回的匹配项只要绑定报告版本仍为当前有效版本，仍允许保存真实发生的采纳或不采纳处理结果」中，后半句（停用不阻止已返回匹配项的处理结果保存）无条目。V44 只覆盖版本失效的失败侧，无「配置停用但版本有效→保存成功」的取证面 | SRS F05 业务规则 14；`CONTEXT.md`「互认处理结果」末段「互认项目或标准目录在匹配返回后停用、不可用，不否定已经形成的互认匹配记录」 | 新增一条（建议编号 V45b，紧随 V45，归批次 3）：前置「匹配项对应的互认配置已停用，且绑定报告版本仍为当前有效版本」，预期「处理结果保存成功、不因配置停用拒绝」；真实数据库「是」、真实 WorkUnit「是」（观察写入成功）；同步分段说明与两份 impl | 否 |
| P6 | low | `阶段5-规格.md` User Stories 第 79 条 | 故事 79「本阶段的四个接口成功返回后可通过接口回读验证」不对应任何 SRS 条款，属取证方式表述；按「用户故事对应 SRS 条款」的核对口径为规格自定项 | SRS F04–F07 全文无对应条款；医院接入接口共同边界 6 只规定返回布尔真与业务异常，未规定回读验证 | 将该条从 User Stories 移入「Testing Decisions」的「前置证据」或「测试接缝」节，作为取证口径保留；User Stories 保持 78 条 | 否 |

严重度判定说明：P1、P2、P3 记 medium——三者会造成实施期返工或跨文档事实矛盾（矩阵口径是实施者安排取证面的直接依据；覆盖缺口使负向断言无取证责任；总体计划矛盾误导收口判断），均不影响设计结论的正确性，故低于 high；P4–P6 记 low——为局部覆盖与表述类缺口，补条目或移动段落即可消除。本轮无 blocker、无 high。

## 三、覆盖与切分评估（独立判断）

**上游要求到文档到矩阵的三段覆盖**：以 SRS F04–F07 的前置条件、后置条件、主事件流、备选流、业务规则五类建立清单，共 165 条（F04 46 条、F05 45 条、F06 41 条、F07 33 条）。正向核对：101 行矩阵条目均能指到真实上游条款或台账决策（V74/V74b 对 S5-D14、V85–V90 对 S5-D4/S5-D24、V78–V80 对映射规范），未发现凭空发明的条目。反向核对：162 条有设计口径与矩阵取证；3 条无条目承接，已列为 P2（F04 后置条件 5）、P4（F04 业务规则 11）、P5（F05 业务规则 14 后半）。另有三类「不建设」面（F04 业务规则 12、F06 业务规则 15、F07 业务规则 13）由 V88 的无写入断言、请求字段集合（V81）与设计排除清单共同承接，无需独立条目。UML 层面：四命令、四实体、十个读模型、事件合并口径全部有落点，无 UML 有而设计未提的元素。`CONTEXT.md` 在线互认全段的约定（含提醒口径、术语方向、业务科室人员规则）逐段核对均已落实。决策台账 24 条全部同时有「已确认设计」正文落点与矩阵待验证面落点。阶段根纳入清单（四能力、四表、三参数、三事件、读模型重建、枚举补充）逐项有落点；排除清单未被矩阵反向纳入（无 PDF 下载实现、API Client、统计类条目）。

**批次粒度与依赖顺序**：七个批次的划分按验证面与契约依赖成立，每批次交付后可独立验证，无需合并或新增拆分。批次 0 把 S5-D14 上游同步列入范围、批次 1 又以其为硬前置，属于同一事项的执行与门禁两个侧面，无矛盾。无需调整任何批次；需要的调整只有上表三个矩阵增补条目及其批次归属（V22b→批次 2、V20b→批次 2、V45b→批次 3）。

**上游之外的能力**：规格故事 79 为唯一自定项（P6）。其余规格故事与 71 条实现决策均能对应 SRS 条款或 24 条台账，无冲突、无内部重复（79 条故事两两主题不同；71 条决策与台账 24 条为投影关系，口径一致）。

## 四、设计选择分歧与无法确认项

| 编号 | 类型 | 位置 | 分歧点或事项 | 你的看法或所需证据 | 需要负责人决策的点 |
|---|---|---|---|---|---|
| F-1 | 分歧 | `design.md` S5-D3 | 可信身份三值不再向组织服务核对存在、启用与父子归属 | 可接受：宿主签发身份可信度高于请求提交值，匹配查询为开单期高频接口；风险已登记且重审条件（出现可复现归属不一致数据、或组织服务提供轻量校验能力）可执行 | 无（维持 `AcceptedRisk`） |
| F-2 | 分歧 | `design.md` S5-D4 | 三个参数不设默认值使任一参数未配置时对应能力整体不可用 | 严格失败符合参数的临床安全取向；部署清单需含三参数配置与代理映射确认，风险表重审条件（部署清单确认后消解）可执行 | 无 |
| F-3 | 分歧 | `design.md` S5-D8 | 候选查询以两个左连接加合并取值取得统一排序键，未实测 | 单语句内可成立，不违反方言禁令（V80 守卫）；运行正确性由 V9–V11 与 V79 在实施期取证即可 | 无 |
| F-4 | 分歧 | `design.md` S5-D14 | 处理结果表与引用事实表删除项目编码列的量化代价 | 代价（按项目汇总需与匹配项表按主键关联）已写明；阶段 6 统计按匹配项关联可行 | 无 |
| F-5 | 分歧 | `design.md` 前置段 | 阶段 5 先行设计与阶段 4 收口并行推进 | 已登记重审条件（回写总体计划后消解）；回写时按 P3 一并修正 | 无 |
| U-1 | 无法确认 | `testReport.md` 执行边界 | 「报告采集与生命周期能力的批次 0–5 已完成」的批次级声明 | 阶段 4 文档可确认：10 张表已建成、目标库与 schema 已确认、两个收尾缺陷已修复复测、宿主验收证据已取得；但阶段 4 `testReport.md` 当前状态为「部分执行」，未声明批次级完成。建议把该句改为可证表述：「报告侧读路径、索引与建表前置已就绪」，或由负责人确认阶段 4 批次状态后保留原文 | 是否改写该句 |
| U-2 | 无法确认 | `Server/design.md` 系统参数集成节 | 参数服务代理映射在目标宿主是否可用 | 配置文件两工作树均不可见；文档已按「不可见、实施时确认」口径表述，实施期在目标宿主核对即可 | 无 |
| U-3 | 无法确认 | `Server/design.md` 事务与事件取证边界 | 事件可见性子面的框架实际行为 | 属运行事实，设计已按框架边界记 N/A 并说明，符合 `dy-framework-workunit.md` §3；实施期以真实入口与失败注入取证回滚面即可 | 无 |

## 五、覆盖度自评

**已覆盖与未覆盖的审查维度**：八个维度全部执行。维度 1（上游一致性）逐条核对了设计引用的 SRS/CONTEXT/UML 条款原文与方向；维度 2（覆盖完整性）建立 165 条五类清单双向比对；维度 3（设计与实施切分）核对了批次表、依赖与文件所有权；维度 4（矩阵质量）逐行核对了层级、取值与不可构造前置（未发现需要空编码账号或令牌缺层的条目，V92 已改为可构造前置）；维度 5（台账质量）逐条回查依据与待验证面；维度 6（测试报告纪律）对照测试基线核对状态词与证据表述；维度 7（既有阶段先例）核对了阶段 2/3 作用域先例、阶段 4 边界与模板结构；维度 8（可执行性与残余风险）核对了风险表 7 条的重审条件与设计一致性。未执行任何运行验证，框架事务回滚与事件顺序的运行事实无法在本轮取得，只能核对设计对取证面的安排。

**机械核对十三项的实际执行方式与命中结果**：

1. **矩阵编号完整性**：脚本抽取矩阵全部表行——101 行、V1–V95 无缺号重号、六个后缀均紧随主编号且位于对应语义段；分段说明区段与表内编号一一对应。未命中。
2. **决策台账编号完整性**：脚本抽取 S5-D1–S5-D24，连续无重号缺号；每条在「已确认设计」正文与后端设计有落点；待验证面引用的全部矩阵编号均存在。未命中。
3. **五类双向覆盖**：建立 165 条清单（F04 46、F05 45、F06 41、F07 33），正向逐条定位、反向逐条检查。命中 3 条无条目（P2、P4、P5）。
4. **批次与矩阵相互覆盖**：脚本按区间展开两份 impl 批次单元格——并集恰为 101 编号、无缺、无重复归属、两份一致。未命中。
5. **内部链接有效性**：脚本提取六份文档全部相对链接并验证目标文件存在，全部通过；链接目标的关键章节（验证矩阵、执行边界、残余风险等）经阅读核对存在。未命中。
6. **禁止性内容**：脚本扫描六份文档的 `Passed`/`Complete`/`Ready`/「验收通过」/「批注」/「负责人裁定」/「回复原文」/「确认来源」/「审查相关」/审查编号模式，全部未命中（`Blocked`/`NotRun` 为合法状态词）。
7. **设计不含实施细节**：脚本扫描 SQL 关键字、物理类型宽度、代码骨架模式，设计文档未命中。
8. **决策依据可回查**：24 条依据逐条打开原文核对方向；8 项代码事实断言逐一在仓库核实成立（结果见「一、已核对且未发现问题」）。未命中。
9. **矩阵条目重复穷举**：脚本对 101 行两两计算「用例与边界＋预期结果」相似度，45 对超过 0.55 的条目逐一人工复核，全部为同族不同条件或跨用例对称条目（V74/V74b 分属两表、V42/V70b 分属两个功能侧）；无同用例内实质重复。最高相似对 V31/V68（0.87）为跨用例对称条目，其取值矛盾已列入 P1。未命中新增重复。
10. **规格与上游及台账对应**：故事 79 条逐一核对——78 条对应 SRS 条款，故事 79 为自定项（P6）；SRS F04–F07 要求均有故事承接，无遗漏；实现决策 71 条与 24 条台账及「已确认设计」正文逐条比对无冲突；规格内部无重复条目。
11. **同口径跨文档一致性**：审查要求列出的十项口径在 `design.md`、`Server/design.md`、规格、两份 impl 与矩阵条目逐项比对，全部一致；禁用变体（三维度、默认关闭/四十八/七百二十、统一返回布尔真、患者标识的索引、多于一条写语句、已存在且可用）脚本扫描未命中。唯一跨文档残留为 P3（001 总体计划回写行，不在十项口径清单覆盖范围内）。
12. **WorkUnit 列声明与取值一致性**：脚本抽取 101 行取值与口径段清单逐一比对——「是」15/15、「否」40/40、N/A 46 行，清单与成员完全一致；但档位「适用情形」描述与部分成员语义不符、同型条目跨段取值不一致，命中 P1。
13. **矩阵规模表述一致性**：`testReport.md` 两处均为 101 行，与实际一致；`impl.md` 与 `Server/impl.md` 按区段表述无总数声明；命中 P3（001 总体计划回写行的「V1-V88」）。

**上游要求清单的建立方式与比对结果**：以 SRS F04–F07 各功能章节的五个小节逐条抄录建立清单，`CONTEXT.md` 在线互认各段与 UML 命令/实体/读模型作交叉印证；清单项数 165，承接 162，无承接 3（见问题清单）。

**本轮严重度判定说明**：见问题清单末段。本轮无 blocker、无 high；3 条 medium、3 条 low。

**本轮审查的边界与已知局限**：只读审查，未运行任何测试与构建；主工作区仅作只读核对（阶段 4 文档与三个建表脚本）；`.agents/instructions/pagination.md` 本工作树不可见，涉及其内容的核对未执行（本轮矩阵与批次表述未依赖该规范）；阶段 1–3 文档仅在核对作用域先例时按需查阅；宿主环境配置不可见，参数代理映射只能核对文档口径。

**已知整改是否引入新问题的结论**：

1. 本轮整改是否引入新的 blocker 或 high：未发现。本轮 16 条整改中 15 条已消失、1 条部分生效（R-02）；引入的新问题为 P1（medium，三分法定义与取值的语义层残余，与 R-02 同位置）与 P3（medium，001 总体计划回写行的旧口径残留，属本轮上游同步动作的连带产物）。
2. 四种已知失效模式逐类结论：
   - 模式一（只改一处、别处残留）：**未完全消除**。审查要求列出的十项口径已无残留，但本轮新引入的回写动作（001 总体计划 007 行）不在该十项口径清单内，其矩阵规模与阶段 4 前置表述仍为旧口径（P3）。建议整改时把「回写总体计划」纳入同口径扫描范围。
   - 模式二（写说明而不改表行）：**已消除**。本轮新增断言（V23、V88、V37、V3/V5/V6、V26–V28、V74b 等）均已写入表行，脚本逐行核对确认；表外说明段与表行无脱节。
   - 模式三（凭本工作树可见内容推断上游事实）：**已消除**。报告时间列、组合索引、业务键唯一索引、旧脚本含项目编码列等上游主张均与主工作区实际脚本逐一相符；代理配置表述已按「不可见」口径书写。
   - 模式四（同口径两处依据来源不同）：**已消除**。事务判断口径、参数口径、集合参数写法在各处引用同一依据；未再发现结论相同而依据相异的条目对。
3. 新的失效模式：未发现第五类独立失效模式。P1 的性质是模式一在矩阵口径层的变体——口径段改写（三分法重列清单）时只重排了清单成员，未对表内既有的同型条目按新定义重新取值；现有防护（清单与逐行机械比对）只能发现成员差异，发现不了定义与取值之间的语义不符，需要增加「按判定步骤重推全列取值」这一步。
