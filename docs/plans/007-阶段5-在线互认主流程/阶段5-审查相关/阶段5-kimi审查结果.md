# 阶段 5 阶段文档审查结果（Kimi）

- 审查时间：2026-09-19
- 写入前确认：**已清空该文档全部历史内容**（该文件本轮写入前为 0 字节空文件，无历史内容可清，从头写入本轮结果）
- 审查对象（均完整读取）：
  - `docs/plans/007-阶段5-在线互认主流程/design.md`（94 行）
  - `docs/plans/007-阶段5-在线互认主流程/Server/design.md`（258 行）
  - `docs/plans/007-阶段5-在线互认主流程/impl.md`（36 行）
  - `docs/plans/007-阶段5-在线互认主流程/Server/impl.md`（17 行）
  - `docs/plans/007-阶段5-在线互认主流程/testReport.md`（35 行）
  - `docs/plans/007-阶段5-在线互认主流程/阶段5-规格.md`（401 行）
- 核对依据（实际读取）：`docs/需求规约SRS.md`（F04–F07 五类条款与「医院接入接口」表、设计约束 17）、`CONTEXT.md`（在线互认全段）、`docs/uml/1-MedicalRecognitionReport.wsd`、`3-HospitalIntegrationApiQuery.wsd`、`4-MedicalRecognitionExternalSystemQuery.wsd`、`命令出参整改矩阵.md`、`docs/plans/001-总体计划/design.md` §4.7 与 `impl.md` §7/§8、`docs/plans/README.md` 与 `stage-template/`、`.agents/instructions/` 共 14 份规范、`server/` 代码事实（聚合、映射 XML、应用服务、架构守卫、枚举注册表、静态探测断言）、主工作区 `E:\MedSync\Dy.MedicalRecognition` 的阶段 4 文档与已交付代码/建表脚本（只读）。
- 未读取到的材料：`appsettings.Development.json`（未纳入版本控制，与工作树声明一致，参数代理映射内容不可见）；阶段 4 文档本工作树不可见，经主工作区只读核对弥补。除此之外无因缺文件而无法核对的项。

## 已整改条目复核

### 第三部分（第三轮 16 条，逐条复核，最高优先级）

| 编号 | 复核结论 | 依据（回到原文核对的结果） |
|---|---|---|
| R-01 | 已消失 | 规格决策 15 现为「三个参数均按平台级编码读取，不传组织、医院、院区维度；参数编码为小写下划线形式的常量，声明于使用处」（`阶段5-规格.md:177`），与 `design.md:26`、`Server/design.md:100`、V90 四口径一致；其引用的 SRS F04 业务规则 8「平台全局参数」（SRS:1349）与 F06 业务规则 8「全平台统一……不建立组织级覆盖」（SRS:1724）均真实且方向一致 |
| R-02 | 已消失 | 用脚本抽取 `Server/design.md` 表内 101 行 WorkUnit 逐行取值：是 15 条、否 40 条、`N/A` 46 条，无其他取值；口径段声明的「是」清单 15 条与实际逐一相符（差异为空）、「否」清单 40 条与实际逐一相符（差异为空）。**但三分法定义与成员语义存在新缺陷，见 K-1** |
| R-03 | 已消失 | 门禁句四处一致：`design.md:90`「S5-D4 的上游同步已完成；S5-D14 与 S5-D6 的上游同步未完成前本阶段不进入实施」；`design.md:26`、`impl.md:30`、`testReport.md:10` 口径相同且无「已完成/未完成」自相矛盾 |
| R-04 | 已消失 | 规格决策 68 现为「本项目首例……以映射语句文本静态取证其展开形态」（`阶段5-规格.md:297`），与 `Server/design.md:94` 一致；决策 14（`阶段5-规格.md:175`）已无「同组织项目的常量声明惯例」表述。代码侧 grep 确认 `server/Dy.MedicalRecognition.Repository` 全部映射 XML 无 `in (` / `in $` / `in @` 形态，「首例」主张属实 |
| R-05 | 已消失 | V74b 表行存在且紧随 V74（脚本验证）；S5-D14 待验证面为「V74 与 V74b」；两份 impl 批次 1 矩阵单元格与归属段均含 V74b |
| R-06 | 已消失 | V95 主题为「业务科室与人员的负向要求」（不向权限系统补查、不拒绝、不校验科室一致），V82/V83 为字段清单，三者主题不同、无包含关系 |
| R-07 | 已消失 | V35b/V58b/V72b 均紧随其主编号（脚本验证）；分段说明与两份 impl 批次归属同步。上游出处逐条回查真实：V35b←SRS F05 业务规则 1（SRS:1517「处理结果值域只包括采纳和不采纳」）；V58b←SRS F06 业务规则 14（SRS:1736「已经提交引用结果也不提前关闭有效期」）；V72b←SRS F07 业务规则 11（SRS:1888「引用结果不设置独立提交期限，不受匹配可互认时间或引用详情有效期限制」）。与既有条目无实质重复 |
| R-08 | 已消失 | V92 现为「未认证请求被拒绝」，前置「不带认证凭据的请求」，并明示「不构造可信身份缺层的登录身份（该状态正常业务不可达）」——前置正常业务可构造；规格测试替身段（`阶段5-规格.md:336`）已改为「仅构造令牌三层齐全一种身份」 |
| R-09 | 已消失 | V23 含「并形成采纳次数与来源医院被互认次数各一次」；V88 含「不形成引用结果、不形成引用次数、不形成新的互认处理」 |
| R-10 | 已消失 | 两份 impl 批次 3「V23-V45（含 V35b、V36b）」、批次 5「V59-V72（含 V70b、V72b）」及归属段均含后缀条目 |
| R-11 | 已消失 | 规格 Further Notes「上游待改项」段（`阶段5-规格.md:387`）为客观现状陈述，无讨论痕迹。**但其同步范围清单与 design.md 有一处差异，见 K-5** |
| R-12 | 已消失 | `testReport.md:5` 与 `:9` 均为「101 行（V1–V95 主编号 + 六个字母后缀条目）」，与表内实际一致 |
| R-13 | 已消失 | S5-D15 依据为「SRS F07 主事件流 2 与 4……`CONTEXT.md`『引用结果』段同口径」（`design.md:74`），条款真实存在（SRS:1834、1838；CONTEXT.md:472-474）且方向一致 |
| R-14 | 已消失 | `Server/design.md:38` 复用行含「该解析只覆盖组织与医院两层；院区层取可信身份为本阶段新增口径，不经该解析器」，与 `TrustedScopeResolver.cs:7-12、133` 代码事实一致 |
| R-15 | 已消失 | 分段说明为编号区段式并注明「区段名只表编号范围，个别条目的层级以其『层级』列为准」；区段与表内编号脚本比对一致 |
| R-16 | 已消失 | 风险表末条为「该口径由 V93 在本阶段承接；提醒次数的读取与报表口径归阶段 6 统计设计」（`testReport.md:31`），与 V93 承接情况相符 |

### 第一、二部分（抽样复核）

- 抽样 04（WorkUnit 整列 N/A）：已改三分法，见 R-02 脚本结论。已消失。
- 抽样 07（引用表同口径）：V74b 存在，见 R-05。已消失。
- 抽样 12（批次一号两归）：脚本核对两份 impl 批次表，101 个编号全覆盖、无重复归属、无虚构编号。已消失。
- 抽样 25（幂等不更新保存时间）：V37 表行含「处理结果保存时间不变」。已消失。
- 抽样 27（F05 归属校验）：V36b 存在且上游出处真实（SRS F05 主事件流 2，SRS:1476）。已消失。
- 抽样 34（失败事实粒度）：V3/V5/V6「指出相关标准项目编码」、V26–V28「指出相关互认匹配项标识」均为表行内加粗断言，非表外说明。已消失。
- 抽样 36（四处覆盖缺口）：V91/V92/V35b/V58b/V72b 均在表内。已消失。
- 抽样 P-01（统一返回布尔真）：`Server/design.md:42` 为两类返回形态。已消失。
- 抽样 P-02（报告时间来源）：三处均为「报告主体上冗余保存的报告时间列」（`design.md:30`、`Server/design.md:96`、规格决策 24），与主工作区阶段 4 交付一致（`MedicalRecognitionReport.cs:48` `ReportTime`；`mrec_medical_recognition_report.sql:11` `report_time … not null`、`:45-48` 组合索引）。已消失。
- 抽样 P-06（规格 10 处旧口径）：抽查决策 24、42、61、68 与故事 78，均已是新口径。已消失。
- 抽样 P-11（索引口径）：规格决策 4 与 `Server/design.md:120` 均为「组合索引 + 业务键唯一索引」，不含患者标识索引；阶段 4 交付中确有这两类索引。已消失（阶段 4 另有患者姓名/证件号前缀索引，但匹配候选查询经平台患者主键关联过滤，不依赖该两索引，不构成阶段 5 依赖声明错误）。

## 一、已核对且未发现问题

1. **矩阵编号完整性**（机械核对 1）：脚本抽取 101 行；V1–V95 主编号连续、无重号、无缺号；V35b/V36b/V58b/V70b/V72b/V74b 均紧随其主编号；分段说明与表内编号一致。
2. **决策台账**（机械核对 2）：S5-D1–S5-D24 连续无重无缺；每条在「已确认设计」正文或后端设计中有落点（D1→接口段、D3→归属段、D20→空匹配段、D24→契约与枚举段等，逐条核过）。
3. **S5-D1–S5-D24 依据列**（机械核对 8，逐条回查）：除自述为技术理由/设计约定的 D3、D5、D14、D18、D21–D24 外，所有引用 SRS/CONTEXT/UML 的依据均真实存在且方向支持结论；第三轮曾出事的「依据方向反转」未复发（如 S5-D4 不再反向引用 F04 业务规则 9）。D14 的 UML 同步确实未完成：`1-MedicalRecognitionReport.wsd:829、845` 两实体仍含 `StandardProjectCode`；S5-D6 的 UML 合并确实未登记：`RequestRecognitionMatchesCommand`（:479-498）仍编排两个创建子命令并各对应一个事件——与文档「未完成」自述一致。
4. **S5-D4 上游同步无残留**：SRS F04 业务规则 8/9（:1349-1351）、F06 业务规则 9（:1725）、设计约束 17（:2269）、CONTEXT.md「本院报告匹配开关」（:442-444）与「引用详情」（:460-467）均为新口径，无默认值/维度覆盖残留；UML 中两处参数语义（`1-MedicalRecognitionReport.wsd:494`、`4-MedicalRecognitionExternalSystemQuery.wsd:55`）也已是「不设默认值」新表述；未发现上游内部自相矛盾（其余「默认启用」均属标准目录/互认配置等无关范围）。
5. **批次与矩阵互覆盖**（机械核对 4）：两份 impl 批次表合计覆盖全部 101 个编号，无未承接、无虚构、无重复归属；批次依赖顺序正确（上游同步批次 0 → 脚本/映射批次 1 → 三个实现批次 → 收尾批次 6）。
6. **内部链接**（机械核对 5）：六份文档全部相对链接指向的文件均存在。
7. **禁止性内容**（机械核对 6）：六份文档中 `Passed`/`Complete`/`Ready`/`验收通过`/`批注`/`裁定`/`回复原文`/`确认来源`/审查方编号/审查相关目录引用全部零命中（Grep 全目录扫描，命中仅落在审查相关目录自身）；未把测试结果写进设计文档；无对话痕迹（`testReport.md:34`「记录存在不代表负责人已批准收口或验收」为否定性合规表述）。
8. **设计不含实施细节**（机械核对 7）：设计文档未写物理类型宽度与建表语句；「集合参数写法」与「两个左连接」属映射写法约定（V78/V79 的静态取证对象），不构成越界。
9. **矩阵条目重复**（机械核对 9）：对 101 行做两两归一化相似度穷举（脚本，阈值 0.72，排除字母后缀与其主编号），最高相似对为 V42/V70b（0.89，F05/F07 两侧对称的名称不参与幂等条目，接口不同、非重复）与 V32/V33、V31/V68、V36/V65 等（同一语义段内的不同边界），无实质重复或包含关系。
10. **规格条目数与对应**（机械核对 10）：用户故事 79 条、实现决策 71 条，与审查要求所述条数一致；抽核故事均可回溯到 SRS F04–F07 条款（无臆造），实现决策与台账/正文口径一致（无冲突）。
11. **同口径跨文档一致性**（机械核对 11）：逐条比对 11 项口径在 design.md、Server/design.md、规格、impl 批次表四处的表述——参数读取维度、参数默认值、返回形态、报告时间来源、归属取值来源（含院区新增口径限定）、报告侧索引表述、领域事件数量（三个 + UML 两个创建子命令事件属同步范围）、事务判断依据（规范原文为「依据业务原子边界、独立数据库语句数量和并发要求……不依据涉及表数量」，`backend-command-query-event.md:72-73`，方向一致）、系统参数代理配置（均为「未纳入版本控制、本工作树不可见」，无人写「已存在且可用」）——全部一致；唯一例外见 K-4（design.md 正文项目编码删列口径只写处理结果表）。
12. **矩阵规模表述**（机械核对 13）：testReport 两处、impl 批次区段、分段说明均为 101 行口径，一致。
13. **测试状态纪律**：testReport 全部记 NotRun/Blocked，无证据性通过表述；数据形成口径写明只经接口/页面形成；不构造空编码账号的声明与矩阵条目一致（V92 已改为未认证请求）。
14. **规范符合性**：WorkUnit 三分法词表与阶段 2/3 先例一致（阶段 1 用「需要/不需要」，规范未强制词表）；事件可见性子面记 `N/A` 有阶段 2/3 先例；NotRun/Blocked 使用与 testing-baseline 定义一致；枚举文本派生与阶段 2/3 先例一致；S5-D18「既有 18 个方法中仅 2 个声明显式事务」与代码逐一清点一致（`MedicalRecognitionReportAppService.cs` 18 个公有写方法，仅 `CreateMedicalStandardGroupAsync:143`、`CreateMedicalStandardItemAsync:214` 带 `[WorkUnit(UseTransaction = true)]`）；`TrustedScopeResolver` 确只覆盖组织/医院两层（:7-12、:133）；阶段 2/3 实体名作用域先例属实；总体计划 §4.7/§8 引用真实，API Client「阶段末按实际需要重生成」条款（impl.md §8）真实存在；纯后端阶段未建前端文档，目录结构符合 stage-template（规格文件属额外设计输入，模板未禁止）。
15. **V95 与 V82/V83 无重叠**（R-06 复核）：主题分别为负向要求与字段清单，通过。

## 二、问题清单

严重度标准：`blocker`＝会导致实施失败或产出错误设计；`high`＝会造成返工或契约不一致；`medium`＝局部不一致或证据缺口，实施前应解决；`low`＝表述、编号、引用类瑕疵。

| 编号 | 严重度 | 位置 | 问题描述 | 对照依据 | 建议处理 | 是否阻塞实施 |
|---|---|---|---|---|---|---|
| K-1 | medium | `Server/design.md` 矩阵说明「真实 WorkUnit 列的取值口径（三分法）」表与矩阵 V58b、V85–V90、V92、V95 行 | 三分法内部定义与取值语义不符：「否」档适用情形定义为「在进入工作单元写入前即终止的纯校验拒绝」，但其成员含 V85（枚举登记）、V86（无新增中间件）、V87（事务声明）、V88（无写入）、V90（参数读取维度）等**静态核对条目**——这些恰落在「`N/A`」档自述定义「不经工作单元入口的只读查询与静态核对条目」内；V58b 为纯只读查询（获取详情），亦记「否」而非「N/A」。清单与表行逐行一致（脚本验证无差异），但口径段的两档定义无法涵盖实际成员，实施者按定义无法自洽判断后续增补条目该归哪档 | 该文件口径段自身定义（:132-138）；`dy-framework-workunit.md`（事件可见性 N/A 先例）；阶段 2/3 矩阵静态条目记 `N/A` 的先例 | 二选一：① 把「否」档适用情形改写为「不需要真实工作单元入口取证即可判定的条目（纯校验拒绝与静态/契约核对）」，并在 N/A 档定义中删去「静态核对条目」；② 将 V85–V88、V90、V58b 改记 `N/A` 并同步两档清单与条数。**推荐①**，改动最小且不触动已审条目取值 | 否（取证方式口径问题，实施前统一即可） |
| K-2 | medium | `testReport.md:10`、`impl.md:36` ↔ `docs/plans/001-总体计划/impl.md:18-19` | 「报告侧前置已就绪」存在跨文档事实矛盾：testReport 与 impl 断言「报告采集与生命周期能力的批次 0–5 已完成，10 张报告表已按三批建成……前置成立」；而总体计划 impl.md（2026-09-19 更新）第 18 行记阶段 4「未开始」、第 19 行称「阶段 4 的报告侧契约与索引尚未交付，本阶段与其并行推进」。且本工作树 `server/` 代码基线确未含阶段 4 交付物（无 `ReportTime`/`PatientName`/`IdentityDocumentNo` 列、脚本仍为旧表名无 `mrec_` 前缀、读模型仍为 `object` 占位），阶段 4 文档未纳入版本控制。另：总体计划 impl.md:19 记「后端验证矩阵 V1-V88 已定义」，与实际 101 行不符 | 主工作区阶段 4 `testReport.md:9、131` 与 `impl.md:32-37`（显示批次完成、10 表建成、目标库确认，但 `:315`「本报告不声明阶段完成」）；本工作树 `server/` 代码事实；`testReport.md:10` 原文 | 由负责人裁定阶段 4 交付状态的单一事实口径：若认定已交付——更新总体计划 impl.md:18-19（含「V1-V88」改为「V1–V95 及六个字母后缀条目共 101 行」），并在 testReport/impl 补一句「该交付位于主工作区、本工作树版本基线尚未包含，实施前需合并/回写」；若认定未交付——回改 testReport.md:10 与 impl.md:36 为「前置未就绪，相关运行项记 Blocked」。两处口径必须只剩一个 | 是（决定批次 2 前置是否成立，但本阶段因 S5-D14/S5-D6 未同步尚不进入实施，可在实施前消解） |
| K-3 | medium | `Server/design.md` 验证矩阵 V1–V95 及六个后缀条目整体 | SRS F04 后置条件 5「查询本身不形成处理结果、引用结果或调阅状态」（SRS:1285）无矩阵条目承接。第三轮曾为同类型的 F06 后置条件 2 新增 V88 断言（R-09），按同一标准 F04 后置条件 5 属同类缺口；匹配查询侧目前只有正向条目（V1），无「不形成处理结果/引用事实」的负向断言 | SRS F04 后置条件 5（SRS:1285）；矩阵 V88（F06 侧同型承接）；第三轮 R-09 处理先例 | 新增字母后缀条目承接（置于 V1-V22 语义段内，如 V22b）：「匹配查询不形成处理结果、引用结果或调阅状态——成功匹配返回后库中无该组的处理结果与引用事实，只存在匹配记录、匹配项与匹配已返回事件」；同步分段说明（V1-V22 含 V22b）与两份 impl 批次 2 单元格及归属段 | 否 |
| K-4 | low | `design.md:42`「已确认设计」项目编码段 | 正文只写「处理结果表不保存冗余项目编码」，同口径的「引用事实表同样删除项目编码列」只出现在台账 S5-D14、`Server/design.md:86` 与规格决策 42，正文未承载。口径本身不矛盾，但属「同一口径未到处」的残留形态（机械核对 11 第 9 项的唯一例外） | S5-D14（`design.md:73`）；`Server/design.md:86`；规格决策 42 | 将 `design.md:42` 首句改为「处理结果表与引用事实表都不保存冗余项目编码」，与 S5-D14 一致 | 否 |
| K-5 | low | `阶段5-规格.md:387`「上游待改项」段 ↔ `design.md:85`「上游同步范围」段 | 同一事实（S5-D4 同步已完成）的两处范围清单不一致：规格列「SRS F04 业务规则 8、9 与 F06 业务规则 9、设计约束 17、CONTEXT.md 两段、**两份 UML**」；design.md 同步范围段只列 SRS 与 CONTEXT.md、**不含 UML**。经核对两份 UML 中确有两处参数语义（`1-MedicalRecognitionReport.wsd:494`、`4-MedicalRecognitionExternalSystemQuery.wsd:55`）且已是新口径，规格的清单更完整 | `1-MedicalRecognitionReport.wsd:494`、`4-MedicalRecognitionExternalSystemQuery.wsd:55`（均为「不设默认值」新表述，无其他需同步的参数默认值残留） | 在 `design.md:85` 上游同步范围段的 S5-D4 项补列「`docs/uml/1-MedicalRecognitionReport.wsd` 匹配查询命令规则 5 与 `4-MedicalRecognitionExternalSystemQuery.wsd` 参数注记」，使两处清单一致 | 否 |
| K-6 | low | `Server/design.md` V23、V72 行 | 「形成采纳次数与来源医院被互认次数各一次」「只增加引用次数」的观察口径未写明：四张表无任何计数列，该次数按 `CONTEXT.md:477` 是由已保存事实派生的统计口径，运行取证时无可直接观察的计数载体，实施者可能误读为需维护计数器 | `CONTEXT.md:476-478`（互认次数与引用次数为分别统计的派生事实）；四表结构（`Server/design.md:81-84` 无计数列）；testReport 风险表末条对提醒口径的同类处理先例 | 在 V23/V72 预期结果括注「次数为按已保存事实派生的统计口径，本阶段不维护计数列，计数读取归阶段 6」，或在该文件「V89-V95 与上游的对应」段末补一句同口径说明 | 否 |
| K-7 | low | `阶段5-规格.md:175` 决策 14 与 `:177` 决策 15 | 规格内部重复：「参数编码以小写下划线形式」「参数编码以常量声明于使用处」两句在决策 14 与 15 中重复出现，决策 15 实际只比 14 多「按平台级编码读取、不传维度」。不构成冲突，属条目冗余 | 规格 Implementation Decisions 节自身 | 决策 14 删去「参数编码以常量声明于使用处」半句（保留编码形态），该句由决策 15 独占 | 否 |

**重开项**：无。第三部分 16 条与抽样复核的历史条目均未复发。

## 三、覆盖与切分评估（独立判断）

- **上游要求 → 文档 → 矩阵三段覆盖**：以 SRS F04–F07 五类条款逐条建单（前置 13、后置 16、主事件流 27、备选流 24、业务规则 84，合计约 164 条），正向逐条映射到 101 行矩阵、反向逐条检查承接，两个方向均执行。核心功能路径、失败路径、边界口径覆盖完整，无凭空发明条目（每个字母后缀条目与 V91/V92/V95 均回查到真实条款）。未单立条目的条款集中在四类，均不构成实施缺口：①业务语义性声明（如 F05 业务规则 2「平台不从匹配返回推断决定」、F07 业务规则 1「只接收正向事实」）由设计正文承载；②客户端重试策略（F05 备选流 7、F07 备选流 5）非服务端验证面；③统计归属与不变性（F05 业务规则 17–19、F06 业务规则 11 等）由已保存事实的不可变结构天然保证，其中 F04 后置条件 5 按第三轮 R-09 的先例标准应立条目而未立，已列 K-3；④PDF 下载/影像/脱敏等范围外或非本阶段功能验证面。CONTEXT.md 在线互认全段 14 项约定逐项有承接（含来源医院/接收医院术语——CONTEXT.md 尚未补充，规格 Further Notes 已如实记为「待落」，属已登记的上游待办，非阶段 5 缺陷）。
- **批次粒度与依赖顺序**：批次 0-6 每批交付后可独立验证，无必须合并才能看结果的批次，无装不下一个执行上下文的批次；上游同步（0）与表结构（1）在其消费者（2-5）之前；S5-D14 为批次 1 硬前置的表述与 UML 实际状态（两实体仍含项目编码列）相符。**无需增删批次**；唯一新增工作是 K-3 的一条矩阵条目，归批次 2。
- **无丢弃工作、无越界**：纳入清单（四接口、四表、三参数、三事件、读模型重建、枚举补充）逐项有落点；排除清单（PDF 下载实现、API Client 重生成、前端、统计）未被任何批次反向纳入；未触碰阶段 4 范围内对象（报告侧索引与专项内容物理宽度只声明依赖不交付）。
- **文档声明了上游之外的能力**：未发现。S5-D3（不向组织服务核对）与 S5-D4（无默认值）为对上游沉默/上游改写的口径，均已改写并同步上游（D4 同步完整无残留；D3 如实登记 AcceptedRisk）。

## 四、设计选择分歧与无法确认项

| 编号 | 类型 | 位置 | 分歧点或事项 | 看法或所需证据 | 需要负责人决策的点 |
|---|---|---|---|---|---|
| F-1 | 分歧 | S5-D3（`design.md:63`） | 可信身份三值不再向组织服务核对，脏归属可落库 | 高频接口避免外部调用有工程合理性，但无上游条款支持；风险已登记重审条件。属可接受的取向取舍 | 是否维持 AcceptedRisk（建议维持，仅确认） |
| F-2 | 分歧 | S5-D4（`design.md:63`） | 三参数不设默认值，任一未配置则对应能力整体不可用 | 严格失败避免错误配置的临床可见后果，代价是部署脆弱性；已登记部署影响风险。属取向取舍 | 是否维持（建议维持，仅确认） |
| F-3 | 分歧 | S5-D14（`design.md:73`） | 删两表项目编码冗余列，按项目汇总需回连匹配项表 | 消除双处不一致 vs 增加一处关联；两表与匹配项均一对一，代价有界。属取向取舍 | 是否维持（建议维持，仅确认） |
| F-4 | 分歧 | S5-D17、S5-D8 | MVP 不重并发（唯一索引兜底）；候选查询两个左连接未实测 | 均如实登记为 AcceptedRisk/待验证面，实施期有静态取证（V75/V78/V79）。属取向取舍 | 是否维持（建议维持，仅确认） |
| W-1 | 无法确认 | V83/规格故事 6、44 | 「来源医疗机构」名称的数据来源设计未写明：阶段 4 版本公共读模型无来源医院/院区名称（仅列表读模型有），阶段 5 设计未说明名称取自报告冗余列还是编码回填服务 | 需确认报告主体/版本是否冗余保存来源医院与院区名称，或实施时复用既有编码名称回填机制；若无现成来源，匹配响应与引用详情的公共上下文字段将无法填充 | 负责人确认取值来源；若需新增回填，补设计口径 |
| W-2 | 无法确认 | K-2 | 阶段 4 交付状态的最终事实（主工作区 testReport 显示完成 vs 总体计划称未交付 vs 本工作树代码未含交付物） | 证据互相矛盾，超出只读审查可裁定范围 | 负责人裁定并统一口径（处理见 K-2） |
| W-3 | 无法确认 | `Server/design.md:94`、V78 | 「参数名直接跟在 `in` 之后、不带括号」在本框架的展开形态无运行证据 | 设计已自述需实施时静态取证，属已登记的实施验证点；审查无法取得运行事实 | 无需本轮决策，实施时按 V78 取证即可 |
| W-4 | 无法确认 | 残余风险表项数 | 审查要求附称风险表「现为 8 项」，实际表内为 7 项（`testReport.md:25-31`） | 计数差异在输入材料侧；7 项本身逐条核对无重复、与设计一致（S5-D3 脏归属、S5-D4 部署影响均在其中，重审条件可执行） | 无需决策，仅说明 |

## 五、覆盖度自评

- **实际读取**：六份阶段文档全文（841 行）；SRS F04–F07 及医院接入接口表、设计约束；CONTEXT.md 在线互认相关段；三份 UML 与命令出参整改矩阵；总体计划 design §4.7 与 impl §7/§8；stage-template 与 plans README；`.agents/instructions/` 14 份规范；`server/` 关键代码（聚合实体、四份映射 XML、应用服务 18 方法、TrustedScopeResolver、架构守卫、枚举注册表、静态探测断言）；主工作区阶段 4 文档与交付代码/脚本（经子代理只读核对）。
- **机械核对十三项执行方式与结果**：①编号完整性——Python 脚本抽取表行，101 行、主编号连续无重无缺、六个后缀均紧随主编号，未命中问题；②台账完整性——逐条人工核，24 条连续且均有落点，未命中；③五类穷举双向——由探针建立约 164 条上游条款清单双向映射，命中 1 处缺口（K-3），其余未承接项经分类判断不构成缺口（见覆盖评估）；④批次互覆盖——脚本展开两份 impl 区段表达式，101 全覆盖、无重复无虚构，未命中；⑤链接——脚本解析六文档全部 Markdown 链接，零断链，未命中；⑥禁止性内容——Grep 关键词全目录扫描，阶段文档零命中；⑦设计不含实施细节——逐节核，未命中；⑧决策依据回查——逐条打开被引条款核对方向，全部真实支持（技术理由类已自述），未命中；⑨条目重复——101 行两两归一化相似度穷举（difflib，阈值 0.72），无实质重复，最高对 V42/V70b 为跨接口对称条目；⑩规格对应——故事 79、决策 71 与要求条数一致，抽查+全量结构核对，无臆造/冲突，内部重复 1 处（K-7）；⑪同口径四处一致性——11 项口径逐条比对，10 项完全一致，1 项例外（K-4）；⑫WorkUnit 声明与取值一致性——脚本抽取逐行取值与口径段两档清单比对，是 15=15、否 40=40 逐一相符、46 条 N/A、无第四取值；清单与表行完全一致，但两档定义与成员语义存在不符（K-1）；⑬矩阵规模表述——101 行口径三处一致，未命中（总体计划中的旧规模句属 K-2 附带发现）。
- **上游清单建立方式**：SRS F04–F07 按前置/后置/主事件流/备选流/业务规则五类逐条编号建单（探针完成），加 UML 命令/实体/事件/读模型与 CONTEXT.md 14 项约定，作为覆盖比对基准；无承接项全部列出并分类处置。
- **严重度判定说明**：K-1 记 medium——列定义自相矛盾会误导后续增补条目与取证方式，但 101 行现有取值自洽且可低成本修订；K-2 记 medium——事实性跨文档矛盾且阻塞实施启动判断，但本阶段尚不进入实施、有明确裁定路径，未达 high 的「已造成契约不一致」；K-3 记 medium——与第三轮已整改的 R-09 同型同标准；K-4–K-7 记 low——表述/清单/观察口径类瑕疵，不动结论。
- **本轮整改总体判定**：①本轮整改**未引入新的 blocker 或 high**（K-1–K-7 均为本轮新发现或历史残留，3 条 medium、4 条 low）；②四种已知失效模式：模式一（只改一处）——机械核对 11 基本消除，仅剩 K-4 一处正文级残留；模式二（写说明不改表行）——已消除（R-02 清单与表行脚本比对一致，失败粒度断言均在表行内）；模式三（凭本工作树推断上游）——已消除（R-01/R-04 与 UML/SRS 均回到原文核对）；模式四（同口径两处依据来源不同）——主体已消除，残留一处轻量变体（K-5，结论一致、清单不同）；③**未发现第五类失效模式**。
- **审查边界与局限**：未读取 appsettings.Development.json（版本控制外）；阶段 4 材料经主工作区只读核对，其未声明阶段完成（testReport:315），K-2 的最终裁定依赖负责人；框架行为（事务回滚、事件可见性、集合参数展开）无运行事实可取证，矩阵自身已按 Blocked/NotRun 与 N/A 口径处理，与本审查无关；K-1 的修订方案选择权（改定义 vs 改取值）建议由整改方按推荐方案①执行，如负责人倾向保持取值不动则选①。
