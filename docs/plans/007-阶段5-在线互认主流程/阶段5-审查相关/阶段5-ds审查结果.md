# 阶段 5 任务票审查结果（ds）

- 审查时间：2026-09-19
- 审查对象：`docs/plans/007-阶段5-在线互认主流程/阶段5-Tickets/` 下实际读取的 10 份文件，行数与勾选项数以本轮实际读取为准。

| 文件 | 行数 | 勾选项数 |
|---|---|---|
| `README.md` | 50 | 0 |
| `00-契约删列与枚举登记.md` | 18 | 6 |
| `01-请求与事件契约、读模型重建.md` | 20 | 8 |
| `02-建表脚本与SqlMap映射语句.md` | 23 | 11 |
| `03-匹配查询（F04）.md` | 29 | 17 |
| `04-处理结果提交（F05）.md` | 27 | 15 |
| `05-引用详情（F06）.md` | 27 | 15 |
| `06-引用结果提交（F07）.md` | 26 | 14 |
| `07-静态收尾与实现约束核对.md` | 19 | 7 |
| `08-真实宿主链路验收与阶段报告.md` | 20 | 8 |

- 核对依据（实际读取）：`docs/plans/007-阶段5-在线互认主流程/` 的 `design.md`（关键决策 S5-D1–S5-D24，含 S5-D17）、`Server/design.md`（280 行，含验证矩阵 105 行、`条目性质与负向要求的承接` 段、`端点与分部文件清单的同步` 段）、`impl.md`、`Server/impl.md`（批次 0–6 与矩阵编号归属）、`阶段5-规格.md`（实现决策、测试替身决策、范围外清单）、`testReport.md`；`docs/plans/006-阶段4-报告采集与生命周期/阶段4-Tickets/` 的 `README.md` 与 `00`／`01`／`02`／`03`／`04`／`05`／`06a`／`06b`／`07`／`08`／`12`／`13`／`14`／`15`／`16`／`17`（先例对照）；`docs/plans/README.md`；`.agents/instructions/` 的 `agent-conduct.md`、`planning-and-evidence.md`、`testing-baseline.md`、`project-context.md`（§3.2 再生成与保护清单）；`docs/需求规约SRS.md` 的 F04–F07、设计约束、医院接入接口；`CONTEXT.md` 在线互认各段；`docs/uml/1-MedicalRecognitionReport.wsd`、`docs/uml/4-MedicalRecognitionExternalSystemQuery.wsd`；`server/` 的测试工程（`Architecture/SourceGuardReuseTests.cs`、`Stage1ArchitectureTests.cs`、`Stage4EndpointTests.cs`、`Stage1SqlMapProbeTests.cs`、`Stage2EnumMetadataQueryTests.cs`）、领域实体与 DTO 源文件、`Repository/Scripts/`；`git status`／`git diff --check`。
- 未读取到的材料：无缺失文件。未取得的证据：未运行测试、构建与宿主链路，未连接目标数据库，未测量各票的实际上下文占用。
- 写入前确认：已彻底清空该文档全部历史内容，本轮结果从头写入（是）。判断依据只来自本轮实际读取到的票面、上游文档、代码与脚本。

## 一、逐份结论

| 文件 | 结论 | 核对了哪些载体 | 残余或问题编号 |
|---|---|---|---|
| `README.md` | 部分成立 | 票一览 9 行与九张票的票号、标题、`Blocked by` 逐项相符；第 19 行的并列关系与票一览自洽；14 条约定逐条回上游依据（约定 2 与 `testing-baseline.md` §3 一致；约定 7 与 `Server/design.md:134-136` 一致；约定 8 与 `:138` 一致；约定 11 的三项依据落点经 SRS F04 业务规则 8／9、F06 业务规则 9、设计约束 17、`CONTEXT.md` 两段、`1-MedicalRecognitionReport.wsd`、`4-MedicalRecognitionExternalSystemQuery.wsd:55` 核对该成立；约定 12 各项分别对应 S5-D1、设计「不新增权限判定」段、V86、`Server/design.md:162`、规格实现决策 6、设计范围排除项、`project-context.md` §3.2、`SourceGuardReuseTests` 的受治理清单；约定 13 对应 `Server/design.md:158` 与 `阶段5-规格.md:353`；约定 14 对应 S5-D4 与 V90／V89）；层级词表逐行与脚本抽取的各票矩阵层级取值比对 | `T-04`、`T-05` |
| `00-契约删列与枚举登记.md` | 成立 | 四个删列对象在代码中确实存在该属性（`Domain/…/RecognitionProcessingResult.cs:27`、`RecognitionReference.cs:23`、`Contracts/…/Dtos/RecognitionProcessingResultDto.cs:27`、`RecognitionReferenceDto.cs:23`）；V90 与 `Server/design.md:271` 同口径；V85 与 `Stage2EnumMetadataQueryTests.Stage2_contract_enums_match_the_registry_whitelist` 的四侧断言（第 560、562、564、566 行）及排除清单的两项一致；外部前置的参数编码确认有 V90 与 `Server/design.md:104` 的依据 | 无 |
| `01-请求与事件契约、读模型重建.md` | 成立 | V81 原文含引用详情请求的四项负向与标识类型要求；V82 含「不含固定的互认超期时间字段」；V83 含「PDF 与影像入口不携带独立有效期，也不携带一次性或消费状态」；十个读模型与三个事件与 `Server/design.md:24-25`、`:35` 一致；`MedicalRecognitionRequestValidator` 与 `RequestValidationProbeTests.cs` 存在；口径段已写明 V84／V85 各自承接的面 | 无 |
| `02-建表脚本与SqlMap映射语句.md` | 部分成立 | V73 含「精度不低于秒」、V75 含四条索引与命名一致、V76 的作用域与语句标识口径、V77 的双向断言（`Stage1SqlMapProbeTests.cs:160-163`、`:168-178`）、V78–V80 的候选查询与方言要求、V73b 的报告使用关系载体，四张表脚本现状（无前缀、无索引、仍含 `standard_project_code`）与票面要求相符；口径段已写明 V76 的数据库面归票 08 | `T-01`、`T-06` |
| `03-匹配查询（F04）.md` | 成立 | 29 个矩阵编号全部真实存在且归属批次 2，与 `impl.md` 的批次归属段一致；V12 与 V13 已分列两条；五类排除时长取值与五个编号数目相符；第 25 行已改为写入口的断言同步并指明查询入口归票 05，与 `Server/design.md:160` 的三类断言口径一致；外部前置三项与 `Server/design.md:138` 一致 | 无 |
| `04-处理结果提交（F05）.md` | 成立 | 27 个编号归属批次 3 与票面一致；V23 的采纳次数与被互认次数口径与 SRS F05 后置条件 3 一致；V95 的负向要求与 SRS F05 主事件流 2、业务规则 8、F07 业务规则 7／8 一致；口径段已写明 V95 的处理结果侧面 | 无 |
| `05-引用详情（F06）.md` | 成立 | 16 个编号归属批次 4 与票面一致；V48／V52／V56／V58b 的口径与 SRS F06 业务规则 10／12／13／14 一致；第 24 行已承接新增查询入口后的三类查询侧断言同步，与本票交付查询入口的事实一致 | 无 |
| `06-引用结果提交（F07）.md` | 成立 | 17 个编号归属批次 5 与票面一致；第 3 行的落库口径已改为「只增加引用次数（采纳次数、来源医院被互认次数与预计节省金额不变）」，与 V72、SRS F07 业务规则 12、`CONTEXT.md:477` 一致，且与本票第 19 行勾选项自洽；V70b／V71／V72b 的口径与 SRS F07 业务规则 9／10／11 一致 | 无 |
| `07-静态收尾与实现约束核对.md` | 成立 | V86／V87／V88 归属批次 6 与票面一致；V87 的「三个入口声明、引用详情不声明」与全仓 6 处 `[WorkUnit(UseTransaction = true)]` 的分布（应用服务 4 处 + 控制器 2 处）一致；第 14 行已覆盖重跑生成器、保护清单与守卫消费方清单登记 | 无 |
| `08-真实宿主链路验收与阶段报告.md` | 部分成立 | V1／V23／V59 的整批零提交面、V73／V75／V76 的数据库面、「真实数据库」列为「是」的 71 条与判据落点（`Server/design.md:138`）核对成立；第 14 行的回读归属已收窄为 V1、V46 并说明两个写入口的回读路径 | `T-02`、`T-03` |

## 二、问题清单

严重度判定标准：`blocker` 指按票执行会产出错误结果或无法开工；`high` 指会造成返工或与上游口径不一致；`medium` 指局部不一致或证据缺口；`low` 指表述、编号、引用类瑕疵。

| 编号 | 严重度 | 位置（文件 + 段落/行号） | 问题描述（含原文摘录） | 对照依据 | 建议处理 | 是否阻塞开工 |
|---|---|---|---|---|---|---|
| `T-01` | medium | `02-建表脚本与SqlMap映射语句.md` 第 3 行与第 11-21 行勾选项段 | What to build 声明「四个映射在既有实体作用域下补齐写入与读取语句（……**映射文件留在程序集根下一级目录**）……既有语句清单断言与**映射位置守卫**同步通过」，但勾选项段没有对应项：第 16 行只保留「四个映射的作用域为各自实体名，物理表映射与既有约定一致（V76）」，第 17 行只覆盖语句清单断言。按票执行时该项无检查点 | 约定 5（每条勾选项内联编号；声明的要求应可勾选核对）；`Server/design.md:96` 的「映射文件的存放位置由阶段 4 确定……本阶段四个映射补齐语句时须留在该位置，不新建分组子目录」；`Server/impl.md:17` 同口径；既有守卫用例 `Stage1SqlMapProbeTests.Sql_map_resources_stay_directly_under_one_folder` | 在票 02 补一条勾选项：「映射文件与仓储类同处程序集根下一级目录，未新建分组子目录（无矩阵编号：改动范围约束，由 `Stage1SqlMapProbeTests.Sql_map_resources_stay_directly_under_one_folder` 守卫）」 | 否 |
| `T-02` | medium | `08-真实宿主链路验收与阶段报告.md` 第 3 行与第 12 行 | 同一票内两处取证方式相反：第 3 行「参数取值缺失与非法与参数服务异常三类前置**在真实参数服务上取证**」；第 12 行「参数严格失败语义**经共享桩取证**……（V89、V49、V50、V51；取证方式见 `阶段5-规格.md` 的测试替身决策）」。且 V89、V49、V50、V51 在矩阵中的「真实数据库」列均记「否」，其取证层级为 `Application` 与 `Query`，不属于本票口径段声明的真实宿主面；票 03 第 23 行与票 05 第 14 行已各自承接这批条目的取证 | `阶段5-规格.md:333`（系统参数服务新增共享桩，桩需可配置返回值、缺失与抛出异常三类行为）；`Server/design.md` 矩阵 V89、V49、V50、V51 行的「真实数据库」列取值；`Server/design.md:158`（请求校验取证的批次归属）；票 08 第 20 行的口径段「本票覆盖真实数据库与宿主面」 | 二者取一：把第 3 行的该分句改为「参数三类前置由票 03 与票 05 以共享桩取证，本票复核其结论」，保留第 12 行为复核项；或保留第 3 行的真实参数服务取证，把第 12 行的共享桩表述移入票 03／05 并从票 08 删除 | 否 |
| `T-03` | medium | `08-真实宿主链路验收与阶段报告.md` 第 3 行与第 15 行 | 第 3 行「……引用事实的唯一索引**在并发重复写入下**表现为业务拒绝」要求并发场景下的行为证据；第 15 行「引用事实表按互认匹配项标识的唯一索引使重复写入表现为业务拒绝」为重复写入（可顺序构造） | `design.md:80` 的 S5-D17「不加锁、不重试、**不做并发测试**」；`阶段5-规格.md:374` 范围外第 12 条「并发压测、悲观锁、自动重试与**并发专项用例**」；`Server/design.md:118`「不加锁、不重试，不做并发压测」；矩阵 V75 行已把「重复提交的幂等判定与冲突拒绝属行为面」指给 V70b | 把第 3 行的「在并发重复写入下表现为业务拒绝」改为「在同一匹配项重复写入时表现为业务拒绝」，并按 V70／V70b 的冲突与幂等判定取证；并发的撞键兜底只作为 V75 的索引形态在目标库核对（即第 15 行的现有写法），不另建并发用例 | 否 |
| `T-04` | low | `README.md`「层级词表对照」表第 44-50 行 | 词表的「票」列漏列票 04：票 04 引用的 27 个编号的层级取值为 `Contract/Domain`、`Domain`、`Domain/Repository`（脚本抽取），这些取值分别列在第 46 行（票 03）与第 48 行（票 06）名下，票 04 的票号未出现。另外第 50 行列票 08 的层级取值只写了 `Application`（V89、V92）与 71 条真实数据库条目，未含票 08 第 12 行引用的 V49／V50／V51 的 `Query` | 矩阵「层级」列实际取值（脚本按票抽取）；票 04 第 27 行与票 08 第 12 行的引用编号 | 第 46 行的「票」列改为「03、04」；第 50 行的取值列补 `Query`（V49／V50／V51 的面） | 否 |
| `T-05` | low | `README.md` 约定 12 末句（第 34 行） | 该约定在保留原有条目的同时新增「**不重跑后端代码生成器、不覆盖再生成保护清单中的配置与启动文件**（见 `project-context.md` §3.2）」与守卫消费方清单登记，末句仍写「该约束由票 07 以**静态断言**核对」。生成器与保护清单两条的核对手段是工作区差异检查，静态断言无法判定 | `project-context.md` §3.2 的再生成与保护清单；票 07 第 14 行（实现约束核对）与第 16 行（构建、静态检查与 `git diff --check`） | 末句改为「该约束由票 07 以静态断言与差异检查核对」 | 否 |
| `T-06` | low | `02-建表脚本与SqlMap映射语句.md` 第 20 行 | 勾选项「脚本交付暂停点：四张表由负责人建成后，**由票 08** 在目标库核对表结构、列注释与索引（无矩阵编号：脚本交付暂停点检查；未建成时本项记 `Blocked`）」的判定动作由票 08 执行，票 02 的执行者无法判定本项通过与否，与该项作为勾选项的形态不符 | 约定 5 与勾选纪律（勾选项须可由本票判定）；票 02 第 23 行的口径段已把 V73／V75／V76 的数据库面指给票 08；票 08 第 15 行已承接该核对 | 把该行改为票面下方的说明（「脚本交付后暂停；表结构、列注释与索引核对由票 08 承接」），或把判定条件改写为本票可判定的形式（如「四张表脚本已交付、索引与列注释声明完整，并已注明暂停点」） | 否 |

## 三、切分评估（独立判断）

**批次 0–6 → 票 → 矩阵三段覆盖。** 批次 0 由票 00（契约删列、枚举登记、参数常量）与票 01（请求类型、事件、读模型）承接，批次 1 由票 02 承接，批次 2–5 由票 03–06 一一对应，批次 6 的静态核对与阶段报告面由票 07 承接、其「适用测试」面由票 08 承接；没有批次项无票承接，也没有票声明批次之外的内容。矩阵覆盖：脚本抽取九张票引用的全部编号，得到 105 个不同编号，与矩阵 105 行完全一致，**无编号无人承接**，也无票引用不存在的编号。归属正确性：票 00／01 → 批次 0、票 02 → 批次 1、票 03 → 批次 2、票 04 → 批次 3、票 05 → 批次 4、票 06 → 批次 5、票 07 → 批次 6，与 `impl.md` 的「矩阵编号的批次归属」段逐条相符。多票共同声明的四处（V85 在 00／01、V84 在 01／05、V95 在 04／06、V88 在 05／07）与 V73／V75／V76 的数据库面（02／08）都在各票口径段写明了各自承接的面。既有同步面承接情况：枚举排除清单（票 00）、语句清单断言（票 02）、写入口契约与写侧分部文件（票 03／04／06）、查询入口契约与查询侧分部文件数量（票 05）、请求校验取证（票 01／03／04／06，票 07 复核）、架构守卫消费方清单（票 07）、生成器与保护清单（票 07）；映射位置守卫只在票 02 的 What to build 出现，勾选项段无承接口（`T-01`）。

**需要拆分的票。** 票 03（29 个矩阵编号、17 个勾选项）与票 08（71 条真实数据库条目、8 个勾选项）的体量居首，是否超出一个上下文窗口无法在只读条件下测量（见 `UA-101`）。若实测超出，按下列面拆分并可立即调整依赖边：

1. 票 03 拆为 03a「项目编码校验、空结果与失败面」（V3–V8、V89、V91、V92）、03b「候选筛选与选优面」（V9–V20b）、03c「写入、事件与提醒面」（V1、V2、V21、V22、V22b、V93、V94）；依赖边为 03a ←（01、02）、03b ← 03a、03c ← 03b，票 04 的 `Blocked by` 改为 03c。
2. 票 08 拆为 08a「表、索引与映射的数据库面」（V73、V74、V74b、V75、V76）、08b「参数与认证面」（V89、V49、V50、V51、V92）、08c「写入与原子性面」（V1、V23、V59 的整批零提交与其余「真实数据库」列为「是」的条目）；三票均 `Blocked by: 07`。

**不建议合并的票。** 票 07 与票 08 不合并：合并后一票要同时覆盖跨接口静态面与 71 条真实数据库条目，且阶段 4 先例把静态核对（票 16、17）与宿主验收（票 12）分开，合并会失去「静态面可在建表前先行完成」的可并行性。票 01 与票 02 也不合并：两者虽都只依赖票 00，但分别承接契约层与物理层。

**依赖图评估。** 九张票的 `Blocked by` 与 README 票一览逐项相符，拓扑序为 00 → 01／02 → 03 → 04 → 05 → 06 → 07 → 08，无环、无漏边、无多余边。票 06 依赖票 05 不是功能门禁（引用结果提交的功能前提是票 03 与票 04 的产物），该边与 `impl.md` 批次 5 的前置「批次 4」一致，按约定 6 票面不得改动批次表，列为分歧（`DA-104`）。外部前置与票间依赖已分离（约定 1）；外部前置登记情况：票 00（参数编码确认）、票 02（脚本交付暂停点）、票 03（四张表建成、三个参数配置、参数编码确认）、票 04／05／06（四张表建成与该组前序产物）、票 07（无）、票 08（四张表建成、三个参数配置、宿主可用、部署宿主的外部服务代理映射已就位）；应用层的组织服务、用户服务与参数服务由共享桩替代（`阶段5-规格.md:332-333`），真实服务可用性只需在票 08 登记，登记位置成立。

**应设而未设的票、不应设而设的票。** 未发现应设而未设的票：既有断言同步面与实现约束面已分别落在票 00、02、03、04、05、06、07，其中映射位置守卫的落点缺失按 `T-01` 在票 02 内补齐即可，不必新增票。未发现不应设而设的票：票 00 与票 02 虽都只依赖票 00，用途不同（契约与物理层），合并会使物理层无法在契约确认后立即开始。

## 四、设计选择分歧与无法确认项

| 编号 | 类型 | 位置 | 分歧点或事项 | 我的看法或所需证据 | 需要负责人决策的点 |
|---|---|---|---|---|---|
| `DA-101` | 分歧 | 票 03、票 08 | 票 03 载 29 个矩阵编号与 17 个勾选项，票 08 载 71 条真实数据库条目的取证 | 粒度属取向；按勾选项数量看两票居首，是否超出一个上下文窗口无法在只读条件下测量 | 是否按第三节的 03a／03b／03c 与 08a／08b／08c 拆分 |
| `DA-102` | 分歧 | 票 00–07 与阶段 4 票 04、16、17 | 阶段 5 把既有断言同步与实现约束核对折进能力票与票 07；阶段 4 为它们各设一张票 | 折进后各票口径段已声明自己承接的面；代价是同一断言面分散在五张票内，读者需要跨票汇总 | 是否恢复一张独立的「既有测试断言同步」票 |
| `DA-103` | 分歧 | `阶段5-Tickets/` 目录 | 阶段 4 有 `请求样例-检验报告.md` 与 `请求样例-检查报告.md`，阶段 5 无请求样例文件 | 阶段 4 的样例对应大体量 JSON 文档，阶段 5 的四个请求是结构化小字段，可由阶段差异解释 | 是否为四个请求补样例文件 |
| `DA-104` | 分歧 | 票 06 `Blocked by: 05` | 引用结果提交的功能前提是匹配项与已保存处理结果（票 03、04 的产物），引用详情不是门禁 | 该边与 `impl.md` 批次 5 的前置「批次 4」一致，票面不得改动批次（约定 6） | 是否保留该顺序边，或先在批次表调整 |
| `DA-105` | 分歧 | 各票外部前置行与阶段 4 同名行 | 阶段 4 每票重复「上述前置未就绪时……记 `Blocked`，其余未执行项记 `NotRun`」；阶段 5 只在 `README.md` 约定 2／8 与票 08 第 16 行写判据 | 两种写法都能落到 `testing-baseline.md` 的词汇，差别在逐票的可判定性 | 是否在每票外部前置行统一补该判据 |
| `UA-101` | 无法确认 | 票 03、票 08 | 两票能否在一个上下文窗口内完成 | 需实测执行时的上下文占用；本轮只读 | — |
| `UA-102` | 无法确认 | 矩阵「真实数据库」列为「是」的 71 条 | 71 条能否全部在真实宿主上构造（部分前置数据依赖既有测试身份与阶段 1–4 配置数据的现状） | 需连接目标库与宿主；本轮未连接 | — |
| `UA-103` | 无法确认 | `Server/design.md` 的条目性质段 | 该段把 SRS F04 业务规则 1「查询截止点使用平台收到本次查询的时间」映射到「V13 的查询时点边界与 V81 的请求字段负向」；V13 的预期结果只断言「匹配基准时间晚于查询时点的报告不作为候选」，覆盖的是该规则的后果面。该映射位于设计文档，不在本轮审查对象内，未判为票面问题 | 需要一条只断言「截止点取平台接收时间」的条目才能判为覆盖 | — |

## 五、对本审查要求本身的事实性异议

1. **第五节的自述表与票面实际一致，未命中异议。** 逐项核对票号、标题、`Blocked by`、对应设计批次与票面声称的矩阵编号：九张票的 `Blocked by` 与 README 票一览逐项相符；票 00（V85、V90）、票 01（V81–V84 与 V85 的读模型面）、票 02（V73、V73b、V74、V74b、V75–V80）、票 03（V1–V22、V20b、V22b、V89、V91–V94）、票 04（V23–V45、V35b、V36b、V45b、V95）、票 05（V46–V58、V58b）、票 06（V59–V72、V70b、V72b）、票 07（V86–V88）均与实际相符；票 08 的「不新增编号」与实际相符。
2. **第三节的环境说明与实际一致，未命中异议。** `阶段5-Tickets/` 在 `git status --short` 中显示为未跟踪 `??`；`server/Dy.MedicalRecognition/appsettings.Development.json` 存在且可读；阶段 4 的任务票目录为本目录约定先例。
3. **第七节第 1 项的「共 105 行」与实际计数一致**（V1–V95 主编号加十个后缀条目），未命中异议。
4. **附节自述的已确认错误方向经核对未复现**：`Server/design.md` 的条目性质段引用的 F06 业务规则 15 与 17 与 SRS 同版编号一致（SRS F06 业务规则 15 为「不建设一次性读取、读取后立即失效或消费状态」，17 为「PDF 下载入口不设置独立期限」）；矩阵 V82 行已含「不含固定的互认超期时间字段」；票面未发现泛化到不可判定的勾选项（待明确处已列入 `T-03`、`T-04`、`T-05`）。未发现要求本身的事实性异议。

## 六、覆盖度自评

**已覆盖的审查维度**：覆盖完整性（批次 → 票 → 矩阵三段）、切分粒度与可执行性、依赖边正确性、票面可判定性、与上游的一致性（设计决策、验收与停止条件、实现约束、批次表、规格的测试替身与范围外清单）、与阶段 4 票目录约定的一致性、风险与漏承接面、切分主张的可否证性。**未完全覆盖的维度**：各票的实际上下文占用（`UA-101`）、71 条真实数据库条目的可构造性（`UA-102`）、设计文档内部映射的覆盖强度（`UA-103`）。

**机械核对的实际执行方式与命中结果**

| 项 | 方式 | 结果 |
|---|---|---|
| 1 矩阵编号的引用与归属 | 脚本抽取九张票的 `V\d+[a-z]?` 引用，与矩阵编号集合双向比对；按票抽取「层级」列取值；逐条对照 `impl.md` 的批次归属；检查多票共同声明处的口径段 | 未命中失效引用与无人承接编号（105/105）；归属与批次表逐条相符；多票共同声明的口径段已写全；命中 1 处层级词表与票号不符（`T-04`） |
| 2 依赖图 | 逐票核对 `Blocked by` 与 README 票一览；检查环路、依据、漏边与外部前置是否混入依赖图 | 未命中环、漏边与多余边；外部前置与票间依赖已分离；外部前置登记已覆盖建表、参数、宿主与外部服务代理映射 |
| 3 票面纪律 | 脚本检索 `Passed`／`验收通过`／`已完成`／`批注`／`负责人裁定`／`回复原文`／`确认来源`／审查方编号／审查相关目录；脚本检查每条勾选项是否内联编号或标注「（无矩阵编号）」 | 未命中无证据的通过性表述与对话痕迹；命中项仅为约定文本自身的引用（约定 2／3 的词汇表、约定 9 的范围排除），与阶段 4 `README.md` 同名写法一致；全部勾选项均已内联编号或标注「（无矩阵编号）」；命中 1 处票内取证方式自相矛盾（`T-02`）与 1 处与范围外清单冲突（`T-03`） |
| 4 票面自定业务语义 | 逐票把 What to build 与勾选项回设计、规格、SRS、`CONTEXT.md` 与矩阵核对 | 未命中票面自定业务语义；命中 1 处勾选项在 What to build 中无检查点（`T-01`）与 1 处勾选项由他票判定（`T-06`） |
| 5 README 与实际文件的一致性 | 比对票一览与文件名、票号、`Blocked by`；逐条回查 14 条约定的上游依据；用脚本抽取各票引用编号的层级取值与层级词表比对 | 命中 2 处（`T-04`、`T-05`）；约定 11 的依据落点、约定 12 的生成器与保护清单条目、约定 13 与 `Server/design.md:158` 的对应关系核对成立 |
| 6 外部前置完整性 | 从 `Server/design.md:138`、`阶段5-规格.md` 的测试替身与范围外清单、`project-context.md` §3.2、阶段 4 各票外部前置行提取门禁项，逐票核对 | 未命中漏登记：建表、参数配置与编码确认、宿主可用、外部服务代理映射（票 08）均已登记；应用层以共享桩替代真实外部服务的口径有规格依据 |
| 7 文件与文本完整性 | 逐文件统计 U+FFFD（全部 0）；检查行尾空白与制表符（全部 0）；统计行数与勾选项数；`git diff --check`（返回 0；票目录未跟踪，另以逐文件空白检查替代） | 未命中 |

**上游执行要求清单的建立方式与比对结果**：以 `impl.md`／`Server/impl.md` 的批次 0–6（范围、前置、矩阵编号归属）为骨架，叠加矩阵 105 行的编号与层级、`design.md` 的 24 条决策与验收停止条件、`Server/design.md` 的四类既有断言同步与映射位置守卫、端点与分部文件清单段、条目性质与负向承接段、`阶段5-规格.md` 的请求校验口径／测试替身／范围外清单，形成 7 项批次范围 + 105 个矩阵编号 + 9 类承接面（语句清单断言、枚举排除清单、写入口契约与写侧分部文件、查询入口契约与查询侧分部文件数量、请求校验取证、映射位置守卫、架构守卫消费方清单、生成器与保护清单、事务声明分布）+ 外部前置 5 项的清单。比对结果：批次范围与 105 个编号全部被票承接；承接面 9 类中 8 类有勾选项承接，映射位置守卫只在 What to build 出现（`T-01`）；外部前置 5 项均已登记。

**本轮的严重度判定说明**：三条 medium 分别是一处检查点缺失（`T-01`）、一处票内取证方式冲突并与矩阵层级不符（`T-02`）、一处与设计决策及范围外清单冲突的取证要求（`T-03`）；三条 low 为序号与取值、核对手段措辞、勾选项判定主体的瑕疵。本轮未发现 `blocker` 与 `high`。

**十份文件的结论汇总**：成立 7 份（票 00、01、03、04、05、06、07），部分成立 3 份（README、票 02、票 08），无不成立。**本轮审查的边界与已知局限**：审查全程只读，未运行测试、构建与宿主；未连接目标数据库；票面所指前置数据的现状未逐一核对；上下文占用的判断留给负责人按执行实测决定。
