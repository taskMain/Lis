# 阶段 5 任务票审查问题追踪（票审查轮）

本文记录**经交叉核对确认的正式问题**，供逐条整改与标记。三份审查方的原始结果不并入本文，只在「来源」列标注其编号以便回查。

**本文档为整体重写**：本轮开始前该文档已被清空为零字节，本次从头写入，不保留、不引用任何历史轮次的口径与编号。审查相关目录内的历史材料不作为本文依据。

---

## 一、交叉核对说明

### 1.1 来源与规模

本轮收到三份任务票审查结果，对象均为 `阶段5-Tickets/` 的 README 与九张票：

| 审查方 | 问题条数 | 分歧 | 无法确认 |
|---|---|---|---|
| ds | 14（DS-101～DS-114） | 5 | 3 |
| glm | 9（H-1、H-2、M-1、M-2、L-1、L-2、L-3、L-4、L-5） | 3 | 1 |
| kimi | 6（K-01～K-06） | 5 | 2 |

三份共提出 **29 条问题主张**（ds 14、glm 9、kimi 6）。核对后：**成立 17 条**（去重合并后见第三节 Q-01～Q-17）、**不成立或降级 4 组**（第二节 M-01～M-04）、其余归入设计选择分歧与无法确认项（第五节）。

### 1.2 核对方式

每条主张都回到载体独立复核，不采信票面自述、也不采信审查方自述：票面原文（文件 + 行号）、`impl.md`／`Server/impl.md` 批次表、`Server/design.md` 矩阵行原文、SRS 条款、`阶段5-规格.md` 的测试替身决策、`.agents/instructions/project-context.md` 的生成来源与保护清单、`server/` 的测试工程、阶段 4 票目录先例。脚本核对项：各票引用编号的实际层级取值、矩阵「真实数据库」列为「是」的条数、矩阵编号与批次的归属。

### 1.3 三份的覆盖差异（决定各自的盲区）

| 事项 | ds | glm | kimi |
|---|---|---|---|
| 票 05「按患者与匹配标识」的请求侧错误 | ❌ 未命中 | ✅ 命中（H-2） | ❌ 未命中 |
| 票 08 参数取证方式与规格共享桩决策的冲突 | ❌ 未命中 | ✅ 命中（M-2） | ❌ 未命中 |
| 票 03／05 查询入口断言同步的错票与断档 | ✅ 命中 | ❌ 未命中 | ✅ 命中 |
| V84 匹配响应面无人承接 | 部分（DS-112） | ✅ 命中（M-1） | ❌ 判「跨票口径声明全部合规」 |
| README 层级词表与实际层级不符 | ✅ | ✅（L-4） | ✅（K-06） |
| 约定 12 缺生成器与保护清单约束 | ✅ 判 high | 反判「可由阶段差异解释」 | 未展开 |
| 外部服务可达性未登记为外部前置 | ✅ 判 medium | 反判「无漏登记」 | 反判「未命中」 |
| README 第 19 行自相矛盾 | ✅ | ✅ | ✅ |

**三份没有任何一份单独完整。** 三份一致命中的只有 2 条（票 06 口径冲突、README 第 19 行笔误）；单份独中且经核为真的有 2 条（票 05 请求侧、票 08 参数取证方式）。

---

## 二、误报、降级与理解偏差记录

| 编号 | 来源 | 主张 | 复核结论 |
|---|---|---|---|
| M-01 | ds `DS-103`（判 high） | README 约定 12 删去阶段 4 的「不重跑后端代码生成器」「不覆盖再生成保护清单」两条，须补回 | **降级为可选登记，不判缺陷**。事实核对成立：`project-context.md:74` 确有「再生成前必须保护的人工改动」清单，阶段 4 约定 12 确含该两条，阶段 5 五份文档检索「生成器／手工维护／保护清单／dy-codegen」命中 0。但同文件 `:72` 明确「报告生命周期、匹配、处理结果、引用和全部查询尚未进入生成范围，需要后续设计与实现」——本阶段产物本就不属生成物；该约束是常驻规范（对所有工作始终生效），阶段文档未重复登记属文档完整性问题，不会导致执行出错。**建议**：可在约定 12 补一句登记（便宜且与阶段 4 同形），但不作为缺陷计 |
| M-02 | ds `DS-106`（判 medium） | 外部服务（组织／用户／参数）可达性未登记为外部前置 | **不成立**。`阶段5-规格.md:332-333` 规定三类外部服务在测试侧使用**共享桩**（组织与用户沿用既有桩、参数服务新增桩），故票级取证不依赖真实服务可达；且 `appsettings.Development.json` 已含三项代理映射。真实可达性只在票 08 的宿主部署面成立，属部署清单事项。**建议**：可选在票 08 外部前置补一句「部署宿主的外部服务代理映射已就位」 |
| M-03 | glm `L-5`（判 low） | 票 04／05／06 把票间产物依赖写入「外部前置」行，违反约定 1 的两分法 | **不成立（属先例写法）**。阶段 4 先例同形：`阶段4-Tickets/04-既有测试断言同步.md` 的外部前置行写「无（本票只依赖票 01 的物理映射声明与票 03 的读模型契约）」，`05-报告与版本基础链路.md` 外部前置行含「批次一 3 张表由负责人建表确认」与状态判据。**建议**：可选统一为「测试数据说明」措辞（属表述取向） |
| M-04 | kimi 逐份结论与归属核对 | 「跨票拆分的票均在口径段写明各自承接的层级面，符合 README 约定 5」 | **不成立**：`01-…读模型重建.md` 的口径段只写 V85 的读模型面，**未声明 V84 的匹配响应面**；票 02 与票 04 也未写各自的数据库面与 V95 侧面。kimi 该判据为漏检（见 Q-05、Q-09） |

---

## 三、问题清单

严重度口径：`blocker` 按票执行会产出错误结果或无法开工；`high` 会造成返工或与上游口径不一致；`medium` 局部不一致或证据缺口；`low` 表述、编号、引用类瑕疵。**本轮无 blocker。**

| 编号 | 状态 | 严重度 | 位置 | 问题描述 | 对照依据 | 拟处理 | 来源 |
|---|---|---|---|---|---|---|---|
| Q-01 | `Fixed` | high | `06-引用结果提交（F07）.md` 第 3 行（What to build） | 原文「整批原子保存、**按设计增加引用次数与来源医院被互认次数** → 登记引用结果已记录事件」。与上游及本票第 19 行勾选项相反 | `Server/design.md` 矩阵 V72「只增加引用次数；采纳次数、来源医院被互认次数与预计节省金额不变」；`docs/需求规约SRS.md:1889`（F07 业务规则 12）；`CONTEXT.md:477`；本票第 19 行 | 改为「整批原子保存，只增加引用次数（采纳次数、来源医院被互认次数与预计节省金额不变），登记引用结果已记录事件」 | ds `DS-101`、glm `H-1`、kimi `K-01`（三份一致） |
| Q-02 | `Fixed` | medium | `05-引用详情（F06）.md` 第 3 行（What to build） | 原文「医院 HIS **按患者与匹配标识**获取引用详情」。引用详情请求不含任何匹配标识，定位只靠患者证件与本次就诊 | `docs/需求规约SRS.md:1645`（F06 前置条件 2「不提交平台患者 ID、互认匹配记录 ID、互认匹配项 ID 或互认时间」）；矩阵 V81；票 01 的引用详情负向字段勾选项 | 改为「医院 HIS 按患者证件类型与号码、本次来源就诊类型与流水号获取引用详情」 | glm `H-2`（单份命中，经核为真） |
| Q-03 | `Fixed` | medium | `03-匹配查询（F04）.md` 第 24 行；`05-引用详情（F06）.md` 勾选项清单；`04`／`06` 的同类勾选项 | ①票 03 写「新增**查询入口**后……同批同步」，而票 03 交付的是三个写入口之一（匹配查询属写侧），入口类型写错；②唯一的新增查询入口（引用详情）由票 05 交付，票 05 **没有任何**既有断言同步勾选项——同步面断档；③票 03／04／06 的同类勾选项含「分部文件数量断言」，而该数量断言（查询侧端口与实现各二）只随查询入口变化 | `impl.md` 与 `Server/design.md:30`／`:32`（写侧新增三个入口、查询侧新增一个入口）；`Server/design.md:160`（三类断言：写入口方法集合 23 项、查询入口契约 11 项、`Stage1ArchitectureTests` 十一条固定方法名、查询侧分部文件数量断言各二）；`Stage4EndpointTests.cs:113`／`:129` | 票 03 该行「查询入口」改为「写入口」；**票 05 补一条勾选项**：「新增查询入口后，查询契约方法集合等值断言与查询侧端口与实现的分部文件数量断言同批同步（无矩阵编号：既有断言同步面）」；从票 03／04／06 的同类勾选项中删去「分部文件数量断言」 | ds `DS-102`、kimi `K-02`（两份一致） |
| Q-04 | `Fixed` | medium | `08-真实宿主链路验收与阶段报告.md` 第 2 项勾选 | 原文「参数严格失败语义**在真实参数服务上**取证：取值缺失、取值非法、参数服务异常三类前置」。与规格的测试决策冲突，且「参数服务异常」在健康真实服务上不可构造，该勾选项按原文不可判定 | `阶段5-规格.md:333`「**系统参数服务：新增一个共享桩**——本阶段是该服务首次被使用，桩需可配置返回具体值、返回值缺失、以及抛出异常三类行为，以覆盖严格失败语义」；矩阵 V89／V49／V50／V51 的「真实数据库」列均为「否」 | 改为「三类前置经共享桩取证（V89、V49、V50、V51）」；如需部署面观察另列一条并注明属部署清单事项 | glm `M-2`（单份命中，经核为真） |
| Q-05 | `Fixed` | medium | `01-请求与事件契约、读模型重建.md` 勾选项与口径段（第 20 行）；`05-引用详情（F06）.md` 第 12 项与口径段 | V84 的**匹配响应面**无人承接：票 01 无「匹配响应不含检查所见与检查结论」勾选项，其口径段也只写 V85 的读模型面、未声明 V84；票 05 第 12 项在勾选里同时断言两面，而口径段把匹配响应面划给票 01。两面声明与实际勾选互相不符 | 矩阵 V84「匹配响应不含检查所见与检查结论；引用详情含该两项」；`README.md` 约定 5（同一编号被多票声明时各票口径须写明承接面） | 票 01 补一条勾选项「匹配响应不含检查所见与检查结论（V84 的匹配响应面）」，并在口径段写明 V84 的匹配响应面归本票；票 05 第 12 项与口径段改为只承接 V84 的引用详情面 | glm `M-1`、ds `DS-112`（部分） |
| Q-06 | `Fixed` | medium | `README.md`「层级词表对照」表 | 词表列出的「对应矩阵层级列取值」与各票实际引用编号的层级不符：`Application` 误挂票 05（实际属票 03 的 V89／V92）；票 03 的 `Application` 与 `Contract/Domain`、票 04 的 `Contract/Domain`、票 05 的 `Contract`／`Static/Domain`／`Query` 均未列出；票 05 行列出其并不引用的 `Application` | 脚本按票抽取的层级取值：票 00 `Contract/Static`、`Static/Contract`；票 01 `Contract`、`Contract/Static`；票 02 `Repository/Static`、`Static`、`Static/DB`、`Static/Domain`；票 03 `Application`、`Contract/Domain`、`Domain`、`Domain/Application`、`Domain/Repository`、`Repository/Contract`、`Repository/Query`；票 04 `Contract/Domain`、`Domain`、`Domain/Repository`；票 05 `Contract`、`Query`、`Static/Domain`；票 06 `Contract/Domain`、`Domain`、`Domain/Repository`；票 07 `Static`、`Static/Domain` | 按上述实际取值重写词表的「票」列；票 05 行改为「`Query`、`Contract`（V84 面）、`Static/Domain`（V88 面）」，增列「参数与认证编排面 → `Application` → 票 03」 | ds `DS-105`、kimi `K-06`、glm `L-4` |
| Q-07 | `Fixed` | low | `08-真实宿主链路验收与阶段报告.md` 第 16 行 | 伞状勾选项「按票 03–06 的清单逐条取得证据」——票 03–06 并未列出该清单，权威清单是矩阵「真实数据库」列（实测记「是」共 **71** 条）；同句「记 `Blocked` 或 `NotRun`」未写选择判据，票面层面不可机械执行 | 矩阵「真实数据库」列脚本抽取（71 条）；`Server/design.md:138`「无上述证据的受影响运行项记 `Blocked`，其余未执行项记 `NotRun`」；`testing-baseline.md` 的两词定义 | 改为「按 `Server/design.md` 矩阵『真实数据库』列记『是』的 71 条（覆盖票 02–06 声明的编号）逐条取得证据；受负责人建表或参数配置阻断的记 `Blocked`，前置就绪而未执行的记 `NotRun`（判据见 `Server/design.md:138`）」 | ds `DS-110`①②、kimi `K-04`／`K-05` |
| Q-08 | `Fixed` | low | `08-真实宿主链路验收与阶段报告.md` 第 14 行 | 勾选项「四个接口成功后可经接口回读验证……（V1、V23、V46、V59）」中，V23 与 V59 的预期结果只断言保存、统计口径与登记事件，不含可回读内容 | 矩阵 V23 行、V59 行、V46 行原文 | 回读归属改为「（V1、V46）」，或把该勾选项标注为「（无矩阵编号：取证形态）」并写明两个写入口的回读经匹配查询与引用详情实现 | ds `DS-110`③ |
| Q-09 | `Fixed` | low | `02-建表脚本与SqlMap映射语句.md` 第 20 项与口径段（第 23 行） | 第 20 项写「（V73、V75 的数据库面，**与票 08 共同承接**）」，口径段却写「V73 与 V75 的真实数据库列结论**由票 08** 在目标库上取证，本票只交付脚本与静态面」——同一面被本票勾选项与他票同时声明 | `README.md` 约定 5；票 08 第 5 项（V73、V75、V76 的数据库面） | 第 20 项改为「（无矩阵编号：脚本交付暂停点检查，负责人建表完成后交票 08 核对）」，或删去该勾选项 | glm `L-3` |
| Q-10 | `Fixed` | low | `02-建表脚本与SqlMap映射语句.md` 第 16 项 | 把「映射文件与仓储类同处程序集根下一级目录」标为「（V76）」，V76 的预期结果不含存放位置要求 | 矩阵 V76 行原文；`Server/design.md:96` 的映射文件位置段；守卫用例 `Stage1SqlMapProbeTests.Sql_map_resources_stay_directly_under_one_folder` | 拆成两条：「作用域与物理表映射（V76）」与「映射文件与仓储类同处程序集根下一级目录（无矩阵编号：守卫用例 `Stage1SqlMapProbeTests.Sql_map_resources_stay_directly_under_one_folder`）」 | ds `DS-108` |
| Q-11 | `Fixed` | low | `02-建表脚本与SqlMap映射语句.md` 第 20 项与外部前置行（第 9 行） | 第 20 项依赖负责人建成四张表，外部前置行却写「脚本交付后暂停；四张表由负责人建成后再由**票 03** 起的集成项继续」，把建表门禁限定在票 03 之后 | 本票第 9 行与第 20 行；票 08 第 5 项同口径的数据库面 | 第 20 项句末补「（本项需负责人建成四张表后执行；未建成时按 `Blocked` 记录）」，或把该勾选项移入票 08 | ds `DS-111` |
| Q-12 | `Fixed` | low | `03-匹配查询（F04）.md` 第 9 项 | 原文「本院报告排除时长的**四类**取值与边界行为」，其后实列五项（开关关闭、未达排除时长、满足排除时长与可互认时间、排除时长为零、大于项目可互认时间） | 同句自身；矩阵 V17、V18、V19、V20、V20b | 「四类」改为「五类」，或改为「各取值与边界行为」 | glm `L-2` |
| Q-13 | `Fixed` | low | `03-匹配查询（F04）.md` 第 17 项 | 原文「可互认时间边界与查询时点边界按包含处理（V12、V13）」。V12 是边界包含判定，V13 的结论是「匹配基准时间晚于查询时点的报告不作为候选」，处置方向不同，并列会使 V13 被读作边界包含 | 矩阵 V12 行与 V13 行原文 | 拆成两条：「可互认时间边界按包含处理（V12）」与「匹配基准时间晚于查询时点的报告不作为候选（V13）」 | ds `DS-109` |
| Q-14 | `Fixed` | low | `07-静态收尾与实现约束核对.md` 第 14 项；`README.md` 约定 12 | `Server/design.md:162` 的两半要求中，「不重复声明共享助手」有票承接，而「新增静态守卫测试若调用共享语法工具，必须登记进守卫的消费方清单」在全部票面无人承接 | `Server/design.md:162`；`server/Dy.MedicalRecognition.Tests/Architecture/SourceGuardReuseTests.cs` 的受治理文件精确等值清单与其失败语义注释 | 在票 07 第 14 项补「新增源码守卫若复用共享语法工具，须登记 `Architecture/SourceGuardReuseTests.cs` 的受治理清单并保持精确一致」，或在 `README.md` 约定 12 登记该条 | ds `DS-104` |
| Q-15 | `Fixed` | low | `README.md` 约定 11 | 「三项上游同步均已完成」未给可核对落点，读者无法据此复核 | 可核对落点：SRS F04 业务规则 8／9、F06 业务规则 9、设计约束 17、`CONTEXT.md` 两段；`docs/uml/1-MedicalRecognitionReport.wsd` 的两实体删列与命令合并注记；`docs/uml/4-MedicalRecognitionExternalSystemQuery.wsd:55` | 句末补落点，例如「（依据：SRS F04 业务规则 8／9、F06 业务规则 9、设计约束 17、`CONTEXT.md` 两段；`1-MedicalRecognitionReport.wsd` 的实体删列与命令合并注记）」 | ds `DS-114` |
| Q-16 | `Fixed` | low | `README.md` 第 19 行 | 原文「票 00 与票 02 在依赖图上并列（都只依赖票 00 的契约结论）」自相矛盾：票 00 的依赖为 None | 同文件的票一览表（01←00、02←00） | 改为「票 01 与票 02 在依赖图上并列（都只依赖票 00 的契约结论）」 | ds `DS-107`、glm `L-1`、kimi `K-03`（三份一致） |
| Q-17 | `Fixed` | low | `04-处理结果提交（F05）.md` 口径段（第 27 行） | 票 04 的 V95 承接面未写明（V95 被票 04 与票 06 共同声明，只有票 06 写了「引用侧面」）；票 02 亦未写 V76 的数据库面 | `README.md` 约定 5；票 05／06／08 的写法为先例 | 票 04 口径段补「V95 在本票承接处理结果侧的负向面，引用结果侧由票 06 承接」；票 02 补「V76 的真实数据库面由票 08 在目标库上取证」 | ds `DS-112`（其余部分，与 Q-05 同源） |

---

## 四、需负责人决策的条目

| 编号 | 需决策的事项 | 可选路径 |
|---|---|---|
| D-01 | 票 03 体量（22 个主编号 + 2 个后缀、16 项勾选、跨 7 个层级词）是否拆分 | ①维持现状（三方均认为属单一用例、可接受，glm 与 kimi 明示不强制拆）；②按 ds 建议拆为 03a 校验与失败面／03b 候选筛选与选优面／03c 写入与事件面，并把票 04 的 `Blocked by` 改为 03c |
| D-02 | 票 08 体量（71 条真实数据库条目）是否拆分 | ①维持现状并按 Q-07 补全清单；②拆为 08a 表与索引面／08b 参数与认证面／08c 写入与原子性面 |
| D-03 | `README.md` 约定 12 是否补登记「不重跑后端代码生成器、不覆盖再生成保护清单中的配置与启动文件」 | ①补一句登记（与阶段 4 同形，成本一行）；②不补（本阶段产物不属生成范围，常驻规范已覆盖） |
| D-04 | 是否恢复一张独立的「既有测试断言同步」票（把分散在票 00／02／03／04／05／06／07 的同步面收拢） | ①维持折进各票的现状；②独立成票（阶段 4 有同名票 04，可减少 Q-03 这类错票风险） |
| D-05 | 票 04／05／06 外部前置行是否移除票间依赖、改为测试数据说明 | ①维持现状（阶段 4 有同形先例）；②统一改为「测试数据由前序票经正常接口形成」的措辞 |
| D-06 | 是否在票 08 外部前置补「部署宿主的外部服务代理映射已就位」 | ①补；②不补（属部署清单事项，测试侧用共享桩） |

---

## 五、设计选择分歧与无法确认项（不构成缺陷）

### 5.1 取向分歧

| 事项 | 三份的读法 | 状态 |
|---|---|---|
| 票 03 粒度 | ds 建议可拆；glm／kimi 判可接受、不强制拆 | 维持（见 D-01） |
| 收口分票方式（票 07／08 分立） | 三份均判不需要合并，与阶段 4 先例同构 | 维持 |
| 不设请求样例文件、不设独立断言同步票 | ds 列为分歧并给可选路径；glm／kimi 判可由阶段差异解释 | 维持（见 D-04） |
| 参数编码取值确认登记为外部前置 | glm／kimi 判可接受（票 00 先按形态声明常量、取值确认后落地） | 维持 |
| 票 06 的 `Blocked by: 05` | ds 指出功能前提是票 03／04 的产物、引用详情并非门禁；但批次表规定批次 5 前置为批次 4，票面不得改批次 | 维持 |
| 约定 12 与阶段 4 同名约定的差异 | ds 判须补回；glm 判可由阶段差异解释 | 见 M-01 与 D-03 |

### 5.2 无法确认项

| 编号 | 事项 | 说明 |
|---|---|---|
| U-01 | 票 03 与票 08 能否在单个上下文窗口内完成 | 需实际执行时测量上下文占用；本轮只读无法判定（三份一致） |
| U-02 | 矩阵「真实数据库」列 71 条能否全部在真实宿主上构造 | 需连接目标库与宿主并核对既有测试身份与阶段 1–4 配置数据现状 |
| U-03 | 票 02「列序按既定规则」的规则载体 | 阶段 1–4 脚本惯例存在，但票面与设计未逐条列出规则内容；实施时以既有脚本对齐 |
| U-04 | 三个参数常量「小写下划线形态」与项目既有常量命名惯例的一致性 | 常量类尚不存在，无法对照先例；取值确认时一并核对 |
| U-05 | 设计文档中「F04 业务规则 1 的承接映射」（`Server/design.md:168`）是否真的覆盖「截止点取平台接收时间」 | 该映射位于设计文档、不在本轮审查对象内；如需判定须另立只断言「截止点取平台接收时间」的条目（本轮不据此提出票面问题） |

---

## 六、整改后复核

整改完成后，对每条 `Fixed` 条目回到票面复核是否真的不再存在，并记录复核方式；仍存在的回退为 `Open` 并注明「重开」。

**本轮整改必须遵守的三条纪律**：

1. **票面口径必须回上游核对**：Q-01 与 Q-02 都是票面 What 段自定或写反口径，而勾选项正确——只读勾选项不足以发现。
2. **同一同步面在多票之间必须只归一处**：Q-03 的错票与断档源于「既有断言同步」被折进多票时的归属失误；改动涉及票 03／04／05／06 四票，须同批处理。
3. **改票不得动设计与批次**（`README.md` 约定 6）：票面修正只改票面文字与归属说明，需要改设计或批次时先暂停并登记。

| 编号 | 复核时间 | 复核方式 | 复核结论 |
|---|---|---|---|
| Q-01 | 2026-09-19 | 复读票 06 第 3 行与矩阵 V72 原文 | 已消失：改为「整批原子保存，只增加引用次数（采纳次数、来源医院被互认次数与预计节省金额不变）」 |
| Q-02 | 2026-09-19 | 复读票 05 第 3 行与 SRS F06 前置 2 | 已消失：改为「按患者证件类型与号码、本次来源就诊类型与流水号获取引用详情」 |
| Q-03 | 2026-09-19 | 脚本抽取各票「写入口／查询入口」与「分部文件数量断言」出现处 | 已消失：票 03 改「新增写入口」、票 05 新增查询侧同步勾选项、票 03／04／06 删去只随查询入口变化的数量断言 |
| Q-04 | 2026-09-19 | 复读票 08 第 2 项与规格测试替身决策 | 已消失：改为「经共享桩取证」并引 `阶段5-规格.md` 的测试替身决策 |
| Q-05 | 2026-09-19 | 复读票 01 勾选与口径、票 05 第 12 项 | 已消失：票 01 新增「匹配响应不含检查所见与检查结论（V84 的匹配响应面）」并在口径声明该面；票 05 只承接 V84 的引用详情面（第一轮该复核结论失实：勾选项当时未插入，见 7.1） |
| Q-06 | 2026-09-19 | 脚本按票抽取实际层级并与词表逐行比对 | 已消失：词表按票重写为实际取值（票 03 行含 Application 与 Contract/Domain；票 04 独立成行；票 05 行改为 Query、Contract（V84 面）、Static/Domain（V88 面）；票 08 行注明 71 条与 V89／V92 宿主运行面（第一轮该复核结论失实：票 04 行当时未插入，见 7.1） |
| Q-07 | 2026-09-19 | 复读票 08 第 16 行 | 已消失：写明「记『是』的 71 条（覆盖票 02–06 声明的编号，逐行判定）」并补判据（引 `Server/design.md` 测试数据边界段原文），另在票 08 外部前置补部署宿主代理映射 |
| Q-08 | 2026-09-19 | 复读票 08 第 14 行与矩阵 V23／V59 原文 | 已消失：回读归属改为「V1、V46；两个写入口的回读经匹配查询与引用详情实现」 |
| Q-09 | 2026-09-19 | 复读票 02 第 20 项与口径段 | 已消失：第 20 项改为脚本交付暂停点（无矩阵编号，交票 08 在目标库核对），与口径段不再双重声明 |
| Q-10 | 2026-09-19 | 复读票 02 第 16 项 | 已消失：拆为「作用域与物理表映射（V76）」与「映射文件存放位置（无矩阵编号，引守卫用例）」两条（第一轮该复核结论失实：新勾选项当时未插入，见 7.1） |
| Q-11 | 2026-09-19 | 复读票 02 第 20 项与外部前置行 | 已消失：暂停点项注明「未建成时本项记 `Blocked`」，与外部前置不再冲突 |
| Q-12 | 2026-09-19 | 复读票 03 第 9 项 | 已消失：「四类」改为「五类」（实列五项） |
| Q-13 | 2026-09-19 | 复读票 03 第 17–18 行与矩阵 V12／V13 原文 | 已消失：拆为「可互认时间边界按包含处理（V12）」与「匹配基准时间晚于查询时点的报告不作为候选（V13）」两条；脚本复验 105 个编号全部被承接 |
| Q-14 | 2026-09-19 | 复读票 07 第 14 项与 README 约定 12 | 已消失：两处均登记「新增源码守卫复用共享语法工具须登记消费方清单」；同批补入生成器与保护清单约束 |
| Q-15 | 2026-09-19 | 复读 README 约定 11 | 已消失：补 SRS F04 规则 8／9、F06 规则 9、设计约束 17、`CONTEXT.md` 两段与两份 UML 的落点 |
| Q-16 | 2026-09-19 | 复读 README 第 19 行与票一览 | 已消失：改为「票 01 与票 02 在依赖图上并列」 |
| Q-17 | 2026-09-19 | 复读票 04 口径段与票 02 口径段 | 已消失：票 04 补「V95 承接处理结果侧负向面（引用结果侧由票 06）」；票 02 补「V73、V75 与 V76 的数据库面由票 08 取证」 |
| D-01 | 2026-09-19 | 已决：维持不拆（票面不动） | 票面已按该决定处理 |
| D-02 | 2026-09-19 | 已决：维持不拆，并按 Q-07 补全 71 条清单与 Blocked／NotRun 判据 | 票面已按该决定处理 |
| D-03 | 2026-09-19 | 已决：补登记（README 约定 12 与票 07 第 14 项已落） | 票面已按该决定处理 |
| D-04 | 2026-09-19 | 已决：维持折进各票，并已按 Q-03 修正错票与断档 | 票面已按该决定处理 |
| D-05 | 2026-09-19 | 已决：维持先例写法（票面不动） | 票面已按该决定处理 |
| D-06 | 2026-09-19 | 已决：补登记（票 08 外部引用前置行已落） | 票面已按该决定处理 |
---

## 七、第二轮（整改复检）结果与处置

三份审查方按同一份审查要求对整改后的票面做了第二轮复核（结果文件由负责人清空后重写）。汇总如下。

| 编号 | 来源 | 严重度 | 事项 | 处置 |
|---|---|---|---|---|
| N-01 | ds `T-02`、glm `R-4`、kimi `K-01`（三份一致） | medium | 票 08 的 What 段仍写「在真实参数服务上取证」，与已改的第一条勾选「经共享桩取证」自相矛盾 | `Fixed`：What 段改为「参数三类前置经共享桩取证（见第 12 行勾选项与 `阶段5-规格.md` 的测试替身决策）」 |
| N-02 | glm `R-1` | medium | 票 01 的口径段已声明 V84 匹配响应面，但该面在全部票面仍无勾选项承接 | `Fixed`：票 01 新增「匹配响应不含检查所见与检查结论（V84 的匹配响应面）」 |
| N-03 | ds `T-01`、kimi `K-02` | low | 票 02 的「映射文件留在程序集根下一级目录」只在 What 段出现，勾选项无落点 | `Fixed`：票 02 新增该勾选项（无矩阵编号，引守卫用例 `Stage1SqlMapProbeTests.Sql_map_resources_stay_directly_under_one_folder`） |
| N-04 | ds `T-04`、glm `R-3` | low | README 层级词表在整改中丢失票 04 行 | `Fixed`：第 46 行票列改为「03、04」，并同步第 45 行取值顺序为脚本抽取的实际值 |
| N-05 | ds `T-04` 后半、glm `R-4` 后半 | low | 词表 08 行未覆盖票 08 第 12 行引用的 V49–V51 | `Fixed`：08 行补「与共享桩面 `Query`（V49–V51）」 |
| N-06 | ds `T-05` | low | 约定 12 末句称约束「由票 07 以静态断言核对」，生成器与保护清单两条实为差异检查 | `Fixed`：改为「以静态断言与差异检查核对」 |
| N-07 | ds `T-06` | low | 票 02 的「脚本交付暂停点」勾选项判定动作落在票 08，票 02 执行者无法判定 | `Fixed`：改为本票可判定（脚本已交付、索引与列注释声明完整、已注明建表暂停点），目标库核对归票 08 |
| N-08 | ds `T-03` | low | 票 08 的 What 段要求「并发重复写入」的行为证据，与 S5-D17「不做并发测试」及规格范围外清单冲突 | `Fixed`：改为「同一匹配项的重复写入」（可顺序构造）；并发兜底只作 V75 的索引形态在目标库核对 |
| N-09 | kimi `K-03` | low | 票 05 的口径段缺「真实数据库面由票 08 取证」句（票 03／04／06 均有） | `Fixed`：票 05 口径段补该句 |
| N-10 | kimi `K-04` | low | 约定 11 引用的 4 号 UML 注记只覆盖本院报告匹配参数，不含引用详情有效时长参数的严格失败语义 | `Fixed`：约定 11 措辞收窄为「本院报告匹配参数注记」（不改上游文档） |
| R-2 | glm（第二轮重提） | low | 票 04／05／06 的外部前置行混入票间产物依赖 | **不构成缺陷（维持）**：阶段 4 票 04 的外部前置行同形（「无（本票只依赖票 01 的物理映射声明与票 03 的读模型契约）」），属既有先例写法；`D-05` 已决维持 |

### 7.1 我方第一轮复核结论的三处失实（本轮由审查方发现）

第一轮整改的复核表中，`Q-05`、`Q-10`、`Q-06` 三条被我记为「已消失」，实际只落盘了一部分——批量替换脚本在多行插入处静默截断：

- `Q-05`：票 01 的口径段已改，**勾选项未插入**（审查方 glm `R-1` 命中，即 `N-02`）；
- `Q-10`：票 02 的原句已替换，**新勾选项未插入**（ds `T-01`、kimi `K-02` 命中，即 `N-03`）；
- `Q-06`：词表的 03 行与 05／06／08 行已改，**票 04 行插入失败**（ds `T-04`、glm `R-3` 命中，即 `N-04`）。

另有 `Q-13` 的拆分只落了前半句（`V13` 一度无人承接），由我方复验发现并已补。三处现均已修复，并在第二轮以脚本逐项确认；此后票面改动一律逐条编辑，不再使用脚本批量插入多行内容。

### 7.2 复核表（第二轮）

| 编号 | 复核时间 | 复核方式 | 复核结论 |
|---|---|---|---|
| N-01 | 2026-09-19 | 复读票 08 第 3 行与第 12 行；全目录 grep「在真实参数服务上取证」 | 已消失：该串在十份票面零残留，What 段与勾选同口径 |
| N-02 | 2026-09-19 | grep 票 01 的 V84 匹配响应面勾选项 | 已消失：勾选项已插入，与口径段互补一致 |
| N-03 | 2026-09-19 | grep 票 02 的映射位置勾选项 | 已消失：勾选项已插入（无矩阵编号并注明守卫用例） |
| N-04 | 2026-09-19 | 脚本复读词表各行并与各票实际层级比对 | 已消失：票 04 归入第 46 行票列，第 45 行取值顺序与实际一致 |
| N-05 | 2026-09-19 | 复读词表 08 行 | 已消失：已补「共享桩面 `Query`（V49–V51）」 |
| N-06 | 2026-09-19 | 复读约定 12 末句 | 已消失：改为「以静态断言与差异检查核对」 |
| N-07 | 2026-09-19 | 复读票 02 该勾选项 | 已消失：判定条件改为本票可判定，票 08 承接目标库核对 |
| N-08 | 2026-09-19 | 复读票 08 的 What 段 | 已消失：改为「同一匹配项的重复写入」 |
| N-09 | 2026-09-19 | 复读票 05 口径段 | 已消失：已补真实数据库与真实工作单元面的分工句 |
| N-10 | 2026-09-19 | 复读约定 11 的 4 号 UML 引用 | 已消失：已收窄为「本院报告匹配参数注记」 |
| R-2 | 2026-09-19 | 复读阶段 4 票 04 的外部前置行先例 | 维持：属既有先例写法，`D-05` 已决 |

### 7.3 第二轮后的票面状态

十份文件：勾选总数 **103**；矩阵编号覆盖 **105/105**（无未承接、无失效引用）；U+FFFD 全为 0；文件换行一致；票面纪律复扫零命中（README 命中处仍为约定条文自身）。
