# 阶段 5 后端实施

依据 [后端设计](design.md) 的验证矩阵安排批次，不另建或复制后端测试矩阵。结果统一记录在 [阶段测试报告](../testReport.md)。

| 批次 | 目标 | 责任范围 | 前置条件 | 矩阵编号 | 执行状态 |
|---|---|---|---|---|---|
| 0 | S5-D14 的两个领域实体与两个对外 DTO 删列、参数键名常量、两个枚举补中文说明与描述器特性并登记**并同步既有枚举元数据守卫用例的排除清单**、请求类型与事件声明、读模型重建 | 契约层与共享领域层、测试 | 设计确认 | V81-V85、V90 | 未开始 |
| 1 | 四张表建表脚本（统一前缀与索引，含按设计删除项目编码列）、四个映射在既有实体作用域下补齐语句、既有语句清单断言同步 | 仓储层与测试 | 批次 0 | V73-V80（含 V73b、V74b） | 未开始 |
| 2 | 匹配查询管理器分步、候选查询语句、参数读取与严格失败语义、匹配记录与匹配项写入 | 领域层、仓储层、应用层 | 批次 1 脚本交付且负责人建表完成 | V1-V22（含 V20b、V22b）、V89、V91-V94 | 未开始 |
| 3 | 处理结果幂等识别、整批校验、金额读取、原子保存、保存时间写入 | 领域层、仓储层、应用层 | 批次 2 | V23-V45（含 V35b、V36b、V45b）、V95 | 未开始 |
| 4 | 引用详情有效期计算、版本有效性逐项校验、部分可用语义、读模型映射 | 应用层、领域层、仓储层 | 批次 3 | V46-V58（含 V58b） | 未开始 |
| 5 | 引用结果逐项反查、归属一致性、时间序、幂等与冲突、唯一索引兜底 | 领域层、仓储层、应用层 | 批次 4 | V59-V72（含 V70b、V72b） | 未开始 |
| 6 | 跨接口静态核对、适用测试、构建、静态检查、差异检查与阶段报告更新 | 全部 | 批次 0-5 | V86-V88 | 未开始 |

矩阵编号各归一个批次，不重复归属；批次 6 只承接 V86-V88 三条跨接口静态条目，不以「全部」覆盖前序批次的取证责任。

新增端点与分部文件时，既有用例的端点方法集合断言与分部文件清单断言须在同一批次内同步：方法集合等值断言三处（写入口契约、查询入口契约与查询契约固定清单），分部文件逐文件等值断言三处（管理器三文件、写侧端口五文件、写侧实现五文件），分部文件数量断言两处（查询侧端口与实现各二）。四个映射补齐语句时须与仓储类同处程序集根下一级目录，不新建分组子目录。前缀与索引随批次 1 的建表脚本一并交付。

数据库迁移需要负责人执行：批次 1 交付脚本和暂停点，未确认执行完成前不继续相关集成测试。
