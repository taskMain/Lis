# 阶段4（F03 报告采集与生命周期）代码现状取证

> 用途：只读调查结论，作为阶段 4 设计/实施的事实基线。**未修改任何既有文件**（`git status` 本轮前仅有 `?? docs/plans/006-阶段4-/` 未跟踪目录）。
> 除"参考资料"一节外，路径均相对 `E:\MedSync\Dy.MedicalRecognition`。

## 1. 工程、类型与契约现状

### 1.1 工程清单（7 个）

| 工程 | .csproj |
|---|---|
| Dy.MedicalRecognition（宿主，SDK=Microsoft.NET.Sdk.Web，无 Program.cs） | `server/Dy.MedicalRecognition/Dy.MedicalRecognition.csproj` |
| Dy.MedicalRecognition.Application | `server/Dy.MedicalRecognition.Application/Dy.MedicalRecognition.Application.csproj` |
| Dy.MedicalRecognition.Application.Contracts | `server/Dy.MedicalRecognition.Application.Contracts/Dy.MedicalRecognition.Application.Contracts.csproj` |
| Dy.MedicalRecognition.Domain | `server/Dy.MedicalRecognition.Domain/Dy.MedicalRecognition.Domain.csproj` |
| Dy.MedicalRecognition.Domain.Share | `server/Dy.MedicalRecognition.Domain.Share/Dy.MedicalRecognition.Domain.Share.csproj` |
| Dy.MedicalRecognition.Repository | `server/Dy.MedicalRecognition.Repository/Dy.MedicalRecognition.Repository.csproj` |
| Dy.MedicalRecognition.Tests | `server/Dy.MedicalRecognition.Tests/Dy.MedicalRecognition.Tests.csproj` |

### 1.2 已存在的报告相关实体（**已找到**，全部是 `partial class`，扁平字段，无导航属性）

10 个报告实体全部在 `server/Dy.MedicalRecognition.Domain/MedicalRecognitionReportAggregate/`，均直接继承 `Dy.Core.Abstractions.Domain.Entity`，无 `[Table]`/`[Scope]` 特性：

| 实体 | 文件:行（class） | 字段数 | 关键字段 |
|---|---|---|---|
| MedicalRecognitionReport | `MedicalRecognitionReport.cs:10` | 13 | `OrganizationCode/HospitalCode/BranchCode/ReportType/ReportNo/PatientId/CurrentVersionId/Status/VoidedTime/VoidReason/OperId/OperTime`（:15-63） |
| MedicalReportVersion | `MedicalReportVersion.cs:10` | 39 | `ReportId/VersionNumber/PdfFileId(:155)/PdfFileName(:159)/SourceModifiedTime/ReceivedTime/...`（:15-171） |
| PlatformPatient | `PlatformPatient.cs:10` | 8 | `IdentityDocumentTypeCode/IdentityDocumentNo/PatientName/PatientGenderCode/PatientBirthDate`（:15-43） |
| LaboratoryReportContent | `LaboratoryReportContent.cs:10` | 17 | `ReportVersionId(:19)` + 标本/采集/检测完成/检验者（:19-83） |
| LaboratoryResultItem | `LaboratoryResultItem.cs:10` | 21 | `ReportVersionId(:19)/SourceDetailKey/StandardProjectCode/ResultType/AbnormalFlag/DisplayOrder`（:19-103） |
| LaboratoryBacteriaResult | `LaboratoryBacteriaResult.cs:10` | 21 | `ReportVersionId(:19)/SourceOrganismCode/DetectionConclusion/TestPanelCode`（:19-99） |
| LaboratoryAntimicrobialSusceptibility | `LaboratoryAntimicrobialSusceptibility.cs:10` | 19 | `BacteriaResultId(:19)/DrugCode/SusceptibilityCode/MicValue/DisplayOrder`（:19-87） |
| ExaminationReportContent | `ExaminationReportContent.cs:10` | 20 | `ReportVersionId(:19)/Findings(:39)/Conclusion(:43)/SourceImageStatus(:75)/ImageAccessUrl(:79)`（:19-95） |
| ExaminationItem | `ExaminationItem.cs:10` | 6 | `ReportVersionId(:19)/SourceProjectName/SourceProjectCode/StandardProjectCode`（:15-39） |
| ExaminationSite | `ExaminationSite.cs:10` | 5 | `ExaminationItemId(:19)/SourceSiteCode/SiteName`（:15-35） |

另有 6 个同目录实体属其它阶段：`MedicalStandardCategory/Group/Item`、`MutualRecognitionItem`、`OrganizationHospitalBranchRecognitionAmount`，以及阶段 5 的 `RecognitionMatchRecord/RecognitionMatchItem/RecognitionProcessingResult/RecognitionReference`（共 20 个实体文件）。

枚举（`server/Dy.MedicalRecognition.Domain.Share/Enums/`）：`MedicalReportType.cs`（Laboratory=1/Examination=2）、`MedicalReportLifecycleStatus.cs`（Effective=1/Voided=2）、`LaboratoryResultType`、`LaboratoryAbnormalFlag`、`SourceImageStatus`、`VisitType` 等已存在。

### 1.3 聚合（**部分找到 / 关键结构不存在**）

- 聚合根 = `MedicalRecognitionReport`：仅由常量声明推定——`server/Dy.MedicalRecognition.Domain.Share/MedicalRecognitionReportAggregate/MedicalRecognitionReportConst.cs:11`
  `AggregateId = "Dy.MedicalRecognition.Domain.MedicalRecognitionReportAggregate.MedicalRecognitionReport"`（注释写明"取聚合根的完整类型名"）。
- **不存在**聚合类（无 `MedicalRecognitionReportAggregate` 类型）。全库 grep `AggregateRoot|IAggregateRoot|\[Aggregate` 命中 0。
- **不存在**任何子集合：`server/Dy.MedicalRecognition.Domain` 全域 grep `List<|ICollection|IReadOnlyList` 只命中查询仓储接口的 `IEnumerable<...>`，实体类无子集合属性。报告↔版本↔内容/项目/部位之间只有 `Guid` 外键字段，没有对象图。
- 领域管理器：**只有 1 个** `MedicalRecognitionReportManager`（`server/Dy.MedicalRecognition.Domain/MedicalRecognitionReportAggregate/MedicalRecognitionReportManager.cs:14`，`: DomainService`，441 行），其全部方法只覆盖标准目录分类/分组/标准项目、互认项目配置、组织医院院区金额（`CreateMedicalStandardCategoryAsync:39` … `SaveOrganizationHospitalBranchRecognitionAmountAsync:401`）。**报告采集、版本追加、作废相关方法 0 个。**

### 1.4 命令 / 应用服务

**已存在的命令类：17 个**，全部在 `server/Dy.MedicalRecognition.Domain/MedicalRecognitionReportAggregate/Commands/`：
`Create/Update/Enable/DisableMedicalStandardCategory`（4）、`Create/Update/Enable/DisableMedicalStandardGroup`（4）、`Create/ChangeRemark/Enable/DisableMedicalStandardItem`（4）、`Create/UpdateConfiguration/Enable/DisableMutualRecognitionItem`（4）、`SaveOrganizationHospitalBranchRecognitionAmountCommand`（1）。

**已存在的应用服务类：3 个**
- `server/Dy.MedicalRecognition.Application/MedicalRecognitionReportAggregate/MedicalRecognitionReportAppService.cs`（19 个 `public async Task<bool>` 方法，全部为目录/配置/金额，行 74…432）
- `server/Dy.MedicalRecognition.Application/Queries/MedicalRecognitionReportQueryAppService.cs`
- `server/Dy.MedicalRecognition.Application/Queries/EnumMetadata/EnumMetadataAppService.cs`

**报告生命周期命令（找到/未找到）：**

| 期望命令 | 状态 |
|---|---|
| `SubmitCompleteLaboratoryReportCommand`（完整检验提交） | **未找到** |
| `SubmitCompleteExaminationReportCommand`（完整检查提交） | **未找到** |
| `VoidLaboratoryReportCommand`（检验作废） | **未找到** |
| `VoidExaminationReportCommand`（检查作废） | **未找到** |
| `CreateOrGetMedicalRecognitionReportCommand`、`AppendMedicalReportVersionCommand`、`CreateLaboratoryReportContentCommand`、`CreateLaboratoryResultItemsCommand`、`CreateLaboratoryBacteriaResultsCommand`、`CreateLaboratoryAntimicrobialSusceptibilityResultsCommand`、`CreateExaminationReportContentCommand`、`CreateExaminationItemsCommand`、`CreateExaminationSitesCommand` | **均未找到**（全库 grep 这 13 个名字命中 0；同时 grep `Facade` 命中 0，无编排层） |

**已存在的领域事件：17 个**（`server/Dy.MedicalRecognition.Domain.Share/MedicalRecognitionReportAggregate/Events/`），全部为 `MedicalStandard*`（12）、`MutualRecognitionItem*`（4）、`OrganizationHospitalBranchRecognitionAmountSavedEvent`（1）。**报告/版本/患者相关事件 0 个。**

### 1.5 Request / DTO 契约

- **Request：18 个**，在 `server/Dy.MedicalRecognition.Application.Contracts/MedicalRecognitionReportAggregate/Requests/`：`Create/Update/Enable/Disable{MedicalStandardCategory,MedicalStandardGroup,MedicalStandardItem,MutualRecognitionItem}Request`、`ChangeMedicalStandardItemRemarkRequest`、`UpdateMutualRecognitionItemConfigurationRequest`、`SaveOrganizationHospitalBranchRecognitionAmountRequest`、`SaveBranchRecognitionAmountRequest`。
- **完整检验/检查报告提交的请求类型：不存在。** 全库无 `SubmitComplete*Request` / `Void*ReportRequest`。
- **DTO：19 个**，在 `.../MedicalRecognitionReportAggregate/Dtos/`，包含报告类：`MedicalRecognitionReportDto`、`MedicalReportVersionDto`（`PdfFileId` :155、`PdfFileName` :159）、`PlatformPatientDto`、`LaboratoryReportContentDto`、`LaboratoryResultItemDto`、`LaboratoryBacteriaResultDto`、`LaboratoryAntimicrobialSusceptibilityDto`、`ExaminationReportContentDto`、`ExaminationItemDto`、`ExaminationSiteDto`、`RecognitionMatch*Dto` 等。DTO 已由生成器产出，但无请求/命令与之配对。
- 映射注册现状：`server/Dy.MedicalRecognition.Application/MedicalRecognitionReportAggregate/MedicalRecognitionReportDataMaps.cs` 中 19 条 Request→Command 映射全部是目录/配置/金额；实体↔DTO 的 TwoWay 映射已包含 10 个报告实体（:33-42），**但没有 Request→Command 是报告类的**。
- UML 依据：`docs/uml/命令出参整改矩阵.md:22-25`（4 个生命周期命令）、`:57-67`（9 个聚合内部命令）。

### 1.6 仓储 + SqlMap

- 仓储接口 `server/Dy.MedicalRecognition.Domain/MedicalRecognitionReportAggregate/IMedicalRecognitionReportRepository.cs`（241 行）：**只有目录/配置/金额共 30 个方法**，`CreateGuid()` :17 起。**报告/版本/患者/内容/项目/部位/匹配 的读写方法 0 个。**
- 仓储实现 `server/Dy.MedicalRecognition.Repository/MedicalRecognitionReportAggregate/MedicalRecognitionReportRepository.cs`（199 行），同样只有目录/配置/金额方法。
- **Scope/sqlId 约定（阶段 1 决定，已落地并被测试锁定）**：
  `MedicalRecognitionReportRepository.cs:22,26,34,38,42` 定义 `const string XScope = "MutualRecognitionItem" / "OrganizationHospitalBranchRecognitionAmount" / "MedicalStandardCategory" / "MedicalStandardGroup" / "MedicalStandardItem"`；
  每个调用显式传参，如 `:51` `dataMapper.InsertAsync(value, scope: MedicalStandardCategoryScope, sqlId: "CreateMedicalStandardCategory")`；
  注释 `:21` 明确"每个方法都逐调用传入该作用域，不使用 `SetContext`"。
  实现注释 `:15` 说明实体无 `[Table]/[Scope]` 时框架 scope 回退为**实体类名**；测试 `server/Dy.MedicalRecognition.Tests/Stage1SqlMapProbeTests.cs:100-108` 断言 `MedicalRecognitionReport.CreateMedicalStandardCategory` **不注册**、`MedicalStandardCategory.CreateMedicalStandardCategory` 注册；`Stage3SqlMapTests.cs:191-201` 记录"作用域曾为聚合名 `MedicalRecognitionReport`"并断言金额 SqlMap 已改为实体名。
- **SqlMap XML 共 20 个**：
  - 手写 5 个（Scope = 实体名，含 Insert/Update/Get/Exists 全量语句）：`MedicalStandardCategory.xml`(102行)、`MedicalStandardGroup.xml`(94行)、`MedicalStandardItem.xml`(100行)、`MutualRecognitionItem.xml`(100行)、`OrganizationHospitalBranchRecognitionAmount.xml`(62行)，位于 `server/Dy.MedicalRecognition.Repository/MedicalRecognitionReportAggregate/`。
  - 生成 14 个报告表 SqlMap（**每个 12 行，只有 `*Columns` + `QueryAll*` 两条语句，无 Insert/Update/GetById/Exists**）：`MedicalRecognitionReport.xml`、`MedicalReportVersion.xml`、`PlatformPatient.xml`、`LaboratoryReportContent.xml`、`LaboratoryResultItem.xml`、`LaboratoryBacteriaResult.xml`、`LaboratoryAntimicrobialSusceptibility.xml`、`ExaminationReportContent.xml`、`ExaminationItem.xml`、`ExaminationSite.xml`、`RecognitionMatchRecord.xml`、`RecognitionMatchItem.xml`、`RecognitionProcessingResult.xml`、`RecognitionReference.xml`（同目录）。
  - 查询 1 个：`server/Dy.MedicalRecognition.Repository/Queries/MedicalRecognitionReportQuery.xml`(207行，`SqlMap Scope="MedicalRecognitionReportQuery"`，6 条语句，全部是目录/配置/金额)。
- **口径不一致（重要事实）**：14 个报告表 SqlMap **全部声明 `Scope="MedicalRecognitionReport"`**（同一 scope），其中 13 个文件的实体名并不是 `MedicalRecognitionReport`。按 1.6 的"scope = 实体名"约定，这些语句在按实体名调用时找不到；`Stage1SqlMapProbeTests.cs:114-115` 目前把 `MedicalRecognitionReport.QueryAllRecognitionReference` / `...RecognitionReferenceColumns` 作为"必须注册"的键锁定。

### 1.7 查询 / 读模型

- 查询应用服务接口 `server/Dy.MedicalRecognition.Application.Contracts/Queries/IMedicalRecognitionReportQueryAppService.cs`：7 个方法（目录 4 + 互认配置 1 + 金额 2）。**报告列表/历史版本/版本详情/PDF 下载查询 0 个。**
- 查询仓储接口 `server/Dy.MedicalRecognition.Domain/Queries/IMedicalRecognitionReportQueryRepository.cs`（83 行）与实现 `server/Dy.MedicalRecognition.Repository/Queries/MedicalRecognitionReportQueryRepository.cs`（88 行）：同上，**无任何报告查询**（实现里显式 `scope: SqlScope("MedicalRecognitionReportQuery"), sqlId: "..."`，`:36-68`）。
- 查询契约/读模型文件 `.../Queries/MedicalRecognitionReportQueryContracts.cs`（216 行）与 `.../Queries/RecognitionAmountReadModel.cs`、`RecognitionProjectConfigurationReadModel.cs`：均为目录/配置/金额。
- **41 个 `ReadModels/*.cs` 是孤岛契约（找到但未接线）**：`server/Dy.MedicalRecognition.Application.Contracts/ReadModels/` 下 41 个 `sealed record`，含 `MedicalReportListReadModel`、`MedicalReportVersionListReadModel`、`MedicalReportVersionDetailQueryReadModel`、`MedicalReportVersionContentReadModel`、`MedicalReportVersionPdfQueryReadModel`（`FileId`、`PdfDownloadUrl`）、`PdfAndImageAccessReadModel`、`ReportVersionCommonReadModel`、`StatisticsExportFileReadModel`（`byte[] FileStream`）等。全库 grep `ReadModels` 只命中这 41 个文件自身的 namespace 行（`:1`），**无任何引用方**。

## 2. DDL / SQL 脚本与数据库现状

- 建表脚本目录：`server/Dy.MedicalRecognition.Repository/Scripts/`，共 **19 个 `.sql`**。
- 10 张阶段 4 表的 CREATE 脚本 **全部已存在**（均为 `create table if not exists`，PostgreSQL 方言）：
  `medical_recognition_report.sql`(17行)、`medical_report_version.sql`(44行)、`platform_patient.sql`(12行)、`laboratory_report_content.sql`(22行)、`laboratory_result_item.sql`(27行)、`laboratory_bacteria_result.sql`(26行)、`laboratory_antimicrobial_susceptibility.sql`(23行)、`examination_report_content.sql`(26行)、`examination_item.sql`(11行)、`examination_site.sql`(10行)。
- 另存在 **4 个阶段 5 表脚本**：`recognition_match_record.sql`、`recognition_match_item.sql`、`recognition_processing_result.sql`、`recognition_reference.sql`。
- **物理表名不是 `mrec_*`**：上述 14 个脚本用**无前缀**表名（如 `medical_recognition_report`、`platform_patient`）；只有阶段 1-3 的 5 张表是 `mrec_*`（`mrec_medical_standard_category`、`mrec_medical_standard_group`、`mrec_medical_standard_item`、`mrec_mutual_recognition_item`、`mrec_organization_hospital_branch_recognition_amount`），且这 5 个脚本带 `comment on` 与唯一索引，报告 10 个脚本**没有注释也没有索引/唯一约束**。
- 与计划的口径冲突（事实并列，未做裁定）：`docs/plans/001-总体计划/impl.md:189` 写阶段 5 将创建 `mrec_recognition_match_record` 等 4 张带前缀表，但现有脚本与 SqlMap 用的是无前缀名。
- **数据库实测（只读）**：对 `server/Dy.MedicalRecognition/EarthraceConfig.json:6` 所指库（`Host=183.224.180.166;Port=15432;Database=DysoftHIS`，DBX 连接"协同平台外网"）查询 `information_schema.tables`：
  - 命中的 mrec/报告相关表仅 8 张：`mrec_medical_standard_category`、`mrec_medical_standard_group`、`mrec_medical_standard_item`、`mrec_mutual_recognition_item`、`mrec_organization_hospital_branch_recognition_amount`，以及**不属于本项目 SqlMap 的遗留表** `medical_standard_category`、`medical_standard_group`、`medical_standard_item`。
  - **10 张阶段 4 报告表在库中一张都不存在；4 张 recognition_* 表也不存在。**（即：脚本已写、未执行。）

## 3. PDF / 文件处理

### 3.1 本项目 server 代码中（**未找到**）

- 全库 grep `IFileService|IFile|Pdf|PDF|FileId|SaveBytes|SaveText|Upload|Stream|byte\[\]` 仅命中：读模型/实体的 `PdfFileId`/`PdfFileName` 文本（`MedicalReportVersion.cs:155,159`、`MedicalReportVersionDto.cs:155,159`、`MedicalReportVersionListReadModel.cs:32`、`MedicalReportVersionPdfQueryReadModel.cs:8,16`、`PdfAndImageAccessReadModel.cs:8,12,16`）与 `StatisticsExportFileReadModel.cs:16 byte[] FileStream`，以及测试里读取 dll 的 `File.ReadAllBytes`（`Stage2EnumContractTests.cs:63`）。
- **没有任何文件存储抽象、文件服务客户端、上传/下载端点、Controller 或 Facade**；本项目也没有 `Controllers/` 目录。
- 配置只有一项：`server/Dy.MedicalRecognition/appsettings.Development.json:60-62` `"FileServer": { "LeaderUrl": "localhost:9333" }`；`appsettings.json` 中**没有** `FileServer` 节。文档口径一致：`.agents/instructions/test-environment.md:19`（文件服务 → `localhost:9333`）、`docs/plans/002-阶段0-现状对齐/impl.md:302,304`（"文件服务（PDF 上传下载）| 配置 有 | 调用实现 未发现"）、`docs/plans/001-总体计划/impl.md:179`（PDF 处理依赖文件服务配置，不可用时只暂停受影响验证）。
- 宿主 `Program.cs` 不在仓库内（`server/Dy.MedicalRecognition/` 只有 `appsettings*.json`、`EarthraceConfig.json`、`csproj`、`Properties/launchSettings.json`），启动装配来自 `Dy.Apron.QuickStart`；**未确认**宿主是否在运行时注册了某个 `IFileService` 实现。

### 3.2 框架包（PackageReference 与本地 NuGet 缓存）

`server/Directory.Packages.props` 的版本：`Dy.Base.Application.Contracts 1.0.0.184`、`Dy.Core.Abstractions 1.1.0.54`、`Dy.Core.Extensions 1.1.0.54`、`Dy.Core.SourceGen 1.0.0.66`、`Dy.Apron.* 1.1.0.48`、`Dy.Earthrace(.Abstractions) 1.0.0.60`、`Npgsql 10.0.3`、`Dy.Plugin.AspNet.Scalar 0.2.0.16`、`Microsoft.AspNetCore.OpenApi 10.0.9`。

**找到**（唯一可用的文件 API 契约）——`C:\Users\test\.nuget\packages\dy.core.abstractions\1.1.0.54\lib\net10.0\Dy.Core.Abstractions.dll`（被 Domain / Application / Application.Contracts / Repository 直接引用）：
- `Dy.Core.Abstractions.IO.IFileService`，方法（IL 元数据去符号）：`DeleteAsync`、`ReadAsync`、`ReadFileTextAsync`、`SaveBytesAsync`、`SaveTextAsync`
- `Dy.Core.Abstractions.IO.IFile` / `Dy.Core.Abstractions.IO.File`：属性 `FileName`、`Length`、`Stream`
- `Dy.Core.Abstractions.Domain.Dtos.FileIdDto`：`FileId`（string）
- 注意版本差异：`IRemoteFileService` / `RemoteFile` 存在于 1.0.0.x 至 1.1.0.49，在 **1.1.0.53/1.1.0.54 已移除**。

**未找到**：
- 本地 NuGet 缓存中**没有任何 `IFileService` 的实现类型**（对全部 `dy.*` 包 net8/9/10 的 `lib` DLL 做类型名扫描，含 `File` 的类型只有：`Dy.Core.Abstractions.IO.{File,IFile,IFileService}`、`Dy.Core.Abstractions.Domain.Dtos.FileIdDto`、`Dy.Apron.Http.Contracts.{FormFileWrapper,IFormFileWrapper}`（表单文件绑定，非存储）、`Dy.Earthrace.Utils.FileSqlMapUtil`、`Dy.Core.Utils.FileHelper`（`Dy.Core.Utils` 未被本项目引用）、`Dy.Base.Application.Contracts.UserAggregate.DeleteFileRequest`）。
- `Dy.Base.Application.Contracts` 1.0.0.184 **没有文件上传/下载/读取应用服务**；与文件有关的只有 `UserAggregate.DeleteFileRequest`（`FileId`）与 `IUserAppService.ChangeAvatarAsync`（头像场景），**不能作为报告 PDF 存取契约**。
- 本项目代码中**没有** `Blob/Oss/S3/Minio/IFormFile/IStorageFile` 等任何文件存储符号。

### 3.3 同盘参考项目如何存文件（**找到**，可复用样板）

`E:\MedSync\Dy.LisCenter\server\`（Dy.LisCenter，非本仓库）采用**自建本地文件存储**：
- 契约 `Dy.LisCenter.Application.Contracts/ApplicationAggregate/ILabReportFileStore.cs`：`Task<string> SaveAsync(Stream)` 返回服务端文件键、`Task<Stream?> OpenReadAsync(string fileKey)`、`Task DeleteAsync(string? fileKey)`。
- 实现 `Dy.LisCenter.Repository/ApplicationAggregate/LocalLabReportFileStore.cs:9` `sealed class LocalLabReportFileStore : ILabReportFileStore, ITransientDependency<ILabReportFileStore>`：
  根目录 `AppContext.BaseDirectory/App_Data/LabReports`（:11-12）；键为 `<yyyyMMdd>/<guid:N>.pdf`（:17）；`FileMode.CreateNew` 写、失败清理（:24-32）；`ResolvePath` 做目录穿越防护（:63-70）。
- 上传入口用 multipart：`Dy.LisCenter/Controllers/ExternalReportFile/ExternalUploadLabReportForm.cs:14-16` `[FromForm(Name="pdfFile")] public IFormFile PdfFile`；控制器 `ExternalReportFileController.cs` 注入 `ILabReportFileStore`（:34,63），`SaveAsync` 得新键（:104），成功后删旧键（:150）、失败时回滚删新键（:186）；下载在 `PageLabReportFile/PageLabReportFileController.cs:108` `OpenReadAsync(report.PdfFileKey)`。
- 相关测试可作样板：`Dy.LisCenter.Application.Tests/ApplicationAggregate/T23ReportFileSystemCompensationTests.cs`、`LocalLabReportFileStoreFailureCleanupTests.cs`、`ExternalReportFileControllerCompensationTests.cs`。
- 该项目**未使用** `Dy.Core.Abstractions.IO.IFileService`（全 `E:\MedSync` 范围 grep `IFileService` 在 `.cs` 中命中 0）。

## 4. 测试工程

- 位置：`server/Dy.MedicalRecognition.Tests/`（单工程）。
- 框架：**xUnit** —— `Dy.MedicalRecognition.Tests.csproj` 引用 `xunit`、`xunit.runner.visualstudio`、`Microsoft.NET.Test.Sdk`，版本在 `server/Directory.Packages.props:20-23`：`xunit 2.9.3`、`xunit.runner.visualstudio 3.1.5`、`Microsoft.NET.Test.Sdk 17.14.1`、`Microsoft.CodeAnalysis.CSharp 4.11.0`；`IsTestProject=true`，并有 `xunit.runner.json`（csproj 注释：并行口径由该文件关闭）。
- 现有测试类共 **16 个**：`SmokeTests`、`RequestValidationProbeTests`、`Stage1ArchitectureTests`、`Stage1SqlMapProbeTests`、`Stage1ReadModelEnumTextTests`、`Stage2ContractTests`、`Stage2EnumContractTests`、`Stage2EnumMetadataQueryTests`、`Stage2QueryTests`、`Stage2WritePathTests`、`Stage3OrganizationPathTests`、`Stage3QueryTests`、`Stage3SqlMapTests`、`Stage3TrustedScopeTests`、`Stage3WritePathTests`、`Architecture/SourceGuardReuseTests`（另有辅助类型 `Architecture/SourceSyntaxGuard.cs`、`TrustedRequestContext.cs`、`StubOrganizationAppService.cs`、`StubUserAppService.cs`）。
- **报告模块测试类：0 个。** 现有关联断言只有：`Stage1SqlMapProbeTests.cs:114-115`（锁定 `MedicalRecognitionReport.QueryAllRecognitionReference`/`RecognitionReferenceColumns` 必须注册）、`Stage2EnumContractTests.cs:483-507`（锁定当前 25 条 `/Api/...` 路由，其中无任何报告接口）。
