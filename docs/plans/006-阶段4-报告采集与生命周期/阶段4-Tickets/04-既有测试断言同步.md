# 04: 既有测试断言同步

**What to build:** 让既有测试断言与本阶段已确认设计同步，并保持「只收紧、不放宽」的方向。SqlMap 探测断言由「聚合名作用域键必须注册」改为锁定「实体名作用域与对应语句标识」；阶段 5 的四个实体键不在本阶段改动范围，其作用域与语句归属由阶段 5 按其自身设计处理，本票只保证不因本阶段十个表文件的语句补齐而破坏该断言中与本阶段无关的部分。语句调用点的完整核对（每个调用点显式传作用域与语句标识）**不属本票**：调用点由票 05、06a、06b、07 落地，其完整核对由票 16 承接。自动端点清单断言的最终形态（锁定本阶段新增后的完整清单）同样**不属本票**：端点由票 05 与票 08 交付，该断言由票 17 承接；本票只保留 SqlMap 探测断言一面。

**Blocked by:** 01（建表脚本与 SqlMap 物理映射）、03（报告读模型重建与分页契约）

**Status:** ready-for-agent

**外部前置：** 无（本票只依赖票 01 的物理映射声明与票 03 的读模型契约）

- [ ] SqlMap 探测断言锁定实体名作用域与语句标识；不再以聚合名作用域作为通过条件（V81 的静态核对面）
- [ ] 阶段 5 的四个实体键的相关断言未被本票改动，且仍可通过（V81，属断言范围约束）
- [ ] 本票未改动自动端点清单断言（该断言由票 17 在其前置交付后落地）（无矩阵编号，属改动范围约束）
- [ ] 断言在既有测试套件中真实执行，失败会准确指向作用域偏差（V81）
- [ ] 未删除、跳过或放宽任何既有有效断言（V81）
- [ ] `git diff --check` 干净；证据记入阶段测试报告（无矩阵编号）

**口径：** 本票覆盖层级为既有断言的源码契约面（V81 的静态核对面）；接口的真实行为由票 05、08 承接。矩阵编号可跨批次引用，按完整用例只统计一次，只完成部分层级不记为通过。
