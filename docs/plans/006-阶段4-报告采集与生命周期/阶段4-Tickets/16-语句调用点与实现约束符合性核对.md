# 16: 语句调用点与实现约束符合性核对

**What to build:** 在报告读写调用点全部落地之后，一次性核对数据访问调用点与本阶段的实现约束，使这两类静态要求有明确归属，不在各实现票之间悬空。调用点面：每个数据访问调用显式传作用域与语句标识，作用域为实体名，不调用共享映射器的上下文设置方法；只访问 `mrec_` 前缀表；本阶段十个表文件里没有业务调用点的无条件全量查询已移除。实现约束面（来自阶段设计决策，不属任何单一功能的交付内容）：未重跑后端代码生成器，仍按受保护的手工维护推进；再生成保护清单中的配置与启动文件未被生成器覆盖；单一聚合、单套管理器与单套仓储，未新增第二套实现；后端未新增异常中间件、异常过滤器、响应转换器或错误包装模型，业务失败直接抛出，仓储的唯一约束冲突翻译只在仓储到管理器内部使用。核对以静态断言或源码守卫为证据形态，不以"人工确认已遵守"作为证据。

**Blocked by:** 05（报告与版本基础链路）、06a（检验报告完整内容）、06b（检查报告完整内容）、07（重提交、并发与文件补偿）、08（管理端报告列表查询与 PDF 下载）

**Status:** ready-for-agent

**外部前置：** 无（全部为静态核对，不需要目标库或存储目录）

- [ ] 每个数据访问调用点显式传作用域与语句标识；作用域为实体名；不调用共享映射器的上下文设置方法（V81 的调用点对齐与作用域两个子面）
- [ ] 调用点只访问 `mrec_` 前缀表；本阶段十个表文件中无业务调用点的无条件全量查询已移除（V81 的物理表子面）
- [ ] 未重跑后端代码生成器；再生成保护清单中的文件未被生成器覆盖（无矩阵编号，承接 S4-D1）
- [ ] 未新增第二套聚合、管理器或仓储实现（无矩阵编号，承接 S4-D2）
- [ ] 后端未新增异常中间件、异常过滤器、响应转换器或错误包装模型；业务失败直接抛出（无矩阵编号，承接 S4-D24）
- [x] 并发冲突口径：由数据库唯一索引兜底，数据库异常按原样向外传播，仓储与管理器均不识别、不翻译冲突（无矩阵编号；原「唯一约束冲突翻译只出现在仓储到管理器内部」一项随之废止）
- [ ] 上述核对以静态断言、源码守卫或可指到的静态检查输出为证据，不以人工确认为证据（无矩阵编号，属证据形态要求）
- [ ] 未删除、跳过或放宽既有源码守卫类断言（无矩阵编号，属断言纪律）
- [ ] `git diff --check` 干净；证据记入阶段测试报告（无矩阵编号）

**口径：** 本票覆盖层级为源码契约与静态核对层（V81 的两个子面——调用点显式对齐语句标识、仅访问 `mrec_` 表，以及调用点完整面；另含 S4-D1、S4-D2、S4-D24 的实现约束面）；V81 的其余子面（作用域为实体名、不调用上下文设置方法、查询 SQL 无方言特征）由票 01 承接。实现约束面不出现在验证矩阵中，属设计决策的符合性核对。矩阵编号可跨批次引用，按完整用例只统计一次，只完成部分层级不记为通过。
