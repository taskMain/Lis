# 00: UML2 上游同步

**What to build:** 让配置界面查询模型与阶段 4 设计对齐，使后端读模型重建与 API Client 生成有唯一的上游依据。同步内容为五项：报告责任人员改为结构化字段（报告医生名称、审核医生名称，检验版本另带检验人名称与明细检测人，检查版本另带检查医生名称）；版本文件信息去掉对外下载地址与文件标识字段；列表读模型补齐来源组织、医院、院区名称回填与患者姓名、证件号码、当前版本序号；版本列表读模型保留「是否已被后续版本替代」；下载与版本详情查询按报告标识加报告版本标识两入参定位。

**Blocked by:** None（可立即开始）

**Status:** ready-for-agent

**外部前置：** 无（本票只改上游文档，不改代码与生成物）

- [ ] 责任人员字段在版本列表与版本详情两处读模型中都改为结构化字段，且与「不做字符串拼接、各版本只返回自身提供的人员事实」一致（V84，交接依据）
- [ ] 版本文件信息读模型不再包含文件标识与对外下载地址字段（V84，交接依据）
- [ ] 列表读模型字段覆盖列表展示与筛选所需的组织、医院、院区名称、患者姓名、证件号码与当前版本序号（V84，交接依据）
- [ ] 版本列表读模型同时保留「是否当前有效版本」与「是否已被后续版本替代」（V84，交接依据）
- [ ] 下载与版本详情查询的入参为报告标识与报告版本标识（V75、V76，交接依据）
- [ ] 文档内既有模块注记（按可信组织、医院、院区归属返回数据；开始与结束时间按报告时间过滤）保持有效（无矩阵编号）
- [ ] 同步内容与 `阶段4-规格.md` 的上游同步点登记逐条对应；该表 UML2 行的状态更新为本票的完成结果（票面不改动设计决策与批次表，见 README 约定 6、11）（无矩阵编号）
- [ ] `git diff --check` 干净；证据记入阶段测试报告（无矩阵编号）

**口径：** 本票覆盖层级为上游文档同步面，不产生可执行的矩阵层级证据；其交付质量由票 03 的契约核对与票 12 的生成物复核承接。矩阵编号可跨批次引用，按完整用例只统计一次。
