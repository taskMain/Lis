# 12: 宿主链路验收与阶段报告

**What to build:** 在真实环境完成阶段 4 的验收并收口阶段报告：从宿主登录页与菜单进入两个页面完成真实只读验收（列表可查、详情可看、版本可切换、PDF 可下载，网络与业务状态一致），用两个既有测试身份交叉验证报告列表与版本可见性（一方提交方、另一方查看方），并取得真实数据库、真实文件系统与真实生产入口上的证据。执行前提：目标库与 schema 已由负责人确认、三批建表全部完成、存储根目录已配置且可写、宿主菜单已配置并可进入两个页面；未满足的条目记 `Blocked`，其余未执行条目记 `NotRun`，不以空结果、构建成功或接口直测替代业务通过。验收数据只经正常业务接口或页面形成，报告数据由两个提交接口本身构造，不直接写库、不使用脚本注入。验收范围包括：四个接入能力的成功与失败语义、报告与版本生命周期、患者归属与冲突拒绝、作废边界与幂等、PDF 落盘与补偿、管理端列表的筛选与排序与分页、版本列表与详情、下载的三个失败面与跨院区归属拒绝、下载入参与报告标识加版本标识的契约一致、患者筛选的前缀匹配与规范化边界。阶段报告按项目测试规范写入阶段根测试报告：结果表、执行命令与证据出处、复用条件、未覆盖面、残余风险；已登记的三项已接受风险（历史版本文件保留、孤儿文件、跨组织宿主面）与一项框架观察面按同一口径登记，不因验收而改变结论口径。

**Blocked by:** 01（建表脚本与 SqlMap 物理映射）、04（既有测试断言同步）、05（报告与版本基础链路）、06a（检验报告完整内容）、06b（检查报告完整内容）、07（重提交、并发与文件补偿）、08（管理端报告列表查询与 PDF 下载）、14（平台管理员页）、15（医院管理员页与可信范围）；同时以票 16、17 的静态核对结果作为收口前置——这两票的失败项不由本票吸收，需先在其自身范围内改正

**Status:** ready-for-agent

**外部前置：** 负责人确认目标库与 schema 并完成三批建表；存储根目录可写；宿主菜单与角色授权已配置；宿主请求体上限与部署侧同步已登记；上述前置未就绪时，依赖该前置的勾选项在阶段报告记 `Blocked`，其余未执行项记 `NotRun`

- [ ] 从宿主登录页与菜单进入平台管理员页，列表可查、详情可看、版本可切换、PDF 可下载；网络与业务状态一致（C28）
- [ ] 从宿主登录页与菜单进入医院管理员页，范围固定为可信组织与医院，行为与上一条一致（C29）
- [ ] 两个测试身份交叉验证：提交方医院上传的报告在查看方入口按归属范围可见性正确（跨医院与跨院区定位不可得）（C27、C28、C29，以及 V62、V91 的归属面）
- [ ] 宿主失败态：触发真实接口失败时页面保留数据、结束加载、允许重试；错误提示由宿主统一展示；不误刷新、不误报成功（C30）
- [ ] 下载跨院区归属失败：以另一身份的报告与版本标识发起下载时服务端拒绝，页面保留当前详情与版本选择、不误报成功（C31、V91）
- [ ] 四个接入接口在真实生产入口下的成功与失败语义正确；失败不产生业务数据（V1、V4、V17、V18、V19、V20、V89）
- [ ] 报告与版本生命周期、患者归属与冲突拒绝、作废边界与幂等、文件补偿在真实数据库与真实文件系统上取得证据（V2、V10、V11、V12、V13、V14、V15、V16、V45、V60、V79、V85、V86）；其中 V85、V86 的冲突处理已按设计变更改为按数据库原始失败暴露，原并发证据作废，两条在阶段报告结果表记 `PendingRetest`，本勾选项的这两条需按新语义另行取证
- [ ] 列表排序与分页在真实库上成立：跨页无重复无遗漏、越界页空集合且总数保留；患者筛选前缀匹配与规范化边界（含空白输入、小写输入、通配字符）成立（V64、V65、V67、V68、V90）
- [ ] 目标库中十张表的实际列、类型、可空性、注释与索引与最终脚本一致，且未创建或改动其他对象（V80b）
- [ ] 矩阵全量检查：后端 93 条与前端 31 条的编号、分段与批次分配一致；未取证条目逐条登记 `Blocked` 或 `NotRun`（无矩阵编号，属矩阵收口检查）
- [ ] 无可证据支撑的 `Passed`、`Complete`、`Ready` 或「验收通过」措辞；阶段报告含结果表、证据出处、复用条件、未覆盖面与残余风险（无矩阵编号，属报告纪律）
- [ ] 三项已接受风险与一项框架观察面按既定口径登记，重审条件不变（无矩阵编号，属风险登记口径）
- [ ] 票 16（语句调用点与实现约束）与票 17（自动端点清单断言）的结转结论已收口：两票的失败项已在其自身范围内改正，无未关闭残留（无矩阵编号，属收口前置）
- [ ] `git diff --check` 干净；构建、静态检查与适用的全量检查已执行（无矩阵编号）

**口径：** 本票覆盖层级为真实数据库、真实文件系统与宿主面（矩阵「真实数据库」列为是的条目与 V80b、V88、V90、V91，其中 V90 的层级为 `Application`、V91 为 `Application/Host`，以及 `Client/testPlan.md` 的 C27–C31）；本票是阶段收口票，不新增业务能力。矩阵编号可跨批次引用，按完整用例只统计一次。
