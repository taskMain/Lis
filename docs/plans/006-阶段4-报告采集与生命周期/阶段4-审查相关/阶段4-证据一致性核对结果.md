# 阶段 4 证据一致性核对结果

- 核对时间：2026-09-18
- 核对对象：阶段 4「报告采集与生命周期（F03）」在本轮收尾取证中发现的证据记录与代码、公共类型、配置事实不一致的条目
- 核对基准：`HEAD` 为阶段 3 的提交 `10b4313`；阶段 4 改动均未提交，差异范围为 `git diff HEAD` 与 `git status --porcelain` 的未跟踪项
- 核对方式：逐条打开被引用的文档、代码与配置原文对照，附实测命令或运行结果
- 本文件只登记证据一致性问题，不替代代码规范审查与前端专项审查结果

## 一、已核对且未发现问题

1. **生成端点清单**：`client/packages/api-client-medical-recognition/openapi/medical-recognition.openapi.json` 的 `paths` 共 39 条，阶段 4 声明的 9 条自动端点与 3 条手写路由全部存在；`kiota-lock.json` 的 `excludePatterns` 为 `/auth/login`，生成产物目录下无 `auth/login`。`testReport.md:20`（C1 行）与 `testReport.md:22`（C2 行）的端点清单描述与文件事实一致。核对方式：读取 OpenAPI 文件并逐条匹配路径，读取 `kiota-lock.json` 与 `src/api/` 目录树。
2. **生成物手写入口保留**：`src/api/index.ts` 的 `ApiRequestBuilder` 四个导航属性与生成结果一致，无手写内容被覆盖痕迹。核对方式：读取该文件全文。
3. **构建与测试基线**：本轮复跑 `dotnet test server\Dy.MedicalRecognition.slnx` 得失败 0、通过 420、跳过 0、总计 420；复跑 `pnpm -F dy-medical-recognition test` 得 12 个测试文件通过、259 项通过、2 项跳过、0 失败。与 `testReport.md:53` 和 `testReport.md:47` 登记的数字一致。核对方式：实际执行两条命令。
4. **矩阵编号无缺号**：`Server/design.md` 的矩阵含 V1-V91（含 V19b、V80b）共 93 条，无缺号、无重号。核对方式：正则提取矩阵行首编号后逐一比对。
5. **阶段 4 前端交付物文件清单**：`reportsApi.test.ts:8` 之外的七个实现文件与两个用例文件均实际存在，`testReport.md:48` 列出的文件名与磁盘一致。核对方式：目录列举。

## 二、问题清单

| 编号 | 严重度 | 位置 | 问题描述 | 对照依据 | 实测事实 | 建议处理 | 是否阻塞收口 |
|---|---|---|---|---|---|---|---|
| E-01 | `high` | `testReport.md:56`、`:57` | V38「检验报告作废后停止追加」、V41「检验明细来源字段原文保存」、V54「检查报告来源检查类型与诊断」、V56「检查项目与部位可选人员成对规则」、V58「检查报告作废后停止追加」、V77「下载文件缺失」、V78「已作废报告的历史版本下载」七个编号既未进入 `:56` 的已覆盖清单，也未进入 `:57` 的未自动化清单，属两处清单同时遗漏 | `testing-baseline.md` 第 3 节状态定义；第 4 节完成证据 | `:56` 的覆盖范围为 V1-V16、V19b、V21-V36、V42-V44、V46-V52、V55、V60、V61-V74、V80、V81、V83、V84、V85、V86、V87、V89、V90；`:57` 的未自动化范围为 V17、V18、V37、V39、V40、V45、V57、V59、V60、V79、V85、V86、V88、V89、V91 与 V19、V19b、V20、V80b、V82、V69。两处清单均不含上述七个编号。行为面核对：V41 的来源字段与 V54 的来源检查类型与诊断字段仅出现在夹具输入（`Stage4WritePathTests.cs:1540-1549`、`:1606-1615`），无持久化断言；V58 的全部作废用例走 `VoidLaboratoryReportAsync`，检查链路的作废后拒绝从未断言；V77、V78 的下载入口 `OpenReportVersionPdfAsync` 在测试工程中从未被调用 | 在两处清单中补登这七个编号，按实际状态逐条记 `NotRun` 或登记为残余风险；V56 另见 E-05 | 是 |
| E-02 | `high` | `testReport.md:56` | 声称 V34「药敏归属字段不可为空」、V43「异常标志与危急值标志取值」、V48「检查项目必填字段」、V50「检查部位归属项目」、V67「列表排序稳定」已有自动化覆盖 | 同上 | 五个编号在 `server/` 全目录搜索零命中；V50 的部位归属在 `Stage4WritePathTests.cs` 的检查报告用例中有间接涉及，但无编号回挂也无独立断言；V48 的编号在 `RequestValidationProbeTests.cs:11` 出现，但该文件用例全部针对阶段 1/2 的请求类型，属跨阶段编号重号，不构成阶段 4 的覆盖 | 同 E-01，逐条按实际覆盖面登记 | 是 |
| E-03 | `high` | `testReport.md:48` | 声称两条报告路由「已接入 `routes.tsx` 并同步 `routes.test.ts`」 | `testing-baseline.md` 第 2 节禁止事项 | `routes.test.ts:56-61` 的路径清单为 `standard-catalog`、`recognition-projects`、`recognition-amounts`、`branch-recognition-amounts` 四项，不含 `report-management` 与 `branch-report-management`；该断言使用 `expect.arrayContaining`，缺项时不失败，因此该文件无法发现这两条路由的缺失。`routes.tsx:48` 与 `:51` 确实已注册这两条路径 | 把路径清单改为完整集合相等并补两条挂载用例；同步修正 `testReport.md:48` 的表述 | 是 |
| E-04 | `high` | `client/apps/dy-medical-recognition/src/pages/reportManagement/reportsApi.test.ts:8` | 声称页面级行为「由 `ReportManagement.test.tsx` 与 `BranchReportManagement.test.tsx` 覆盖」 | `testing-baseline.md` 第 1.6 条；`frontend-testing.md` | `BranchReportManagement.test.tsx` 不存在。目录下实际文件为 9 个，无该用例文件；全 `client` 目录搜索 `BranchReportManagement` 仅命中 `routes.tsx:10`、`:51`、`BranchReportManagement.tsx:3`、`:17`、`:37`、`:40` 与该注释本身，无任何测试文件引用或渲染该组件 | 补 `BranchReportManagement.test.tsx` 覆盖医院管理员页；在补测完成前修正该注释 | 是 |
| E-05 | `medium` | `Server/design.md:349`、`阶段4-Tickets/06b-检查报告完整内容.md:14` | V56「检查项目与部位可选人员成对规则」要求验证两级明细的人员成对规则，但该字段在设计与公共类型中均不存在，用例无法构造 | `docs/需求规约SRS.md:1217`、`:1219` 的两张属性表；`Server/design.md:146-159` 的两张字段表 | 提交输入 `ExaminationItemRequest`（`MedicalReportSubmissionRequests.cs:321-331`）只有来源项目名称、来源项目编码、互认项目编码、部位集合四个成员；`ExaminationSiteRequest`（`:336-342`）只有来源部位编码与部位名称。SRS 的属性表与设计的字段表同样未定义人员字段。SRS 真正要求成对人员的两级是报告级检查医生（`SRS:1193`）与检验侧检测人（`SRS:1067`） | 已按负责人裁定改为记 `N/A` 并写明依据；票 06b 已移除该编号引用 | 否 |
| E-06 | `medium` | `Server/design.md:64`、`阶段4-Tickets/02-PDF存储与宿主请求体配置.md:18` | 设计要求「宿主请求体上限与部署侧反向代理上限需同步到同一取值（宿主配置点与归属在实施批次 0 登记）」，票 02 勾选项声称该配置点与归属已登记，但源码内无显式配置点 | `Server/design.md:64`；`阶段4-Tickets/02-PDF存储与宿主请求体配置.md:18` | `appsettings.json` 与 `appsettings.Development.json` 均无请求体上限键（`MaxRequestBodySize`、`RequestSizeLimit`、`MultipartBodyLengthLimit`、`FormOptions` 在 `server/` 内全部零命中）。实测宿主在 104857600 字节处返回 HTTP 400 并给出 `The max request body size is 104857600 bytes.`，该值与 `ReportPdfFiles:MaxFileBytes` 相同；该上限由框架程序集 `Dy.Apron.dll` 调用 `ConfigureKestrel` 设置 `KestrelServerLimits.MaxRequestBodySize` 生效，应用源码内无对应配置键 | 在配置或启动代码中显式登记该上限，使其与 `MaxFileBytes` 取自同一处，可核对、可同步；票 02 勾选项在完成该项后再勾 | 是 |
| E-07 | `medium` | `ReportPdfFileController.cs:210` | 控制器动作的「超过单文件上限」拒绝分支在宿主当前上限下不可达 | `Server/design.md:313`（V20 行）「超过配置的 `ReportPdfFiles:MaxFileBytes` 时由控制器动作在读取文件流前拒绝」 | 框架层上限与 `MaxFileBytes` 取值相同，均为 104857600 字节。实测 100 MiB 请求在 multipart 读取阶段即被框架以 HTTP 400 拒绝，请求未到达控制器动作；99.999 MiB 请求可到达控制器并继续业务校验。因此 V20 登记���「控制器在读取文件流之前按该配置值拒绝超限请求」在当前配置下不会被实际触发，两层的失败语义也不同（框架层为 `400 application/problem+json`，控制器层为 `500 text/plain`） | 由负责人决定两者取值的相对关系：保持控制器上限低于框架上限，使控制器分支可达；或把超限拒绝的承载位置改为框架层并登记该结论 | 否 |
| E-08 | `low` | `.agents/instructions/agent-conduct.md:53` | `git diff --check` 报该行行尾多余空白，退出码 2 | 仓库根 `AGENTS.md` 第 2 节第 7 条要求收尾执行 `git diff --check` | 该行内容为负责人本人的未提交编辑（在禁止进入 plan mode 之后补充「和 "目标"(Goal) 功能」），行尾含一个空格。该文件不属阶段 4 改动范围，本轮未对其执行任何写操作 | 由负责人裁定是否清理该行尾空白 | 否 |

## 三、无法确认项

| 编号 | 位置 | 需要什么证据才能确认 | 建议的取证方式 |
|---|---|---|---|
| U-01 | V88「宿主请求体上限与超限失败语义」的部署侧反向代理面 | 部署侧反向代理的实际配置与请求体上限取值 | 仓库内无 nginx、IIS `web.config`、Docker、网关等部署配置（全仓库搜索 `*.conf`、`nginx*`、`Dockerfile*`、`docker-compose*`、`*.yaml`、`*.yml` 仅命中 pnpm 工作区文件与技能配置）。该面需部署侧提供配置后取证，当前记 `Blocked` |
| U-02 | V77「下载文件缺失」与 V78「已作废报告的历史版本下载」的规范符合性判定 | 测试工程是否应建立真实文件系统测试基础设施 | 现有后端测试工程全部使用替身（`WebApplicationFactory`、`TestServer`、`HttpClient`、`Microsoft.AspNetCore.Mvc.Testing` 零命中），下载入口 `OpenReportVersionPdfAsync` 在测试中从未被调用。该两项要求真实文件系统与真实入口，是否在本阶段建立该基础设施需负责人裁定 |
| U-03 | `MedicalReportVersionPdfQueryReadModel` 与 `ReportPdfDownload` 的归属 | 是否为后续阶段预留 | 两者在生产代码中未找到调用方。需查阶段 5 及后续阶段的设计确认其归属 |

## 四、覆盖度自评

- **核对方式**：全部条目均为打开原文逐条对照，不依据文档自述。涉及运行事实的条目（E-06、E-07）在真实宿主上实测取得；涉及代码事实的条目使用文件读取与正则检索取得。
- **实测环境**：宿主以 `ASPNETCORE_ENVIRONMENT=Development` 启动，监听 `http://localhost:15014`；前端开发服务器监听 `http://localhost:3008`；数据库只读核对使用 dbx MCP 连接 `DysoftHIS` 的 `public` schema。
- **探针无残留**：V88 取证的探针文件写在 `var/probe/`（被仓库根 `.gitignore` 的 `var/` 规则覆盖），探针请求的报告文档故意非法，业务侧必然拒绝，事务回滚；只读核对 `mrec_medical_recognition_report` 中无探针报告单号，存储目录 `var/report-pdf/` 无本轮新增文件。
- **未覆盖面**：本文件不评价代码规范符合性与前端专项，另见同目录的代码与规范符合性审查结果、前端子应用审查结果。本文件不重复登记测试覆盖率缺口的完整清单，该清单属测试合理性审查的范围。
- **编号说明**：本文件的 E-01 至 E-08 与 U-01 至 U-03 编号只在本文件内有效，不进入阶段设计文档。

## 五、对阶段收口的判定

E-01 至 E-04 属证据记录与事实不一致，其中 E-01、E-02、E-03、E-04 的四面均涉及「声称已覆盖而实际未覆盖」，在 `testing-baseline.md` 第 3.1 节口径下先行消除。E-05 已按裁定处理完毕。E-06、E-07 需负责人决定处理方式。E-08 待负责人裁定。

本文件不声明阶段 `Complete`、`Ready` 或「验收通过」。
