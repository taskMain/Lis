# 阶段 4 任务票审查结果（DS）

- 审查时间：2026-09-18（任务票审查轮·第 2 次；写入前已清空结果文件全部历史内容，本文件只承载本轮结论）
- 审查对象（票与行数；README 单列）：

| 票 | 文件 | 行数 |
|---|---|---|
| README | `阶段4-Tickets/README.md` | 59 |
| 00 | `00-UML2上游同步.md` | 20 |
| 01 | `01-建表脚本与SqlMap物理映射.md` | 21 |
| 02 | `02-PDF存储与宿主请求体配置.md` | 21 |
| 03 | `03-报告读模型重建与分页契约.md` | 21 |
| 04 | `04-既有测试断言同步.md` | 18 |
| 05 | `05-报告与版本基础链路.md` | 29 |
| 06a | `06a-检验报告完整内容.md` | 24 |
| 06b | `06b-检查报告完整内容.md` | 22 |
| 07 | `07-重提交并发与文件补偿.md` | 21 |
| 08 | `08-管理端报告列表查询与PDF下载.md` | 26 |
| 12 | `12-宿主链路验收与阶段报告.md` | 26 |
| 13 | `13-累计APIClient生成与适配层.md` | 22 |
| 14 | `14-平台管理员页.md` | 27 |
| 15 | `15-医院管理员页与可信范围.md` | 19 |
| 16 | `16-语句调用点与实现约束符合性核对.md` | 21 |
| 17 | `17-自动端点清单断言.md` | 18 |

16 张票 + README 共 415 行、162 条勾选项。票号 00–08、12–17 连续（06a/06b 为同顺序位并列票），无缺号无重号；README 票一览 16 行与实际文件一一对应，标题逐字一致。

- 核对依据（本轮实际读取到的设计文档、批次表、矩阵、上行依据与代码事实）：
  - 阶段设计（本轮未修改，与上一轮读取版本一致）：`design.md`（105 行，决策台账 S4-D1–S4-D43）、`Server/design.md`（384 行，验证矩阵 V1–V91、写用例事务表与框架机制映射、索引定义节、提交请求字段章节）、`Client/design.md`（67 行）、`Client/testPlan.md`（46 行，C1–C31）、`阶段4-规格.md`（271 行，决策索引 43 条与上游同步点）
  - 批次表：`impl.md`（47 行，批次 0–5）、`Server/impl.md`（31 行，S1–S5）、`Client/impl.md`（29 行，C1–C5）、`testReport.md`（35 行，执行边界、待取证范围、已确认的验证降级）
  - 配置：`server/Dy.MedicalRecognition/appsettings.json`（33 行）、`appsettings.Development.json`（66 行）、仓库根 `.gitignore` 第 410 行
  - 任务票格式与约定先例：`docs/plans/005-阶段3-互认项目金额维护/阶段3-Tickets/`（本轮复核其 README 十条约约定、层级词表与票 04 的部分依赖写法）
  - 权威依据：`AGENTS.md`、`.agents/instructions/`（`planning-and-evidence.md`、`testing.md`、`testing-baseline.md`、`backend-design-gates.md`、`backend-architecture.md`、`backend-command-query-event.md`、`dy-framework-workunit.md`、`frontend-application.md`、`frontend-api-client.md`、`project-context.md`（§3.2 再生成保护清单）、`test-environment.md`）、`docs/需求规约SRS.md` F03.1/F03.2/F03.3 与设计约束、`CONTEXT.md` 报告采集段、`docs/uml/1-MedicalRecognitionReport.wsd` 与 `2-MedicalRecognitionConfigurationUiQuery.wsd`、`docs/plans/001-总体计划/design.md` §4.6
  - 仓库真实事实：`server/`（10 个报告实体、19 个 DTO、14 个报告 SqlMap、10 个建表脚本、宿主无控制器、管理器 17 个既有方法）、`client/apps/dy-medical-recognition/src/`（无分页设施、既有适配层与可信上下文接缝）；参考实现 `E:\MedSync\Dy.LisCenter` 的 `ExternalReportFileController`、`ILabReportFileStore`、`IntegrationPlatformOptions`、`tablePagination.ts`（仅同构机制对照）
- 未读取到的材料：
  - 按审查范围排除要求，`阶段4-审查相关/` 目录内所有文件未作为审查依据读取。
  - `.agents/instructions/` 中的 `csharp-*`、`frontend-style.md`、`frontend-testing.md`、`agent-conduct.md`、`repository-safety.md`、`gitnexus-workflow.md` 未逐行读取（本轮无结论依赖其条款）。
  - 无运行事实：10 张表未建、后端未启动、未执行 `restore`/`build`/Kiota 生成，因此票面主张中凡涉及运行结果的判断本轮只核对证据形态（第五节）。
  - 阶段目录内 `阶段4-现状取证-F03代码基线.md`、`选项配置.md` 不在审查对象清单内（票面亦未引用）。

## 一、已核对且未发现问题

1. **票号与文件对应、README 与票面依赖一致（机械核对 1）** — 脚本比对：16 张票文件与 README 票一览逐行一致（标题、Blocked by 集合）；票面 `Blocked by` 去掉区间与描述性括注后与 README 单元格的票号集合逐个相等（16/16 一致，含票 12 的"并以 16、17 的结转结论为收口前置"表述）；目录内无旧编号残留（无 09/10/11 文件，票面亦无对这些票号的引用）。
2. **依赖图为无环 DAG、无孤立票（机械核对 2）** — 脚本按 README 声明的边做传递闭包：无任何节点可达自身；00/01/02 无前置但分别被 03/13、04/05/12、05 依赖，12 为唯一收口票，无孤立票；各票「外部前置」已不再夹带票间依赖（仅票 04 的说明句"本票只依赖票 01 的物理映射声明与票 03 的读模型契约"与它的 Blocked by 一致，非新增边）。
3. **矩阵编号全覆盖、双向无悬空（机械核对 4）** — 脚本展开票面区间与子编号：后端 93 条（V1–V91 含 V19b/V80b）与前端 31 条（C1–C31）**全部被至少一票声明**；反向核对票面声明的编号全部存在于矩阵（无 V92、C32 之类）；票 16 按审查要求不引用矩阵条目（其口径只引用 V81 与 S4-D1/D2/D24 的约束面），未被计为"未承接"。
4. **票与三张批次表的编号与范围一致** — 批次 0↔00/01/02/03/04/17（含 V80/V81/V82/V83/V84/V88）、批次 1↔05（V1-V20、V19b、V87、V89）、批次 2↔06a（V21-V45）、批次 3↔06b（V46-V60）、批次 4↔13/14/15（C1-C31）、批次 5↔12（V61-V79、V80b、V88、V90、V91）；`Server/impl.md` S1–S5 与 `Client/impl.md` C1–C5 的分段与票的分工一致。V85/V86 由票 07 承接（批次表把它们列在批次 1），符合 README 约定 5"矩阵编号可跨批次、跨票引用，按完整用例只统计一次"。
5. **关键口径投影正确（逐条打开设计核对，抽样 16 处）** — ① multipart 两命名部件、成功返回布尔真、失败抛业务异常（05 对 S4-D6/D29、`Server/design.md:62/64`）；② 上限取配置并由控制器动作在读取文件流前拒绝（02 勾选项 14、05 勾选项 22 对 S4-D7、`Server/design.md:64/212`、V20）；③ 本地存储三操作、完整绝对路径、文件键格式、不覆盖写入、写失败清理、越出根目录拒绝（02 对 S4-D10/D37、`Server/design.md:212-223`）；④ 报告主体三列在同一事务内随版本更新且非空（05 勾选项 12、`Server/design.md:178/204`、V2）；⑤ 患者身份规范形式（去首尾空白、证件号码大写、版本表保留来源原值）（05 勾选项 18、08 勾选项 14 对 S4-D43、`Server/design.md:161/235`、V9/V10/V64/V86）；⑥ 作废只改状态与作废事实、两端边界包含、幂等与冲突、已作废不得再提交（05 勾选项 19/20 对 S4-D22、`Server/design.md:208`、V13-V16）；⑦ 文件名回退不拒绝、文件本身校验失败即拒绝（05 勾选项 21 对 S4-D36、V19/V19b）；⑧ 提交请求携带不可读文件键即拒绝且不产生业务数据（05 勾选项 23 对 `Server/design.md:191`、V89）；⑨ 请求反序列化拒绝未知字段、camelCase、`StandardProjectCode`（05 勾选项 15 对 S4-D29/D31、`Server/design.md:64/78`）；⑩ 明细唯一性三条与检验侧字段口径（06a 勾选项 12/13/15/19 对 S4-D30、`Server/design.md:111-132`、V23-V44）；⑪ 来源影像状态未提供按未知处理并保存（06b 勾选项 12/13 对 S4-D42、`Server/design.md:142`、V52）；⑫ 下载按报告标识与报告版本标识定位并按归属校验、三个失败面（08 勾选项 21/22、13 勾选项 18 对 S4-D18/D28、`Server/design.md:229`、V75/V76/V77/V91）；⑬ 分页窗口校验在首次仓储访问前、边界 1 与 200（08 勾选项 17、13 勾选项 16 对 S4-D3、`frontend-api-client` §3、V84/V90）；⑭ 名称回填分层批量、调用次数不随行数增长（08 勾选项 18 对 S4-D27、`Server/design.md:239`、V69）；⑮ 脱敏与枚举文本（08 勾选项 20、14 勾选项 22/23 对 S4-D12/D25）；⑯ 前端错误边界与四类失败态、页面无写入口（14/15 对 S4-D24、`Client/design.md:34`）。
6. **既有断言的拆分成立（本轮审查维度 3 指定项）** — 票 04 只保留 SqlMap 探测断言（其 Blocked by 为 01、03，勾选项含"本票未改动自动端点清单断言"）、票 17 只做自动端点清单断言（Blocked by 05、08），票 16 只在调用点全部落地后核对（Blocked by 05、06a、06b、07、08）；三票的口径各自声明自己的面（V81 静态核对面 / V83 端点形态与清单核对面 / V81 调用点完整面＋实现约束面），未相互重复声明同一层级。
7. **06a 与 06b 真并列（本轮审查维度 3 指定项）** — 两票的 Blocked by 均为 05，互不为前置；外部前置分别只列批次二 4 张表与批次三 3 张表；口径分别声明 V21–V45 与 V46–V60；票面只以"检查侧由票 06b 承接""批次 2 的检验侧由票 06a 承接"作交叉说明，未形成隐含顺序。
8. **05 与 08 的顺序已声明（本轮审查维度 3 指定项）** — 票 08 的 Blocked by 明确写"03、05——与票 05 的顺序理由：两个下载动作与两个上传动作落在同一个控制器类型上，下载动作在票 05 落地该类型后追加"，与 `Server/design.md:36` 的单一 `ReportPdfFileController` 类型一致。
9. **上游同步票与静态交付票的排序正确** — 00（UML2 同步）先于 03 与 13；01/02/03/04 先于 05；05 先于 06a/06b；08 在 03、05 之后；13 在 00/05/06a/06b/08 之后，14/15 在 13 之后；16、17 在全部实现票之后；12 在 14/15 之后并以 16/17 的结转结论为收口前置。无消费者早于上游的排序。
10. **实现约束有明确归属（README 约定 12 + 票 16）** — S4-D1（不重跑后端代码生成器、再生成保护清单不被覆盖）、S4-D2（单一聚合、单套管理器与仓储）、S4-D24（不新增异常中间件、过滤器、响应转换器或错误包装）集中由票 16 以静态断言或源码守卫核对，票 16 的勾选项逐条对应且明确"不以人工确认为证据"，票 12 勾选项 23 要求 16/17 的结转结论已收口。
11. **宿主请求体上限配置有票承接** — 票 02 更名并在 What to build 与勾选项 18 覆盖"配置点与归属已登记、取值与应用侧单文件上限一致、部署侧同步要求已写明并留待真实入口取证"，与 S4-D7、`Server/design.md:64`、V88 一致；票 12 的外部前置"宿主请求体上限与部署侧同步已登记"由此有了承接方。
12. **提交请求的契约载体已写明** — 票 05 的 What to build 明确"请求字段的填报要求以服务端设计的提交请求字段章节为正式载体（本票的勾选项只承载接口级口径，不复制字段表）"，与 S4-D33 一致，且勾选项 15 覆盖 camelCase、拒绝未知字段与 `StandardProjectCode` 命名。
13. **测试状态与证据纪律（机械核对 8）** — 162 条勾选项与各票头部均未出现 `Passed`、`Failed`、`Complete`、`Ready`、「验收通过」等无证据措辞；`Status: ready-for-agent` 与阶段 3 先例同名，README 约定 2 明确其为领取标签；无「批注」「负责人裁定」「回复原文」「确认来源」类痕迹；未引用 `阶段4-审查相关/` 目录（README 约定 9 反而将其排除在票范围外）。
14. **勾选项证据形态（机械核对 5）** — 逐条阅读未发现「正常」「合理」「完善」「尽量」类不可判定措辞（票 07 两处"正常"分别指"正常提交接口"与"可正常回读与下载"，语义具体）；证据形态均为可静态断言、可回读数据或可观察接口/页面行为；票 16 的静态断言与票 07 的失败注入替身均与测试基线一致（静态不冒充行为通过、失败注入只覆盖本项目代码路径）。
15. **票面无易腐内容（机械核对 6）** — 无代码块、无文件清单、无逐行实现步骤；对设计的引用为章节级或结论级（"以设计的索引定义小节为准""以票 00 同步后的上游模型为源""以服务端设计的提交请求字段章节为正式载体"），符合项目"设计文档承载路径与类型归属"的约定。
16. **阶段内规则与残余风险在票面体现** — 负责人在建的建表按批执行与未就绪记 `Blocked`／`NotRun`（各票「外部前置」已补该句）、验收数据只经正常接口或页面形成（07 勾选项 17、12 的 What to build）、不测框架事务与事件行为（07 的 What to build）、三项 AcceptedRisk 与一项框架观察面按既定口径登记（07 勾选项 5/6、12 勾选项 12）、宿主验收边界"本地直达、构建成功与接口直测不替代宿主验收"（12、README 约定 8）。
17. **排除清单未被反向纳入** — 逐票检索推送/PushCenter/影像文件接收/DICOM/版本差异比较或恢复/独立版本管理菜单/报告查看权限模型/上传界面：票面只在"不提供…"的意义上出现（14 的"不提供比较、修改、删除、恢复或重新设为当前版本"、08 勾选项 23 的"未引入报告查看权限模型或角色门控"），未声明任何被排除能力。
18. **票 12 的收口范围与批次 5 一致** — 勾选项覆盖两个页面的宿主实测、两个身份交叉验证、宿主失败态、下载跨院区归属拒绝、四个接入接口真实入口成功与失败语义、真实库与真实文件系统上的生命周期与补偿证据、目标库结构核对（V80b）、矩阵全量检查（93/31）、无 `Passed` 措辞、风险登记与 16/17 收口，口径列明 V90 与 V91 的矩阵层级。

## 二、问题清单

严重度取值：`blocker`（会导致实施失败或产出错误设计）、`high`（会造成返工或契约不一致）、`medium`（局部不一致或证据缺口）、`low`（表述、编号、引用类瑕疵）。
说明：负责人本轮声明"已整改"但未随要求提供条目化清单；本轮对上一轮问题涉及的同一批审查点做了独立复核，标 `重开` 的条目属**声明已完成但其口径在当前票面仍可复现**的项（判断依据全部来自本轮读取到的当前票面与设计）。

| 编号 | 严重度 | 位置（票号 + 段落名） | 问题描述 | 对照依据（设计条款/矩阵编号/规范条款/批次表） | 建议处理 | 是否阻塞实施 |
|---|---|---|---|---|---|---|
| DS5-01 | medium | 票 13「口径」段（第 22 行）与勾选项（第 11–20 行）；票 14 勾选项 13、17、18、24 | 票 13 的口径声明"C1–C10"，但 10 条勾选项未覆盖其中三条：**C7（分页控件配置，矩阵层级 `Unit`）**的内容中"页容量可选值、显示总数文案、加载中禁用"没有对应勾选项（"改变页容量回到第 1 页"与"不在页面内联分页对象"由票 14 的组件层勾选项 17/24 覆盖）；**C9（范围切换重置，层级 `Unit`）**在票 13 与票 14 的勾选项中都没有对应项（票 14 勾选项 13 只覆盖"筛选变化回到第 1 页并保留页容量"，不含"切换组织、医院或院区时清空旧数据与未提交筛选"）；**C10（过期响应丢弃，层级 `Unit`）**在票 13 无对应项（票 14 勾选项 18 有组件层对应）。按 README 约定 5"只完成某一层级不把整条用例记为通过"，这三条在票 13 的 Unit 层会缺证据，且 C9 的行为在两张页面票的勾选项中也未出现。 | `Client/testPlan.md:22/24/25`（C7、C9、C10 的层级 `Unit` 与预期）；`Client/impl.md:12`（C2 批次范围为"适配层与视图模型、分页状态处理、PDF 下载处理"，即 C4-C10）；`Client/design.md:18`（分页控件口径）、`:34`（范围切换清空旧数据与未提交筛选）；`阶段4-Tickets/README.md` 约定 5 | 在票 13 的勾选项中补 C7（分页控件配置：页容量可选值、显示总数文案、加载中禁用、不内联分页对象）与 C9（范围切换重置）两条；C10 可保留在票 14 的组件层并在票 13 的口径中注明其归属，或同样在票 13 补 Unit 层勾选项。 | 否（补齐两条勾选项即可；不补时 C7/C9/C10 的 Unit 证据与 C9 的行为存在漏做风险） |
| DS5-02 | low | 票 03「口径」段（第 21 行）、票 05「口径」段（第 28 行）、票 08「口径」段（第 26 行）、票 17「口径」段（第 18 行） | V83 的分面划分仍不完整：票 03 声明"V83 的点位路由面"、票 08 声明"V83 的两个下载动作形态面"、票 17 声明"V83 的端点形态与清单核对面"，但 **V83 的第一分面"两个上传动作只接受 multipart 且仅两个部件"没有任何票声明或设置勾选项**（票 05 交付两个上传动作，其口径不含 V83、勾选项也没有部件数断言）；同时票 03 声明的"点位路由面"在它的交付物（Contracts 的读模型与分页契约）中无法取得证据，路由来自票 05/08 的控制器动作。 | `Server/design.md:377`（V83 预期："两个上传动作只接受 multipart 且仅两个部件；两个下载动作返回二进制；路由不与既有自动端点冲突"）、`:64`（部件名固定为 `reportJson` 与 `pdfFile`）、`:36`（控制器类型与四个动作）；`阶段4-Tickets/README.md` 约定 5 | 在票 05 的「口径」与勾选项中补"两个上传动作只接受 multipart 且仅两个部件（reportJson/pdfFile）"；把票 03 的"点位路由面"改为可取证表述（例如"契约方法名推导出的自动端点路径不与被路由掩盖"）或删除该声明。 | 否 |
| DS5-03 | low | 票 05 勾选项 25（第 25 行）与「口径」段（第 28 行） | 票 05 勾选项 25 含"仓储调用点显式传作用域与语句标识"，但该面未出现在票 05 的口径声明中（口径只列 V1–V20、V19b、V87、V89 的静态与层级证据，以及 V84 的 Request 侧契约面）；该面实际由票 04（V81 静态核对面）与票 16（V81 调用点完整面）承接，README 约定 12 亦规定实现约束由票 16 集中核对、不分散写入各实现票。 | `Server/design.md:375`（V81 预期）、`:194`（机械检查点："仓储调用点必须显式传作用域与语句标识（沿用既有源码守卫）"）；`阶段4-Tickets/README.md` 约定 12；票 04 与票 16 的口径段 | 删除票 05 勾选项 25 中的"仓储调用点显式传作用域与语句标识"分句（保留"事务声明可静态识别"部分），或把该分句对应的编号（V81）补进票 05 的口径。 | 否 |
| DS5-04 | low | 票 01「外部前置」段（第 9 行）与勾选项 15（第 15 行） | 票 01 的「外部前置」声明"语句调用点的完整核对不属本票，由票 16 在调用点全部落地后承接"，但同一票的勾选项 15 含"**无业务调用点的**无条件全量查询已移除"——该判定依赖票 05/06a/06b/07/08 落地后的调用点，属本票声明移交票 16 的那一类核对，票面自相冲突（按现列前置无法在票 01 内判定）。 | `Server/design.md:264`（"删除这 10 个文件里没有业务调用点的无条件全量查询"）、`:272`（两处断言同步的分工）；`Server/impl.md:11`（S1 范围）；票 16 勾选项 2（同一断言的完整核对面） | 把勾选项 15 改为"本阶段十个表文件覆盖插入、按业务键或主键读取、条件更新与存在性判断"（去掉"无业务调用点"分句），该分句保留在票 16 勾选项 2。 | 否 |
| DS5-05 | low | 票 17「What to build」段（第 3 行）与勾选项 12（第 12 行） | 票 17 的清单枚举为"既有的 26 条业务端点加上…两个作废端点的自动端点、两个列表查询、版本列表查询、版本详情查询，以及手写控制器的两个上传动作与两个下载动作"，**漏列两个提交入口方法的自动端点**。设计明确"两个提交方法与两个作废方法均由框架按接口自动暴露为端点"，因此新增自动端点应为 4 条（2 提交＋2 作废）而非 2 条；按票面的枚举锁定清单会漏掉提交侧的两条。 | `Server/design.md:191`（"两个提交方法与两个作废方法均由框架按接口自动暴露为端点"）、`:36`（手写控制器另承载上传与下载动作）、`:272`（"自动端点清单断言由当前 26 条改为锁本阶段新增后的完整清单"） | 在票 17 的 What to build 与勾选项 12 的枚举中补"两个提交入口方法的自动端点"，或把枚举改为"本阶段新增的全部自动端点（提交两个、作废两个、查询四个）"。 | 否 |
| DS5-06 | low | 票 17「外部前置」段（第 9 行） | 外部前置写"如需从宿主取端点清单，则按**批次 0 的宿主启动条件**执行"——"批次 0 的宿主启动条件"在本阶段的批次表、README 与设计中没有定义（`impl.md` 批次 0 的范围为静态交付与配置，不含宿主启动条件；环境值登记在 `test-environment.md`），该条件句无法执行。 | `impl.md:9`（批次 0 的范围为静态交付与配置，不含宿主启动条件）；`阶段4-Tickets/README.md` 约定 1（外部前置只登记负责人动作、环境与部署面）；`test-environment.md` 第 1 节（宿主地址、本地后端端口 15014 与登录入口的登记处，环境值引用应指向此处） | 改为可执行表述（例如"如改为从运行中的宿主取端点清单，则按 `test-environment.md` 登记的本地后端端口与测试身份执行"），或删除该条件句。 | 否 |
| DS5-07（重开） | low | 票 01 勾选项 17（第 17 行） | 勾选项 17 仍把负责人动作写进验收项："**负责人分三批确认建表完成**，目标库中十张表的实际列、类型、可空性、注释与索引与最终脚本一致，且未创建或改动其他对象"——前半句是外部前置（该票「外部前置」已登记"负责人确认目标库与 schema 并执行三批建表"），后半句是 V80b 的核对（同时被票 12 勾选项 9 声明）；把两者并入同一条勾选项后，票 01 在负责人建表前无法判定该条。 | `阶段4-Tickets/README.md` 约定 1（外部前置单列）与约定 8（建表由负责人执行）；`Server/design.md:374`（V80b 预期）；票 12 勾选项 9（同一矩阵条目的收口面） | 勾选项 17 只保留"目标库中十张表的实际列、类型、可空性、注释与索引与最终脚本一致，且未创建或改动其他对象（V80b，负责人建表完成后核对）"，"负责人确认建表完成"留在外部前置。 | 否 |
| DS5-08 | low | 票 03「What to build」段（第 3 行）与勾选项（第 11–19 行） | 票 03 的 9 条勾选项未覆盖"版本列表与版本详情读模型的报告责任人员为结构化字段"（S4-D26 与票 00 同步结论的第 1 项）：勾选项 1 覆盖枚举与嵌套占位类型的强类型化、勾选项 3 覆盖文件信息、勾选项 4 覆盖两个版本标识，但没有一条断言责任人员由字符串字段改为结构化字段；票 00 的同步勾选项与票 03 的"以票 00 同步后的上游模型为源"只能保证上游口径，不构成票 03 的验收判据。 | `design.md:69`（S4-D26：报告医生名称、审核医生名称，检验版本另带检验人名称与明细检测人，检查版本另带检查医生名称，不做字符串拼接）；`Server/design.md:237`（读模型字段清单）；票 00 勾选项 11（上游同步面） | 在票 03 的勾选项中补"版本列表与版本详情读模型的报告责任人员为结构化字段（报告医生、审核医生；检验版本另带检验人与明细检测人，检查版本另带检查医生）"。 | 否 |

## 三、票切分与依赖图评估（独立判断）

**整体粒度：合适。** 16 张票与设计批次一一对应（批次 0↔00/01/02/03/04/17；批次 1↔05；批次 2↔06a；批次 3↔06b；批次 4↔13/14/15；批次 5↔12；07 与 16 为跨批次的并发/补偿与约束核对票），最大票为 05（16 条勾选项）与 14（15 条），最小票为 17（6 条），未见明显装不下一个执行上下文的票，也未见"两票除交付物外无差别"的过碎切分。每票都能独立演示或验证：00 交付同步后的上游文档、01 交付 DDL 与 SqlMap 静态面、02 交付存储端口与两处配置、03 交付读模型与分页契约、04 交付 SqlMap 探测断言、05 交付基础链路、06a/06b 交付两类完整内容、07 交付并发与补偿、08 交付查询与下载、13 交付生成客户端与适配层、14/15 交付两个页面、16/17 交付两类静态核对、12 收口验收。

**需要合并的票：** 无。
**需要拆分的票：** 无（上一轮建议拆分的票 06 已按批次 2/3 拆为 06a/06b，且两票互不为前置、建表前置分别落在各自票上，本轮复核成立）。
**票号与编号顺序（取向，不计为问题）：** 12 保留在 13–17 之前（README 说明为保持历史编号稳定），票号顺序不等于依赖顺序，但每票的 Blocked by 已完整表达依赖，依赖图无环。

**依赖边（逐条给出本轮判断）：**
- **缺失门禁（应按票补齐的边）**：无新的缺失；上一轮的 01→(05/06/07)、04→(05/08)、09→(05/06) 三处错挂「外部前置」的边已按建议处理（01 的语句核对移交 16、04 的端点断言拆给 17、13 的契约稳定前置移入 Blocked by）。
- **不成立的门禁（可并行却写成串行）**：未发现。06a/06b 互不为前置；08 依赖 05 有明确理由（同一控制器类型）；13 依赖 05/06a/06b/08 与设计"批次 4 前置＝批次 1-3 后端契约稳定"一致。
- **需删除的边**：无。
- **需新增的边**：无（若采纳 DS5-04 的修法，票 01 不需要新增边，只是把勾选项分句移给票 16；若采纳 DS5-01 的修法，票 13 仍只需 00；若采纳 DS5-02 的修法，票 05 仍不需要新增边）。
- **外部前置的构成**：各票外部前置均为负责人动作或环境项（建表、存储目录、宿主菜单与角色授权、部署侧上限、外部组织服务、生成命令）——符合 README 约定 1；仅票 17 的"批次 0 的宿主启动条件"引用无处定义（DS5-06）。
- **设计能力是否有票承接**：阶段根纳入清单（四个接入能力→05/06a/06b；十张表→01；两个入口与页面→14/15＋08；API Client 累计生成→13）全部有承接；43 条台账决策中 42 条在票面可查（16 承接 S4-D1/D2/D24，02 承接 S4-D7 的宿主上限面，05 承接 S4-D33 的载体引用），仅 S4-D26（结构化责任人员）在实现侧票 03 缺验收勾选项（DS5-08）；16 个批次段全部有承接票。
- **票面是否声明设计之外的能力**：未发现（票面未自定业务语义；06a/06b 的口径分别对齐批次 2/3 的矩阵段落）。

## 四、设计选择分歧与切分方式分歧（不构成缺陷）

| 编号 | 位置 | 分歧点 | 我的看法 | 需要负责人决策的点 |
|---|---|---|---|---|
| DS5-F1 | README 约定 12；票 16 全票 | 把 S4-D1/D2/D24 三条实现约束集中到一张静态核对票（16），并规定"不分散写入各实现票的勾选项" | 让抽象约束有明确归属与证据形态（静态断言/源码守卫）比分散到各票更可核查；代价是实现者在各自票面上看不到这些约束，只能通过 README 约定与设计了解。属取向 | 是否需要在各实现票头部加一行"实现约束见 README 约定 12 与票 16"的指引（提高可见性，不改变归属） |
| DS5-F2 | 票 08「Blocked by」段 | 因两个上传动作与两个下载动作落在同一控制器类型，08 必须排在 05 之后（串行） | 按设计现状（`Server/design.md:36` 的一个 `ReportPdfFileController`）该顺序必要且理由已写明；若希望两票并行，只能回设计把上传与下载拆成两个控制器类型（属设计变更，票不得自定） | 是否接受"08 在 05 之后"的顺序（我建议接受）；若希望并行，需先更新设计 |
| DS5-F3 | 票号 12 位于 13–17 之前；06a/06b 使用字母后缀 | 票号不严格按依赖顺序递增 | README 已解释（保持历史编号稳定、字母后缀表示同顺序位并列票），且每票的 Blocked by 已完整表达依赖；不影响可执行性。属取向 | 无需决策（若希望票号即顺序，可在阶段收尾后统一重排，但会使既有引用失效） |
| DS5-F4 | 票 13 的 C1–C10 与票 14 的 C11–C22 在"分页控件"上存在行为与层级的交叉（C7 Unit 与 C16 Component 都涉及页容量与第 1 页） | 分页控件配置的取证层级归属 | 矩阵已为两条用例分别标注层级（C7 Unit、C16 Component），按层级归票最清晰（见问题清单 DS5-01）；页面侧只保留组件层判据即可。属取向 | 无需决策（如采纳 DS5-01，C7 留在票 13、C16 留在票 14） |

## 五、无法确认项

| 编号 | 位置 | 需要什么证据才能确认 | 建议的取证方式 |
|---|---|---|---|
| DS5-U1 | 16 票的 `Status: ready-for-agent` 行 | 哪些票的前置实际已满足、哪些应处于阻断态。README 约定 10 声明状态以阶段根 `testReport.md` 为准，而该报告当前为"未执行" | 实施启动时在 `testReport.md` 登记首轮票状态与阻断原因后逐票核对 |
| DS5-U2 | 票 00 的五项同步勾选项、`阶段4-规格.md` 的 UML2 行状态（"待完成"） | UML2 同步完成后的实际内容（当前 `docs/uml/2-MedicalRecognitionConfigurationUiQuery.wsd` 未修改：仍含 `FileId`、`PdfDownloadUrl`，查询入参已是两入参） | 票 00 完成后逐条核对 UML2 的读模型字段与查询入参，并把差异记入 `testReport.md` |
| DS5-U3 | 票 13 的生成结果（公开入口、累计范围、`kiota-lock.json`、手写入口恢复） | 需要 05/06a/06b/08 交付后端契约后实际生成一次 OpenAPI 与客户端；本轮无生成与运行证据 | 票 13 执行时按 `frontend-api-client` §1 的累计规则逐项核对，并把清单与差异记入阶段报告 |
| DS5-U4 | 票 12 的宿主面可构造性（两个页面从宿主菜单进入、跨院区下载拒绝 V91/C31、宿主失败态 C30） | 需要负责人完成宿主菜单与角色授权配置，且两个测试身份在其院区范围内确有可交叉验证的数据 | 负责人配置后由票 12 实测登记；不可构造部分按 S4-D39 与 `testReport.md` 的降级登记记 `Blocked` |

## 六、覆盖度自评

### 1. 已覆盖与未覆盖的审查维度

- 已覆盖：维度 1（投影一致性，抽样 16 处逐条打开设计核对）、维度 2（覆盖完整性：阶段根纳入/排除清单、43 条台账决策、矩阵 93＋31 条）、维度 3（切分与可独立验证性，含本轮新增的四项指定核对：断言三票拆分、06a/06b 并列、05 与 08 顺序、上游票排序）、维度 4（依赖图正确性，含环检测与孤立票检查）、维度 5（验收标准与证据形态，162 条勾选项逐条过）、维度 6（顺序、范围与文件所有权，含层级词表映射）、维度 7（测试与证据纪律）、维度 8（可执行性与残余风险）。
- 覆盖方式：按审查方法第 1 步建立设计侧能力清单，再逐票比对（第 2 步）、票间横比（第 3 步）、外部一致性核对（第 4 步）。
- 未覆盖或只做范围限定核对的部分及原因：
  - 票面主张的运行结果无法核实：10 张表未建、后端未启动、未生成客户端，凡涉及"运行通过"的判据本轮只核对证据形态（第五节）。
  - 未审查 `阶段4-审查相关/` 内任何文件；未审查其他阶段文档内容本身（仅用阶段 3 的票格式与约定作对照）。
  - 未对设计文档做全文复核（设计侧已在前几轮审查，且本轮设计未修改），本轮只核对票面引用是否指向真实存在且支持该票结论的内容。
  - 未评估各票的实际工作量（人时），无法从票面确认单票是否在一天内可完成；只按交付面与勾选项数量判断粒度。

### 2. 机械核对的实际执行方式与命中结果（逐项对应 8 项）

1. **票号与文件对应** — 方式：脚本列出目录内 16 张票文件，与 README 票一览逐行比对编号与标题；再逐票解析票面 `Blocked by` 行与 README 单元格，用票号集合比较。命中：编号连续（00–08、12–17，06a/06b 并列）、标题一致、Blocked by 集合 16/16 一致；无旧编号残留。**未命中问题。**
2. **依赖边有效性** — 方式：把 README 声明的边建成图，做传递闭包检测环，并检查每票 Blocked by 引用是否存在、是否有孤立票；另检索各票「外部前置」是否夹带票间依赖。命中：**无环（DAG）**、引用票号全部存在、无孤立票；外部前置仅票 04 的说明句涉及其 Blocked by 内的两票（与依赖一致，非新增边）。**未命中问题。**
3. **依赖边与票面能力的匹配** — 方式：逐票判断勾选项所需能力由本票或 Blocked by 中的票交付，并检查「外部前置」是否属负责人/环境项。命中：票 01 勾选项 15 的判定依赖未列入前置的调用点（DS5-04）；票 17 的外部前置引用了无处定义的"批次 0 的宿主启动条件"（DS5-06）。其余各票匹配（含 06a/06b 的批次二/批次三前置、07 的两条链路、12 的 16/17 收口前置）。**命中 2 项。**
4. **矩阵编号一致性** — 方式：脚本展开票面编号与区间，与矩阵全集双向求差；列出多票共同声明项并核对各自口径是否写明层级面。命中：93 条 V 与 31 条 C 全部有票声明、票面编号全部存在、无悬空；多票共同声明 14 组（V18、V25、V26、V35、V45、V60、V79、V80b、V81、V83、V84、V88、V90、V91），其中 V83 的分面划分缺"上传动作形态面"（DS5-02）；票 13 声明的 C1–C10 缺 C7/C9/C10 勾选项（DS5-01）；票 03 声明 V84 但缺结构化责任人员的勾选项（DS5-08）。**命中 3 项。**
5. **验收标准的证据形态** — 方式：脚本检索 162 条勾选项的模糊措辞并逐条阅读，抽查与设计/矩阵的对应。命中：措辞层面未命中（票 07 两处"正常"为具体语义）；判据层面命中 1 项（票 01 勾选项 17 把负责人动作并入验收项，DS5-07）。**命中 1 项。**
6. **票面易腐内容** — 方式：检索代码块、文件清单与逐行实现步骤；逐票阅读 What to build。命中：无代码块、无写死实现细节。**未命中问题。**
7. **票间冲突** — 方式：按交付物（控制器类型、SqlMap 文件、读模型、配置节、断言、矩阵编号）逐个检索被多票声明的对象，检查顺序说明与口径一致性。命中：控制器类型冲突已在票 08 的 Blocked by 中说明（未命中）；SqlMap 语句的"无业务调用点"判定在票 01 与票 16 间存在同一条断言的两处声明（DS5-04）；其余未发现两票对同一口径给出不同描述。**命中 1 项。**
8. **禁止性内容** — 方式：脚本检索 `Passed|Failed|Complete|Ready|验收通过|已完成|批注|负责人裁定|回复原文|确认来源|审查相关|自审`。命中：仅票面引用这些词的纪律条款（12 勾选项 21、README 约定 2/3/9）与 `Status: ready-for-agent`；无测试结果被写入票面、无对话痕迹、无对审查目录的引用。**未命中问题。**

### 3. 设计能力清单的建立方式与比对结果

- **建立方式**：①从阶段根决策台账提取 43 条决策（S4-D1–S4-D43），逐条判断是否至少落在一票的 What to build／勾选项／口径中；②从跨端批次表与两端实施批次表提取 16 个批次段（批次 0–5 与 S1–S5、C1–C5），逐段判断承接票；③从后端与前端矩阵提取 124 条条目（V1–V91 含 V19b/V80b 共 93 条；C1–C31 共 31 条），脚本展开票面声明的编号集合后求差集；④另按本轮要求单独核对四项切分指定项（断言三票拆分、06a/06b 并列、05 与 08 顺序、上游票排序）。
- **清单项数**：43（决策）＋16（批次段）＋124（矩阵条目）＋4（切分指定项）＝187 项。
- **票的承接情况**：
  - 矩阵条目：124/124 有票声明、双向无悬空；多票共同声明 14 组，其中 13 组在各票口径中写明了层级面；**3 处验收项缺口**（票 13 的 C7/C9/C10、票 03 的 V84 责任人员面、票 05 未声明 V83 的上传形态面）。
  - 批次段：16/16 有承接票（上一轮的"宿主请求体上限配置"缺口已由票 02 承接）。
  - 决策：43 条中 42 条在票面可查；S4-D26（结构化责任人员）只在票 00 的上游同步面出现，实现侧票 03 缺验收勾选项（DS5-08）。
  - 切分指定项：4/4 成立（断言三票各自只依赖真正需要的前置；06a/06b 互不为前置且建表前置分别落在各自票；05/08 顺序已在票 08 的 Blocked by 中说明；上游同步票与静态交付票均排在消费者之前）。
- **无票承接项汇总**：无整项无票；存在 3 处"分面/分条未承接"（见上）与 1 处同一条断言两处声明（DS5-04）。

### 4. 本轮严重度判定说明

- 无 `blocker`、无 `high`：本轮未发现会直接导致实施失败、产出错误设计或造成返工/契约不一致的条目；上一轮的高/中严重度条目（依赖图环、检验侧归属错位、控制器顺序、宿主上限无票、口径与勾选项错配、约束无票）经本轮复核均已闭合（见第 5 小节）。
- `medium` 1 条（DS5-01）：票 13 的口径声明 C1–C10，但 C7/C9 无对应勾选项、C10 只在组件层出现，其中 C9 的行为（范围切换清空旧数据与未提交筛选）在票 13 与票 14 的勾选项中都未出现——属"证据缺口＋行为漏做风险"，且修正成本为两条勾选项，故定 medium 而非 high。
- `low` 7 条（DS5-02 ~ DS5-08）：分面划分不完整（V83 上传形态）、勾选项超出本票口径（票 05 调用点分句）、同一条断言在两票声明且票面自相冲突（票 01 勾选项 15 与外部前置）、端点清单枚举漏两条（票 17）、外部前置引用无处定义的条件（票 17）、负责人动作并入验收项（票 01 勾选项 17，重开）、实现侧缺 S4-D26 的验收勾选项（票 03）——均为表述、编号、引用与验收项归属类瑕疵，不改变交付内容与判定结果。
- 未判为问题但已记录的分歧 4 条（DS5-F1 ~ DS5-F4），均为切分方式或票面约定取向。

### 5. 整改复核（负责人声明"已整改"，未提供条目化清单）

本轮对上一轮问题涉及的同一批审查点做了独立复核（判断依据全部来自本轮读取到的当前票面与设计）：
1. **依赖图环 → 已闭合**：票 04 只保留 SqlMap 探测断言，端点清单断言拆为票 17（Blocked by 05、08）；票 01 的"语句调用点完整核对"移交票 16；票 13 的"后端契约稳定"移入 Blocked by（00、05、06a、06b、08）。脚本传递闭包复核：无环。
2. **检验侧实现与前置错位 → 已闭合**：票 06 拆为 06a（外部前置批次二 4 张表、口径 V21–V45）与 06b（外部前置批次三 3 张表、口径 V46–V60）；票 05 的 What to build 改为"调用专项内容与明细的写入步骤"，并明确"检验与检查两类的专项内容与全部明细分别由票 06a、票 06b 落地，本票不实现明细校验"。
3. **05 与 08 共用控制器类型无顺序 → 已闭合**：票 08 的 Blocked by 增列 05 并写明顺序理由。
4. **宿主请求体上限配置无票承接 → 已闭合**：票 02 更名并在 What to build 与勾选项 18 覆盖配置点登记、取值一致与部署侧同步留待真实入口取证。
5. **口径与勾选项错配 → 部分闭合**：票 05 移除 V85/V86、补 V84 Request 面；票 07 移除 V25/V26/V35 并注明由票 06a 承接；票 03 改写 V83/V84 声明——但 V83 的上传形态面仍无承接（本轮 DS5-02），票 05 勾选项 25 仍含本票口径未声明的调用点分句（DS5-03），票 03 仍缺 S4-D26 的勾选项（DS5-08）。
6. **实现约束无票承接 → 已闭合**：README 新增约定 12（集中由票 16 以静态断言/源码守卫核对，不分散写入各实现票），票 16 的 9 条勾选项逐条对应 S4-D1/D2/D24 与语句调用点面，票 12 勾选项 23 要求 16/17 结转结论收口。
7. **环境就绪写成验收项 → 部分闭合**：票 14 勾选项 1 与票 15 勾选项 6 已改为"路由与菜单名称已确定并登记；路由接入完成，可进入页面"并把宿主菜单可达性交给票 12——但票 01 勾选项 17 仍含"负责人分三批确认建表完成"（本轮 DS5-07，重开）。
8. **票 02 失败时点判据超设计 → 已闭合**：勾选项 5 改为"配置缺失或非法…时抛出明确业务事实，不静默回落；实际失败时点如实记录，不作为本票的判定条件"。
9. **未取证态处理未逐票写明 → 已闭合**：各依赖新表/存储目录的票在「外部前置」补"上述前置未就绪时，依赖该前置的勾选项在阶段报告记 `Blocked`，其余未执行项记 `NotRun`"。
10. **票 00 要求改设计正文登记状态 → 已闭合**：勾选项 17 改为"该表 UML2 行的状态更新为本票的完成结果（票面不改动设计决策与批次表，见 README 约定 6、11）"，README 新增约定 11 明确边界。
11. **层级词表对照不齐 → 已闭合**：词表补齐 C3 的 `Static`（票 13）与 V90 `Application`／V91 `Application/Host`（票 12），并为票 16 增设"源码契约与静态核对层（无矩阵条目）"。
12. 本轮的 8 条问题均为**复核后的残留或新发现**（1 条标 `重开`：票 01 勾选项 17），未发现上一轮其他条目以原形态存在。

### 6. 本轮审查的边界与已知局限

- **只读审查**：除本结果文件外未创建、修改或删除任何文件；未执行有副作用的命令（仅使用 `Get-Content`／`Select-String`／`git status`／`Test-Path` 与只读脚本）。
- **无运行事实**：10 张表未建、后端未启动、未执行 `restore`/`build`/Kiota 生成，票面运行类判据只核对证据形态（第五节列出 4 项无法确认）。
- **未提供上一轮已整改条目清单**：问题清单中只对上一轮所涉同一批审查点做了复核（第 5 小节），未做全量条目化比对；标 `重开` 的 1 条已说明其当前证据。
- **排除目录**：未把 `阶段4-审查相关/` 内任何文件作为审查依据，未与任何外部审查结论比对。
- **审查对象边界**：主对象是 `阶段4-Tickets/` 下 16 票与 README；设计文档、批次表、矩阵只用于判断票面是否失实或遗漏（设计侧本轮未修改，按前几轮已审版本核对）；`阶段4-现状取证-F03代码基线.md`、`选项配置.md` 未作为审查对象。
- **参照先例**：`阶段3-Tickets/` 只用于格式与约定对照，未把其业务口径当作本阶段依据。
- **主观与客观分界**：需要运行或环境才能确认的判断全部列入第五节；属切分取向与票面约定的取舍全部列入第四节；两者均未写入问题清单。
