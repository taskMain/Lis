# 阶段 4 任务票审查结果（glm）

- 审查时间：2026-09-18（第二轮票审查；票结构经整改后由 13 票重排为 16 票，本轮按新票结构独立审查）
- 审查对象（实际读取全文，行数为本轮实测）：
  - `阶段4-Tickets/README.md`（60 行）
  - 票 00 `00-UML2上游同步.md`（21 行）
  - 票 01 `01-建表脚本与SqlMap物理映射.md`（22 行）
  - 票 02 `02-PDF存储与宿主请求体配置.md`（22 行）
  - 票 03 `03-报告读模型重建与分页契约.md`（22 行）
  - 票 04 `04-既有测试断言同步.md`（19 行）
  - 票 05 `05-报告与版本基础链路.md`（30 行）
  - 票 06a `06a-检验报告完整内容.md`（25 行）
  - 票 06b `06b-检查报告完整内容.md`（23 行）
  - 票 07 `07-重提交并发与文件补偿.md`（22 行）
  - 票 08 `08-管理端报告列表查询与PDF下载.md`（27 行）
  - 票 12 `12-宿主链路验收与阶段报告.md`（27 行）
  - 票 13 `13-累计APIClient生成与适配层.md`（23 行）
  - 票 14 `14-平台管理员页.md`（28 行）
  - 票 15 `15-医院管理员页与可信范围.md`（20 行）
  - 票 16 `16-语句调用点与实现约束符合性核对.md`（22 行）
  - 票 17 `17-自动端点清单断言.md`（19 行）
- 核对依据（本轮实际读取，先读设计后读票）：阶段根 `design.md`（106 行，台账 S4-D1–S4-D43 共 43 条）、`Server/design.md`（385 行，验证矩阵 93 行条目 V1–V91 含 V19b/V80b，V91 在 371 行实读）、`Client/testPlan.md`（47 行，C1–C31）、阶段根 `impl.md`（48 行批次表）、`Server/impl.md`（32 行全文）、`Client/impl.md`（30 行全文）、`testReport.md`（36 行全文）、`阶段4-规格.md` 上游同步点表；阶段 3 `阶段3-Tickets/README.md` 先例；上一轮票面问题按本轮票面实际状态自然复核（负责人未提供整改清单，见覆盖度自评边界说明）。
- 未读取到的材料：`E:\MedSync\Dy.LisCenter`（参考实现，票面未依赖其内部形态）；`testing.md`、`test-environment.md`、`repository-safety.md`、`project-context.md` 未逐字读取（票面纪律以 README 约定与 `testing-baseline.md` 核对为准）；运行事实（测试执行、生成命令、部署面）因只读限制无法取证。

## 一、已核对且未发现问题

1. **票号与文件对应（机械核对 1）** → 16 张票（00、01、02、03、04、05、06a、06b、07、08、12、13、14、15、16、17）与 README 票一览 16 行一一对应；06a/06b 并列票的字母后缀约定、12 保留原编号为收口票的说明在 README 中写明；无重号缺号。
2. **上轮 T-01（依赖相向边）整改核实** → 票 04 现在只保留 SqlMap 探测断言一面（自动端点清单断言明确「不属本票、由票 17 承接」），其 `Blocked by` 只剩 01、03，不再有对 05 的部分依赖；票 01 的「语句调用点完整核对」也明确移交票 16，其 `Blocked by` 回到 None。脚本环检测：全量边无环、含部分依赖注记的边也无环。上轮的 01↔05、04↔05 两组相向边已消除。
3. **上轮 T-02（票 10/11 宿主菜单勾选）整改核实** → 票 14 勾选项改为「路由字符串与菜单名称已确定并登记；路由接入完成，可进入页面且不提供本地直达入口」，票 15 同句并注明「宿主菜单可达性由票 12 验收」；两票 What 段均声明「从宿主菜单进入该页的验收由票 12 承接，本票交付到……为止」——勾选项回到组件层可判定范围。
4. **上轮 T-03（票 00 越界回写设计）整改核实** → 票 00 勾选项改为「该表 UML2 行的状态更新为本票的完成结果（票面不改动设计决策与批次表，见 README 约定 6、11）」；README 新增约定 11 明确上游同步票的边界（可更新规格同步点表状态列，不得改设计决策内容、批次表与矩阵编号）——范围收窄且有了约定依据。
5. **上轮 T-04（票 07 口径段 V25/V26/V35 与勾选项不符）整改核实** → 票 07 口径段已改为「V45、V60、V79、V85、V86，以及 V18 的失败零残留面；明细唯一性三条（V25、V26、V35）由票 06a 承接」——归属改到票 06a，其勾选项「明细唯一性三条各自成立……」有对应内容。
6. **上轮 T-05（README 层级对照表 12 行粒度）整改核实** → 对照表票 12 行已改为逐条列举（`Static/DB`（V80b）、`Integration`（V88 与集成类条目）、`Application`（V90）、`Application/Host`（V91）），与矩阵层级列实值一致。
7. **票与设计的投影一致性** → 票 01 的前缀索引（列、运算符类、命名以设计索引定义小节为准）、三冗余列位置、报告版本唯一索引、阶段 5 四文件不改语句——与 `Server/design.md` 数据库章及索引定义小节逐条一致；票 05 的管理器五步顺序（定位或创建 → 患者解析核对 → 版本追加与三列更新 → 专项内容写入步骤 → 事件登记）与台账「四个医院接入能力」段的顺序一致，且「调用专项内容与明细的写入步骤」的表述正确投影了明细下沉到 06a/06b 的拆分；票 02 新增的宿主请求体上限配置点登记（与批次 0 范围「宿主请求体上限配置」对应）；票 08 的下载两入参、分层名称回填、窗口校验先于第一次仓储访问——均与设计一致，未发现曲解或自定业务语义。
8. **06a/06b 并列拆分（审查要求维度 3 专项）** → 两票 `Blocked by` 均只有 05，互不依赖、无隐含顺序；外部前置分别落在各自票上（06a 批次二 4 张表、06b 批次三 3 张表），与批次表批次 2/3 的建表前置一一对应；06b 不再包含检验侧回归项（原票 06 的回归勾选项已随拆分移入 06a 的正常勾选项），两票口径段分别声明 V21–V45 与 V46–V60 且互相注明对方承接范围。
9. **04/17 断言拆分（审查要求维度 3 专项）** → 票 04 只做 SqlMap 探测断言、依赖 01+03（其真正前置）；票 17 只做自动端点清单断言、依赖 05+08（端点交付后）；两票无文件交叉，各自只依赖其真正需要的前置；Server/impl 交接行「既有测试断言的两处同步在 S1 与 S5 内完成」与该拆分对应。
10. **票 16 定位（审查要求维度 3 专项 + 机械核对 4 特别说明）** → 票 16 承接调用点完整核对与实现约束符合性（S4-D1/D2/D24 的静态面），`Blocked by` 05、06a、06b、07、08（调用点全部落地后）正确；其口径段不引用矩阵编号，按本轮审查要求属预期，不作未承接命中；README 对照表为它单列「源码契约与静态核对层（无矩阵条目）」一行，层级声明自洽；「不以人工确认为证据」的证据形态声明与 `testing-baseline` 的静态证据口径一致。
11. **票 08 与票 05 的顺序声明（审查要求维度 3 专项）** → 票 08 `Blocked by` 行内写明理由「两个下载动作与两个上传动作落在同一个控制器类型上，下载动作在票 05 落地该类型后追加」——同一控制器类型的先后关系已声明，符合本轮审查要求新增的检查点。
12. **票 12 的收口前置写法** → 票 12 面以「同时以票 16、17 的静态核对结果作为收口前置——这两票的失败项不由本票吸收，需先在其自身范围内改正」声明，并有专门勾选项「票 16 与票 17 的结转结论已收口」；README 一览单元格以「并以 16、17 的结转结论为收口前置」同义表述。语义两处一致（本轮脚本按编号解析报告的差异属文字形态差异，非实质不一致，见覆盖度自评）；16/17 是票间依赖，写在 `Blocked by` 内符合约定 1（外部前置才不并入依赖图）。
13. **矩阵编号承接（机械核对 4）** → 脚本展开各票口径段区段：V1–V91 每个主编号均被至少一票承接（V80/V81→票 01+04+16 调用点面、V82→票 02、V83→票 03/08/17、V84→票 03+05、V88 配置登记面→票 02、V88 真实入口→票 12、V89→票 05、V90/V91→票 08+12）；C1–C31 全部由票 13/14/15/12 承接；无悬空编号。多票共同声明的编号（V18、V45、V60、V79、V83、V84、V85、V86、V88、V90、V91）在各票口径段均写明了自己承接的层级面，符合 README 约定 5 的新增要求。
14. **testReport 对照** → 票 12 勾选项「后端 93 条与前端 31 条」与 testReport 声明及矩阵实际行数一致；待取证表的批次划分（批次 0/一/二/三/5/前端）与票的承接范围可互相映射；三项已接受风险与一项框架观察面在票 07（S4-D38 孤儿文件）与票 12（三项+一项）按登记口径处理。
15. **禁止性内容与 Status（机械核对 8）** → 17 个文件命中的 `Passed`/`Complete`/`Ready` 均为票 12 的否定式纪律勾选项与 README 约定 2/3 的规则声明；全部票 `Status` 均为 `ready-for-agent`（领取标签）；无对话痕迹；README 约定 9 的审查相关目录为排除声明；票面无测试结果状态；每票收尾统一有「`git diff --check` 干净；证据记入阶段测试报告」。
16. **易腐内容（机械核对 6）** → 17 个文件均无代码块、无逐行实现步骤；实现细节一律以「以设计的某小节为准」「以服务端设计的提交请求字段章节为正式载体（本票的勾选项只承载接口级口径，不复制字段表）」方式引用。
17. **验收数据与证据纪律** → 票 05/06a/06b/07/12 的外部前置与勾选项均写明未就绪时记 `Blocked`、其余记 `NotRun`、不以空结果或接口直测替代；票 07/12 写明验收数据经正常提交接口建立、不伪造重复主键或孤儿关联、防御分支前置标注目的；票 12 勾选项明确宿主验收不以本地直达、构建成功、接口直测替代。

## 二、问题清单

| 编号 | 严重度 | 位置（票号 + 段落名） | 问题描述 | 对照依据（设计条款/矩阵编号/规范条款/批次表） | 建议处理 | 是否阻塞实施 |
|---|---|---|---|---|---|---|
| T-01 | low | 票 05「口径」段 | V83 的「两个上传动作形态面」承接悬空：票 03 口径段写「两个上传与两个下载动作的形态面分别由票 05、票 08 承接」，把上传动作的 multipart 形态面（V83 预期的「两个上传动作只接受 multipart 且仅两个部件」）转给了票 05，但票 05 口径段只列「V1–V20、V19b、V87、V89……以及 V84 的 Request 侧契约面」，未列 V83；票 08 口径段只认领了「V83 的两个下载动作形态面」，票 17 认领的是端点清单与路由核对面、勾选项无 multipart 部件形态内容。结果：V83 的上传动作形态面在三票之间无明确归属（票 03 指给票 05，票 05 未接） | `Server/design.md` V83 预期原文（两个上传动作只接受 multipart 且仅两个部件）；票 03 口径段原文；票 05 口径段原文；票 08 口径段原文；README 约定 5（多票共同声明须写明各自承接的层级面） | 在票 05 口径段的括号内补「以及 V83 的两个上传动作形态面」，或由票 17 口径段明确把上传动作的 multipart 形态面并入其核对范围 | 否（取证归属歧义，一处文字补齐即可） |

## 三、票切分与依赖图评估（独立判断）

**整体粒度：合适。** 16 票的切分粒度比上一轮更均匀：票 05 收窄为「报告与版本基础链路」后成为单一执行上下文（基础链路 + 请求信封 + 作废语义），检验与检查明细拆为 06a/06b 并列票后各自对应一个批次建表前置；新增的 16（调用点与实现约束静态核对）与 17（自动端点清单断言）把上一轮悬空的「断言最终核对」职责显式落票。未发现必须合并才能验证的票；未发现除交付物外无差别的过碎票（06a/06b 交付物分别是检验与检查两套专项内容，非重复）。

**需要合并或拆分的票：无。**

**依赖边结论（可执行）**：
- 全量依赖边：00→03→{04,05}；01→{04,05,12}；02→05；04→05；05→{06a,06b,07,08,13,16,17,12}；06a→07；06b→07；08→{13,16,17,12}；13→{14,15}；14→15；15→12；16→12；17→12。脚本检测：无环、无孤立票（00/01/02 为入度零起点，12 为唯一收口）。
- 漏标的门禁：未发现。票 13 依赖 06a/06b（生成需检验与检查端点契约稳定）正确补上了上一轮「09 依赖 05/06」写法的缺口；票 16 依赖 07（补偿路径调用点）合理。
- 不成立的门禁：未发现。上一轮 T-01 的相向边已通过拆出票 16/17 消除；票 05→04 现在是单向边（04 只做 SqlMap 断言、无对 05 的反向核对依赖）。
- 可并行却写成串行的边：票 02 与票 01/03/04 并行可行且依赖图为 None，未被错误串行；06a/06b 并列且互不依赖。
- 收口前置：票 12 对 16/17 的「结转结论收口前置」是票间依赖的正确写法，且明确了「失败项不由本票吸收」的处置方向。

**覆盖完整性结论**：设计能力清单（台账 43 条决策、纳入清单 4 大项、矩阵 V1–V91 共 93 行、C1–C31 共 31 行）逐项比对：四大能力全部有票（05/06a/06b、01、14/15、13）；43 条决策全部可映射到票范围（S4-D11/D38/D39/D40 落票 07/12 的已接受风险登记项；S4-D1/D2/D24 的约束面落票 16）；矩阵 V/C 无悬空编号（V83 上传形态面的归属歧义见 T-01，属文字缺漏而非无票承接——票 03 与票 08 都在处理 V83 相邻面）；排除清单未被任何票反向纳入。README 新增约定 12（实现约束集中由票 16 核对、不分散写入各实现票）与票 16 的定位互相支撑，消除了约束类要求在各票间悬空的可能。

## 四、设计选择分歧与切分方式分歧（不构成缺陷）

| 编号 | 位置 | 分歧点 | 你的看法 | 需要负责人决策的点 |
|---|---|---|---|---|
| F-01 | 票 12 编号保留在 13–17 之后 | 收口票编号 12 保留在末位之前「以保持历史编号稳定」，新增票按依赖顺序编到 17 | 保持编号稳定避免外部引用失效，代价是票号顺序与执行顺序不一致（README 已加说明文字）；属可接受的取舍 | 无需决策 |
| F-02 | 票 02 吸收宿主请求体上限配置点 | 上一轮该配置点散在批次 0 描述中，本轮集中到票 02 并把 V88 拆为「配置与登记面（票 02）+ 真实入口取证（票 12）」 | 同一配置节的能力合并一票、按证据层级拆分取证归属，符合「一票一个执行上下文」；V88 的两处声明各自写明了层级面，符合约定 5 | 无需决策 |
| F-03 | 票 16 不引用矩阵编号 | 调用点核对与实现约束符合性以静态断言为证据、不占矩阵编号 | 与审查要求对本票的特别说明一致；这些要求在设计中有决策编号（S4-D1/D2/D24）与 V81 的部分面，票 16 口径段对 V81 的引用（「V81 的调用点完整面」）与票 01/04 的 V81 静态面形成分层，各写明了承接面 | 无需决策 |

## 五、无法确认项

| 编号 | 位置 | 需要什么证据才能确认 | 建议的取证方式 |
|---|---|---|---|
| N-01 | 票 16 勾选项「未重跑后端代码生成器；再生成保护清单中的文件未被生成器覆盖」 | 「未重跑」的静态证据形态（负面事实的取证方式）在实施期如何固定（如保护清单文件哈希比对或生成器未运行的日志） | 票 16 执行时确定并登记具体取证手段；若无可指到的检查输出，按其自身口径不以人工确认为证据 |
| N-02 | 票 17 外部前置「如需从宿主取端点清单，则按批次 0 的宿主启动条件执行」 | 宿主启动取清单与「不启动宿主」两种取证路径的实际可行性 | 票 17 执行时按实际路径登记 |
| N-03 | 票 12 外部前置「宿主请求体上限与部署侧同步已登记」 | 部署侧反向代理上限现状（V88 取证前提） | 票 12 执行时按 V88 前置登记核对 |

## 六、覆盖度自评

**已覆盖的审查维度**：8 个维度全部覆盖。维度 1（投影一致性）逐票打开被引设计条款核对（票 01 对索引定义小节、票 05 对台账能力段步骤序列与提交请求正式载体声明、票 08 对 V64/V91 预期、票 12 对 testReport 降级登记）；维度 2（覆盖完整性）以台账 43 条 + 纳入清单 4 大项 + 矩阵 93+31 行为基准逐项比对；维度 3（切分与可独立验证性）对审查要求点名的四个专项（04/17 拆分、06a/06b 并行、05→08 同控制器顺序、16 的定位）逐一给出结论；维度 4（依赖图）以脚本做环检测与孤立票检测并给出逐边结论；维度 5（验收标准与证据形态）逐条扫描勾选项，模糊措辞零命中，未发现环境就绪写成验收项、正常业务不可达前置未标注、框架行为强制自测项；维度 6（顺序、范围与文件所有权）核对到票不越界改设计（票 00 受约定 11 约束）、SqlMap 文件的三段分工（01 骨架 → 05/06a/06b 调用点 → 16 核对）已声明顺序；维度 7（测试与证据纪律）核对到 `Blocked`/`NotRun` 口径、验收数据纪律、宿主验收替代禁令均在相关票写明；维度 8（可执行性与残余风险）核对到每票 What 段讲清交付后的可用能力、四项风险/观察面按登记口径处理。

**未覆盖或范围限定的核对及原因**：`E:\MedSync\Dy.LisCenter` 未读取；部分规范文件未逐字读取（票面纪律已由 README 约定与 `testing-baseline.md` 覆盖）；运行事实因只读限制无法取证（列入无法确认项）。

**机械核对八项的实际执行方式与命中结果**：
1. **票号与文件对应**：脚本列出 16 票与 README 16 行逐一对照 → 一一对应、无重号缺号，未命中。
2. **依赖边有效性**：脚本提取全部 `Blocked by` 引用并做全量边与含部分边的两次环检测 → 引用票号全部存在、无环；票 12 的 README 单元格与票面的 16/17 表述为同义文字形态差异（单元格写「并以 16、17 的结转结论为收口前置」、票面写「同时以票 16、17 的静态核对结果作为收口前置」），语义一致，未命中实质问题；部分依赖范围（票 12 的 16/17）与其收口勾选项实际依赖相符，未命中。
3. **依赖边与票面能力匹配**：逐票核对验收能力来源 → 票 12 经 14/15 传递覆盖 13、经 05 传递覆盖 02，闭包完整；票 17 依赖 05+08 与其端点清单范围一致；票 16 依赖五张实现票与其调用点核对范围一致，未命中。
4. **矩阵编号一致性**：脚本展开各票口径段 V/C 区段与矩阵定义（93 行 V、31 行 C）比对 → V1–V91、C1–C31 无悬空编号；**命中 1 条**：V83 的上传动作形态面在票 03 的转指与票 05 的口径段之间断链（T-01，low）。票 16 不引用矩阵编号按本轮审查要求属预期，未作为命中。
5. **验收标准的证据形态**：脚本扫描勾选项模糊措辞（正常/合理/完善/尽量/适当，排除「正常业务/接口/提交/回读」合法用法）→ 零命中。
6. **票面易腐内容**：脚本扫描代码块与实现细节 → 17 个文件均无代码骨架，未命中。
7. **票间冲突**：横比同一交付物（SqlMap 十文件：01 骨架 → 05/06a/06b 调用点 → 16 核对，顺序由依赖边与票面文字双重声明；测试断言文件：04 与 17 各自独占；手写入口文件：13 独占并自含恢复勾选项；路由文件：14 先行、15 复用）→ 无未声明顺序的冲突，未命中。
8. **禁止性内容**：脚本扫描禁用词与 Status 标签 → 命中均为否定式纪律声明；全部 `Status` 为 `ready-for-agent`；无测试结果写进票面，未命中。

**设计能力清单的建立方式与比对结果**：以台账 43 条、纳入清单 4 大项、批次表 6 批、矩阵 V1–V91（93 行）与 C1–C31（31 行）为基准；比对结果——四大能力全部有票、43 条决策全部落票、矩阵无悬空编号、排除清单零反向纳入；唯一的承接缺口是 V83 上传形态面的归属文字断链（T-01）。

**严重度判定说明**：本轮仅 1 条 low（T-01）：V83 上传形态面的三个承接者（03/05/08）都在处理相邻面、实现不会漏做（票 05 的勾选项「提交接口成功返回布尔真」隐含上传动作行为），缺的是口径段的归属文字，故定 low；无 blocker/high/medium。

**本轮审查的边界与已知局限**：静态只读审查，未执行任何票；依赖图评估按 README 约定 1 的「部分依赖算 `Blocked by` 合法形态」语义进行；上一轮 T-01～T-05 的整改未获负责人清单，本轮以上一轮结果文件在清空前记载的票号与问题描述为对照做了逐条复核（T-01～T-05 均已确认整改，未发现「重开」项）——该对照基于上一轮实际记载，非负责人提供的清单。
