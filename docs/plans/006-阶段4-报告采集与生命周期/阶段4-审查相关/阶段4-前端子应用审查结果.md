# 阶段 4 前端子应用审查结果（Dy.WebMicro 子应用专项）

- 审查时间：2026-09-18
- 审查技能：`dywm-sub-app-code-review`（按名加载，并读取其 `references/placement-rules.md`）
- 审查对象：`client/apps/dy-medical-recognition/` 下阶段 4 的新增与改动内容，即 `src/pages/reportManagement/` 的九个文件与 `src/router/routes.tsx` 的两条路由
- 审查基准：`HEAD` 为阶段 3 的提交 `10b4313`；阶段 4 改动均未提交，差异范围为 `git diff HEAD` 与 `git status --porcelain` 的未跟踪项
- 核对依据：`AGENTS.md`、`.agents/instructions/agent-conduct.md`、子应用 `AGENTS.md`、`.agents/instructions/frontend-api-client.md`、`.agents/instructions/frontend-testing.md`、`Client/design.md`、`Client/impl.md`、`Client/testPlan.md`，以及既有先例 `pages/recognitionAmounts/`、`pages/recognitionProjects/`、`pages/standardCatalog/`
- 未读取到的材料：宿主菜单配置不在仓库内，未据此下结论；未运行 pnpm 测试、lint、typecheck、build 与宿主验收，行为结论以静态阅读为限

## 一、已核对且未发现问题

1. **文件位置**：九个新增文件全部位于 `src/pages/reportManagement/`，页面注册在 `src/router/routes.tsx:48` 与 `:51`，未绕开路由文件。页面目录为扁平结构，页面样式与用例相邻，与 `recognitionAmounts/`、`recognitionProjects/`、`standardCatalog/` 三个既有目录的组织方式一致。
2. **依赖边界与 MicroSDK**：页面经 `contexts/ApiClientContext.tsx:19` 取得客户端，适配层调用生成客户端（`reportsApi.ts:292`、`:299`、`:397`、`:407`、`:419`、`:441`），未出现 `fetch`、`axios` 或自建适配器；页面未调用 `MicroSDK`；`main.tsx:17-20` 保留 `setupSubAppDateLocale(dayjs)` 与 `window.__MICRO_APP_BASE_ROUTE__` 的既有写法。
3. **路由与菜单路径**：两条路径为 `report-management` 与 `branch-report-management`，与设计「两个入口、两条独立路由」一致，未提供本地直达入口。该两项的挂载断言缺失另见问题清单 P-01。
4. **认证与可信范围取值**：平台页三级范围取自 `BaseScopeSelector` 的受控值并随请求提交（`ReportManagement.tsx:34-44`、`:52-60`）；医院页的组织与医院取自 `recognitionProjectsOrganizationScope.ts:36` 与 `reportsTrustedScope.ts:45`，请求体不含这两项（`reportsApi.ts:255-263`）；可信范围判定与既有接缝口径一致。
5. **配置、主题、日期与国际化**：`package.json` 未新增依赖；未硬编码接口地址、basePath 或端口；日期筛选使用原生 `input type='date'`（`ReportManagementBoard.tsx:391-408`），`toRequestDate`（`reportsApi.ts:189-196`）按本地日期构造且不把时区信息写入请求值，符合 `Client/design.md:22`；未引入 dayjs 的额外用法。
6. **公共类型形态**：`PageInfoDto` 的 `items` 与 `page`（`models/index.ts:2625-2634`）与适配层取用一致；`totalCount` 经安全整数与值域校验（`reportsApi.ts:317-319`）；分页选项 `10/20/50/100` 落在上限 200 之内；`Alert` 的 `title` 属性在所用 antd 6.6.3 版本有效（`node_modules/antd/es/alert/Alert.d.ts:48`）。
7. **组件复用**：两个页面都渲染同一套 `ReportManagementBoard` 与 `ReportVersionDetailPanel`（`ReportManagement.tsx:79`、`BranchReportManagement.tsx:109`），取值来源不同。该复用目前只有源码事实，无断言，另见问题清单 P-01 的覆盖缺口。

## 二、问题清单

| 编号 | 严重度 | 位置 | 问题描述 | 对照依据 | 建议处理 | 是否阻塞收口 |
|---|---|---|---|---|---|---|
| P-01 | `high` | 目录 `client/apps/dy-medical-recognition/src/pages/reportManagement/`；`BranchReportManagement.tsx:37` | 医院管理员页没有任何组件层用例。目录内只有 `ReportManagement.test.tsx`；`reportsApi.test.ts:8` 指名的 `BranchReportManagement.test.tsx` 在仓库内不存在（全 `client` 目录搜索该名称只命中源码与路由注册）。因此可信范围阻断、院区默认值回填、范围切换重置、请求只提交院区四项行为均无自动化证据 | `Client/testPlan.md:38-41` 把 C23、C24、C25、C26 列为医院管理员页的 Component 层条目；`Client/impl.md` 批次 C4 即该页；既有先例 `pages/recognitionAmounts/BranchRecognitionAmounts.test.tsx:455` 对同形态页面有用例，并含 `await screen.findByText('可信范围不可用')` | 新增相邻的 `BranchReportManagement.test.tsx`，装配沿用先例的 `vi.mock('@dy/auth')` 与 `vi.mock('@dy/auth-react')` 方式 | 是 |
| P-02 | `medium` | `ReportManagement.tsx:62`、`BranchReportManagement.tsx:69` | 两个页面都对适配层出口的可能为 `null` 的结果做了类型断言。原文分别为 `return queryReportPage(client, built as ReportListQuery)` 与 `buildBranchReportListQuery({ ...filters, branchCode: branchId }, page) as BranchReportListQuery`。适配层声明 `reportsApi.ts:219`、`:229` 的返回类型含 `null`，`:223`、`:231` 明确返回 `null` | `.agents/instructions/frontend-api-client.md` 第 3 节禁止用非空断言伪造请求必填值 | 由页面直接使用上层传入的查询对象，或由适配层提供非空变体 | 否 |
| P-03 | `medium` | `reportsApi.ts:76-87`、`ReportManagementBoard.tsx:48-51` | 适配层为报告类型与报告状态各写了一份本地中文兜底（`reportTypeText`、`reportStatusText`、`REPORT_TYPE_METADATA_FALLBACK`），与 `Client/design.md:32`「页面不新写中文映射表；本地兜底只用于契约字段缺失」之间需要说明边界。同域既有中文来源为 `Domain.Share/Enums/MedicalReportType.cs:19` 的 `[Description("检验报告")]` 与 `MedicalRecognitionEnumDescriptorRegistry.cs:28` | `Client/design.md:32` | 设计已授权本地兜底，本项按来源重复登记，判定为取向分歧而非缺陷；如需消除重复，可由设计明确兜底文案的归属 | 否 |

## 三、无法确认项

| 编号 | 位置 | 需要什么证据才能确认 | 建议的取证方式 |
|---|---|---|---|
| U-01 | `client/packages/api-client-medical-recognition/src/api/v1/reportPdf/examinationReport/` 与 `.../laboratoryReport/` | 这两个前端目录在仓库内无调用点，且找不到把它们列入或排除本阶段累计清单的成文依据 | 查阶段 4 设计与票 13 的累计清单口径，确认这两个端点是否属本阶段应当被前端调用的范围 |
| U-02 | 宿主菜单项路径 | 宿主菜单配置不在仓库内 | 由负责人提供宿主侧菜单配置，或按既有口径继续以宿主实际渲染结果为准 |

## 四、覆盖度自评

- **核对方式**：按技能条款逐项进行静态阅读与检索，未执行运行期验证。技能要求的行为类结论（加载、失败态、交互）以源码与既有先例对照得出，未在本文件中作为通过结论登记。
- **已覆盖的审查项**：文件位置、依赖边界与 MicroSDK、路由与菜单路径、认证与可信范围、配置与主题、日期与国际化、公共类型形态、组件复用，共八项，逐项记录在上文第一节。
- **未覆盖的审查项**：运行期行为（测试、构建、宿主渲染）未验证；宿主菜单配置不在仓库内，无法核对菜单路径一致性。
- **与本轮其他结果文件的关系**：账实不符类条目（`reportsApi.test.ts:8` 的失效引用、`routes.test.ts` 的路径清单缺项）登记在同目录的证据一致性核对结果中；本文件只登记子应用专项的独立发现。
- **编号说明**：P-01 至 P-03 与 U-01 至 U-02 只在本文件内有效，不进入阶段设计文档。
