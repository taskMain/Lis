# 阶段 4 设计审查结果（自审）

- 审查时间：2026-09-17
- 审查对象（实际读取到的文件与行数）：

| 文件 | 行数 | 说明 |
|---|---|---|
| `docs/plans/006-阶段4-报告采集与生命周期/design.md` | 91 | 阶段根设计（含 S4-D1…S4-D32 台账、S4-P1…S4-P3 待决项） |
| `docs/plans/006-阶段4-报告采集与生命周期/impl.md` | 47 | 跨端批次表、阶段内规则、执行记录、待办与交接 |
| `docs/plans/006-阶段4-报告采集与生命周期/Server/design.md` | 256 | 后端设计（类型归属、写用例事务表、DDL/索引、V1-V86 验证矩阵） |
| `docs/plans/006-阶段4-报告采集与生命周期/Server/impl.md` | 31 | 后端批次 S1-S5 |
| `docs/plans/006-阶段4-报告采集与生命周期/Client/design.md` | 67 | 前端设计（两个页面、分页、下载、失败态、已接受风险） |
| `docs/plans/006-阶段4-报告采集与生命周期/Client/impl.md` | 29 | 前端批次 C1-C5 |
| `docs/plans/006-阶段4-报告采集与生命周期/Client/testPlan.md` | 45 | 前端测试前矩阵 C1-C30 |
| `docs/plans/006-阶段4-报告采集与生命周期/testReport.md` | 34 | 阶段测试报告（未执行状态） |
| `docs/plans/006-阶段4-报告采集与生命周期/阶段4-讨论内容.md` | 248 | 讨论期决策台账（按审查要求允许引用） |

上述 9 个文件均已产出，无「未产出」项。

- 依据材料（实际读取到的权威依据）：
  - `docs/需求规约SRS.md`：F03.1（:737-946）、F03.2（:947-1092）、F03.3（:1093-1230）、设计约束（:2233-2275）、接口章医院接入接口表与共同边界（:2281-2329）、用户界面通用要求与「报告管理与历史版本」（:2335-2415）、当前版本范围与排除项（:320-396）。
  - `CONTEXT.md`：项目目录/角色与菜单授权（:51-56）、报告采集全段（:78-430，重点 :336-430）。
  - `docs/uml/1-MedicalRecognitionReport.wsd`（936 行全文）、`docs/uml/2-MedicalRecognitionConfigurationUiQuery.wsd`（:1-492）。
  - `docs/plans/001-总体计划/design.md`（279 行全文）、`docs/plans/001-总体计划/impl.md`（:150-221）。
  - 阶段 1/2/3 先例：`003-阶段1-标准项目目录维护/design.md`（S1-D4/S1-D30/S1-D31 等）、`003-…/Server/design.md`（:56-107、:370-389、:448-514）、`004-阶段2-标准项目互认配置/design.md`（S2-D15）、`005-阶段3-互认项目金额维护/design.md`（S3-D1/S3-D7/S3-D11/S3-D12/S3-D19/S3-D20/S3-D21）、`005-…/Server/design.md`。
  - `.agents/instructions/`：`planning-and-evidence.md`、`backend-design-gates.md`、`backend-architecture.md`、`backend-command-query-event.md`、`dy-framework-workunit.md`、`frontend-application.md`、`frontend-api-client.md`、`testing-baseline.md`、`project-context.md`、`test-environment.md`、`AGENTS.md`。
  - `docs/plans/stage-template/Server/design.md`、`docs/plans/README.md` 所在目录结构。
  - 真实代码与生成物：`server/Dy.MedicalRecognition.Domain/MedicalRecognitionReportAggregate/`（20 个实体文件、管理器、仓储接口）、`server/Dy.MedicalRecognition.Application.Contracts/`（19 个 Dto、18 个 Request、ReadModels/）、`server/Dy.MedicalRecognition.Repository/MedicalRecognitionReportAggregate/*.xml`（19 个 SqlMap）、`server/Dy.MedicalRecognition.Repository/Scripts/*.sql`（19 个）、`server/Dy.MedicalRecognition.Tests/Stage1SqlMapProbeTests.cs`、`Stage2EnumContractTests.cs`、`server/Dy.MedicalRecognition/appsettings*.json`、`client/apps/dy-medical-recognition/src/`。
  - 参考实现仅在核对同构机制时对照：`E:\MedSync\Dy.LisCenter\server\Dy.LisCenter\Controllers\ExternalReportFile\ExternalReportFileController.cs`（:84-85 控制器动作上的 `[WorkUnit(UseTransaction = true)]`）。

- 未读取到的材料：无。需说明两点：（1）审查期间（2026-09-17 16:43）阶段目录新出现 `阶段4-规格.md`（195 行），不在派发清单与本审查范围内，本轮未作为审查对象，见「无法确认项 U03」；（2）`阶段4-现状取证-F03代码基线.md` 属讨论期材料、不在派发清单内，本轮只把它当作定位线索，所有事实均回到真实代码复核。

## 一、已核对且未发现问题

### 1. 范围与边界

- **阶段范围与总体计划一致。** 阶段根 `design.md:7-9` 的纳入范围等于 001-总体计划 `design.md:154-164`（§4.6）的 F03（F03.1/F03.2/F03.3 + 「报告管理与历史版本」页面）；前置「阶段 3 完成」与 §4.6「前置：阶段 3 完成」及 §3.4 串行口径一致。
- **10 张表名逐字一致。** 001-总体计划 `design.md:162` 列出的 `mrec_platform_patient`…`mrec_examination_site`（10 张）与 `Server/design.md:136-145` 的 10 行逐名一致，也与 `Server/design.md:132`「10 张表统一 `mrec_` 前缀」一致。
- **未纳入阶段 5、阶段 6 能力。** 阶段根 `design.md:9` 排除「HIS 侧匹配与引用能力（阶段 5）」「互认统计与导出（阶段 6）」；`Server/design.md:5` 只声明采集与生命周期能力，未见匹配、处理结果、引用或统计的设计内容。
- **未纳入 SRS 排除项。** 阶段根 `design.md:9` 排除的推送/PushCenter、影像文件接收与代理、更正原因与版本变更原因、历史版本差异比较与修改删除恢复重启用、独立版本管理菜单、报告查看角色或权限项、报告使用关系表，分别对应 SRS 业务规则 28（:909）、业务规则 29（:911）、业务规则 30（:913）、设计约束 11（:2257）、设计约束 12（:2259）、排除项 6（:360）、排除项 23（:394）；`Server/design.md:5` 未出现这些能力。
- **批次划分与总体计划 §7 对应。** 阶段根 `impl.md:10-12` 的批次 1-3（报告与版本基础 / 检验专项 / 检查专项）与 001-总体计划 `impl.md:159-169`（§7.1-§7.3）内容一致；阶段根 `impl.md:20`「批次 1 不稳定前不进入批次 2 和批次 3」与 §7.1「此批次不稳定前不进入后续批次」一致；`impl.md:24-28` 的阶段内规则 1-3 与 §7.5 三条对应。

### 2. 上游一致性

- **F03.1 逐条核对通过（抽查并逐条确认，非抽点）。** 提交/作废请求形态（业务规则 1、2）、业务标识组成（业务规则 3）、无版本号与内容比较（业务规则 4、24、25，设计约束 18）、无更正原因（业务规则 5）、作废不生成版本与不删历史（业务规则 6、7）、源端报告修改时间只作追溯（业务规则 8）、公共时间（业务规则 9、10）、就诊类型与住院辅助字段（业务规则 12、13）、患者识别与核对及跨患者迁移禁止（业务规则 14）、联系电话脱敏（业务规则 15）、科室人员必填与成对（业务规则 16、17、18、19）、互认项目编码不阻止接收（业务规则 20、21，前置条件 2，备选流 5）、来源保密标识只按来源保存（业务规则 22）、不采集过程字段（业务规则 23）、PDF 文件名优先与回退（业务规则 26，前置条件 5）、无主动推送（业务规则 28）、只读历史版本（业务规则 29）、不新增报告查看权限（业务规则 30）、HIS 只取当前有效版本（业务规则 31）均在设计中有对应表述：阶段根 `design.md:20-34`、`Server/design.md:58-66`、`Server/design.md:98-114`、`Server/design.md:118-128`。
- **作废语义与 SRS 备选流 3 逐字对齐。** 「作废时间不得早于当前版本平台接收时间、不得晚于本次请求接收时间，两端含边界；不与源端报告修改时间比较」见阶段根 `design.md:24`、`Server/design.md:104`，对应 SRS:837 与 CONTEXT.md:410。
- **F03.2 与 F03.3 的必填/成对/唯一性口径与职责边界一致。** 标本信息与检测完成时间必填、细菌未检出仍提交一条、药敏归属细菌结果、展示序号唯一范围、检查所见与结论为报告级、检查项目不采集展示序号、检查部位归属项目、来源影像状态三值与地址一致性，见阶段根 `design.md:20-32`、`Server/design.md:62`、`Server/design.md:191-230`（V21-V60），分别对应 SRS:973-977、:1015、:1055、:1061-1065、:1119-1123、:1157-1163、:1173-1179、:1197。
- **CONTEXT.md 既有结论均已反映。** 报告业务标识（:352-353）、完整报告提交（:364-366）、报告提交路径（:368-370）、报告接收结果（:376-378）、源端报告修改时间（:380-382）、报告 PDF（:384-387）、报告后续版本（:401-403）、报告版本当前性（:405-407）、报告作废（:409-411）、管理端报告历史版本（:413-415）、报告变更处理边界（:421-423）、跨院患者标识（:336-338）、报告版本患者归属（:340-342）、患者联系电话（:344-346）在设计中的对应表述见阶段根 `design.md:22-32` 与 `Server/design.md:96-128`；未发现与这些结论相反的表述（例外见问题 A04）。
- **UML1 命令与事件清单一致。** `Server/design.md:74-79` 的 Command/事件表与 UML1:359-441 一致：`SubmitCompleteLaboratoryReportCommand`→`CompleteLaboratoryReportSubmittedEvent`、`SubmitCompleteExaminationReportCommand`→`CompleteExaminationReportSubmittedEvent`、`AppendMedicalReportVersionCommand`（提交命令的内部命令）→`MedicalRecognitionReportCreatedEvent`（仅新建报告）+`MedicalReportVersionAppendedAsCurrentEvent`、`VoidLaboratoryReportCommand`/`VoidExaminationReportCommand`→`LaboratoryReportVoidedEvent`/`ExaminationReportVoidedEvent`。阶段根 `design.md:75`「报告提交由编排命令改为普通命令，报告创建与取得并入版本追加步骤，聚合内部子命令与其事件不再独立建模」与 UML1:366-397、:442-455 的现状一致；9 个内部步骤不再独立建模的口径在 UML1 落地，设计未重新为其建命令。
- **台账与讨论台账逐条一致。** 阶段根 `design.md:42-73` 的 S4-D1…S4-D32 与 `阶段4-讨论内容.md:22-53` 的 S4-D1…S4-D32 一一对应：编号连续无缺无重、状态列相同（仅 S4-D11 为 `AcceptedRisk`，其余 `DesignConfirmed`）、决策内容一致（差异仅为表述精简，见「设计选择分歧」说明）；未发现正式文档中出现台账未确认的新增口径。台账引用的上游先例均真实存在：S1-D30（阶段 1 `design.md:244`）、S2-D15（阶段 2 `design.md:43`）、S3-D1/S3-D7/S3-D11/S3-D12/S3-D19/S3-D20/S3-D21（阶段 3 `design.md:33/39/43/44/51/52/53`）。
- **真实代码事实与设计描述吻合。** `Server/design.md:7` 的五项现状声明逐项复核为真：10 个报告实体在 `Domain/MedicalRecognitionReportAggregate/`（`partial`、扁平字段、无导航集合）；19 个对外 DTO 在 `Application.Contracts/MedicalRecognitionReportAggregate/Dtos/`（`MedicalRecognitionReportDto.cs:31,47` 的 `ReportType`/`Status` 为强类型枚举）；14 个报告表 SqlMap 每个恰 2 条语句（Columns + QueryAll）且 `Scope="MedicalRecognitionReport"`（另有 5 个手写文件为实体名作用域，合计 19 个 xml）；10 个建表脚本无 `mrec_` 前缀、无 comment、无索引；4 个读模型把枚举/嵌套退化为 `object`（`MedicalReportListReadModel.cs:24,36`、`MedicalReportVersionListReadModel.cs:44`、`MedicalReportVersionDetailQueryReadModel.cs:8,40,44`、`MedicalReportVersionContentReadModel.cs:8,12,16`）；报告生命周期命令、管理器方法、仓储方法、查询实现均不存在；宿主工程无 `server/Dy.MedicalRecognition/Controllers/` 目录。`Server/design.md:54` 声称复用的 `OrganizationPathResolver`、`TrustedScopeResolver` 在 `Application/Validation/` 下真实存在，且 `OrganizationPathResolver` 确实是「一次组织全量读取 + 按医院/院区去重读取」并返回三层名称的实现，名称回填口径与阶段 3 S3-D7 先例一致。`Server/design.md:153`「当前 26 条」自动端点与 `Stage2EnumContractTests.cs:482-507` 的 26 条路径清单一致。
- **既有测试断言的同步范围正确。** 需同步的两处断言确实存在：`Stage1SqlMapProbeTests.cs:100-115`（聚合名作用域键与实体名作用域键）与 `Stage2EnumContractTests.cs:482-507`（端点全量集合）；`Server/design.md:153` 只声明这两处，未多改。

### 3. 契约与模型

- **四个接口的传输形态与结果语义完整。** 两个提交为 `multipart/form-data`、两个作废为标准 JSON、四个接口成功统一返回 `Boolean=true`、失败走既有框架业务异常（阶段根 `design.md:20,70`、S4-D6；`Server/design.md:58-60`），与 SRS 医院接入接口表（:2293-2299）及共同边界第 6 条（:2327）一致。
- **分页契约与项目公共分页规范一致。** 请求「一基页码与页容量（1–200）」、响应「当页数据 + 页码/页容量/总数」、总数为长整型（S4-D3、`Server/design.md:50,254`），与 `frontend-api-client.md:30`（`page:{pageIndex,pageSize}` 与 `items + page{...totalCount}`、不得暴露 `Pagination`/`SkipCount`/`PageCount`）一致；前端适配层做 `Number.isSafeInteger(totalCount) && totalCount >= 0` 校验（`Client/design.md:40`、C5）对应 `frontend-api-client.md:31-32`；排序稳定性用唯一列兜底（S4-D19、`Server/design.md:120`）符合「查询必须定义稳定唯一排序」要求。
- **读模型字段覆盖页面需要的内容（除 A05 的 UML 同步问题外）。** 患者姓名/证件号码、报告责任人员、来源备注、PDF 文件名、当前版本与已作废标识、枚举与文本均在 `Server/design.md:122` 与 `Client/design.md:28-33` 有对应；「不返回文件键、物理路径或对外下载地址」在 `Server/design.md:48,112` 与 S4-D18 一致。
- **无「写了但无调用方」的类型。** `Server/design.md:26-52` 类型表的每一项都有明确调用方与职责：`MedicalReportSubmitForm`（两个上传动作）、`ReportPdfFileController`、`ReportFileUploadApplicationService`、`IReportPdfFileStore`/`LocalReportPdfFileStore`、`SubmitComplete*Command` 与 `AppendMedicalReportVersionCommand`、`Void*ReportCommand`、`ReportListQueryRequest`/`BranchReportListQueryRequest`、分页公共类型、查询仓储端口与 `MedicalRecognitionReportQuery.xml`。

### 4. 领域规则与不变量

- **报告定位唯一确定。** 组织、医院、院区、报告类型、报告单号五元组定位（阶段根 `design.md:22`、`Server/design.md:98`），与 SRS 业务规则 3（:859）及 `MedicalRecognitionReport.cs:33-35` 的字段注释一致；业务键唯一索引不加状态过滤（`Server/design.md:137`、S4-D19）保证作废报告仍占住业务键，避免同号二次创建。
- **版本追加与当前版本指向自洽。** 「旧版本不写可变状态、历史性由当前版本指向推导」与 CONTEXT.md:405-407、UML1:383 一致；「已作废拒绝追加、不得通过提交或历史版本恢复」与 SRS 备选流 2（:835）、业务规则 6（:865）一致。
- **患者归属规则完整且与 SRS 逐条对应。** 证件类型+证件号码识别、姓名/性别/出生日期核对、首次创建与后续必须解析到原患者、跨患者迁移禁止、身份冲突不覆盖不新建，见 `Server/design.md:102` 与 V9-V12（:179-182），对应 SRS 业务规则 14（:881）、备选流 4（:839）与 CONTEXT.md:336-342。
- **互认项目编码不构成接收前置。** 「编码缺失或不可用不影响接收」在 `Server/design.md:62`、UML1:387、V27（:197）与 V48（:218）三处一致；未发现把编码校验写成接收前置条件的表述。
- **并发兜底口径符合规范。** 唯一索引兜底 + 冲突翻译为业务拒绝、不自动重试（S4-D20、`Server/design.md:94`），对应 `backend-command-query-event.md:77`（先读后写须说明并发兜底）与阶段 3 S3-D12 先例；V85/V86 的前置为「正常接口建立前置后并发提交」（:255-256），未要求构造不可达数据。

### 5. PDF 文件处理

- **存储契约、键、目录穿越防护、失败清理、写库补偿与旧文件保留策略齐备。** S4-D10/S4-D21、`Server/design.md:106-114`；「文件落盘固定在数据库写入之前、位于事务之外」与 `backend-command-query-event.md:90`（文件保存与数据库写入必须定义成功、失败与补偿顺序）一致；「删除文件失败只记日志、`throw;` 保留原异常」与 001-总体计划 `design.md:230`（仅清理/补偿时可捕获，须 `throw;`）一致。
- **未设计框架事务/事件行为的自测与包装。** `Server/design.md:167`、阶段根 `impl.md:28`（阶段内规则 5）与 `dy-framework-workunit.md:39` 的「框架负责事件队列且项目无专用观察点时该验证面记 N/A」口径一致；未出现自建事务 API、`TransactionScope` 或事件队列手工操作。
- **下载失败语义与非泄漏要求明确。** 版本不存在与文件缺失分别抛业务事实、不返回文件键/路径/下载地址（S4-D18、S4-D28、`Server/design.md:112`），与 SRS 页面要求 5（:2408）、CONTEXT.md:385-387 一致。
- **未回退为「拒绝整份报告」的文件名规则在正式设计中成立**（`Server/design.md:114`、S4-D22），与 SRS 前置条件 5（:787）、业务规则 26（:905）一致；矩阵中的相反表述见问题 A06。

### 6. 管理端页面与查询

- **两个入口取值来源与 SRS 页面要求一致，未引入角色门控。** 平台管理员取请求、医院管理员组织与医院取可信上下文并覆盖同名请求字段、院区必须属于可信医院（S4-D8、`Server/design.md:118`、`Client/design.md:16`），对应 SRS 页面要求 1、6（:2400、:2410）与 `CONTEXT.md:51-56`；未出现角色判断或权限二次确认代码路径（阶段根 `design.md:34`、`Client/design.md:5`），与 `frontend-application`/`frontend-api-client` 的宿主授权边界一致。
- **筛选、排序、分页、空结果与失败态完整。** 八个筛选条件、时间范围左闭右开、排序与空值置后、越界页回退、空结果不分页错误（`Server/design.md:120-122`、V61-V70、`Client/design.md:18-22,36`、C11-C17、C24）对应 SRS 页面要求 1（:2400）；`Client/impl.md:22` 先把失败态与分页行为建成失败证据再补成功路径，符合 `testing-baseline.md:7`（RED 先行）。
- **名称回填明确为「一次调用取全量映射后整体赋值」。** `Server/design.md:124`、S4-D27、V69（:239）与阶段 3 S3-D7、`backend-architecture.md:200-210`（5.3 批量读取与 N+1 禁令）一致；全文检索未发现按行或按项逐条请求外部服务的表述。
- **患者信息展示与脱敏前后端一致。** 「姓名与证件号码完整展示、联系电话默认脱敏」在 `Server/design.md:126`、`Client/design.md:26`、C21、V74 四处一致，符合 SRS 通用界面要求 3（:2343）、SRS 业务规则 15（:883）与 `CONTEXT.md:344-346`。
- **前端遵守既有错误边界与失败恢复。** `Client/design.md:30,34,40`、C8、C14、C15、C26、C30 与 001-总体计划 `design.md:231-236`（5.2 第 3-6 条：不解析响应体、不本地提示、保留输入与数据、不误刷新）逐条对应；`Client/impl.md:28` 明确不新增本地错误提示、不新增拦截器。

### 7. 数据与脚本

- **DDL 规则口径与项目规则一致（时间类型除外，见 A10）。** `mrec_` 前缀、列序「主键 → UML 业务列序 → 生命周期状态标志 → 操作时间 → 操作人」、表与列中文注释、只创建本平台表、不复用不改名不迁移既有对象（`Server/design.md:132,147`、阶段根 `design.md:36`），与 `project-context.md:127-135`（第 4 节，依据 S1-D30/S1-D31）逐条一致；现有 5 张手写脚本的形态（如 `organization_hospital_branch_recognition_amount.sql:2-27`，列序 `id…current_amount, oper_time, oper_id` + 表列注释 + 唯一索引 + 索引注释）可作为实施样板。
- **索引覆盖已声明的业务不变量与查询契约（报告时间列除外，见 A03）。** 报告业务键唯一索引、版本（报告标识+版本序号）唯一索引、平台患者证件键唯一索引、版本证件号码与姓名辅助索引、明细表按报告版本标识索引，分别支撑 V85（并发首插）、S4-D22（并发追加）、V86（并发患者）、V64/V65（证件与姓名筛选）、V40/V71（明细与版本查询）。
- **SqlMap 作用域与语句标识口径与已落地约定一致。** `Server/design.md:149` 要求「作用域与调用点统一为实体名、每调用显式传作用域、不调用共享映射器的上下文设置方法、语句标识业务化」，与 `project-context.md:121-135`、阶段 1 S1-D1/S1-D2 及 `Stage1SqlMapProbeTests.cs:100-129` 锁定的实测结论一致；`Server/design.md:151-153` 明确既有断言同步，未放宽为宽松形式。
- **建表脚本重写范围与「只指向本平台表」的要求明确。** `Server/design.md:147`「只创建本平台 `mrec_` 表，不复用、不改名、不迁移或修改任何既有对象」与 `project-context.md:123,135`、001-总体计划 `design.md:68` 一致；V80（:250）与 V81（:251）分别核对库结构与 SqlMap 映射仅访问 `mrec_` 表。
- **建表批次与依赖新表的条目一一对应。** 阶段根 `design.md:46`（S4-D5）、`impl.md:10-12`、`Server/impl.md:12-14`、`testReport.md:18-20` 的批次一 3 张、批次二 4 张、批次三 3 张划分一致，且各批次均写明「未完成建表的依赖条目记 `Blocked`」（阶段根 `impl.md:25`、`Server/impl.md:24`、`Server/design.md:165）。

### 8. 验证矩阵

- **编号连续无重号漏号（脚本核对）。** `Server/design.md` 的矩阵恰为 V1…V86，86 行，无重复、无缺号；`Client/testPlan.md` 恰为 C1…C30，30 行，无重复、无缺号。
- **每条具备可判定预期与前置数据列。** 矩阵七列（编号/用例与边界/层级/预期/真实数据库/真实文件系统/前置数据）与 `planning-and-evidence.md:23`（用例、边界、测试层级、预期结果 + 真实依赖）要求的方向一致（缺列问题见 A22）。
- **不设不可达前置、不测框架行为的总体口径明确。** `Server/design.md:159-167`（测试数据边界）与 `testing-baseline.md:14`、`dy-framework-workunit.md:39` 一致；19 条 × 4 类的非法文件、超限、冲突、越界页等负向条目齐全（V5-V8、V18-V20、V25-V26、V34-V35、V43、V49、V51-V52、V56、V68、V76-V77、V85-V86），不是只有正向用例。
- **真实数据库与真实文件系统依赖逐条标注。** 全表两列均逐行填写「是/否」，PDF 相关条目（V19、V20、V75-V79、V82、V85-V86）标注真实文件系统，与 `testing.md`/阶段 3 的标注方式一致。
- **前端矩阵覆盖两个页面、分页、下载、失败态与宿主验收。** C1-C3 契约与生成、C4-C10 适配层与分页/下载/过期响应、C11-C22 平台管理员页、C23-C26 医院管理员页、C27-C30 路由与宿主，与 `Client/impl.md:11-15` 的批次编号一一对应，也与总体计划 `design.md:263`（宿主验收不可由本地直达/接口直测替代）一致。

### 9. 可执行性与残余风险

- **实施批次可执行、无循环依赖。** 阶段根 `impl.md:9-14` 的批次 0→1→2/3→4→5 依赖为单向链；`Server/impl.md:11-15` 的 S1→S2→S3/S4→S5 与 `Client/impl.md:11-15` 的 C1→C2→C3/C4→C5 同为单向；跨端顺序「后端契约稳定 → API Client 生成 → 前端页面」与 001-总体计划 `design.md:220` 一致。
- **待决项均标注处理时点。** S4-P1/P2/P3（阶段根 `design.md:89-91`）三条都有处理时点，且 P2/P3 明确延后到页面完成/实施前报批；未发现被标为延后却阻塞实施的事项（P1 的表述问题见 A15）。
- **环境边界与降级明确，不会被误读为已完成验证。** 「跨组织真实宿主面不可构造 → 以应用层证据为准 + `AcceptedRisk` + 真实宿主面 `Blocked`」在 `impl.md:45` 与 `testReport.md:28` 双处登记；`Server/design.md:165` 区分 `Blocked` 与 `NotRun`，`testReport.md:10` 同样区分；`test-environment.md:15` 证实环境只有同一组织下的两个院区账号，降级前提成立。
- **残余风险与重审条件已登记。** 历史版本文件全保留登记为 `AcceptedRisk` 并给出重审条件（S4-D11、阶段根 `design.md:52`、阶段根 `impl.md:44`、`Client/design.md:58`、`testReport.md:29`）。
- **未出现无证据的通过类表述。** 全文检索 `Passed`/`Complete`/`Ready`/「验收通过」：正式文档中只出现在 `testReport.md:3`（明确声明「不声明 Passed、Complete、Ready 或验收通过」）与 `Server/design.md:7` 的「不是已验证结论」语境；`Client/design.md:67` 明确「不宣称视觉设计或真实宿主验收完成」。符合 `testing-baseline.md:52-62` 与 `AGENTS.md` 闸门 6。
- **未出现对话痕迹与越界引用。** 正式阶段文档中无「批注」「裁定原文」「确认来源」「回复原文」等表述；`impl.md:42`、`Server/impl.md:29`、`testReport.md:9` 出现的「由负责人决定/确认」均为执行权限与流程声明（与阶段 1-3 既有文档同类表述一致），不承载设计依据。全文未引用 `阶段4-审查相关/` 目录、`阶段4-讨论内容.md` 或 `阶段4-现状取证-F03代码基线.md`，符合讨论台账自身的引用禁令（`阶段4-讨论内容.md:5`）。
- **决策依据客观。** S4-D1…S4-D32 的「依据」列只引用 SRS/UML/`CONTEXT.md`/`.agents/instructions`/总体计划/既有阶段先例/参考实现，未出现以负责人意见为唯一依据的条目。

## 二、问题清单

| 编号 | 严重度 | 位置（文件:行或章节） | 问题描述（含原文摘录） | 对照依据（上游原文或规范条款） | 建议处理 | 是否阻塞实施 |
|---|---|---|---|---|---|---|
| A01 | high | `Server/design.md:62`；阶段根 `design.md` 全文（无字段表） | 提交请求的字段级填报要求只有讨论台账承载，正式设计无载体且交叉引用失效。原文：`Server/design.md:62`「八组字段与各自必填、成对（ID 与名称同时有值或同时为空）、条件应填（源系统存在时必须提供）规则见阶段根 `design.md` 的字段表口径」。阶段根 `design.md`（91 行）无任何字段表（全文检索「填报」「表 A」无命中），字段表实际只存在于 `阶段4-讨论内容.md:114-205`（表 A–H），而该文件声明「正式阶段文档不得引用本文件」（:5）并将随阶段文档完成后删除（:240） | SRS F03.2 前置条件 2/4、业务规则 2/3/4/5/6/8/11/15/17/18/19/20/21（:973-977、:1029-1067）、F03.3 前置条件 3/4、业务规则 4/5/9/11/12/14（:1121-1123、:1177-1197）要求逐字段的必填/应填口径；`AGENTS.md` 第 1 节（阶段范围与设计以阶段 `design.md` 为载体）；审查要求第 165 行（只由台账承载的结论必须显式说明） | 把表 A–H 的字段与填报要求落成正式文档章节（建议 `Server/design.md` 的「提交请求字段」节或阶段根设计附录），并把 `Server/design.md:62` 的引用改为该章节；台账删除前完成 | 是 |
| A02 | high | `Server/design.md:33,87-88,92,167`；阶段根 `design.md:55`（S4-D14） | 事务入口落在手写控制器动作，与本项目已核实的 WorkUnit 入口口径与规范不一致，且未填写「框架机制映射」、未按要求取得例外批准。原文：`Server/design.md:87`「是；`[WorkUnit(UseTransaction = true)]` 声明在控制器动作上」；`:33`「`ReportFileUploadApplicationService` … 禁止职责：实现 IApplicationService（避免再生成 JSON 端点）」 | `dy-framework-workunit.md:7-16`「需要显式事务的公开 AppService 方法使用框架现有声明…AppService 只声明边界；Manager、Repository、Entity 和内部辅助方法不控制事务生命周期」；`backend-command-query-event.md:79`「一个写用例的事务边界定义在公开 Application Service 入口。例外必须说明框架限制和替代边界，并在实现前取得明确批准」；`:92-96`（§4.2 要求填写工作单元声明方式、公开事务入口、事件处理顺序及已验证边界）；`backend-design-gates.md:99`（该表列名为「Application Service 入口」）、`:119`（未完成事务机制映射是停止条件）；阶段 1 `Server/design.md:378`「事务入口｜公开 Application Service 方法」（已按本项目包版本核实）、`:107` 要求事务声明机械检查；UML1:366「由应用服务在单一工作单元内编排」 | 二选一并登记：①把提交用例的事务边界放到公开应用服务入口（内部服务实现 `IApplicationService` 并显式路由），②保留控制器动作入口但补齐框架机制映射、说明框架限制、取得负责人批准，并增加事务声明的机械检查与「控制器入口真实事务生效」的验证条目；同时修订 UML1:366 或设计表述使二者一致 | 是 |
| A03 | blocker | 阶段根 `design.md:36,60`（S4-D19）；`Server/design.md:120,137,237` | 报告列表的索引与排序口径建立在「报告主体表有报告时间列」之上，但该列不存在于实体、UML 与该表脚本；且「报告时间为空的行置后」与 V67 的前置在正常业务下不可构造。原文：`Server/design.md:137` 表 `mrec_medical_recognition_report` 一行「组合索引覆盖组织、医院、院区、报告时间倒序、主键倒序」；`:120`「报告时间为空的行置后」；`:237` V67 前置「含空时间与同时间数据」 | 报告时间在版本实体上：`MedicalReportVersion.cs:143`（`ReportTime`，`DateTime` 非空）；报告实体无该属性：UML1:588-602、`MedicalRecognitionReport.cs:15-63`；真实建表脚本 `Scripts/medical_recognition_report.sql:2-17` 无 `report_time` 列；SRS F03.1 业务规则 9（:871）要求报告时间必填、报告创建与首个版本在同一提交内完成（阶段根 `design.md:20`、UML1:388），故「报告时间为空」的正常业务前置不可达；`testing-baseline.md:14` 禁止把不可达状态作为普通用例前置 | 明确列表排序与筛选所依据的报告时间归属（当前版本）与查询关联方式，并把索引改为可落地形态（例如在版本表建 `(report_id, report_time desc, id desc)`，或按当前版本形态给出等价方案）；若确认保留空值分支，需给出可达来源；同步改写 V67 的前置与预期 | 是 |
| A04 | high | 阶段根 `design.md:34`、`:50`（S4-D9）；`Server/design.md:128` | 查看与下载「定位到即返回、不引入数据范围校验」与上游「其他医院不得查看来源医院报告」的归属范围要求冲突。原文：`Server/design.md:128`「**查看与下载不新增数据范围校验。** 定位到报告版本即可返回」；阶段根 `design.md:50`「报告与版本的查看、下载不新增数据范围校验：定位到报告版本且文件存在即返回」 | SRS F03.1 业务规则 30（:913）「医院管理员默认可以查看本医院及有权院区上传报告的全部历史版本…平台管理员按现有数据权限查看授权范围，其他医院不得查看来源医院的历史版本」；SRS「报告管理与历史版本」要求 6（:2410）同义，且 `CONTEXT.md:415` 的 `_Avoid_` 明列「医院管理员查看其他医院历史报告」；UML2:448「报告列表、历史版本列表、版本详情和PDF查询仅按可信组织、医院、院区业务归属返回数据」；SRS 当前版本范围第 8 项（:342）把「页面数据权限」列为当前版本包含范围；`CONTEXT.md:53`「平台保留的组织、医院和院区校验仅用于确认请求的可信业务归属及记录归属一致性」 | 保留「不新增报告查看权限模型、不做角色门控」的现有结论，但补齐业务归属口径：列表、版本列表、版本详情与下载按可信范围（医院管理员限可信组织/医院/院区）校验后再返回，并在设计与 V 矩阵中给出「跨医院院区定位不可得」的负向条目；若坚持不校验，需写明该口径覆盖 SRS 业务规则 30 与 UML2:448 的理由并登记风险 | 是 |
| A05 | high | 阶段根 `design.md:75`；`Server/design.md:48,122` | 台账声称读模型字段口径已与 UML 配置界面查询模型对齐，但 UML2 的对应读数与设计不一致：UML2 仍保留字符串责任人员、文件标识与对外下载地址，且缺设计要求的枚举文本、回填名称与患者字段。原文：阶段根 `design.md:75`「S4-D25、S4-D26 的读模型字段口径已与 UML 配置界面查询模型口径对齐」；`Server/design.md:122`「版本列表返回版本标识、版本序号、源端报告修改时间、平台接收时间、报告医生名称、审核医生名称…」 | UML2:230、:251 `报告责任人员(ResponsibleStaff Text)`（单文本，未按 S4-D26 结构化）；UML2:425-431 `MedicalReportVersionPdfQueryReadModel` 仍含 `文件标识(FileId String)` 与 `PDF下载入口(PdfDownloadUrl String)`（与 S4-D25「去掉对外下载地址字段」及 `Server/design.md:48`「不含…文件键」相反）；UML2:198-209 的 `MedicalReportListReadModel` 无组织/医院/院区名称、无当前版本序号、无患者姓名与证件号码、无 `XxxText`，而设计要求列表返回这些字段；001-总体计划 `design.md:245`（5.3 第 4 条）要求面向列表与详情的 UI 查询 DTO 同时返回枚举值与 `XxxText`，UML2:31-37/56-57 的阶段 1 读模型已按该契约落地 | 实施批次 0 前同步 `docs/uml/2-MedicalRecognitionConfigurationUiQuery.wsd` 的三处报告读模型（结构化责任人员、去掉文件标识/下载地址、补齐文本字段与列表展示字段），或在正式文档中登记该契约变更的同步时点与责任人；同时修正 `design.md:75` 的「已对齐」表述 | 是 |
| A06 | high | `Server/design.md:189`（V19）对照 `Server/design.md:112`、阶段根 `design.md:63`（S4-D22） | V19 与设计的文件名回退规则直接矛盾：矩阵要求「文件名为空或含控制字符均拒绝」，设计正文要求回退为「报告单号加扩展名」且不因文件名问题拒绝整份报告；矩阵亦无文件名回退的正向条目。原文：V19 预期「非 PDF、类型不符、扩展名不符、文件头签名无效、文件名为空或含控制字符均拒绝」；`Server/design.md:112-114`「请求携带的文件名安全时按其保存为下载名；缺失或不安全时回退为『报告单号加扩展名』，不因文件名问题拒绝整份报告」 | SRS F03.1 前置条件 5（:787）「文件名缺失或无效时由平台使用『报告单号.pdf』作为安全下载名称」；业务规则 26（:905）「文件名缺失或无效时使用安全处理后的『报告单号.pdf』…不参与报告身份、版本或幂等判断」；`CONTEXT.md:385` 同义；S4-D22「不回退为拒绝」 | 把 V19 拆为两类：真正的非法文件（非 PDF/签名无效/扩展名不符）拒绝，文件名缺失或含控制字符改为断言「回退为报告单号加扩展名并成功保存」，并补一条回退名的读回用例 | 是 |
| A07 | high | 阶段根 `impl.md:9`（批次 0 矩阵编号列）、`:16` | 批次表的矩阵编号与本阶段实际矩阵分段不一致：批次 0 写「V61-V75 的静态子面」，但 V61-V79 是管理端查询与下载（`Server/impl.md:15`）、静态项是 V80-V84（`Server/impl.md:11`），且 V76-V86 未出现在任何跨端批次行的编号列中。原文：阶段根 `impl.md:9`「… \| V61-V75 的静态子面 \|」 | `Server/design.md:157`「V61-V86 覆盖管理端查询、读模型契约、静态与真实库真实文件系统面」；`Server/design.md:169-256` 实际矩阵 V1-V86；`Server/impl.md:11-15`（S1=V80-V84、S2=V1-V20/V85/V86、S3=V21-V45、S4=V46-V60、S5=V61-V79）；`testReport.md:21`（批次四=V61-V84）；阶段根 `impl.md:16`「矩阵编号可跨批次引用，按完整用例只统计一次」 | 把批次 0 的编号改为 V80-V84（静态子面），并按 `Server/impl.md` 的分段把 V76-V79 归入批次 4、V85/V86 归入批次 1，或明确写出「其余编号见各端 `impl.md`」的对应关系 | 否 |
| A08 | medium | `Server/design.md:31,60`；阶段根 `design.md:20,70`（S4-D29） | 两个提交接口只固定了「两个部件」，未定义部件名与文本部件的顶层字段名清单；医院侧无法据此实现，验收样例如按实现自选名称写会与最终契约脱节。原文：`Server/design.md:31`「`MedicalReportSubmitForm`（两个）\| 新增 Form \| multipart 绑定：文本部件与文件部件」；`:60`「文本部件承载完整报告 JSON 文档…文件部件承载 PDF 文件流」 | SRS 医院接入接口表只给业务输入（:2293-2297），SRS 接口章第 4 条（:2323）不定义报文对象与字段结构，因此字段结构属本阶段设计职责；参考实现的对应做法是显式命名部件（`E:\MedSync\Dy.LisCenter\server\Dy.LisCenter\Controllers\ExternalReportFile\ExternalUploadLabReportForm.cs:14-16` 用 `[FromForm(Name="pdfFile")]`）；V83（:253）只锁「仅两个部件」 | 在 `Server/design.md` 的提交请求信封节写明部件名（JSON 文本部件名与 PDF 文件部件名）与文本部件的顶层字段名，并让 V83 锁这两个部件名 | 否 |
| A09 | medium | 阶段根 `design.md:48`（S4-D7）；`Server/design.md:60,190`（V20） | 100 MB 上限只有「同步宿主与反向代理上限」的意图，没有配置点、责任时点与验证条目；现有宿主配置中没有任何请求体或多部分上限设置，框架默认上限可能低于 100 MB，导致「读取文件流前拒绝」不可达或失败语义与设计不符。原文：S4-D7「单个 PDF 上限 100 MB…宿主请求体上限与反向代理请求体上限同步到该值」；`:60`「单文件上限 100 MB，超限在读取文件流前拒绝」 | `server/Dy.MedicalRecognition/appsettings.json` 与 `appsettings.Development.json` 中无请求体/表单上限配置（只有 Kestrel 端点、Cors、Jwt、HttpConfig、FileServer）；`.agents/instructions/` 中无该上限的已有约定（全文检索无命中）；V19/V20 的判定依赖该上限实际生效 | 明确宿主请求体上限与 multipart 上限的配置键与归属（应用配置或宿主统一配置）并在 V 矩阵增加一条「框架层上限已同步、超限在框架层或应用层均按设计语义失败」的条目；若宿主上限无法在本阶段调整，则把设计上限与实际可得上限对齐并登记 | 否 |
| A10 | medium | `Server/design.md:132`；对照 `Server/design.md:250`（V80） | DDL 时间列类型口径与实体/UML/既有生成脚本不一致且含义不清：设计写「时间列使用带时区类型」，而版本时限业务列在实体与 UML 中是 `DateTime`、在生成脚本中是 `timestamp without time zone`；项目 DDL 规则只规定列序与注释，未规定时间类型。原文：`Server/design.md:132`「字符串列使用文本类型，不加长度限制；时间列使用带时区类型；逐表唯一索引与查询索引如下」 | `MedicalReportVersion.cs:139,143,147,151`（`ApplicationTime`/`ReportTime`/`SourceModifiedTime`/`ReceivedTime` 均为 `DateTime`）；UML1:637-640 同；`Scripts/medical_report_version.sql:34-37` 为 `timestamp without time zone`；`project-context.md:131-135`（第 4 节）无时间类型条款；阶段 1 S1-D4（`003-…/design.md:218`）只确认 `oper_time timestamptz` 与 `DateTimeOffset` 对齐，未覆盖业务时限列；V80（:250）要求「类型…与设计一致」 | 把该句限定为操作时间列（与 `DateTimeOffset` 对齐），或明确写出业务时限列与实体的类型映射（`DateTime` ↔ `timestamp without time zone`），并让 V80 按该口径核对 | 否 |
| A11 | medium | `Server/design.md:108`；阶段根 `design.md:28`、`:90`（S4-P3） | PDF 存储的关键配置值只在讨论台账：配置键名与文件键格式未进入正式文档，台账删除后无载体。原文：`Server/design.md:108`「实现按配置根目录（完整绝对路径）解析文件键，文件键格式为日期目录加平台生成的文件名」；阶段根 `design.md:28`「存储根目录由配置键提供完整绝对路径」 | 讨论台账 S4-D10（`阶段4-讨论内容.md:31`）明确「配置键 `ReportPdfFiles:RootDirectory`」「文件键为 `<yyyyMMdd>/<guid:N>.pdf`」，而该文件将删除（:5、:240）；阶段根 `design.md:90`（S4-P3）只写「存储根目录的开发期绝对路径取值与配置写入」需报批，未写键名 | 在 `Server/design.md` 的 PDF 存储节写明配置键名与文件键格式（键名属可执行设计，开发期取值仍按 S4-P3 报批） | 否 |
| A12 | medium | `Server/design.md:28-29,36-37` | 写请求 DTO 命名与 UML1 的名称登记不一致：设计给提交文档取名 `LaboratoryReportSubmitRequest`/`ExaminationReportSubmitRequest`，UML1 登记的是 `LaboratoryReportVersionRequest`/`ExaminationReportVersionRequest`；表 A–H 对应值对象在设计中未命名，而 UML1 已登记 8 个名称与两层请求结构。原文：`Server/design.md:28`「`LaboratoryReportSubmitRequest`、`ExaminationReportSubmitRequest` … multipart 文本部件反序列化后的完整报告文档」；`:29`「表 A–H 对应的请求值对象 \| 新增 Request」 | UML1:9「写请求 DTO 名称登记：F03 使用 `LaboratoryReportVersionRequest`、`MedicalRecognitionReportWriteRequest`、`MedicalReportVersionWriteRequest`、`LaboratoryReportContentRequest`、`LaboratoryResultItemRequest`、`BacterialIdentificationResultRequest`、`DrugSusceptibilityResultRequest`、`MedicalReportVoidRequest`、`ExaminationReportVersionRequest`、`ExaminationReportContentRequest`、`ExaminationItemRequest`、`ExaminationSiteRequest`」；UML1:10 固定「报告与版本由同一命令在同一步内完成」的两层结构；S4-D31（阶段根 `design.md:72`）以「与 UML1 一致、不引入改名层」为口径 | 二选一：按 UML1:9-10 的名称与两层结构改写 `Server/design.md:28-29`，或先同步 UML1 再按新名称落设计；并在设计与 V84 的契约条目中写出这些类型名 | 否 |
| A13 | medium | `Server/design.md:7,149,153` | 14 个报告表 SqlMap 的口径把 4 个阶段 5 表文件（`RecognitionMatchRecord`、`RecognitionMatchItem`、`RecognitionProcessingResult`、`RecognitionReference`）一并纳入本阶段的「补齐语句」范围，同时又要求「删除没有业务调用点的语句」；阶段边界与改动范围不清，且既有探测断言锁定的正是其中一个阶段 5 语句键。原文：`Server/design.md:149`「14 个报告表文件需要补齐插入、按业务键或主键读取、条件更新与存在性判断等语句，删除没有业务调用点的无条件全量查询」 | 四个文件属阶段 5 实体（001-总体计划 `design.md:172`），仓库内实际文件为 `Repository/MedicalRecognitionReportAggregate/{RecognitionMatchRecord,RecognitionMatchItem,RecognitionProcessingResult,RecognitionReference}.xml`（各 2 条语句、`Scope="MedicalRecognitionReport"`）；`Stage1SqlMapProbeTests.cs:114-115` 锁 `MedicalRecognitionReport.QueryAllRecognitionReference`/`…RecognitionReferenceColumns`，`:153` 的断言同步会触及该阶段 5 作用域；阶段根 `design.md:9` 明确排除阶段 5 能力 | 在 `Server/design.md:149` 明确本阶段只补 10 个报告表文件的语句、4 个阶段 5 文件只做作用域口径对齐（或不改），并写明 `Stage1SqlMapProbeTests` 中阶段 5 键的处置方式（保留/改锁实体名）与理由 | 否 |
| A14 | medium | 阶段根 `design.md:28`（S4-D10）；`Server/design.md:106-108` | PDF 存储由「文件服务配置」改为本地目录存储，与总体计划的口径不同，但阶段文档未登记该覆盖关系。原文：S4-D10「PDF 采用本地文件目录存储…配置缺失、为空、目录不存在或不可写时直接抛异常」 | 001-总体计划 `impl.md:179`「PDF 文件处理依赖文件服务配置；该配置不可用时只暂停受影响验证」与 `impl.md:219-220`「文件服务可用性 \| 未验证 \| 阶段 4 的 PDF 相关验证依赖该服务」；`test-environment.md:19` 登记「文件服务 → `localhost:9333`」；本项目 `appsettings.Development.json` 仍有 `FileServer` 节 | 在阶段根设计或 S4-D10 的适用范围中登记「本阶段以本地目录存储覆盖总体计划 §7.5.3 的文件服务口径」并向负责人报批，同时提示需要同步 001-总体计划 `impl.md` 的待办与交接项；明确既有 `FileServer` 配置的处置（保留不用/后续阶段处理） | 否 |
| A15 | low | 阶段根 `design.md:89`（S4-P1） | 待决项 S4-P1 的三项已在后端设计正文落实，但阶段根仍列为待决且未标注处理结果，与「文档只表达最终设计状态」的要求不符。原文：S4-P1「列表查询 SQL 的筛选组合、投影与计数语句形态；10 张表建表脚本的 `mrec_` 前缀重写清单；读模型落位 \| 本阶段设计正文内完成，不阻塞实施」 | `Server/design.md:68,120`（查询链与筛选/排序/计数口径）、`:48`（读模型落位 C/Queries）、`:132-147`（10 张 mrec_ 表与索引）、`:149`（SqlMap 语句补齐范围）已给出结论；`planning-and-evidence.md:28`「每个阶段文档只表达所属阶段的最终设计状态…不保留中间过渡」 | 将 S4-P1 标记为已在正式设计落实，或把其剩余未定项（若仍有）拆成明确的待决条目并写明处理时点 | 否 |
| A16 | low | `Server/design.md:169-256`（矩阵）；依据规则见 SRS:879 | 公共字段值域中缺「就诊类型五值不接受未知」的正反用例：V42/V43/V52 分别覆盖结果类型、异常标志与影像状态，唯独就诊类型无条目。原文（设计侧规则，仅在台账表 A 中）：`阶段4-讨论内容.md:130`「就诊类型、就诊流水号 \| 必填（就诊类型五值，不接受未知）」 | SRS F03.1 业务规则 13（:879）「就诊类型使用门诊、急诊、住院、体检和其他五类固定值，不接受未知」；UML1:21-29 的 `VisitType` 五值定义；阶段 1-3 对枚举值域均设校验与用例（如 `IMedicalRecognitionReportQueryAppService.cs:25`「配置状态不是已定义枚举值时抛出」） | 增补一条「就诊类型非法值拒绝」的条目，与 A01 的字段表一起把该规则落到正式文档 | 否 |
| A17 | low | `Server/impl.md:11`；对照 `Server/design.md:250`（V80） | S1 的范围包含 V80，但 V80 需要目标库中 10 张表已建，S1 的前置只有「设计确认」，与分批建表的时序不符。原文：`Server/impl.md:11`「S1 \| 静态交付… \| 设计确认 \| V80-V84」；`Server/design.md:250` V80「10 张表列序、类型、可空性、注释与索引与设计一致… \| 是（真实数据库）\| 最终脚本与目标库」 | 阶段根 `impl.md:10-12`（批次一才建前 3 张表，批次三完成才建齐 10 张）；`testReport.md:21` 把 V61-V84 放在批次四 | 把 V80 的取证时点改为三批建表全部完成后（或拆为「脚本静态核对（S1）」与「目标库结构核对（建表后）」两条），并在 `Server/impl.md`/`testReport.md` 同步 | 否 |
| A18 | low | `Server/design.md:112`；对照 `CONTEXT.md:385` | 下载响应只写了「禁止缓存」，未沿用上游明确要求的下载文件名响应头口径。原文：`Server/design.md:112`「返回文件流与该版本保存的下载文件名，响应头禁止缓存；不返回文件键、物理路径或对外下载地址」 | `CONTEXT.md:385`「平台文件下载接口直接以 `application/pdf` 文件流返回…并通过 `Content-Disposition` 使用该版本保存的下载文件名」；SRS 页面要求 5（:2408）「下载使用该版本所保存文件名的 PDF」 | 在下载设计（`Server/design.md:112`）与 V75 中明确 `Content-Disposition` 文件名口径，与前端「按该版本保存的下载文件名触发保存」（`Client/design.md:30`）对齐 | 否 |
| A19 | low | `Client/design.md:48` | 「复用」表把「分页配置工厂」列为复用项，但本项目 client 中不存在分页设施（既有页面均不分页、无公共分页模块），该项实为新增或取自参考实现。原文：`Client/design.md:48`「复用 \| API Client 上下文；每页一个适配层的既有约定；列表骨架…；分页配置工厂；测试装配与全局 setup」 | 仓库实测：`client/apps/dy-medical-recognition/src` 下只有页面级组件（`pages/{standardCatalog,recognitionProjects,recognitionAmounts,prototype}`）与 `hooks/useEnumMetadata.ts`，无分页工厂/公共分页模块；S4-D3 的依据写的是「参考实现既有分页模块与前端分页工厂」 | 把「分页配置工厂」移到「新增」列（或注明「按参考实现形态新建」），避免实施按「已有可复用件」理解 | 否 |
| A20 | low | `Server/design.md:204`（V34） | V34 的前置「悬空药敏集合」按设计的请求结构不可构造：药敏结果嵌套在每条细菌鉴定结果内，无法在正常业务路径上产生「无法归属的集合」，与设计自订边界冲突。原文：V34「药敏必须关联细菌结果 \| Domain \| 药敏结果无法归属具体细菌结果时拒绝整份报告 \| 前置数据：悬空药敏集合」 | 阶段根 `design.md:20` 与 `阶段4-讨论内容.md:120`「`BacterialResults`（可空，表 D，每条内含 `DrugSusceptibilityResults` 表 E）」；UML1:448「药敏归属到具体细菌鉴定结果」；SRS F03.2 业务规则 18（:1061）；`Server/design.md:163`「代码中的防御性校验保留但不转换为必须造前置的测试条目」；`testing-baseline.md:14` | 改为以契约/代码证据核对「药敏归属字段不可为空/不可悬空」的静态断言，或删除该条目并在覆盖说明中登记该防御面 | 否 |
| A21 | low | `Server/design.md:169`（矩阵表头）；对照 `Server/design.md:167` | 后端矩阵缺「真实 WorkUnit」列（本阶段以「真实文件系统」列替代），与本项目规范与阶段 1-3 先例不一致；V17/V18/V79 等集成条目是否在真实工作单元下取证因此无处标注。原文：`:169`「\| 编号 \| 用例与边界 \| 层级 \| 预期 \| 真实数据库 \| 真实文件系统 \| 前置数据 \|」 | `planning-and-evidence.md:23`「至少包含用例、边界、测试层级、预期结果，以及是否需要真实数据库或真实 WorkUnit」；`docs/plans/stage-template/Server/design.md:15` 与阶段 1/2/3 的矩阵表头均为「真实数据库 \| 真实 WorkUnit \| 前置数据」 | 在矩阵中恢复「真实 WorkUnit」列（本阶段的文件系统依赖可另行保留一列），并对集成类条目给出「需要/不需要」的标注；若确认本阶段不设 WorkUnit 观察面，按 `dy-framework-workunit.md:39` 记 `N/A` 并说明依据 | 否 |
| A22 | low | 阶段根 `design.md:40-74`（台账）；对照 `impl.md:45`、`testReport.md:28` | 跨组织真实宿主面的验证降级未进入阶段根决策台账，与阶段 3 先例不一致，重审条件也未写。原文：`testReport.md:28`「跨组织真实宿主面：平台管理员入口的跨组织数据在本环境不可构造，按既有降级口径以应用层证据为准并登记 `AcceptedRisk`，真实宿主面记 `Blocked`」 | 阶段 3 把同类降级登记为台账行 S3-D20（`AcceptedRisk`，`005-阶段3-…/design.md:52`）；`backend-design-gates.md:48` 要求高风险决策台账承载框架限制与关键假设，`testing-baseline.md:37-45` 的 `AcceptedRisk` 为决策标签 | 在阶段根台账新增一条 `AcceptedRisk`（跨组织宿主面降级）或在 S4-D9 的适用范围中登记该降级与重审条件，保持台账与 `testReport.md` 一致 | 否 |
| A23 | low | `阶段4-现状取证-F03代码基线.md:152`；对照 `Server/design.md:153` | 两处对既有自动端点数量的描述不一致（25 vs 26），实测为 26 条。原文：现状取证「锁定当前 25 条 `/Api/...` 路由」；`Server/design.md:153`「自动端点清单断言由当前 26 条改为锁本阶段新增后的完整清单」 | 实测 `Stage2EnumContractTests.cs:482-507` 的路径集合恰为 26 条（`/Api/EnumMetadata/GetEnumMetadata` 1 条 + `/Api/MedicalRecognitionReport/*` 18 条 + `/Api/MedicalRecognitionReportQuery/*` 7 条）；`Server/design.md` 的 26 与实测一致 | 以 `Server/design.md:153` 的 26 为准修订取证材料（该材料随讨论期材料删除，非阻塞） | 否 |
| A24 | low | `Client/design.md:34` | 失败与恢复口径包含只读页面不存在的「写操作」与「不可写状态」，属模板残留，可能误导实施加入不必要的写门控。原文：`Client/design.md:34`「读取失败时保留已有数据、进入不可写状态并允许重试；写操作失败时保留输入与当前弹窗状态、不误刷新列表、不把失败路径当成功」 | 本阶段两个页面为只读：SRS「报告管理与历史版本」要求 2、7（:2402、:2412）与 `Client/design.md:28`「页面不提供版本差异比较、修改、删除、恢复、重新设为当前版本或重新启用」；页面唯一非读取动作为 PDF 下载（`Client/design.md:30`） | 把该段改为只读页面的实际失败面（列表加载失败、版本列表/详情读取失败、下载失败），或明确保留写门控仅为既有骨架的通用条款、本阶段不触发 | 否 |

## 三、设计选择分歧（不构成缺陷）

| 编号 | 位置 | 分歧点 | 我的看法 | 需要负责人决策的点 |
|---|---|---|---|---|
| B01 | `Server/design.md:62,78`；讨论台账表 C/D/E 的「编码缺失不影响接收」 | 互认资格是否需要在报告接收时落库：设计选择只原样保存编码，资格由后续匹配按当前配置判定 | SRS F03.1 业务规则 21（:895）写「互认项目编码在报告接收时校验并随报告明细保存；后续互认项目配置变化不重写已有报告关系」，字面可读成「接收时形成并保存资格事实」。设计口径与 `CONTEXT.md:63`（可互认时间「查询始终使用互认项目的当前配置」）和 UML1:469（匹配时校验当前配置）相符，因此两种读法都自洽 | 确认「接收时校验」仅指编码取值随明细保存、资格在匹配时按当前配置判定；若是，建议在阶段设计或上游补一句明确说明，避免阶段 5 重新解释 |
| B02 | `Server/design.md:110`、S4-D21 | 写库失败后的文件补偿只做「删除本次新文件、删除失败只记日志」，未登记「孤儿文件」这一残余面与重审条件 | 与 SRS 设计约束 10（:2255）和 `backend-command-query-event.md:90` 一致，属可接受的实现取舍；但删除失败静默留文件是本阶段唯一会产生不可追踪文件残留的路径 | 是否需要把「删除失败可能留下孤儿文件」补登为 `AcceptedRisk` 并给出重审条件（如出现可复现的目录膨胀） |
| B03 | 阶段根 `design.md:46`（S4-D5）、`impl.md:10-12` | 建表批次拆为 3 批（3/4/3），与代码批次一致 | 与 001-总体计划 `impl.md:178`「不在批次一一次性建完全部 10 张表」一致，属既有口径的细化 | 无（记录分歧点：若负责人希望减少一次建表交互，可调整为两批，需要同步 D5 与两处 impl 的批次表） |
| B04 | `Server/design.md:60,189`（PDF 类型/扩展名/签名校验） | 设计增加了 SRS 未要求的 PDF 类型、扩展名与文件头签名校验（并据此拒绝） | 属防御性校验，不违反 SRS；但拒绝语义会自动带入医院接入方的重试行为，需要与「共同成功或共同失败」的失败口径一起被医院侧理解 | 确认这些额外校验的失败文案与医院侧重试责任（是否要求与 SRS 业务事实一致） |

## 四、无法确认项

| 编号 | 位置 | 需要什么证据才能确认 | 建议的取证方式 |
|---|---|---|---|
| U01 | `Server/design.md:87`、S4-D14 | 手写控制器动作上的 `[WorkUnit(UseTransaction = true)]` 在本项目实际包版本下是否真的开启事务并拦截 | 反编译/元数据核对 `Dy.Core.Abstractions.Http.WorkUnitAttribute` 的拦截入口，或（更直接）按阶段 1 V54 的方式在真实数据库与真实入口下做失败注入，观察零提交；结论写入框架机制映射表（见 A02） |
| U02 | `Server/design.md:31,60` | 医院侧采集服务实际约定的部件名与字段命名（无上游依据，SRS 不定义报文结构） | 由负责人/对接方在实施前确认部件名与字段命名，再回填设计与 V83（见 A08） |
| U03 | 阶段目录新增 `阶段4-规格.md`（195 行，2026-09-17 16:43 写入） | 该文件与本轮审查清单（阶段根 `design.md`/`impl.md`、`Server/`、`Client/`、`testReport.md`）的关系：是阶段正式文档之一还是并行产物？其中「Implementation Decisions」与阶段根设计、`Server/design.md` 存在大面积同构表述 | 由负责人明确阶段 4 正式文档集合与权威来源（`planning-and-evidence.md:22` 规定跨端阶段的最小集合），若保留该文件需登记其与 `design.md`/`Server/design.md` 的边界，避免同一口径两处维护；本轮未对该文件提出问题 |
| U04 | `impl.md:45`、`testReport.md:28` | 跨组织宿主面降级是否已由负责人作为本阶段 `AcceptedRisk` 正式接受（正式文档中只有执行口径，无接受记录） | 按 A22 补台账 `AcceptedRisk` 行时一并确认；确认依据写入阶段根设计，不引用对话内容 |
| U05 | `Server/design.md:239`（V69） | 「组织服务调用次数不随行数增长」用什么替身取证：设计只写「计数替身断言」，未说明替身边界（是否允许 Application 层替代外部组织服务） | 参照阶段 3 V19（`005-…/Server/design.md:186`）的既有做法确认替身形态与断言位置，避免实施时误用真实外部服务或越过适配层 |

## 五、覆盖度自评

- **已覆盖的审查维度**：第 1-9 组维度全部逐组检查并给出结论，包括本轮特别指出的核对面：S4-D1…S4-D32 与讨论台账逐条比对（编号/状态/内容，见「一、2」）、`Server/design.md` 的 V1-V86 连续性与阶段根 `impl.md` 批次分段（脚本核对，问题 A07）、`Client/testPlan.md` 的 C1-C30 连续性与两处引用一致性（无误）、跨文件引用与链接有效性（脚本扫描全部 markdown 链接，无失效目标；但发现 A01 的章节级失效引用）、实体/字段清单与真实代码比对（10 实体、19 DTO、14 SqlMap、10 建表脚本、读模型、26 条自动端点、无 Controllers 目录）、与 UML1 的一致性（命令/事件/实体/聚合边界一致；读请求 DTO 命名见 A12）、与 UML2 的一致性（读模型见 A05）、数据范围口径（A04）、未取证通过类表述与对话痕迹（未发现）、DDL 索引口径跨文件比对（阶段根与 `Server/design.md` 一致，`Client/design.md` 不含 DDL 口径故无冲突；报告时间列问题见 A03）。
- **未覆盖的维度**：无整组未覆盖。以下子面做了范围限定：
  - 未审查 `阶段4-审查相关/` 目录内任何文件（按审查要求第 45、56 行排除）；
  - 未审查其他阶段文档内容本身，仅引用其决策编号与矩阵形态作对照；
  - 未审查 `阶段4-现状取证-F03代码基线.md`（讨论期材料，只在核对 A23 时引用其行号）；
  - 未运行构建、测试、数据库查询或生成器（审查为只读工作），因此「框架是否真的拦截控制器动作」「宿主请求体默认上限」等运行事实只能作为无法确认项。
- **本轮审查的边界与已知局限**：
  1. 问题判定基于 2026-09-17 当日工作区状态；`阶段4-规格.md` 于审查期间写入，未纳入范围（U03），后续若它成为正式文档，需重新核对与 A01/A04 等口径的一致性。
  2. 参考实现 `E:\MedSync\Dy.LisCenter` 只用于核对同构机制（控制器上的 WorkUnit 声明、multipart 表单命名），不作为本项目业务依据。
  3. 严重度判定按「是否会导致实施失败或产出错误设计（blocker）/ 是否会造成返工或契约不一致（high）/ 局部不一致或证据缺口（medium）/ 表述编号引用类瑕疵（low）」执行；A03 判为 `blocker` 是因为按现有设计无法创建所声明的索引、V67 前置不可达；A02 判为 `high`（需先补机制映射或取得例外批准，规范列为停止条件），若下一步确认控制器动作确实不被拦截，则该条应升级为 `blocker`。
