# 阶段 4 前端设计

## 页面与交互

本阶段交付两个页面，名称与菜单由本项目提供，菜单子项与授权由负责人在权限系统配置；页面不实现角色判断、不做权限二次确认，能进入页面即视为已获授权。**两个页面的路由字符串在页面实现阶段确定**，设计阶段只固定"两个入口、两条独立路由、两个页面、两个菜单"这一结构。

| 页面 | 菜单名称 | 数据范围 |
|---|---|---|
| 平台管理员页 | 报告管理与历史版本 | 组织、医院、院区取值随请求提交 |
| 医院管理员页 | 本院报告管理与历史版本 | 组织与医院取可信上下文并固定展示，院区可选且须属于可信医院 |

两个页面都从宿主登录页与宿主菜单进入，不提供本地直达入口。

**页面结构。** 每个页面由三部分构成：列表筛选区、报告列表表格、报告详情与只读历史版本区域。列表列的筛选条件为组织、医院、院区、报告时间日期范围、报告类型、报告单号、患者证件号码与患者姓名；患者证件号码与患者姓名都由服务端按规范形式做前缀匹配（输入值去首尾空白、证件号码统一大写后比较），用户输入即查询条件，页面不做患者身份匹配，也不做本地规范化或大小写转换。列表列为报告单号、报告类型、报告时间、来源组织、医院、院区、患者姓名、证件号码、当前版本序号、报告状态与操作入口。

**范围取值。** 平台管理员页的组织、医院、院区三级均可选，取值随请求提交；医院管理员页把可信上下文的组织与医院固定传入选择器（对应级不渲染下拉），只让院区可选，请求不提交组织与医院。可信组织或医院缺失时页面给出阻断提示、不渲染院区选择、不发业务请求，不伪造组织、不降级为空值。医院管理员页的院区必须属于可信医院，服务端会拒绝不属于可信医院的院区，页面不做本地归属判断。

**列表与分页。** 列表使用服务端分页：向服务端提交页码与页容量，取回当页数据与分页信息（页码、页容量、总数）；表格按受控分页配置呈现（分页控件位于表格下方、页容量可选、显示记录总数、改变页容量时回到第 1 页）。筛选条件变化时回到第 1 页并保留当前页容量；页面当前页因为数据变化而越界时，允许按服务端返回的总数回退到最后一页一次。页容量的可选值域与服务端一致，不在页面另定一套。（对应阶段根台账 S4-D3、S4-D4、S4-D19）。

**排序。** 服务端按报告时间倒序加主键倒序返回；页面不自行排序，也不做本地过滤与本地分页。

**时间筛选。** 报告时间使用日期范围选择器；提交时把起止日期作为请求条件，区间边界由服务端按左闭右开处理，页面不拼接时分秒，也不把本地时区偏移写进请求值。

**名称展示。** 来源组织、医院、院区名称取服务端返回值，页面不自行按编码拼接名称，也不调用组织服务补名称。

**患者信息展示。** 列表与详情完整展示患者姓名与证件号码；患者联系电话按服务端返回值展示（服务端已脱敏），页面不做二次脱敏或还原。（对应阶段根台账 S4-D12）；报告类型、状态与版本状态的取值来源见「枚举展示」。

**报告详情与只读历史版本。** 从列表进入详情后，页面展示报告公共信息与该报告的全部版本列表；版本列表按平台形成顺序（版本序号）展示，并明确标识当前有效版本、已被后续版本替代的历史版本与报告已作废状态。每个版本展示版本序号、源端报告修改时间、平台接收时间、报告医生、审核医生、来源报告备注与 PDF 文件名。用户可以选择任一版本查看该版本的完整结构化内容：检验报告展示标本信息、检验时间线、普通结果、细菌鉴定结果与药敏结果；检查报告展示检查所见与结论、临床背景、检查信息、影像状态与调阅地址、检查项目与检查部位。页面不提供版本差异比较、修改、删除、恢复、重新设为当前版本或重新启用，也不展示或要求更正原因与版本变更原因。

**PDF 下载。** 每个版本提供下载入口，点击后从已鉴权的接口客户端取回文件二进制，按该版本保存的下载文件名触发浏览器保存；不拼接对外下载地址、不使用未经鉴权的浏览器直达、不把文件内容转成文本。下载失败不弹本地错误提示，按既有失败态口径处理。（对应阶段根台账 S4-D18、S4-D28、S4-D36）。

**枚举展示。** 报告类型、报告状态等枚举取值的中文取服务端随行返回的文本字段；筛选与表单选项使用集中缓存的枚举元数据。页面不新写中文映射表；本地兜底只用于契约字段缺失，且仅保留页面真正用到的枚举。（对应阶段根台账 S4-D25）。

**加载与失败。** 页面首次进入自动加载，并提供手动刷新。两个页面为只读页面，失败面为列表读取失败、版本列表读取失败、版本详情读取失败与 PDF 下载失败四类；读取失败时保留已有数据、结束加载态并允许重试，页面不进入"可写状态"判断（本阶段无写操作入口）。下载失败保留当前详情与版本选择，不误刷新列表、不把失败路径当成功。列表加载失败后不显示本地接口错误提示；查询失败后可以结束加载态并保留已有数据。范围（组织、医院、院区）切换时清空上一范围的数据与未提交筛选，目标范围读取失败不得进入其可操作状态。（对应阶段根台账 S4-D24）。

**分页与失败的组合口径。** 页码越界或数据收缩导致的空页，按服务端返回的总数回退一次；同一筛选条件下快速连续操作时，只采纳最后一次请求的结果，丢弃过期响应。

## API 与组件

使用现有 API Client 上下文，不在页面创建客户端。查询使用两个列表入口、版本列表与版本详情接口；下载使用按报告标识与版本标识取文件流的接口。适配层负责把生成的请求与读模型转换为稳定视图模型，页面不直接扩散生成的 Kiota 类型；分页响应在适配层收敛为页面分页模型，并对页码、页容量与总数做安全整数与值域检查，非法响应直接按契约错误处理、不伪造分页状态。

**列表筛选状态。** 页面的筛选条件与分页状态由页面自身维护，切换范围时重置到第 1 页；不把患者证件号码等敏感条件写入地址栏。

**复用与新增。**

| 类别 | 内容 |
|---|---|
| 复用 | API Client 上下文；每页一个适配层的既有约定；列表骨架（筛选区、表格、空态、失败态）；枚举元数据缓存钩子与本地兜底模块约定；可信上下文接缝；测试装配与全局 setup |
| 新增 | 报告管理页目录与两个页面组件、筛选区组件、报告表格组件、报告详情与历史版本区域组件、适配层与视图模型、受控分页配置工厂（按参考实现形态新建，本项目此前无分页设施）、分页状态处理、PDF 下载处理 |
| 依赖 | 本阶段不新增前端第三方依赖；文件下载使用浏览器原生能力与已鉴权客户端 |

**API Client 累计生成。** 保留阶段 1、阶段 2、阶段 3 已交付接口，加入本阶段稳定契约（两个作废端点、两个提交端点、两个列表查询、版本列表查询、版本详情查询、按版本取文件流的下载端点），排除后续阶段接口与登录接口。生成前核对累计清单，生成后核对公开入口、请求与读模型字段、可空性、枚举、分页形状与无残留后续端点；生成过程不得提交本机路径、日志或临时文件；生成会覆盖手写入口文件，生成后必须核对并恢复手写内容。

**提交接口在管理端的呈现。** 两个完整报告提交接口面向医院侧采集服务，管理端不为其提供上传页面；本阶段不做上传界面，也不在管理端页面暴露提交入口。

## 已接受风险

- 历史版本 PDF 全部保留、本阶段不做清理或归档，重审条件见阶段根设计。
- 报告列表依赖外部组织服务提供名称；该服务不可用时列表整体失败，不降级为编码或空名称。
- 平台管理员页的跨组织数据无法在本地环境构造，真实宿主面按阶段根设计的降级口径处理。
- 患者证件号码与姓名在列表与详情完整展示，为 MVP 口径；后续阶段如需收紧展示范围，按新阶段设计处理。

## 验证

前端测试前矩阵见 [testPlan.md](testPlan.md)。真实页面必须从宿主菜单进入；本地直达、构建成功与接口直测不替代宿主验收。环境值引用项目环境规范，不在本文复制。

本文件只完成前端行为与契约设计，不宣称视觉设计或真实宿主验收完成。
