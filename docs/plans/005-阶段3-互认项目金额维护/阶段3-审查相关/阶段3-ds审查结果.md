# 阶段 3 审查结果（DS）— Tickets 轮（第三次）

本文件为本轮独立审查结果。已按审查要求 H1 **整体覆盖**写入，不含任何历史轮次结论。

**本轮审查对象**：`docs/plans/005-阶段3-互认项目金额维护/阶段3-Tickets/`（`README.md` + 01～09，共 10 份文件）。

---

## 1. 审查范围与版本

**起始提交**：`dafa6a69363266814e3817fc5eb71900e1484706`（`fix: 修复标准项目列表的启用状态筛选并统一布尔参数绑定`，2026-09-15 18:31:25 +0800）。本轮在未提交工作区之上审查，HEAD 未变。

**工作区标识**：`git -c core.quotepath=false status --short` 显示 `docs/plans/005-阶段3-互认项目金额维护/阶段3-Tickets/` 为**未跟踪**（`??`）。目录内文件修改时间为 2026-09-16 17:13～17:39（其中 01、02、03、04、05、06、07、09、README 为 17:38～17:39；08 为 17:13）。

**实际读取的被审文件**（10 份全文）：

| 文件 | Status | Blocked by | 外部前置行 |
|---|---|---|---|
| `README.md` | — | — | — |
| `01-外部组织服务接入与组织路径解析.md` | ready-for-agent | None | None（包引用与实测构建是本票首个勾选项） |
| `02-金额保存写入与事件.md` | ready-for-agent | 01 | 票 01 包引用与实测构建；真实库面另需负责人建表 |
| `03-金额查询读模型.md` | ready-for-agent | 01 | 同票 02 |
| `04-建表脚本与SqlMap物理映射.md` | ready-for-agent | 02、03（仅 V24 完整核对）；脚本与 DDL 静态面可立即开始 | 负责人确认目标库与 schema 并执行建表；写语句映射核对依赖票 02、03 |
| `05-前端依赖与运行时配置.md` | ready-for-agent | None | 包私服可达；平台 Base 服务可达；环境值按环境规范引用 |
| `06-累计APIClient生成与适配层.md` | ready-for-agent | 02、03 | 包私服与生成工具可用；后端四端点契约稳定 |
| `07-两个页面与路由.md` | ready-for-agent | 05、06 | 票 05、06 交付；菜单与角色授权由负责人配置 |
| `08-后端真实数据库验收.md` | ready-for-agent | 04 | 负责人建表；外部组织服务实测可用；真实入口与目标库可用 |
| `09-宿主链路验收与阶段报告.md` | ready-for-agent | 04、07 | 票 07 交付；菜单与角色授权；真实宿主入口与目标库可用 |

**对照读取的权威依据**：`docs/plans/005-阶段3-互认项目金额维护/design.md`、`Server/design.md`、`Server/impl.md`、`Client/design.md`、`Client/impl.md`、`Client/testPlan.md`、`impl.md`、`testReport.md`、`docs/需求规约SRS.md`（F10 章节/页要求/关键属性）、`CONTEXT.md`、`docs/uml/1-MedicalRecognitionReport.wsd`、`docs/uml/2-MedicalRecognitionConfigurationUiQuery.wsd`、`docs/uml/命令出参整改矩阵.md`、`.agents/instructions/planning-and-evidence.md`、`testing-baseline.md`、`testing.md`、`backend-architecture.md`、`frontend-api-client.md`、`AGENTS.md`；以及作为**先例**（非本阶段对象）读取的 `docs/plans/004-阶段2-标准项目互认配置/阶段2-Tickets/`。

**未审（H2）**：`docs/plans/005-阶段3-互认项目金额维护/阶段3-审查相关/` 全部内容。**未审（第 2 节边界）**：`docs/plans/004-阶段2-标准项目互认配置/` 的内容本身，仅作先例与现状读取。

**时间**：2026-09-16。

---

## 2. 审查依据与取证方式

**执行的只读命令**：`git -c core.quotepath=false status --short`、`stat`（文件时间）、`ls`、`cat`/Read（10 份票面全文）、`grep -rn`（§0.1 禁用词、术语残留、矩阵编号、勾选状态、层级声明、设计锚点）、Python 脚本（展开 `V1-V3`/`C5-C20` 区间并计算覆盖率、逐票解析 `Blocked by`/`Status`/外部前置、与 README 票一览对账、抽取 `Client/testPlan.md` 每条 C 的层级并逐条比对票面层级声称、检查依赖图回边）。

**审查口径**：

1. 任务票是**设计与矩阵的执行分解**，正确性以阶段设计、`Server/design.md` 验证矩阵（V1-V30）、`Client/testPlan.md` 前端矩阵（C1-C26）、各 `impl.md` 批次表为准；票面不自定业务语义。
2. `Status` 是**领取标签**，不是测试状态（`testing-baseline.md §3` 六状态）。
3. 票面验收项必须可追溯到矩阵编号；矩阵编号的层级（Component/Host/真实库）决定票面归属。
4. 票面不得引用审查编号、口头表述、讨论过程（`AGENTS.md §0.1` 第 7–9 条）。
5. 票面不得携带"已完成/通过"等无证据措辞。

**无法取得**：本轮为文档审查，不执行构建、不调用外部服务；票面中"实测通过"类验收项的实际结果不在本轮范围（阶段 `testReport.md` 现行 56 条全部 `NotRun`）。

---

## 3. 逐项发现表

> 严重度：`Blocker`／`High`／`Medium`／`Low`。

| 编号 | 位置 | 类别 | 严重度 | 问题 | 证据 | 影响 | 建议方向 |
|---|---|---|---|---|---|---|---|
| U-01 | `阶段3-Tickets/README.md:25` vs `06-…md:22`、`05-…md:21` | G | **Low** | README 约定第 5 条要求"每票末尾的「口径」段声明本票覆盖的层级"。逐票核对：票 01（"应用层"）、02（"契约、领域写入与应用编排层"）、03（"查询契约、原因派生与应用编排层"）、04（"DDL 静态面与 SqlMap 映射面"）、05（"依赖与运行时配置的组件层"）、06（"客户端契约与适配层的组件层"）、07（"页面与组件的组件层"）、08（"真实数据库与真实生产入口层级"）、09（"带 `Host` 层的用例（C1、C3、C5-C20、C22、C23、C26）"）——**9 票均已给出层级，约定已落实**。仅提示：层级命名不统一（票 01 用"应用层"、票 02/03 用"…与编排层"、票 04 用"静态面/映射面"、票 05/06/07 用"组件层"），与 `Server/design.md` 验证矩阵的"层级"列取值（Domain/Repository、Application、Query、Contract、Static/DB、Framework/Integration）**词表不同**。 | `README.md:25`；9 票口径段；`Server/design.md:165-196`（层级列）、`Client/testPlan.md:9-34`。 | 低：无层级冲突，仅命名体系与矩阵不对齐，读者须自行映射。 | 可统一沿用矩阵的层级词表，或在 README 中给出映射表。 |
| U-02 | `阶段3-Tickets/07-…md:12`、`06-…md:16` | G | **Low** | 票 06 勾选项"交付适配层测试，覆盖生成类型到视图模型的映射、可空字段与枚举转换"、票 07 勾选项"交付组件层测试，覆盖列表字段集合、未配置与零元呈现、金额校验、范围切换清空等组件层行为"——两票新增了"交付测试"这一**成果型**勾选项。阶段 2 先例票 07 的对应勾选项写法为"覆盖 C1-C3、C6、C8、C10-C11、C13-C21、C23；归档组件、typecheck 和必要 Host 证据到阶段 `testReport.md`"，其前一条为"先建立组件/契约失败证据，再实现并精确复测"，**以矩阵编号为验收锚点**；票 06/07 的新增项**未绑定矩阵编号**（C4/C21/C24 等由测试覆盖的条目与"交付测试"之间的关系需读者推断）。 | 票 06 `:16`、票 07 `:12`；先例 `阶段2-Tickets/07-互认项目管理页面与组件验证.md`（Verification and evidence 段）；`README.md:5`（矩阵口径）。 | 低：新增勾选项在"矩阵编号可追溯"口径下达不到票面自校。 | 为"交付测试"项绑定其覆盖的矩阵编号，或改为在对应 C 编号勾选项中说明测试证据形式。 |
| U-03 | `阶段3-Tickets/09-…md:12` vs `:25` | G | **Low** | 票 09 勾选项 `:12` 现写"承接票 07 带 `Host` 层用例的宿主面证据：C1、C3、C5-C10、**C12-C14**、C16、C17、C23；其中 C12、C13、C14 为纯宿主层用例…；**C11、C15、C18-C20、C22、C26 见本票下列各条**"——已把 `:25` 口径段的完整集合拆分列明（**该处已一致**）。仅提示：`:12` 的枚举**未列 C2、C4、C21、C24、C25**（口径段 `:25` 已声明其"无宿主层级，不在本票层级"），两处**均正确**，但读者须联读两行才能得到完整边界。 | 票 09 `:12`、`:25`；`Client/testPlan.md:9-34`（逐条层级）。 | 低：无。 | 无需整改。 |
| U-04 | `阶段3-Tickets/04-…md:5` vs `README.md:12`、`Server/impl.md:12`、`impl.md:11` | C/G | **Low** | 票 04 的 `Blocked by` 现为"02、03（仅 V24 的完整核对）；脚本与 DDL 静态面可立即开始"，README 票一览同步为"02、03（仅 V24 完整核对）"——**票面与 README 两处一致**。逐条核对批次表：`Server/impl.md:12` 批次 4 前置"设计确认；独立脚本可先交付；建表由负责人执行"、`impl.md:11` 批次 3 前置"设计确认；脚本可先交付"，**方向与票 04 的"静态面可立即开始"一致**；依赖图无回边（04→02/03→01，08→04，09→04/07）。**该处已自洽**。仅提示：票面把"部分依赖"写进 `Blocked by`，与 README 第 1 条"`Blocked by` 只表达票与票之间的依赖"的**字面**略有张力（该句未限定"完整依赖"）。 | 票 04 `:5`；`README.md:12`；`Server/impl.md:12`；`impl.md:11`。 | 低：无实质冲突。 | 可在 README 第 1 条补"含部分依赖时须注明范围"。 |
| U-05 | `阶段3-Tickets/06-…md:17`、`07-…md:31` | G/H | **Low** | S3-D14 现拆为两条：票 06 `:17`"生成物未被手工修改（S3-D14）"、票 07 `:31`"本阶段页面不下推枚举筛选（S3-D14）"——**按"生成物"与"页面行为"分置，承接时点与各自票面范围相符**（`design.md:46` 含"页面不下推枚举筛选，不手改生成物"两项）。**该处已自洽**。 | 票 06 `:17`、票 07 `:31`；`design.md:46`。 | 无。 | 无需整改。 |
| U-06 | `阶段3-Tickets/01-…md:13`、`04-…md:3`、`05-…md:3` | A/H | **Low** | 票 01 已给包名 `Dy.Base.Application.Contracts` + 设计锚点（"见 `Server/design.md` 的公共契约与分层段"）；票 04 已给表名 `mrec_organization_hospital_branch_recognition_amount`、索引名 `ux_mrec_org_hos_brh_project`、四业务键列名、脚本路径 `Repository/Scripts/organization_hospital_branch_recognition_amount.sql` + 设计锚点；票 05 已给 `@dy/components-base@0.1.4`、`@dy/api-client-base@0.1.39`、`1.0.0-preview.103` + 设计锚点（"见 `Client/design.md` 的新增运行时配置与依赖段"）。**锚点齐备且指向设计文档**（本轮 `grep` 逐项命中）。 | 票 01 `:3`、票 04 `:3`、票 05 `:3`；`Server/design.md:29,122,137`；`Client/design.md:30`。 | 无。 | 无需整改。 |
| U-07 | `阶段3-Tickets/07-…md:28` | G | **Low** | 票 07 现写"含组件层的 C1、C3、C5-C10、C16、C17、C23 由本票负责组件层证据"——已由"组件层"改为"**含**组件层"，与 `Client/testPlan.md` 中 C1/C3/C5-C10/C16/C17 为 `Component/Host`、C23 为 `Host/Component` 的事实**相符**（逐条比对无冲突项）。**该处已自洽**。 | 票 07 `:28`；`Client/testPlan.md:9-34`。 | 无。 | 无需整改。 |
| U-08 | `阶段3-Tickets/08-…md:14` | G | **Low** | 票 08 现写"事件可见性子面记 `N/A` 并附 `AcceptedRisk`；该 `N/A` 不使整条 `V27` 结案，`V27` 的数据库写入与失败响应面须单独真实取证"——与 `Server/design.md:118,193` 对 V27 的专门约束**一致**。**该处已自洽**。 | 票 08 `:14`；`Server/design.md:118,193`。 | 无。 | 无需整改。 |
| U-09 | `阶段3-Tickets/README.md:29,30` | G | **Low** | README 第 9 条已改为"审查工作目录与另一条并行工作线（阶段 2 目录）不属于任何票的输入、输出或修改范围"（**去掉了具体目录名**）；第 10 条已改为"**状态查询**：当前执行状态、阻断前提与测试状态以阶段根 `testReport.md` 为准；本目录不维护状态快照"（**去掉了状态快照**）。**两处均已自洽**。仅提示：第 9 条仍提及"阶段 2 目录"，属范围声明（先例 README 有同类句）。 | `README.md:29,30`；先例 `阶段2-Tickets/README.md` 末段。 | 无。 | 无需整改。 |
| U-10 | `阶段3-Tickets/04-…md:3` | E | **Low** | 票 04 声称"本票的脚本与静态映射面可立即开始"。核对现行真实实现：金额表脚本 `Scripts/organization_hospital_branch_recognition_amount.sql` 仍为生成态（无 `mrec_` 前缀、`varchar(100)`、`create table if not exists`、无注释、无唯一索引），SqlMap `OrganizationHospitalBranchRecognitionAmount.xml` 仍用旧聚合 scope 且只有无 where 的 `QueryAll`——**与 `Server/design.md:7` 的"当前代码事实"描述一致**，即"可立即开始"的起点就是这两份生成态文件。票面未复述这两份文件的现状路径与问题，实施方须回 `Server/design.md:7` 取。 | 票 04 `:3`；`Server/design.md:7`；实体/脚本/SqlMap 实读。 | 低：票面未给"起点现状"锚点。 | 可在票面注明"起点为 `Server/design.md` 设计基线段列出的两份生成态文件"。 |
| U-11 | `阶段3-Tickets/README.md:5`（票一览） | G | **Low** | README 票一览表列 9 票 + 标题 + `Blocked by`，**未列各票的矩阵编号或票面主责层**。先例 README 亦无该表。属可接受（矩阵编号在各票勾选项内），仅登记。 | `README.md:5-17`；先例 README（无票一览表）。 | 低：无。 | 无需整改。 |

---

## 4. 事实复核结果

本轮被审对象为任务票，无"调研事实"章。按相同口径复核**票面陈述的事实性内容**（凡引用阶段设计/矩阵/上游的，逐条回原文核对）：

| # | 票面陈述 | 复核结论 | 证据 |
|---|---|---|---|
| 1 | 票 01：路径解析"一次批量读取组织、医院、院区，在内存校验三层的存在、启用、父子归属，并返回三级名称" | **与设计一致** | `Server/design.md:63`（`OrganizationPathResolver` 职责）、`:73`（保存链）、`:79`（查询链） |
| 2 | 票 01：三段归属校验拒绝（V8-V10） | **与设计一致** | `Server/design.md:174-176`（V8/V9/V10 分别覆盖组织/医院/院区） |
| 3 | 票 01：包名 `Dy.Base.Application.Contracts` | **与设计一致** | `Server/design.md:29`、`:47`（S3-D15） |
| 4 | 票 01：编码取 `Id`、启用取 `IsValid`，且注明"设计侧为元数据级核对，未逐类型复核" | **与设计一致（且如实标注）** | `Server/design.md:29`（"元数据级核对，**未逐类型绑定复核**"） |
| 5 | 票 02：两入口共用同一 Command 与同一事件 | **与设计一致** | `Server/design.md:75,77,91`；UML1:341（唯一金额命令）、`:861-862`（两角色指向同一命令） |
| 6 | 票 02："写入最多一条语句，不引入工作单元、不手工开事务" | **与设计一致** | `Server/design.md:100`（事务决策） |
| 7 | 票 02：并发首插"唯一索引兜底、冲突翻译为业务拒绝、不自动重试" | **与设计一致** | `Server/design.md:112` |
| 8 | 票 02：V25 事件字段与"校验失败、0 行、大于 1 行、唯一冲突均不登记" | **与设计一致** | `Server/design.md:87,191` |
| 9 | 票 03：返回字段集（12 项）、不含三个编码与最后修改信息 | **与设计一致** | `Server/design.md:25`；UML2:131-143（Python 计数=12） |
| 10 | 票 03：不可用原因"按分类、分组、标准项目取首条；配置自身停用不占用" | **与设计、SRS 一致** | `Server/design.md:143`；SRS:2364-2365 |
| 11 | 票 03：V29"按 `S3-D20` 以应用层证据取证，宿主面记 `Blocked`" | **与设计一致** | `design.md:52`（S3-D20）；`Server/design.md:195` |
| 12 | 票 04：表名 `mrec_organization_hospital_branch_recognition_amount` | **与设计一致** | `Server/design.md:122` |
| 13 | 票 04：唯一索引 `ux_mrec_org_hos_brh_project` 覆盖四业务键列、无状态过滤 | **与设计一致** | `Server/design.md:137` |
| 14 | 票 04：四业务键列名 `organization_code`/`hospital_code`/`branch_code`/`standard_project_code` | **与设计一致** | `Server/design.md:127-130` |
| 15 | 票 04：脚本路径 `Repository/Scripts/organization_hospital_branch_recognition_amount.sql` | **与设计一致** | `Server/design.md:122` |
| 16 | 票 04：脚本"列序、类型、可空性、注释…与设计逐字一致；无 `varchar(n)`、无默认值、无跨系统外键" | **与设计一致** | `Server/design.md:124-137` |
| 17 | 票 04："原无业务调用点的无 where 查询语句已移除" | **与设计一致** | `Server/design.md:139` |
| 18 | 票 05：`@dy/components-base@0.1.4`、`@dy/api-client-base@0.1.39`、kiota `1.0.0-preview.103` | **与设计一致** | `Client/design.md:30`；`design.md:42`（S3-D10） |
| 19 | 票 05：不回落"同一包双实例"的版本组合 | **与设计一致** | `Client/design.md:30`（"不使用会与现有 `@dy/auth@0.1.39` 形成双实例的 `0.1.28` 组合"） |
| 20 | 票 05："生产配置文件本阶段不改，也不登记生产地址"（C25 配置面） | **与设计、矩阵一致** | `Client/design.md:30`；`design.md:45`（S3-D13）；`Client/testPlan.md:33`（C25） |
| 21 | 票 06："净增四个本阶段端点"（两保存 + 两查询） | **与设计一致** | `Server/design.md:13,20`；`Client/testPlan.md:32`（C24） |
| 22 | 票 06：`/auth/login` 保持排除 | **与矩阵、规范一致** | `Client/testPlan.md:32`；`frontend-api-client.md §1.3` |
| 23 | 票 07：可信院区缺失"不自动选中、须显式选择、不发空值参数" | **与设计一致** | `Client/design.md:14`；`testPlan.md:11`（C3） |
| 24 | 票 07：C3 需两份身份上下文（C3 ×2） | **与设计、批次一致** | `Client/impl.md`（C3 ×2）；`testPlan.md:11` |
| 25 | 票 07：C13/C14/C23 受控构造与"不新增生产测试入口" | **与测试矩阵一致** | `Client/testPlan.md` 末段 |
| 26 | 票 08："事件可见性子面记 `N/A` 并附 `AcceptedRisk`" | **与设计一致** | `Server/design.md:118` |
| 27 | 票 08："该 `N/A` 不使整条 `V27` 结案，V27 的数据库写入与失败响应面须单独真实取证" | **与设计一致** | `Server/design.md:118,193` |
| 28 | 票 09：C26 宿主链路、劫持生效、Console 无业务错误 | **与矩阵一致** | `testPlan.md:34`（C26） |
| 29 | 票 09："只按完整用例统计一次" | **与规范、批次表一致** | `Server/impl.md:15`；`impl.md:16` |
| 30 | README："阶段 3 的 56 条业务用例在阶段根 `testReport.md` 中全部为 `NotRun`" | **与现状一致** | `testReport.md:32`（`NotRun` 56）；`:8`（V30+C26=56） |
| 31 | README：票一览 9 票的 `Blocked by` | **与各票票面逐票一致（9/9）** | Python 逐票对账；`README.md:9-17` |
| 32 | README：约定 1-10 对先例九项约定的承接 | **已全部承接并新增"矩阵口径"** | 先例 `阶段2-Tickets/README.md` 对照 |

**覆盖率复核（Python 展开区间计算）**：票面**覆盖 V1-V30 全部 30 条与 C1-C26 全部 26 条**；**无遗漏、无越界**（多出 V/C 均为空）；矩阵定义数（V=30、C=26）与票面覆盖数一致。

**层级声称复核（Python 逐条比对 `Client/testPlan.md`）**：
- 票 07 声称负责组件层的集合（C1、C3、C5-C10、C16、C17、C23）**逐条命中 Component 层**（无冲突项）；其声明的"C12、C13、C14 为纯宿主层"与矩阵 `Host` **一致**。
- 票 09 口径段集合（C1、C3、C5-C20、C22、C23、C26）**逐条命中 Host 层**；"矩阵有 Host 层但票 09 未列"为空集，即**无遗漏**；纯 Component 的 C2、C4、C21、C24、C25 被**正确排除**。

**小结**：32 条票面事实性陈述**全部与阶段设计/矩阵/上游/现状一致**；票面引用的 `S3-D10`、`S3-D13`、`S3-D14`、`S3-D20`、`V8-V10`、`V19`、`V27`、`V29`、`C3`、`C24`、`C25`、`C26` 等编号均能在设计或矩阵中找到对应定义。**未发现票面自行改变业务语义或引入设计未定义的行为。**

---

## 5. 决策落实与依据来源核对（类别 H 独立小节）

### 5.1 票面是否引用口头表述 / 讨论过程 / 审查编号

| 检查项 | 结果 |
|---|---|
| "负责人批注""裁定原文""确认来源""回复原文" | **0 命中** |
| 审查报告编号（`R-xx`/`U-xx`/`F-xx`/`D-xx`/`Q-xx`/`S-xx`） | **0 命中** |
| "独立审查""审查结果""整改来源" | **0 命中** |
| 引用 `阶段3-讨论内容.md` | **0 命中** |
| 引用审查工作目录名称 | `README.md:29` 为"审查工作目录"**泛称**（无具体目录名）；先例 README 有同类句 |
| 引用 `S3-D10`/`S3-D13`/`S3-D14`/`S3-D20` | 票 03 `:17`、票 05 `:15`（S3-D13 语义）、票 06 `:17`、票 07 `:31`、票 08 `:3`、票 09 `:20`——**均为阶段设计台账内的正式编号**（`design.md:42,45,46,52` 有定义），非口头表述，**允许** |
| 是否落入无锚点抽象 | **已消除**：票 01/04/05 均给具体包名/版本/表名/索引名/脚本路径 + 设计文档锚点（第 4 节 3、12-15、18 项） |

**结论：10 份文件（README + 9 票）符合 `AGENTS.md §0.1` 第 7–9 条，无违反项。**

### 5.2 票面是否把决策落成可执行验收

以 `design.md` 台账 20 条决策抽验票面落点：

| 决策 | 票面落点 | 结论 |
|---|---|---|
| S3-D1 两页面两信任边界 | 票 02（V12/V13）、票 07（C1-C4）、票 09（C15） | 已落为可验收项 |
| S3-D2 不维护权限模型 | 票 07（"页面不实现角色判断，也不做权限二次确认"） | 已落 |
| S3-D3 一 Command 两入口 | 票 02（正文 + "两个入口共用同一个领域命令与同一个事件"） | 已落 |
| S3-D4 业务键定位/同值也保存 | 票 02（V1-V3、V7） | 已落 |
| S3-D5 行集由互认配置驱动 | 票 03（正文 + V17） | 已落 |
| S3-D6 列表字段集 | 票 03（V18）、票 07（C5） | 已落 |
| S3-D7 批量读取、N+1 | 票 01（V19）、票 03（V19） | 已落 |
| S3-D8 不分页 | 票 03（正文）、票 09（C22） | 已落 |
| S3-D9 实体语义修正 | 票 02（V28） | 已落 |
| S3-D10 前端依赖与 overrides | 票 05（含具体版本 + 设计锚点） | 已落 |
| S3-D11 可信上下文缺层拒绝 | 票 01（V14）、票 02（V14） | 已落 |
| S3-D12 唯一冲突兜底 | 票 02（V16）、票 08（V16） | 已落 |
| S3-D13 不处理生产配置 | 票 05（C25 配置面） | 已落 |
| S3-D14 页面不下推枚举筛选 + 不手改生成物 | 票 07 `:31`（页面侧）+ 票 06 `:17`（生成物侧） | **已落（且按两侧分置）** |
| S3-D15 新增包引用 | 票 01（首个勾选项，含包名） | 已落 |
| S3-D16 运行时配置 | 票 05（含 `runtimeConfig` 与配置项） | 已落 |
| S3-D17 平台管理员取值口径 | 票 03（V29）、票 07（C2） | 已落 |
| S3-D18 排序与可空金额 | 票 03（V18/V20）、票 07（C16） | 已落 |
| S3-D19 院区范围 | 票 02（V30）、票 09（C11） | 已落 |
| S3-D20 跨组织降级 | 票 03（V29 应用层面）、票 08（口径段）、票 09（`:20`） | 已落 |

**结论：20 条决策全部已在票面落成可验收项。**

### 5.3 与先例约定的对照

先例 `阶段2-Tickets/README.md` 的九项约定在本阶段 README 的落点：

| 先例约定 | 本阶段 README | 结论 |
|---|---|---|
| `Blocked by` 表示完整交付的阻塞关系 | 第 1 条（并明确"外部前置不并入依赖图"） | **已承接且更明确** |
| `Status` 是领取标签而非测试状态 | 第 2 条 | 已承接 |
| `done` 仅在全部勾选有证据时使用 | 第 3 条（未复述 `done` 一词，改为"勾选项只有在有实际执行证据后才可勾选"） | 已承接（语义等价） |
| 勾选须附命令或证据出处 | 第 3 条 | 已承接 |
| 证据归档到阶段根 `testReport.md` | 第 4 条 | 已承接 |
| 测试数据经真实业务入口形成 | 第 7 条 | 已承接 |
| 不把重复主键/孤儿关联作为前置 | 第 7 条 | 已承接 |
| 建表与授权由负责人执行 | 第 8 条 | 已承接 |
| 审查工作目录不属于任何票 | 第 9 条 | 已承接 |
| 矩阵口径（跨批次引用、只统计一次） | 第 5 条 | **本阶段新增且必要** |
| — | 第 10 条（状态查询指向 `testReport.md`，不维护快照） | 本阶段新增 |

---

## 6. 无法判断项

| 编号 | 无法判断的内容 | 缺什么信息 | 由谁提供 |
|---|---|---|---|
| X-01 | 9 票 `Status: ready-for-agent` 是否经负责人批准进入实施 | 负责人对进入实施的指示（阶段 3 仍为设计阶段） | 项目负责人 |
| X-02 | 任务票在本项目中是否是被规范的文档类型 | `.agents/instructions/` 与 `AGENTS.md` 均未定义任务票；本阶段以 README 自建约定 | 项目负责人 / 规范维护方 |
| X-03 | 票 01 包组合 `1.0.0.147` + `Dy.Core.Abstractions 1.1.0.54` 是否可构建 | 实测 `restore/build`（票面已设为勾选项，本轮不执行） | 实施方（票 01） |
| X-04 | `@dy/components-base@0.1.4` 与 `@dy/api-client-base@0.1.39` 具名导出/类型声明是否与设计所用名字兼容 | 安装后 `typecheck`/`build`（票面已设为硬闸门） | 实施方（票 05） |
| X-05 | `183.224.180.166:35001` 是否可真实调用 | 真实调用结果（票 05 已设为勾选项） | 实施方（票 05） |
| X-06 | 目标表是否已由负责人建成 | 负责人建表确认（票 04 已设闸门） | 项目负责人 |

---

## 7. 无问题项

以下范围本轮查过并认为**成立**：

- **矩阵编号覆盖（A/D）**：Python 展开区间计算确认票面覆盖 **V1-V30 全部 30 条**与 **C1-C26 全部 26 条**；"缺 V/C"与"多出 V/C"均为空集；矩阵定义数（V=30、C=26）与覆盖数一致。
- **票面事实性陈述与设计一致（B）**：抽检 **32 条**票面陈述（表名/索引名/列名/脚本路径/字段集/入口数/事件口径/降级口径/受控构造/包名与版本/C25/C26/统计口径/现状），**全部能在 `Server/design.md`、`Client/design.md`、`Client/testPlan.md`、`impl.md`、`Server/impl.md`、`Client/impl.md`、UML1/2、SRS 中找到对应定义**，无票面自定业务语义。
- **依赖图无环且与 README 对账一致（C）**：票间依赖 01←02/03、02/03←04（部分）、02/03←06、05/06←07、04←08、04/07←09；**回边检查为空**，构成无环 DAG；`Blocked by` 引用的票号**全部存在**（无悬空引用）；**逐票与 README 票一览对账 9/9 一致**。
- **票 04 的"部分依赖"与批次表方向一致（C/G）**：票面"脚本与 DDL 静态面可立即开始"与 `Server/impl.md:12`、`impl.md:11` 的"独立脚本可先交付/脚本可先交付"一致；票面"仅 V24 的完整核对依赖 02、03"与 `Server/design.md:190`（V24 前置数据"目标表已由负责人建立"）及 `Server/design.md:139`（写入语句需票 02/03）方向一致。
- **层级归属与矩阵逐条相符（G）**：Python 逐条比对确认票 07 的组件层集合**全部命中 Component 层**、票 09 的 Host 层集合**全部命中 Host 层**且**无遗漏**、**正确排除了纯 Component 的 C2/C4/C21/C24/C25**；票 08 正确排除非真实库层级的 V29。
- **双票分层承接（G）**：C1、C3、C5-C10、C16、C17、C23（`Component/Host`）由票 07 负责组件层、票 09 负责宿主层，**两票均有对应勾选项**；C12、C13、C14（纯 `Host`）票 07 明确"由宿主链路验收票取证"、票 09 明确"无组件层证据可替代"；票 09 `:12` 另列"C11、C15、C18-C20、C22、C26 见本票下列各条"——**边界互补、无遗漏**。
- **§0.1 合规（H）**：10 份文件**无**"负责人批注/裁定原文/确认来源/回复原文"、**无**审查报告编号、**无**"独立审查/整改来源"；对包名/版本/表名/索引名/脚本路径**均已给出具体值 + 设计文档锚点**；`S3-D10/S3-D13/S3-D14/S3-D20` 属台账内正式编号，允许引用。
- **术语合规（B）**：票面**无**"有权院区""数据权限""院区级数据权限"；相关表述统一为"不属于可信医院即拒绝"。全仓 `grep "具有数据权限\|无权院区"`（排除审查工作目录）**0 命中**；SRS:2366 页要求 1、SRS:224 参与者行、UML1:355、`CONTEXT.md` 两处均为"可信上下文所属医院及其院区"口径。
- **测试状态纪律（F/G）**：9 票**勾选数全为 0**（本轮逐票 `grep -c "[x]"` 均为 0）；票面出现的 `Passed`/`验收通过`/`已完成` **均为规则引用**（README 第 3 条、票 08 `:17`），**无把未执行写成通过之处**；`Status: ready-for-agent` 属领取标签，与 `testing-baseline.md §3` 不冲突。
- **README 约定完整性（G）**：README 含"票一览"表（9 行，与票面逐票对账一致）与 10 条约定，**完整承接先例九项约定并新增"矩阵口径""状态查询"两条**；第 5 条"每票末尾的口径段声明本票覆盖的层级"**已在 9 票落实**。
- **票 04 的建表闸门（G）**：票 04 正文含"**闸门：负责人确认建表完成前，依赖新表的集成验证不执行**"，勾选项含"未建成前相关用例记 `NotRun`，实际执行时若仍未建成才记 `Blocked`"，与 `Server/impl.md` 待办、`testReport.md:36` 口径一致。
- **票 05 的硬闸门（E/G）**：票 05 把"两个依赖安装成功，`typecheck` 与 `build` 实测通过"标为**本票硬闸门**，并把"生成器版本统一后无冲突，否则回退并登记""平台 Base 服务真实调用确认"写入勾选项，与 `Client/impl.md`、`Client/design.md:30,32` 一致。
- **票 09 的宿主边界（G）**：票 09 明确"本地直达、构建成功与接口直测都不替代宿主验收"、菜单未授权记 `Blocked`，与 `testing.md §1,2`、`testPlan.md` 末段一致。
- **C3 ×2 一致性（G）**：票 07 勾选项"可信院区缺失与存在两份身份上下文分别取证……（C3 ×2）"与 `Client/impl.md`、`testPlan.md:11` 一致。
- **新增验收项质量（G）**：票 06/07 新增"先建立失败基线再实现"（票 06 `:15`、票 07 `:11`）与"交付适配层测试/组件层测试"（票 06 `:16`、票 07 `:12`），与 `testing-baseline.md §1`（RED-GREEN-REFACTOR）方向一致（可追溯性见 U-02）。

---

## 8. 反例构造（类别 I）

按第 4 节 I 项要求，检验"按票面实施会出现什么"：

| 场景 | 按票面实施的预期行为 | 与设计/SRS 比对 | 结论 |
|---|---|---|---|
| 并发重复保存 | 票 02（V16）"唯一索引兜底、库中恰 1 行、无脏写、无事件，冲突返回业务拒绝"；票 08（V16）真实库"并发首插与并发覆盖的库内结果" | 与 `Server/design.md:110,112` 一致 | 成立 |
| 零元与未配置 | 票 02（V3）+ 票 03（V17）+ 票 07（C6、C7）三票共同覆盖"可区分" | 与 SRS F10 业务规则 2、页要求 4 一致 | 成立 |
| 金额精度边界 | 票 02 的 `V4-V6` 覆盖负数/超两位/缺省与非数值 | 与 SRS F10 备选流 2 一致 | 成立 |
| 范围跨组织 | 票 03（V29 应用层）+ 票 08（口径段说明 V29 非本票层级）+ 票 09（`:20` 记 `Blocked`） | 与 S3-D17/S3-D20 一致 | 成立 |
| 范围跨医院/跨院区 | 票 02（V13、V30）、票 03（V22）、票 09（C11） | 与 SRS 备选流 1、UML1:355 一致 | 成立 |
| 院区存在/启用/父子校验 | 票 01 `:13`"组织、医院、院区不存在、已停用、父子归属不匹配时拒绝（V8-V10）" | 与 `Server/design.md:73,79,106`、UML2 两条 Query 说明一致 | 成立 |
| 互认配置不存在 | 票 02（V11）、票 09（C11①） | 与 SRS 前置条件 3、备选流 3 一致 | 成立 |
| 目录或配置停用 | 票 02（V15）、票 03（V21）、票 09（C18-C20） | 与 SRS 业务规则 5、页要求 3 一致 | 成立 |
| 可信上下文缺层 | 票 01（V14）、票 02（V14）、票 09（C15） | 与 S3-D11、`CONTEXT.md`「组织、医院和院区」一致 | 成立 |
| 查询越权 | 票 03（V22） | 与 SRS 前置条件 1-2 一致 | 成立 |
| 名称解析行数放大 | 票 01（V19）、票 03（V19，10 行与 100 行两组计数替身） | 与 S3-D7、`backend-architecture.md §5.3` 一致 | 成立 |
| 组件首屏自动选中 | 票 07 `:29`"范围选择器以受控方式使用，不因非受控自动选中首项而自动写入" | 与 `Client/design.md:34` 一致 | 成立 |
| 页面不下推枚举筛选 | 票 07 `:31`（页面侧）+ 票 06 `:17`（生成物侧） | 与 `design.md:46`（S3-D14 `AcceptedRisk`）一致 | 成立 |

---

## 9. 总体结论

**10 份文件（README + 9 票）的来源、编号、业务语义、矩阵覆盖、层级归属与依赖图均与阶段设计一致；§0.1 合规，无未执行勾选，无"已完成/通过"类措辞。**

共登记 11 条发现：**0 Blocker／0 High／0 Medium／11 Low**。全部为表述与词表层面的问题，**无一条会产出错误业务结果或违反上游规则**：

- **U-01**：9 票层级命名不统一，且与 `Server/design.md` 验证矩阵的"层级"列词表不同（无冲突，仅需映射）。
- **U-02**：票 06/07 新增的"交付测试"勾选项未绑定矩阵编号，在"矩阵编号可追溯"口径下达不到票面自校。
- **U-03**：票 09 `:12` 与 `:25` 两处集合正确且互补，但须联读两行才得完整边界。
- **U-04**：票 04 把"部分依赖"写进 `Blocked by`，与 README 第 1 条"只表达票与票之间的依赖"字面略有张力（无实质冲突）。
- **U-05**：S3-D14 已按"生成物"/"页面行为"分置两票，正确。
- **U-06**：票 01/04/05 的包名/版本/表名/索引名/脚本路径锚点齐备。
- **U-07**：票 07 `:28` 已改为"含组件层的…"，与矩阵逐条相符。
- **U-08**：票 08 已承接 V27 不结案约束。
- **U-09**：README 第 9/10 条已改为泛称与"状态查询"。
- **U-10**：票 04 未复述"起点为两份生成态文件"的现状锚点。
- **U-11**：README 票一览未列矩阵编号或主责层。

**前置条件**：进入实施须由负责人指示（X-01）；票面所依赖的外部条件（包组合可构建、依赖可安装、平台 Base 服务可达、负责人建表、菜单与角色授权）未满足前，相应票无法完整交付（X-03～X-06）。票面已通过"外部前置"行逐票登记，README 第 10 条并明确"当前执行状态以阶段根 `testReport.md` 为准"。

**本报告不使用"验收通过""已完成"等措辞；上述结论均为基于仓库原文与只读取证的判断。**
