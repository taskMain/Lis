# 08: 后端真实数据库验收

**What to build:** 在负责人建表完成、外部组织服务实测可用之后，只经正常业务接口造数据，在真实数据库与真实生产入口上验证金额保存与查询的全部真实面：首次新建与覆盖、零元与未配置、同值重复保存、互认配置缺失拒绝、目录或配置停用后仍可维护、并发首插唯一索引兜底、排序与空结果、不可用原因派生、越权院区拒绝，以及无工作单元生产入口下的写入与失败响应。事件可见性子面记 `N/A` 并附 `AcceptedRisk`，但该子面不得使整条用例结案；跨组织的真实宿主面不在本票层级，按 `S3-D20` 由宿主链路验收票登记为 `Blocked`。不以带工作单元的探针入口替代真实入口。

**Blocked by:** 04（建表脚本与 SqlMap 物理映射）

**Status:** ready-for-agent

**外部前置：** 负责人确认建表完成（票 04 交付脚本）；外部组织服务实测可用（票 01 交付）；真实生产入口与目标库可用

- [ ] V1-V3、V7、V11、V15-V17、V20-V21、V23-V24、V27 的真实库与真实入口面取得证据
- [ ] 并发首插与并发覆盖的库内结果（恰 1 行、无脏写）有证据（V16）
- [ ] 无工作单元生产入口：正常保存返回成功且回读一致；失败注入下返回业务错误且无脏写（V27）
- [ ] 事件可见性子面记 `N/A` 并附 `AcceptedRisk`；该 `N/A` 不使整条 `V27` 结案，`V27` 的数据库写入与失败响应面须单独真实取证
- [ ] 全部业务数据与状态只经正常业务接口形成；未直接写库、未伪造脏数据
- [ ] 组织服务相关验证在包引用实测构建通过后才执行；未就绪的受影响运行项记 `Blocked`，其余未执行项记 `NotRun`
- [ ] 阶段根测试报告更新为真实结果，统计自洽；无证据时不得写 `Passed` 或「验收通过」
- [ ] `git diff --check` 干净

**口径：** 本票只覆盖所引用矩阵用例的真实数据库与真实生产入口层级；`V29` 非该层级，其宿主面按 `S3-D20` 由宿主链路验收票登记；矩阵编号可跨批次引用，按完整用例只统计一次，只完成部分层级不记为通过。
