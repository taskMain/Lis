# 09: 宿主链路验收与阶段报告

**What to build:** 两个页面必须从宿主登录页登录后、经宿主菜单真实进入，完成首屏加载与主流程验收；本地直达、构建成功与接口直测都不替代宿主验收。本票承接页面实现票中所有带 `Host` 层用例的宿主面证据，以及宿主侧边界：当前组织无互认配置时的保存拒绝、请求院区不属于可信医院的拒绝、可信上下文缺组织或医院时的阻断提示与零业务请求、逐层停用的原因文案、配置自身停用不影响维护、真实业务数据量下无卡顿与不分页、可信院区缺失分支，并登记跨组织降级的环境边界。两个菜单子项与角色授权由负责人在权限系统配置；未授权时相关用例记 `Blocked`，不以本地直达替代。最后更新阶段根测试报告：统计口径一致、只按完整用例统计一次、部分层级不记为通过。

**Blocked by:** 04（建表脚本与 SqlMap 物理映射）、07（两个页面与路由）

**Status:** ready-for-agent

**外部前置：** 票 07 交付；两个菜单子项与角色授权由负责人在权限系统配置；真实宿主入口与目标库可用

- [ ] 两个菜单子项与角色授权由负责人在权限系统配置完成；未授权用例记 `Blocked`
- [ ] 承接票 07 带 `Host` 层用例的宿主面证据：C1、C3、C5-C10、C12-C14、C16、C17、C23；其中 C12、C13、C14 为纯宿主层用例，无组件层证据可替代；C11、C15、C18-C20、C22、C26 见本票下列各条
- [ ] 从宿主登录页登录后分别从菜单进入两个页面，在新标签载入并完成首屏加载，劫持生效，Console 无业务错误（C26）
- [ ] 保存被拒绝的两类前置分别在两个页面取证，页面呈现一致、零脏写（C11）
- [ ] 可信上下文缺少组织或医院时显示阻断提示、全程零业务请求、不伪造组织、不发写请求（C15）
- [ ] 仅停用分类、仅停用分组、仅停用标准项目分别显示对应文案；三层启用时原因为空（C18）
- [ ] 配置自身停用时状态列显示停用、原因列为空（C19）
- [ ] 目录或配置停用状态下仍可提交金额并保存成功，状态与原因列不因此改变（C20）
- [ ] 真实业务规模数据下连续操作未出现卡顿、请求超时或内存问题，未新增分页（C22）
- [ ] `V29` 跨组织真实宿主面按 `S3-D20` 记 `Blocked`，登记为已接受的环境边界，不作为阻塞项
- [ ] 阶段根测试报告更新：`Passed`、`Failed`、`Blocked`、`NotRun`、`N/A`、`PendingRetest` 只用真实证据；只按完整用例统计一次
- [ ] 阶段设计、实施批次与矩阵编号未被本票改动；实现文件未在本票被修改
- [ ] `git diff --check` 干净

**口径：** 本票覆盖带 `Host` 层的用例（C1、C3、C5-C20、C22、C23、C26）的宿主面证据；纯组件层用例 C2、C4、C21、C24、C25 无宿主层级，不在本票层级；矩阵编号可跨批次引用，按完整用例只统计一次，只完成部分层级不记为通过。
