# 05: 前端依赖与运行时配置

**What to build:** 让子应用具备组织/医院/院区选择控件所需的依赖与运行时配置。新增组件库 `@dy/components-base@0.1.4` 与平台 Base 客户端 `@dy/api-client-base@0.1.39` 两个依赖（不使用会与项目现有鉴权包形成同一包双实例的版本组合），在工作区依赖解析配置中把生成器版本统一到 `1.0.0-preview.103`，新增运行时配置承载 `apiBaseUrl` 与 `baseApiBaseUrl`，入口调用一次组件库初始化，开发配置补 `baseApiBaseUrl`；版本与配置口径见 `Client/design.md` 的新增运行时配置与依赖段。生产配置文件本阶段不改，也不登记生产地址。若依赖解析覆盖与既有版本并存产生冲突，回退覆盖并登记为已知依赖形态。

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

**外部前置：** 包私服可达（依赖安装）；`baseApiBaseUrl` 指向的平台 Base 服务可达（真实调用确认）；环境值按项目环境规范引用，不在票面复制

- [ ] 两个依赖安装成功，`typecheck` 与 `build` 实测通过（本票硬闸门）
- [ ] 组件库的具名导出与平台 Base 客户端的类型声明与设计所用名字兼容（安装后编译期确认）
- [ ] 生成器版本统一后依赖树无冲突；若冲突则回退依赖解析覆盖并登记为已知依赖形态
- [ ] 运行时配置承载 `apiBaseUrl` 与 `baseApiBaseUrl`，入口只初始化一次组件库
- [ ] 开发配置含 `baseApiBaseUrl`；生产配置未被改动，未在本阶段引入生产地址（C25 配置面）
- [ ] `baseApiBaseUrl` 指向的平台 Base 服务以真实调用确认可用；确认前范围下拉不视为已验证
- [ ] 未回落到会与现有鉴权包形成同一包双实例的依赖组合
- [ ] 适用测试、构建、静态检查通过；`git diff --check` 干净
- [ ] 证据记入阶段测试报告

**口径：** 本票覆盖层级为依赖与运行时配置的组件层；本票只覆盖所引用矩阵用例在该层级的证据，矩阵编号可跨批次引用，按完整用例只统计一次，只完成部分层级不记为通过。
