# 06: 证据收口与发布结论

**What to build:** 阶段 7 收口：核对矩阵冻结与例外增补记录，完成上游依赖健康、端口与进程边界、起始提交快照、测试数据余量检查，按分项口径完成既有证据复用登记与分类汇总，逐条登记残余风险与未关闭项，输出 `Ready`/`ReadyWithAcceptedResiduals`/`NotReady` 三态发布结论。

**Blocked by:** 票 05。

**Status:** ready-for-agent

- [ ] V30 矩阵冻结核对：分母（本阶段 V1-V33/C1-C22 + 过堂清单）与冻结记录一致；例外增补的重新冻结记录完整
- [ ] V31 上游依赖与外部服务健康：组织服务、参数服务、权限系统可达
- [ ] V32 端口与进程边界、起始提交与工作区快照、测试数据余量核对记录完整
- [ ] C19-C21 复用登记与汇总：既有 `Passed` 条目与失败路径复用（输入来自票 05 的复用决定）按阶段、按验证面分项登记复用条件（影响范围、证据版本与环境、时间、复用理由），收口分类列出复用项、失效项与修复后重测项；C20 定案：汇总票 03、票 05 的同步动作输入并核对登记齐全；V19 追加登记齐全性核对（输入来自票 02、票 05）
- [ ] V33 残余风险逐条登记与发布结论：结论只取 `Ready`/`ReadyWithAcceptedResiduals`/`NotReady` 三态之一（`Ready` 无残余；`ReadyWithAcceptedResiduals` 残余全部已定级接受；存在未定级条目或未关闭项即 `NotReady`——未关闭项与 S7-D4② 联动），负责人裁定写入结论段
- [ ] 适用测试、构建、静态检查与 `git diff --check` 通过；根 testReport 与总体计划进度表 009 行更新
