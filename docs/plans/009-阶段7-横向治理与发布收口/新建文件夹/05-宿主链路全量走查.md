# 05: 宿主链路全量走查

**What to build:** 发布前全量联调：医院接入接口 9 项与管理端 10 个页面在真实宿主下的成功主路径全量走查（身份按票 00 核对的菜单授权分配），双入口请求形态与权限不扩散前端核对，以及同类能力（时间控件、分页形态、弹窗表单）的统一判定与执行。本票发现的核对问题按 S7-D4 三桶在本票内分流处置（与票 02 口径一致）。

**执行节奏：** 本票按「接口组（V20-V29）→ 页面组（C1-C10、C14-C15）→ 控件组（C16-C18）」三段推进，每段完成即登记根 testReport 再进入下一段；中途上下文耗尽时按已登记进度续做，不重复已取证条目。

**Blocked by:** 票 03、票 04。

**Status:** ready-for-agent

- [ ] V21-V29 医院接入接口 9 项成功主路径：检验/检查报告提交、两类作废、匹配查询、处理结果、引用详情、引用结果、PDF 下载；调用身份与鉴权方式复用阶段 5 联调已验证方式，首条核对调用可达
- [ ] C1-C10 管理端页面走查：10 个页面按实际菜单授权逐页成功主路径（含统计四页的下钻、原因区、导出入口）
- [ ] C14 双入口请求形态核对：本院入口页面请求不含本侧组织/医院字段
- [ ] C15 权限不扩散前端面：页面无二次权限确认交互，接口失败由宿主统一处理
- [ ] C16-C18 同类能力统一判定：按 §2 判定规则逐对核对（成对列举页面与组件引用）；本就一致的取证记 `Passed` 并写明「无需统一」；确需统一的按等价修复执行并经负责人确认；统一改写若使既有阶段证据失效，在本票内重取受影响用例证据（登记为修复后重测项），回写登记动作作为 C20 输入由票 06 定案
- [ ] V20 同类能力统一回写登记：统一动作的后端回写逐条登记（含「如需后端配合」分支），与 C20（前端同步登记，票 06 定案）并行记录
- [ ] 失败路径与边界复用：记录本次走查的复用决定（作为 C19 输入；分项登记与「复用/失效/重测」分类归票 06）
- [ ] 核对问题按 S7-D4 三桶分流处置（同票 02 末项口径）
