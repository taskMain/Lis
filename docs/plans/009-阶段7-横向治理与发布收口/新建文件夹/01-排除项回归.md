# 01: 排除项回归

**What to build:** 按排除项核对清单逐条确认 SRS 排除项与设计约束中的能力级不建设内容未被实现：以静态守卫（端点/OpenAPI/菜单/路由清单、代码关键字扫描、契约字段与表形态扫描、导出契约、设计比对）为主体，辅以行为证据（HIS 历史版本拒绝面、导出响应内容类型）。

**Blocked by:** 票 00（分母冻结后方可取证登记）。

**Status:** done（2026-09-22）

- [x] V1 端点、OpenAPI 操作与管理端路由清单守卫：EX-02、EX-04、EX-07、EX-11、EX-16、EX-17、EX-20、EX-22、EX-24、EX-25、EX-27、DC-01 所列「无端点」类操作与菜单不存在 → `Stage7ExclusionGuardTests` 三个 V1 测试 + 证据见根 testReport 批次0表
- [x] V2 HIS 面开放范围守卫：EX-23、DC-11——历史版本接口不向 HIS 开放，管理端历史版本面只读 → 两个 V2 测试（注释按 V6 行为事实修正，断言改为共享前缀下无历史版本查询路由的精确形态）
- [x] V3 代码关键字扫描：EX-03、EX-06、EX-08、EX-10、EX-13、EX-15、DC-04、DC-06、DC-07、DC-09——含不自行拼接影像调阅地址 → 两个语法节点级测试 + grep 全文甄别记录
- [x] V4 契约字段与表形态扫描：EX-05（含过程字段）、EX-12、EX-14、EX-19、DC-10——三项系统参数均复用既有参数服务 → 五个 V4 测试
- [x] V5 导出契约守卫：EX-18、EX-25、DC-08——仅 xlsx、仅既有明细导出、继承查询条件与数据范围 → V5 测试 + 既有 Stage6StatisticsContractTests 断言引用
- [x] V6 行为证据：HIS 历史版本路由拒绝面、导出响应内容类型 → 宿主实测（见根 testReport V6 节），`Passed`；`OpenReportVersionPdf` 共享前缀可达性事实登记并转残余风险终审
- [x] V7 设计比对守卫：EX-01、EX-09、EX-17、EX-21、EX-26、DC-02、DC-03、DC-05 → 与阶段5/6设计逐条比对一致
- [x] 按合并声明核对同源条目的主取证编号，每条 EX/DC 的取证证据登记根 testReport → 见批次0表逐条映射

**实施审查（2026-09-22 通过，整改已完成）：** 独立审查覆盖守卫测试质量、EX/DC 覆盖（38/38 条）、V6 定性、状态纪律、改动边界与语言纪律；两处 P1（V3 登记数字 73→实测 347、V6 事实登记补全第二条 PDF 承载路由与 S4-D28 语义）与 P2（禁用字、V2 摘要精确化、状态行一致化）均已整改，整改后守卫测试 13/13 通过、`git diff --check` 干净。统一用语结论：新增内容继续沿用设计清单既有用语「契约」，不作改写。CONTEXT.md 匹配响应 PDF 下载入口绑定版本重校验的承接存续核对转票02（见根 testReport「转入批次1 的核对输入」）。
