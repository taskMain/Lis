# 前端测试矩阵

只记录测试前计划，不记录通过结果。

| 编号 | 页面或能力 | 身份与前置数据 | 操作及边界 | 预期页面、请求和业务状态 | 层级 |
|---|---|---|---|---|---|
| C1 | 标准项目目录维护（同一工作台承载分类分组管理与标准项目管理两界面节，S1-D20） | 平台管理员 | 分类/分组/标准项目列表、启停、新增编辑成功主路径 | 页面正常渲染与操作，宿主菜单进入 | 宿主 |
| C2 | 互认项目管理 | 平台管理员 | 列表、可互认时间维护、启停成功主路径 | 同上 | 宿主 |
| C3 | 互认项目金额维护 | 平台管理员 | 查询、保存成功主路径，组织/医院/院区请求值生效 | 同上 | 宿主 |
| C4 | 本院区互认项目金额 | 医院管理员（两身份之一） | 查询、保存成功主路径，院区限可信医院 | 同上 | 宿主 |
| C5 | 报告管理（平台管理员） | 平台管理员 | 列表、筛选、历史版本查看、PDF 下载成功主路径 | 同上；患者字段原值展示 | 宿主 |
| C6 | 报告管理与历史版本（医院管理员） | 医院管理员 | 列表、筛选、历史版本查看、PDF 下载成功主路径，范围限可信医院 | 同上；患者字段原值展示 | 宿主 |
| C7 | 接收医院互认使用统计（平台/本院双入口） | 按实际菜单授权（见 C11） | 汇总、明细下钻、原因区、导出入口成功主路径；患者字段原值核对 | 同上；后端分页生效 | 宿主 |
| C8 | 本院互认使用统计（双入口） | 按实际菜单授权 | 同上 | 同上 | 宿主 |
| C9 | 来源医院被互认统计（双入口） | 按实际菜单授权 | 同上 | 同上 | 宿主 |
| C10 | 本院被互认统计（双入口） | 按实际菜单授权 | 同上 | 同上 | 宿主 |
| C11 | 宿主菜单注册核对 | 两身份 | 从宿主登录后读取两身份各自可见菜单清单，与本设计页面清单（10 个）比对 | 菜单项与页面清单一一对应；差异逐条说明并修正矩阵身份分配 | 宿主 |
| C12 | 各阶段非干净条目前端面过堂 | 按原条目要求 | 阶段 2 C17、阶段 3 C3/C11/C15/C22/C23、阶段 4 前端未收口条目、阶段 6 潜在前端非干净条目及本阶段矩阵宿主类前端条目（宿主不可用场景）逐条定级 | 逐条定级（三出口见 S7-D2）并登记 | 宿主 |
| C13 | C-1 患者原值核对 | 两身份（按 C11 授权），覆盖平台与本院两侧入口 | 报告管理与统计页面患者姓名、证件号码核对；报告版本详情联系电话核对 | 全部原值展示，无掩码 | 宿主 |
| C14 | C-2 双入口请求形态核对 | 两身份 | 本院入口页面请求不含本侧组织/医院字段 | 网络请求与口径一致 | 宿主 |
| C15 | C-4 权限不扩散前端面 | 两身份 | 页面无二次权限确认交互；接口失败由宿主统一处理 | 无本地错误文案与扩散代码 | 宿主 |
| C16 | 时间/日期筛选控件一致性 | 按 C11 授权 | 核对范围：已有日期筛选的页面（现阶段为报告管理两入口，统计页交付后扩入）；核对对象成对列举（页面 + 组件引用） | 已有实现本就一致时取证（组件引用比对）记 Passed 并写明「无需统一」结论；存在 ≥2 页面形态不一致时按 S7-D7 统一并经负责人确认 | 宿主 |
| C17 | 分页形态核对 | 按 C11 授权 | 阶段 6 统计明细分页交互；阶段 1–3 维持不分页 | 与各自阶段设计一致；阶段 4 与阶段 6 前端分页形态若不一致按 S7-D7 统一 | 宿主 |
| C18 | 弹窗与表单交互一致性 | 按 C11 授权 | 新增/编辑弹窗、提交中状态、失败保留输入；核对对象成对列举 | 同类交互统一为实现一套或取证「本就一致」；统一目标以既有实现或宿主界面规范为准 | 宿主 |
| C19 | 证据复用核对 | — | 失败路径与边界用例复用各阶段既有证据 | 复用条件按阶段、按验证面分项登记（影响范围、证据版本与环境、时间、复用理由），收口分类列出复用/失效/重测项 | 静态 |
| C20 | 同步动作登记 | — | 前端文档同步（患者原值、控件统一等）逐条登记 | 受影响阶段文档与实现一致 | 静态 |
| C21 | 前端面证据汇总 | — | C1-C20 与 C22 证据归集进阶段根 testReport | 汇总完整 | 静态 |
| C22 | 业务规则类约束承接核对 | — | 核对 [阶段设计](../design.md) §3 首段例举约束（SRS :2067/:2434、:274、:276、:1517）的既有承接用例证据存续与阶段 6 交付物口径一致 | 承接证据存续、口径一致；异常项按 S7-D4 分流登记 | 静态 |

环境值只引用项目环境规范；运行命令、截图、响应和结果进入 [阶段测试报告](../testReport.md)。
