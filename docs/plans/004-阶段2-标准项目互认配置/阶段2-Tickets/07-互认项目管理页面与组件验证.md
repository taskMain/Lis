# 07: 互认项目管理页面与组件验证

**What to build:** 通过阶段2稳定 API Client 实现“互认项目”管理页面，完成组织范围、目录选择、配置维护、启停、刷新、失败恢复和写后状态更新的可验证用户流程。

**Blocked by:** 03 互认配置写入与启停并发规则；04 互认配置查询与目录停用说明；06 累计 API Client 生成与契约校验

**Status:** done（2026-09-15：组件/构建层的全部交付项完成；Host 端到端验证归 Ticket 08）

**已完成**：契约无关基线（页面、适配层、组织唯一接缝、方案文档、39 条用例）+ **路由与真实接线**（`routes.tsx` 注册 `recognition-projects`；5 个 I/O 出口全部改为生成端点调用；契约收敛完成，`pendingIntegration` 全仓零残留）。组织接缝按 S2-D12 结案口径接入 `LoginUserManager.getOrgID()` + `useAuth()`，空/空白即不可用、不伪造组织、不退化。**配置状态筛选不下推**：这是业务口径——下推会缩小读取范围、破坏新增排除集合的完整性（该枚举的契约缺陷已于 2026-09-16 修复，生成端按 `number | null` 数值读写，见阶段 `testReport.md` 的枚举契约修复节），页面按设计在本地筛选。独立复核（[审查-Ticket07接线复核.md](../Client/审查-Ticket07接线复核.md)）：Blocker 0 / High 0；其 2 项 Medium 已整改（移除会写出 `{}` 的状态筛选通路并把用例改为断言"不下发"；修正 `Client/impl.md` 与代码矛盾的状态描述），8 项 Low 已登记待评估。

- [x] 进入页面自动加载当前组织配置和当前有效标准目录，并提供手动刷新。**组件面已实现并有用例；宿主面由 Ticket 08 完成（菜单授权已补齐，宿主验收已执行）。**
- [x] 新增选择器只允许选择最底层当前有效标准项目；当前组织已存在配置的项目（含停用配置）不可选；切换组织后按目标组织重新计算排除集合。**排除集合按全量配置计算（非筛选后子集），理由见方案文档第 5 节。**
- [x] 支持新增、修改时间、独立启停；停用目录仍可修改时间和停用，重新启用失败时保留状态和操作入口。**组件面已实现；真实后端拒绝语义属 Ticket 08。**
- [x] 写入失败保留必要输入和弹窗；写成功后刷新列表和选择数据；刷新失败不改判为写入失败并阻止继续写入。
- [x] 覆盖组织切换清理、迟到响应隔离、查询失败恢复、同值保存和重复提交保护。
- [x] 覆盖未保存新增/编辑内容时的组织切换确认；取消保留内容，确认才清理。
- [x] 页面设计遵循已确认的前端规范，不新增权限模型、错误拦截器或分页。

**Verification and evidence:**

- [x] 先建立组件/契约失败证据，再实现并精确复测。（接线批次以既有 39 条用例为基线扩展，接线时点相关文件为 `recognitionProjectsApi` 27 条、`RecognitionProjects` 21 条、`routes` 3 条；**条数以阶段 `testReport.md` 的"门禁终值"节为准**）
- [x] 覆盖 C1-C3、C6、C8、C10-C11、C13-C21、C23；归档组件、typecheck 和必要 Host 证据到阶段 `testReport.md`。（组件/typecheck 面已归档；Host 面由 Ticket 08 归档）
- [x] C25 的不分页风险证据在真实环境由本票或 Ticket 08 归档。**已由 Ticket 08 宿主验收归档**：真实环境数据量（1-2 条，宿主验收结束时为 4 条）下未出现卡顿、请求超时或内存问题，未新增分页。
- [x] 完成适用组件测试和检查，证据归档到阶段 `testReport.md`。（本票执行时门禁为 test 85 passed / 2 skipped、build、lint、`tsc --noEmit`、`git diff --check` 均 exit 0；**该时点之后经批次A/B 与复审整改，终值见阶段 `testReport.md` 的"门禁终值"节**）
