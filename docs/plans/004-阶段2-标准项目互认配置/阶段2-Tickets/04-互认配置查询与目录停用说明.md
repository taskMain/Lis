# 04: 互认配置查询与目录停用说明

**What to build:** 提供当前组织的互认项目配置查询，实时关联标准目录资料，并准确表达目录停用原因、配置自身状态、空结果和稳定排序。

**Blocked by:** 01 组织上下文与事件机制取证；02 互认配置公共契约与生成物整理

**Status:** done（2026-09-15 交付；真实库与真实 token 面由 Ticket 08 于 2026-09-15 执行、2026-09-16 整改后按 Testing Baseline §5 重取）

- [x] 按可信组织范围过滤配置，拒绝越权组织查询，不降级为空集合。校验先于查询端口调用（测试断言端口调用次数为 0）。
- [x] 支持标准项目编码和配置状态筛选，不增加未经确认的名称、分类、分组或类型筛选。**口径**：服务端查询契约与 SQL 支持这两项筛选（由 V12-V14 的端口/SQL 面覆盖）；页面按设计在已读取结果上本地筛选、不下推，原因是下推会缩小读取范围并破坏新增排除集合的完整性（见阶段 `Client/design.md` 的"筛选口径"）。
- [x] 实时关联标准项目名称、类型、分类和分组，不在互认配置中重复保存目录属性。SQL 内联标准目录三表取展示资料。
- [x] `UnavailableReason` 按分类、分组、标准项目顺序返回单一停用原因；目录全部启用时返回 `null`；配置自身停用不占用该字段。不构造目录资料缺失等非正常业务数据。
- [x] 按标准项目编码升序排序，不分页，空结果返回空集合；不构造同编码或相同 ID 数据验证附加排序。排序键为 `standard_project_code asc, id asc`。
- [x] 查询语句归入既有查询 SqlMap，使用独立查询 scope `MedicalRecognitionReportQuery`；不得放入互认项目实体 SqlMap。注册键实测为 `MedicalRecognitionReportQuery.QueryRecognitionProjectConfigurationList`。

**Verification and evidence:**

- [x] 先建立查询契约/投影失败证据，再实现并精确复测。先落 `NotImplementedException` 骨架使测试可编译，`--filter Stage2QueryTests` 失败 15/16（行为缺失），实现后 16/16 通过。
- [x] 覆盖 V12-V14、V19-V23；记录组织过滤、原因优先级、空值、排序和异常边界到阶段 `testReport.md`。V13/V19/V20 逻辑面已覆盖；V12/V14 的 SQL 面与 V23 的运行面已由 Ticket 08 执行（参见阶段 `testReport.md` 的宿主验收章节）。
- [x] 按项目现行规范完成必要检查，证据归档到阶段 `testReport.md`。（该轮时点值：构建 0 错误、全量 75/75、`git diff --check` 0；全阶段终值见 `testReport.md` 的"门禁终值"节。）`Stage1ArchitectureTests` 的查询契约断言已随"扩展同一接口"从方法数改为冻结方法名集合（**加强**，非削弱），需在阶段2 报告中登记该阶段1 测试适配。
