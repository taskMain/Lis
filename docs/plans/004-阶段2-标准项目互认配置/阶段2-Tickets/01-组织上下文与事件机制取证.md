# 01: 组织上下文与事件机制取证

**What to build:** 明确互认项目配置使用的可信组织来源、组织选择传递链、授权范围校验，以及无显式 WorkUnit 的生产入口中事件登记、处理、数据库提交和失败传播边界。

**Blocked by:** None (can start immediately)

**Status:** done（2026-09-15；证据见 `../Server/取证-组织与事件机制.md`，决策状态见 `../design.md` 的 S2-D12/S2-D13）

- [x] 核实组织服务接口、授权组织集合、所选组织从宿主到服务端的可信传递链，以及缺失和越权错误语义。结论：组织编码来源与类型、传递链已确认（token `org` claim → `HttpRequestInfo.OrgId`）；平台无"调用方授权组织集合"契约来源，采用"只信可信当前组织"口径。
- [x] 核实无 WorkUnit 生产入口的事件登记、处理、提交和失败观察点；不使用带 WorkUnit 的替代探针。结论：Flush 主体、时点与失败语义已 `SourceConfirmed`；运行面由 Ticket 08 执行（写入与失败响应面实测通过；事件可见性子面按 Testing Baseline §3 记 `Blocked`）。
- [x] 更新 S2-D12、S2-D13 的状态或保留明确阻断结论，并记录可复核证据。两项均已结案（`DesignConfirmed`）。
- [x] 不修改审查材料目录，不执行数据库写入或代码生成。

**Verification and evidence:**

- [x] 先形成源码/配置静态失败证据或准确的待核实证据，再完成取证。
- [x] 覆盖 V11、V16、V21-V24 的取证面；证据归档到阶段 `testReport.md`（"本轮执行结果"章节）；**事件登记/处理/提交的可见性（V24）** 的运行面未执行，按 Testing Baseline §3 整条记 `Blocked`（受阻原因登记为决策标签，接受口径待负责人确认）；执行状态以阶段 `testReport.md` 为准。
