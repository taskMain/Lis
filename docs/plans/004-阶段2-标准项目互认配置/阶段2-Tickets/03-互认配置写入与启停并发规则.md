# 03: 互认配置写入与启停并发规则

**What to build:** 让互认项目配置的新增、可互认时间修改、启用和停用形成完整后端业务闭环，并保证组织归属、唯一性、幂等、并发状态更新和领域事件语义一致。

**Blocked by:** 01 组织上下文与事件机制取证；02 互认配置公共契约与生成物整理

**Status:** done（2026-09-15 交付；真实库与宿主面由 Ticket 08 于 2026-09-15 执行、2026-09-16 整改后按 Testing Baseline §5 重取）

- [x] 新增时按编码取得标准项目，校验标准项目、分类和分组当前有效，创建后默认启用。应用层按编码解析 `StandardItemId`；Manager 复用三层读取校验；创建固定 `is_valid = true`。
- [x] 修改只改变可互认时间；停用配置仍可修改；同值保存仍正常登记修改事件。条件更新只改天数与操作字段，同值仍影响 1 行并登记事件。
- [x] 启停按 `ID + 可信组织 + 原状态` 条件更新，影响1行才登记事件。启用 `is_valid = false`、停用 `is_valid = true` 且均带 `organization_code`。
- [x] 重复同方向操作幂等成功；0行通过可信组织复读区分幂等、并发冲突、不存在和越组织；不自动无限重试。已由 `ResolveConcurrentStateChangeAsync` 复读一次实现，>1 行抛不变量异常且不登记。
- [x] 创建事件包含启用状态；修改和启停事件包含所需配置、组织、项目和公共操作字段。四 Event 已补 `OrganizationCode`/`StandardProjectCode`/`IsValid`，`EventCreator←OperId`、`EventCreatedTime←OperTime`。
- [x] 唯一键冲突由数据库唯一索引兜底，数据库异常按原样向外传播，仓储与管理器均不做冲突翻译；新增写入前按组织编码与标准项目编码查重（不筛选启用状态），命中即按重复配置拒绝；跨组织配置 ID 写入拒绝且不修改数据或登记事件。

**Verification and evidence:**

- [x] 先取得准确 RED 或静态失败证据，再实现并精确复测。**部分满足并如实记录**（该勾选项按"已在阶段报告如实登记该边界"闭合）：本轮实际取得的是**编译级**真实失败（19 个 `error CS` 逐个点名缺失成员），因写入侧与查询侧并发修改同一解决方案，工作区曾短暂不可编译，**行为级 RED 未捕获**；静态失败证据在 Ticket 01 取证与契约批次已取得。该边界见阶段 `testReport.md`。
- [x] 覆盖 V1-V9、V11、V15-V16、V26-V27；所有前置配置通过正常接口或页面建立，记录证据到阶段 `testReport.md`。单元面由 `Stage2WritePathTests.cs`（32 用例，含 2 个三分支 Theory）覆盖；V2/V15 的真实库翻译与 V26 的并发同步屏障已由 Ticket 08 执行（V26 的"事件恰好登记一次"计数子面记 `Blocked`，见阶段 `testReport.md`）。
- [x] 组织和事件机制未解除时，只执行不依赖它们的契约/静态工作，相关验证保持 `Blocked`。S2-D12/S2-D13 已结案，实现不再受阻。
- [x] 四个写入口各保持最多一条写语句且不声明 `[WorkUnit]`；若出现同表多写或跨表原子写入，先修订设计再实现。已由反射断言 `Mutual_write_entrypoints_declare_no_work_unit` 固定。
- [x] 按项目现行规范完成必要检查，证据归档到阶段 `testReport.md`。（该轮时点值：构建 0 错误、全量测试 75/75、`git diff --check` 0；全阶段终值见 `testReport.md` 的"门禁终值"节。）
