# 08: 数据库与宿主真实验收

**What to build:** 在前置条件齐备后，从真实数据库和宿主登录菜单验证互认项目配置的持久化、组织范围、事件边界、页面操作和最终业务状态。

**Blocked by:** 01-07 完成相应交接；阶段1收口、负责人建表确认、组织与事件机制仍是外部验收前置

**Status:** blocked-until-prerequisites-ready

- [ ] 核验负责人已确认目标数据库/schema并完成 `mrec_mutual_recognition_item` 建表；建表动作由负责人执行，不由本票代执行。
- [ ] 确认阶段1前置收口、目标环境组织服务、宿主菜单和测试身份。
- [ ] 仅通过真实业务接口或页面添加测试数据、修改时间和启停，验证回读、唯一约束、跨组织拒绝及目录停用行为；不执行SQL或通过数据库工具、脚本、测试夹具直接写库。
- [ ] 验证事件登记/处理/提交边界；无法安全观察的子面按框架边界记录，不伪造通过。
- [ ] 从宿主登录页和菜单进入页面，完成 Network、Console、DOM 和最终业务状态证据。
- [ ] 更新阶段2 `testReport.md`，按 Testing Baseline 使用 `NotRun`、`Passed`、`Failed`、`Blocked`、`PendingRetest`、`N/A` 六种状态。

**Verification and evidence:**

- [ ] 覆盖 V2、V5、V7、V12-V15、V17-V18、V20、V23-V27 的真实适用面；V10按矩阵记录为 `N/A`。
- [ ] 覆盖 C4-C9、C12-C13、C17、C20-C21、C25 的 Host/Evidence 面。
- [ ] 记录每条用例的命令/操作、响应、页面状态、持久化结果、事件观察边界和残余风险。
- [ ] 真实验收完成后按项目规范归档检查和测试证据，不将本票 Status 作为测试状态。
