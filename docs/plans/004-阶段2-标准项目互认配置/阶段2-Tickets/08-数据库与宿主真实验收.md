# 08: 数据库与宿主真实验收

**What to build:** 在前置条件齐备后，从真实数据库和宿主登录菜单验证互认项目配置的持久化、组织范围、事件边界、页面操作和最终业务状态。

**Blocked by:** 01-07 完成相应交接；阶段1收口、建表完成确认、组织与事件机制仍是外部验收前置

**Status:** done（2026-09-15 执行、2026-09-16 整改后重取；`Passed` 47 / `Blocked` 4 / `N/A` 1，逐条证据见阶段 `testReport.md` 的"Ticket 08 宿主验收执行结果"与"批次A/B 整改后的宿主重取（2026-09-16）"）

**执行结果**：从宿主登录页（入口「检验检查结果互认平台」）经菜单进入「互认项目」，在同一链路完成 Network/Console/DOM/响应/最终业务状态取证；经业务接口与页面完成增改启停、停用后修改时间、目录停用下的操作分支与目录停用原因验证。2026-09-15 当日补做两条原 `Blocked`：**两页面并发重复配置**（两个宿主标签各持载入时选项快照，后到提交得统一业务错误、表单与弹窗保留、零刷新、无脏写）与 **写成功但刷新失败**（浏览器侧一次性受控故障注入，仅拦截列表请求；写入 `200` 且落库、页面不改判为写失败、进入可重试阻断态）。**四条用例整条记 `Blocked`（2026-09-16 采用严格基线口径）**：**组织切换后目标组织读取失败**的真实宿主页面层（需第二个授权组织，本环境无法构造；替代做法需篡改共享登录存储，不予采用，采纳组件层自动化证据）、**事件登记/处理/提交的可见性**、**并发停用时"恰好登记一次事件"的计数子面**、**跨组织写配置 ID 的拒绝**（构造不出"第二个可信组织持有配置"的合法前置）。这四条的受阻原因均登记为决策标签（接受口径待负责人确认），按 Testing Baseline §3 不改变状态、决策标签不计入统计（4 条 `Blocked` 计入 52 条分母并单列）。

- [x] 核验目标数据库/schema 已确认并完成 `mrec_mutual_recognition_item` 建表；建表动作由执行方完成，不由本票代执行。**已完成：目标为 `DysoftHIS` 的 `public`，最终 DDL 已执行，并完成只读元数据逐项核实（V18/V25 `Passed`）。**
- [x] 确认阶段1前置收口、宿主菜单和测试身份。**已具备：阶段1 已收口（`Complete`）、组织/事件机制已结案、宿主菜单已授权可达、子系统业务编码 `medical-recognition` 已确认、账号与密码登记在整体测试规范第 1 节。**
- [ ] 目标环境组织服务的核对**未完成**：`Server/design.md` 的静态配置核对记为"只证明开发配置存在代理项、不证明该接口提供当前组织或授权集合"，外部代理契约与可用性尚无核对结论。本阶段四个写入口与查询入口的写法不依赖该代理（组织取自框架请求上下文的可信组织），故该未完成项不阻断本阶段已声明层级。
- [x] 仅通过真实业务接口或页面添加测试数据、修改时间和启停，验证回读、唯一约束、跨组织拒绝及目录停用行为；不执行SQL或通过数据库工具、脚本、测试夹具直接写库。**已完成：创建/修改/启停/查询全部经宿主页面与业务接口；唯一约束冲突返回统一业务拒绝且无脏写；越权组织查询被拒绝；目录停用三条原因与优先级实测通过；未执行任何直接写库。**
- [x] 验证事件登记/处理/提交边界；无法安全观察的子面按框架边界记录，不伪造通过。**数据库写入与失败响应已实测（200 并回读一致；失败 500 且无脏写）；"事件是否被登记/处理/提交"的可见性子面无可控观察点，按 Testing Baseline §3 整条记 `Blocked`，受阻原因登记为决策标签（接受口径待负责人确认，不改状态）。**
- [x] 从宿主登录页和菜单进入页面，完成 Network、Console、DOM 和最终业务状态证据。**已完成；2026-09-16 批次A/B 整改后按 Testing Baseline §5 对受影响用例重取**（真实宿主链路重跑，见阶段 `testReport.md` 的"批次A/B 整改后的宿主重取（2026-09-16）"）。
- [x] 更新阶段2 `testReport.md`，按 Testing Baseline 使用 `NotRun`、`Passed`、`Failed`、`Blocked`、`PendingRetest`、`N/A` 六种状态。**已完成：`Passed` 47 / `Blocked` 4 / `N/A` 1。**4 条 `Blocked` 为环境或框架边界导致规定验证面未执行——组织切换后目标组织读取失败的宿主页面层、事件可见性、并发停用的事件计数子面、跨组织写拒绝；其受阻原因登记为决策标签（接受口径待负责人确认，按 Testing Baseline §3 不改变状态、决策标签不计入统计；4 条 `Blocked` 计入 52 条分母并单列）。

**Verification and evidence:**

- [x] 覆盖 V2、V5、V7、V12-V15、V17-V18、V20、V23-V27 的真实适用面；V10按矩阵记录为 `N/A`。**例外（按 Testing Baseline §3）**：**事件登记/处理/提交的可见性（V24）**、**并发同方向停用时"恰好登记一次事件"的计数子面（V26）**、**跨组织写配置 ID 的拒绝（V27）**三条的受阻子面未执行，整条记 `Blocked`（受阻原因登记为决策标签，接受口径待负责人确认，不改变状态、决策标签不计入统计；三条 `Blocked` 计入 52 条分母并单列）。
- [x] 覆盖 C4-C9、C12-C13、C17、C20-C21、C25 的 Host/Evidence 面。**除 C17 的 Host 面（环境边界，受阻原因登记为决策标签、接受口径待负责人确认）外全部覆盖；组织切换后目标组织读取失败（C17）采纳 Component 面证据——用例「C17：目标组织加载失败时不进入可操作状态，也不展示旧组织数据」（`RecognitionProjects.test.tsx`）。**
- [x] 记录每条用例的命令/操作、响应、页面状态、持久化结果、事件观察边界和残余风险。
- [x] 真实验收完成后按项目规范归档检查和测试证据，不将本票 Status 作为测试状态。
