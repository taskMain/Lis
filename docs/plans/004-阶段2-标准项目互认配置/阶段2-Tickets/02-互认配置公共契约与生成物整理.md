# 02: 互认配置公共契约与生成物整理

**What to build:** 形成稳定的互认项目配置公共契约，使创建请求、查询请求、ReadModel、枚举、可空字段和生成遗留类型能够被后端与 API Client 一致使用。

**Blocked by:** None (contract work can start immediately; organization/event runtime evidence remains a separate dependency)

**Status:** done（2026-09-15；第 5 项的影响清单已产出并转 Ticket 06 执行，见 `../impl.md` 待办与交接）

- [x] 创建 Request 只提交标准项目编码和可互认时间，不提交组织编码或内部标准项目 ID。已移除 `OrganizationCode`，生成映射仍正常（契约测试断言组织未被映射）。
- [x] 查询 Request 保留必填组织编码，并明确由服务端校验授权范围。已新增 `RecognitionProjectConfigurationListQueryRequest`；校验口径按 S2-D12 结案为"只接受与可信当前组织相等的组织"。
- [x] ReadModel 不包含创建/修改信息及两个已删除派生状态字段；`UnavailableReason` 可空；状态和项目类型复用服务端正式枚举 `MedicalItemType`、`ConfigurationStatus`（生成端按 `number | null` 数值读写，生成端不产出 TypeScript 枚举，前端取值由适配层本地常量承载）。已迁入 `Queries/`。
- [x] 明确 `MutualRecognitionItemDto` 生成遗留类型不属于阶段2公共契约，并按生成器所有权决定后续删除或继续排除。结论已记录，未改动其代码、未新增调用方。
- [x] 完成 OpenAPI、生成入口、锁文件和 API Client 影响清单；C22/C24由Ticket 06执行，C23由Ticket 07执行，本票只提供稳定契约输入。影响清单已登记到 `../impl.md` 待办；生成与核对归 Ticket 06。

**Verification and evidence:**

- [x] 先建立契约静态失败证据，再修正并精确复核。
- [x] 覆盖 V21；为 C22-C24提供稳定契约、字段清单和影响说明，实际验证归Ticket 06/07。V21 契约面已执行 5 条断言，完整契约面随四个写请求校验在 Ticket 03 关闭。
- [x] 按项目现行规范完成必要检查，证据归档到阶段 `testReport.md`（"本轮执行结果"章节）。（该轮时点值：构建 0 错误、测试 27/27、`git diff --check` 0、`detect-changes` 风险 low；终值见 `testReport.md` 的"门禁终值"节。）
