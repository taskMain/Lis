# 02: 互认配置公共契约与生成物整理

**What to build:** 形成稳定的互认项目配置公共契约，使创建请求、查询请求、ReadModel、枚举、可空字段和生成遗留类型能够被后端与 API Client 一致使用。

**Blocked by:** None (contract work can start immediately; organization/event runtime evidence remains a separate dependency)

**Status:** ready-for-agent

- [ ] 创建 Request 只提交标准项目编码和可互认时间，不提交组织编码或内部标准项目 ID。
- [ ] 查询 Request 保留必填组织编码，并明确由服务端校验授权范围。
- [ ] ReadModel 不包含创建/修改信息及两个已删除派生状态字段；`UnavailableReason` 可空；状态和项目类型使用正式枚举。
- [ ] 明确 `MutualRecognitionItemDto` 生成遗留类型不属于阶段2公共契约，并按生成器所有权决定后续删除或继续排除。
- [ ] 完成 OpenAPI、生成入口、锁文件和 API Client 影响清单；C22/C24由Ticket 06执行，C23由Ticket 07执行，本票只提供稳定契约输入。

**Verification and evidence:**

- [ ] 先建立契约静态失败证据，再修正并精确复核。
- [ ] 覆盖 V21；为 C22-C24提供稳定契约、字段清单和影响说明，实际验证归Ticket 06/07。
- [ ] 按项目现行规范完成必要检查，证据归档到阶段 `testReport.md`。
