# 阶段2 写入（Ticket 03）与查询（Ticket 04）独立复核

复核人：独立审查子代理（未参与开发，只读复核，不改动任何被审查文件）
复核时间：2026-09-15
复核范围：Ticket 03 / Ticket 04 的全部交付文件、两份新增测试文件、`Stage1ArchitectureTests.cs` 的一处断言修改，以及 Ticket 03/04 在阶段 `testReport.md` 中登记的声明。
复核基线：`git status --short --branch` 起始提交 `5521cb6`（工作区未提交；`main...origin/main [ahead 2]`）。
基线说明：`main` 领先 `origin/main` 的 2 个提交（`51a5d15`、`dafa6a6`，2026-09-15 18:16 / 18:31）为阶段1既有提交，非本轮产生。

> 部分被审查文档（`阶段2-Tickets/03|04`、`testReport.md`）在我复核期间被主线程并发更新（`testReport.md` 最后写入 21:09:37，晚于我首次读取的 21:03:54）。本报告对文档的结论以 2026-09-15 21:09 之后读到的版本为准，并在相关条目注明观察时点。

---

## 一、问题清单（按严重程度从高到低）

### 无 Blocker、无 High

未发现阻塞后续交付的问题。以下为 Medium / Low。

---

### M1 `Medium` 查询请求的公共契约注释声明“精确匹配”，实际 SQL 为“字面包含”，契约注释与实现语义相反

- 位置：`server/Dy.MedicalRecognition.Application.Contracts/Queries/RecognitionProjectConfigurationListQueryRequest.cs:19-23`；对应实现 `server/Dy.MedicalRecognition.Repository/Queries/MedicalRecognitionReportQuery.xml:154`。
- 证据（原文）：

  ```csharp
  /// 标准项目编码精确匹配条件；不传表示不按编码过滤，传入则须为非空白文本。
  [NonEmpty(ErrorMessage = "参数校验失败：标准项目编码不能是空白。")]
  public string? StandardProjectCode { get; init; }
  ```

  ```xml
  <IsNotEmpty Prepend="and" Property="StandardProjectCode">m.standard_project_code like concat('%', replace(replace(replace($StandardProjectCode, '!', '!!'), '%', '!%'), '_', '!_'), '%') escape '!'</IsNotEmpty>
  ```

  同一语义在 `server/Dy.MedicalRecognition.Domain/Queries/IMedicalRecognitionReportQueryRepository.cs:59` 被写成“标准项目编码按字面包含匹配，`%`、`_` 不作为通配符”，与查询请求的“精确匹配”互相矛盾。
- 与哪条规范/设计冲突：`csharp-documentation-and-reuse.md` 第 2、3 节（注释必须让维护者独立理解成员用途，不得隐藏业务规则；注释与实现不一致属完成闸门未满足）；同时 [backend-architecture.md](../../../../.agents/instructions/backend-architecture.md) 要求 Query Request 契约稳定表达调用方可见语义。
- 影响：调用方/前端会按“精确匹配”理解并据此设计交互（例如以完整编码回填再查询）；后续维护者若按注释“修正”实现为精确匹配，会在设计未裁决的情况下改变筛选行为。设计本身未定义该筛选语义（`Server/design.md:19` 只写“`StandardProjectCode`（可选）”），因此这是一处无权威定义的语义，注释却单方面给出了与实现相反的结论。
- 最小修正建议：把该属性注释改为“标准项目编码按字面包含匹配，`%`、`_`不作为通配符，不传表示不按编码过滤”；并在 `testReport.md` 或阶段 `design.md` 的待确认项中登记“编码筛选匹配语义（字面包含）待确认”。若确认应为精确匹配，则同步改 SQL 与两项测试期望，不应只改注释。

---

### M2 `Medium` V23 的互认/查询注册键证据只有控制台打印，没有断言保护，回归不会被测试发现

- 位置：`server/Dy.MedicalRecognition.Tests/Stage1SqlMapProbeTests.cs:99-112`；阶段声明见 `testReport.md:25`（“SqlMap 注册键（V23 静态/注册面）”）与 `阶段2-Tickets/04-互认配置查询与目录停用说明.md:14`（“注册键实测为 `MedicalRecognitionReportQuery.QueryRecognitionProjectConfigurationList`”）。
- 证据（原文）：该探针把所有键打印到控制台，但断言只覆盖阶段1 键：

  ```csharp
  Console.WriteLine("Registered Statement keys:");
  foreach (var key in registeredKeys) Console.WriteLine(key);

  Assert.DoesNotContain(aggregateKey, registeredKeys);
  Assert.Contains(categoryKey, registeredKeys);
  Assert.Contains("MedicalStandardCategory.UpdateMedicalStandardCategory", registeredKeys);
  Assert.Contains("MedicalStandardGroup.CreateMedicalStandardGroup", registeredKeys);
  Assert.Contains("MedicalStandardItem.CreateMedicalStandardItem", registeredKeys);
  Assert.Contains("MedicalRecognitionReportQuery.QueryMedicalStandardCategoryList", registeredKeys);
  Assert.Contains("MedicalRecognitionReportQuery.QueryEffectiveMedicalStandardCatalog", registeredKeys);
  Assert.Contains("MedicalRecognitionReport.QueryAllRecognitionReference", registeredKeys);
  Assert.Contains("MedicalRecognitionReport.RecognitionReferenceColumns", registeredKeys);
  ```

  实测打印出的 7 个 `MutualRecognitionItem.*` 键、`MedicalRecognitionReportQuery.QueryRecognitionProjectConfigurationList` 与新增的 `MedicalStandardItem.GetMedicalStandardItemByCode` 均**未出现在任何断言中**（键清单原文见第四节）。该测试文件本轮未被修改（不在 `git status` 修改列表），即阶段2 没有把 V23 的注册面固化为机器断言。
- 与哪条规范/设计冲突：`AGENTS.md` 第 2 节门 7（完成时执行适用静态检查）与 [backend-design-gates.md](../../../../.agents/instructions/backend-design-gates.md) 第 6 节“事务/作用域声明能够通过反射、架构测试或静态分析识别时，必须增加机械检查”；S2-D11 明确要求“运行键需取证”，而“取证”若只靠人工阅读控制台，无法在后续改动中自动复现。
- 影响：任何人后续把 `MutualRecognitionItem.xml` 的 `Scope` 改名、或把某个写入方法的 `sqlId` 写错，`dotnet test` 仍会全绿，V23 类保证静默失效；阶段2 交付的“注册键一致”结论只对本次打印时点有效，不可回归。
- 最小修正建议：在 `Runtime_registration_reports_full_sql_ids_without_opening_database` 中补 8 条断言（7 个互认键 + `MedicalRecognitionReportQuery.QueryRecognitionProjectConfigurationList`），并把 `MedicalStandardItem.GetMedicalStandardItemByCode` 一并冻结；若不便改阶段1 文件，可在 `Stage2WritePathTests` 增加同等断言。改动量约 10 行。

---

### L1 `Low` 新增类型 `DuplicateMutualRecognitionItemException` 未登记进 `Server/design.md` 的类型归属表

- 位置：新增文件 `server/Dy.MedicalRecognition.Domain/MedicalRecognitionReportAggregate/DuplicateMutualRecognitionItemException.cs`；应登记位置 `docs/plans/004-阶段2-标准项目互认配置/Server/design.md:38-65`（“层目录与类型归属”表）。
- 证据：`git grep -n "DuplicateMutualRecognitionItemException"` 在全仓库只命中 4 处代码/注释（`IMedicalRecognitionReportRepository.cs:170`、`MedicalRecognitionReportManager.cs:249`、`MedicalRecognitionReportRepository.cs:91,101`）与 `阶段2-Tickets/03-...md:14`，`Server/design.md` **零命中**。而该表自身声明“只列出本功能实际新增或修改的类型，不得使用‘等’‘相关类’省略”，设计正文也只写到“Repository 负责……唯一约束和影响行数”（`Server/design.md:21`），未声明新增异常类型。
- 与哪条规范/设计冲突：`AGENTS.md` 第 2 节门 2（架构/类型归属变化先更新设计再实现）；[backend-design-gates.md](../../../../.agents/instructions/backend-design-gates.md) 第 4 节类型归属表要求逐项列出新增/修改类型；[csharp-documentation-and-reuse.md](../../../../.agents/instructions/csharp-documentation-and-reuse.md) 第 1 节“出现未在设计中声明的新共享抽象……不得声明任务完成”。
- 影响：范围可控（该异常是聚合内、单一实现、有业务名称的窄类型，不是通用 Result/Exception，因此不构成抽象预算超支），但类型归属表出现遗漏会使后续“是否有计划外新类型”的差异审查失去基线。
- 最小修正建议：在 `Server/design.md` 类型归属表补一行：`DuplicateMutualRecognitionItemException | 新增领域异常 | D/Aggregate | 由 Repository 抛出、Manager 捕获翻译 | 表达唯一约束冲突的持久化事实，不承载用户文案 | 作为公共契约返回值`。并保留其在 Ticket 03 中已有的描述。

---

### L2 `Low` 启用/停用互认请求未做非空 Guid 契约校验，且缺类型级 XML 文档，与本批修改请求及阶段1 同类请求不对称

- 位置：`server/Dy.MedicalRecognition.Application.Contracts/MedicalRecognitionReportAggregate/Requests/EnableMutualRecognitionItemRequest.cs`、`DisableMutualRecognitionItemRequest.cs`。
- 证据（两文件全文，均为裸 Id）：

  ```csharp
  [IPropertyChangedAware]
  public partial record EnableMutualRecognitionItemRequest : Dto
  {
    /// <summary>ID</summary>
    public partial Guid Id { get; set; }
  }
  ```

  对照同一批被修改的 `UpdateMutualRecognitionItemConfigurationRequest.cs:18` 已加 `[NonEmpty(ErrorMessage = "参数校验失败：互认项目配置ID不能为空。")]`；阶段1 同类请求 `EnableMedicalStandardItemRequest.cs` 既有类型级 `<summary>` 也有 `[NonEmpty]`。
- 与哪条规范/设计冲突：[csharp-documentation-and-reuse.md](../../../../.agents/instructions/csharp-documentation-and-reuse.md) 第 2 节要求手写生产代码的每个类型都有中文 XML 文档；`Server/design.md:42-43` 只把这两个请求标为“**核对** Request”（故未改造本身不算违反设计）。
- 影响：`Guid.Empty` 的启用/停用请求不会在请求校验层被拒绝，而是穿透到 Manager 的 `GetMutualRecognitionItemByIdAsync(id, org)` 后以“业务拒绝：互认项目配置不存在。”返回。功能仍被拒绝、不写库、不登记事件（`Stage2WritePathTests.cs:631-659` 与 `699-712` 已覆盖越组织与缺组织路径），所以**不是行为缺陷**；但同一聚合内四个写入口的失败语义与错误文案不一致，调用方无法区分“ID 为空”与“配置不存在”。
- 最小修正建议：为两文件补类型级 `<summary>/<remarks>` 与 Id 上的 `[NonEmpty]`（文案与 Update 保持一致），并相应在 `Stage2ContractTests` 补一条空 Guid 断言；若认为“核对”范围不含改造，则至少在设计或 ticket 中登记该差异。注意两文件为生成物，改造前按其生成所有权流程确认。

---

### L3 `Low` 两处条件更新/零行断言只校验“业务拒绝”字样，未校验具体不变量语义

- 位置：`server/Dy.MedicalRecognition.Tests/Stage2WritePathTests.cs:399`（`Update_with_zero_affected_rows_is_rejected_without_event`）、`:605`（`Conditional_update_affecting_multiple_rows_is_rejected_without_event`）。
- 证据（原文）：两处均只有 `Assert.Contains("业务拒绝", error.Message);`。而 Manager 的 >1 行分支实际文案是“业务拒绝：互认项目配置状态更新异常，未完成启用。”（`MedicalRecognitionReportManager.cs:307`），0 行修改分支是“业务拒绝：互认项目配置更新失败。”（`:278`）。
- 与哪条规范/设计冲突：[csharp-backend-testing.md] 与 [testing-baseline.md](../../../../.agents/instructions/testing-baseline.md) 要求的“断言真实反映业务语义”而非同义反复；`Server/design.md:105` 对“>1 行视为持久化不变量异常”有明确语义，测试未固定该语义。
- 影响：把 >1 行分支的异常错误地换成“配置不存在”，或把“不变量异常”与“并发冲突”混淆，测试仍会通过；“不返回成功、不登记事件”这一半被 `Assert.Empty(events.Events)` 有效覆盖，因此风险限于错误分类与文案。
- 最小修正建议：把断言改为具体文案（例如 `Assert.Contains("状态更新异常", error.Message)` / `Assert.Contains("更新失败", error.Message)`），或用独立异常类型/错误码表达分类。

---

### L4 `Low` DDL 建表语句由 `create table if not exists` 改为 `create table`，该非幂等变更未在设计或 ticket 登记

- 位置：`server/Dy.MedicalRecognition.Repository/Scripts/mutual_recognition_item.sql:2`（`git diff` 显示同一行由 `create table if not exists mutual_recognition_item (` 改为 `create table mrec_mutual_recognition_item (`）。
- 证据：`git diff -- server/.../Scripts/mutual_recognition_item.sql` 第一处改动即此；`Server/design.md:119-134` 只规定列序、类型、注释与索引，未规定 `if not exists` 的取舍。
- 与哪条规范/设计冲突：`AGENTS.md` 第 2 节门 2 的“普通等价修复无需制造文档改动”在此不适用——这是脚本可重复执行性的行为变化（由可重复执行变为第二次执行报错）。方向本身是合理的（避免在表结构不符时静默跳过），但没有留下决策记录。
- 影响：执行 DDL 时若脚本已部分执行或需重跑，会直接报错而非静默通过；对“交付一次性最终脚本”的口径是改善，对“可反复执行”的口径是破坏。当前表尚未建立（见第四节数据库只读核对），因此实际影响有限。
- 最小修正建议：在阶段 `design.md` 的 DDL 段或 Ticket 04/05 证据中登记该选择及其理由（一次性最终建表脚本、不再静默跳过），或恢复 `if not exists` 并保持设计原样。

---

### L5 `Low` 两份新增测试都写全局静态状态，测试工程未禁用并行集合，理论存在跨类干扰（8 次重复运行未复现）

- 位置：`server/Dy.MedicalRecognition.Tests/Stage2WritePathTests.cs:45-56、223-235`（`HttpContextInfoFactory...SetCurrent()` 与反射写入 `EventBusFactory.CurrentEventQueue`）；`server/Dy.MedicalRecognition.Tests/Stage2QueryTests.cs:260-269`（反射写入 `HttpContextInfo.Current`）。
- 证据：`git grep -n "CollectionBehavior|DisableTestParallelization|Parallelize" -- server` 无命中；`Dy.MedicalRecognition.Tests.csproj` 无 `xunit.runner.json` 配置，即采用 xUnit 默认“每个测试类各自一个集合、集合间并行”。其中 `Stage2QueryTests` 有一例把可信组织设为 `string.Empty`（`:119`）、`Stage2WritePathTests` 有一例设为 `"   "`（`:701`），若与另一文件的用例并行，对方读取 `OrgId` 时可能看到空组织。
- 与哪条规范/设计冲突：[csharp-backend-testing.md] 对测试可靠性的要求；不算设计冲突。
- 影响：可能产生偶发失败（“无法确定当前可信组织”/“无法确定有效的操作人”），使证据不可复现。已如实记录为脆弱点（两文件的 `<remarks>` 均说明需反射注入的原因），但未记录跨类并行这一具体风险。
- 最小修正建议：在测试工程加 `[assembly: CollectionBehavior(DisableTestParallelization = true)]`，或把两份阶段2 测试放入同一个 xUnit 集合（`[Collection("stage2-global-context")]`）。实测证据：连续 8 次全量运行均 75/75（见第四节），故当前不构成失败，仅登记为可靠性风险。

---

### L6 `Low` `Stage2QueryTests` 用反射写 `HttpContextInfo` 内部 setter，而同解决方案已有更稳妥的公开工厂且被姊妹文件使用

- 位置：`server/Dy.MedicalRecognition.Tests/Stage2QueryTests.cs:260-269` 对 `HttpContextInfo.Current`、`HttpContextInfo.RequestInfo`、`HttpRequestInfo.OrgId` 逐一 `GetSetMethod(nonPublic: true)!.Invoke(...)`。
- 证据：`server/Dy.MedicalRecognition.Tests/Stage2WritePathTests.cs:45-56` 使用的是框架公开工厂：

  ```csharp
  HttpContextInfoFactory
    .CreateHttpContextInfo(null, 0, 0, HttpContextInfoFactory.CreateHttpRequestInfo(
      orgId: organizationCode, subApplication: "SUB", hosId: "HOS-1",
      branchId: "BRH-1", deptId: "DPT-1", userId: userId), null)
    .SetCurrent();
  ```

- 与哪条规范/设计冲突：[csharp-backend-testing.md] 优先复用既有测试手段；不复用需要理由。事件队列那一处（`EventBusFactory.CurrentEventQueue`）确实无公开写入 API，属不可替代并已在 `:218-235` 如实记录为脆弱点——这一处符合要求；组织上下文这一处则存在更稳妥的替代。
- 影响：反射依赖框架内部成员名与可访问性；框架升级或成员改名会使测试以反射期错误失败，而工厂 API 会给出编译期错误。功能正确性不受影响。
- 最小修正建议：`SetTrustedOrganization` 改为复用 `HttpContextInfoFactory`，与 `Stage2WritePathTests` 保持一致。

---

## 二、已确认无问题

以下各项均已按实际代码/实际命令核对，未发现问题。

### 写入侧（Ticket 03）

1. **四个 Command / Event 字段与 `Server/design.md:79-84` 的“事件最小字段”逐项一致**：
   - `Create`：`Id`、`OrganizationCode`、`StandardProjectCode`、`RecognitionDurationDays`、`IsValid`（`CreateMutualRecognitionItemCommand.cs:52` 硬编码 `isValid: true`）✔
   - `Update`：`Id`、`OrganizationCode`、`StandardProjectCode`、修改后 `RecognitionDurationDays` ✔
   - `Enable` / `Disable`：`Id`、`OrganizationCode`、`StandardProjectCode`、`IsValid=true/false` ✔
   - 四个事件都保留 `AggregateId = MedicalRecognitionReportConst.AggregateId` 与 `EventType = nameof(...)`；`EventCreator ← OperId`、`EventCreatedTime ← OperTime` 由四个 `Create...Event` 工厂显式传入 ✔（`Stage2WritePathTests` 逐字段断言 `EventCreator`/`EventCreatedTime`/`AggregateId`/`EventType`）
   - 修改/启停的事件组织与编码取 Manager 已校验归属的配置记录（`MedicalRecognitionReportManager.cs:279、302、328`），不由请求补入 ✔
2. **Manager 目录三层校验时机**：创建始终校验（`:241`）、真实启用才校验（`:297` 幂等返回在前、`:298` 校验在后）、停用不校验（`:320-334` 无目录读取）✔
3. **归属校验先于幂等判断**：`GetMutualRecognitionItemByIdAsync(id, organizationCode)` 先行，随后才判 `IsValid`（`:295-297`、`:322-324`）✔
4. **幂等不更新操作字段、不登记事件**：两条路径均直接 `return true`，未触达写方法；测试断言 `EnableCalls == 0` / `DisableCalls == 0` 且事件为空 ✔
5. **恰好 1 行才登记**：`affectedRows == 1` 分支唯一登记点（`:300-304`、`:326-330`）✔
6. **0 行复读一次的三分支**：`ResolveConcurrentStateChangeAsync`（`:366-372`）——`null` → “配置不存在”（不存在/越组织同口径）、已是目标状态 → `true` 幂等无事件、仍反向 → “请刷新后重试”冲突 ✔
7. **大于 1 行视为不变量异常且不登记**：`:307`、`:333`；测试 `Conditional_update_affecting_multiple_rows_is_rejected_without_event` 断言事件为空 ✔
8. **不自动无限重试**：全仓库无循环重试；复读仅一次（`:368`）✔
9. **AppService 顺序与可信字段**：`MedicalRecognitionRequestValidator.Validate(request)` 先于 `.MapTo`（`:264-265`、`:286-287`、`:305-306`、`:324-325`）；`OperId` 解析失败或为 `Guid.Empty` 一律 `throw`（`:267` 等，对比原实现的 `if (TryParse) 赋值` 静默保留默认值已修正）；操作时间由 `DateTimeOffset.Now` 改为 `DateTimeOffset.UtcNow`（`:269` 等）；组织编码统一由 `ResolveOrganizationCode()` 注入（`:343-348`），空白即拒绝，不使用默认值或空串 ✔
10. **S2-D6 硬约束（用实际代码 + 反射断言核对）**：`git grep -n "WorkUnit" -- server` 仅 2 处生产命中，均在阶段1 的 `CreateMedicalStandardGroupAsync` / `CreateMedicalStandardItemAsync` 上，属既有；四个互认写入口无 `[WorkUnit]` ✔。四个入口各只有一条写语句：`CreateMutualRecognitionItemAsync`（1 INSERT）、`UpdateMutualRecognitionItemConfigurationAsync`（1 UPDATE）、`EnableMutualRecognitionItemAsync`（1 UPDATE）、`DisableMutualRecognitionItemAsync`（1 UPDATE），其余均为读；且 `Stage2WritePathTests.Mutual_write_entrypoints_declare_no_work_unit`（`:731-744`）以反射固定该约束 ✔
11. **唯一约束冲突识别的 Provider 中立性**：`MedicalRecognitionReportRepository.cs:140-148` 沿 `InnerException` 链查找 `System.Data.Common.DbException` 并比较 `SqlState == "23505"`；全文件无 `Npgsql`/`PostgresException`/`SqlClient` 引用；异常被框架包装的情况由链式查找覆盖（`AggregateException` 单内层亦可穿过）✔
12. **冲突翻译为统一业务错误**：Repository 抛 `DuplicateMutualRecognitionItemException`，Manager 捕获后统一抛 `InvalidOperationException("业务拒绝：该组织已配置此标准项目。")`（`MedicalRecognitionReportManager.cs:245-253`），测试以假仓储触发并断言文案与空事件 ✔
13. **SqlMap（`MutualRecognitionItem.xml`）**：`Scope="MutualRecognitionItem"`；SQL 只访问 `mrec_mutual_recognition_item`（无 `mutual_recognition_item` 裸表名）；`oper_time` 绑定 `$OperTime`，无 `current_timestamp`；`Update`/`Enable`/`Disable` 均带 `organization_code = $OrganizationCode`，启停另带 `is_valid = false/true` 原状态条件；INSERT 显式写 `is_valid` 常量 `true` 且不绑定 `$IsValid`；每条 Statement 前有功能 XML 注释、投影/赋值字段有中文行内注释（含 `CASE`/`JOIN` 类复杂块的处理/原因/结果注释）✔
14. **仓储不使用 `SetContext`**：`git grep -n "SetContext" -- server` 仅命中一处注释说明“不使用”；所有互认方法逐调用显式传 `scope: MutualRecognitionItemScope, sqlId: <StatementId>`（`:96、110、116、122、128`），与 XML 的 `Scope` 与 `Statement Id` 一一对应 ✔
15. **契约校验**：`CreateMutualRecognitionItemRequest` — `StandardProjectCode` `[Required]`、`RecognitionDurationDays` `[Range(1, int.MaxValue)]`；`UpdateMutualRecognitionItemConfigurationRequest` — `Id` `[NonEmpty]`、天数 `[Range(1, int.MaxValue)]`；**均未引入任何长度/格式/正则上限**，符合讨论决策 51“不新增未经确认的长度、格式或正则限制”，且与 `Server/design.md:132`“字符串没有已确认业务长度上限”一致 ✔

### 查询侧（Ticket 04）

16. **扩展既有接口而非新建同义接口**：`IMedicalRecognitionReportQueryAppService` 增加 1 个方法（`git diff` 为 4 行追加），未新建接口；`MedicalRecognitionReportQueryAppService` 同步扩展，`sealed class` 未被复制 ✔
17. **查询语句落在既有查询 SqlMap 且使用既有查询 scope**：新增 `<Statement Id="QueryRecognitionProjectConfigurationList">` 位于 `Repository/Queries/MedicalRecognitionReportQuery.xml`（`Scope="MedicalRecognitionReportQuery"`），未进入实体 SqlMap `MutualRecognitionItem.xml`；仓储侧 `SqlScope = "MedicalRecognitionReportQuery"` 逐调用显式传入 ✔
18. **组织校验**：读 `HttpRequestInfo?.OrgId`，空白即 `InvalidOperationException`；`string.Equals(request.OrganizationCode, currentOrganizationCode, StringComparison.Ordinal)` 不等即拒绝，**不降级为空集合**；校验位于查询端口调用之前（`Stage2QueryTests` 断言 `repository.CallCount == 0`）✔
19. **原因派生**：`ResolveUnavailableReason` 顺序为分类 → 分组 → 标准项目，三条文案严格等于 `所属分类已停用`/`所属分组已停用`/`标准项目已停用`，三层全启用返回 `null`；`ConfigurationStatus` 由配置自身 `IsValid` 换算，**不占用** `UnavailableReason`（`Stage2QueryTests` 覆盖配置自身停用时 `UnavailableReason` 为 `null`）✔
20. **内部投影携带三层状态且不泄漏**：`RecognitionProjectConfigurationListItem` 含 `CategoryIsValid`/`GroupIsValid`/`ItemIsValid`；公共 `RecognitionProjectConfigurationReadModel` 无这三个字段。`Stage2ContractTests.Read_model_matches_the_published_field_shape` 以 `BindingFlags.DeclaredOnly` 冻结 9 个属性名并逐项校验类型与可空性，另断言 `IsStandardCatalogValid`、`IsAvailableForNewMatch`、`CreationTime`、`Creator`、`LastModifiedTime`、`LastModifier` 均不存在 ✔
21. **9 字段、`UnavailableReason` 可空、既有枚举**：`ConfigurationId`/`StandardProjectCode`/`StandardItemName`/`ItemType`/`CategoryName`/`GroupName`/`RecognitionDurationDays`/`ConfigurationStatus`/`UnavailableReason`；`ItemType` 为既有 `MedicalItemType`、`ConfigurationStatus` 为既有枚举；未新增替代枚举 ✔
22. **无写入/事件/消息，未新增筛选或分页**：`MedicalRecognitionReportQueryAppService` 全文件无 `AddEvent`/写仓储调用；请求契约只有 `OrganizationCode`（必填）、`StandardProjectCode`（可选）、`ConfigurationStatus`（可选），`Stage2ContractTests` 冻结字段集合并断言无分页字段 ✔
23. **编码筛选的 LIKE 转义（判定结论：与项目既有写法一致，无注入与转义缺陷）**：`replace(replace(replace($X, '!', '!!'), '%', '!%'), '_', '!_')` 先加倍转义符、再转义 `%`、`_`，`escape '!'` 声明转义符，参数化绑定 `$StandardProjectCode`。顺序正确（先把 `!` 变 `!!` 才能避免二次转义）；`$` 参数由 Earthrace 绑定，不存在字符串拼接注入；该写法与阶段1 既有的 `QueryMedicalStandardItemList`（`:82、88`）和 `QueryEffectiveMedicalStandardCatalog`（`:121`）逐字符一致，属项目既有约定，**不是缺陷**。设计未定义该筛选语义，故按你的要求列为待确认项（见 M1、第三节）。
24. **`ConfigurationStatus = Disabled` 的布尔筛选不会被误判为“未传”**（额外核对，因该分支决定筛选是否生效）：反编译 `Dy.Earthrace 1.0.0.60`（`%TEMP%` 输出，未写入仓库）后确认 `IsNotEmpty.IsCondition → !IsEmpty.IsEmptyValue`（`Dy.Earthrace.decompiled.cs:12423-12429`）；`IsEmptyValue` 的“属性变更跟踪”分支要求上下文 Request 实现 `IPropertyChangedAware`（`:12284-12292`），而查询仓储传入的是匿名类型（未实现该接口），故按值判定；`bool false` 落入兜底 `obj.ToString().Length == 0`（`:12305`）→ `"False".Length == 5` → 非空 → 条件生效。同时 `EnablePropertyChangedTrack` 仅在语句类型为 `GetEntity`/`Update` 且 `Settings.EnablePropertyChangedTrack` 为真时自动开启（`:14685`），`server/Dy.MedicalRecognition/EarthraceConfig.json` 未配置该项。因此 `m.is_valid = $IsValid` 在传 `false` 时正常生效 ✔
25. **`Stage1ArchitectureTests.cs` 的断言修改判定：加强，不是削弱；不影响阶段1 已收口结论**：原断言 `Assert.Equal(4, ...GetMethods().Length)` 只固定数量；新断言固定完整方法名集合（仍为精确序列比较，长度 5）。阶段1 的 4 个查询方法全部保留且被点名，任何新增/改名/删除都会失败；同方法内“写入口契约不得出现 `Query*` 方法”的守卫未改动。数量由 4→5 是阶段2 扩展同一接口的必然结果，属设计要求的“扩展既有查询契约”。`Ticket 04:19` 已如实登记为“加强”，`testReport.md:34` 亦已登记该适配 ✔

### 测试质量与证据

26. **两份测试的用例数与实测一致**：`--filter FullyQualifiedName~Stage2WritePathTests` → 32/32 通过（28 个测试方法，其中 2 个三分支 Theory，共 26 Fact + 6 Theory case）；`--filter FullyQualifiedName~Stage2QueryTests` → 16/16 通过；`Stage2ContractTests` → 5/5 通过；全量 75/75。与 `testReport.md:21-23`、`Ticket 03:19`（“32 用例，含 2 个三分支 Theory”）的声明一致 ✔
27. **断言反映业务语义而非复述实现**：写入侧逐字段核对事件与实体值、以 `ScriptedConfigurationReads`/`ScriptedConfigurationWriteRows` 精确构造 0 行复读三分支与 >1 行分支、以 `Assert.Empty(events.Events)` 覆盖“失败/幂等不登记”、`Assert.Equal(0, repository.EnableCalls)` 覆盖“幂等不触达写方法”；查询侧以 `CallCount == 0` 覆盖“校验先于查询”、以参数化 `[Theory]` 覆盖单层与多层停用优先级、以“逐层恢复”覆盖实时重算 ✔
28. **无空洞占位、无被跳过用例**：两次运行 `已跳过: 0`；未使用 `Skip`；`Assert.True(true)` 类占位不存在 ✔
29. **未用构建成功/静态检查冒充业务通过**：`Ticket 03:18` 明确写出“行为级 RED 未捕获，实际取得的是编译级失败（19 个 `error CS`）”，`testReport.md:37` 单列“RED 边界（如实记录）”；`testReport.md:47-56` 把 V1-V27 保持 `NotRun`/`Blocked`，未把本次单元证据记为用例通过 ✔
30. **测试状态只用 Testing Baseline 六种状态**：`testReport.md` 使用 `NotRun`/`Blocked`/`N/A`/`Passed`，`Ticket` 的 `Status` 明确标注为任务标签而非测试状态（`阶段2-Tickets/README.md:7`）✔
31. **脆弱点已如实记录**：`Stage2WritePathTests.cs:218-235` 说明为何必须反射写入事件队列、`Stage2QueryTests.cs:252-258` 说明为何必须反射写入请求上下文；均未把脆弱手段包装成普通做法 ✔（改进建议仅见 L6）
32. **V11/V22 的“可信组织/操作人”离线面被真实覆盖**：反射断言四个请求类型均无 `OrganizationCode` 属性；`Write_entrypoints_take_organization_from_trusted_context_only` 断言四个入口写入的组织与操作人均取自上下文；`Write_entrypoints_are_rejected_when_trusted_organization_is_missing` 与 `..._operator_cannot_be_resolved` 断言拒绝且零写入零事件 ✔

### 流程闸门

33. **无分支/commit/push**：`git status --short --branch` = `main...origin/main [ahead 2]`，ahead 的 2 个提交为阶段1 既有（18:16、18:31），本轮未新增提交、未创建分支 ✔
34. **未误改 `阶段2审查相关/`**：`git status` 全程无该目录条目；`阶段2审查相关/` 5 个文件时间戳停留在 09-14/09-15 16:08 ✔
35. **未回退/覆盖非本轮内容**：逐文件 `git diff` 复核（Manager/AppService/Repository/Command/Event/两个 XML/契约/DDL/Stage1ArchitectureTests），改动均为追加或定向修正，未见删除他人内容；唯一的删除是 `Application.Contracts/ReadModels/RecognitionProjectConfigurationReadModel.cs` → 迁入 `Application.Contracts/Queries/`，`git grep` 确认无残留引用 ✔
36. **无临时文件或生成物残留**：`git status --porcelain -uall` 的未跟踪项（复核时为 18 条，含本报告本身）全部是业务源码（`recognitionProjects/*`、API Client 生成分片、4 个阶段2 新增类型、3 个测试文件）、测试与阶段文档；无 `bin/obj`、无反编译产物、无 `~$` 临时文件（`ilspycmd` 输出在 `%TEMP%`）✔
37. **无伪造的 `Passed`/`Complete`/`Ready`**：`testReport.md` 仅在 C22/C24 使用 `Passed`（属 Ticket 06 API Client 批次，附有实际命令，超出本次复核范围，我未复跑）；业务用例 0 条 `Passed`；`Server/impl.md` 的 B2/B3 状态仍为“进行中”，未提前宣告完成（`Server/impl.md` 最后写入 21:04:03，早于最后的代码写入 21:04:36，属如实反映）✔

---

## 三、待确认或未验证

1. **编码筛选语义（字面包含）**：设计未定义；实现与阶段1 既有查询一致（见第 23 项），故按既有约定判定为**无缺陷**，但需先确认“字面包含”是期望语义。若确认为字面包含，只需修正 M1 的契约注释；若应为精确匹配，需改 SQL 与测试期望。
2. **`concat()` 与 `escape '!'` 的 Provider 可移植性**：二者均与阶段1 既有语句一致，项目当前仅 PostgreSQL（`EarthraceConfig.json` 的 `DbProvider.Name = "PostgreSql"`，`Dy.Earthrace.Oracle` 包未被引用）。`escape` 在 MySQL 需用 `\`、Oracle 的 `CONCAT` 仅接受 2 参，因此该写法只对 PostgreSQL 成立。**未验证**其他 Provider；若将来扩展数据库需重新设计。
3. **INNER JOIN 使“目录资料缺失”的配置从列表静默消失**：`QueryRecognitionProjectConfigurationList` 以 `join mrec_medical_standard_item / mrec_medical_standard_group / mrec_medical_standard_category` 内联三层且附加 `c.id = g.category_id`。由于 `standard_item_id` 无外键（S2-D5），若数据库中标准项目/分组/分类记录缺失或跨分类不一致，该配置会从列表结果中整行消失（而非返回空名称）。`Server/design.md:138` 明确“目录资料缺失不是正常业务可达状态，不建立测试夹具或直接写库构造该数据”，因此**未验证**，登记为残余风险而非缺陷。
4. **V4 的部分分支无独立证据**：设计 V4 列“时间为空、0、负数、小数、永久有效”。已有证据覆盖 0、负数、编码为空（`Stage2WritePathTests.cs:331-347`）；`小数` 与 `永久有效` 在 `int` 契约下无法表示（小数在请求绑定阶段即失败、永久有效无对应取值），因此**未单独取证**，属契约层的结构性拒绝而非遗漏。建议在 `testReport.md` 的 V4 行注明这两分支不可离线构造，避免“已覆盖”被读成全部分支已验证。
5. **C22 / C24 的 `Passed` 未由我复跑**：二者属 Ticket 06 API Client 批次（`testReport.md:29、53、55`），依赖后端 OpenAPI 实测与 pnpm 生成链路，超出本次复核范围，**未验证**。
6. **V23 运行面、V2/V15 真实唯一约束、V26 同步屏障并发、V22 真实 token、V24 运行面**：**未验证**，均由目标表未建 + 数据库只读 + 宿主菜单未配置阻断，留 Ticket 08（见 `testReport.md:39、49、51`）。
7. **GitNexus `detect-changes` 的 `high` 未由我独立归属**：我实测得到“变更：27 个文件，97 个符号 / 受影响流程：13 / 风险等级：high”，`testReport.md:31` 已登记并说明“工作区含多批次并发改动，聚合风险 high；逐项归属复核后均属阶段2 已批准票”。13 条受影响流程全部是 `Create/Update/Enable/DisableMutualRecognitionItemAsync → 对应 Command/Event` 的互认链路（属预期传播），但我无法把 97 个符号逐一归属到本批（工作区同时含 client/、Ticket 01/02/05/06 的改动），故该归属结论**未独立验证**。
8. **`git worktree list` 存在额外工作树**：`E:/MedSync/MedicalRecognition.worktrees/request-234-branch`（分支 `agents/request-234-branch`，提交 `5521cb6`）。它与 Ticket 03/04 无关（分支名指向另一请求），我未发现本批触碰它的证据，故只登记不判责。
9. **并发环境错误**：本轮未遇到文件锁/访问被拒类失败；构建与测试仅出现 5 条 git 换行（LF→CRLF）提示类警告，无编译警告。

---

## 四、实测命令与结果

### 1. 构建

```
dotnet build server/Dy.MedicalRecognition.slnx --no-restore
```

结果：**0 个错误**，5 个警告（全部为 MSBuild 目标的 `EXEC : warning : in the working copy of '.../Server/design.md', LF will be replaced by CRLF ...` 换行提示，非代码警告），`已用时间 00:00:01.23`。

### 2. 全量测试

```
dotnet test server/Dy.MedicalRecognition.Tests/Dy.MedicalRecognition.Tests.csproj --no-restore
```

结果：`已通过! - 失败: 0，通过: 75，已跳过: 0，总计: 75，持续时间: 204 ms`（与主线程复跑一致）。

### 3. 分文件计数

```
dotnet test ... --filter "FullyQualifiedName~Stage2WritePathTests"   → 通过 32 / 总计 32
dotnet test ... --filter "FullyQualifiedName~Stage2QueryTests"       → 通过 16 / 总计 16
dotnet test ... --filter "FullyQualifiedName~Stage2ContractTests"    → 通过  5 / 总计  5
```

### 4. 重复运行（并行静态状态风险探测）

连续 8 次全量运行：`run 1..8: 已通过! - 失败: 0，通过: 75，总计: 75`（181–206 ms）。未复现偶发失败（见 L5）。

### 5. `git diff --check`

退出码 **0**，无空白错误（stderr 仅有 `LF will be replaced by CRLF` 换行提示类警告，随工作区文件数变化）。

### 6. 数据库只读核对（dbx 连接名 `协同平台外网`，仅 `select`）

```sql
select table_name from information_schema.tables
where table_schema='public' and table_name like 'mrec_%' order by table_name
```

实际结果：仅 3 行 —— `mrec_medical_standard_category`、`mrec_medical_standard_group`、`mrec_medical_standard_item`。**`mrec_mutual_recognition_item` 不存在**（3 rows, 19ms），与 `testReport.md:30` 的记录一致，故依赖该表的用例保持 `Blocked` 有实际依据。

### 7. SqlMap 注册键实测原文（V23 静态/注册面证据）

命令：

```
dotnet test server/Dy.MedicalRecognition.Tests/Dy.MedicalRecognition.Tests.csproj --no-restore --no-build \
  --filter "FullyQualifiedName~Runtime_registration_reports_full_sql_ids_without_opening_database" \
  -l "console;verbosity=detailed"
```

结果：`已通过 Dy.MedicalRecognition.Tests.Stage1SqlMapProbeTests.Runtime_registration_reports_full_sql_ids_without_opening_database`（测试总数 1 / 通过数 1）。控制台原文摘录（完整，逐行照录）：

```
FullSqlId lookup: MedicalRecognitionReport.CreateMedicalStandardCategory => not registered
FullSqlId lookup: MedicalStandardCategory.CreateMedicalStandardCategory => registered
Registered Statement keys:
MedicalRecognitionReport.QueryAllRecognitionReference
MedicalRecognitionReport.RecognitionReferenceColumns
MedicalRecognitionReportQuery.QueryEffectiveMedicalStandardCatalog
MedicalRecognitionReportQuery.QueryMedicalStandardCategoryList
MedicalRecognitionReportQuery.QueryMedicalStandardGroupList
MedicalRecognitionReportQuery.QueryMedicalStandardItemList
MedicalRecognitionReportQuery.QueryRecognitionProjectConfigurationList
MedicalStandardCategory.CreateMedicalStandardCategory
MedicalStandardCategory.DisableMedicalStandardCategory
MedicalStandardCategory.EnableMedicalStandardCategory
MedicalStandardCategory.GetMedicalStandardCategoryById
MedicalStandardCategory.MedicalStandardCategoryColumns
MedicalStandardCategory.MedicalStandardCategoryHasGroups
MedicalStandardCategory.MedicalStandardCategoryNameExists
MedicalStandardCategory.QueryAllMedicalStandardCategory
MedicalStandardCategory.UpdateMedicalStandardCategory
MedicalStandardGroup.CreateMedicalStandardGroup
MedicalStandardGroup.DisableMedicalStandardGroup
MedicalStandardGroup.EnableMedicalStandardGroup
MedicalStandardGroup.GetMedicalStandardGroupById
MedicalStandardGroup.MedicalStandardGroupColumns
MedicalStandardGroup.MedicalStandardGroupNameExists
MedicalStandardGroup.QueryAllMedicalStandardGroup
MedicalStandardGroup.UpdateMedicalStandardGroup
MedicalStandardItem.ChangeMedicalStandardItemRemark
MedicalStandardItem.CreateMedicalStandardItem
MedicalStandardItem.DisableMedicalStandardItem
MedicalStandardItem.EnableMedicalStandardItem
MedicalStandardItem.GetMedicalStandardItemByCode
MedicalStandardItem.GetMedicalStandardItemById
MedicalStandardItem.MedicalStandardItemCodeExists
MedicalStandardItem.MedicalStandardItemColumns
MedicalStandardItem.QueryAllMedicalStandardItem
MutualRecognitionItem.CreateMutualRecognitionItem
MutualRecognitionItem.DisableMutualRecognitionItem
MutualRecognitionItem.EnableMutualRecognitionItem
MutualRecognitionItem.GetMutualRecognitionItemById
MutualRecognitionItem.MutualRecognitionItemColumns
MutualRecognitionItem.QueryAllMutualRecognitionItem
MutualRecognitionItem.UpdateMutualRecognitionItemConfiguration
```

与本次复核相关的键（原文存在，逐字校对）：

| 期望键 | 是否出现在实测清单 |
|---|---|
| `MutualRecognitionItem.CreateMutualRecognitionItem` | 是 |
| `MutualRecognitionItem.UpdateMutualRecognitionItemConfiguration` | 是 |
| `MutualRecognitionItem.EnableMutualRecognitionItem` | 是 |
| `MutualRecognitionItem.DisableMutualRecognitionItem` | 是 |
| `MutualRecognitionItem.GetMutualRecognitionItemById` | 是 |
| `MutualRecognitionItem.MutualRecognitionItemColumns` | 是 |
| `MutualRecognitionItem.QueryAllMutualRecognitionItem` | 是（共 7 条 `MutualRecognitionItem.*`） |
| `MedicalRecognitionReportQuery.QueryRecognitionProjectConfigurationList` | 是 |
| `MedicalStandardItem.GetMedicalStandardItemByCode`（本批新增语句） | 是 |
| `MedicalRecognitionReport.CreateMutualRecognitionItem`（旧聚合键，应为不存在） | 否（清单中无该键） |

即：`Server/design.md:136`、S2-D11 要求的“实际查找键与注册键一致”在注册面上成立，且互认语句的作用域已由旧的 `MedicalRecognitionReport` 迁至 `MutualRecognitionItem`。**该结论的证据形式是控制台打印（断言未覆盖互认键），见 M2。**

### 8. GitNexus（只读）

```
gitnexus detect-changes --repo Dy.MedicalRecognition
```

结果：`变更：27 个文件，97 个符号 / 受影响流程：13 / 风险等级：high`；13 条受影响流程全部为 `Create/Update/Enable/DisableMutualRecognitionItemAsync → CreateMutualRecognitionItem*Event` 互认链路。工作区同时含其他批次改动，聚合值不可全部归因本批（见第三节第 7 项）。

### 9. 框架只读反编译（用于核对布尔筛选判定）

```
ilspycmd C:\Users\test\.nuget\packages\dy.earthrace\1.0.0.60\lib\net10.0\Dy.Earthrace.dll -o %TEMP%\dsh-earthrace-decomp
```

输出落在 `%TEMP%`，未写入仓库；结论见第 24 项。

---

## 五、结论

- 未发现 `Blocker` / `High`。Ticket 03、Ticket 04 的写入侧与查询侧实现在**设计与规范要求的可离线验证范围内**逐项核对通过，S2-D6 单写语句/无 WorkUnit、S2-D11 scope/无 SetContext、S2-D14 原因优先级、S2-D8 不泄漏派生状态、讨论决策 51 不新增长度限制等硬约束均有实际代码或实际命令证据支撑。
- 需主线程优先处理的两项：**M2**（把 V23 的互认/查询注册键写成断言，约 10 行，消除“打印即证据”的不可回归点）与 **M1**（修正查询请求“精确匹配”的错误契约注释，并把编码匹配语义列入待确认项）。
- 其余 6 项为 `Low`：新增异常未登记进设计类型归属表、启停请求缺 `[NonEmpty]` 与类型级文档、两处弱断言、DDL 去掉 `if not exists` 未登记、测试工程未禁用并行集合（未复现）、查询测试未复用公开上下文工厂。
- 后续交付不因本报告阻塞；上述 `Medium` 项建议在 Ticket 08 真实验证前闭合，`Low` 项可在同一轮一并处理。
