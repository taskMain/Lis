# 阶段 2 后端实施

## 维护边界

本文件记录后续实施批次和验证编号；详细前置数据与预期见 [Server/design.md](design.md) 的验证矩阵。业务代码在设计通过并获得实施授权后修改；建表由负责人（迁移执行方）执行，实施侧只交付最终 DDL，不自行操作数据库。

后续按项目既有代码与生成物维护规则实施；相应SRS/UML契约由设计主责同步。

## 批次与依赖

以下文件范围均相对 `server/`，只修改所属类型/语句的阶段2内容，不重写整份共享文件。主责为后续交接角色，每批仅一个主责。

| 批次 | 主责 | 文件范围与交付 | 前置条件 | 矩阵 | 当前状态 |
|---|---|---|---|---|---|
| B1 取证 | 后端架构主责 | 核对四命令/事件、AppService、Manager、仓储及XML；组织来源/选择传递、scope键清单、无WorkUnit生产入口事件机制 | 当前设计及SRS/UML确认；不以替代入口证明生产路径 | V11、V16、V21-V24的取证面 | 已完成：组织编码来源（token `org` → `HttpRequestInfo.OrgId`）与传递链、无 `[WorkUnit]` 入口的 Flush 主体/时点/失败语义均 `SourceConfirmed`，见 [取证-组织与事件机制](取证-组织与事件机制.md)；V24 运行面保持 `Blocked` |
| B2 写 | 后端实现主责 | `Dy.MedicalRecognition.Application.Contracts/MedicalRecognitionReportAggregate/Requests/*MutualRecognition*.cs`及既有写接口；Application同聚合AppService/DataMaps；Domain同聚合四Command、MutualRecognitionItem、Manager、仓储接口；Domain.Share同聚合四Event；Repository同聚合仓储实现和`MutualRecognitionItem.xml`，必要的`MedicalStandardItem.xml`编码读取语句 | B1确认生成保护和实际机制；设计确认、symbol影响分析及准确RED；现有目录仓储复用 | V1-V9、V11、V15-V17、V21-V22、V26-V27；真实事件另见V24；V10为N/A | 已完成（离线面）：Ticket 02 契约基线与 Ticket 03 四个写入口（含事件字段、条件更新、幂等与 0 行复读、唯一冲突翻译、单写语句且无 `[WorkUnit]`）交付，`Stage2WritePathTests.cs` 32 用例全绿，V21 完整契约面随之关闭；需库用例由 B5 执行。**RED 边界**：本轮为编译级真实失败，行为级 RED 因并发编辑未捕获 |
| B3 查询 | 后端查询主责 | Contracts/Queries既有接口、新建`RecognitionProjectConfigurationListQueryRequest.cs`及迁入的`RecognitionProjectConfigurationReadModel.cs`；Application/Queries既有AppService；Domain/Queries既有仓储接口及新建`RecognitionProjectConfigurationListItem.cs`；Repository/Queries既有仓储及XML | B1确认契约、scope；S2-D12 已裁定为"只信可信当前组织"；准确RED；复用既有查询接口 | V12-V14、V19-V23 | 已完成（离线面）：查询契约、内部投影、查询端口、查询 SQL（独立查询 scope）与应用层原因派生交付，`Stage2QueryTests.cs` 16 用例全绿；V12/V14 的 SQL 面与 V23 运行面由 B5 执行 |
| B4 DDL | 数据库设计主责 | `Dy.MedicalRecognition.Repository/Scripts/mutual_recognition_item.sql`及所属阶段迁移交付；核对B2/B3 XML映射，不并行改其主责文件 | B1文件保护取证；设计明确逐列类型、无DB默认值、列序、中文注释和短唯一索引；无需等待组织服务 | V17-V18、V23、V25静态面 | 已完成（静态面）：脚本交付并逐项复核，写入侧映射经 B2 交付后复核通过（实体 scope、仅访问 `mrec_` 表、显式 `scope/sqlId`、无 `SetContext`）；等待建表完成后由 B5 归档数据库证据 |
| B5 真实验证 | 后端测试主责 | `Dy.MedicalRecognition.Tests/`精确测试、实际Host入口/数据库/事件证据；交阶段根`testReport.md`主责归档 | B2/B3/B4对应实现完成；阶段1 证据与建表完成确认；可经业务接口/页面造数 | V1-V27全部适用验证面，重点V24、V26-V27及DB面 | 已完成：真实库面和真实入口面均已执行，逐条结果、受阻项与登记口径见阶段 `testReport.md`（该报告的"结果"节按矩阵编号逐条列出，本表不重复统计） |
| B6 审查整改 | 后端实现主责（批次A）+ 主线程交叉审查 | 启停/创建请求的属性约束；新增 `Application/TrustedOrganizationResolver.cs` 并接入两个 AppService；`Repository/Queries/MedicalRecognitionReportQueryRepository.cs` 的枚举→布尔纯映射；`Domain` 的死赋值与无用构造清理；`Tests/` 的旧键断言、公开工厂与源码探针 | 独立审查 finding（整改范围为 A、B 两组） | 与 B2/B3/B5 相同编号（整改不新增 V 编号） | 已完成（2026-09-16）：8 项整改落地，门禁数字与用例计数见阶段 `testReport.md` 的对应轮次小节（终值见其"门禁终值"节）；受影响真实运行面（V2/V13/V15/V19/V22/V24）已按 Testing Baseline §5 在宿主重取，见阶段 `testReport.md` 的"批次A/B 整改后的宿主重取（2026-09-16）" |
| B7 整改后复审与收口前清理 | 主线程 | 查询侧组织比较与下传（请求侧同裁剪）；启停请求类型级 XML；查询接口 summary；`Server/design.md` 类型归属表补登 `TrustedOrganizationResolver`；两启停请求与创建语句相关的判定按项目约定保留 | 批次A/B 完成 | 与 B2/B3/B5/B6 相同编号 | 已完成（2026-09-16）：门禁数字与用例计数见阶段 `testReport.md` 的对应轮次小节（终值见其"门禁终值"节）；`Stage1SqlMapProbeTests` 补 `$IsValid` 谓词引用断言、`Stage2QueryTests` 补"两行结果仍只查询一次"的调用次数断言（对齐 `backend-architecture.md` §5.3 验证要求）；真实宿主复验列表查询与越权拒绝均通过。**注意**：创建路径的 `configuration.IsValid = true` 按项目既有约定保留（落库启用状态由新增语句写常量，`MutualRecognitionItem.xml` 注释已说明），不按该条 finding 改 SQL |
| B8 第三轮复审后的修复（批次C/D/E） | 主线程 | `Repository/Queries/MedicalRecognitionReportQueryRepository.cs`（`<remarks>` 合并、注释按事实改写）；`Application/Queries/MedicalRecognitionReportQueryAppService.cs`（去冗余 `?.`）；`Tests/Stage2QueryTests.cs`、`Tests/Stage2WritePathTests.cs` | 第三轮复审完成（整改范围为批次C+D+E） | 与 B2/B3/B5/B6/B7 相同编号（修复不新增 V 编号） | 已完成（2026-09-16）：合并同一成员的两个 `<remarks>` 并置于 `<param>`/`<returns>` 之前；删除紧跟 `Assert.Single` 的恒真 `Assert.NotNull`（改为断言返回项编码）；补 `Assert.Contains("true", statements["CreateMutualRecognitionItem"])`——修复前把该语句的常量 `true` 改成 `false` 全量测试仍全绿；"新增配置状态时编译期即暴露"改为与事实一致的表述（无 `TreatWarningsAsErrors`，只产生 CS8509 告警，真正兜底是运行期 `SwitchExpressionException`）。**登记不改**：事件观察的反射写入、`MapConfigurationStatusToIsValid` 的 `public static` 可见性、筛选值属性名与投影列近义、`StandardProjectCode` 不 Trim（阶段1 约定）、互认语句 `$IsValid=false` 的真库渲染（页面不下推该筛选，当前不可达）。门禁数字与用例计数见阶段 `testReport.md` 的对应轮次小节（终值见其"门禁终值"节）；查询应用层改动已按 Testing Baseline §5 在真实宿主重取（列表查询 `200` + 越权组织 `500`），见阶段 `testReport.md` 的"第三轮复审后的修复（批次C/D/E，2026-09-16）" |
| B9 枚举契约缺陷修复（跨阶段立项） | 主线程 | 新增 `Application/Queries/EnumMetadata/MedicalRecognitionEnumDescriptorRegistry.cs`、`Application/MedicalRecognitionEnumOpenApiDocumentTransformer.cs`；`MedicalRecognitionApplicationModule.cs` 注册转换器；`Domain.Share/Enums/ConfigurationStatus.cs`、`MedicalItemType.cs` 补 `[EnumDescriptor]`/`[Description]`；`Tests/Stage2EnumContractTests.cs`（新增 8 条） | 口径参照同组织 `Dy.LisCenter` | 与 B2/B3/B5/B6/B7/B8 相同编号（契约修复不新增 V 编号；V21 契约面加强） | 已完成（2026-09-16）：真实 `/openapi/v1.json` 实测 `ConfigurationStatus` = `{"enum":[1,2],"type":"integer","x-enumNames":["Enabled","Disabled"],"x-enumDescriptions":["启用","停用"]}`、`MedicalItemType` 同构；`internal` 的注册表与转换器通过新增 `InternalsVisibleTo(Dy.MedicalRecognition.Tests)` 直接验收；新增 `Microsoft.AspNetCore.OpenApi` 包引用（10.0.9，与既有解析版本一致）。门禁数字与新增用例计数见阶段 `testReport.md` 的对应轮次小节（终值见其"门禁终值"节）。详见阶段 `testReport.md` 的"枚举契约缺陷修复（跨阶段立项，2026-09-16）" |
| B10 枚举中文来源（LisCenter 同口径） | 主线程 | 新增 `Application.Contracts/Queries/EnumMetadata/EnumDescriptorText.cs`、`EnumMetadataItemDto.cs`、`QueryEnumMetadataRequest.cs`、`IEnumMetadataAppService.cs`；`Application/Queries/EnumMetadata/EnumMetadataAppService.cs`；`Application.Contracts/Queries/RecognitionProjectConfigurationReadModel.cs` 补 `ItemTypeText`/`ConfigurationStatusText` 计算属性；`Application/Queries/EnumMetadata/MedicalRecognitionEnumDescriptorRegistry.cs` 注释补元数据查询用途；`Tests/Stage2EnumMetadataQueryTests.cs`（新增 11 条）、`Tests/Stage2ContractTests.cs`（读模型字段集期望随之更新） | 口径参照 `Dy.LisCenter` 的 `EnumMetadataAppService` 与 `EnumDescriptorText` | 与 B2/B3/B5/B6/B7/B9 相同编号（中文来源属契约交付方式，不新增 V 编号；V21 契约面加强） | 已完成（2026-09-16）：`EnumMetadataAppService` 复用静态登记注册表按名称返回只读 `{value,name,description}`，未登记/大小写不符/空白一律 `ValidationException`（不降级为空集合），返回值非数组且不可变；只读模型中文由 `EnumDescriptorText` 解析——`ConfigurationStatus` 由布尔派生、集合封闭用**严格解析**，`MedicalItemType` 取自无 `CHECK` 约束的 `item_type` 列用**安全解析**（未登记返回 `null`，避免一条越界数据让整表查询失败，与页面"未知取值安全展示"既有约定一致）。门禁数字与新增用例计数见阶段 `testReport.md` 的对应轮次小节（终值见其"门禁终值"节）；宿主实测 `POST /Api/EnumMetadata/GetEnumMetadata` → `200` + `[{"value":1,"name":"Enabled","description":"启用"},{"value":2,"name":"Disabled","description":"停用"}]`，列表查询响应含 `itemTypeText`/`configurationStatusText`。详见阶段 `testReport.md` 的"枚举中文来源（LisCenter 同口径，2026-09-16）" |
| B11 增量复审整改（第 1 轮，后端侧） | 主线程 | `Tests/Stage2EnumContractTests.cs`（模块注册守卫 + 4 条变体自校验、冻结文档路径清单与全局归一守卫、精确回退类型断言）、`Tests/Stage2EnumMetadataQueryTests.cs`（React 层无关；注册表消费变体自校验、删同源重言断言、URI 模板与出口签名断言、两条中文通道交叉断言、阶段2 契约枚举登记双向断言）、`Application.Contracts/Queries/**` 与 `Application/Queries/**` 的 XML 文档与诊断文案（`SingleOrDefault`→`FirstOrDefault`、错误消息带枚举名、补 `param`/`returns`/`exception`）、注册表措辞收窄 | 增量复审完成；逐条复核成立性 | 与 B2/B3/B5/B6/B7/B9/B10 相同编号（复审整改不新增 V 编号） | 已完成（2026-09-16）：整改 5 条 High/Medium 与 8 条 Low 中属于后端的部分；**误报 2 条**（`_itemTypeMember1` 断言恒真的说法不成立——实测当前生成物 `Member1` 出现 0 次；"null 请求未覆盖"不成立——用例第 68 行已断言 `ArgumentNullException`）。门禁数字与用例计数见阶段 `testReport.md` 的对应轮次小节（终值见其"门禁终值"节）。详见阶段 `testReport.md` 的"增量复审与整改（2026-09-16）" |
| B12 增量复审整改（第 2 轮，后端侧） | 主线程 | `Tests/Stage2EnumContractTests.cs`（注册谓词放宽为三子串并登记启发式边界）、`Tests/Stage2EnumMetadataQueryTests.cs`（反射判据改调用形状、排除清单互斥断言、嵌套公开类型、`[Description]` 正则与成员数锚点、新增注释变体）、`Application/Queries/EnumMetadata/MedicalRecognitionEnumDescriptorRegistry.cs`（生产注释只留登记义务） | 增量复审完成；逐条复核成立性 | 与 B2/B3/B5/B6/B7/B9/B10/B11 相同编号（复审整改不新增 V 编号） | 已完成（2026-09-16）：整改后端侧的守卫强度与生产注释问题；**误报与边界如实登记**（启发式判据不覆盖别名 using、条件编译与未调用方法）。门禁数字与用例计数见阶段 `testReport.md` 的对应轮次小节（终值见其"门禁终值"节） |
| B13 增量复审整改（第 3 轮，后端侧） | 主线程 | `Tests/Stage2EnumMetadataQueryTests.cs`（字典正则放宽 + 4 条变体、`[Description]` 正则与注释剥离边界登记）、`Tests/Stage2EnumContractTests.cs`（注册判据上限登记） | 增量复审完成；逐条复核成立性 | 与 B2/B3/B5/B6/B7/B9/B10/B11/B12 相同编号（复审整改不新增 V 编号） | 已完成（2026-09-16）：整改后端侧判据强度与文档口径；**误报如实登记**（互斥断言并非恒真——集合模拟显示它是唯一能挡"排除清单侵占本阶段枚举"的断言）。门禁数字与用例计数见阶段 `testReport.md` 的对应轮次小节（终值见其"门禁终值"节） |
| B14 源码守卫对齐（仅测试侧） | 主线程 | 新增 `Tests/Architecture/SourceSyntaxGuard.cs`（共享语法工具）、`Tests/Architecture/SourceGuardReuseTests.cs`（复用治理）；`Tests/Stage2EnumContractTests.cs`（注册判据锚定 `OnPostConfigureServices` 方法体）、`Tests/Stage2EnumMetadataQueryTests.cs`（注册表消费判据与枚举契约判据改语法节点）、`Tests/Stage1ArchitectureTests.cs` 与 `Tests/Stage2WritePathTests.cs`（改用共享仓库根定位）；`server/Directory.Packages.props` 与 `Dy.MedicalRecognition.Tests.csproj` 新增 `Microsoft.CodeAnalysis.CSharp` 4.11.0（`PrivateAssets="all"`） | 判据写法对齐同组织 `Dy.LisCenter` 的 `Architecture` 测试做法 | 与 B2/B3/B5/B6/B7/B8/B9/B10/B11/B12/B13 相同编号（测试写法整改不新增 V 编号） | 已完成（2026-09-16）：判据不再用文本或正则匹配源码——注释与字符串字面量不是语法节点，限定名与可空写法由节点给出，注册语句锚定在 `OnPostConfigureServices` 方法体内；新增覆盖注释掉、写进字符串、写在别的方法里、`#if false`、静态限定名调用、`ConfigureAll<OtherOptions>`、`Type?`/`System.String`/全限定/`IReadOnlyDictionary`、别名 using 的源码变体，并新增复用治理用例与共享工具消费方清单断言。**本轮新变体当场报出两处自身缺陷并已修**：全限定泛型 `Dictionary<string, Type>` 漏判（补 `TypeArgumentList` 解包）、复用治理自指（删冗余字面量规则并对共享工具豁免）。去重：删除 4 份 `FindRepositoryRoot` 与 2 份 `StripComments`。门禁数字与用例计数见阶段 `testReport.md` 的对应轮次小节（终值见其"门禁终值"节）；未改生产代码，宿主证据无需重取。详见阶段 `testReport.md` 的"源码守卫对齐（2026-09-16，收口后仅测试侧改动）" |
| B15 复审后整改（后端侧：注释与风格、测试缺口） | 后端实现主责（注释与风格）+ 主线程 | `Application`、`Domain`、`Domain.Share`、`Repository` 的既有成员（注释、`using` 顺序、私有字段改名、`if` 花括号）；`Dy.MedicalRecognition.Tests/**`（测试缺口与判据） | 复审完成；逐条复核成立性 | 与 B2/B3/B5/B6/B7/B8/B9/B10/B11/B12/B13/B14 相同编号（复审整改不新增 V 编号；不改矩阵用例定义与编号） | 已完成（2026-09-16）：**未改生产行为**（只做注释、`using` 顺序、私有字段改名与 `if` 花括号），测试侧补本轮复审指出的缺口。门禁数字与用例计数见阶段 `testReport.md` 的对应轮次小节（终值见其"门禁终值"节）；未改前端与客户端包，宿主证据无需重取。详见阶段 `testReport.md` 的"复审后整改（2026-09-16，批次16 / B15 / F12）" |
| B16 第二批整改（写入口契约口径、测试守卫与运行配置） | 主线程 | `Application.Contracts/MedicalRecognitionReportAggregate/IMedicalRecognitionReportAppService.cs`（四个互认写入口的 XML 返回/异常口径按终态补齐）；`Application/MedicalRecognitionReportAggregate/MedicalRecognitionReportAppService.cs`（私有组织解析包装按共享解析点 `TrustedOrganizationResolver` 内联，等价重构）；新增 `Tests/Stage1ArchitectureTests.cs` 的 Provider 中立静态守卫用例（含扫描范围判据、禁词表与两条自校验）；`Tests/SmokeTests.cs` 的契约断言按事实替换；新增 `Tests/xunit.runner.json` 关闭程序集与集合并行 | 复审登记的"契约断言恒真"与"测试工程未禁用并行集合"两项；逐条复核成立性 | 与 B1-B15 相同编号（契约注释、等价重构与测试侧判据整改不新增 V 编号；不改矩阵用例定义与编号） | 已完成（2026-09-16）：①新增 `Public_types_and_shared_sql_stay_database_provider_neutral`——扫描范围为 `server` 下五个受治理工程（公共契约、应用、领域、领域共享、仓储）的手写 `.cs`，以及 `Repository` 下的 SqlMap `.xml` 与建表 `.sql`；排除 `bin`、`obj` 与 `*.g.cs`、`*.Designer.cs`；Provider 类型名与 SQL 关键字按**不区分大小写的整词匹配**（词边界使 C# 的 `DateTimeOffset` 不会命中方言关键字 `offset`），方言符号 `::`、`/*+`、`count(distinct (` 按原文匹配；`Repository/EarthraceConfig.json` 明确不在范围内，理由是**该文件的作用正是选择当前 Provider 与连接串**，不属于被禁的公共类型与共享语句；当前实测 **0 违规**，并含受治理文件数下界断言与两条自校验（含具体 Provider 类型与各方言特征的合成源码必须报出违规、只含 `DateTimeOffset`/`int.MaxValue`/`JsonSerializerOptions` 的中立写法必须不报）。②`SmokeTests.Generated_contract_is_available` 删除恒真的 `typeof(...) != null`，改为三条可判别判据：契约类型由契约程序集声明且不是测试程序集里的同名替身、四个互认写入口各自恰好一处声明、返回类型为 `Task<bool>`。③新增 `Tests/xunit.runner.json` 关闭并行（`parallelizeAssembly` 与 `parallelizeTestCollections` 均为 `false`）：依据是源码静态判据跨文件读取源码，且测试进程内有共享注册状态（`TypeHandlerFactory.Register`、共享 SqlMap 注册表），并行集合之间共享进程状态构成非确定性来源；**实测代价**为同一命令重复三次报告 768/782/790 ms（关闭并行）与 281/287/285 ms（开启并行），差约 0.5 秒，按可接受处置。门禁数字与用例计数见阶段 `testReport.md` 的对应轮次小节（终值见其"门禁终值"节）；本批不改变生产行为（契约改动为写入口 XML 文档，应用层改动为按共享解析点内联私有包装的等价重构），故未重取宿主证据；**该批确实改动了生产源文件，提交前仍须按 [gitnexus-workflow](../../../../.agents/instructions/gitnexus-workflow.md) §1 第 5 条（提交是独立闸门）执行 `detect_changes()`**。四个互认写入口的返回与异常口径按终态登记在 [design.md](design.md)。详见阶段 `testReport.md` 的"第二批整改（2026-09-16，批次17 / B16 / F13）" |
| B17 第三批整改（契约异常声明、防御分支与守卫覆盖） | 主线程 | `Application.Contracts/MedicalRecognitionReportAggregate/IMedicalRecognitionReportAppService.cs`（四个互认写入口补 4 条 `ArgumentNullException` 声明）；`Domain/MedicalRecognitionReportAggregate/MedicalRecognitionReportManager.cs`（两个启停入口的"条件更新影响多行"登记为防御分支、物理不可达，行数口径注释统一）；`Tests/Stage1ArchitectureTests.cs`（`Public_types_and_shared_sql_stay_database_provider_neutral` 的自校验样本扩到全部 29 条判据，remarks 登记不覆盖 `Repository/EarthraceConfig.json` 的原因；新增 `Test_runner_disables_assembly_and_collection_parallelization`）；既有测试运行配置 `Tests/xunit.runner.json` 与其复制指令 | 第二批整改完成后的第三批整改；逐条复核成立性 | 与 B1-B16 相同编号（契约注释、防御分支登记与测试侧判据整改不新增 V 编号；不改矩阵用例定义与编号） | 已完成（2026-09-16）：①四个互认写入口的 XML 文档按事实补 4 条 `ArgumentNullException`（请求为 `null` 时请求校验先于一切判定，不进入领域调用、不写入数据）；②两个启停入口的"条件更新影响多行"按代码事实登记为**防御分支、物理不可达**（更新语句按主键等值与可信组织编码等值定位，最多命中一行），行数口径统一为 1 行成功并登记事件、0 行进入同一组织的复读、其他取值为防御分支异常；③Provider 中立守卫的自校验样本覆盖**全部 29 条判据**并按特征名逐条断言，同时断言判据条数等于命中条数、特征名两两不同；④新增 `Tests/xunit.runner.json` 的静态守卫用例，按 JSON 键断言 `parallelizeAssembly` 与 `parallelizeTestCollections` 均为 `false`。门禁数字与用例计数见阶段 `testReport.md` 的对应轮次小节（终值见其"门禁终值"节）；本批未重取宿主证据（改动为契约注释、守卫测试与测试运行器配置，不改变生产行为），但确实改动了生产源文件，提交前仍须执行 `detect_changes()`。详见阶段 `testReport.md` 的"第三批整改（2026-09-16，批次18 / B17 / F14）" |
| B18 宿主验收（第三批整改后的行为面） | 测试主责 | 本轮无后端代码改动与交付物；宿主链路验收证据（入口与开发地址拦截、首次加载与组织编码、修改可互认时间、启停、筛选口径、读取失败门控与重试）由阶段 `testReport.md` 归档 | 批次B17 完成；真实后端与宿主链路、前端 dev server 均已具备 | 沿用 V1-V27 / C1-C25，不新增编号 | 已执行（宿主验收，详见阶段 testReport.md 对应小节） |

顺序为B1取证后按依赖推进B2写、B3查询、B4DDL，最后B5真实验证；独立文件和不依赖Pending的工作可并行，共享Repository/XML交接串行。B1不因某项Blocked暂停全部取证；B4交付完成后暂停依赖新表的集成测试，收到建表完成确认才恢复。

## 阶段1前置证据

阶段1已于 2026-09-15 完成规定收口（阶段状态 `Complete`，结论见阶段1 `testReport.md`）；目标库 `DysoftHIS` 的 `public` schema 已建成 `mrec_medical_standard_category`、`mrec_medical_standard_group`、`mrec_medical_standard_item` 三表（2026-09-15 只读元数据核实）；作用域注册键清单可由 `Dy.MedicalRecognition.Tests/Stage1SqlMapProbeTests.Runtime_registration_reports_full_sql_ids_without_opening_database` 在不连库条件下输出实际 `FullSqlId`。

进入 B5 对应真实验证的前置已齐：宿主页面路由与真实 API 接线已随 Ticket 07 完成，真实 token 中 `org` 非空的运行验证已取得（`org="01"`，V22）。**写入边界**：允许经真实业务接口或宿主页面写入测试数据，不得直接改库、执行 DDL/DML 脚本或使用测试夹具绕过业务入口；阶段2目标表已建成并完成只读元数据核实。缺任一项，只阻断依赖该项的集成验证，不把构建或静态检查写成业务通过。

组织取证已按 S2-D12 结案（"只信可信当前组织"口径：服务端只接受与 `HttpRequestInfo.OrgId` 相等的组织，组织切换由宿主重新签发 token 完成）；事件取证已按 S2-D13 结案（无 `[WorkUnit]` 生产入口的 Flush 主体、时点与失败语义 `SourceConfirmed`）。**事件可见性（V24）**：数据库写入与失败响应运行面已实测通过（表已建、真实入口已验），"事件是否被登记/处理/提交"无观察点，按 Testing Baseline §3 整条记 `Blocked`，受阻原因登记为决策标签（接受口径待负责人确认）；与并发停用的事件计数子面（V26）、跨组织写拒绝（V27）同口径。逐条状态、实测面与处置口径见阶段 `testReport.md`，不得因受阻原因已登记为决策标签而把这三条记为通过。独立查询投影、契约检查和 DDL 设计继续。

## 生成与人工维护

本阶段涉及 Request、ReadModel、Command、Event、Entity、DataMap、Repository、XML、DDL、Application、Manager 和必要 Host 接入。

## 执行记录

- [x] 完成失败证据、实现、复测和阶段报告归档。
- [x] 按 V1-V25 和新增 V26-V27 执行精确验证；V11命令与V19查询、V13单层与V20多层分别保留证据。
- [x] 将实际结果、证据及残余阻断交阶段根 `testReport.md` 主责归档（后端矩阵的逐条状态、受阻项及其登记口径见该报告的"结果"节；本表不重复统计）。
