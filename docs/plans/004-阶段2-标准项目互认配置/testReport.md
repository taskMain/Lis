# 阶段 2 测试报告

## 执行边界

本报告记录阶段2自身的文档检查及尚未执行的业务矩阵。阶段1正在实施，本轮不审查或裁定其当前进度；依赖验证开始前另核对负责人收口确认。

- 报告状态：`NotRun`
- 矩阵版本：2026-09-14，后端 V1-V27（27条），前端 C1-C25（25条），总分母52条；多层级一条只计一次。
- 起始提交：`5521cb6b5eb0ba0382eab0efc4c6fdcacfefa1c6`。本轮只在共享工作区改文档，不创建分支或提交。
- 起始状态：本轮开始前执行了 `git status --short --branch`；工作区已有其他修改，未回退或清理。
- 本轮未修改业务实现代码，未执行建表、迁移、数据库写入、代码生成或宿主验收。

## 结果

下列按相同状态合并编号，覆盖52条且无重复。业务用例均未执行，实际命令、业务会话、截图及响应均为“无”；不得用本轮文档检查填充业务证据。Blocked 表示本阶段尚未取得对应验证前置，不推断目标系统实际部署状态。

| 用例编号 | 验证面 | 状态 | 命令/操作及证据 | 未覆盖面/原因 |
|---|---|---|---|---|
| V1、V3、V4、V6、V8、V9、V16、V21 | 单元、契约 | NotRun | 无，未执行 | 业务行为尚未验证 |
| V11、V19、V22、V27 | 真实可信组织与资源范围 | Blocked | 无，未执行 | S2-D12未解除；可先编写独立失败测试，不算真实范围通过 |
| V2、V5、V7、V12、V13、V14、V15、V17、V18、V20、V23、V25、V26 | 数据库及混合静态/真实验证 | Blocked | 无，未执行 | 未取得本阶段目标/schema及负责人建表确认；静态部分可独立执行，但不能代表完整用例通过 |
| V10 | 不存在配置 ID | N/A | 无，正常业务流程不产生该数据状态 | 不通过直接写库或伪造数据构造该前置 |
| V24 | 无WorkUnit生产入口事件处理 | Blocked | 无，未执行 | S2-D13未解除，不能以替代入口证明 |
| C1、C2、C3、C10、C11、C14、C15、C16、C18、C19 | 独立组件 | NotRun | 无，未执行 | 组件行为未验证 |
| C22 | 累计Client契约 | NotRun | 无，未执行 | 可在后端契约稳定后独立执行；当前未执行，不是宿主阻断 |
| C23 | 创建/查询请求适配 | NotRun | 无，未执行 | 组件/类型检查未执行，不以宿主缺失阻断 |
| C24 | Client生成入口保护 | NotRun | 无，未执行 | 可独立执行；当前未执行，不依赖登录 |
| C4、C5、C6、C7、C8、C9、C12、C13、C17、C20、C21、C25 | Host及含Host的完整用例 | Blocked | 无，未执行 | 尚未取得阶段2真实API、目标菜单、身份及前置确认；组件子项可以独立执行 |

业务统计：NotRun 21条，Blocked 30条，N/A 1条，Passed/Failed/PendingRetest均0条。独立文档检查不计入52条分母。

## 未覆盖面与残余风险

- 当前没有阶段2运行、数据库、API Client或宿主页面证据。
- 组织服务的具体接口和授权组织集合仍需实施前按实际代码与宿主请求核实。
- 作用域方案与运行证据分开，阶段2实际注册键由V23验证；不裁定阶段1当前探测结果。
- 不分页风险引用 S2-D7，不创建外键引用 S2-D5。
- 不创建 `StandardItemId` 外键为已确认设计，不属于遗漏。

## 结论

本轮为文档整改，不是业务实现验收。S2-D12/S2-D13未解除前，不放行依赖它们的实现；独立文档、DDL设计和组件准备不被整体阻断。

## 文档检查证据

2026-09-14，仅检查本阶段根 Markdown、Server/Client Markdown 和本轮涉及的SRS/UML；检查范围不包含阶段2审查辅助目录，未改写历史审查结果。

| 检查 | 结果 | 实际操作及证据 | 边界 |
|---|---|---|---|
| 矩阵定义与引用 | Passed | PowerShell读取正式文档，提取V/C编号及编号区间；V1-V27与C1-C25连续无重复，所有引用均有定义 | 不代表用例执行 |
| 文档链接及表格列数 | Passed | PowerShell逐文件解析Markdown相对链接并Test-Path；连续表格逐行核对列数，无断链/列数错误 | 仅本地相对Markdown链接 |
| UML1基础检查 | Passed | 执行dy-puml的validate-wsd.ps1，Path为docs/uml/1-MedicalRecognitionReport.wsd：0 errors、1 warning（CRLF） | 只验证基础块结构，不代表渲染或可生成性 |
| UML2基础检查 | Passed | 同脚本检查docs/uml/2-MedicalRecognitionConfigurationUiQuery.wsd：0 errors、1 warning（CRLF） | 无业务或生成器运行 |

这些结果独立于52条业务用例统计。模型渲染、生成、代码测试及真实数据库/宿主验证未执行。

### 独立子代理复核

审查代理 Mencius（`01a09f13-49ce-7e12-97f7-d47d260802c9`）未参与文档编写，仅只读复核；不是将编写代理自己的检查当作独立审查。

独立子代理复核已重新读取受影响段落及最终配置说明，未发现待修正文档问题。它同时核对V1-V27、C1-C25及52条状态分母。本次复核不要求或产生业务运行证据，也不解除S2-D12/S2-D13。
