# 后端设计

模板，仅后端或跨端阶段保留。总体范围与关键决策见 [阶段设计](../design.md)。

## 领域与契约

填写聚合边界、业务不变量、类型归属、调用链及关键字段来源。按 `backend-design-gates.md` 和 `backend-command-query-event.md` 填写适用的层映射、写用例事务表与 Command-Event 表，不为填表新增无用类型。

## 数据与集成

填写实际数据库结构、Repository/SQL、并发、事件、外部调用、失败语义和迁移执行责任。框架机制按本项目版本核实。

## 验证矩阵

| 编号 | 用例与边界 | 层级 | 预期结果 | 真实数据库 | 真实 WorkUnit | 前置数据 |
|---|---|---|---|---|---|---|

本表只记录测试前设计，实际执行结果进入 [阶段测试报告](../testReport.md)。
