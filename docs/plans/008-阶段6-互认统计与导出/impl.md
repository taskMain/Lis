# 阶段 6 实施

设计见 [design.md](design.md)（[后端设计](Server/design.md)、[前端设计](Client/design.md)、[前端矩阵](Client/testPlan.md)）；结果将见 [testReport.md](testReport.md)。当前进度与交接见本文「待办与交接」段。

## 批次与依赖

| 批次 | 范围 | 主责 | 文件所有权 | 前置条件 | 矩阵编号 | 当前状态 |
|---|---|---|---|---|---|---|
| 0 | 契约准备：十一个请求类型、六个统计读模型及其嵌套子模型占位重建（强类型、患者结构化字段、决策枚举 + 文本计算属性）、汇总维度与明细类型两个新枚举的描述器补全与登记、同批更新 `Stage2EnumContractTests` 的 `ExpectedEnumNames` 清单并补三个枚举的取值/成员名/中文说明冻结断言、既有断言（端点清单、分部清单、分页禁用字段）同批同步 | 后端主责 | Contracts、Domain.Share、Tests | 设计确认 | V1-V6 | 已完成（V1-V6 静态面通过，实施审查第1轮通过；Application 层 10 个 `NotImplementedException` stub 为保编译授权口径，票 02-05 替换） |
| 1 | 索引迁移与建表脚本双写：三个统计索引的迁移文件（`Scripts/Migrations/<日期>_s6_statistics_indexes.sql`）与三份建表脚本同步更新、ClosedXML 集中包声明与工程引用 | 后端主责 | Repository、构建配置、Tests | 批次 0 | V7、V8 | 已完成（静态面，实施审查第2轮通过；ClosedXML 0.105.0）；索引 DDL 由负责人执行（迁移文件 `20260921_s6_statistics_indexes.sql`），执行前 V43 记 `Blocked` |
| 2 | 统计语句与仓储分片：汇总/明细/匹配记录视图全部语句、查询端口与实现的 Statistics 分片、窗口校验接入 | 后端主责 | Repository、Application | 批次 1 | （语句面不独立占号；行为面编号随批次 3/4/7） | 已完成（17 条语句 + 端口/实现分片，实施审查第3轮通过） |
| 3 | 接收侧汇总与明细应用层：范围解析、双版本注入、汇总组装（率与占比计算、原因装配、名称批量回填）、明细组装 | 后端主责 | Application | 批次 2 | V11-V30 | 已完成（替身面取证，实施审查第4轮通过，修复项闭环） |
| 4 | 来源侧汇总与明细与匹配记录视图应用层、来源侧维度子集拒绝 | 后端主责 | Application | 批次 3 | V31-V33、V39 | 已完成（替身面取证，实施审查第5轮通过，证据面补齐） |
| 5 | 导出：计数与 10 万行上限、ClosedXML 组装、两个 POST 控制器动作返回文件流、零记录拒绝 | 后端主责 | Application、Host | 批次 3 | V34-V38 | 已完成（替身面与静态面取证，实施审查第6轮通过，三项低严重度建议已修复；两导出入口以实现方法 `[NonAction]` 排除自动端点，宿主 9 自动端点 + 2 控制器 POST；真实宿主链路归批次 7） |
| 6 | 静态收尾：统计链零写入静态守卫、客户端路径扫描登记导出控制器、构建与全量测试与差异检查 | 后端主责 | Tests、全部 | 批次 0-5 | V9、V10 | 已完成（静态面，实施审查第7轮通过，两项低严重度残留已修复；为批次 7 提供干净基线） |
| 7 | 真实库与宿主取证：四查询成功面与只读回库核对、分页行为、索引核对、宿主导出下载与 401、组织服务失败面；问题与残余风险写入阶段报告 | 后端主责 | Tests、文档 | 批次 0-6、索引建成、宿主可用 | V40-V45 | 已完成（实施审查第10轮通过；V40-V45 与 C23-C30 全部取证或按三类口径登记；数据全部经接口构造零写库；两处等价修复与两项登记不修待负责人裁定；阶段收口三态待负责人确认） |

**矩阵编号的批次归属**：批次 0 归 V1-V6；批次 1 归 V7、V8；批次 2 语句面不独立占号，行为面编号随批次 3/4/7 落地；批次 3 归 V11-V30；批次 4 归 V31-V33、V39；批次 5 归 V34-V38；批次 6 归 V9、V10；批次 7 归 V40-V45。各编号只归一个批次。前端 C1-C22 随客户端批次，C23-C30 随宿主验收批次，归属在 [Client/impl.md](Client/impl.md) 登记。

**新增端点与分部文件的清单同步**：既有查询入口方法集合断言、查询侧分部文件清单与数量断言、语句清单与列一致性守卫，在批次 0（端点与分片声明）与批次 2（语句补齐）分别同批同步，只加端点而留下清单断言失败的状态不得跨批次存在。

## 执行记录

- [x] 完成适用设计及确认。
- [x] 建立目标失败证据或等价基线，完成适用影响分析。
- [x] 完成当前批次实现与直接影响验证。
- [x] 完成适用最终检查并更新根测试报告。

## 待办与交接

- 阶段 6 已收口（2026-09-22）：票 00-09 全部完成并通过实施审查（10 轮），批次状态见上表与 [testReport.md](testReport.md)。
- 交付物提交：阶段 0-6 全部改动未提交，提交时点与范围由负责人决定（`.zcodeignore` 已裁定纳入提交）。
- 移交后续：跨医院互认匹配候选查询缺陷属阶段 5 交付面，按阶段 5 流程处理；S6-D9 患者原值已接受偏差在阶段 7 排除项回归时复核；接近 10 万行上限的真实导出按 S6-D16 随真实业务量增长复核。
