# 阶段 6 前端测试前矩阵

本文件是阶段 6 前端的测试前矩阵（用例、边界、测试层级、预期结果）。实际执行命令、结果、证据、状态与残余风险只写阶段根 [testReport.md](../testReport.md)。

## 测试数据与前置边界

- 验收数据只通过平台接口形成：统计与导出的事实由阶段 4 的报告提交接口与阶段 5 的匹配、处理结果、引用接口按业务顺序构造；禁止直接写库或使用脚本注入数据。
- 两个测试身份分属同一组织下的不同医院与院区，用于范围隔离与互认双方构造；本院页的可信上下文缺失面用可控替身验证。
- 不设置需要构造正常业务不可达前置的用例；页面级失败态用可控的接口失败注入验证；导出失败与零记录面用计数替身与后端真实行为验证。
- 依赖真实后端、真实数据库与真实宿主的条目在矩阵中标注；条件不具备的受影响条目记 `Blocked`。

## 矩阵

| 编号 | 用例与边界 | 层级 | 预期 | 依赖真实后端 |
|---|---|---|---|---|
| C1 | API Client 累计生成与手写入口 | Contract | 生成后公开入口完整；请求与读模型字段、可空性、枚举、分页形状与后端契约一致；保留阶段 1-5 已交付接口；登录接口仍被排除；手写入口文件内容恢复 | 否 |
| C2 | 累计范围核对 | Contract | 无后续阶段端点泄漏；无退化类型；无残留占位模型 | 否 |
| C3 | 构建与类型检查 | Static | 生成包与应用均可构建、类型检查通过 | 否 |
| C4 | 适配层双版本请求映射 | Unit | 平台版提交组织、医院、院区（来源侧两组），本院版不提交组织与医院；平台版接收侧请求提交接收组与来源组两组（含两组织编码），供组织一致性校验；本院版来源侧请求提交接收医院/院区，本院版接收侧请求提交来源医院/院区，组织字段不提交；分页对象内嵌；日期原样提交不拼接时分秒；敏感条件不入地址栏 | 否 |
| C5 | 适配层分页响应收敛 | Unit | 合法响应收敛为页面分页模型；页码、页容量与总数做安全整数与值域校验，非法响应按契约错误处理 | 否 |
| C6 | 适配层读模型映射 | Unit | 枚举值与文本随行映射；未参与分组的维度字段为 `null` 时视图模型兜底；患者姓名与证件号码原样映射；原因汇总嵌套集合映射 | 否 |
| C7 | 分页控件配置 | Unit | 页容量可选值与服务端一致；显示总数；改变页容量回到第 1 页；加载中禁用 | 否 |
| C8 | 导出下载处理 | Unit | 使用已鉴权客户端取字节；按响应文件名触发保存；对象地址释放；失败不产生本地错误提示、不误刷新 | 否 |
| C9 | 筛选与维度变化复位 | Unit | 筛选、汇总维度或明细类型变化时回到第 1 页并清空旧结果；过期响应丢弃 | 否 |
| C10 | 平台接收侧页加载与筛选 | Component | 进入自动加载；日期必填；组织、医院、院区、科室、项目筛选生效；来源组组织/医院/院区筛选可选生效；维度单选切换出列正确；页面不做最大跨度本地校验 | 否 |
| C11 | 汇总表展示 | Component | 行结构随维度变化；六项指标、同期互认率（未计算显示 —）、金额与原因占比展示；无趋势图、排名或绩效元素；互认科室维度行不含未反馈项的口径展示 | 否 |
| C12 | 汇总数字下钻联动 | Component | 点击指标数字预填明细类型与维度值并查询；点击原因行加原因代码筛选；金额随采纳明细展示 | 否 |
| C13 | 明细表四类型与未反馈 | Component | 明细类型切换生效；未反馈行显示「未反馈」且处理结果留空；互认医生与原因筛选生效 | 否 |
| C14 | 匹配记录弹窗 | Component | 从明细行打开；展示组级信息、就诊、状态、决定主体与全部匹配项；不提供报告、PDF 或影像入口 | 否 |
| C15 | 导出入口沿用条件 | Component | 汇总与明细导出分别沿用当前查询条件发起；导出中禁用重复提交；零记录、超过 10 万行上限与失败由宿主统一提示（上限提示缩小查询条件） | 否 |
| C16 | 患者信息展示 | Component | 患者姓名与证件号码按服务端原值展示，页面不做二次处理 | 否 |
| C17 | 枚举文本展示 | Component | 项目类型、就诊类型、处理结果与状态使用服务端返回文本；页面无本地中文映射表 | 否 |
| C18 | 空结果与加载失败 | Component | 空结果显示空态不报错；读取失败保留已有数据、结束加载态、允许重试、无本地错误提示 | 否 |
| C19 | 页码越界自愈 | Component | 当页为空且总数为正时按服务端总数回退最后一页一次 | 否 |
| C20 | 本院接收侧页范围 | Component | 组织与医院固定展示且请求不提交；院区可选，院区不选择时按可信医院全院查询；来源组医院/院区可选筛选生效，组织字段不提交；可信上下文缺失时阻断页面不发请求 | 否 |
| C21 | 本院来源侧页锁定 | Component | 来源组织与来源医院固定，只来源院区可选；接收组医院/院区可选筛选生效，组织字段不提交；越界选择由服务端拒绝，页面不做本地归属判断 | 否 |
| C22 | 来源侧页面口径 | Component | 展示被互认次数与明细；不展示金额；无明细类型切换与不采纳原因区 | 否 |
| C23 | 路由接入 | Integration | 四条路由可进入且与宿主菜单路径一致；不提供本地直达入口 | 是 |
| C24 | 宿主验收：平台接收侧页 | Host | 从宿主登录页与菜单进入，筛选、汇总、下钻、明细分页与弹窗真实可用 | 是 |
| C25 | 宿主验收：本院接收侧页 | Host | 同上，范围固定为可信组织与医院 | 是 |
| C26 | 宿主验收：平台来源侧页 | Host | 同上，汇总与明细只计被互认次数 | 是 |
| C27 | 宿主验收：本院来源侧页 | Host | 同上，来源范围固定为可信医院 | 是 |
| C28 | 宿主导出下载 | Host | 两类导出生成真实 `.xlsx` 并可打开；内容与页面口径一致；零记录时宿主提示无可导出数据 | 是 |
| C29 | 宿主失败态 | Host | 真实接口失败时页面保留数据、结束加载、允许重试；错误提示由宿主统一展示；不误刷新、不误报成功 | 是 |
| C30 | 范围隔离宿主面 | Host | 以另一测试身份访问本院页数据不可越权；平台页跨组织面不可构造时按降级口径记录 | 是 |
