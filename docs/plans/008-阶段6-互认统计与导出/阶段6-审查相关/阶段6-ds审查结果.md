# 阶段 6 票拆分审查结果（ds）

审查对象：`docs/plans/008-阶段6-互认统计与导出/阶段6-Tickets/` 下 `README.md` 与票 00 至票 09 全部票文件（唯一审查事实载体）。
审查依据：阶段 6 设计文档全集（根 `design.md` 含 S6-D1 至 S6-D18 台账、`Server/design.md` 含 V1-V45、`Client/design.md` 与 `Client/testPlan.md` 含 C1-C30、根/`Server/`/`Client/` 三个 `impl.md`、根 `testReport.md`）；上游文档仅在核对验收项依据时引用。
审查方式：全部为只读核对；未参阅同目录其他审查方的过程或结论。
说明：本轮为票拆分审查的复审，已按纪律清空重写，不保留上一轮结论与旧口径；发现编号重新起算。归类按票审查口径（票缺陷 / 设计本身如此 / 理解偏差 / 疑问）。

---

## 一、发现清单

### 1. 票集合缺「设计确认」门槛，票 00 声明可立即开始并与设计「确认前不进入批次 0」冲突

| 要素 | 内容 |
|---|---|
| 类型 | 票缺陷 |
| 严重度 | 中 |
| 位置 | `00-契约准备与枚举登记.md:5`（**Blocked by:** None（可立即开始））、`:7`（Status: ready-for-agent）；`README.md:7`-`:16`（全票 ready-for-agent）、`:18`（外部前置段） |
| 证据 | 票面事实：票 00 写「`**Blocked by:**` None（可立即开始）」，票 00 至票 09 的状态列全部为 `ready-for-agent`；`README.md:18`「**外部前置**」段只登记了三项——票 09 依赖三个统计索引建成、宿主可用、两测试身份同组织，未提及设计确认。<br>设计依据：根 `impl.md:9` 批次 0 的「前置条件」列为「**设计确认**」；根 `impl.md:31`「设计确认：根 `design.md` 台账（S6-D1 至 S6-D18）与 Server/Client 设计待负责人确认；**确认前不进入批次 0**」；`Server/impl.md:29`「**设计确认前不开工**」；`Client/impl.md:24`「**设计确认前不开工**」；根 `testReport.md:3` 现行状态为「设计已起草**待确认**」。 |
| 影响 | 按票面执行会把批次 0 当作可立即开工：实施者在设计（含 S6-D1 至 S6-D18 台账、读模型与请求字段口径）未被负责人确认前开始改契约与登记枚举，一旦确认过程修改任一决策，已完成的契约重建与枚举冻结断言需返工；同时票 01-09 均经票 00 串行，门槛缺失会沿依赖链传导到全部票。该门槛与设计已为票 09 登记的「索引 DDL 由负责人执行」「宿主可用」属同一类非票面前置，README 既有「外部前置」段可承载却遗漏。 |
| 建议方向 | 在 `README.md`「外部前置」段补设计确认门槛（并写明确认范围含根 `design.md` 台账 S6-D1 至 S6-D18 与 Server/Client 设计），票 00 的 `Blocked by` 相应改为指向该门槛（或写成「外部前置：设计确认」），使票面与三份 `impl.md`、`testReport.md` 的现行口径一致。 |

### 2. 票 02 残留 V9 引用，与 README「各编号只归一票」的唯一归属冲突

| 要素 | 内容 |
|---|---|
| 类型 | 票缺陷 |
| 严重度 | 低 |
| 位置 | `02-统计语句与查询仓储分片.md:9`；`README.md:20`；`06-静态收尾与阶段收口检查.md:10` |
| 证据 | 票面事实：票 02 验收项 `:9`「语句标识与端口方法名一一对应（方法名去掉 `Async` 即语句标识），作用域为 `MedicalRecognitionReportQuery`（**V9 的语句面**）」；同票其余三项已改写为不引用编号的表述（`:11`「（本票语句面静态判据）」、`:12`「（本票语句面静态判据）」、`:10`「（本条静态面在票 06 复核）」）。票 06 `:10`「既有断言同步未放宽：端点清单、分部文件清单与数量、语句清单与列一致性守卫逐项等值（**V9**）」；`README.md:20`「V9、V10 归票 **06**……**各编号只归一票**」。设计侧：`Server/design.md` V9 为「既有断言同步 | Static | 查询入口方法集合、分部文件清单与数量、语句清单、列一致性守卫同批同步且未放宽」，其归属批次为批次 6（根 `impl.md:18`）。 |
| 影响 | 唯一归属纪律在本轮整改后已基本落实（V2、V23、V33、V42 的跨票重复已消除），仅 V9 在票 02 与票 06 并列出现。实施票 02 的智能体会读到「V9 的语句面」并倾向把 V9 勾在本票，而 README 与设计都把 V9 归于票 06，两处口径不一致会让同一编号被两次声明完成；按 README 逐格机械核对「各编号只归一票」时该行仍不成立。 |
| 建议方向 | 把票 02 `:9` 的「（V9 的语句面）」改为不引用编号的等价表述（例如「（本票语句面静态判据，随票 06 的 V9 同批复核）」），与同票其余三项的改写方式一致。 |

### 3. 票 03 声明 V23 真实库面由票 09 核对，但票 09 清单未承接该编号

| 要素 | 内容 |
|---|---|
| 类型 | 建议 |
| 严重度 | 低 |
| 位置 | `03-接收侧汇总与明细查询.md:17`；`09-真实库与宿主取证收口.md:9`-`:18`；`README.md:20` |
| 证据 | 票面事实：票 03 验收项 `:17`「排序稳定唯一，跨页无重复无遗漏（**V23；真实库跨页行为面随票 09 真实库批次回库核对，本票以替身取证语句与排序键**）」；票 09 的验收清单逐条为 V41、V42、V43、V44、V45、V40 与 C23-C30（`:9`-`:18`），**未出现 V23**。<br>设计依据：`Server/design.md` V23 为「汇总排序稳定唯一 | **Repository** | 排序键稳定唯一，跨页无重复无遗漏；窗口由 Provider 适配层承载，XML 无方言分页 | **真实数据库＝是**」；V22 的设计预期另注「真实库分页面归 V41/V42」；`README.md:20` 按 V11-V30 把 V23 唯一归给票 03。 |
| 影响 | V23 的设计判据含「真实数据库＝是」的运行期面，而票 03 自述以替身取证、把真实库面交给票 09，票 09 又未列该编号：跨票承接为单边声明，读票 09 无法得知 V23 尚有待核面，阶段收口时该面可能无人取证。实质行为（真实库跨页无重复无遗漏）与票 09 的 V42 高度重叠，故后果有限，但编号级归属与实际取证面不对齐。 |
| 建议方向 | 在票 09 的清单显式登记 V23 的真实库面（或在 V42 项内注明同时覆盖 V23 的跨页运行期判据），使票 03 的单边声明闭合；若确认该面已由 V42 完整覆盖，则把票 03 `:17` 的括注改为引用 V42。 |

---

## 二、按审查维度的小结

| 维度 | 结论 |
|---|---|
| 1. 票 ↔ 设计忠实性 | **有发现**（发现 1）。逐票核对设计口径未发现走样：票 00 的请求类型数量（11）、双入口字段口径、六个顶层读模型与「两个汇总读模型补标准项目名称、接收侧汇总读模型补汇总维度字段」、`ExpectedEnumNames` 与三枚举冻结断言、UML 5 同批同步（现行设计 `design.md:63`、`:71`、`impl.md:33`、`Server/impl.md:29`、`testReport.md:12` 统一为随批次 0 同批同步）逐项相符；票 01 的三条索引列与命名、既有 `business_key` 索引列序差异、ClosedXML 在 Application 工程显式引用与 S6-D7/S6-D12 及 V7/V8 一致；票 02 的「17 条语句」与 `Server/design.md` 语句清单逐条相符（接收侧汇总 3、来源侧汇总 2、四类接收侧明细计数与取页 8、来源侧明细 2、匹配记录视图 2），方言中立与窗口交 Provider 口径一致；票 03 的双版本范围、本院版院区为空「不进入院区层解析」与 `Server/design.md:60` 一致，S6-D3/S6-D17/S6-D18 均按登记口径承接；票 04 的来源侧维度子集、只计采纳不含金额、患者原值与 S6-D9 一致；票 05 的七类导出、上限与「零记录与超限不返回文件响应、不产生空工作簿」与设计 V38 现行文本（含「（F09 备选流 2、3）」）逐字相符；票 06、07、08 的静态与组件项与 V9/V10、C1-C22 一致；票 09 的 V40-V45、C23-C30 与 S6-D9/S6-D13/S6-D18 承接口径正确。缺陷为设计确认门槛未进入票面（发现 1）。 |
| 2. 票集合完备性与编号归属 | **有发现**（发现 2）。完备性成立：V1-V45 全部被票覆盖（V1-V6→00、V7-V8→01、V11-V30→03、V31-V33/V39→04、V34-V38→05、V9-V10→06、V40-V45→09）且与三份 `impl.md` 的批次归属逐项一致；C1-C30 全部被票覆盖（C1-C8→07、C9-C22→08、C23-C30→09），本轮票 07 已补 C7 项，前后端编号无遗漏；S6-D1 至 S6-D18 全部有票承接（S6-D1/D13/D14→00，S6-D7→01，S6-D3/D17/D18→03，S6-D9→04，S6-D5/D16→05，S6-D1/D9/D11/D18→07、08，S6-D9/D13/D18→09，S6-D2/D4/D6/D8/D10/D12/D15 由对应能力票正文与矩阵承接）；票间共用前置（索引 DDL 由负责人执行、宿主可用、两测试身份）声明一致。唯一归属仅余 V9 一处未闭合（发现 2）。 |
| 3. 阻塞边与依赖正确性 | **有发现**（发现 1）。阻塞图无环、无缺失、无多余硬阻塞，且逐条等于设计批次前置：00→{01, 02, 07}、01→02、02→03、03→04、03→05、{00, 01, 02, 03, 04, 05}→06、07→08、{06, 08}→09（对照根 `impl.md:9`-`:16` 批次 0-7 的前置条件列与 `Client/impl.md:9`-`:11`）；可立即开工的前沿判断正确；票 09 对索引建成与宿主可用的依赖以「外部前置」而非票间阻塞表述，符合维度 3 对宿主面的要求。唯一缺口是全集合缺「设计确认」这一非票面门槛（发现 1），以及 V23 的单边承接（发现 3）。 |
| 4. 验收项可判定性 | **有发现**（发现 3）。绝大多数验收项有明确矩阵编号或设计段落作判据且在本票内可判定：票 03、04、05、09 逐条对应 V11-V45 与 C23-C30，票 06、07、08 对应 V9/V10 与 C1-C22；未见「证据见后续票」式空话；未有正常业务不可达前置混入——票 03、票 09 写明「不构造空组织/医院/院区账号类用例」，票 09 另按「条目构造口径」三类（可达即取证、不可达记 `NotRun` 并写明理由、静态已冻结记 `NotRun`）定案，与 V 矩阵层级标注一致。缺口为 V23 的运行期判据在票 09 无对应条目（发现 3）。 |
| 5. 范围纪律 | **无发现**。逐票核对排除项与已登记口径：无票引入趋势图、医生互认率/金额排名/绩效（排除项 17）、CSV/PDF 等其他导出格式或借导出解除脱敏（排除项 18）、脱敏算法、独立互认匹配记录导出或跨组织统计；票 05 明确「文件不含完整报告结构化内容、PDF 文件标识与下载入口、影像调阅地址、院内项目编码/名称/项目映射」。已登记口径未被扩张或收缩：10 万行上限（票 05 按 S6-D5「已接受收窄」承接并保留缩小条件提示）、不限日期跨度（票 08「不做跨度本地校验」）、行内占比分母（票 03 按 S6-D3 作为确认偏差承接）、六个顶层读模型与 10 方法/11 端点（票 00 逐项相符）、互认科室维度不可对账（票 03、票 08 按 S6-D17 承接）、平台版空取值与不构造账号用例（票 03、票 04、票 07、票 09 按 S6-D18 承接）。 |
| 6. 票大小与可实施性 | **无发现**（附观察）。各票改动面与设计批次一一对应，无「宽重构」型票：票 00-06 分别落在 Contracts/Domain.Share/Tests、Repository/构建配置、Repository/Application、Application、Application/Host、Tests；票 07、08 与宿主收口票 09 边界清楚。票 03 的验收项最多（12 项，覆盖 V11-V30），但其内容共享同一批类型与同一分片文件，按设计批次边界交付优于拆票制造跨票共享文件的并发修改面。另需指出：票 00-05 存在共同触碰的构建/配置类文件（`MedicalRecognitionApplicationModule.cs`、`Application.csproj`、`Directory.Packages.props` 分别为票 00/01/05 涉及），因设计与仓库规则要求同工作区串行（`.agents/instructions/repository-safety.md:7` 未获授权不得创建额外 worktree），该共享面不构成本轮发现；若后续允许并行执行，需在票面补文件所有权与串行说明。 |

## 三、上一轮票审查发现的复查结果

| 上轮编号与标题 | 本轮状态 | 依据 |
|---|---|---|
| 1 V 矩阵编号在票间重复归属 | 大部整改，残留一处 | V2（票 04）、V33（票 02）、V42（票 02）、V23（票 02）的重复引用均已移除，票 02 的语句面判据改写为不引用编号的表述；仅票 02 `:9` 仍引用 V9（本轮发现 2） |
| 2 票 00 把 UML 5 同步写成无条件验收项 | 已整改 | 设计侧现行口径统一为「随批次 0 同批同步」：根 `design.md:63`、`design.md:71`（并把该同步从停止条件例外中排除）、根 `impl.md:33`、`Server/impl.md:29`、根 `testReport.md:12`；票 00 `:3`、`:15` 的「同批同步」表述与之一致，已无「待负责人确认」与票面口径的冲突 |
| 3 票 00 携带批次 0 不存在的语句清单登记面 | 已整改 | 票 00 `:14` 改为「既有断言同批同步未放宽：查询入口方法集合、分部文件清单与数量、分页禁用字段断言」，已删除「语句清单与列一致性守卫登记面」；语句清单登记仍归票 02 `:10`，复核归票 06 `:10`，与根 `impl.md:20` 的两段同步纪律一致 |
| 4 票 07、票 08 未引用任何 S6-D 编号 | 已整改 | 票 07 `:18` 补「双版本请求与范围取值口径依据 S6-D1 与 S6-D18；患者字段原样映射依据 S6-D9」；票 08 `:19` 补「页面结构、下钻联动与四页面依据 S6-D11；范围取值与本院页院区为空即全院依据 S6-D1 与 S6-D18；患者信息原值展示依据 S6-D9」 |

**审查方自查更正**：本轮曾拟就「票 05 `:13` 把零记录与超限的负向断言记在 V38，超出该编号的设计预期」立项，复核现行 `Server/design.md` 的 V38 已含「零记录与超限两种业务拒绝下不返回文件响应、不产生空工作簿（F09 备选流 2、3）」，票面与该编号逐字相符，该疑点撤销、未计入发现。另：上一轮对根 `impl.md:33` 的引用与现行文本不符（现行及复核所见均为「随批次 0 同批同步」），本轮以现行文本为准，相关结论以上表第 2 条为准。

## 四、设计已登记事项的承接核对（不计入发现）

| 登记项 | 票面承接 | 结论 |
|---|---|---|
| S6-D1 双入口与本院版字段口径 | 票 00 `:3`、票 03 `:3`、票 04 `:3`、票 07 `:18`、票 08 `:3`、`:19` | 设计本身如此，承接正确 |
| S6-D3 占比分母取行内不采纳次数 | 票 03 `:22` 口径段标注为对 SRS 业务规则 11 的确认偏差 | 设计本身如此，承接正确 |
| S6-D5 导出 10 万行上限 | 票 05 `:3`、`:11` 按「已接受收窄」承接并保留缩小条件提示 | 设计本身如此，未加码 |
| S6-D9 患者信息按业务原值 | 票 04 `:13`、票 07 `:18`、票 08 `:15`、`:19`、票 09 `:18` | 设计本身如此，承接正确 |
| S6-D11 四页面与下钻联动 | 票 08 `:3`、`:19` | 设计本身如此，承接正确 |
| S6-D13 UML 5 同步 | 票 00 `:3`、`:15` 按现行设计「随批次 0 同批同步」承接 | 设计本身如此，承接正确 |
| S6-D16 导出内存峰值 | 票 05 `:16` 以备注承接，写明「接近上限规模的真实导出不构造」 | 设计本身如此，未加码为硬验收 |
| S6-D17 互认科室维度不可对账 | 票 03 `:22`、票 08 `:11` 按登记口径承接展示与说明 | 设计本身如此，承接正确 |
| S6-D18 范围口径与不构造账号用例 | 票 03 `:3`、`:10`，票 04 `:9`，票 07 `:18`，票 08 `:19`，票 09 `:17`、`:20` | 设计本身如此，承接正确 |

## 五、未覆盖范围及理由

- `docs/plans/008-阶段6-互认统计与导出/阶段6-审查相关/` 目录内除本文外全部内容不属审查对象（审查方向与审查结果），本轮未将其作为票面事实载体，也未在同目录其他结果文件中查找或引用结论。
- `阶段6-讨论内容.md` 按审查要求属排除范围（过程记录），未参与本轮核对。
- 仓库代码实现不属审查对象（本阶段尚未开工）；仅在核对票面依据时只读查看既有文件结构（`Application/Queries/`、`Repository/Queries/`、`Contracts/Queries/` 的现有分片形态）与仓库级规则（`.agents/instructions/repository-safety.md`），未据此评价实现。
- 其他阶段的阶段文档与历史结论不属审查对象；仅在确认阶段 2–5 存在 `阶段N-Tickets/` 目录（命名习惯）时查看目录，未审查其内容。
- 未执行构建、测试、数据库或宿主验证：`testReport.md` 登记为「未开始」，本审查不产生任何通过性结论。
- 未覆盖：票面对具体实现方式的描述是否最优（例如票 02 的 17 条语句命名是否与既有语句命名完全同构、票 05 的控制器动作命名），本轮只核对票与设计的一致性、完备性、依赖与可判定性；票 03 的 12 条验收项在单一上下文窗口内的实际耗时属实施排期判断，非设计事实。
- 「理解偏差」登记：审查方未与票作者确认拆分意图，票 02 `:9` 的「（V9 的语句面）」可被理解为以限定语表达「本票承担 V9 的语句面部分」；但按 `README.md:20`「各编号只归一票」的文本口径，该写法使唯一归属无法机械核对，故按票缺陷立项（发现 2）。另本轮有一条拟立项经复核撤销（见第三节「审查方自查更正」）。
- 「设计本身如此」登记：S6-D1、S6-D3、S6-D5、S6-D9、S6-D11、S6-D13、S6-D16、S6-D17、S6-D18 各项在票面均按登记口径承接，未改为硬验收条件、未删除、未加码，不计为发现（明细见第四节）。
- 「疑问」登记：本轮未新增疑问项。发现 1、2 有票原文与设计条款的直接证据（设计确认门槛、唯一归属声明），发现 3 有票原文与设计 V23 层级及数据库列的对照证据，均未作「疑问」处理。
