# 阶段 6 票拆分审查结果（第二轮）

审查对象与依据同首轮：`docs/plans/008-阶段6-互认统计与导出/阶段6-Tickets/`（README 与票 00–09）为唯一审查事实载体；阶段 6 设计文档全集为对照基准（只读）；上游文档仅在核对验收项设计依据时引用。
写入说明：按任务分配沿用本文件；写入前已清空首轮票拆分审查结果，本轮重新编号。本轮为收敛确认轮：首轮 6 条发现全部整改到位，无新发现。

## 上轮发现整改核对

| 首轮编号 | 主题 | 本轮核对结论 |
|---|---|---|
| 1 | V23 无票承接 | 已整改：票 03 补「排序稳定唯一，跨页无重复无遗漏（V23；真实库跨页行为面随票 09 真实库批次回库核对，本票以替身取证语句与排序键）」，真实库面与票 09 的 V42（首中末页与越界页无重复无遗漏）衔接明确 |
| 2 | C7 无票承接 | 已整改：票 07 补「分页控件配置：页容量可选值与服务端一致（1–200）、显示总数、改变页容量回到第 1 页、加载中禁用（C7）」，C1-C8 在票 07 完整 |
| 3 | 票 02「16 条语句」计数失实 | 已整改：更正为「17 条语句」，与自身枚举及 `Server/design.md` 语句清单一致 |
| 4 | 空取值语义自创 + S6-D18 依据错配 | 已整改（设计侧登记）：S6-D18 扩展为「任一范围条件为空时不附加该层过滤（全部为空即平台按授权的全量范围）」，依据含「请求级空取值行为与阶段 4 报告列表 `HasAnyScope` 口径一致」（经代码核实：`MedicalRecognitionReportQueryAppService.Reports.cs:39,576` 该方法存在）；`Server/design.md` V12/V35/矩阵头/关键字段来源与 V13（院区为空不进入院区层解析，前置数据含院区为空分支）同步更新；票 03/04 的引用由此成立，票 07/08 新增的 S6-D18 范围取值引用同此口径 |
| 5 | 票 02 阻塞边与设计批次依赖不一致 | 已整改：票 02 改为「Blocked by: 00、01」并在口径中说明「批次 2 在设计中前置批次 1……语句编写本身对索引与包声明无产物依赖」；README 票表同步 |
| 6 | V2/V9 归属表述混乱、票 00 范围超出 | 已整改：票 00 验收项删除「语句清单与列一致性守卫登记面」及 V9 标签（只剩端点/分部/分页禁用字段，与根 impl 批次 0 范围一致）；票 02 的 V23/V42/V33 引用改为「本票语句面静态判据」「本条静态面在票 06 复核」；V9 现唯一归票 06（票 02 仅保留带限定的「V9 的语句面」）；V2 唯一归票 00（Contract 面），其 Application 运行期面由票 04（V32 维度子集校验）与票 05（V34 导出维度子集校验）按行为承接，标签一致 |

本轮另核对本轮设计侧新增改动（S6-D18 扩展、V12/V13/V35、上游同步范围改为「随批次 0 同批同步」）与票面的一致性：票 00 的 UML 同批同步、票 03 的「不进入院区层解析」、票 09 悬空项登记均与更新后的设计一致，无漂移。

## 发现

无。

## 覆盖声明

| 维度 | 结论 |
|---|---|
| 1. 票 ↔ 设计忠实性 | **无发现**。票 00-09 的正文与验收项对照第四轮定稿设计（含本轮 S6-D18 扩展）逐条核对：11 请求类型/10 方法/11 端点/六个顶层读模型/17 条语句/C1-C30 与 V1-V45 的数字与口径全部一致；S6-D3/D5/D9/D16/D17/D18 六项已登记偏差均按登记口径承接，无擅自加码、删除或改为硬验收条件。 |
| 2. 票集合完备性与编号归属 | **无发现**。V1-V45 与 C1-C30 经逐条映射全部被票覆盖且唯一归属（V23 由票 03 承接并显式声明真实库面随票 09 的 V42 取证；C7 已补入票 07；V2 的 Contract 面归票 00、运行期面由票 04/票 05 按行为承接且标签一致）；S6-D1 至 S6-D18 全部有票承接；共用前置（索引 DDL 由负责人执行、宿主可用、两测试身份、S6-D18 不构造口径）在票 01/09 与 README 声明一致。 |
| 3. 阻塞边与依赖正确性 | **无发现**。票 02 阻塞边已与设计批次依赖对齐（00、01）；全图无环；前沿票 00 判断正确；票 06 聚合 00-05、票 09 聚合 06+08 与批次收口一致；前端票 07 依赖票 00 契约、票 08 组件层替身取证并以「真实宿主面归票 09」表述，符合非硬阻塞要求。 |
| 4. 验收项可判定性 | **无发现**。各验收项均有矩阵编号、设计段落或 S6-D 决策判据；空取值场景经 S6-D18 扩展后 V12/V31/V35 有明确判据；无正常业务不可达前置混入（账号级空组织/医院/院区在票 09 与矩阵头均按「不构造」承接）；无「证据见后续票」式空话——票 02 的「在票 06 复核」为静态面归属声明，其自身验收（登记全部新语句）在票内可判定。 |
| 5. 范围纪律 | **无发现**。票 00-09 未引入排除项 17/18、脱敏算法、独立匹配记录导出或跨组织统计；10 万行上限、不限日期跨度、行内占比分母、六个顶层读模型、10 方法/11 端点、不构造空账号用例等已登记口径均按登记承接，未扩张或收缩。 |
| 6. 票大小与可实施性 | **无发现**。票与设计批次一一对应，单票工作量适合单上下文完成；按阻塞顺序执行时每票独立可验证、可交付；无宽重构型票（UML 同步为单文件随票 00、API Client 重生成为生成物核对）。 |

**「设计本身如此」事项说明（不进入发现列表）**：院区为空按全院与不进入院区层解析（V13 明文）；10 万行收窄与内存峰值不构造（S6-D5、S6-D16）；占比分母（S6-D3）与科室维度不可对账（S6-D17）；患者原值（S6-D9）；票 09 三类定案（testing-baseline `NotRun`/`Blocked` 定义相容）；票 01「两处定义逐字一致」为表结构变更双写规则的具体化。

**未覆盖范围及理由**：`阶段6-审查相关/` 全部内容（含本文件与其他审查方文件，独立审查纪律）；`阶段6-讨论内容.md`（票审查排除范围）；仓库代码实现（阶段未开工；本轮仅按证据核对需要只读查看了 `HasAnyScope` 的存在性，未审实现）；其他阶段阶段文档与 `var/` 目录（排除范围）。

**总体结论**：票拆分经两轮审查收敛，当前未再发现票缺陷、遗漏、风险或疑问；票集合与第四轮定稿设计及 V1-V45/C1-C30 矩阵完整对应，可按阻塞顺序进入执行。
