# 03: 接收侧汇总与明细查询

**What to build:** 让管理端的接收侧互认使用统计可用：平台管理员按请求范围（接收组与来源组两组可选筛选，空取值不附加该层过滤、全空即按授权全量，见 S6-D18）、医院管理员按可信上下文注入（本侧院区可选、为空按可信医院全院且不进入院区层解析）查询接收侧汇总与明细；汇总按单一维度分组（医院/院区/互认科室/标准项目），行内六项指标按各自业务时间列独立过滤同一日期范围，同期互认率与占比在应用层由行内次数计算（提醒为零不计算），不采纳原因汇总嵌入行内（其他原因归入其他），互认科室按院区+ID 归类、展示名称取范围内互认时间最晚处理结果名称；明细按提醒/采纳/不采纳/引用四类型出行业务时间（未反馈项只进提醒明细并显示未反馈，不计入互认科室维度分组），组织、医院、院区名称按当页编码批量回填（调用次数不随行数增长）；来源组与接收组组织编码不一致时业务拒绝。

**Status:** done（实施审查第4轮通过，F-01 提醒明细决策列、F-02 部分空层范围组合、F-04 本院版窗口用例、F-05 设计表路径更正、F-06 注释统一均已修复；F-03 perl 过程违规已核验等价、是否回退由负责人裁定；真实库面归票 09）

**Blocked by:** 02

**Status:** ready-for-agent

- [ ] 分页窗口校验早于仓储访问，非法窗口业务拒绝（V11）
- [ ] 平台版范围按请求并经组织路径解析校验；本院版注入、院区可选为空全院且不进入院区层解析、非空校验属于可信医院；对侧组筛选限可信组织（V12、V13）
- [ ] 汇总行结构：每行为所选维度一个分组值、未选维度为 `null`、无总计行（V14）
- [ ] 提醒按匹配生成时间、采纳/不采纳/金额按互认时间、引用按实际引用时间，同一日期范围各指标独立过滤（V15、V16、V17、V21）
- [ ] 同期互认率行内计算、提醒为零不计算；短周期率可超百分之百原始展示（V18）
- [ ] 原因汇总嵌入行内、行内不采纳为零不计算占比、其他归入其他（V19）
- [ ] 互认科室按院区+ID 归类、展示名称取最晚互认时间处理结果名称、明细保留各记录自身名称（V20）
- [ ] 空结果空页成功；计数与数据同筛选；越界页行为符合公共分页口径（V22）
- [ ] 排序稳定唯一，跨页无重复无遗漏（V23；真实库跨页行为面随票 09 真实库批次回库核对，本票以替身取证语句与排序键）
- [ ] 名称批量回填次数不随行数增长（V24）
- [ ] 汇总数字与下钻明细计数一致（各自时间列口径下）（V25）
- [ ] 明细四类型业务时间、未反馈语义、明细筛选、明细分页排序、患者与人员字段原样（V26-V30）

**口径：** 未反馈项无科室归属是事实结构所致（S6-D17 已登记 AcceptedRisk）；占比分母取行内不采纳次数为对 SRS 业务规则 11 的确认偏差（S6-D3）。
