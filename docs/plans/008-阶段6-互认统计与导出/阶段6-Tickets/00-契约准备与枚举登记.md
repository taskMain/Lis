# 00: 契约准备与枚举登记（含 UML 5 同批同步）

**What to build:** 让阶段 6 的对外契约具备可实施形态：十一个请求类型（四个查询能力 × 平台/本院双版本、平台/本院两个导出请求、匹配记录视图请求）按双入口口径声明——平台版含本侧与对侧两组组织/医院/院区可选筛选，本院版不含本侧组织与医院字段、本侧院区可选（为空按可信医院全院）、携带对侧组医院/院区可选筛选（S6-D1）；六个顶层统计读模型及其嵌套子模型由 `object` 占位重建为强类型只读属性，患者信息以姓名+证件号码结构化字段承载，决策用互认结果枚举承载并提供文本计算属性，两个汇总读模型补标准项目名称、接收侧汇总读模型补汇总维度字段（S6-D13）；汇总维度、明细类型两个新枚举补描述器特性与中文说明，连同导出类型枚举登记描述器注册表，同批更新 `Stage2EnumContractTests` 的 `ExpectedEnumNames` 清单并补三个枚举取值、成员名与中文说明冻结断言（S6-D14）；既有端点清单、分部文件清单与分页禁用字段断言同批同步；UML 5 按 S6-D13 扩展范围同批同步（患者字段结构化、决策枚举承载、汇总读模型名称与维度字段、双入口/导出 POST/分页对象结构偏差）。

**Blocked by:** 无票间阻塞；外部前置为设计确认（根 `design.md` 台账与 Server/Client 设计经负责人确认，见 README「外部前置」）

**Status:** ready-for-agent

- [ ] 查询契约 Statistics 分片恰含 8 查询 + 1 导出 + 1 匹配记录视图共 10 个方法（宿主 11 个端点，导出由控制器平台/本院两个 POST 动作承载）；请求字段集合与双入口口径逐项相符，导出请求含必填汇总维度与可选不采纳原因代码、不含分页对象（V1）
- [ ] 汇总维度恰四值、明细类型恰四值、导出类型恰七值，均带描述器特性与中文说明；来源侧维度子集校验适用于导出链（V2、V3）
- [ ] 三个枚举登记描述器注册表；`ExpectedEnumNames` 同批更新并补三个枚举取值、成员名与中文说明冻结断言；排除清单保持为空且未放宽其他断言（V4）
- [ ] 六个顶层统计读模型及嵌套子模型重建：强类型只读、无 `object`、无 `MaskedPatientInfo`、决策枚举 + 文本计算属性、汇总读模型含标准项目名称与（接收侧）汇总维度字段（V5）
- [ ] 四个查询请求内嵌 `page` 对象（一基页码、页容量 1–200），响应 `items` + `page`，禁用字段不出现（V6）
- [ ] 既有断言同批同步未放宽：查询入口方法集合、分部文件清单与数量、分页禁用字段断言
- [ ] `docs/uml/5-RecognitionStatisticsUiQuery.wsd` 按 S6-D13 扩展范围同批同步完成
- [ ] `git diff --check` 干净

**口径：** 本票覆盖契约与静态面（矩阵层级 `Contract`/`Static`），不产生运行期证据；读模型的运行期映射面由票 03-05 承接。请求字段以根设计「双入口形态」段与 S6-D1 为权威口径。
