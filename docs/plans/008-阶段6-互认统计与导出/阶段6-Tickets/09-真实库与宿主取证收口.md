# 09: 真实库与宿主取证收口

**What to build:** 让阶段 6 的交付在真实数据库与真实宿主上取得最终证据并收口：四个查询与导出、匹配记录视图在真实库的成功面与只读回库核对（数据只经阶段 4/5 接口与本阶段接口按业务顺序构造）；分页首中末页与越界行为、目标库三条统计索引核对（负责人执行迁移后）；宿主链路十一个端点未认证 401、宿主导出下载（`.xlsx` 可打开、内容与页面口径一致）、组织服务失败面；四个页面从宿主登录页与菜单进入完成真实验收（平台/本院 × 接收/来源）、范围隔离宿主面、宿主失败态；结果、证据、未执行项与残余风险写入阶段根 `testReport.md`，无实际证据不写通过。

**Blocked by:** 06、08

**Status:** ready-for-agent

- [ ] 真实库四查询成功面与只读回库核对一致（V41）
- [ ] 真实库分页行为：计数与数据同筛选、跨页无重复无遗漏（V42；含 V23 真实库跨页面，承接票 03 的委托）
- [ ] 目标库三条统计索引存在且列定义与迁移脚本一致（V43）
- [ ] 宿主导出下载链路：真实 `.xlsx` 可取回可打开、失败宿主统一提示（V44）
- [ ] 组织服务失败面：范围解析或名称回填失败整次失败、按原样抛出（V45）
- [ ] 宿主未认证：十一个端点不带凭据均 401（V40）
- [ ] 四条路由宿主接入与四页面真实验收（C23-C27）
- [ ] 宿主导出下载与宿主失败态（C28、C29）
- [ ] 范围隔离宿主面：本院页不可越权，跨组织面按 S6-D18 口径记录（C30）
- [ ] 阶段根 `testReport.md` 按条目构造口径定案，残余风险与悬空项（S6-D9 阶段 7 复核、S6-D13 UML 同步状态）如实登记

**外部前置：** 三个统计索引已由负责人按票 01 迁移文件建成并完成确认；宿主可用且外部服务代理映射就位。两测试身份（组织 `01` 下 `0101` 与 `CSYY2`）用于互认双方构造与范围隔离；不构造空组织/医院/院区账号类用例（S6-D18）。

**口径：** 按「条目构造口径」三类定案：业务可达且静态判据盖不住则取证；正常业务不可达记 `NotRun` 并写明理由；已由静态判据冻结且无运行期增量记 `NotRun`。不直接写库、不用脚本注入数据。
