# 01: 索引迁移与导出库声明

**What to build:** 让三个统计索引具备可执行的迁移文件并与建表脚本保持一致（表结构变更双写规则）：`mrec_recognition_match_record` 按接收三值加互认匹配生成时间、`mrec_recognition_processing_result` 按互认时间、`mrec_recognition_reference` 按实际引用时间；迁移文件按 LisCenter 先例命名（`Scripts/Migrations/<日期>_s6_statistics_indexes.sql`）且同步回写三份对应建表脚本，两者均含索引中文注释、不加 `if not exists`、不改既有列与既有索引；匹配记录表新索引与既有 business_key 索引的列序差异（时间列在证件列之后、不能支撑范围扫描）在设计中已写明，本票以断言冻结。同时把 ClosedXML 以集中包管理声明并在 Application 工程显式引用（导出组装在应用层，S6-D7）。

**Blocked by:** 00

**Status:** done（静态面交付完成，实施审查第2轮通过，F-01/F-02 已修复；DDL 由负责人执行，V7 的 DB 面与 V43 在执行前记 `Blocked`）

- [x] 迁移文件含三条索引 DDL 与逐条中文注释，命名符合 `YYYYMMDD_s6_statistics_indexes.sql` 形态（V7 静态面）
- [x] 三份对应建表脚本同步含三条索引，迁移文件与建表脚本两处定义逐字一致；无 `if not exists`；既有列与既有索引未变（V7 静态面）
- [x] 匹配记录表新索引与既有 `ix_mrec_recognition_match_record_business_key` 的列序差异有静态断言（时间列位置与范围扫描能力）（V7）
- [x] 集中包管理新增 ClosedXML 0.105.0（本机 NuGet 缓存）并在 Application 工程显式引用；未引入其他 Excel 库（V8）

**外部前置：** 迁移文件由负责人在本票交付后执行最终 DDL；执行完成确认前，依赖索引性能面的条目（目标库索引核对等）记 `Blocked`，不影响本票静态面取证。

**口径：** 本票只交付脚本与依赖声明的静态面（`Static`），不在真实库执行任何 DDL；数据库变更一律由负责人执行。
