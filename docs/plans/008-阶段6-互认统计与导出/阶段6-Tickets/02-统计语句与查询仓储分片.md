# 02: 统计语句与查询仓储分片

**What to build:** 让全部统计查询具备方言中立的取数语句：在既有查询作用域 `MedicalRecognitionReportQuery` 下补齐 17 条语句（接收侧汇总分组计数/当页分组/当页原因汇总、来源侧汇总分组计数/当页分组、接收侧提醒/采纳/不采纳/引用明细的计数与取页各一对、来源侧明细计数与取页一对、匹配记录视图组级与匹配项两条），条件聚合按各指标自身业务时间列过滤同一日期范围（CASE 条件聚合，原因汇总独立成行避免方言字符串聚合），计数语句与数据语句共用同一套筛选条件，排序稳定唯一、分页窗口交 Provider 适配层；查询端口与实现新增 Statistics 分片，分页窗口校验接入；全部新语句登记进既有语句清单与跨文件列一致性守卫。

**Blocked by:** 00、01

**Status:** done（实施审查第3轮通过，F-01 子查询范围限定与 F-02 恒真断言已修复；F-03 守卫拦截面接受登记；F-04 项目类型筛选列同源口径移交票 03 裁定；F-05 原因行全量取回、应用层装配当页的口径接受登记；真实库行为面归票 09）

- [x] 语句标识与端口方法名一一对应（方法名去掉 `Async` 即语句标识），作用域为 `MedicalRecognitionReportQuery`（本票语句面静态判据，随票 06 的 V9 同批复核）
- [x] 语句清单守卫与跨文件列一致性守卫登记全部新语句（本条静态面在票 06 复核）
- [x] 排序稳定唯一；XML 无方言分页、方言聚合、JSON 运算符与系统表（本票语句面静态判据）
- [x] 汇总分页作用于聚合后的分组行：分组行计数语句与当页语句同筛选（本票语句面静态判据）

**口径：** 本票交付语句与映射静态面及仓储分片，行为面（聚合口径、范围解析、率与占比计算）由票 03-05 取证；语句清单守卫的首次登记随本票同批完成（与清单同步段口径一致）。批次 2 在设计中前置批次 1（索引迁移与包声明先行），本票与票 01 串行执行；语句编写本身对索引与包声明无产物依赖。
