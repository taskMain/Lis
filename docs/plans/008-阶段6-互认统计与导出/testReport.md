# 阶段 6 测试报告

**当前状态：阶段 6 收口（2026-09-22）。** 票 00-09 全部完成并通过实施审查（共 10 轮，记录见 [阶段6-实施审查结果](阶段6-审查相关/阶段6-实施审查结果.md)）：后端矩阵 V1-V45 与前端矩阵 C1-C30 逐条取得证据或按条目构造口径登记（取证 / `NotRun` / 降级），全部批次状态已在三份实施文档登记。全量后端测试 767/0/0、前端 352 通过 + 2 既有跳过、双端构建 0 错误、`git diff --check` 干净（收口整改后基线）。收口裁定与整改记录见本文「登记不修与处置记录」「收口裁定登记」两节；残余风险与悬空项见「问题与残余风险」，其中跨医院互认匹配候选查询事项属阶段 5 交付面，按阶段 5 流程处理，阶段 6 按本轮既有事实边界收口。阶段 0-6 交付物未提交，提交时点与范围由负责人决定。

测试前矩阵见 [后端设计](Server/design.md) 的验证矩阵章节（V1-V45）；批次与矩阵编号的对应见 [实施](impl.md)、[后端实施](Server/impl.md) 与 [前端实施](Client/impl.md)。批次 0 至批次 6（票 00-08）的静态面、接口约定面与应用层替身面证据由各批次交付与实施审查记录承载，本报告不重复登记。

## 执行边界

- 数据形成口径：统计与导出的事实只经阶段 4 的报告提交接口与阶段 5 的匹配、处理结果、引用接口按业务顺序构造；不直接写库、不使用脚本注入数据。本轮对数据库只做只读核对（`information_schema`、`pg_indexes`、`pg_index`/`pg_attribute` 与业务表 `SELECT`）。
- 三个统计索引已由负责人按迁移文件 `server/Dy.MedicalRecognition.Repository/Scripts/Migrations/20260921_s6_statistics_indexes.sql` 建成（票 01 交付、负责人执行），V43 据此取证。
- 本阶段无写用例：统计链零写入由批次 6 的静态守卫取证，本轮不重复。
- 不构造需要空组织、空医院或空院区账号的条目（权限系统限制，正常业务不可达，S6-D18）；账号级跨组织真实宿主面不可构造（仅同组织两测试账号），以应用层与平台页证据为准。
- 开发期冗余、孤岛或错误数据不作为缺陷处理；本轮造数产生的 1 条冗余匹配记录按此口径登记。
- S6-D9 患者原值口径与 S7-D8（阶段 7 设计台账，患者信息全平台按业务原值展示，出处 `docs/plans/009-阶段7-横向治理与发布收口/design.md` 关键决策表）的全平台口径一致，实现已复核一致，该项按统一口径关闭；S6-D13 的 UML 5 同步已随批次 0 完成（读模型字段与结构形态偏差已同步，文件随阶段交付未提交）。

## 票 09：真实库与宿主取证（取证完成）

### 运行环境与本轮口径

| 项 | 本轮实际值 |
|---|---|
| 起始工作区 | `main`、跟踪 `origin/main`、领先 3 个提交；阶段 0-6 全部改动未提交，本轮未创建提交、分支或 worktree，既有未提交改动一律保护 |
| 后端 | `dotnet run --project Dy.MedicalRecognition`，监听 `http://localhost:15014`，工作目录 `server/`，`ASPNETCORE_ENVIRONMENT=Development`；OpenAPI 与 Scalar 可访问 |
| 前端 | `pnpm -F dy-medical-recognition dev`，监听 `http://localhost:3008`，basename `/subApps/medical-recognition` |
| 宿主 | `http://183.224.180.166:35000` 登录页登录，登录入口「检验检查结果互认平台」；开发地址拦截映射 `medical-recognition → http://localhost:3008` 两级启用，生效证据为 Console `[vite] connected.` 与 Network 中子应用资源来自 `localhost:3008`（`@vite/client`、`src/main.tsx`、`src/pages/recognitionStatistics/*.tsx`） |
| 测试身份 | `yangkj`（组织 `01`/医院 `0101`/院区 `0101001`，宿主显示名 杨康健）与 `lisadmin`（组织 `01`/医院 `CSYY2`/院区 `CSYQ2-1`，宿主显示名 检验协同平台管理员）；各自独立浏览器隔离上下文登录，token 只存在于会话内，不写入任何文件、不写入宿主认证存储 |
| 菜单 | 宿主菜单「检验检查结果互认」下登记四项：接收医院互认使用统计、本院互认使用统计、来源医院被互认统计、本院被互认统计（两身份均可见） |
| 浏览器与控制工具 | Chrome DevTools MCP（Chrome 153，MCP 管理实例，不固定日常 Profile）；`lisadmin` 使用 MCP `isolatedContext` 隔离会话；接口造数在宿主登录会话的页面上下文内携带会话令牌调用 `localhost:15014`（沿用阶段 4/5 取证方式） |
| 目标库 | dbx 只读连接 `DysoftHIS` 的 `public`；仅执行元数据与业务数据 `SELECT` |
| 进程收尾 | 后端与前端 dev 进程在本轮结束时停止，`15014`、`3008` 端口释放（仅余 TIME_WAIT） |
| 证据留存 | `var/st6/`（不纳入版本控制）：`v40_unauthenticated.txt`（11 端点未认证状态码）、`export-branch-summary-20260801-20260930.xlsx`（8906 字节）、`export-branch-details-20260801-20260930.xlsx`（19101 字节）、`dump-schema.js`（schema 探测辅助） |
| 注入窗口 | V45 运行期取证开启过一个注入窗口并已还原：以启动环境变量把 `IOrganizationAppService` 代理覆盖为未监听端口 `http://localhost:15999`，取证后还原并复跑确认恢复（同一请求还原前 500、还原后 200） |

### 取证过程中等价修复的两处交付缺陷（显著说明）

| 缺陷 | 现象 | 修复 | 修复后复验 |
|---|---|---|---|
| 导出控制器 DI 注册缺失 | 宿主页面发起任何导出，`POST /api/v1/statistics-export/branch` 返回 500：`Unable to resolve service for type 'MedicalRecognitionReportQueryAppService' while attempting to activate 'RecognitionStatisticsExportController'`。根因：控制器构造函数按具体类注入（本院导出入口未声明在接口上），框架扫描只注册接口形态。该缺陷阻塞 V44/C28 全部取证 | `server/Dy.MedicalRecognition.Application/MedicalRecognitionApplicationModule.cs` 的 `OnPostConfigureServices` 补一行 `AddScoped<MedicalRecognitionReportQueryAppService>()`，纯 DI 注册补齐，无行为与接口约定变化 | 同一导出请求返回 200 与完整工作簿；零记录请求返回宿主统一提示「业务拒绝：当前条件下无可导出数据。」 |
| 冻结 OpenAPI 端点清单未同步 | `Stage2EnumContractTests.Frozen_openapi_keeps_the_expected_path_set_and_no_unnormalized_shapes` 失败：冻结清单停留在阶段 4 快照（39 条），票 07 重生成后的快照含 54 条（阶段 6 的 11 个端点 + 按累计规则补齐的阶段 5 已交付 4 个端点）。该用例备注载明「新增或删除端点时必须同步本清单与生成入口」，属批次 0「既有断言同批同步」义务的遗漏 | 按实际交付快照逐条同步 `server/Dy.MedicalRecognition.Tests/Stage2EnumContractTests.cs` 的冻结清单（39 → 54 条），并把备注中的基准措辞更新为阶段 6 交付后 | 该用例通过；清单与快照逐条等值 |

### 登记不修与处置记录

| 事项 | 事实 | 处置与边界 |
|---|---|---|
| 跨医院互认匹配在候选查询层不可达（待裁定） | 候选报告查询（`server/Dy.MedicalRecognition.Repository/Queries/MedicalRecognitionReportQuery.xml` 第 656-658 行）在 SQL 层强制 `organization_code`、`hospital_code`、`branch_code` 三值等于接收三值；同一行注释写明「来源医院：与接收医院是否相同决定本院报告是否参与匹配，判定在应用层完成」。SQL 过滤使应用层的本院报告判定空转，跨医院报告永不进入候选。属阶段 5 交付面 | 本轮以「本院报告匹配（超排除时长 4 小时）」构造全部互认事实，两个测试身份各自在本医院内形成完整事实链，来源侧统计仍取到真实事实；未改动阶段 5 代码与语句。修复需按阶段 5 流程处理并回补跨院匹配取证，裁定前本阶段统计的既有事实边界不变 |
| 阶段 5 受保护路径守卫与阶段 6 前端交付冲突（已处置） | `Stage5ConstraintTests.Protected_configuration_and_client_sources_are_untouched` 的受保护差异清单含整个 `client` 路径，阶段 6 票 07/08 已授权前端交付在未提交期间与该清单必然冲突（765/766 常驻失败）；守卫要防的「再生成覆盖人工维护配置」并未发生 | 收口整改：`client` 移出受保护差异清单（其余 server 配置路径判定原样保留），人工维护面改由新增内容判据 `Api_client_package_keeps_the_human_maintained_surfaces` 承接——锚点覆盖包入口手写便捷创建、客户端构造、`prepare-openapi.mjs` 两处归一化计数与 `kiota-lock.json` 的 `/auth/login` 排除冻结，全部含变异证据；全量测试恢复 767/767 全绿。锚点为文本存在性判定，Kiota 大版本升级改写生成器写法时按错误信息补锚点即可 |

### 收口裁定登记

| 事项 | 结论 |
|---|---|
| 票 03 过程违规（perl 批量处理 18 行空白） | 接受终态，不要求重做；逐项核验等价、无语义变化，登记为过程记录 |
| 票 07 过程违规（两次 node 脚本改码） | 接受终态，不要求重做；终态经编译、无死代码、命名一致、无禁用模式、导出面全覆盖五项证据核验完整，登记为过程记录 |
| 导出下载文件名口径（C8） | 确认为既定口径：生成端不透出响应头，下载名由调用方按服务端命名规则（导出类型中文名-yyyyMMdd-yyyyMMdd.xlsx）构造，沿用阶段 4 PDF 下载先例；页面构造已与服务端 Content-Disposition 逐字一致（第 9 轮审查镜像核对） |
| `.zcodeignore`（仓库根未跟踪文件） | 纳入提交，随阶段交付一并提交 |

### 数据构造清单（全部经平台接口按业务顺序）

构造方式：在两身份各自的宿主登录会话页面上下文内调用 `localhost:15014`；报告提交为 `multipart/form-data`（`ReportJson` + 最小合法 PDF），匹配、处理结果、引用为 JSON 请求。任何一步未直接写库。

| 步骤 | 身份 | 接口 | 规模与关键标识 |
|---|---|---|---|
| 报告 R01-R22 | `lisadmin` | `POST /api/v1/report-pdf/laboratory-report` | 22 份检验报告，报告号 `MR-ST6-R-20260922-01..22`，来源三值 `01/CSYY2/CSYQ2-1`，患者 陈互认/`530101198501012201`，就诊 `VISIT-ST6-R01..R22`，检验检测完成时间 2026-08-28 至 2026-09-18 逐日分布（均超本院排除时长、未超 30 天可互认时间），标准项目编码 `123` |
| 报告 S01-S22 | `yangkj` | 同上 | 22 份检验报告，报告号 `MR-ST6-S-20260922-01..22`，来源三值 `01/0101/0101001`，患者 林来源/`530101198601012202`，就诊 `VISIT-ST6-S01..S22`，时间分布同上 |
| 匹配（CSYY2 侧） | `lisadmin` | `POST /Api/MedicalRecognitionReport/RequestRecognitionMatches` | 22 条匹配记录（本院报告匹配），逐次成功 |
| 匹配（0101 侧） | `yangkj` | 同上 | 23 条匹配记录：22 条计划内 + 1 条探测阶段产生的冗余记录（`VISIT-ST6-S01` 重复匹配，开发期冗余，保留为未反馈事实） |
| 处理结果（0101 侧） | `yangkj` | `POST /Api/MedicalRecognitionReport/SubmitRecognitionProcessingResults` | 20 条：18 条采纳（互认科室 `ST6-D01..D18`，预计节省金额各 12.34）+ 2 条不采纳（`ST6-D19` 原因 3；`ST6-D20` 原因 6 携带补充说明）。首轮因互认时间取 UTC 墙上时间早于匹配生成时间被整批拒绝（业务拒绝语义的真实取证面），改本地无时区格式后全部成功，拒绝轮零写入 |
| 处理结果（CSYY2 侧） | `lisadmin` | 同上 | 14 条：12 条采纳（`ST6-E01..E12`）+ 2 条不采纳（`ST6-E13` 原因 2；`ST6-E14` 原因 6 携带补充说明）。R11-R14 首轮因互认时间整秒取值早于带微秒的匹配生成时间被拒，稍后重发成功，拒绝轮零写入 |
| 引用（0101 侧） | `yangkj` | `POST /Api/MedicalRecognitionReport/SubmitRecognitionReferences` | 5 条（`ST6-D01..D05` 对应采纳项） |
| 引用（CSYY2 侧） | `lisadmin` | 同上 | 3 条（R01-R03 对应采纳项） |
| 未反馈 | — | — | 0101 侧 3 项（含冗余项）、CSYY2 侧 8 项保持未反馈 |

只读核对基线（dbx，期间 2026-08-01 至 2026-09-30，`VISIT-ST6-%`）：0101 侧匹配记录 23、匹配项 23、处理结果 20（采纳 18/不采纳 2）、引用 5；CSYY2 侧 22/22/14（12/2）/3。

### 条目状态表

状态词只用 `NotRun / Passed / Failed / Blocked / PendingRetest / N/A`；`Passed` 只写在有本轮运行证据的条目上。矩阵编号与用例定义见 [后端设计](Server/design.md) 验证矩阵（V40-V45）与 [前端测试前矩阵](Client/testPlan.md)（C23-C30）。

| 编号 | 用例 | 状态 | 本轮证据 |
|---|---|---|---|
| V40 | 未认证请求 | `Passed` | 十一端点（9 自动端点 + 2 导出 POST）不带凭据逐个 POST，全部返回 401；证据 `var/st6/v40_unauthenticated.txt` |
| V41 | 真实库四查询成功面与只读回库核对 | `Passed` | 四查询（`QueryRecognitionUsageSummary`、`QueryRecognitionUsageDetails`、`QuerySourceRecognitionSummary`、`QuerySourceRecognitionDetails`）全部 200 且带真实数据。回库核对全部一致：互认科室维度 `ST6-D01..D20` 20 行逐行对照（提醒/采纳/不采纳/引用/金额与 SQL 聚合逐格相等，D19/D20 不采纳原因 3 与 6 及占比随行返回）；医院维度 0101 总行 77/28/3/9 与 SQL 相等；CSYY2 侧 23/12/2/3 与 SQL 相等；平台明细四类型合计 100/40/5/12 与两院 SQL 合计相等；来源侧汇总 0101=28、CSYY2=12 与 SQL 相等。匹配记录视图（`QueryRecognitionMatchRecord`）按明细行标识取单记录 200，组级信息、患者原值、就诊、反馈状态、决定主体与全部匹配项齐备 |
| V42 | 真实库分页行为（含 V23 真实库跨页委托） | `Passed` | 接收侧汇总（互认科室维度，总数 35）：页容量 7 取 5 页（7/7/7/7/7），跨页合并与全量清单逐键相等、无重复无遗漏、顺序稳定；越界第 6 页返回空 `items` 与真实总数 35。接收侧提醒明细（总数 100）：页容量 10 取全部 10 页，无重复无遗漏，越界第 11 页空页与真实总数。来源侧明细（总数 40）：页容量 12 取 4 页（12/12/12/4），无重复无遗漏。来源侧汇总（总数 2）：页容量 1 首末页分别返回 0101 与 CSYY2，越界第 3 页空页与真实总数。各页 `totalCount` 与计数语句同筛选恒等 |
| V43 | 目标库三条统计索引核对 | `Passed` | `pg_indexes` 确认三条索引存在；`pg_index`/`pg_attribute` 取列序：`ix_mrec_recognition_match_record_receiver_time`（接收组织编码、接收医院编码、接收院区编码、互认匹配生成时间）、`ix_mrec_recognition_processing_result_recognition_time`（互认时间）、`ix_mrec_recognition_reference_referenced_time`（实际引用时间），列定义与迁移脚本逐列一致 |
| V44 | 宿主导出下载链路 | `Passed`（10 万行上限面 `NotRun`） | 从宿主页面发起：汇总导出（导出类型 1）经浏览器真实下载保存 8906 字节（与响应 `content-length` 一致），提醒明细导出（导出类型 2）保存 19101 字节；两文件 Content-Type 为 xlsx MIME、`cache-control: no-store, no-cache, must-revalidate`；文件名为「导出类型中文名-yyyyMMdd-yyyyMMdd.xlsx」（`接收侧互认使用汇总-20260801-20260930.xlsx`、`接收侧提醒明细-20260801-20260930.xlsx`，`filename*=UTF-8''`）；两文件可解包打开（zip 完整性通过），汇总簿 22 行 = 表头 + 21 组（与页面汇总 21 组一致，含 18 处金额 12.34），明细簿 78 行 = 表头 + 77 行（与页面明细 77 条一致，携带互认匹配记录 ID 与互认匹配项 ID 列，未反馈行与患者原值在列）；零记录（期间 2020-01-01 至 2020-01-31）宿主统一提示「业务拒绝：当前条件下无可导出数据。」。接近 10 万行上限的真实规模导出不构造，按 S6-D16 既有口径随真实业务量增长复核 |
| V45 | 组织服务失败面 | `Passed` | 应用层替身面：`Stage6StatisticsQueryTests` 范围越界三条用例（`Usage_summary_rejects_scope_outside_the_organization_tree` 等）过滤执行 3/3 通过，范围解析零结果整次业务拒绝。运行期面（沿用阶段 5 注入先例）：`IOrganizationAppService` 代理覆盖为 `http://localhost:15999` 后，本院汇总端点返回 HTTP 500，异常按原样抛出（`HttpRequestException` → `SocketException 10061`，无降级、无部分结果），未认证对照仍 401；还原后同一请求 200（医院维度行 77/28 与注入前一致）。沿用阶段 4 先例：真实外部组织服务的故障注入面以等价替身证据与本次受控注入共同承载，不再作为独立取证项 |
| C23 | 路由接入 | `Passed` | 四条菜单子项在两身份的宿主菜单中登记；`yangkj` 会话从菜单真实打开全部四页（接收医院互认使用统计、本院互认使用统计、来源医院被互认统计、本院被互认统计），`lisadmin` 会话打开两个本院页；页面资源来自 `localhost:3008` 且 Console `[vite] connected.`，页面标题与四路由对应 |
| C24 | 宿主验收：平台接收侧页 | `Passed` | 从宿主菜单进入，平台版组织/医院/院区与来源组选择器齐备（本侧选择器联动禁用）；期间 2026-08-01 至 2026-09-30 汇总返回两院两行（县人民医院 36.36%、测试医院二 52.17%，与只读核对一致），不采纳原因行随组展示；明细共 100 条含两院数据。下钻联动为与 C25 同一组件，已在 C25 逐项交互取证 |
| C25 | 宿主验收：本院接收侧页 | `Passed` | 从宿主菜单进入，可信范围只读展示 组织 01/医院 0101；期间与维度切换触发重查（医院维度 36.36% = 28/77、互认科室维度 21 组与接口一致）；汇总翻页（第 2 页换页成功）；点击指标数字下钻（科室筛选预填 `ST6-D01`、明细 1 条）；明细翻页（77 条 8 页，第 2 页首行 `VISIT-ST6-S12` 与倒序一致）；匹配记录弹窗（组级信息、患者原值、就诊、反馈状态、决定主体、全部匹配项，无 PDF/影像入口）；科室筛选填入不存在值显示空态、无报错、清除后恢复 |
| C26 | 宿主验收：平台来源侧页 | `Passed` | 从宿主菜单进入，来源医院维度汇总两行（0101 被互认 28、CSYY2 被互认 12，与 SQL 一致）；明细共 40 条（被互认明细含来源/接收三值、科室、医生、互认时间、患者原值）；页面无金额列 |
| C27 | 宿主验收：本院来源侧页 | `Passed` | 从宿主菜单进入，来源范围固定可信医院；汇总 1 行（本院被互认 28）、明细共 28 条；页面无金额列 |
| C28 | 宿主导出下载 | `Passed` | 同 V44：两类导出真实下载、可打开、内容与页面口径一致、零记录宿主统一提示 |
| C29 | 宿主失败态 | `Passed` | 停止本地后端后点击「重新读取统计数据」：汇总与明细各出「待刷新／读取失败，已有数据保留；可重试读取。」提示与「重试」按钮；已有数据保留（汇总 77/28/3/9 与金额、明细 77 条分页不变）、加载态结束、无本地接口错误文本。恢复后端后分别点击两处「重试」：提示全部消失，汇总与明细按同条件重载成功（首行 `VISIT-ST6-S22`） |
| C30 | 范围隔离宿主面 | `Passed`（跨组织面按 S6-D18 记录） | `lisadmin` 会话：本院互认使用统计可信范围只读展示 组织 01/医院 CSYY2，汇总仅 1 组（测试医院二，52.17% = 12/23 与只读核对一致）、明细共 23 条，无 0101 侧数据（无县人民医院、无 `ST6-D` 科室、无林来源）；本院被互认统计汇总 1 组、明细共 12 条，无越权数据；医院/院区选择器禁用，页面无跨院选择入口。`yangkj` 会话对应页面仅见 0101 数据。账号级跨组织真实宿主面不可构造（仅同组织两测试账号），平台页跨组织可见性以 C24 的两院数据展示为准，按 S6-D18（[阶段设计](design.md) 关键决策表）登记 |

### 用例状态统计

- 票 09 取证面 16 条（V40-V45 六条、C23-C30 八条，V44 含 1 个 `NotRun` 边界、C30 含 1 个按决策登记的降级面）：`Passed` 16 条，其中 V44 的「接近 10 万行上限规模」子面与 C30 的「账号级跨组织宿主面」子面分别按 S6-D16 与 S6-D18 记 `NotRun`／降级登记，不构成整条失败或阻塞。
- 全量后端测试：767 条全部通过（收口整改后基线：受保护路径守卫按「`client` 移出差异清单 + 内容判据承接」调整并新增 1 条内容判据用例，守卫常驻失败消除）。构建 0 错误；`git diff --check` 无空白错误（仅既有 CRLF 提示）。
- GitNexus `detect-changes -r Dy.MedicalRecognition -s unstaged`：52 个文件、2181 个符号、7 条受影响流程、风险等级 high——分母覆盖阶段 0-6 全部未提交交付而非仅本轮；受影响流程全部属于本阶段统计与导出链（含本轮触达的导出读模型与匹配记录视图链），未命中 GitNexus 工作流的停止条件。

## 问题与残余风险

| 风险 / 悬空项 | 说明 | 重审条件 |
|---|---|---|
| 跨医院互认匹配不可达（登记不修） | 见上文「登记不修与处置记录」表：候选查询 SQL 强制来源三值与接收三值一致，应用层本院报告判定空转。阶段 6 的互认双方构造、来源侧「跨来源医院」语义与 V36 类跨院拒绝面均受此边界约束 | 按阶段 5 流程修正候选查询过滤并回补跨院匹配取证，裁定前本阶段统计的既有事实边界不变 |
| 超过 10 万行的真实导出未构造 | V44/C28 的上限拒绝面由应用层用例与静态判据承载，真实规模按 S6-D16 已接受风险不构造 | 接近上限的真实业务量出现后实测 |
| 导出下载的二次自动下载拦截 | 自动化浏览器对同一会话的第二次程序化下载按「多次自动下载」策略拦截；本轮以「释放首个下载槽位后重试」完成明细导出文件的保存。真实用户场景首次导出不受影响 | 仅影响自动化取证方式，无需产品处理；后续取证沿用槽位释放做法 |
| 隔离上下文的开发拦截映射需按上下文配置 | MCP 隔离上下文的 localStorage 与默认上下文隔离，`lisadmin` 会话需补写本项目拦截映射后子应用方可加载；为环境配置事项（沿用 [Test Environment](../../.agents/instructions/test-environment.md) 第 4 节登记值），非产品缺陷 | 仅影响自动化多账号取证配置，登记备查 |
| S6-D9 患者原值口径复核 | 按 S7-D8（阶段 7 设计台账，患者信息全平台按业务原值展示，出处 `docs/plans/009-阶段7-横向治理与发布收口/design.md` 关键决策表）的统一口径复核，实现与该口径一致，该项按统一口径关闭 | 无 |
| S6-D13 的 UML 5 同步状态 | 已随批次 0 同步（读模型字段、集合形态、结构形态偏差；`docs/uml/5-RecognitionStatisticsUiQuery.wsd` 已含集合形态与决策枚举承载），文件随阶段交付未提交 | 随阶段交付提交后消解 |
| 阶段 6 交付物尚未提交 | 阶段 0-6 全部改动（含本轮两处等价修复与 `var/` 归属）在工作区未提交；提交前按 GitNexus 工作流重跑变更检查 | 负责人决定提交时点 |
