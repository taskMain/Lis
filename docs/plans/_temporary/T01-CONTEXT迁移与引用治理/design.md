# T01 CONTEXT.md 迁移与引用治理（任务书）

## 0. 背景与目标

删除仓库根目录 `CONTEXT.md`（555 行，领域事实与决策记录的唯一载体）。删除前把其中无等价表述的事实并入 `docs/需求规约SRS.md`，然后把全仓库对它的引用改写为对 SRS 条款的引用或事实陈述。全仓库现状：`CONTEXT` 共 174 处引用、81 个文件（其中带 `.md` 后缀 161 处）。

本任务书是唯一权威范围。执行智能体不得超出本文件清单与规范行事；发现清单之外的情况，停止该点并回报，不自行扩大范围。

## 1. 负责人已裁定决策（原文照录，不得二次解释）

1. **事实去向**：CONTEXT.md 中无等价表述的事实（尤其 `_Avoid_` 清单）**并入 SRS**，不随删除废弃。
2. **历史证据不动**：历史证据类文档（testReport、审查结果、审查问题追踪、审查要求、讨论内容等）**一律不改**。
3. **阶段设计文档改写方式**：阶段设计文档中的引用**改引 SRS 条款号**（如「依据：SRS F04 业务规则 8」），不改写成内联事实。

## 2. 执行铁律（摘自 AGENTS.md，执行前必读）

- 开始前 `git status --short --branch`，保护已有修改；不回退、不清理非本任务内容。
- 不创建分支、不 commit、不 push。
- 数据库只读；本任务不涉库。
- 每处替换必须落在本任务书清单内；清单外文件一律不动。
- 最小改动：只替换引用与必要措辞，不顺手修订目标文件的其他内容（尤其 SRS——本轮只有执行智能体修改 SRS，且只准做并入 CONTEXT.md 事实与 §9.2 澄清补写两件事，§9.2 是负责人裁定的明确例外）。
- 禁止引用行号（`SRS:1234` 之类），一律用章节/条款号定位，行号会漂移。

## 3. 执行顺序

- **Step 0 逐条核对**：按 §4 映射表逐条核对 CONTEXT.md 与 SRS 等价性，产出《事实迁移核对表》（写在根 testReport.md 的「事实迁移核对表」节）：每条 CONTEXT 条目 → 结论（已有等价，给 SRS 章节锚点 / 需并入，给目标位置与并入草案）。
- **Step 1 SRS 并入**：仅对核对表结论为「需并入」的条目，按就近原则并入 SRS 对应章节；**已有等价的一律不再并入**（重复事实比缺失更糟）。并入时保持 SRS 原有文风（规约句式），不整段搬运 `_Avoid_` 原文格式。
- **Step 2 引用改写**：按 §5 清单与 §6 规范改写引用。顺序：AGENTS.md → `.agents/instructions/` → 角色人格文件（.zcode/.codex）→ docs 现行说明与 UML 注释 → 阶段设计/规格文档 → 代码注释。
- **Step 3 删除本体**：全部改写完成、§7 验证通过后，最后删除 `CONTEXT.md`。删除前先 `git status` 确认工作区只有本任务改动。
- **Step 4 交接**：按 §8 格式写 impl.md 与 testReport.md。

## 4. 事实迁移映射表（章节级，Step 0 逐条核对的框架）

SRS 章节结构（目标落位候选）：`定义、首字母缩写词和缩略语`（:51）、`假设与依赖关系`（:288）、`当前版本范围与排除项`（:320）、F01 标准医疗项目目录管理（:406）、F02 标准项目互认配置（:520）、F03 报告采集与生命周期（:737，含 F03.1 公共报告要求与生命周期 :741、F03.2 检验 :947、F03.3 检查 :1093）、F04 互认匹配查询（:1231）、F05 互认处理结果（:1413）、F06 获取引用详情（:1613）、F07 提交引用结果（:1775）、F08 互认统计与分析（:1917）、F09 互认统计导出（:2103）、F10 互认项目金额维护（:633）、`设计约束`（:2233）、`接口·医院接入接口`（:2281）、`修订历史记录`（:5）。

> 本节与映射表中的行号（`:807` 之类）是任务书起草时的 Step 0 工作参考，Step 1 插入内容后会整体漂移；实际执行一律先按章节名定位，再以节内条款号核对，禁止把行号写进任何产出文档（§2、§6 的规定不变）。§9.2 与 §7.6 已经改用条款号锚点。

| CONTEXT.md 段落 | 初步判断 | SRS 落位候选 |
|---|---|---|
| 4 条决策记录（:7–10，2026-09-01/08/08/21） | 规则本体已由各 F 章节承载 | 转化为 `修订历史记录` 条目；规则本身核对后不再并入 |
| 医院业务科室和人员统一规则 15 条（:12–30） | 部分已由 F03.1/F05/F07 承载（ID/名称成对、String、不补查等） | 逐条核对；缺口并入就近 F 章节业务规则 |
| 项目目录词汇表（标准医疗项目、组织医院院区、互认项目类型分类分组、角色与菜单授权、互认项目、可互认时间、院内项目、金额、标准目录不可用，:32–77） | 部分已有（F01/F02/F10、整体说明） | 定义类并入 `定义、首字母缩写词和缩略语`；规则类并入 F01/F02/F10 业务规则；`角色与菜单授权` 候选 `假设与依赖关系` |
| 报告采集全段（检验/检查各条目，:79–430） | F03.1/F03.2/F03.3 覆盖面大，阶段 4 自审记录称 CONTEXT 既有结论均已在设计中反映 | 逐条核对；缺口并入 F03 就近小节 |
| 医院 HIS 互认接入边界、报告变更处理边界、报告使用关系（:418–428） | 部分已有 | F04 需求背景或 `接口·医院接入接口`；就近并入 |
| 批量互认匹配查询、互认匹配记录、最近一次互认匹配、互认匹配返回内容（:430–452） | 部分已有 | F04 业务规则/备选流就近并入 |
| 本院报告匹配开关（:442–445） | **已知已同步**（阶段 5 S5-D4 记录：已并入 SRS F04 业务规则 8、9 与设计约束 17） | 核对确认即可，不重复并入 |
| 互认处理结果、引用详情、引用结果（:454–475） | **引用详情参数段已知已同步**（阶段 5 S5-D4）；互认次数口径已知在 F07 业务规则 12 | F05/F06/F07 业务规则就近并入；核对后去重 |
| 提醒次数、同期互认率、统计时间归属、项目级处理结果、不可更改、不采纳原因（含字典与统计）、统计项目分类、金额统计、两类分析、统计导出（:477–546） | F08/F09 覆盖面大 | F08/F09 业务规则就近并入 |
| 患者脱敏相关（:10 决策记录、:344–346、:537、:541、:545） | **已知已同步**（阶段 7 S7-D8 记录：SRS :883、:2343 已修订） | 核对确认即可 |
| 互认项目院区金额、标准项目与互认项目编码使用（:548–555） | F10 大概率已有 | F10 业务规则就近并入 |

`_Avoid_` 落位规则：每条 `_Avoid_` 随其所属条目整体就近并入对应 SRS 章节的业务规则或 `当前版本范围与排除项`，**不单独集中堆放**，保持"约束与其约束的对象相邻"。

## 5. 文件清单

### 5.1 应改清单（执行改写）

**常驻规则与规范（5 个文件）**

| 文件 | 处数 | 改写要点 |
|---|---|---|
| `AGENTS.md` | 3 | §0.1 第 8 条依据清单、`§1` 权威表「长期业务规则」行去掉 `CONTEXT.md`，权威改为「当前 SRS、UML」；其余措辞不变 |
| `.agents/instructions/project-context.md` | 2 | 删除 `:25` 附近「业务上下文」链接条目；另一处按上下文改写 |
| `.agents/instructions/frontend-application.md` | 1 | 按上下文改写 |
| `.agents/instructions/csharp-backend-style.md` | 1 | 按上下文改写 |

**角色人格文件（约 39 个文件，模板替换）**：`.zcode/agents/*.md` 与 `.codex/agents/*.toml`（含 `COLLABORATION.md`）。绝大多数是同一句模板「先遵循项目 `AGENTS.md`、SRS、UML、`CONTEXT.md` 及 `.codex/agents/COLLABORATION.md` 的项目边界」，统一删除其中的「、`CONTEXT.md`」。注意 `.zcode/agents/design-ux-architect.md` 有第 2 处（:299 的阅读顺序句），`.codex/agents/design-ux-architect.toml` 同。完整文件列表以全局搜索 `CONTEXT` 结果为准，逐个处理并留痕。

**现行说明与图（4 个文件）**：`docs/流程图.md`（1）、`docs/业务流程图.md`（1）、`docs/uml/6-MedicalRecognitionCommandContracts.Meta.puml`（注释 1）、`docs/plans/README.md`（1）。「以 `CONTEXT.md` 与《需求规约 SRS》为准」类表述统一改为「以《需求规约 SRS》与 UML 为准」。

**阶段设计与规格文档（应改）**

| 文件 | 处数 |
|---|---|
| `docs/plans/001-总体计划/design.md` | 1 |
| `docs/plans/001-总体计划/impl.md` | 3 |
| `docs/plans/002-阶段0-现状对齐/design.md` | 1 |
| `docs/plans/002-阶段0-现状对齐/impl.md` | 1 |
| `docs/plans/003-阶段1-标准项目目录维护/design.md` | 4 |
| `docs/plans/004-阶段2-标准项目互认配置/design.md` | 1 |
| `docs/plans/005-阶段3-互认项目金额维护/design.md` | 6 |
| `docs/plans/006-阶段4-报告采集与生命周期/design.md` | 18 |
| `docs/plans/006-阶段4-报告采集与生命周期/Server/design.md` | 1 |
| `docs/plans/006-阶段4-报告采集与生命周期/阶段4-规格.md` | 1 |
| `docs/plans/007-阶段5-在线互认主流程/design.md` | 8 |
| `docs/plans/007-阶段5-在线互认主流程/Server/design.md` | 1 |
| `docs/plans/007-阶段5-在线互认主流程/阶段5-规格.md` | 4 |
| `docs/plans/007-阶段5-在线互认主流程/阶段5-Tickets/README.md` | 1 |
| `docs/plans/008-阶段6-互认统计与导出/design.md` | 6 |
| `docs/plans/009-阶段7-横向治理与发布收口/design.md` | 6 |
| `docs/plans/009-阶段7-横向治理与发布收口/impl.md` | 1 |

改写规则：「`CONTEXT.md`「某段」」→「SRS Fxx 业务规则 N」或「SRS 定义·某术语」，条款号必须真实存在（Step 1 之后以 SRS 现行章节为准）；找不到对应条款的，先回到 Step 1 补并入，再回来改引。

**代码（1 处）**：`server/Dy.MedicalRecognition.Tests/Stage7ExclusionGuardTests.cs:216` 注释——「CONTEXT.md 报告历史版本段：医院 HIS 在线业务接口只提供当前有效版本……」改为陈述事实或改引 SRS F03.1 对应条款。

### 5.2 禁改清单（历史证据，一律不动）

- 各阶段 `testReport.md`（003、004、009 共 9 处）
- 各阶段审查相关目录全部文件：`004/阶段2审查相关/*`、`006/阶段4-审查相关/*`（自审结果 15 处、审查要求 2 处、ds 审查结果 1 处）、`007/阶段5-审查相关/*`（审查问题追踪 4 处、审查要求 2 处、kimi 审查结果 4 处、ds 审查结果 4 处）、`008/阶段6-审查相关/*`（4 个文件共 8 处）、`009/阶段7-审查相关/*`（2 个 txt）
- 讨论记录：`004-阶段2/讨论内容.md`（1）、`008-阶段6/阶段6-讨论内容.md`（3）
- `009/新建文件夹/01-排除项回归.md`（1）

这些文件引用 CONTEXT.md 是时点事实，引用一个后来删除的文档不失真；改动等于伪造历史证据。

## 6. 改写规范

1. 一律用「SRS Fxx 业务规则 N」「SRS 定义·术语」「SRS 设计约束 N」等条款号定位，**禁止行号**。
2. 替换后句子必须仍成立：先读引用点前后文，确认条款号指向的事实与原引用一致；不一致时回到 Step 0/1 处理，不硬替。
3. 不改动目标文件的无关内容；不换行、不重排段落。
4. 角色人格文件的模板句替换后保持句子通顺（删「、`CONTEXT.md`」后检查顿号/逗号）。
5. 全部替换完成后全局搜索 `CONTEXT`，期望结果：仅剩 §5.2 禁改清单内文件命中，其余零残留。

## 7. 验证标准（全部满足才算完成）

1. 根 testReport.md 含《事实迁移核对表》，覆盖 CONTEXT.md 全部 H2 段与加粗条目，每条有结论与锚点。
2. SRS 并入内容无重复（抽查已知已同步的四类：本院报告匹配开关、引用详情参数、患者脱敏、互认次数口径，SRS 中无第二份拷贝）。
3. 全局搜索 `CONTEXT`：禁改清单外零残留。
4. `git diff --check` 干净；`git status` 中改动文件与 §5.1 清单一致（允许多出 `docs/plans/_temporary/T01-*/` 下三个文件与 SRS）。
5. 删除 `CONTEXT.md` 后，§5.1 中所有改写仍可独立成立（条款号真实、事实完整）。
6. §9.2 处理结果可核对：核对表该行的逐项判定都有条款号依据；实际补入句逐项可追溯到「缺句新补」判定，不含与 SRS 既有条款（含 F03.1 后置条件 6、F03.1 业务规则 31、「报告管理与历史版本」页面要求 3、5、6、8）重复的内容；「已有组合等价，不补」是被允许并有依据的结论；补写后两类下载入口的适用范围与 S4-D28、F04 现行口径一致、无相互矛盾表述；「后续事项已关闭」结论已写入核对表。

## 8. 交接格式（写入 impl.md 与 testReport.md 末尾）

```markdown
## 任务交接

- 主责智能体：
- 任务目标：
- 负责范围：
- 关键决策：
- 修改文件：
- 已执行验证：
- 未执行或受阻验证：
- 遗留风险：
- 建议下一主责：审查方（负责人指定的审查智能体）
```

## 9. 负责人裁定补充（2026-09-23，执行中回报事项）

本节是 §2–§8 的增补裁定：与此前条款字面冲突之处，以本节为准。

### 9.1 来源医院/接收医院术语（回报事项 1，批准并入）

- Step 1：两条术语并入 SRS「定义、首字母缩写词和缩略语」，文字沿用 `阶段5-规格.md:393` 已起草版本（含「同一家医院可以既是某份报告的来源医院，又是另一份报告的接收医院」句）。
- Step 2：`阶段5-规格.md` 中「`CONTEXT.md` 需补充……待落」表述更新为最终口径（已并入 SRS 定义段，改引条款定位），属 §1 裁定 ③ 在该文件的自然延伸。

### 9.2 两类 PDF 下载入口适用范围（回报事项 2，裁定选 A：借本轮澄清关闭后续事项）

目标：在 Step 1 结束时关闭 009 testReport:333 登记的后续事项（「匹配响应 PDF 下载入口绑定版本重校验」与 S4-D28 的适用范围张力），关闭方式是经 Step 0 核对后把两类下载入口的适用范围以 SRS 现行条款为准固定下来。**本节一切补写以 Step 0 逐句判定为准，允许的最终结果包括「一句都不补」**。涉及锚点一律用条款号：SRS F03.1 后置条件 6、F03.1 业务规则 31、SRS「报告管理与历史版本」页面要求 3、要求 5、要求 8；CONTEXT.md「互认匹配返回内容」段与「管理端报告历史版本」段。

- **HIS 匹配入口侧**：按 CONTEXT.md「互认匹配返回内容」段核对 F04 现行条款——每次下载校验调用医院院区、匹配归属及绑定报告版本仍为当前有效版本；报告形成后续版本或作废后立即停止提供原版本文件；该入口不用于恢复或提供历史版本。F04 已覆盖的只核对不并入；判定缺口才补，补句主语必须限定为「医院 HIS 使用匹配中的 PDF 下载入口时」（主语一限定，"管理端是否受该校验约束"的问题按构造消解）。
- **管理端侧**：「已作废报告的历史版本仍可下载」按裁定 ① 走 Step 0 等价判定——CONTEXT.md 侧存在近似句（「管理端报告历史版本」段与「互认匹配返回内容」段末句「来源医院管理员仍可按照报告历史版本查看权限下载本院旧版 PDF」），SRS 侧存在组合等价候选（F03.1 后置条件 6 + 页面要求 3 + 页面要求 5 + 页面要求 6：管理端可下载各版本 PDF、版本列表含已作废状态、医院管理员可查看全部历史版本及 PDF）。判定组合等价成立则不补；判定缺口则从 CONTEXT.md 对应句并入 SRS，**该句属裁定 ① 的 CONTEXT 事实搬迁，在本任务书内允许**。
- 原拟消歧句「管理端历史版本下载不受匹配入口校验约束」**不再预设必补**：若 HIS 侧补句主语按上条限定，Step 0 可判其「无需补」；仅当实际补句无法限定主语时才补。

《事实迁移核对表》为本节单独登记一行，格式：逐项（句）→ 等价判定依据（条款号）→ 结论（已有等价/事实搬迁并入/无需补/缺句新补）→ 实际补入的句子清单。「关闭后续事项」的最终结论也写在该行。

说明：本节是借本轮 SRS 修订关闭已登记后续事项，归入 §2 准做事项的第二件；其中从 CONTEXT.md 迁入的句子按裁定 ① 归类登记。testReport 原文属 §5.2 禁改清单，一律不动。
