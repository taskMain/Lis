# 阶段 1 实施

**阶段 1 已于 2026-09-15 完成收口（负责人裁定），阶段状态为 `Complete`。** 后端领域、仓储、SqlMap、四个查询、三张 `mrec_` 表 DDL 与 API Client 均已交付；正式标准目录页面已实现，原型已做生产隔离。真实宿主业务矩阵已在 2026-09-14 第 21/22 轮执行，S1-D29 五项验收已在 2026-09-15 第 24 轮完成并修复一处停用过滤缺陷。后端 V 编号已逐条核对，第 29 轮按三方独立审查完成代码治理整改；第 30–39 轮完成 V 矩阵终态收口与前端矩阵全部剩余用例清理，**两张矩阵均无 `PendingRetest`**。第 40 轮完成后端输入校验归位与过度封装清理（必填/值域/长度统一由 Request 特性声明、`MedicalRecognitionRequestValidator` 在 16 个入口执行，Manager 删除 8 个私有封装）。仍未闭合的移交项均为非阻塞既存项，见下方待办与交接表。

设计见 [design.md](design.md)，后端实施见 [Server/impl.md](Server/impl.md)，前端实施见 [Client/impl.md](Client/impl.md)，结果见 [testReport.md](testReport.md)。

## 进入实施前的前置条件

| 前置条件 | 状态 | 约束范围 |
|---|---|---|
| 阶段设计定稿并经项目负责人审查通过 | 已满足；S1-D29 于 2026-09-14 由负责人确认 `DesignConfirmed` | 不再阻塞实施批次 |
| S1-D31 独立新建表及物理前缀 | DesignConfirmed | 采用 `mrec_`，实体不带前缀；不复用、改名或迁入其他系统的表和数据 |
| S1-D29 筛选组合细化 | DesignConfirmed：负责人于 2026-09-14 确认 | 已于 2026-09-15 完成实现、缺陷修复与真实宿主验收 |
| 页面与交互设计完成并通过 | DesignConfirmed：负责人已确认当前 A2，包含停用筛选 | 已满足 |
| 最终建表脚本及目标数据库/schema 核对 | 已交付并执行 | 三张独立表含主键、业务唯一索引、列序与注释 |
| 目标库建表由项目负责人执行完成 | 已完成 | 依赖这些表的读写验证已解除阻塞 |
| 宿主菜单项已新增 | 已完成 | 菜单父项「检验检查结果互认」→ 子项「标准项目目录维护」 |
| 测试账号具备平台管理员角色 | 已确认可用 | 账号 `yangkj` 可从「检验检查结果互认平台」入口登录并进入目标菜单 |
| 标准项目「使用情况」口径 | 按范围延期处理，已同步 SRS | 不阻塞本阶段 |

**约束范围说明**：本平台独立新建 `mrec_` 表，不迁移、重命名或复用其他系统的对象与数据。建表已由负责人执行；依赖目标库的验证不再受建表前置阻塞，仍受 V 编号逐条核对与安全注入观察点限制。

## 批次与依赖

| 批次 | 范围 | 主责 | 文件所有权 | 前置条件 | 矩阵编号 | 当前状态 |
|---|---|---|---|---|---|---|
| 1 | 作用域修正与首批运行时探测 | — | `Repository` 仓储与 SqlMap | 设计通过 | 探测结论 | 已完成；真实写入链路未再复现作用域失败 |
| 2 | 领域规则与写用例 | — | `Domain`、`Application`、`Repository` | 批次 1；按已确认 S1-D26 实现整份保存和固定分组归属 | V1–V20、V31–V36、V40–V41、V43–V48、V50–V54、V56 | 代码已完成；真实写读证据见第 21/22 轮；V 编号逐条汇总已于第 25/26/30 轮完成，终态 `Passed` 54 / `AcceptedRisk` 1 / `N/A` 1 |
| 3 | 查询与 ReadModel 修正 | — | `Contracts`、`Domain/Queries`、`Repository/Queries`、`Application` | 批次 2 | V21–V28、V37–V39、V49；完整复跑 V53 | 代码与分层断言已完成 |
| 4 | 三表新建 DDL、物理映射、列序、注释及唯一索引交付 | — | 本模块 `Repository/Scripts`；不改后续阶段表或其他系统对象 | 设计通过；执行前确认目标数据库/schema、名称无冲突及脚本范围 | V30、V57、V58；V55 已退役 | DDL 已交付并由负责人执行；V58 第 26 轮以只读元数据结案；**V57 静态核对子项全部通过 → 记 `Passed`**（见 [V 矩阵核对](Server/V矩阵核对-20260915.md) 第 11.3 节） |
| 5 | 目标库数据库集成测试 | — | 测试项目 | 批次 4 目标库执行完成确认 | 适用数据库读写用例及 V29 | 真实宿主读写已取得证据；V54/V56 未执行 |
| 6 | API Client 重生成与前端实施 | — | `client/`；前端批次主责负责正式交付前原型隔离 | 批次 3 契约稳定 | C1–C74 | 已实现；S1-D29 与停用过滤已验收与修复；`C58`–`C69` 组件部分（第 31 轮）、`C62`/`C66`/`C67` 宿主部分（第 32 轮）、`C17`/`C24`/`C36`/`C37`/`C39`/`C41`/`C55`/`C56`（第 33 轮零写入）、`C51`/`C54`（第 34 轮）、`C38`/`C40`/`C43`（第 35 轮）、`C45`/`C46`（第 36 轮）、`C34`/`C35`（第 37 轮）、`C32`/`C52`/`C53`（第 38 轮）、`C42`/`C57`（第 39 轮新增 8 条自动化用例）均已完成；`C64` 宿主分支记 `N/A`（环境边界）。前端矩阵（`C1`–`C74` 排除 `C25`/`C26` 占位共 72 项）**已无 `PendingRetest`**：`C64` 为 `N/A`，其余为 `Passed` |

V53 的写入口工作单元声明与查询分层断言已在批次 2、3 完成后复跑；V29 的测试侧同步仅确保唯一索引异常翻译分支可达，不扩展为专门并发启停或跨行一致性治理。

## 执行记录

- [x] 完成适用设计及确认。
- [x] 建立目标失败证据或等价基线（S1-D29 停用过滤缺陷先建立失败单测再修复）。
- [x] 完成当前批次实现与直接影响验证。
- [x] 完成后端 V 编号逐条汇总复核（第 25 轮核对表 + 第 26 轮裁定）并更新根测试报告。
- [x] 完成后端代码治理整改（第 29 轮：三方审查 → 负责人裁定 → 实施 → 构建/测试/真实页面复测）。
- [x] 收口：V 矩阵 C/D 类补测**已完成**（第 27/28/30 轮，终态无 `PendingRetest`）；第 40 轮完成后端输入校验归位与过度封装清理（后端测试 21/21、全量重建 0 错误、真实链路 17 例复测、宿主页面提示与零写入取证）。负责人于 2026-09-15 裁定收口，阶段状态标记为 `Complete`；移交项均为非阻塞既存项。

## 待办与交接

| 项 | 状态 | 说明 |
|---|---|---|
| V 矩阵 C/D 类补测 | **已完成** | 第 27/28/30 轮真实宿主 token 补测 23 条；终态 `Passed` **54** / `AcceptedRisk` 1 / `N/A` 1（共 56 条，**无 `PendingRetest`**），见 [Server/V矩阵核对-20260915.md](Server/V矩阵核对-20260915.md) 第 11 节 |
| V22 分组列表按分类筛选 | **已完成**（第 30 轮） | 宿主 token 同一认证链路补测：两个分类的筛选结果与第三个分类的分组数恰好划分未筛选全集（2+3+1=6），证明筛选下推到 SQL 生效；证据见核对表第 11.5 节 |
| **`server/`、`client/` 入库时机** | **负责人指示：阶段 1 全部完成后才提交** | 在此之前**不得 `git add` / `commit`**。期间代码基线与注释可回滚性依赖临时快照 |
| 框架层请求绑定缺陷 | 已移交 | 类型不匹配入参返回 500 而非 400；请求到不了 Application 层，不写防御代码，建议反馈框架方 |
| 阶段 2 既存问题 7 项 | 已登记未改 | 含 `standard_item_id` 声明非空却写入 `Guid.Empty`（风险最高）；清单见测试报告第二十九轮 9.6 |
| Provider 中立性与 `<IsNotEmpty>` 空值语义 | 待验证 | 变参 `concat`、布尔字面量、值类型空值判定；当前仅 PostgreSQL，无现网影响 |
| C30–C69 纯形态与受控失败分支 | **已完成** | 第 33–39 轮逐条清理（组件级与宿主层写读、Contract/Component 用例补齐）；前端矩阵已无 `PendingRetest`，`C64` 宿主分支记 `N/A`（环境边界），详见 [testReport.md](testReport.md) 第 33–39 轮 |
| 仓库缺 XML 文档 Analyzer | 待治理 | `Directory.Build.props` 未启用 `GenerateDocumentationFile`，注释缺口无法被 CI 拦截 |
| GitNexus 索引 | 已过期 | 需精确影响面时先执行 `analyze` |
| 标准项目「使用情况」 | 按范围延期 | 已同步 SRS，本阶段不展示也不返回 |
