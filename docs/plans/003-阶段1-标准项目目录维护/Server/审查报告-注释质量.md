# 阶段 1（标准项目目录维护）后端手写生产代码 XML 注释质量审查报告

审查日期：2026-09-15
审查对象：`E:\MedSync\Dy.MedicalRecognition` 阶段 1 后端手写生产代码
审查性质：只读静态审查（未修改任何生产代码）

---

## 1. 审查范围与依据

### 1.1 依据规范（已逐字读完）

| 规范 | 关键条目 |
|---|---|
| `.agents/instructions/csharp-documentation-and-reuse.md` | §1 完成闸门；§2 XML 注释覆盖（含 public/protected/internal/private/partial/局部函数/只读字段；重点业务成员清单；partial 主声明职责）；§3 注释质量（`summary` 不复述名称、`param` 说明来源/单位/可信性/空值/顺序、`returns` 不写空话、可预期异常用 `exception`、复杂方法按业务阶段补方法内注释、禁止临时计划编号、禁止 `seam`/「事实重读」/「事务内聚」/「能力闭环」/「适配边界」及「通过依赖注入调用」/「完成聚合编排」）；§7 自动化 |
| `.agents/instructions/csharp-backend-style.md` | §6 框架和映射（`ObjectMap`/`*DataMaps.cs`）；§7 组织信息；§8 数据库与契约 |

### 1.2 审查文件清单（116 个手写 `.cs`，约 4 551 有效行）

| 区域 | 文件数 |
|---|---|
| `Application.Contracts\MedicalRecognitionReportAggregate\`（Requests 16、Dtos 19、`IMedicalRecognitionReportAppService`） | 36 |
| `Application.Contracts\Queries\` | 2 |
| `Application\MedicalRecognitionReportAggregate\`（AppService、DataMaps） | 2 |
| `Application\Queries\` | 1 |
| `Domain\MedicalRecognitionReportAggregate\`（Commands 16、Manager、实体 19、Repository 端口） | 37 |
| `Domain\Queries\` | 2 |
| `Domain.Share\MedicalRecognitionReportAggregate\`（Events 16、Const） | 17 |
| `Domain.Share\Enums\` | 13 |
| `Repository\MedicalRecognitionReportAggregate\` + `Queries\` | 2 |
| `Tests\` | 3 |

附带审查（按判定规则 7「Repository/技术层」）：`Repository\MedicalRecognitionReportAggregate\*.xml`（19 个 SqlMap）、`Repository\Queries\MedicalRecognitionReportQuery.xml`、`Repository\Scripts\*.sql`（19 个）。

排除：`obj\`、`bin\`、`*.g.cs`、`Global.cs`（`global using` + 程序集属性，按 `csharp-backend-style.md` §2 不适用一类型一文件与注释规则）、`*Module.cs`（模块注册，不属阶段 1 业务范围）。

### 1.3 阅读方式声明

全部 116 个文件均纳入机器化扫描（类型级文档检测、`summary` 文本全量归类、标签全量检索）。其中约 60 个文件逐行完整阅读；其余属于三类高度同构的文件族（Requests / Dtos / 实体、Commands、Events），已通过全族扫描 + 代表样本逐行核对确认无偏离，具体见 §5 审查局限。

---

## 2. 覆盖缺口清单

### 2.1 总体度量

| 度量项 | 结果 |
|---|---|
| 类型声明总数 | 129 |
| 有类型级 `<summary>` 的类型 | **14**（仅 `Application.Contracts\Queries\` 的 13 个类型 + `MedicalStandardUsageStatus`） |
| **无类型级 `<summary>` 的类型** | **115（89.1%）** |
| 手写生产代码中 `<param>` 标签 | **0** |
| 手写生产代码中 `<returns>` 标签 | **0** |
| 手写生产代码中 `<exception>` 标签 | **0** |
| 手写生产代码中 `<remarks>` / `<value>` / `<inheritdoc>` 标签 | **0** |
| 完全没有任何 `///` 注释的文件 | **14**（见 2.2） |

结论：当前 XML 注释体系**只有 `summary` 一种节点，且只覆盖 11% 的类型**。规范 §2 要求的 `param`、`returns`、`exception` 三类节点在整个后端实现中**零覆盖**。

### 2.2 整文件零 XML 注释（14 个，全部为手写生产/测试代码）

| # | 文件 | 行数 | 未覆盖成员 |
|---|---|---|---|
| 1 | `server\Dy.MedicalRecognition.Domain\MedicalRecognitionReportAggregate\MedicalRecognitionReportManager.cs` | 160 | 类型 + 1 只读依赖字段 + 16 公有方法 + 9 私有方法 = **27** |
| 2 | `server\Dy.MedicalRecognition.Repository\MedicalRecognitionReportAggregate\MedicalRecognitionReportRepository.cs` | 40 | 类型 + 4 私有常量 + `DataMapper` 属性 + 24 实现方法 = **30** |
| 3 | `server\Dy.MedicalRecognition.Domain\MedicalRecognitionReportAggregate\IMedicalRecognitionReportRepository.cs` | 32 | 类型 + 24 端口方法 = **25** |
| 4 | `server\Dy.MedicalRecognition.Application\MedicalRecognitionReportAggregate\MedicalRecognitionReportAppService.cs` | 171 | 类型 + 2 只读依赖字段 + 构造函数 + 16 公有方法 = **20** |
| 5 | `server\Dy.MedicalRecognition.Application.Contracts\MedicalRecognitionReportAggregate\IMedicalRecognitionReportAppService.cs` | 25 | 类型 + 16 契约方法 = **17** |
| 6 | `server\Dy.MedicalRecognition.Application\MedicalRecognitionReportAggregate\MedicalRecognitionReportDataMaps.cs` | 41 | 35 条 `[assembly: ObjectMap]` 映射声明 = **35** |
| 7 | `server\Dy.MedicalRecognition.Application\Queries\MedicalRecognitionReportQueryAppService.cs` | 96 | 类型 + 1 只读依赖字段 + 构造函数 + 4 公有方法 + 3 私有校验方法 + 5 私有映射方法 = **15** |
| 8 | `server\Dy.MedicalRecognition.Domain\Queries\MedicalRecognitionReportQueryModels.cs` | 48 | 4 个 record 类型 + 33 个属性 = **37** |
| 9 | `server\Dy.MedicalRecognition.Repository\Queries\MedicalRecognitionReportQueryRepository.cs` | 26 | 类型 + 1 私有常量 + 1 只读依赖字段 + 构造函数 + 4 方法 = **8** |
| 10 | `server\Dy.MedicalRecognition.Domain\Queries\IMedicalRecognitionReportQueryRepository.cs` | 9 | 类型 + 4 端口方法 = **5** |
| 11 | `server\Dy.MedicalRecognition.Domain.Share\MedicalRecognitionReportAggregate\MedicalRecognitionReportConst.cs` | 6 | 类型 + `AggregateId` 常量 = **2** |
| 12 | `server\Dy.MedicalRecognition.Tests\Stage1SqlMapProbeTests.cs` | 171 | 类型 + 3 `[Fact]` + 6 私有辅助方法 = **10**（测试，降一档） |
| 13 | `server\Dy.MedicalRecognition.Tests\Stage1ArchitectureTests.cs` | 48 | 类型 + 3 `[Fact]` = **4**（测试，降一档） |
| 14 | `server\Dy.MedicalRecognition.Tests\SmokeTests.cs` | 12 | 类型 + 1 `[Fact]` = **2**（测试，降一档） |

规范化条目：`csharp-documentation-and-reuse.md` §2「所有手写生产代码中的类型、构造函数、方法、属性、字段、事件、枚举项、参数、返回值和可预期异常，都必须有中文 XML 文档。覆盖范围包括 public、protected、internal、private、partial、局部函数以及只读字段；不得因为名称、类型、访问级别、代码行数或调用次数看似易于推断而省略。」

### 2.3 成员级缺口明细（按重点抽查对象）

#### 2.3.1 Manager（`MedicalRecognitionReportManager.cs`）

| 文件:行 | 成员名 | 缺失节点 |
|---|---|---|
| `MedicalRecognitionReportManager.cs:6` | `MedicalRecognitionReportManager`（`partial`，主声明） | summary（整体职责，规范 §2 末段明确要求） |
| `MedicalRecognitionReportManager.cs:8` | `repository` 只读依赖字段 | summary（规范 §2 要求说明「负责读取或执行的业务对象」，不能只写类型名） |
| `MedicalRecognitionReportManager.cs:10` | 构造函数 | summary + param |
| `MedicalRecognitionReportManager.cs:12` | `CreateMedicalStandardCategoryAsync` | summary + param + returns + exception（`业务拒绝：分类名称已存在。`/`分类保存失败。`）+ 参数校验异常 |
| `MedicalRecognitionReportManager.cs:22` | `UpdateMedicalStandardCategoryAsync` | summary + param + returns + exception（含 `分类存在下级分组时不可修改项目类型。`） |
| `MedicalRecognitionReportManager.cs:35` | `EnableMedicalStandardCategoryAsync` | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:44` | `DisableMedicalStandardCategoryAsync` | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:53` | `CreateMedicalStandardGroupAsync` | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:64` | `UpdateMedicalStandardGroupAsync` | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:75` | `EnableMedicalStandardGroupAsync` | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:84` | `DisableMedicalStandardGroupAsync` | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:93` | `CreateMedicalStandardItemAsync` | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:106` | `ChangeMedicalStandardItemRemarkAsync` | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:115` | `EnableMedicalStandardItemAsync` | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:124` | `DisableMedicalStandardItemAsync` | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:133` | `RequireCategoryAsync`（私有） | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:134` | `RequireGroupAsync`（私有） | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:135` | `ValidateName`（私有静态） | summary + param + exception |
| `MedicalRecognitionReportManager.cs:136` | `ValidateCode`（私有静态） | summary + param + exception |
| `MedicalRecognitionReportManager.cs:137` | `ValidateItemType`（私有静态） | summary + param + exception |
| `MedicalRecognitionReportManager.cs:138` | `ValidateId`（私有静态） | summary + param + exception |
| `MedicalRecognitionReportManager.cs:139` | `Input`（私有静态，异常构造） | summary + param + returns |
| `MedicalRecognitionReportManager.cs:140` | `Business`（私有静态，异常构造） | summary + param + returns |
| `MedicalRecognitionReportManager.cs:142` | `CreateMutualRecognitionItemAsync` | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:147` | `UpdateMutualRecognitionItemConfigurationAsync` | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:152` | `EnableMutualRecognitionItemAsync` | summary + param + returns + exception |
| `MedicalRecognitionReportManager.cs:156` | `DisableMutualRecognitionItemAsync` | summary + param + returns + exception |

Manager 内共 **36 处显式抛出**（`throw Business(` 32 处、`throw Input(` 4 处），**全部没有 `exception` 节点**。

#### 2.3.2 Repository 端口与实现

| 文件:行 | 成员名 | 缺失节点 |
|---|---|---|
| `IMedicalRecognitionReportRepository.cs:6` | `IMedicalRecognitionReportRepository`（`partial`） | summary（整体职责） |
| `IMedicalRecognitionReportRepository.cs:8–31` | 24 个端口方法（`CreateGuid`、`Create/Update/Enable/DisableMedicalStandardCategoryAsync`、…、`MedicalStandardItemCodeExistsAsync`、`Create/Update/Enable/DisableMutualRecognitionItemAsync`） | 每个方法 summary + param + returns ＋ `exception`；规范 §3 要求 Repository 说明「读写什么数据，不替领域判断状态」 |
| `MedicalRecognitionReportRepository.cs:8` | `MedicalRecognitionReportRepository`（`partial`） | summary |
| `MedicalRecognitionReportRepository.cs:10–13` | 4 个私有常量 `MedicalRecognitionReportScope` / `MedicalStandardCategoryScope` / `MedicalStandardGroupScope` / `MedicalStandardItemScope` | summary（须说明与 SqlMap `Scope` 属性的绑定契约） |
| `MedicalRecognitionReportRepository.cs:14` | `dataMapper`（`= default!`） | summary + 空值语义（为何用 `default!`） |
| `MedicalRecognitionReportRepository.cs:15` | `DataMapper` 属性 | summary + value |
| `MedicalRecognitionReportRepository.cs:16–39` | 24 个实现方法 | 每个方法 summary + param + returns；sqlId↔SqlMap 语句映射关系 |

#### 2.3.3 AppService / QueryAppService

| 文件:行 | 成员名 | 缺失节点 |
|---|---|---|
| `MedicalRecognitionReportAppService.cs:10` | `MedicalRecognitionReportAppService`（`partial`） | summary（整体职责 + 事务边界） |
| `MedicalRecognitionReportAppService.cs:12–13` | `manager` / `repository` 只读依赖字段 | summary（业务对象职责） |
| `MedicalRecognitionReportAppService.cs:14` | 构造函数 | summary + param |
| `MedicalRecognitionReportAppService.cs:21,31,41,51,61,71,81,91,101,111,121,131,140,148,156,164` | 16 个公有方法 | 每个 summary + param + returns + exception（`无法确定有效的操作人。`） |
| `IMedicalRecognitionReportAppService.cs:7` | 接口类型 | summary |
| `IMedicalRecognitionReportAppService.cs:9–24` | 16 个契约方法 | 每个 summary（业务动作）+ param + returns（`bool` 语义）+ exception |
| `MedicalRecognitionReportQueryAppService.cs:6` | 类型 | summary |
| `MedicalRecognitionReportQueryAppService.cs:8` | `repository` 只读依赖字段 | summary |
| `MedicalRecognitionReportQueryAppService.cs:10` | 构造函数 | summary + param |
| `MedicalRecognitionReportQueryAppService.cs:12,19,26,34` | 4 个公有查询方法 | summary + param + returns + exception |
| `MedicalRecognitionReportQueryAppService.cs:46,52,58,67,72,77` | 6 个私有映射方法 | summary + param + returns |
| `MedicalRecognitionReportQueryAppService.cs:82,87,92` | 3 个私有校验方法 | summary + param + exception |
| `IMedicalRecognitionReportQueryRepository.cs:3` | 类型 | summary |
| `IMedicalRecognitionReportQueryRepository.cs:5–8` | 4 个查询端口方法 | 每个 summary + param + returns（**注意**：此端口返回 `IEnumerable<>`，规范要求说明顺序语义） |
| `MedicalRecognitionReportQueryRepository.cs:8` | 类型 | summary |
| `MedicalRecognitionReportQueryRepository.cs:10` | `SqlScope` 常量 | summary（与 `<SqlMap Scope="MedicalRecognitionReportQuery">` 的绑定契约） |
| `MedicalRecognitionReportQueryRepository.cs:11` | `dataMapper` 只读依赖字段 | summary |
| `MedicalRecognitionReportQueryRepository.cs:13` | 构造函数 | summary + param |
| `MedicalRecognitionReportQueryRepository.cs:15,18,21,24` | 4 个实现方法 | summary + param + returns |
| `MedicalRecognitionReportQueryModels.cs:3,13,23,35` | 4 个查询模型 record | 类型 summary |
| `MedicalRecognitionReportQueryModels.cs:5–10,15–20,25–32,37–47` | 33 个属性 | 每个属性 summary（规范 §2：保存业务编码/状态/时间的字段须说明业务语义） |
| `MedicalRecognitionReportConst.cs:3` | `MedicalRecognitionReportConst` | summary |
| `MedicalRecognitionReportConst.cs:5` | `AggregateId` 常量 | summary（须说明该字符串与事件 `AggregateId`、聚合类型的绑定约束） |
| `MedicalRecognitionReportDataMaps.cs:7–41` | 35 条 `[assembly: ObjectMap]` | 每条映射的 summary 或文件级说明（哪些字段可直接转换、哪些必须由 AppService 显式转换） |

#### 2.3.4 Commands / DomainEvents

| 文件:行 | 成员名 | 缺失节点 |
|---|---|---|
| `Commands\*.cs` 全部 16 个 Command 类型声明（如 `CreateMedicalStandardCategoryCommand.cs:11`、`DisableMedicalStandardCategoryCommand.cs:9`） | 类型 | summary（规范 §3：Command 说明完成什么动作） |
| `Commands\*.cs` 共 **16 个 `CreateXxxEvent()` 方法**：`CreateMedicalStandardCategoryCommand.cs:34`、`UpdateMedicalStandardGroupCommand.cs:33`、`CreateMedicalStandardItemCommand.cs:42`、`CreateMutualRecognitionItemCommand.cs:34`、`UpdateMutualRecognitionItemConfigurationCommand.cs:30`、`UpdateMedicalStandardCategoryCommand.cs:38`、`CreateMedicalStandardGroupCommand.cs`、`ChangeMedicalStandardItemRemarkCommand.cs`、`EnableMedicalStandardGroupCommand.cs`、`DisableMedicalStandardGroupCommand.cs`、`EnableMedicalStandardItemCommand.cs`、`DisableMedicalStandardItemCommand.cs`、`EnableMedicalStandardCategoryCommand.cs`、`DisableMedicalStandardCategoryCommand.cs`、`EnableMutualRecognitionItemCommand.cs`、`DisableMutualRecognitionItemCommand.cs` | 全部 16/16 | summary + param + returns（这些方法承载事件构造与 `eventCreator`/`eventCreatedTime` 的取值来源，规范 §2 要求说明「时间/空值来源」） |
| `Domain.Share\...\Events\*.cs` 全部 16 个 DomainEvent 类型（如 `MedicalStandardCategoryCreatedEvent.cs:6`） | 类型 | summary。规范 §3 明确要求「DomainEvent 用过去式说明已发生事实」，当前 16 个事件**零**类型级文档 |
| `Domain.Share\...\Events\*.cs` 全部 16 个 `AggregateId` 覆写（如 `MedicalStandardCategoryCreatedEvent.cs:8`） | 属性 | summary |
| `Domain.Share\...\Events\*.cs` 全部 16 个 `EventType` 覆写（如 `MedicalStandardCategoryCreatedEvent.cs:9`） | 属性 | summary（须说明该值即事件契约标识，改动影响订阅者） |
| `Domain.Share\...\Events\*.cs` 全部 16 个事件的业务属性 | 属性 | 现有 summary 仅为名称复述，见 §3.1 |

#### 2.3.5 枚举项

13 个枚举中 **13 个全部缺少类型级 `summary`**，仅 `MedicalStandardUsageStatus.cs:3` 有。

| 文件:行 | 类型 | 缺失节点 |
|---|---|---|
| `Enums\MedicalItemType.cs:3` | `MedicalItemType` | 类型 summary |
| `Enums\MedicalReportType.cs:3` | `MedicalReportType` | 类型 summary（**且**：`MedicalReportType.Laboratory=1/Examination=2` 与 `MedicalItemType.Laboratory=0/Examination=1` 编号不一致，无语义说明） |
| `Enums\ConfigurationStatus.cs:3` | `ConfigurationStatus` | 类型 summary（该枚举在阶段 1 目录链路中未被使用，无语义说明） |
| `Enums\LaboratoryAbnormalFlag.cs:3` | `LaboratoryAbnormalFlag` | 类型 summary（`High`/`Low` 未说明是相对参考范围偏高/偏低） |
| `Enums\LaboratoryResultType.cs:3` | `LaboratoryResultType` | 类型 summary |
| `Enums\MedicalReportLifecycleStatus.cs:3` | `MedicalReportLifecycleStatus` | 类型 summary（状态流转语义缺失） |
| `Enums\RecognitionNonAdoptionReason.cs:3` | `RecognitionNonAdoptionReason` | 类型 summary；6 个枚举项仅有政策条文式短语，未说明各自适用判定场景 |
| `Enums\RecognitionResult.cs:3` | `RecognitionResult` | 类型 summary |
| `Enums\RecognitionStatisticsExportType.cs:3` | `RecognitionStatisticsExportType` | 类型 summary（7 个导出类型未说明导出文件粒度） |
| `Enums\RecognitionStatisticsGroupDimension.cs:3` | `RecognitionStatisticsGroupDimension` | 类型 summary |
| `Enums\RecognitionUsageDetailType.cs:3` | `RecognitionUsageDetailType` | 类型 summary（`Reminder`/`Adopted`/`NotAdopted`/`Referenced` 与 `RecognitionResult` 的对应关系未说明） |
| `Enums\SourceImageStatus.cs:3` | `SourceImageStatus` | 类型 summary（`Unknown` 与 `None` 的区分条件未说明） |
| `Enums\VisitType.cs:3` | `VisitType` | 类型 summary |

#### 2.3.6 测试代码（严重度降一档）

| 文件:行 | 成员名 | 缺失节点 |
|---|---|---|
| `SmokeTests.cs:5,8` | 类型 + `Generated_contract_is_available` | summary（测试意图） |
| `Stage1ArchitectureTests.cs:9,12,33,43` | 类型 + 3 个 `[Fact]` | summary（每条断言保护的不变量） |
| `Stage1SqlMapProbeTests.cs:10,13,27,42` | 类型 + 3 个 `[Fact]` | summary（尤其是 `:42` 通过反射验证 SqlMap 注册键，须说明为何必须用反射、替代方案与残余风险） |
| `Stage1SqlMapProbeTests.cs:98,116,121,140,153,166` | 6 个私有辅助方法 | summary + param + returns + exception（`FindHostOutputDirectory`/`FindRepositorySqlMap` 抛 `DirectoryNotFoundException`/`FileNotFoundException` 未记录） |

### 2.4 partial 主声明职责缺口

规范 §2 末段：「同一手写 partial 类型的主声明说明整体业务职责」。

| 文件:行 | partial 类型 | 现状 |
|---|---|---|
| `MedicalRecognitionReportManager.cs:6` | `public partial class MedicalRecognitionReportManager : DomainService` | 无主声明 summary |
| `IMedicalRecognitionReportRepository.cs:6` | `public partial interface IMedicalRecognitionReportRepository : IRepository` | 无主声明 summary |
| `MedicalRecognitionReportRepository.cs:8` | `public partial class MedicalRecognitionReportRepository` | 无主声明 summary |
| `IMedicalRecognitionReportAppService.cs:7` | `public partial interface IMedicalRecognitionReportAppService : IApplicationService` | 无主声明 summary |
| `MedicalRecognitionReportAppService.cs:10` | `public partial class MedicalRecognitionReportAppService : ApplicationService` | 无主声明 summary |

**正面结论**：全仓检索 `此分片`/`该分片` 命中 0 处，**未发现规范 §2 禁止的「此分片负责…」空话**，无需整改。

---

## 3. 质量问题清单

### 3.0 禁用表达与计划编号泄漏专项核查（规则 4，重点核查项）

对 `server\` 下全部 `.cs` 与 `.xml` 执行下列模式的全量检索：

| 检索模式 | 命中 |
|---|---|
| `seam` | 0 |
| `事实重读` | 0 |
| `事务内聚` | 0 |
| `能力闭环` | 0 |
| `适配边界` | 0 |
| `通过依赖注入调用` | 0 |
| `完成聚合编排` | 0 |
| `S1-D` / `V48` / `C61` / `批次` / `整改` | 0 |
| `TODO` / `FIXME` / `HACK` / `临时` | 0 |

**唯一命中**（属计划编号泄漏，但仅出现在测试类型名，非注释）：

| 文件:行 | 现状原文 | 问题类型 | 建议改法 |
|---|---|---|---|
| `Tests\Stage1ArchitectureTests.cs:9` | `public sealed class Stage1ArchitectureTests` | 计划编号泄漏（类型名，非注释） | 规范 §3 禁止「将临时计划编号、整改批次和仅对当前任务有意义的说明写入生产代码」。测试代码虽非生产代码，但该名称把阶段编号固化进长期标识符。建议改为表达不变量主题的名称（如 `WriteEntrypointWorkUnitTests` / `QueryWriteSeparationTests`），并同步文件名；迁移需同步 `docs/plans/003-.../impl.md` 与 V 矩阵中的引用 |
| `Tests\Stage1SqlMapProbeTests.cs:10` | `public sealed class Stage1SqlMapProbeTests` | 计划编号泄漏（类型名，非注释） | 同上，建议改为 `SqlMapRegistrationTests`；`:1` 的文件名同步 |

**结论：规范 §3 禁止的架构术语与实现说明式表达在注释中零命中，此项无注释类问题。** 仅存在 2 处测试类型名中的阶段编号固化，严重度低。

### 3.1 `summary` 只复述名称（规范 §3 第 1 条）

全量归类 199 个多行 `summary` 文本，高频项全部为裸标签：

| 出现次数 | 现状原文 | 问题类型 | 建议改法 |
|---|---|---|---|
| 78 | `ID` | 复述名称 | 按承载对象说明：实体/Command 中为「标准医疗项目分类主键」，事件中为「本次新建分类的标识」，Request 中为「待更新分类的标识」 |
| 54 | `操作人` | 复述名称 | 说明身份空间与来源：`OperId` 为平台登录用户 ID（`Guid`），由 AppService 从 `HttpRequestInfo.UserId` 解析后写入，非调用方传入、不可由前端指定 |
| 54 | `操作时间` | 复述名称 | 说明时间来源与时区：目录链路写 `DateTimeOffset.UtcNow`，互认配置链路写 `DateTimeOffset.Now`（见 §3.4）；数据库侧由 `current_timestamp` 覆盖 |
| 24 | `备注` | 复述名称 + 未写空值语义 | 「为空表示未填写；`ChangeMedicalStandardItemRemark` 以本次值整体覆盖，不区分空串与 null」 |
| 18 | `标准项目编码` | 复述名称 | 区分侧别：`MutualRecognitionItem.StandardProjectCode` 是配置侧业务编码；`ExaminationItem.StandardProjectCode` 是识别结果回填的互认编码，为空表示未匹配 |
| 12 | `报告版本ID` | 复述名称 | 「所属报告版本标识，同一报告的不同版本共享 `ReportId` 但版本 ID 不同」 |
| 11 | `分类ID` | 复述名称 | 「所属分类标识，须与分组的分组所属分类一致（见 Manager 校验）」 |
| 10 | `项目类型` | 复述名称 | 「检验/检查，同 `MedicalItemType`；标准项目编码在全局唯一，跨类型不重复」 |
| 9 | `组织编码` | 复述名称 + 未写可信性 | **重点**：`CreateMutualRecognitionItemRequest.OrganizationCode` 由调用方明文传入且 AppService 未覆盖（见 §3.4），注释必须写明「调用方传入的组织编码，服务端当前未做归属校验」 |
| 8 | `可互认时间天数` | 复述名称 + 未写单位与边界 | 「单位：自然日；`0` 在 `UpdateMutualRecognitionItemConfiguration` 中被动态 SQL 忽略（见 §3.5）」 |
| 8 | `是否启用` | 复述名称 | 「`true` 启用 / `false` 停用；停用不删除数据，停用项在有效目录查询中被过滤」 |
| 8 | `分组名称` / 8 | `分类名称` | 复述名称 | 「同一分类下唯一（数据库唯一索引 `ux_mrec_medical_standard_group_category_name`）；分类名全局唯一」 |
| 6 | `证件号码` / 6 | `证件类型代码` | 复述名称 | 「来源医院上报的患者证件信息，未与平台患者主数据校验」 |
| 6 | `来源明细标识` | 复述名称 | 「来源报文内的明细标识，用于同一报告版本内明细去重」 |
| 4 | `整体异常标识` | 复述名称 | 「来源系统给出的整体异常标志原文，未归一化为布尔值」 |
| 4 | `展示序号` | 复述名称 | 「同一父级内的展示排序，来源报文顺序，不保证连续」 |
| 2 | `预计节省金额` | 复述名称 | 「单位：元；由来源费用项目推算，为空表示来源未提供」 |
| 2 | `来源结果原文` | 复述名称 | 「来源系统结果原文，未做数值/单位归一化」 |

规范化条目：`csharp-documentation-and-reuse.md` §3「`summary` 说明业务职责、适用范围或不可见约束，不复述类型和方法名称。」

### 3.2 类型级 `summary` 完全缺失

见 §2.1 与 §2.3。115/129 类型无类型级文档。规范 §2 要求类型必须有中文 XML 文档，§3 要求 `summary` 说明业务职责。

### 3.3 `param` / `returns` / `exception` 节点零覆盖

| 文件:行 | 现状原文 | 问题类型 | 建议改法 |
|---|---|---|---|
| 全仓 | 无任何 `<param>` | 节点缺失 | 逐个方法补 `param`，说明来源、可信性、空值、顺序 |
| 全仓 | 无任何 `<returns>` | 节点缺失 | 补业务结果语义，禁止「返回结果」 |
| 全仓 | 无任何 `<exception>` | 节点缺失 | 补可预期业务/参数异常触发条件 |

`returns` 语义缺失的具体高风险点（返回值承载业务分支，注释未说明）：

| 文件:行 | 现状代码 | 问题 | 建议改法 |
|---|---|---|---|
| `MedicalRecognitionReportManager.cs:39` | `if (existing.IsValid) return true;` | `EnableMedicalStandardCategoryAsync` 在「已启用」时**不落库、不发事件**直接返回 `true`，与「实际执行了启用」返回 `true` 无法区分 | `returns` 写明：「目标已处于启用态时直接返回 `true` 且不产生事件；返回 `true` 不表示本次发生了状态变更」 |
| `MedicalRecognitionReportManager.cs:48` | `if (!existing.IsValid) return true;` | 同上（停用幂等短路） | 同上 |
| `MedicalRecognitionReportManager.cs:79` / `:88` / `:119` / `:128` | 同样的幂等短路 | 同上 | 同上 |
| `MedicalRecognitionReportManager.cs:150` | `return result > 0;` | `UpdateMutualRecognitionItemConfigurationAsync` 在目标不存在时返回 `false`，**不抛异常**；而同文件的 `UpdateMedicalStandardCategoryAsync:31`、`UpdateMedicalStandardGroupAsync:71`、`ChangeMedicalStandardItemRemarkAsync:111` 在目标不存在时**抛 `业务拒绝：…不存在。`** | 统一后补 `returns`/`exception`：明确「目标不存在时返回 `false`（历史行为）还是抛业务异常」，并把差异写入注释 |
| `MedicalRecognitionReportManager.cs:154` / `:158` | `return result > 0;` | `Enable/DisableMutualRecognitionItemAsync` 同样返回 `false` 而不抛异常，且**没有**「已处于目标态则提前返回」的短路（对比 `:39`/`:48`） | 补 `returns`/`exception`，并说明与标准目录 Enable/Disable 的行为差异 |
| `MedicalRecognitionReportManager.cs:144` | `await repository.CreateMutualRecognitionItemAsync(item);` | **返回值被丢弃**，没有 `<= 0` 失败判定；而同文件 `:18`、`:60`、`:102` 均有 `if (await repository.Create…Async(…) <= 0) throw Business("…保存失败。");` | 补注释说明「未校验写入行数」是有意为之还是缺陷；若为缺陷应补失败判定后再补 `exception` |
| `IMedicalRecognitionReportAppService.cs:9–24` | 16 个 `Task<bool>` | 全部无 `returns`，调用方无法从契约得知 `bool` 的语义 | 补 `returns`（成功/幂等无变更/未找到的统一约定） |

### 3.4 业务语义缺失（规范 §2 重点成员 + §3 第 3 条）

#### 3.4.1 用户 / 组织 / 时间来源未说明

| 文件:行 | 现状原文 | 问题类型 | 建议改法 |
|---|---|---|---|
| `MedicalRecognitionReportAppService.cs:24` | `if (!Guid.TryParse(HttpRequestInfo?.UserId, out var userId) \|\| userId == Guid.Empty) throw new InvalidOperationException("无法确定有效的操作人。");` | 可信来源未说明；且与 `:143` 行为不一致 | 补方法注释：「操作人取自登录上下文 `HttpRequestInfo.UserId`；解析失败即拒绝，避免写入不可信 `OperId`」 |
| `MedicalRecognitionReportAppService.cs:143` | `if (Guid.TryParse(HttpRequestInfo?.UserId, out var userId)) createMutualRecognitionItemCommand.OperId = userId;` | **同一文件内两种身份策略**：12 处解析失败即抛异常（`:24,:34,:44,:54,:64,:74,:84,:94,:104,:114,:124,:134`），4 处解析失败静默跳过（`:143,:151,:159,:167`），后者会写入 `OperId = Guid.Empty` | 统一策略并补注释说明差异原因；否则须在注释中明确「该接口在无登录上下文时允许匿名写入 `Guid.Empty`」 |
| `MedicalRecognitionReportAppService.cs:26` 等 12 处 | `createMedicalStandardCategoryCommand.OperTime = DateTimeOffset.UtcNow;` | 时间来源为 UTC，未说明 | 补注释：「事件时间取服务端 UTC；数据库 `oper_time` 另由 `current_timestamp` 写入，两者时钟不同源」 |
| `MedicalRecognitionReportAppService.cs:144` 等 4 处 | `createMutualRecognitionItemCommand.OperTime = DateTimeOffset.Now;` | **与上面 12 处时区不一致**（本地时间 vs UTC），同一 `OperTime` 概念承载两种时区，注释未说明 | 统一为 `UtcNow`，或在注释中明确该链路的本地时间语义与下游消费方预期 |
| `MedicalRecognitionReportAppService.cs:140,148,156,164` | 4 个互认配置方法**无 `[WorkUnit]` 特性**，而 12 个目录方法均有（`:20,:30,:40,:50` 等为 `[WorkUnit]`，`:60,:100` 为 `[WorkUnit(UseTransaction = true)]`） | 事务/工作单元边界差异未说明（规范 §3：承载事务、补偿、异常处理的方法须说明业务后果） | 补注释说明该差异是有意的（互认配置为阶段 2 范围、暂不纳入工作单元）还是待整改缺陷 |
| `MedicalRecognitionReportAppService.cs:140–170` | 4 个互认配置方法无任何注释 | 方法注释全缺 | 补 summary/param/returns/exception + 事务边界说明 |
| `MedicalRecognitionReportAppService.cs:12–13` | `private readonly MedicalRecognitionReportManager manager;` / `private readonly IMedicalRecognitionReportRepository repository;` | 只读依赖字段未说明业务职责（规范 §2 明确要求，不能只写类型名） | 「`manager`：执行标准目录的领域校验与事件登记；`repository`：生成实体主键、判定名称/编码唯一性」 |
| `MedicalRecognitionReportManager.cs:8` | `private readonly IMedicalRecognitionReportRepository repository;` | 同上 | 「读取目录现有状态并执行增改与状态更新，不承担业务判断」 |

#### 3.4.2 保存状态 / 时间 / 快照的字段未说明业务语义

| 文件:行 | 现状原文 | 问题类型 | 建议改法 |
|---|---|---|---|
| `Dtos\MedicalRecognitionReportDto.cs:46` | `/// <summary>作废时间</summary>` `public partial DateTime? VoidedTime { get; set; }` | 状态流转语义缺失 | 「报告被作废的业务发生时间；与 `OperTime`（最后一次数据操作）不同，仅在 `Status == Voided` 时非空」 |
| `Dtos\MedicalRecognitionReportDto.cs:50` | `/// <summary>作废原因</summary>` | 同上 | 「作废原因文本；与 `VoidedTime` 成对出现，`Status == Effective` 时应为空」 |
| `Dtos\MedicalReportVersionDto.cs:144` | `/// <summary>源端报告修改时间</summary>` `SourceModifiedTime` | 三个时间源未区分 | 明确三者的区别与用途：`SourceModifiedTime`（来源系统最后修改时间，用于判断是否需要重传）/ `ReceivedTime`（平台接收时间）/ `OperTime`（平台数据操作时间） |
| `Dtos\MedicalReportVersionDto.cs:148` | `/// <summary>平台接收时间</summary>` `ReceivedTime` | 同上 | 同上 |
| `Dtos\MedicalReportVersionDto.cs:48` | `/// <summary>报告时年龄</summary>` `string? AgeAtReport` | 类型与单位未说明（字符串而非数值） | 「报告签发时按来源口径记录的年龄文本（来源可能为『3月』『5岁』），未归一化为数值」 |
| `Dtos\MedicalReportVersionDto.cs:116` | `/// <summary>审核时间</summary>` `DateTime? ReviewTime` | 空值语义未说明 | 「报告审核通过时间；未审核时为 null」 |
| `Dtos\MedicalReportVersionDto.cs:160` | `/// <summary>来源保密标识</summary>` `string? SourceConfidentialFlag` | 取值域未说明 | 「来源系统的保密级别原文，未归一化枚举；影响后续可见范围判断」 |
| `Dtos\RecognitionProcessingResultDto.cs:52` | `/// <summary>不采纳原因</summary>` `RecognitionNonAdoptionReason? NonAdoptionReason` | 与 `RecognitionResult` 的联动未说明 | 「仅当 `RecognitionResult == NotAdopted` 时填写；`Adopted` 时应为 null」 |
| `Dtos\RecognitionProcessingResultDto.cs:60` | `/// <summary>预计节省金额</summary>` `decimal? EstimatedSavingAmount` | 单位未说明 | 「单位：元；由来源收费项目推算，为空表示来源未提供」 |
| `Dtos\RecognitionReferenceDto.cs:24` | `/// <summary>实际引用时间</summary>` `DateTime ReferencedTime` | 与 `OperTime` 的区别未说明 | 「医生实际引用该互认结果的时间；`OperTime` 为平台记录写入时间」 |
| `Dtos\RecognitionMatchRecordDto.cs:44` | `/// <summary>互认匹配生成时间</summary>` `MatchCreatedTime` | 未说明为系统生成 | 「系统生成互认匹配的时间，非人工操作时间」 |
| `Dtos\RecognitionMatchRecordDto.cs:48` | `/// <summary>处理结果保存时间</summary>` `DateTime? DecisionSavedTime` | 空值语义未说明 | 「医生保存采纳/不采纳结论的时间；未处理时为 null」 |
| `Dtos\RecognitionMatchItemDto.cs:24` | `/// <summary>标准项目编码</summary>` | 与 `RecognitionMatchRecordDto` 中同名项区分未说明 | 「被匹配到的标准项目编码；为空/null 不出现，匹配失败项不进入该表」 |
| `Entities\MedicalRecognitionReport.cs:44` | `/// <summary>生命周期状态</summary>` `MedicalReportLifecycleStatus Status` | 状态流转规则未说明 | 「`Effective` → `Voided` 单向流转；作废后 `CurrentVersionId` 保持指向最后版本」 |
| `Entities\MedicalReportVersion.cs:24` | `/// <summary>版本序号</summary>` `int VersionNumber` | 生成规则与唯一性未说明 | 「同一 `ReportId` 下从 1 递增；与 `Id` 不同，重传生成新版本」 |
| `Entities\PlatformPatient.cs:16` | `/// <summary>证件类型代码</summary>` | 取值域与可信性未说明 | 「来源系统证件类型代码原文，未映射平台字典；与 `IdentityDocumentNo` 组合标识患者」 |
| `Entities\MedicalStandard*.cs` 中 `IsValid`（`MedicalStandardCategory.cs:24`、`MedicalStandardGroup.cs:24`、`MedicalStandardItem.cs:32`） | `/// <summary>是否启用</summary>` | 业务语义未说明 | 「`true` 启用 / `false` 停用；停用为标准目录的软删除手段，数据保留且可重新启用」 |
| `Domain\Queries\MedicalRecognitionReportQueryModels.cs:46–47` | `public Guid OperId { get; init; }` / `public DateTimeOffset OperTime { get; init; }` | 无任何注释；`EffectiveMedicalStandardCatalogItem` 承载「最后实际维护人/时间」快照语义 | 「最后一次实际维护该标准项目的操作人与时间（`oper_id`/`oper_time`），用于有效目录的溯源展示」 |
| `Application.Contracts\...\Requests\CreateMutualRecognitionItemRequest.cs:12` | `/// <summary>组织编码</summary>` `OrganizationCode` | **可信性未说明**（规范 §3 要求 `param`/字段说明可信性） | 「调用方传入的组织编码。当前 AppService 未从登录上下文覆盖（对比 `OperId`），因此该值可信度取决于调用方」，或修正实现后改为「服务端按登录上下文回填」 |
| `Application.Contracts\...\Requests\CreateMutualRecognitionItemRequest.cs:20` / `UpdateMutualRecognitionItemConfigurationRequest.cs:16` | `/// <summary>可互认时间天数</summary>` `RecognitionDurationDays` | 单位与 `0` 的边界语义未说明 | 「单位：自然日；`0` 在更新语句中会被动态 SQL 忽略（见 `MutualRecognitionItem.xml:17`），无法用于关闭互认」 |
| `Domain\Commands\CreateMutualRecognitionItemCommand.cs:16` | `/// <summary>组织编码</summary>` | 同 Request | 同步修正 |

#### 3.4.3 承载状态流转 / 兼容 / 并发的方法未说明业务后果

| 文件:行 | 现状代码 | 问题类型 | 建议改法 |
|---|---|---|---|
| `MedicalRecognitionReportManager.cs:28–29` | `if (existing.ItemType != command.ItemType && await repository.MedicalStandardCategoryHasGroupsAsync(command.Id)) throw Business("分类存在下级分组时不可修改项目类型。");` | 跨层级业务规则，仅以异常消息表达，无 XML 文档 | 补 `summary`/`exception`：说明该规则用于防止下级分组的项目类型与分类不一致，以及触发条件（存在下级分组**且**类型发生变化） |
| `MedicalRecognitionReportManager.cs:30` | `MedicalStandardCategory category = command.MapToMedicalStandardCategory(); category.IsValid = existing.IsValid;` | **兼容性约束**：`UpdateMedicalStandardCategory` 语句不写 `is_valid`（`MedicalStandardCategory.xml:14–17`），若不回填则映射默认值会污染实体；无任何注释 | 补行内注释：「更新语句不包含 `is_valid`，此处回填现有状态，避免映射默认值覆盖启用状态」 |
| `MedicalRecognitionReportManager.cs:70` | `MedicalStandardGroup group = command.MapToMedicalStandardGroup(); group.CategoryId = existing.CategoryId; group.IsValid = existing.IsValid;` | 同上 + 分组不可改父分类（`Stage1ArchitectureTests.cs:43` 专门守护该契约） | 补行内注释：「`UpdateMedicalStandardGroupRequest` 不含 `CategoryId`，此处回填现有父分类，保证分组不跨分类迁移」 |
| `MedicalRecognitionReportManager.cs:99` | `if (group.CategoryId != category.Id) throw Business("分组不属于所选分类。");` | 跨聚合一致性规则无注释 | 补 `exception`：「`CategoryId` 与 `GroupId` 不构成父子关系时抛出」 |
| `MedicalRecognitionReportManager.cs:98` | `if (!category.IsValid \|\| !group.IsValid) throw Business("分类和分组必须启用。");` | 状态前置条件无注释 | 补 `exception`：「分类或分组任一处于停用态时抛出，停用层下不允许新增标准项目」 |
| `MedicalRecognitionReportManager.cs:15` / `:18` | `if (await repository.MedicalStandardCategoryNameExistsAsync(command.Name)) throw Business("分类名称已存在。");` … `if (await repository.CreateMedicalStandardCategoryAsync(category) <= 0) throw Business("分类保存失败。");` | **先查后写并发窗口**：检查与插入之间无锁，并发下唯一索引 `ux_mrec_medical_standard_category_name`（`medical_standard_category.sql:22–23`）会抛原生唯一约束冲突，绕过业务异常链路 | 补行内注释说明检查为「优化提示、非并发保证」，并说明冲突时用户可见的错误形态与后续处理 |
| `MedicalRecognitionReportManager.cs:58` / `:60`、`:100` / `:102` | 同样的先查后写（分组名、项目编码） | 同上；对应唯一索引 `medical_standard_group.sql:22–23`、`medical_standard_item.sql:26–27` | 同上 |
| `MedicalRecognitionReportManager.cs:14` | `ValidateName(command.Name); ValidateItemType(command.ItemType);` | 一行两条校验，无阶段说明 | 补方法内注释区分「格式校验」与「唯一性/存在性校验」阶段（规范 §3 第 5 条） |

### 3.5 复杂方法缺少方法内业务阶段注释（规范 §3 第 5 条）

规范 §3：「复杂或较长方法必须按业务阶段补充方法内注释，说明校验、读取、批量处理、外部调用、写入、事件、空值和失败后果；只写一条笼统注释不算覆盖。」

| 文件:行 | 现状 | 问题类型 | 建议改法 |
|---|---|---|---|
| `MedicalRecognitionReportManager.cs:12–20` | 单行内串联「两项校验 → 唯一性查询 → 映射 → 生成主键/置启用 → 插入并判失败 → 登记事件 → return」7 个阶段，方法内注释 0 条 | 多阶段方法无阶段注释 | 按阶段补行内注释：校验 / 唯一性读取 / 构造实体 / 写入与失败后果 / 事件登记 |
| `MedicalRecognitionReportManager.cs:22–33` | 串联「校验 → 读取现有 → 唯一性（排除自身）→ 跨层级规则 → 映射并回填状态 → 更新判失败 → 事件」 | 同上 | 同上 |
| `MedicalRecognitionReportManager.cs:93–104` | 串联「4 项校验 → 读取分类 → 读取分组 → 两级启用态校验 → 父子一致性校验 → 编码唯一性 → 映射/主键/置启用 → 写入判失败 → 事件」，共 10 个阶段，方法内注释 0 条 | 同上 | 按「校验」「读取」「跨聚合一致性」「唯一性」「写入」「事件」六段补注释 |
| `MedicalRecognitionReportAppService.cs:21–28` | 每次请求 4 个阶段（映射 → 解析操作人 → 覆盖权威字段 → 调用领域），16 个方法均 0 条方法内注释，且 12 个方法完全重复 | 阶段注释缺失 + 未说明为何要覆盖 | 至少为「解析操作人」与「覆盖服务端权威字段（`OperId`/`OperTime`）」两个阶段补注释，说明为何 Request 不含这两个字段（规范 §6：不得向 Request 增加可信字段） |
| `MedicalRecognitionReportAppService.cs:140–146` | `if (Guid.TryParse(...)) cmd.OperId = userId;` + `cmd.OperTime = DateTimeOffset.Now;` — 静默降级分支无注释 | 空值/失败后果未注释 | 补注释说明解析失败时的后果（写入 `Guid.Empty`） |
| `MedicalRecognitionReportQueryAppService.cs:34–44` | `QueryEffectiveMedicalStandardCatalogAsync`：调用仓库后做四级分组投影，`ItemTypes` 的排序契约完全不可见 | 复杂映射无阶段注释 | 补注释说明「分组层级 = 项目类型 → 分类 → 分组 → 项目」及排序契约 |
| `MedicalRecognitionReportQueryAppService.cs:42,49,55,61` | `OrderBy(group => group.Key)` / `OrderBy(group => group.First().CategoryName).ThenBy(group => group.Key)` / … `/ OrderBy(item => item.Name).ThenBy(item => item.ItemId)` | **稳定排序契约未文档化**：注释未说明「同名时以主键打破平局」（`csharp-backend-style.md` §8 要求查询明确稳定唯一排序） | 补 `<returns>`/行内注释，说明输出顺序及平局裁决键 |
| `MedicalRecognitionReportQueryRepository.cs:15–25` | 4 条 `new { … }` 匿名参数对象直接对应 SqlMap 的 `$Name` 占位符，字段名/大小写/空值省略的绑定约定无注释 | 技术契约无注释 | 补注释说明匿名对象属性名必须与 SqlMap 占位符一致，且 `null` 会被 `<IsNotEmpty>` 判为「不参与筛选」 |
| `MedicalRecognitionReportRepository.cs:16–39` | 24 处 `(scope:, sqlId:)` 二元组映射，`SqlMap` 的 `Scope` 与 `sqlId` 拼接规则无任何注释 | 技术契约无注释（`Tests\Stage1SqlMapProbeTests.cs` 正是为守护该拼接规则而存在） | 补注释说明 `scope` 必须等于对应 SqlMap 的 `Scope` 属性、`sqlId` 必须等于 `<Statement Id>`，并说明 `MutualRecognitionItem` 复用 `MedicalRecognitionReport` scope 的原因 |
| `MedicalRecognitionReportDataMaps.cs:7–41` | 35 条 `[assembly: ObjectMap]` 无任何注释 | 映射契约无注释 | 补文件级说明（或分组行内注释）：前半段为 Request→Command 单向映射，后半段为实体↔DTO 双向映射；并说明 `ObjectMap` 只处理可直接转换的同名字段，编码→ID 等真实转换由 AppService 显式完成（`csharp-backend-style.md` §6） |

### 3.6 partial 分片

无问题：未发现「此分片负责…」类空话（全仓 0 命中）。仅主声明职责缺失，已计入 §2.4。

### 3.7 Repository / 技术层：事务、唯一约束、特殊 SQL（规范 §3 末段）

规范 §3 末段：「Repository、Infrastructure、外部适配器、文件存储和并发控制可以在正确性需要时说明事务与补偿顺序、锁和唯一约束、协议兼容、资源清理及特殊 SQL。技术注释必须写清行为、原因和错误后果。」

| 文件:行 | 现状原文 | 问题类型 | 建议改法 |
|---|---|---|---|
| `Repository\Queries\MedicalRecognitionReportQuery.xml:26` | `i.code like concat('%', replace(replace(replace($Code, '!', '!!'), '%', '!%'), '_', '!_'), '%') escape '!'` | **特殊 SQL 无注释**：三层 `replace` 是 LIKE 通配符转义（`!`→`!!`、`%`→`!%`、`_`→`!_`），无任何说明 | 补 XML 注释：「对用户输入的 `%`/`_`/`!` 做转义，使 `like … escape '!'` 按字面包含匹配，避免用户输入被当作通配符」 |
| `Repository\Queries\MedicalRecognitionReportQuery.xml:27` | 同上（`Name` 参数） | 同上 | 同上 |
| `Repository\Queries\MedicalRecognitionReportQuery.xml:39` | 同上（`CategoryName` 参数） | 同上 | 同上 |
| `Repository\Queries\MedicalRecognitionReportQuery.xml:5` | `case when exists (…) then 1 else 0 end as usage_status` | **派生列映射为枚举**无注释；`usage_status` 是 `MedicalStandardUsageStatus`（1=InUse/0=Unused） | 补注释：「派生列 `usage_status` 映射 `MedicalStandardUsageStatus`，仅表示是否存在下级对象，与启用状态 `is_valid` 无关」 |
| `Repository\Queries\MedicalRecognitionReportQuery.xml:13` | 同上（分组层级） | 同上 | 同上 |
| `Repository\MedicalRecognitionReportAggregate\MedicalStandardCategory.xml:19` | `… where id = $Id and is_valid = false` | **幂等/并发保护条件无注释**：`and is_valid` 使重复启用成为 0 行更新 | 补注释：「仅当当前为停用态才更新，重复启用影响 0 行；调用方（Manager）据此提前返回而不报错」 |
| `Repository\MedicalRecognitionReportAggregate\MedicalStandardCategory.xml:22` | `… where id = $Id and is_valid = true` | 同上（停用方向） | 同上 |
| `Repository\MedicalRecognitionReportAggregate\MedicalStandardGroup.xml:19` / `:22` | 同上 | 同上 | 同上 |
| `Repository\MedicalRecognitionReportAggregate\MedicalStandardItem.xml:19` / `:22` | 同上 | 同上 | 同上 |
| `Repository\MedicalRecognitionReportAggregate\MutualRecognitionItem.xml:28` | `update mutual_recognition_item set is_valid = true, … where id = $Id` | **与其他三张表行为不一致**：缺少 `and is_valid` 守卫（已用 XML 解析逐条核对：Category/Group/Item 的 6 条 Enable/Disable 均有守卫，`MutualRecognitionItem` 的 2 条均无） | 补注释说明是否有意省略；若为保持一致应补守卫，并在 Manager 对应方法（`:152`/`:156`）注释中记录行为差异 |
| `Repository\MedicalRecognitionReportAggregate\MutualRecognitionItem.xml:31` | 同上 | 同上 | 同上 |
| `Repository\MedicalRecognitionReportAggregate\MutualRecognitionItem.xml:17–19` | `<IsNotEmpty Prepend="," Property="RecognitionDurationDays"> recognition_duration_days = $RecognitionDurationDays </IsNotEmpty>` | **动态 SQL 边界无注释**：`IsNotEmpty` 对值类型 `0` 判为「空」，导致「可互认天数设为 0」静默不生效，且用户看到成功 | 补注释：「`RecognitionDurationDays = 0` 时该片段被省略，更新不会把天数改为 0；需要支持 0 时改用 `<IsNotNull>` 或显式布尔开关」 |
| `Repository\MedicalRecognitionReportAggregate\MutualRecognitionItem.xml:20–22` | `<IsNotEmpty Prepend="," Property="OperId"> oper_id = $OperId </IsNotEmpty>` | 同上（`OperId` 为 `Guid`，`Guid.Empty` 会被省略，与 §3.4.1 的静默降级叠加） | 同上，并说明与 AppService `Guid.Empty` 写入路径的叠加后果 |
| `Repository\MedicalRecognitionReportAggregate\MedicalStandardGroup.xml:31–33` | `<Statement Id="MedicalStandardGroupHasItems"> select count(1) from mrec_medical_standard_item where group_id = $GroupId </Statement>` | **死语句**：`IMedicalRecognitionReportRepository` 24 个方法中无任何方法引用该 `sqlId`（对照 `MedicalStandardCategoryHasGroups` 有 `MedicalStandardCategoryHasGroupsAsync` 使用） | 补注释说明保留原因，或删除该语句 |
| `Repository\MedicalRecognitionReportAggregate\*.xml:6–10`（19 个文件） | `<Statement Id="QueryAllXxx">` | 与 `MedicalStandardGroupHasItems` 同类的未引用语句群，无注释说明其用途（是否供框架全量加载或运维使用） | 补注释说明用途，或确认为死代码后清理 |
| `Repository\Scripts\medical_standard_category.sql:22–23` | `create unique index ux_mrec_medical_standard_category_name on mrec_medical_standard_category (name);` | 约束存在，但代码侧先查后写的并发窗口未在任何注释中与原生日志关联 | 在 SqlMap/Repository 补注释说明唯一约束是并发的最终防线，冲突以数据库异常形态暴露 |
| `Repository\Scripts\medical_standard_group.sql:22–23` | `create unique index ux_mrec_medical_standard_group_category_name on mrec_medical_standard_group (category_id, name);` | 同上；注意唯一性是**按分类内**而非全局，而分类名唯一性（`medical_standard_category.sql:22–23`）是**全局** | 在 `MedicalStandardGroup.xml:27–30` 与 `MedicalGroupNameExistsAsync` 补注释，明确「分组名在同一分类内唯一，跨分类可重名；分类名全局唯一」 |
| `Repository\Scripts\medical_standard_item.sql:26–27` | `create unique index ux_mrec_medical_standard_item_code on mrec_medical_standard_item (code);` | 编码唯一性为**全局**（不区分 `item_type`），与 Manager `:100` 的全局查重一致但无注释 | 补注释明确「标准项目编码全局唯一，检验与检查之间不可重复」 |
| `Repository\Scripts\mutual_recognition_item.sql:2` | `create table if not exists mutual_recognition_item (` | 与其余 18 个脚本的 `create table`（无 `if not exists`）不一致；且该脚本**没有任何 `comment on`**，而同属配置面的三张标准目录表脚本均有完整 `comment on table/column` | 统一脚本风格；补齐 `comment on table/column`，至少说明 `standard_item_id` 与 `standard_project_code` 并存的原因及 `recognition_duration_days` 单位 |
| `Repository\Scripts\mutual_recognition_item.sql:5–6` | `standard_item_id uuid not null,` / `standard_project_code varchar(100) not null,` | 两列并存且 `not null`，但 Manager `:144` 仅从 Command 映射 `StandardProjectCode`，`StandardItemId` 从未赋值 → 写入 `Guid.Empty` | 补注释说明两列关系（编码为业务键、ID 为解析后的内部键）与解析责任方；当前注释缺失掩盖了未解析 Transport 标识的问题（`csharp-backend-style.md` §6 禁止「Command 保留尚未解析的 Transport 标识」） |
| `Repository\MedicalRecognitionReportAggregate\MedicalRecognitionReportRepository.cs:10–13` | 4 个 `*Scope` 常量 | scope↔SqlMap `Scope` 绑定契约无注释；`MutualRecognitionItem` 的 4 个方法用 `MedicalRecognitionReportScope`，而对应文件 `MutualRecognitionItem.xml:1` 声明 `Scope="MedicalRecognitionReport"`，属非直观耦合 | 补常量注释说明绑定关系与 `MutualRecognitionItem` 复用该 scope 的原因 |
| `Repository\MedicalRecognitionReportAggregate\MedicalRecognitionReportRepository.cs:14–15` | `private IDataMapper dataMapper = default!;` / `public IDataMapper DataMapper { get => dataMapper; set => dataMapper = value; }` | 属性注入 + `default!` 抑制空值告警，资源初始化与空值语义无注释 | 补注释：「由框架通过 `IHasDataMapper` 属性注入；在注入前访问 `dataMapper` 会抛 `NullReferenceException`」 |
| `Repository\Queries\MedicalRecognitionReportQueryRepository.cs:13` | `public MedicalRecognitionReportQueryRepository([FromKeyedServices(BuilderConst.DEFAULT_ALIAS)] IDataMapper dataMapper) => this.dataMapper = dataMapper;` | 键控服务解析的用意（为何需要 `DEFAULT_ALIAS`）无注释 | 补注释说明该键控别名用于选取默认数据源连接，及选错时的错误后果 |

### 3.8 自动化缺口（规范 §7）

| 现状 | 问题 | 建议改法 |
|---|---|---|
| `server\Directory.Build.props` 未启用 `GenerateDocumentationFile`；`server\.editorconfig` 仅有 `csharp_style_namespace_declarations = file_scoped:warning`（第 15 行），无 StyleCop 或等价 Roslyn 文档分析器，无 XML 文档 baseline | 规范 §7 要求「使用 StyleCop 或等价 Roslyn Analyzer 检查选定的 XML 文档规则，排除生成代码并为历史违规维护固定 baseline」，当前**零自动化覆盖**，注释质量完全依赖人工审查，本次审查发现的 115 个未文档类型无法被 CI 拦截 | 引入 `GenerateDocumentationFile` + 文档规则分析器（或 StyleCop），为存量违规建立固定 baseline，禁止新增违规；优先启用「类型与方法必须有 `summary`」这一类可机械判断规则 |

---

## 4. 合格样例（正面基准）

以下 3 处已达到规范 §3 要求，可作为后续整改的基准写法。

### 样例 1：`Domain.Share\Enums\MedicalStandardUsageStatus.cs:3–10` — 枚举类型与项都承载语义

```csharp
/// <summary>表示标准目录是否存在下级业务对象。</summary>
public enum MedicalStandardUsageStatus
{
  /// <summary>没有下级对象。</summary>
  Unused = 0,
  /// <summary>存在下级对象，包含已停用对象。</summary>
  InUse = 1
}
```

**好在哪**：类型级 `summary` 说明了「存在下级业务对象」这一不可从枚举名推断的判定口径；`InUse` 的注释额外点出「包含已停用对象」，直接对上了 SQL 的 `exists (select 1 from …)` 不附加 `is_valid` 条件的实现事实（`MedicalRecognitionReportQuery.xml:5,13`）。维护者无需读 SQL 即可知道停用子项也计入「使用中」。这正是规范 §3「说明业务职责、适用范围或不可见约束」的示例，也是 13 个枚举中唯一达标者。

### 样例 2：`Application.Contracts\Queries\MedicalRecognitionReportQueryContracts.cs:6,52,139–142` — 空值语义与快照语义写明

```csharp
/// <summary>可选项目类型；为空表示全部类型。</summary>
public MedicalItemType? ItemType { get; init; }
...
/// <summary>备注；为空表示未填写。</summary>
public string? Remark { get; init; }
...
/// <summary>最后实际维护该项目的操作人。</summary>
public Guid OperId { get; init; }
/// <summary>最后实际维护该项目的操作时间。</summary>
public DateTimeOffset OperTime { get; init; }
```

**好在哪**：三点达标。（1）`:6` 用「为空表示全部类型」明确了 `null` 的业务含义（不筛选），而非只说「可选项目类型」——恰是规范 §3 要求的「空值语义」。（2）`:52` 的「为空表示未填写」区分了「未填写」与「无此属性」，避免了读者对 `null` 的歧义。（3）`:139–142` 是全仓唯一把 `OperId`/`OperTime` 写成业务语义（「最后实际维护该项目的操作人/时间」）而非「操作人」「操作时间」的地方，与本报告 §3.1 中 54 次裸「操作人」形成直接对照——**同一语义、同一代码库，已存在达标写法，整改无须新造标准**。

### 样例 3：`Application.Contracts\Queries\IMedicalRecognitionReportQueryAppService.cs:5,15` — 接口职责与业务过滤条件写明

```csharp
/// <summary>提供标准医疗项目目录的四项只读查询。</summary>
public interface IMedicalRecognitionReportQueryAppService : IApplicationService
{
  ...
  /// <summary>查询仅三级状态均启用的层级目录。</summary>
  Task<EffectiveMedicalStandardCatalogReadModel> QueryEffectiveMedicalStandardCatalogAsync(EffectiveMedicalStandardCatalogQueryRequest request);
}
```

**好在哪**：`:5` 的类型级 `summary` 说明了整体职责并给出成员数量（「四项」），充当了规范 §2 要求的「整体业务职责」声明，弥补了接口无主声明文档的缺口；`:15` 的方法 `summary` 用「仅三级状态均启用」精确描述了 SQL 中 `i.is_valid = true and g.is_valid = true and c.is_valid = true`（`MedicalRecognitionReportQuery.xml:37`）的业务过滤口径，而不是复述方法名「查询当前有效标准目录」。对照 `IMedicalRecognitionReportAppService.cs`（16 个方法 0 注释），证明同层同族接口的达标写法已经存在。

---

## 5. 审查局限

1. **无自动化基线可交叉验证**：`server\.editorconfig` 与 `server\Directory.Build.props` 未配置任何 XML 文档分析器或 `GenerateDocumentationFile`（见 §3.8），因此无法用「Analyzer 结论」交叉校验本次人工判定，也无法确认是否存在被编译器静默接受的其它文档形式（如局部函数、`record` 位置参数）。本报告的类型/成员计数全部由脚本对源文件直接统计，可复现，但不具备 Analyzer 的语义精度（例如 `partial` 跨文件合并、SourceGen 生成成员不计入）。
2. **三个同构文件族的抽样而非逐行**：116 个文件中约 60 个逐行通读。`Application.Contracts\...\Requests\`（16）、`Dtos\`（19）、`Domain\...\Commands\`（16）、`Domain\...\`实体（19）、`Domain.Share\...\Events\`（16）、`Enums\`（13）六族已通过全量机器扫描确认：类型级文档仅 14 个（全部位于 `Application.Contracts\Queries\` + `MedicalStandardUsageStatus`）、`<param>/<returns>/<exception>` 全仓 0 个、`summary` 文本仅 49 种内联式与 199 种多行式且无一超出「裸标签」模式。抽样文件（各类族首尾各若干）均与该扫描结论一致，未发现偏离样本。**残余风险**：若某族中间存在个别写法不同的文件，其独特性可能未被逐行确认。
3. **未做运行时验证**：本审查为静态只读。§3.7 中关于「`IsNotEmpty` 对 `0` 判空导致更新静默跳过」「`Guid.Empty` 被省略」「`MedicalStandardGroupHasItems` 为死语句」等结论来自 XML 文本与 C# 调用点的静态比对，未启动数据库或执行语句验证实际生成的 SQL。这些结论用于说明「注释应说明的行为」，不构成行为缺陷的最终定论。
4. **SqlMap/SQL 脚本的性质界定**：`.agents/instructions/csharp-documentation-and-reuse.md` §2 的 XML 注释覆盖要求面向 C# 代码。本报告对 19 个 SqlMap 与 19 个 SQL 脚本的注释要求，仅依据规范 §3 末段「Repository、Infrastructure…可以在正确性需要时说明事务与补偿顺序、锁和唯一约束、协议兼容、资源清理及特殊 SQL」推导，属规范精神的外推，**不是 §2 强制覆盖范围的直接要求**；相关条目可按「建议」而非「缺口」定级。
5. **业务事实判定依赖 SRS/UML 之外的代码内证**：`OrganizationCode` 可信性、`RecognitionDurationDays = 0` 的业务预期、互认配置链路时区选择等结论，均以代码现状（`MedicalRecognitionReportAppService.cs:140–170` 未覆盖组织编码、`:144` 用 `DateTimeOffset.Now`）为据，未与 `docs\需求规约SRS.md` / `docs\uml\*.wsd` 的业务契约逐条比对。若设计已明确这些行为，则相应条目应从「缺陷」降级为「注释未同步设计」。
6. **测试代码严重度已降一档**：§2.3.6 的测试条目按任务要求降档处理，未计入优先整改建议的估算。
7. **未评估生成代码与 `*Module.cs`**：`obj\`、`bin\`、`*.g.cs`、`Global.cs`、`MedicalRecognition*Module.cs` 按规范与任务范围排除，其注释状态不在本报告结论内。

---

## 6. 最该优先修的 3 件事

1. **补 11 个整文件零注释的核心生产文件（约 220 个成员 / 11 个文件）** —— `MedicalRecognitionReportManager.cs`（27）、`MedicalRecognitionReportRepository.cs`（30）、`IMedicalRecognitionReportRepository.cs`（25）、`MedicalRecognitionReportAppService.cs`（20）、`IMedicalRecognitionReportAppService.cs`（17）、`MedicalRecognitionReportQueryAppService.cs`（15）、`MedicalRecognitionReportQueryModels.cs`（37）、`MedicalRecognitionReportQueryRepository.cs`（8）、`IMedicalRecognitionReportQueryRepository.cs`（5）、`MedicalRecognitionReportDataMaps.cs`（35）、`MedicalRecognitionReportConst.cs`（2）。这 11 个文件集中了全部业务语义风险点，且改动不触及生成代码。

2. **全仓首次引入 `param` / `returns` / `exception` 三类节点（当前 0 个，需新增约 260 处）** —— 优先 `MedicalRecognitionReportManager.cs` 的 36 处显式抛出（补 `exception`）、16 个 `Task<bool>` 契约方法（补 `returns`，明确「成功 / 幂等无变更 / 未找到」的返回约定）、以及全部带参方法（补 `param`，重点写来源、可信性、空值与顺序）。

3. **把 5 类高风险行为差异写进注释（约 12 处，无代码改动）** —— ① `AppService` 内 `DateTimeOffset.UtcNow`（12 处）与 `DateTimeOffset.Now`（4 处）并存；② 同一文件 12 处「解析失败抛异常」与 4 处「静默写入 `Guid.Empty`」并存；③ 4 个互认配置方法缺 `[WorkUnit]`；④ `MutualRecognitionItem.xml:28,31` 缺 `and is_valid` 守卫、`:17` `IsNotEmpty` 使 `0` 静默不生效；⑤ 三个唯一索引对应的「先查后写」并发窗口与冲突错误形态。这 5 类正是规范 §3「注释不得隐藏业务规则、可信数据来源、空值语义、异常、顺序或兼容边界」直接命中的对象。

**工作量估算**：涉及 **11 个必改文件 + 约 20 个同步补注释文件**（16 个 Command 的事件工厂方法、16 个 DomainEvent 类型、13 个枚举类型级、16 个 Request/DTO 类型级）；成员级约 **490 个成员**需补注释节点（其中类型级 115、`param`/`returns`/`exception` 约 260、高风险行为差异说明约 12、SqlMap/SQL 技术注释约 25）。若按「先补 11 个核心文件的类型与方法级 summary + `exception`」为第一轮，可覆盖全部业务语义风险点，工作量约为总量的 45%（约 220 个成员 / 11 个文件）；第二、三轮处理契约层与 SQL 层。
