# 阶段 1（标准项目目录维护）后端手写生产代码 XML 注释覆盖 —— 全量复核报告

## 0. 复核时间戳与并行写入声明

- **本报告数据快照时间戳：`2026-09-15 11:15:40 +08:00`**（末次全量重算完成时刻；随后 11:15:43 复核计数未变）。
- 复核期间确认存在**其他子代理并行补注释**：同一批文件在本次复核过程中被多次改写，同一分析口径下的统计在 `11:13:47` 到 `11:15:40` 之间发生变化（例如 `Domain.Share\Enums\`、`Application.Contracts\...\Dtos\`、`Domain\...\Commands\`、`Domain.Share\...\Events\`、`Domain\...\IMedicalRecognitionReportRepository.cs` 等均由「存在缺口」变为「合规」）。
- **结论有效期声明**：本报告的 A/B/C 类结论仅代表上述时间戳的仓库状态。**若在复核期间或之后有并行写入，以本报告时间戳的状态为准**；负责人据本报告派单前，应先按第 1 节方法复算一次，确认缺口未再被并行消耗。
- 本次复核**未修改任何生产代码**，只新增本报告（以及复核用的临时脚本/中间数据，见第 9 节，可整体删除）。

---

## 1. 复核范围与方法（如何做到逐成员而非抽样）

### 1.1 依据

逐字读完 `E:\MedSync\Dy.MedicalRecognition\.agents\instructions\csharp-documentation-and-reuse.md`（114 行），重点：

- 第 2 节：类型、构造函数、方法、属性、字段、事件、枚举项、参数、返回值、可预期异常都必须有中文 XML 文档；覆盖 public/protected/internal/private/partial/局部函数/只读字段；**不得因为名称、类型、访问级别、代码行数或调用次数看似容易推断而省略**；接口实现无额外业务行为可用 `inheritdoc`，但**成员仍必须保留 XML 文档节点**。
- 第 3 节：`summary` 不复述类型和方法名称；`param` 说明来源/单位/可信性/空值/顺序；`returns` 不写「返回结果」；可预期业务和参数异常用 `exception` 说明触发条件；禁止把临时计划编号、整改批次写入生产代码；禁止 `seam`、「事实重读」、「事务内聚」、「能力闭环」、「适配边界」、「通过依赖注入调用」、「完成聚合编排」等表达。

### 1.2 方法：AST 全量枚举，不抽样

为避免「抽样统计」与「正则猜成员」两类失真，复核用 Roslyn 语法树做**机械枚举**：

1. 用 `Microsoft.CodeAnalysis.CSharp.CSharpSyntaxTree::ParseText` 解析每个文件，取 `DescendantNodes()`，只保留成员节点：`BaseTypeDeclarationSyntax`（class/interface/enum/record/struct）、`MethodDeclarationSyntax`、`ConstructorDeclarationSyntax`、`PropertyDeclarationSyntax`、`FieldDeclarationSyntax`、`EventFieldDeclarationSyntax`、`EventDeclarationSyntax`、`EnumMemberDeclarationSyntax`、`DelegateDeclarationSyntax`。
2. 对**每个**节点取 `GetLeadingTrivia()` 中的文档注释节点（实测 `RawKind = 8544` / `8545`），据此判定 A 类；把文档文本重组为 XML 用 `XmlDocument` 解析（**不是正则**）提取 `<summary>/<param>/<returns>/<exception>/<inheritdoc>`。
3. 对**每个**节点用 `DescendantNodes()` 单独收集 `ThrowStatementSyntax` 与 `ThrowExpressionSyntax`，取行号与异常类型；把 `throw Business(...)`、`throw Input(...)` 这类**异常工厂调用**解析为其声明返回类型（本仓库为 `InvalidOperationException`），再与同成员的 `<exception cref>` 比对。
4. 判定口径（严格按任务给定）：
   - **A 类**：该成员引导 trivia 中**没有任何** `///` 文档节点。
   - **B 类**：有 `summary`，但只重复成员名/类型名（如 `ID`、`备注`、`操作人`、`操作时间`、`名称`、`分类 ID`），**全部列出原文**。
   - **C 类**：有注释但缺规范要求节点 —— 方法缺 `<param>`、有返回值的方法缺 `<returns>`、显式 `throw` 无对应 `<exception>`、`summary` 为空。
   - **D 类**：合规，只给计数。

### 1.3 范围与排除

复核范围与任务给定一致；`obj\`、`bin\`、`*.g.cs` 全量排除。

**补充排除说明**：任务要求排除「文件名含 `Mutual` 的阶段 2 文件」。实际扫描到 **`Mutual` 仅出现在成员名而非文件名**：`MedicalRecognitionReportAppService.CreateMutualRecognitionItemAsync` 等 4 个方法、`MedicalRecognitionReportManager.CreateMutualRecognitionItemAsync` 等 4 个方法。这些属于阶段 2，已**按成员粒度排除**（共剔除 8 个成员 + 其所在文件的 12 个方法级 throw 归并行）。若不排除，AppService/Manager 两族的 A 类数字会各虚增 4。

最终纳入统计：**100 个文件、931 个成员**。

### 1.4 统计口径说明（避免误读）

- 类型声明的 `DescendantNodes()` 天然包含其所有成员，因此类型行的「缺 `<exception>`」判定**不代表该类型自身需要 `<exception>` 契约**。本报告在显式 `throw` 对照表（第 6 节）中已**剔除类型聚合行**，只按成员逐一判定；第 5 节 C 类中**仅 2 条属于该口径**，已单独标注为「弱结论」。
- 各文件族成员数之和 = 931，A+B+C+D = 101+47+3+780 = 931，无重复计数、无遗漏。

---

## 2. 总览统计

| 文件族 | 成员总数 | A 类 | B 类 | C 类 | D 类 |
|---|---:|---:|---:|---:|---:|
| Request | 38 | 0 | 0 | 0 | 38 |
| Dto | 272 | 0 | 0 | 0 | 272 |
| Command | 74 | 0 | 0 | 0 | 74 |
| Event | 66 | 0 | 0 | 0 | 66 |
| Entity | 272 | 18 | 47 | 0 | 207 |
| Manager | 23 | 0 | 0 | 1 | 22 |
| AppService | 15 | 0 | 0 | 1 | 14 |
| AppServiceInterface | 13 | 13 | 0 | 0 | 0 |
| RepositoryInterface | 21 | 0 | 0 | 0 | 21 |
| RepositoryImpl | 27 | 27 | 0 | 0 | 0 |
| RepositoryQueryImpl | 8 | 8 | 0 | 0 | 0 |
| RepositoryInfra | 2 | 2 | 0 | 0 | 0 |
| Enum | 62 | 0 | 0 | 0 | 62 |
| Const | 2 | 2 | 0 | 0 | 0 |
| Query | 16 | 16 | 0 | 0 | 0 |
| Test | 20 | 15 | 0 | 1 | 4 |
| **合计** | **931** | **101** | **47** | **3** | **780** |

**明确合规（无需修复、不凑数）的文件族**：`Request`(38/38)、`Dto`(272/272)、`Command`(74/74)、`Event`(66/66)、`Enum`(62/62)、`RepositoryInterface`(21/21) —— **全部成员均达 D 类**。

> 注：`RepositoryInterface` 的 21 个成员在本时间戳已全部具备文档（复核早期为 21 个 A 类，已被并行补齐）；`inheritdoc` 使用数为 **0**。

**缺口总量：151 = A 类 101 + B 类 47 + C 类 3。**

---

## 3. A 类清单（完全缺失，共 101 条）

| 编号 | 文件:行 | 成员 | 缺什么 | 严重度 |
|---|---|---|---|---|
| A-01 | `Application.Contracts\MedicalRecognitionReportAggregate\IMedicalRecognitionReportAppService.cs:7` | `IMedicalRecognitionReportAppService`（接口） | 整个接口无 XML 注释 | 高 |
| A-02 | 同上 `:9` | `CreateMedicalStandardCategoryAsync` | 无 XML 注释 | 高 |
| A-03 | 同上 `:10` | `UpdateMedicalStandardCategoryAsync` | 无 XML 注释 | 高 |
| A-04 | 同上 `:11` | `EnableMedicalStandardCategoryAsync` | 无 XML 注释 | 高 |
| A-05 | 同上 `:12` | `DisableMedicalStandardCategoryAsync` | 无 XML 注释 | 高 |
| A-06 | 同上 `:13` | `CreateMedicalStandardGroupAsync` | 无 XML 注释 | 高 |
| A-07 | 同上 `:14` | `UpdateMedicalStandardGroupAsync` | 无 XML 注释 | 高 |
| A-08 | 同上 `:15` | `EnableMedicalStandardGroupAsync` | 无 XML 注释 | 高 |
| A-09 | 同上 `:16` | `DisableMedicalStandardGroupAsync` | 无 XML 注释 | 高 |
| A-10 | 同上 `:17` | `CreateMedicalStandardItemAsync` | 无 XML 注释 | 高 |
| A-11 | 同上 `:18` | `ChangeMedicalStandardItemRemarkAsync` | 无 XML 注释 | 高 |
| A-12 | 同上 `:19` | `EnableMedicalStandardItemAsync` | 无 XML 注释 | 高 |
| A-13 | 同上 `:20` | `DisableMedicalStandardItemAsync` | 无 XML 注释 | 高 |
| A-14 | `Application\Queries\MedicalRecognitionReportQueryAppService.cs:6` | `MedicalRecognitionReportQueryAppService`（类） | 类型无 XML 注释 | 高 |
| A-15 | 同上 `:8` | `repository`（私有只读依赖字段） | 字段无 XML 注释（规范第 2 节明确要求说明其负责读取的业务对象） | 高 |
| A-16 | 同上 `:10` | 构造函数 `MedicalRecognitionReportQueryAppService` | 无 XML 注释 | 高 |
| A-17 | 同上 `:12` | `QueryMedicalStandardCategoryListAsync` | 无 XML 注释 | 高 |
| A-18 | 同上 `:19` | `QueryMedicalStandardGroupListAsync` | 无 XML 注释 | 高 |
| A-19 | 同上 `:26` | `QueryMedicalStandardItemListAsync` | 无 XML 注释 | 高 |
| A-20 | 同上 `:34` | `QueryEffectiveMedicalStandardCatalogAsync` | 无 XML 注释 | 高 |
| A-21 | 同上 `:46` | `MapType` | 无 XML 注释 | 中 |
| A-22 | 同上 `:52` | `MapCategory` | 无 XML 注释 | 中 |
| A-23 | 同上 `:58` | `MapGroup` | 无 XML 注释 | 中 |
| A-24 | 同上 `:67` | `Map`（重载 1） | 无 XML 注释 | 中 |
| A-25 | 同上 `:72` | `Map`（重载 2） | 无 XML 注释 | 中 |
| A-26 | 同上 `:77` | `Map`（重载 3） | 无 XML 注释 | 中 |
| A-27 | 同上 `:82` | `ValidateOptionalId` | 无 XML 注释 | 中 |
| A-28 | 同上 `:87` | `ValidateOptionalText` | 无 XML 注释 | 中 |
| A-29 | 同上 `:92` | `ValidateOptionalItemType` | 无 XML 注释 | 中 |
| A-30 | `Domain\MedicalRecognitionReportAggregate\ExaminationItem.cs:6` | `ExaminationItem`（实体） | 类型无 XML 注释 | 高 |
| A-31 | 同上 `ExaminationReportContent.cs:6` | `ExaminationReportContent`（实体） | 类型无 XML 注释 | 高 |
| A-32 | 同上 `ExaminationSite.cs:6` | `ExaminationSite`（实体） | 类型无 XML 注释 | 高 |
| A-33 | 同上 `LaboratoryAntimicrobialSusceptibility.cs:6` | `LaboratoryAntimicrobialSusceptibility`（实体） | 类型无 XML 注释 | 高 |
| A-34 | 同上 `LaboratoryBacteriaResult.cs:6` | `LaboratoryBacteriaResult`（实体） | 类型无 XML 注释 | 高 |
| A-35 | 同上 `LaboratoryReportContent.cs:6` | `LaboratoryReportContent`（实体） | 类型无 XML 注释 | 高 |
| A-36 | 同上 `LaboratoryResultItem.cs:6` | `LaboratoryResultItem`（实体） | 类型无 XML 注释 | 高 |
| A-37 | 同上 `MedicalRecognitionReport.cs:6` | `MedicalRecognitionReport`（聚合根） | 类型无 XML 注释 | 高 |
| A-38 | 同上 `MedicalReportVersion.cs:6` | `MedicalReportVersion`（实体） | 类型无 XML 注释 | 高 |
| A-39 | 同上 `MedicalStandardCategory.cs:6` | `MedicalStandardCategory`（实体） | 类型无 XML 注释 | 高 |
| A-40 | 同上 `MedicalStandardGroup.cs:6` | `MedicalStandardGroup`（实体） | 类型无 XML 注释 | 高 |
| A-41 | 同上 `MedicalStandardItem.cs:6` | `MedicalStandardItem`（实体） | 类型无 XML 注释 | 高 |
| A-42 | 同上 `OrganizationHospitalBranchRecognitionAmount.cs:6` | `OrganizationHospitalBranchRecognitionAmount`（实体） | 类型无 XML 注释 | 高 |
| A-43 | 同上 `PlatformPatient.cs:6` | `PlatformPatient`（实体） | 类型无 XML 注释 | 高 |
| A-44 | 同上 `RecognitionMatchItem.cs:6` | `RecognitionMatchItem`（实体） | 类型无 XML 注释 | 高 |
| A-45 | 同上 `RecognitionMatchRecord.cs:6` | `RecognitionMatchRecord`（实体） | 类型无 XML 注释 | 高 |
| A-46 | 同上 `RecognitionProcessingResult.cs:6` | `RecognitionProcessingResult`（实体） | 类型无 XML 注释 | 高 |
| A-47 | 同上 `RecognitionReference.cs:6` | `RecognitionReference`（实体） | 类型无 XML 注释 | 高 |
| A-48 | `Domain.Share\MedicalRecognitionReportAggregate\MedicalRecognitionReportConst.cs:3` | `MedicalRecognitionReportConst`（常量类） | 类型无 XML 注释 | 中 |
| A-49 | 同上 `:5` | `AggregateId`（常量字段） | 字段无 XML 注释 | 中 |
| A-50 | `Repository\MedicalRecognitionReportAggregate\MedicalRecognitionReportRepository.cs:8` | `MedicalRecognitionReportRepository`（类） | 类型无 XML 注释 | 高 |
| A-51 | 同上 `:10` | `MedicalRecognitionReportScope`（私有只读依赖字段） | 字段无 XML 注释 | 高 |
| A-52 | 同上 `:11` | `MedicalStandardCategoryScope` | 字段无 XML 注释 | 高 |
| A-53 | 同上 `:12` | `MedicalStandardGroupScope` | 字段无 XML 注释 | 高 |
| A-54 | 同上 `:13` | `MedicalStandardItemScope` | 字段无 XML 注释 | 高 |
| A-55 | 同上 `:14` | `dataMapper`（私有只读依赖字段） | 字段无 XML 注释 | 高 |
| A-56 | 同上 `:15` | `DataMapper`（属性） | 属性无 XML 注释 | 中 |
| A-57 | 同上 `:16` | `CreateGuid` | 无 XML 注释（含主键生成规则语义） | 高 |
| A-58 | 同上 `:17` | `CreateMedicalStandardCategoryAsync` | 无 XML 注释 | 高 |
| A-59 | 同上 `:18` | `UpdateMedicalStandardCategoryAsync` | 无 XML 注释 | 高 |
| A-60 | 同上 `:19` | `EnableMedicalStandardCategoryAsync` | 无 XML 注释 | 高 |
| A-61 | 同上 `:20` | `DisableMedicalStandardCategoryAsync` | 无 XML 注释 | 高 |
| A-62 | 同上 `:21` | `CreateMedicalStandardGroupAsync` | 无 XML 注释 | 高 |
| A-63 | 同上 `:22` | `UpdateMedicalStandardGroupAsync` | 无 XML 注释 | 高 |
| A-64 | 同上 `:23` | `EnableMedicalStandardGroupAsync` | 无 XML 注释 | 高 |
| A-65 | 同上 `:24` | `DisableMedicalStandardGroupAsync` | 无 XML 注释 | 高 |
| A-66 | 同上 `:25` | `CreateMedicalStandardItemAsync` | 无 XML 注释 | 高 |
| A-67 | 同上 `:26` | `ChangeMedicalStandardItemRemarkAsync` | 无 XML 注释 | 高 |
| A-68 | 同上 `:27` | `EnableMedicalStandardItemAsync` | 无 XML 注释 | 高 |
| A-69 | 同上 `:28` | `DisableMedicalStandardItemAsync` | 无 XML 注释 | 高 |
| A-70 | 同上 `:29` | `GetMedicalStandardCategoryByIdAsync` | 无 XML 注释 | 高 |
| A-71 | 同上 `:30` | `MedicalStandardCategoryNameExistsAsync` | 无 XML 注释 | 高 |
| A-72 | 同上 `:31` | `MedicalStandardCategoryHasGroupsAsync` | 无 XML 注释 | 高 |
| A-73 | 同上 `:32` | `GetMedicalStandardGroupByIdAsync` | 无 XML 注释 | 高 |
| A-74 | 同上 `:33` | `MedicalStandardGroupNameExistsAsync` | 无 XML 注释 | 高 |
| A-75 | 同上 `:34` | `GetMedicalStandardItemByIdAsync` | 无 XML 注释 | 高 |
| A-76 | 同上 `:35` | `MedicalStandardItemCodeExistsAsync` | 无 XML 注释 | 高 |
| A-77 | `Repository\MedicalRecognitionRepositoryModule.cs:6` | `MedicalRecognitionRepositoryModule`（类） | 类型无 XML 注释 | 中 |
| A-78 | 同上 `:9` | 构造函数 `MedicalRecognitionRepositoryModule` | 无 XML 注释 | 中 |
| A-79 | `Repository\Queries\MedicalRecognitionReportQueryRepository.cs:8` | `MedicalRecognitionReportQueryRepository`（类） | 类型无 XML 注释 | 高 |
| A-80 | 同上 `:10` | `SqlScope`（私有只读依赖字段） | 字段无 XML 注释 | 高 |
| A-81 | 同上 `:11` | `dataMapper`（私有只读依赖字段） | 字段无 XML 注释 | 高 |
| A-82 | 同上 `:13` | 构造函数 `MedicalRecognitionReportQueryRepository` | 无 XML 注释 | 高 |
| A-83 | 同上 `:15` | `QueryMedicalStandardCategoryListAsync` | 无 XML 注释 | 高 |
| A-84 | 同上 `:18` | `QueryMedicalStandardGroupListAsync` | 无 XML 注释 | 高 |
| A-85 | 同上 `:21` | `QueryMedicalStandardItemListAsync` | 无 XML 注释 | 高 |
| A-86 | 同上 `:24` | `QueryEffectiveMedicalStandardCatalogAsync` | 无 XML 注释 | 高 |
| A-87 | `Tests\SmokeTests.cs:5` | `SmokeTests`（测试类） | 类型无 XML 注释 | 低（测试降一档） |
| A-88 | 同上 `:7` | `Generated_contract_is_available` | 无 XML 注释 | 低 |
| A-89 | `Tests\Stage1ArchitectureTests.cs:9` | `Stage1ArchitectureTests`（测试类） | 类型无 XML 注释 | 低 |
| A-90 | 同上 `:135` | `Query_contract_is_separated_from_write_service_and_repository_implementation` | 无 XML 注释 | 低 |
| A-91 | 同上 `:145` | `Update_group_contract_cannot_change_parent_category` | 无 XML 注释 | 低 |
| A-92 | `Tests\Stage1SqlMapProbeTests.cs:10` | `Stage1SqlMapProbeTests`（测试类） | 类型无 XML 注释 | 低 |
| A-93 | 同上 `:12` | `Create_statements_default_is_valid_without_request_parameter` | 无 XML 注释 | 低 |
| A-94 | 同上 `:26` | `Enable_and_disable_statements_keep_boolean_literals` | 无 XML 注释 | 低 |
| A-95 | 同上 `:41` | `Runtime_registration_reports_full_sql_ids_without_opening_database` | 无 XML 注释 | 低 |
| A-96 | 同上 `:98` | `GetRegisteredKeys` | 无 XML 注释 | 低 |
| A-97 | 同上 `:116` | `Invoke` | 无 XML 注释 | 低 |
| A-98 | 同上 `:121` | `Flatten` | 无 XML 注释 | 低 |
| A-99 | 同上 `:140` | `FindHostOutputDirectory` | 无 XML 注释 | 低 |
| A-100 | 同上 `:153` | `FindRepositorySqlMap` | 无 XML 注释 | 低 |
| A-101 | 同上 `:166` | `LoadAssembly` | 无 XML 注释 | 低 |

**A 类分布小结**：Repository 实现层（`RepositoryImpl` 27 + `RepositoryQueryImpl` 8 + `RepositoryInfra` 2 = **37 条**）与 Query 应用服务（16）、接口/实体类型声明（13+18+2=33）、测试（15）构成全部 A 类。**整个 Repository 项目（`.cs`，不含 `.xml`）当前 A 类覆盖率 37/37 = 100% 缺失**，与负责人上一轮发现的「漏文件」属同一性质。

---

## 4. B 类清单（复述名称，共 47 条，含原文）

全部 47 条均位于 **`Domain\MedicalRecognitionReportAggregate\` 的 18 个实体类**，形态高度一致：实体属性用中文短标签复述属性名，未说明业务语义、可信来源、空值语义或写入时机。

| 编号 | 文件:行 | 成员 | summary 原文 | 缺什么 | 严重度 |
|---|---|---|---|---|---|
| B-01 | `ExaminationItem.cs:12` | `Id` | 「ID」 | 仅复述成员名，未说明主键来源与生成时机 | 高 |
| B-02 | `ExaminationItem.cs:32` | `OperId` | 「操作人」 | 仅复述成员名，未说明取自登录上下文 | 高 |
| B-03 | `ExaminationItem.cs:36` | `OperTime` | 「操作时间」 | 仅复述成员名，未说明为服务端 UTC 时钟 | 高 |
| B-04 | `ExaminationReportContent.cs:12` | `Id` | 「ID」 | 同上 | 高 |
| B-05 | `ExaminationReportContent.cs:92` | `OperId` | 「操作人」 | 同上 | 高 |
| B-06 | `ExaminationReportContent.cs:96` | `OperTime` | 「操作时间」 | 同上 | 高 |
| B-07 | `ExaminationSite.cs:12` | `Id` | 「ID」 | 同上 | 高 |
| B-08 | `ExaminationSite.cs:28` | `OperId` | 「操作人」 | 同上 | 高 |
| B-09 | `ExaminationSite.cs:32` | `OperTime` | 「操作时间」 | 同上 | 高 |
| B-10 | `LaboratoryAntimicrobialSusceptibility.cs:12` | `Id` | 「ID」 | 同上 | 高 |
| B-11 | `LaboratoryAntimicrobialSusceptibility.cs:48` | `MicValue` | 「MIC」 | 用缩写复述成员名，未说明单位与判定语义 | 高 |
| B-12 | `LaboratoryAntimicrobialSusceptibility.cs:80` | `OperId` | 「操作人」 | 同上 | 高 |
| B-13 | `LaboratoryAntimicrobialSusceptibility.cs:84` | `OperTime` | 「操作时间」 | 同上 | 高 |
| B-14 | `LaboratoryBacteriaResult.cs:12` | `Id` | 「ID」 | 同上 | 高 |
| B-15 | `LaboratoryBacteriaResult.cs:92` | `OperId` | 「操作人」 | 同上 | 高 |
| B-16 | `LaboratoryBacteriaResult.cs:96` | `OperTime` | 「操作时间」 | 同上 | 高 |
| B-17 | `LaboratoryReportContent.cs:12` | `Id` | 「ID」 | 同上 | 高 |
| B-18 | `LaboratoryReportContent.cs:76` | `OperId` | 「操作人」 | 同上 | 高 |
| B-19 | `LaboratoryReportContent.cs:80` | `OperTime` | 「操作时间」 | 同上 | 高 |
| B-20 | `LaboratoryResultItem.cs:12` | `Id` | 「ID」 | 同上 | 高 |
| B-21 | `LaboratoryResultItem.cs:44` | `LoincCode` | 「LOINC编码」 | 仅展开缩写后复述成员名 | 高 |
| B-22 | `LaboratoryResultItem.cs:96` | `OperId` | 「操作人」 | 同上 | 高 |
| B-23 | `LaboratoryResultItem.cs:100` | `OperTime` | 「操作时间」 | 同上 | 高 |
| B-24 | `MedicalRecognitionReport.cs:12` | `Id` | 「ID」 | 同上 | 高 |
| B-25 | `MedicalRecognitionReport.cs:56` | `OperId` | 「操作人」 | 同上 | 高 |
| B-26 | `MedicalRecognitionReport.cs:60` | `OperTime` | 「操作时间」 | 同上 | 高 |
| B-27 | `MedicalReportVersion.cs:12` | `Id` | 「ID」 | 同上 | 高 |
| B-28 | `MedicalReportVersion.cs:164` | `OperId` | 「操作人」 | 同上 | 高 |
| B-29 | `MedicalReportVersion.cs:168` | `OperTime` | 「操作时间」 | 同上 | 高 |
| B-30 | `OrganizationHospitalBranchRecognitionAmount.cs:12` | `Id` | 「ID」 | 同上 | 高 |
| B-31 | `OrganizationHospitalBranchRecognitionAmount.cs:36` | `OperId` | 「操作人」 | 同上 | 高 |
| B-32 | `OrganizationHospitalBranchRecognitionAmount.cs:40` | `OperTime` | 「操作时间」 | 同上 | 高 |
| B-33 | `PlatformPatient.cs:12` | `Id` | 「ID」 | 同上 | 高 |
| B-34 | `PlatformPatient.cs:36` | `OperId` | 「操作人」 | 同上 | 高 |
| B-35 | `PlatformPatient.cs:40` | `OperTime` | 「操作时间」 | 同上 | 高 |
| B-36 | `RecognitionMatchItem.cs:12` | `Id` | 「ID」 | 同上 | 高 |
| B-37 | `RecognitionMatchItem.cs:36` | `OperId` | 「操作人」 | 同上 | 高 |
| B-38 | `RecognitionMatchItem.cs:40` | `OperTime` | 「操作时间」 | 同上 | 高 |
| B-39 | `RecognitionMatchRecord.cs:12` | `Id` | 「ID」 | 同上 | 高 |
| B-40 | `RecognitionMatchRecord.cs:52` | `OperId` | 「操作人」 | 同上 | 高 |
| B-41 | `RecognitionMatchRecord.cs:56` | `OperTime` | 「操作时间」 | 同上 | 高 |
| B-42 | `RecognitionProcessingResult.cs:12` | `Id` | 「ID」 | 同上 | 高 |
| B-43 | `RecognitionProcessingResult.cs:64` | `OperId` | 「操作人」 | 同上 | 高 |
| B-44 | `RecognitionProcessingResult.cs:68` | `OperTime` | 「操作时间」 | 同上 | 高 |
| B-45 | `RecognitionReference.cs:12` | `Id` | 「ID」 | 同上 | 高 |
| B-46 | `RecognitionReference.cs:44` | `OperId` | 「操作人」 | 同上 | 高 |
| B-47 | `RecognitionReference.cs:48` | `OperTime` | 「操作时间」 | 同上 | 高 |

**B 类原文去重汇总（8 种形态）**：

| summary 原文 | 出现次数 | 对应成员 |
|---|---:|---|
| 「ID」 | 18 | 18 个实体的 `Id` |
| 「操作人」 | 18 | 18 个实体的 `OperId` |
| 「操作时间」 | 18 | 18 个实体的 `OperTime` |
| 「备注」 | 0（本时间戳） | — |
| 「分类ID」 | 0（本时间戳） | — |
| 「分组ID」 | 0（本时间戳） | — |
| 「MIC」 | 1 | `MicValue` |
| 「LOINC编码」 | 1 | `LoincCode` |

> 说明：复核早期（`11:13:47`）B 类曾达 118 条，含 Command 族 29 条、Event 族 31 条、Dto 族 6 条（原文同为「ID」「操作人」「操作时间」「备注」「分类ID」「分组ID」）；这些已在复核期间被并行补写为具体业务语义（现均为 D 类），本报告不将其列为未闭合缺口。

**典型对照（同一聚合内同类语义的正确写法，可作修复范本）**：
- `Application.Contracts\...\Requests\CreateMedicalStandardGroupRequest.cs:17` —— `CategoryId` 的 summary 为「新分组所属的分类标识；必须为非空 Guid，由应用层与领域层校验，DataAnnotations 无法约束值类型空值。」（说明了必填、校验层次与约束边界），**符合规范**。
- 而实体 `MedicalStandardGroup.cs:16` 的 `CategoryId` 仅为「分类ID」，属 B 类（该条在本次快照中已因并行写入而不在 B 清单内，仅作对照说明写法差异来源）。

---

## 5. C 类清单（质量不达标，共 3 条）

| 编号 | 文件:行 | 成员 | 缺什么 | 严重度 |
|---|---|---|---|---|
| C-01 | `Application\MedicalRecognitionReportAggregate\MedicalRecognitionReportAppService.cs:16` | 类型 `MedicalRecognitionReportAppService` | 类型级文档 `<summary>` 未声明该类型对外可预期的 `InvalidOperationException`；该类型下的 12 个写入口方法**各自已正确声明** `<exception>`（见第 6 节，L43/60/77/94/112/129/146/163/181/198/215/232 全部「已声明」），故此处仅为**类型级聚合口径的弱结论**，非真实契约缺失 | 低 |
| C-02 | `Domain\MedicalRecognitionReportAggregate\MedicalRecognitionReportManager.cs:15` | 类型 `MedicalRecognitionReportManager` | 同上：类型级文档未声明 `InvalidOperationException`；该类型下 36 个 `throw` **全部已由所属方法声明** `<exception>` | 低 |
| C-03 | `Tests\Stage1ArchitectureTests.cs:124` | `FindRepositoryRoot` | 方法有返回值但缺 `<returns>`；且 L132 的 `throw new InvalidOperationException` 无对应 `<exception>` 说明触发条件 | 低（测试降一档） |

**C 类真实性核查结论**：C-01、C-02 属「类型声明的 `DescendantNodes()` 聚合了成员 throw」造成的口径性结论，**不建议**为类型补 `<exception>`；真正的成员级 `<exception>` 覆盖率为 **48/58 已声明**，未声明的 10 处全部见第 6 节，其中 7 处落在注释完全缺失的 Query 应用服务（其为 A 类文件，补注释时应一并补 `<exception>`），3 处落在测试文件。

---

## 6. 显式 `throw` 与 `<exception>` 对照表

**全量口径**：范围文件内共 **58 处**显式 `throw`（含 `throw` 语句与 `throw` 表达式），逐处判定如下。已剔除类型聚合行，按成员逐一归属。

### 6.1 判定汇总

| 状态 | 数量 |
|---|---:|
| 已声明 `<exception>` | 48 |
| **缺 `<exception>`** | **10** |

抛出的异常类型分布：`InvalidOperationException` 52 处、`ArgumentNullException` 4 处、`DirectoryNotFoundException` 1 处、`FileNotFoundException` 1 处。

> 口径说明：`throw Business("…")` 与 `throw Input("…")` 是本仓库的异常工厂调用，其声明返回类型为 `InvalidOperationException`（`MedicalRecognitionReportManager.cs:278`、`:285`），已按返回类型与 `<exception cref>` 比对，**不计为缺失**。

### 6.2 缺 `<exception>` 的 10 处（需修复）

| 编号 | 文件:行 | 异常类型 | 所属成员 | 当前 `<exception>` |
|---|---|---|---|---|
| T-01 | `Application\Queries\MedicalRecognitionReportQueryAppService.cs:14` | `ArgumentNullException` | `QueryMedicalStandardCategoryListAsync` | 无（该成员无任何 XML 注释） |
| T-02 | 同上 `:21` | `ArgumentNullException` | `QueryMedicalStandardGroupListAsync` | 无 |
| T-03 | 同上 `:28` | `ArgumentNullException` | `QueryMedicalStandardItemListAsync` | 无 |
| T-04 | 同上 `:36` | `ArgumentNullException` | `QueryEffectiveMedicalStandardCatalogAsync` | 无 |
| T-05 | 同上 `:84` | `InvalidOperationException` | `ValidateOptionalId` | 无 |
| T-06 | 同上 `:89` | `InvalidOperationException` | `ValidateOptionalText` | 无 |
| T-07 | 同上 `:94` | `InvalidOperationException` | `ValidateOptionalItemType` | 无 |
| T-08 | `Tests\Stage1ArchitectureTests.cs:132` | `InvalidOperationException`（throw 表达式） | `FindRepositoryRoot` | 无 |
| T-09 | `Tests\Stage1SqlMapProbeTests.cs:150` | `DirectoryNotFoundException` | `FindHostOutputDirectory` | 无 |
| T-10 | `Tests\Stage1SqlMapProbeTests.cs:163` | `FileNotFoundException` | `FindRepositorySqlMap` | 无 |

### 6.3 已正确声明的 48 处（对照证据，按文件）

| 文件 | throw 处数 | 状态 | 备注 |
|---|---:|---|---|
| `Application\MedicalRecognitionReportAggregate\MedicalRecognitionReportAppService.cs` | 12 | 全部已声明 | L43/60/77/94/112/129/146/163/181/198/215/232，各方法均声明 `ValidationException` + `InvalidOperationException` |
| `Domain\MedicalRecognitionReportAggregate\MedicalRecognitionReportManager.cs` | 36 | 全部已声明 | L37/40/56/57/59/61/74/76/89/91/107/108/110/124/125/127/140/142/155/157/175/176/177/179/192/194/207/209/222/224（`throw Business/Input`，按 `InvalidOperationException` 声明）、L234/242（throw 表达式）、L249/256/263/271（校验方法） |
| `Application\Queries\MedicalRecognitionReportQueryAppService.cs` | 7 | 全部缺声明 | 即 T-01～T-07 |
| `Tests\Stage1ArchitectureTests.cs` | 1 | 缺声明 | 即 T-08 |
| `Tests\Stage1SqlMapProbeTests.cs` | 2 | 缺声明 | 即 T-09、T-10 |

---

## 7. 计划编号与禁用表达扫描结果

对范围内 **100 个 `.cs` 文件**做逐行全量匹配（注释与代码一并扫描），结果如下。

### 7.1 禁用表达（规范第 3 节第 44 行）

| 禁用表达 | 命中数 |
|---|---:|
| `seam` | 0 |
| 「事实重读」 | 0 |
| 「事务内聚」 | 0 |
| 「能力闭环」 | 0 |
| 「适配边界」 | 0 |
| 「通过依赖注入调用」 | 0 |
| 「完成聚合编排」 | 0 |

**结论：禁用表达 0 命中，合规。**

### 7.2 计划编号与任务痕迹（规范第 3 节第 40 行）

| 模式 | 命中数 |
|---|---:|
| `S1-D` | 0 |
| `V48` | 0 |
| `C61` | 0 |
| `TODO` | 0 |
| `FIXME` | 0 |
| `HACK` | 0 |
| `XXX` | 0 |
| 「整改」 | 0 |
| 「批次」 | 0 |
| 「阶段 1」（`阶段\s*1`） | **3** |

**3 处命中明细（全部位于同一测试文件）**：

| 编号 | 文件:行 | 原文 | 严重度 |
|---|---|---|---|
| P-01 | `Tests\Stage1ArchitectureTests.cs:12` | `/// 校验阶段 1 的写入口只按业务原子边界声明事务：仅"先校验父级启用再写入"的` | 低（测试降一档） |
| P-02 | 同上 `:37` | `/// 阶段 1 的 12 个写入口名称，供事务声明与校验入口断言共用。` | 低 |
| P-03 | 同上 `:47` | `/// 校验阶段 1 的 12 个写入口都在映射领域命令之前执行了公共请求校验，` | 低 |

**判定**：`S1-D`、`V48`、`C61`、批次、整改、TODO 等**均未混入生产代码**。P-01～P-03 是唯一残留，属「临时计划编号」性质；但该文件本身即名为 `Stage1ArchitectureTests.cs`、类名为 `Stage1ArchitectureTests`，测试断言对象就是阶段 1 的 12 个写入口，去掉「阶段 1」会使该测试失去可读性。**建议**：不改代码，仅在阶段 2 收口时评估是否将测试改名为面向不变量（如 `WriteEntrypointTransactionTests`）的命名；若保留，需负责人确认该例外。

---

## 8. 仍未闭合的缺口总数与优先修复顺序

### 8.1 缺口总数

| 类别 | 数量 | 说明 |
|---|---:|---|
| A 类（完全缺失） | 101 | 逐条见第 3 节 |
| B 类（复述名称） | 47 | 逐条见第 4 节 |
| C 类（质量不达标） | 3 | 其中 2 条为类型级弱结论 |
| **合计未闭合缺口** | **151** | 另有 10 处 `throw` 缺 `<exception>`（7 处含在 Query 的 A 类内，3 处在测试文件） |

按规范第 1 节完成闸门，只要存在 A/B/C 任一类即**不得声明阶段 1 注释任务完成**。上一轮「只覆盖 Manager、AppService、Request、SqlMap」的做法漏掉了本报告第 3 节 A-50～A-86（Repository 全层 37 条）与 A-14～A-29（Query 应用服务 16 条），这正是本轮要求「一次性盘干净」的核心原因。

### 8.2 按文件族排序的优先修复顺序

| 优先级 | 文件族 | 缺口 | A | B | C | 修复理由 |
|---|---|---:|---:|---:|---:|---|
| P0 | **RepositoryImpl**（`Repository\MedicalRecognitionReportAggregate\MedicalRecognitionReportRepository.cs`） | 27 | 27 | 0 | 0 | 整文件 0 覆盖；含主键生成（`CreateGuid`）、写入行数判定、状态条件更新等承载并发与唯一约束语义的方法，维护风险最高 |
| P0 | **RepositoryQueryImpl**（`Repository\Queries\MedicalRecognitionReportQueryRepository.cs`） | 8 | 8 | 0 | 0 | 整文件 0 覆盖；承载特殊 SQL / `SqlScope` 读取语义，规范第 3 节要求说明特殊 SQL 的原因与错误后果 |
| P1 | **Query**（`Application\Queries\MedicalRecognitionReportQueryAppService.cs`） | 16(+7 thrown) | 16 | 0 | 0 | 整文件 0 覆盖，且 7 处 `throw` 无 `<exception>`（T-01～T-07）；查询契约对外可见 |
| P1 | **AppServiceInterface**（`IMedicalRecognitionReportAppService.cs`） | 13 | 13 | 0 | 0 | 公共契约文件整文件 0 覆盖，是前端/调用方唯一契约面 |
| P2 | **Entity 类型声明**（18 个实体类各 1 条） | 18 | 18 | 0 | 0 | 聚合根与实体缺少整体职责说明；规范第 2 节要求主声明说明整体业务职责 |
| P2 | **Entity 属性**（B 类） | 47 | 0 | 47 | 0 | 集中在 `Id`/`OperId`/`OperTime`（各 18 处）与 `MicValue`/`LoincCode`；应说明主键来源、操作人取自登录上下文、操作时间为服务端 UTC 时钟、数值单位等不可见约束 |
| P3 | **RepositoryInfra**（`MedicalRecognitionRepositoryModule.cs`） | 2 | 2 | 0 | 0 | 仅类型与构造函数两处，成本极低 |
| P3 | **Const**（`MedicalRecognitionReportConst.cs`） | 2 | 2 | 0 | 0 | 常量语义（`AggregateId`）未说明，成本极低 |
| P4 | **Test**（`SmokeTests.cs` / `Stage1ArchitectureTests.cs` / `Stage1SqlMapProbeTests.cs`） | 16 | 15 | 0 | 1 | 规范同样要求，但按任务要求严重度降一档；含 T-08～T-10 三处 `<exception>` |
| — | **Manager / AppService** | 2 | 0 | 0 | 2 | 均为类型级弱结论（见 5.1），成员级已全部合规；建议不修，或仅补一句类型级说明 |
| — | Request / Dto / Command / Event / Enum / RepositoryInterface | 0 | 0 | 0 | 0 | **合规** |

**建议派单顺序**：先 P0 两个 Repository 文件（35 条），再 P1（29 条），再 P2 实体（65 条），最后 P3/P4（20 条）。P0+P1 合计 64 条即可闭合「整文件 0 覆盖」这一被负责人认定为漏检的症结。

---

## 9. 复核局限

1. **并行写入导致快照时效有限**：复核期间确认至少 5 个文件族被并行补齐（`Enums`、`Dtos`、`Commands`、`Events`、`IMedicalRecognitionReportRepository.cs`）。本报告结论严格对应第 0 节时间戳。**负责人派单前应复算**；若并行代理继续补注释，本报告 A/B/C 清单会持续缩短，但**不会**出现新的缺口类型。
2. **B 类判定含启发式成分**：B 类通过「去后缀骨架比对 + 缩写对照表」判定 `summary` 是否仅复述名称。第 4 节 8 种原文形态（「ID」「操作人」「操作时间」「MIC」「LOINC编码」）均已人工核对原文并确认属复述；但该口径对「短但确有其义」的 summary 可能偏严，落地时若认为某条可接受，应以规范第 2 节第 28 行「不能把可由名称推断当作省略理由」为准逐条裁定。
3. **C 类类型级结论不作为修复依据**：C-01、C-02 源于「类型声明的语法子树包含成员 throw」这一分析特性，本报告已明确标注为弱结论，并已在第 6 节以成员级口径复核（成员级 `<exception>` 覆盖 48/58）。负责人不应据 C-01/C-02 为类型补 `<exception>`。
4. **未评估注释与实现是否语义一致**：本次复核判定「有无」与「节点是否齐备」，并人工抽核了 Request、Command、Manager、AppService、Entity、Enum、Event、Repository 接口的代表性文件确认注释与实现方向一致；但**未逐条**验证每句注释的业务表述与代码行为完全等价（例如具体阈值、状态流转分支的文字描述）。该维度需另有语义审查。
5. **未覆盖范围外代码**：`.xml`（SqlMap）、`obj\`、`bin\`、`*.g.cs`、`Mutual` 相关阶段 2 代码，以及 `server\` 下其他项目（如 IMedicalRecognitionReportAppService 之外的 Contracts 区域）均不在本次判定内。
6. **自动化缺口**：规范第 7 节要求以 StyleCop 或等价 Roslyn Analyzer 落规则并维护 baseline。本次为人工+脚本复核，**未**确认仓库已配置此类 Analyzer，因此上述 151 条缺口在当前 CI 中**不会被机械拦截**，存在再次漏检的结构性风险。建议将 A 类规则（文档节点存在性）纳入 Analyzer 并设 baseline，仅允许存量收敛、禁止新增。

---

## 附：复核可复现性与工作区变更声明

### A.1 工作区变更声明

本次复核**只读生产代码，未修改任何生产代码**（`server\` 全程无写入）。复核过程中生成的临时分析脚本与中间数据已全部删除，`docs\plans\003-阶段1-标准项目目录维护\Server\` 下**仅新增本报告一个文件**。

### A.2 如何独立复现（无需依赖本次的临时脚本）

本报告所有条目均由 Roslyn AST 全量枚举得出，可用等价方法独立复算：

1. 用 `Microsoft.CodeAnalysis.CSharp.CSharpSyntaxTree::ParseText` 解析范围内每个 `.cs`，取 `DescendantNodes()` 中所有成员声明节点（class/interface/enum/record/struct、method、constructor、property、field、event、enum member、delegate）。
2. 判 A 类：节点的 `GetLeadingTrivia()` 中是否存在 `RawKind = 8544`（单行文档注释）或 `8545`（多行文档注释）的 trivia。
3. 判 B/C 类：把文档注释文本重组为 XML 后用 `XmlDocument` 解析，读取 `<summary>`、`<param name>`、`<returns>`、`<exception cref>`；**注意 PowerShell 的 XML 适配器会把 `<param name="x">` 的节点名改写为 `x`，须用 `LocalName` 取真实元素名**。
4. 判 `<exception>`：用 `DescendantNodes()` 单独收集每个成员的 `ThrowStatementSyntax` / `ThrowExpressionSyntax`；`throw Business(...)`、`throw Input(...)` 需解析为工厂方法的返回类型（`MedicalRecognitionReportManager.cs:278`、`:285` → `InvalidOperationException`）再比对 `cref`。

### A.3 复算时需注意的两个失真点（本次已修正）

- **文档注释 trivia 的 `RawKind` 为 8544/8545**（不是 `Kind().ToString() == "SingleLineDocumentationCommentTrivia"`，且 `SyntaxTrivia` 是结构体，`-is` 与 `Kind()` 在 PowerShell 中不可靠）。
- **分隔符不要用 U+001F**：`ConvertTo-Json` 会把它转义为 `\u001f`，`ConvertFrom-Json` 回来会变成逗号，导致 `<param>`/`<exception>` 解析结果静默丢失。

以上两点及「类型语法子树包含成员 throw」的口径陷阱，是本轮复核中先后出现过的四类假阳性/假阴性来源，均已在定稿数据中消除，特此记录以便后续复核不再重复踩坑。
