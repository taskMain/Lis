# 检验检查结果互认平台业务流程图

本文档使用自上而下的标准流程图描述平台核心业务过程。每张流程图均可单独展示；流程之间需要衔接时直接使用完整流程名称，不使用数字编号代称。

本文只表达业务参与方、核心步骤、主要判断和最终结果。理解流程所必需的规则直接写在对应流程图后，不展开字段清单、页面布局、技术接口和全部异常校验。

本文包含以下流程：

- 端到端业务主流程
- 互认项目与项目编码对照流程
- 报告采集与生命周期流程
- 在线互认与病历引用闭环流程

## 端到端业务主流程

本流程展示从互认项目准备、报告采集、在线互认到统计监管的核心路径。节点颜色区分责任方：蓝色表示平台，绿色表示医院及厂商，橙色表示医生，黄色表示判断。

```mermaid
flowchart TD
    P1["平台：从标准项目中选定并维护互认项目"]
    H2["医院：维护当前医院、院区的院内项目编码对照"]
    H3["医院侧采集服务：报告审核后自动增量上传<br/>必要患者及就诊信息 + 结构化报告 + PDF + 影像调阅地址"]
    P5["平台：接收报告并纳入可互认查询"]
    H5["医院 HIS：患者接诊或开单时发起查询<br/>患者身份 + 拟开项目编码"]
    P11["平台：按患者身份和标准项目编码精确匹配"]
    R3{"存在匹配的可互认报告？"}
    P12["平台：返回无可互认结果"]
    H6["医院 HIS：继续原诊疗或开单流程"]
    P13["平台：返回报告明细、PDF及影像调阅地址"]
    H7["医院 HIS：向医生展示候选结果、结构化结果及文件查看入口"]
    D1["医生：在医院 HIS 中查看并作出临床决定"]
    H11["医院 HIS：提交采纳或不采纳结果"]
    P14["平台：记录互认处理结果"]
    R4{"医生是否采纳？"}
    H12["医院 HIS：获取已采纳候选的引用详情"]
    P16["平台：返回病历引用所需内容"]
    H13["医院 HIS：将互认结果写入本次病历"]
    H14["医院 HIS：写入成功后提交引用结果"]
    P17["平台：记录已引用事实"]
    P15["平台：生成审计记录和统计监管报表"]

    P1 --> H2 --> H3 --> P5 --> H5 --> P11 --> R3
    R3 -- "否" --> P12 --> H6
    R3 -- "是" --> P13 --> H7 --> D1 --> H11 --> P14 --> R4
    R4 -- "否" --> P15
    R4 -- "是" --> H12 --> P16 --> H13 --> H14 --> P17 --> P15

    classDef platform fill:#e8f3ff,stroke:#2563eb,color:#172554;
    classDef hospital fill:#ecfdf3,stroke:#16a34a,color:#14532d;
    classDef doctor fill:#fff7ed,stroke:#ea580c,color:#7c2d12;
    classDef decision fill:#fffbea,stroke:#ca8a04,color:#713f12;

    class P1,P5,P11,P12,P13,P14,P15,P16,P17 platform;
    class H2,H3,H5,H6,H7,H11,H12,H13,H14 hospital;
    class D1 doctor;
    class R3,R4 decision;
```

流程规则：

1. 平台先建立互认项目，医院再维护院内项目编码对照；审核完成的新报告通过医院侧采集服务自动增量提交。
2. 在线互认由医院 HIS 在患者接诊或开单过程中发起，平台提供候选，医生负责作出采纳或不采纳决定。
3. 候选查看、PDF 或影像调阅不自动表示采纳；病历写入成功后，医院 HIS 才提交引用结果。
4. 项目准备、报告处理和在线使用分别在“互认项目与项目编码对照流程”“报告采集与生命周期流程”“在线互认与病历引用闭环流程”中展开。

## 互认项目与项目编码对照流程

本流程展示平台建立互认项目，以及医院将本院项目映射到互认项目的核心过程。

```mermaid
flowchart TD
    S([开始]) --> A["平台：获取当前有效的标准项目目录"]
    A --> B{"目录是否获取成功？"}
    B -- "否" --> C["平台：保留既有状态并提示刷新失败"]
    C --> E1([本次刷新结束])
    B -- "是" --> D["平台管理员：选择互认项目<br/>维护可互认时间、参考价格和启停状态"]
    D --> E["平台：保存可供医院对码的互认项目"]
    E --> F["医院管理员：在当前医院、院区<br/>填写院内项目并选择互认项目"]
    F --> G{"编码对照是否可以保存？"}
    G -- "否" --> H["平台：提示冲突或项目不可用"]
    H --> F
    G -- "是" --> I["平台：保存并启用项目编码对照"]
    I --> J["平台管理员或医院管理员：<br/>按权限维护项目及对照状态"]
    J --> END([结束])
```

流程规则：

1. 互认项目从当前有效的最细层标准项目中选择，并维护可互认时间、当前参考价格、启停状态和来源状态。
2. 标准目录获取失败时保留既有状态，不使用空目录或不完整结果覆盖现状。
3. 医院管理员只能维护当前医院、院区范围内的编码对照；同一医院、院区和院内项目编码同时最多存在一条启用对照。
4. 互认项目停用或来源不可用时，停止相关新增对照、新报告上传和候选查询，但不删除历史数据。
5. 编码对照修改或停用只影响后续上传报告，不重写已经接收的报告。

## 报告采集与生命周期流程

本流程展示审核完成的检查、检验报告如何新增、更正或作废，以及失败后如何修正重试。

```mermaid
flowchart TD
    S([开始]) --> A["医院源系统：报告审核、修订后再审核或作废"]
    A --> B["医院侧采集服务：确定新增、更正或作废操作"]
    B --> C{"操作类型是什么？"}
    C -- "新增" --> D["提交完整报告及 PDF"]
    C -- "更正" --> E["提交更正后的完整报告及 PDF"]
    C -- "作废" --> F["提交报告标识及作废信息"]
    D --> G["平台：校验并处理报告"]
    E --> G
    F --> G
    G --> H{"处理是否成功？"}
    H -- "否" --> I["医院侧采集服务：根据失败原因修正<br/>或在结果未知时重试"]
    I --> B
    H -- "是" --> J["平台：保存当前报告状态及必要版本关系"]
    J --> END([结束])
```

流程规则：

1. 检验报告和检查报告分别提交，但共用新增、更正、作废和版本生命周期；每次请求只处理一份报告变更。
2. 新增和更正提交完整结构化报告及 PDF，并共同成功或共同失败；作废只提交报告业务标识及作废信息。
3. 报告至少包含一个能够关联当前有效互认项目的明细时才进入平台，未对码明细随完整报告保存但不形成候选。
4. 相同操作和内容重复提交按幂等成功处理；相同报告标识但内容不一致时不得静默覆盖。
5. 更正形成新版本，原版本停止参与后续候选和新的引用；作废不物理删除报告，但报告不再参与后续候选和新的引用。
6. 调用失败、超时或未取得明确结果时，医院侧采集服务通过同一报告流程修正或重试。

## 在线互认与病历引用闭环流程

本流程展示医院 HIS 查询候选、医生作出决定，以及采纳后将结果写入病历并反馈引用的连续过程。

```mermaid
flowchart TD
    S([开始]) --> A["医院 HIS：提交患者身份、本次来源就诊<br/>和一个或多个拟开院内项目"]
    A --> B["平台：校验查询并匹配当前有效候选"]
    B --> C{"是否存在符合条件的候选？"}
    C -- "否" --> D["平台：返回成功的空候选集合"]
    D --> E["医院 HIS：继续原诊疗或开单流程"]
    E --> END1([结束])
    C -- "是" --> F["平台：返回候选内容、报告入口和候选引用"]
    F --> G["医院 HIS：向医生展示候选"]
    G --> H{"医生是否采纳？"}
    H -- "否" --> I["医院 HIS：提交不采纳决定及原因"]
    I --> J["平台：记录不采纳决定"]
    J --> END2([本次处理结束])
    H -- "是" --> K["医院 HIS：提交采纳决定"]
    K --> L["平台：记录采纳决定及预计节省金额"]
    L --> M["医院 HIS：获取引用详情并写入本次病历"]
    M --> N{"病历是否写入成功？"}
    N -- "否" --> O["医院 HIS：不提交引用结果"]
    O --> END2
    N -- "是" --> P["医院 HIS：提交实际引用结果"]
    P --> Q["平台：记录引用事实"]
    Q --> END2
```

流程规则：

1. 平台按当前医院、院区解析拟开院内项目，只返回符合条件的候选；没有候选时返回成功的空集合，不逐项返回无候选或未对码状态。
2. 多个拟开院内项目映射到同一互认项目时合并处理，每个互认项目最多返回最近一份当前有效报告；联合报告按完整报告展示。
3. 候选必须满足报告版本有效、互认项目可用和可互认时间要求。本院报告是否参与由平台全局配置控制。
4. 候选返回、结构化内容展示、PDF 或影像访问均不自动形成采纳、不采纳或引用事实。
5. 医生针对候选中的具体互认项目作出决定。不采纳必须提供平台统一原因；采纳时形成对应预计节省金额。
6. 医院 HIS 获取引用详情并成功写入本次病历后才提交引用结果；写入失败时不得提交引用成功。
7. 引用结果形成前可以更正同一次互认决定；引用结果形成后，该次决定固定为采纳，不得再改为不采纳。
