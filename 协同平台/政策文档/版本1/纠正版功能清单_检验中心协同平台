# 检验中心协同平台 — 纠正版功能清单与平台能力边界

> 版本：讨论修订版 v0.1 | 日期：2026-05-13 | 状态：讨论中
>
> **定位**：本文档基于《P0功能清单_检验中心协同平台.md》逐项讨论后形成，用于沉淀已确认的平台功能边界、核心流程、异常处理和对接注意事项。本文只描述 **平台功能** 或 **平台应提供的能力**，不把 HIS、财务、医保、医生工作站原生界面等外部系统能力纳入平台功能范围。

---

## 1. 修订原则

修订原则已单独沉淀至 `纠正版功能清单_修订原则说明.md`。

本文后续所有功能修订均以该说明文档为准，避免在功能清单正文中重复维护全局规则。

---

## 2. 功能讨论进度

功能讨论进度以 `讨论todo` 为准。只有状态为 `已完成` 的功能，才会在本文形成正式修订内容。

---

## 3. 已完成讨论的功能

### F-001 跨机构电子检验申请

| 属性 | 内容 |
|------|------|
| 功能编号 | F-001 |
| 功能名称 | 跨机构电子检验申请 |
| 所属模块 | 业务流程管理 |
| 业务价值 | 区域检验协同入口。平台接收 HIS 已开立、已确认可送检的检验申请，生成平台申请、采样计划和样本条码，为后续采样、物流、核收、检测、报告回传提供统一业务主线。 |
| 核心用户 | HIS 系统、采样端、平台运维/配置人员 |
| 平台入口/接口 | 创建平台申请、撤销平台申请、按 HIS 申请号查询、按平台申请单号查询申请详情 |
| 当前状态 | 已完成讨论 |

#### 功能定位

F-001 不是医生开医嘱功能，也不是收费功能。医生在 HIS 中完成检验项目开立后，HIS 调用平台创建区域检验申请。平台负责接收申请数据、校验平台侧必要信息、创建平台申请单、生成采样计划、生成样本条码，并形成待采样任务。

一条 HIS 申请对应一条平台申请；一条平台申请可以包含多个检验项目。平台不合并多条 HIS 申请，只在样本层面根据采样规则拆管或合管。

#### 平台负责

- 维护以检验中心为主的区域标准项目库。
- 接收 HIS 已开立、已确认可送检的检验申请。
- 校验必填字段、平台标准项目编码、临床诊断、来源类型、组织人员编码等平台侧必要信息。
- 创建或关联患者档案、就诊记录、平台申请单。
- 根据平台采样规则生成采样计划。
- 按“一支物理样本容器一个条码”生成样本条码。
- 返回平台申请单号、样本条码列表、采样计划等创建结果。
- 创建平台内部待采样任务。
- 支持未采样前撤销申请，并作废全部样本条码。
- 提供按 HIS 申请号查询平台申请、按平台申请单号查询申请详情的能力。
- 记录完整接口审计日志。

#### 平台不负责

- 不提供 HIS 医生工作站界面。
- 不通过 iframe、弹窗、嵌入式 Web 页面接入 HIS。
- 不负责医嘱开立、收费、医保结算、退费。
- 不接收或保存项目价格、收费状态、医保结算状态、退费状态。
- 不维护 HIS 本地项目到平台标准项目的映射。
- 不根据 HIS 本地项目编码自行查找映射关系。
- 不校验机构可申请项目范围。
- 不校验项目与患者性别、年龄、孕产、儿童、特殊人群等临床适用性。
- 不做重复申请提醒。
- 不校验患者历史未完成申请。
- 不做创建接口幂等。
- 不主动通知采样护士、物流人员或检验中心。
- 不返回预计报告时间。
- 不返回报告回传配置。
- 不提供按样本条码反查申请的能力，该能力归入 F-005。

#### 触发条件

HIS 中医生完成一次检验开立动作后，HIS 调用平台创建申请接口。HIS 调用平台即表示该申请已由 HIS 确认可进入区域检验流程。

#### 前置条件

- HIS 已完成本地检验开立。
- HIS 已维护本地项目到平台标准项目的映射。
- HIS 调用平台时传入平台标准项目编码。
- 平台中对应标准项目存在、启用，并具备完整采样规则。
- HIS 与平台已完成接口认证和机构接入配置。

#### 后置结果

创建成功后：

- 平台生成平台申请单号。
- 平台申请状态为“待采样”。
- 平台创建或关联 Patient、Visit/Encounter、Order、Specimen 等对象。
- 平台生成采样计划。
- 平台为每支物理样本生成样本条码。
- 平台生成申请级待采样任务和样本级待采样任务。
- 平台向 HIS 返回平台申请单号、样本条码列表和采样计划。

创建失败时：

- 平台不生成平台申请单号。
- 平台不生成样本条码。
- 平台不生成待采样任务。
- 平台返回结构化错误信息。

#### 核心字段/数据对象

平台需要区分以下对象：

| 对象 | 含义 | 关键说明 |
|------|------|----------|
| Patient | 患者档案 | 表示“这个人”，可被 EMPI 后续合并 |
| Visit/Encounter | 就诊记录 | 表示某人在某机构的一次就诊 |
| Order | 平台检验申请 | 表示某次就诊下的一张检验申请 |
| Specimen | 样本 | 表示一支物理样本容器 |
| Report | 报告 | 报告必须关联具体申请，不能只挂在患者名下 |

最低必填字段：

- 患者姓名。
- 性别。
- 年龄或出生日期。
- HIS 患者 ID。
- HIS 就诊号或本次就诊流水号。
- HIS 申请号。
- 申请来源类型。
- 申请机构编码。
- 申请科室编码。
- 申请医生编码。
- 临床诊断。
- 平台标准项目编码。

可选字段：

- HIS 医嘱号。
- HIS 本地项目编码。
- HIS 原始项目名称。
- 项目级加急标识。
- 项目级备注。
- 申请级加急标识。
- 医生备注/申请备注。
- 检验目的。
- 症状描述、病情摘要。
- 患者手机号/联系电话。
- 患者证件类型和证件号码。
- 患者地址。
- 申请医生姓名、手机号、职称。
- HIS 本地机构 ID、科室 ID、医生 ID。

#### 申请来源类型

申请来源类型必填。

| 编码 | 名称 | 说明 |
|------|------|------|
| OP | 门诊 | 门诊就诊产生的检验申请 |
| IP | 住院 | 住院就诊产生的检验申请 |
| ER | 急诊 | 急诊就诊产生的检验申请 |
| PE | 体检 | 体检业务产生的检验申请 |
| OTHER | 其他 | 无法归入上述类型的申请来源 |

当来源类型为 `OTHER` 时，HIS 必须传入来源备注。住院病区、床号、当前科室等字段暂不在 F-001 中定稿，已登记为内部待讨论事项。

#### 主流程

1. HIS 完成本地检验开立。
2. HIS 调用平台创建申请接口。
3. 平台校验患者、就诊、申请、项目、来源、组织人员、临床诊断等必要字段。
4. 平台根据患者信息查找患者档案。
5. 明确匹配时关联已有患者；未找到或疑似重复但无法确认时，创建新患者档案，后续由 EMPI 处理。
6. 平台创建或关联本次就诊记录。
7. 平台创建平台申请单。
8. 平台根据标准项目和采样规则生成采样计划。
9. 平台按物理样本容器生成样本条码。
10. 平台生成申请级和样本级待采样任务。
11. 平台返回平台申请单号、样本条码列表、采样计划和申请状态。

#### 样本条码与采样计划

- 样本条码由平台统一生成。
- 条码粒度是一支物理样本容器一个条码。
- 平台不生成申请单级条码。
- 平台返回平台申请单号，但不返回申请单条码。
- 一个样本条码可以关联多个检验项目。
- 平台创建申请成功后立即生成全部样本条码和采样计划。
- 平台只返回条码和采样计划数据，HIS 或采样端自行打印。
- 待采样任务 ID 不作为 F-001 对外返回字段。

#### 异常处理

- 任一平台标准项目编码不存在、停用或缺失时，整单拒绝。
- 同一申请中存在重复平台标准项目编码时，整单拒绝。
- 采样规则缺失时，整单拒绝，并返回缺失字段、影响项目和是否可重试等错误详情。
- 患者最低身份字段缺失时，整单拒绝。
- 临床诊断缺失时，整单拒绝。
- 申请来源类型缺失或无效时，整单拒绝。
- 申请机构编码、申请科室编码、申请医生编码缺失或无效时，整单拒绝。
- 结构化错误信息应包含错误分类、错误原因、错误字段或项目、是否可修正后重试等内容。

具体错误码、HTTP 状态码、错误响应 JSON 结构已暂存至 `系统实现相关.md`，后续统一设计。

#### 申请撤销

- 平台仅允许未采样前撤销申请。
- HIS 调用撤销接口时必须传入平台申请单号。
- 平台不支持仅通过 HIS 申请号撤销申请。
- 撤销原因必填。
- 撤销成功后，平台申请状态变更为“已撤销”。
- 撤销成功后，申请级待采样任务和全部样本级待采样任务关闭。
- 撤销成功后，全部样本条码作废。
- 作废条码不允许用于采样确认、物流出库、样本核收或检测。
- 不单独提供撤销结果查询；撤销状态通过申请详情查询查看。

#### 查询能力

- 支持按平台申请单号查询申请详情。
- 支持按申请机构编码 + HIS 申请号查询平台申请。
- 如果按 HIS 申请号匹配到多条平台申请，返回全部匹配记录。
- 申请详情查询返回申请状态、患者信息、就诊信息、申请机构/科室/医生、项目明细、样本条码列表、采样计划、备注等。
- 申请详情查询不承担报告查询职责，报告查询归入 F-008。

#### 通知与审计要求

- F-001 创建成功后不主动通知采样护士、物流人员、检验中心。
- 待采样列表和采样提醒归入 F-002。
- 状态推送和消息通知归入 F-005/F-020。
- 平台记录完整接口审计日志，成功和失败请求都记录。
- 日志至少包含请求来源系统、请求机构、请求时间、接口名称、HIS 申请号、HIS 医嘱号、平台申请单号、患者标识摘要、项目摘要、校验结果、返回结果、失败原因、调用账号、请求 IP、Trace ID/Request ID。
- 患者敏感信息在日志中应脱敏或仅记录摘要。

#### 平台 vs 外部系统边界

| 事项 | 平台 | HIS/外部系统 |
|------|------|--------------|
| 医生开立检验 | 不负责 | HIS 负责 |
| 收费/医保/退费 | 不负责，不接收费用字段 | HIS/院内财务体系负责 |
| 本地项目映射 | 不维护 HIS 本地映射 | HIS 维护本地项目到平台标准项目映射 |
| 平台标准项目 | 维护中心标准项目库 | HIS 调用时传平台标准项目编码 |
| 条码生成 | 平台生成样本条码 | HIS/采样端负责打印 |
| 待采样任务 | 平台生成内部任务 | 采样端在 F-002 中查询和处理 |
| 报告回传 | F-001 不处理 | F-008 中讨论 |
| 预计报告时间 | F-001 不返回 | F-005 中动态讨论 |
| 样本条码反查 | F-001 不提供 | F-005 中讨论 |

#### 遗留问题与内部待确认

以下事项已登记在 `风险_待内部讨论确定.md`，不作为 F-001 无条件定稿结论：

- 平台 P0 不做创建接口幂等。
- 平台 P0 不做重复申请提醒。
- 平台只返回条码和采样计划数据，HIS 或采样端自行打印。
- 申请机构/科室/医生平台编码如何提供给 HIS、如何与 HIS 本地 ID 映射。
- 平台不校验项目与患者性别、年龄等基本信息冲突。
- 住院病区、床号、科室信息如何处理。

以下事项已登记在 `系统实现相关.md`，待需求边界确定后讨论：

- F-001 创建申请接口性能要求。
- F-001 错误码与错误响应结构。

#### 本轮确认结论

F-001 的平台职责收敛为“接收 HIS 已开立申请并生成平台申请、采样计划、样本条码和待采样任务”。平台不参与 HIS 开立、收费、项目本地映射、打印控制、报告回传、预计时间计算和临床合理性拦截。后续 F-002、F-005、F-008、F-016、F-022 将分别承接采样、状态追踪、报告、患者主索引、采样规则等能力。

---

### F-002 样本标签打印与采集管理

| 属性 | 内容 |
|------|------|
| 功能编号 | F-002 |
| 功能名称 | 样本标签打印与采集管理 |
| 所属模块 | 业务流程管理 |
| 业务价值 | 承接平台已生成的申请、采样计划和样本条码，为采样人员提供待采样任务查询、采前核验、标签打印/重打、采集确认和采集更正能力，并将已采样本移交后续物流流程。 |
| 核心用户 | 采样人员、采样端、HIS/外部采样系统、平台运维/配置人员 |
| 平台入口/接口 | 待采样任务查询、样本条码核验、标签打印数据获取、平台侧标签打印、重打换码、采集确认、采集更正 |
| 当前状态 | 已完成讨论 |

#### 功能定位

F-002 承接 F-001 创建平台申请后生成的采样计划、样本条码和待采样任务。平台负责让采样人员能够查看待采样任务、完成采前核验、获取或打印样本标签、按样本条码确认采集，并把已采样本纳入后续物流待出库范围。

平台按“混合兼容型”建设：平台自身提供待采样列表、扫码核验、标签打印/重打、采集确认、更正和审计能力；同时提供接口能力给 HIS 或外部采样端集成。具体部署时以平台页面为主，还是以 HIS/外部采样端为主，暂放入内部待讨论确定。

#### 平台负责

- 查询待采样申请和样本任务。
- 展示采样计划、样本条码、容器要求、项目明细和患者摘要。
- 支持按样本条码扫码核验。
- 提供标签打印数据，并支持平台侧直接打印。
- 支持未采集样本的标签重打换码，旧条码作废。
- 按样本条码确认采集。
- 支持申请单级批量确认多个样本条码。
- 支持部分采集，并要求登记未采、暂缓、采集失败原因。
- 支持物流出库前的采集记录更正。
- 将已采样本纳入 F-003 样本物流追踪的待出库范围。
- 记录完整操作审计和接口审计。

#### 平台不负责

- 不负责医生开单、收费、医保结算、退费。
- 不处理 HIS 未提交到平台的申请。
- 不控制医院本地打印机驱动、纸张耗材、打印机维护等现场设备问题。
- 不把患者腕带、身份证、人脸识别等强身份核验纳入 P0。
- 不主动通知物流人员，通知策略放入 F-020 消息通知中心。
- 不在 F-002 中定义采集状态对外推送规则，状态推送归入 F-005 样本状态全程追踪。
- 不处理物流出库、转运、签收，相关能力归入 F-003。
- 不处理报告查询、报告回传，相关能力归入 F-008。

#### 触发条件

F-001 创建平台申请成功后，平台已生成采样计划、样本条码、申请级待采样任务和样本级待采样任务，采样人员或外部采样端开始处理待采样任务。

#### 前置条件

- 平台申请已创建成功，且未撤销。
- 平台申请已生成采样计划。
- 每支物理样本容器已生成平台样本条码。
- 样本处于待采样、未采、暂缓采集或采集失败等采样侧可处理状态。
- 采样人员、采样机构/科室或外部采样端具备对应操作权限。

#### 后置结果

采集成功后：

- 平台生成或更新采集记录。
- 样本状态变更为“已采集/待出库”。
- 已采样本进入 F-003 的物流待出库范围。
- 平台保留采集确认审计记录。

未采、暂缓或采集失败时：

- 样本不进入物流待出库范围。
- 平台记录未采、暂缓或失败原因。
- 样本保留在采样侧待后续处理。

当申请下所有样本都完成已采或登记未采、暂缓、采集失败原因后：

- 申请采样环节状态变更为“采样已处理”。

#### 核心字段/数据对象

| 对象 | 含义 | 关键说明 |
|------|------|----------|
| SamplingTask | 采样任务 | 分申请级任务和样本级任务 |
| Specimen | 样本 | 一支物理容器对应一个样本条码 |
| SpecimenBarcode | 样本条码 | 需支持有效、作废、替换关系 |
| SamplingPlan | 采样计划 | 来自 F-001，根据项目和采样规则生成 |
| CollectionRecord | 采集记录 | 记录采集确认、更正、失败、暂缓等动作 |
| PrintRecord | 打印记录 | 记录打印、重打、换码、失败、操作来源 |
| AuditLog | 审计日志 | 记录关键操作和接口调用 |

采集确认最低必填字段：

- 样本条码。
- 采集时间。
- 采集人。
- 采集机构/科室或采集点。
- 采集状态：已采、未采、暂缓、采集失败。
- 未采、暂缓、失败原因。
- 备注。
- 操作终端或来源系统。
- 操作时间。

可选字段：

- 采样部位。
- 采样方式。
- 采样量。
- 容器状态。
- 患者配合情况。
- 现场补充说明。

#### 主流程

1. F-001 创建平台申请成功后，平台生成申请级待采样任务和样本级待采样任务。
2. 采样人员或外部采样端查询待采样列表，可按申请机构、科室、申请时间、采样状态、加急标识、患者信息、样本条码等条件筛选。
3. 采样人员选择任务或扫描样本条码。
4. 平台展示患者摘要、申请摘要、检验项目、样本类型、容器要求、采集注意事项等信息。
5. 采样人员核对无误后，打印标签或确认已取得有效标签。
6. 如未采集前发现标签污染、丢失、打印错误等问题，可发起重打换码；平台生成新样本条码，旧条码作废，并记录原因和审计。
7. 采样人员按样本条码确认采集；申请单级确认仅作为批量确认多个样本条码的便捷操作。
8. 对已采样本，平台记录采集信息并将样本状态更新为“已采集/待出库”。
9. 对未采、暂缓、采集失败样本，平台保留在采样侧，并要求登记原因。
10. 当申请下所有样本都完成已采或登记未采、暂缓、失败原因后，申请采样环节状态变为“采样已处理”。
11. 已采样本进入 F-003 样本物流追踪的待出库范围；未采、暂缓、失败样本不进入物流待出库范围。

#### 样本采集状态

- 样本初始状态：待采样。
- 已成功采集：已采集/待出库。
- 未能采集：未采。
- 暂时不能采：暂缓采集。
- 采集失败：采集失败。
- 重打换码后的旧条码：已作废。
- 已采样本进入物流后：后续由 F-003 接管。
- 已核收样本进入检验中心后：后续由 F-004/F-005 接管。

#### 标签打印与重打换码

- 平台提供标签打印数据。
- 平台支持平台侧直接打印。
- HIS 或外部采样端可通过接口获取打印数据并自行控制打印。
- 标签重打必须登记原因、操作人、操作时间和操作来源。
- 标签重打不是同一条码重复输出，而是生成新的样本条码。
- 重打换码后，旧条码立即作废。
- 旧条码不得用于采集确认、物流出库、样本核收或检测。
- 重打换码仅允许在样本未采集状态下执行。
- 已采集样本不能直接重打换码，必须走重采、补采或后续异常处理流程。

#### 采前核验

采样人员扫描样本条码后，平台应展示患者和申请摘要，供采样人员核对后再确认采集。

P0 需要展示的信息包括：

- 患者姓名。
- 性别。
- 年龄或出生日期。
- 申请机构。
- 申请科室。
- 检验项目。
- 样本类型。
- 容器要求。
- 采集注意事项。

患者腕带、身份证、人脸识别等强身份核验不纳入 P0，可由院内系统负责或后续扩展。

#### 采集更正

- 物流出库前允许更正采集记录。
- 更正必须登记原因。
- 平台保留原始采集记录和更正后记录。
- 更正后应立即同步更新样本状态和物流待出库范围。
- 样本一旦物流出库或被检验中心核收，采样侧不能直接更正状态，只能走后续异常处理或质量处理流程。

#### 异常处理

- 标签污染、丢失、打印不清：允许在未采集状态下重打换码，平台生成新条码，旧条码作废。
- 旧条码被扫描：平台提示条码已作废，并返回新条码或关联样本信息；旧条码不能继续采集、出库、核收或检测。
- 已采集后发现标签错误：不能直接重打换码，应走重采、补采或后续异常处理流程。
- 扫描不存在条码：提示条码不存在或不属于当前机构可见范围。
- 扫描已撤销申请的条码：提示申请已撤销，禁止采集。
- 重复采集确认：如样本已确认采集，应提示当前状态，不重复生成采集记录；需要修改时走更正流程。
- 部分采集：允许保存已采样本，未采、暂缓、失败样本必须登记原因。
- 误确认更正：物流出库前可更正采集记录，必须登记原因并保留原记录；出库或核收后不允许采样侧更正。
- 采样计划缺失或采样规则异常：不应出现在 F-002 正常待采样列表中，此类问题应在 F-001 创建申请时拦截。
- 打印失败：平台记录打印请求和结果；打印机、纸张、驱动等现场设备问题作为外部设备问题处理，平台只保留可追溯记录。

#### 性能与时效要求

- 待采样列表查询应支持按机构、科室、时间、状态、加急标识、患者关键字、样本条码筛选。
- 样本条码扫码后，应快速返回患者摘要、申请摘要、项目和容器信息，避免影响现场采样效率。
- 采集确认应实时更新样本状态。
- 已采样本应立即进入物流待出库范围。
- 重打换码应实时作废旧条码并生成新条码，避免新旧条码并存可用。
- 更正采集记录后，应立即同步更新样本状态和物流待出库范围。
- 具体接口性能指标、并发规模、超时时间暂存至 `系统实现相关.md`，后续统一讨论。

#### 通知与审计要求

- 平台记录采样任务查询、标签打印、重打换码、采集确认、采集更正、旧条码扫描拦截等关键操作。
- 审计至少包含操作人、操作机构、操作时间、操作终端/来源系统、样本条码、平台申请单号、变更前状态、变更后状态、原因、备注、Trace ID/Request ID。
- 采集成功后是否通知物流人员，不在 F-002 内直接定义，放入 F-020 消息通知中心。
- 采集状态变化是否推送 HIS/LIS，不在 F-002 内直接定义，放入 F-005 样本状态全程追踪。
- 患者敏感信息在日志中应脱敏或仅记录摘要。

#### 与 HIS/LIS/物流/患者端的边界

| 事项 | 平台 | 外部系统 |
|------|------|----------|
| 待采样任务 | 平台生成并提供查询 | HIS/采样端可调用接口展示 |
| 样本条码 | 平台生成、作废、换码 | 外部系统不得自行生成平台样本条码 |
| 标签打印 | 平台提供打印数据并可支持平台侧打印 | HIS/采样端可负责实际打印控制 |
| 打印设备 | 记录打印请求和结果 | 本地打印机、驱动、纸张、耗材由现场负责 |
| 采前核验 | 平台展示患者和申请摘要 | 强身份核验可由院内系统负责 |
| 采集确认 | 平台按样本条码记录确认 | HIS/采样端可通过接口提交 |
| 物流交接 | 已采样本进入待出库范围 | 具体出库、转运、签收归 F-003 |
| 状态推送 | F-002 更新内部状态 | 对外推送归 F-005/F-020 |
| 报告相关 | 不处理 | 归 F-008 |

#### 遗留问题与内部待确认

以下事项已登记在 `风险_待内部讨论确定.md`，不作为 F-002 无条件定稿结论：

- 部署时以平台采样页面为主，还是以 HIS/外部采样端为主。
- 不同机构是否允许不同打印模式。
- 平台侧直接打印的实现方式、打印控件、浏览器兼容性、打印模板管理。
- 标签纸规格、条码格式、打印 DPI、打印机适配范围。

以下事项已登记在 `系统实现相关.md`，待需求边界确定后讨论：

- F-002 相关接口性能指标、并发规模、超时时间。
- F-002 标签打印、重打换码、采集确认、采集更正接口形态。

#### 本轮确认结论

F-002 的平台职责收敛为“围绕平台样本条码完成标签打印与采集闭环”。平台支持混合兼容模式：既提供平台侧采样操作能力，也提供接口给 HIS 或外部采样端集成。采集确认以样本条码为标准粒度，申请单级确认仅作为批量操作。允许部分采集，未采、暂缓、失败必须登记原因。标签重打采用换码机制，仅限未采集样本，旧条码作废且不得继续使用。已采样本实时进入物流待出库范围，物流出库前可更正采集记录并保留审计；出库或核收后采样侧不再直接改状态。

---

### F-003 样本物流追踪

| 属性 | 内容 |
|------|------|
| 功能编号 | F-003 |
| 功能名称 | 样本物流追踪 |
| 所属模块 | 业务流程管理 |
| 业务价值 | 承接已采集/待出库样本，通过转运箱建箱、装箱、出库、在途记录、签收、异常登记和超时预警，形成从采样点到检验中心的物流追踪闭环。 |
| 核心用户 | 发出方采样点/机构、物流人员或承运方、接收方检验中心、平台运维/配置人员 |
| 平台入口/接口 | 创建转运箱、样本装箱、出库确认、在途节点登记、签收确认、异常登记、运输撤回、超时预警查询 |
| 当前状态 | 已完成讨论 |

#### 功能定位

F-003 承接 F-002 中状态为“已采集/待出库”的样本。平台负责让发出方把样本装入转运箱、确认出库，让接收方按转运箱签收，并在平台中形成从出库到签收的物流追踪记录。

平台采用“转运箱主导型”：物流操作以转运箱为主，箱内绑定样本条码清单；样本状态仍按样本条码同步更新。P0 必须记录出库和签收两个关键节点，中间物流节点、温度、GPS、冷链设备数据作为可选扩展。

#### 平台负责

- 创建转运箱。
- 将已采集/待出库样本扫码装箱。
- 校验样本是否可装箱。
- 记录转运箱与箱内样本清单。
- 出库确认，记录发出方、交接对象、运输条件和箱内明细。
- 按转运箱码进行接收方签收。
- 支持签收时核对箱内样本数量。
- 支持签收时可选逐支样本扫码核对。
- 登记物流异常。
- 按机构/线路配置运输时限触发超时预警。
- 支持未签收前的运输撤回/异常撤回。
- 记录完整物流审计。

#### 平台不负责

- 不负责实际运输调度、车辆调度、路线规划。
- 不接入或强制依赖 GPS、温度探头、冷链设备数据。
- 不在 F-003 中判定样本是否合格。
- 不在 F-003 中决定是否拒收、补采、重采。
- 不直接定义超时通知对象和通知渠道，放入 F-020 消息通知中心。
- 不处理检验中心核收和质量初筛，放入 F-004 样本核收与质量初筛。

#### 触发条件

F-002 中样本完成采集确认后，样本状态进入“已采集/待出库”，发出方开始创建转运箱并装箱出库。

#### 前置条件

- 样本已完成采集确认。
- 样本状态为“已采集/待出库”。
- 样本未绑定其他未完成物流任务。
- 发出机构/采样点、接收机构或运输线路已具备基础配置。
- 操作人或外部系统具备建箱、装箱、出库或签收权限。

#### 后置结果

出库确认后：

- 转运箱状态变更为“已出库/转运中”。
- 箱内样本状态同步变更为“已出库/转运中”。
- 平台启动或记录运输时限计算。
- 平台保留出库交接审计记录。

签收无异常后：

- 转运箱状态变更为“已签收”。
- 箱内无异常样本状态变更为“已签收/待核收”。
- 样本进入 F-004 样本核收与质量初筛范围。

签收或在途发现异常时：

- 平台登记物流异常。
- 受影响样本进入“物流异常待处理”。
- 是否拒收、补采、重采或质量判定，交由 F-004 或后续质量流程处理。

#### 核心字段/数据对象

| 对象 | 含义 | 关键说明 |
|------|------|----------|
| TransportBox | 转运箱 | 物流操作主对象 |
| TransportBoxCode | 转运箱码 | 用于出库、在途、签收扫描 |
| TransportTask | 物流任务 | 记录一次出库到签收的运输过程 |
| TransportBoxItem | 箱内样本明细 | 绑定样本条码 |
| TransportNodeRecord | 物流节点记录 | 记录出库、签收和可选中间节点 |
| TransportException | 物流异常记录 | 记录短少、破损、错送、超时等异常 |
| TransportTimeoutRule | 运输时限规则 | 按发出机构、接收机构或线路配置 |
| AuditLog | 审计日志 | 记录关键操作和接口调用 |

出库确认必填字段：

- 转运箱码。
- 发出机构/采样点。
- 目标接收机构。
- 出库时间。
- 出库操作人。
- 交接对象：物流人员或承运方。
- 物流人员/承运方标识。
- 箱内样本数量。
- 箱内样本清单。
- 运输温度要求或运输条件。
- 备注。

签收确认必填字段：

- 转运箱码。
- 接收机构。
- 签收时间。
- 签收操作人。
- 实收箱数或箱体确认结果。
- 实收样本数量。
- 是否存在异常。
- 异常类型和异常说明。
- 备注。

#### 主流程

1. F-002 中样本完成采集确认后，样本状态进入“已采集/待出库”。
2. 发出方在平台创建转运箱，生成或录入转运箱码。
3. 发出方扫描样本条码装箱，平台校验样本是否处于“已采集/待出库”，且未绑定其他未完成物流任务。
4. 装箱完成后，平台记录转运箱、箱内样本清单、样本数量、发出机构/采样点、目标接收机构、运输条件等信息。
5. 发出方进行出库确认，记录出库时间、出库操作人、交接对象、物流人员/承运方标识、箱内样本数量和备注。
6. 出库确认后，转运箱状态变为“已出库/转运中”，箱内样本状态同步变为“已出库/转运中”。
7. 平台按机构/线路配置的运输时限计算预计送达或超时时间。
8. 如有中间物流节点，可记录取件、中转、异常停留、温度异常等节点；P0 不强制。
9. 接收方收到转运箱后扫描转运箱码，平台展示箱内样本清单、样本数量、出库信息和运输条件。
10. 接收方确认实收数量，可选择逐一扫描样本条码核对。
11. 签收无异常时，转运箱状态变为“已签收”，箱内样本状态变为“已签收/待核收”。
12. 签收发现异常时，平台登记物流异常；无异常样本进入“已签收/待核收”，异常样本进入“物流异常待处理”。
13. “已签收/待核收”样本进入 F-004 样本核收与质量初筛范围。

#### 转运箱与样本物流状态

转运箱状态：

- 待装箱。
- 已装箱/待出库。
- 已出库/转运中。
- 转运超时，作为超时标记，不中断转运中主流程。
- 已签收。
- 已撤回。
- 异常结束。

样本物流状态：

- 已采集/待出库。
- 已装箱/待出库。
- 已出库/转运中。
- 转运超时，作为超时标记，不直接判定不合格。
- 已签收/待核收。
- 物流异常待处理。

关键规则：

- 只有“已采集/待出库”样本可以装箱。
- 一个样本同一时间只能属于一个未完成物流任务的转运箱。
- 转运箱出库后，箱内样本状态同步进入转运中。
- P0 必须记录出库和签收两个关键节点。
- 中间节点、温度、GPS、冷链设备数据作为可选扩展。
- 签收以转运箱为主，逐支样本扫码核对为可选。
- 异常样本不直接进入待核收。

#### 超时预警

- P0 按机构/线路配置运输时限。
- 出库确认后，平台根据发出机构、接收机构或运输线路计算预计送达或超时时间。
- 超过配置时限仍未签收时，平台生成超时预警。
- 转运箱可标记为“转运超时”，但仍保留在“转运中”主流程内，等待后续签收。
- 箱内样本同步带有超时标记，但不直接判定不合格。
- 超时通知对象和渠道放入 F-020 消息通知中心。
- 超时是否影响样本质量、是否拒收，放入 F-004 样本核收与质量初筛。

#### 运输撤回

- 未签收前允许发出方发起运输撤回或异常撤回。
- 撤回必须登记原因、操作人、时间和责任方。
- 撤回后转运箱状态变为“已撤回”或“异常结束”。
- 箱内样本回退到“已采集/待出库”或进入“物流异常待处理”，具体取决于撤回原因。
- 已签收后发出方不能直接撤回，只能由接收方在签收或核收环节登记异常。

#### 异常处理

- 转运箱未按时送达：平台生成超时预警，标记转运箱和箱内样本为“转运超时”，但不直接判定样本不合格。
- 转运箱破损：接收方签收时登记异常，关联转运箱和受影响样本。
- 样本短少：接收方登记实收数量和短少样本清单，短少样本进入“物流异常待处理”。
- 多出样本：接收方登记多出样本条码或无法识别样本信息，进入物流异常处理。
- 样本标签异常或无法识别：登记异常，不直接进入待核收。
- 样本容器破损/泄漏：登记物流异常，质量判定放入 F-004。
- 运输条件不符合要求：登记异常，如温度超限、未按要求冷藏等；是否影响质量由 F-004 判断。
- 错送机构或错收转运箱：登记错送/错收异常，转运箱进入异常处理，不自动签收为正常。
- 出库后撤回或取消运输：未签收前允许发出方发起运输撤回/异常撤回；已签收后发出方不能直接撤回。
- 重复签收：平台提示当前转运箱已签收，不重复生成签收记录。
- 扫描不存在或无权限转运箱码：提示转运箱不存在或不属于当前机构可见范围。

#### 性能与时效要求

- 转运箱创建、样本装箱、出库确认、签收确认应实时更新转运箱和样本状态。
- 装箱扫码时应快速校验样本状态、是否已绑定其他未完成转运箱、当前机构是否有权限操作。
- 出库确认后应立即启动运输时限计算。
- 签收确认后，无异常样本应立即进入“已签收/待核收”。
- 异常样本应立即进入“物流异常待处理”。
- 超时预警任务应按配置时限自动触发。
- 具体接口响应时间、并发规模、超时任务调度频率暂存至 `系统实现相关.md`，后续统一讨论。

#### 通知与审计要求

- 平台记录建箱、装箱、移除样本、出库、在途节点、签收、异常登记、超时预警、撤回等操作。
- 审计至少包含操作人、操作机构、操作时间、操作终端/来源系统、转运箱码、样本条码清单、变更前状态、变更后状态、异常类型、原因、备注、Trace ID/Request ID。
- 超时、错送、短少、破损等是否主动通知相关人员，不在 F-003 中定义，放入 F-020 消息通知中心。
- 物流状态变化是否推送 HIS/LIS/采样端，不在 F-003 中定义，放入 F-005 样本状态全程追踪。
- 患者敏感信息在日志中应脱敏或仅记录摘要。

#### 与 HIS/LIS/物流/患者端的边界

| 事项 | 平台 | 外部系统/后续模块 |
|------|------|------------------|
| 建箱装箱 | 平台创建转运箱并绑定样本条码 | 外部采样端可通过接口提交 |
| 出库确认 | 平台记录发出方、交接对象、箱内明细 | 实际运输交接由现场执行 |
| 在途追踪 | P0 必记出库和签收，可选记录中间节点 | GPS、温度、冷链设备数据可后续接入 |
| 签收确认 | 平台按转运箱签收并登记异常 | 逐支样本扫码核对为可选 |
| 超时预警 | 平台按机构/线路时限生成预警和状态标记 | 通知渠道归 F-020 |
| 物流异常 | 平台登记物流异常和影响样本 | 质量判定、拒收、补采、重采归 F-004 |
| 状态推送 | F-003 更新内部物流状态 | 对外推送归 F-005 |
| 核收质检 | 不处理 | 归 F-004 |
| 运输调度 | 不负责 | 由物流/院内管理体系负责 |

#### 遗留问题与内部待确认

以下事项已登记在 `风险_待内部讨论确定.md`，不作为 F-003 无条件定稿结论：

- 转运箱码由平台生成，还是允许录入已有实体箱码。
- 是否需要维护转运箱基础档案、周转箱库存、箱体启停用状态。
- 是否限制一个转运箱可装样本数量、样本类型或温控要求。
- 是否要求装箱后封箱，是否记录封签号。
- 是否需要限制单个样本被移出转运箱的时机和权限。
- 超时任务调度频率、运输线路配置粒度、预警升级规则。
- GPS、温度、冷链设备数据是否进入后续版本。

以下事项已登记在 `系统实现相关.md`，待需求边界确定后讨论：

- F-003 相关接口性能指标、并发规模、超时时间。
- F-003 建箱、装箱、出库、签收、异常登记、撤回接口形态。
- F-003 超时预警任务调度方式和状态更新机制。

#### 本轮确认结论

F-003 的平台职责收敛为“以转运箱为主对象完成样本物流追踪”。平台按转运箱进行建箱、装箱、出库和签收，箱内绑定样本条码明细，样本状态随转运箱物流节点同步更新。P0 必须记录出库和签收两个关键节点，中间节点、GPS、温度和冷链设备数据作为可选扩展。物流异常和超时在 F-003 中登记、标记和审计，但不直接做质量判定、拒收、补采或重采，这些由 F-004 或后续质量流程承接。

---

### F-004 样本核收与质量初筛

| 属性 | 内容 |
|------|------|
| 功能编号 | F-004 |
| 功能名称 | 样本核收与质量初筛 |
| 所属模块 | 业务流程管理 |
| 业务价值 | 承接已签收/待核收样本，对转运箱内样本进行数量核对、核收确认和基础质量初筛，决定样本是否可以进入后续检测或前处理范围，并对异常样本形成待处理事项。 |
| 核心用户 | 检验中心核收人员、检验中心质量人员、平台运维/配置人员 |
| 平台入口/接口 | 待核收列表、转运箱批量核收、样本异常登记、质量初筛、待处理事项生成、核收更正 |
| 当前状态 | 已完成讨论 |

#### 功能定位

F-004 承接 F-003 中状态为“已签收/待核收”的样本。它的核心目标是让检验中心在接收转运箱后，对箱内样本完成核收确认和基础质量初筛，判断样本是否可以进入后续检测或前处理范围。

平台采用“转运箱批量核收 + 异常样本单独处理”方式：正常样本可以随转运箱批量核收通过；异常样本必须按样本条码单独登记异常原因、核收结果、质量初筛结果和待处理事项。最终结果仍落到每支样本条码。

#### 平台负责

- 查询“已签收/待核收”的转运箱和样本。
- 展示箱内样本清单、申请信息、物流异常和超时标记。
- 支持按转运箱批量核收。
- 支持异常样本按样本条码单独登记。
- 同一流程中完成数量核对和质量初筛。
- 分别记录核收结果和质量初筛结果。
- 记录不合格原因并生成待处理事项。
- 将“待补单”拆分为“待补资料”与“待补采/重采待确认”。
- 核收通过且质量初筛通过后，将样本状态更新为“已核收/待检测”。
- 支持检测/前处理前的核收结果更正，并保留审计。

#### 平台不负责

- 不负责实际仪器上机检测。
- 不负责检验结果产生。
- 不直接触发补采/重采任务。
- 不直接决定 HIS 医嘱变更、退费或重新开单。
- 不在 F-004 中定义 LIS 推送接口细节。
- 不处理结果审核、复检、人工干预，这些归入后续 F-006/F-007 或质量流程。

#### 触发条件

F-003 转运箱签收完成后，无物流异常的样本进入“已签收/待核收”，检验中心核收人员开始对转运箱和箱内样本进行核收与质量初筛。

#### 前置条件

- 转运箱已完成签收。
- 样本状态为“已签收/待核收”，或存在物流异常但允许在核收页面查看和人工确认。
- 样本具备平台样本条码。
- 样本关联平台申请、患者、项目和采样计划。
- 核收人员具备核收、异常登记或更正权限。

#### 后置结果

核收通过且质量初筛通过后：

- 样本状态变更为“已核收/待检测”。
- 样本进入后续检测或前处理范围。
- 平台保留核收记录、质量初筛记录和审计记录。

核收未通过或质量初筛未通过后：

- 样本不进入待检测。
- 平台记录异常原因。
- 平台生成待处理事项，例如待补资料、待补采/重采待确认、物流异常待确认或人工确认。

#### 核心字段/数据对象

| 对象 | 含义 | 关键说明 |
|------|------|----------|
| SpecimenReceipt | 样本核收记录 | 记录核收时间、核收人、核收结果 |
| QualityScreeningRecord | 质量初筛记录 | 记录初筛结果、不合格原因和备注 |
| ReceiptBatch | 核收批次 | 可对应一个转运箱或一次批量核收操作 |
| SpecimenException | 样本异常记录 | 记录异常类型、来源、影响样本和处理状态 |
| PendingAction | 待处理事项 | 例如待补资料、待补采/重采待确认 |
| CorrectionRecord | 更正记录 | 记录核收或初筛结果更正 |
| AuditLog | 审计日志 | 记录关键操作和接口调用 |

核收/初筛最低必填字段：

- 样本条码。
- 转运箱码或核收批次。
- 核收时间。
- 核收人。
- 核收机构/检验中心。
- 核收结果：通过/不通过。
- 质量初筛结果：通过/不通过。
- 异常原因，异常时必填。
- 待处理事项类型，异常时必填。
- 备注。
- 操作终端或来源系统。
- 操作时间。

#### 主流程

1. F-003 转运箱签收完成后，无物流异常的样本进入“已签收/待核收”；物流异常样本进入“物流异常待处理”。
2. 核收人员查询待核收转运箱或样本列表。
3. 平台展示转运箱信息、箱内样本清单、申请摘要、样本类型、容器要求、采集时间、出库/签收时间、物流异常和超时标记。
4. 核收人员打开转运箱并进行数量核对。
5. 正常样本可按转运箱批量核收通过。
6. 对发现异常的样本，核收人员按样本条码单独登记核收结果、质量初筛结果、异常原因和备注。
7. 平台根据登记结果生成对应待处理事项，例如“待补资料”、“待补采/重采待确认”、“物流异常待确认”。
8. 核收通过且质量初筛通过的样本，状态变为“已核收/待检测”。
9. 核收未通过或质量初筛未通过的样本，状态变为对应异常状态，不进入待检测。
10. 在样本进入检测或前处理前，如发现误核收或误判，可更正核收/初筛结果，并保留原记录。
11. 样本一旦进入检测/前处理或产生结果，F-004 不再直接更正核收状态，转入结果审核、复检、质量异常或人工干预流程。

#### 样本核收状态

- 已签收/待核收。
- 核收中。
- 已核收/待检测。
- 核收不通过。
- 质量初筛不通过。
- 待补资料。
- 待补采/重采待确认。
- 物流异常待确认。
- 已更正。

关键规则：

- 正常样本支持按转运箱批量核收通过。
- 异常样本必须按样本条码单独登记。
- 核收结果和质量初筛结果应分开记录。
- 只有核收通过且质量初筛通过的样本进入“已核收/待检测”。
- F-004 不负责上机检测、不负责结果产生。
- LIS 推送接口细节不在 F-004 中定稿。

#### 质量初筛异常原因

P0 质量初筛异常原因包括：

- 样本量不足。
- 容器错误。
- 样本类型不符。
- 标签无法识别或标签信息不一致。
- 样本破损、泄漏。
- 严重溶血、脂血、黄疸等肉眼可见异常。
- 凝血/抗凝异常。
- 运输超时。
- 运输温度或运输条件不符合要求。
- 申请信息缺失或与样本不匹配。
- 未收到样本但系统显示已签收。
- 多出样本或无法匹配申请。

这些原因在 P0 作为平台可选字典；字典体系归系统架构/平台框架层面处理，不在本 PRD 中展开。

#### 待处理事项

待处理事项分为：

- 待补资料：申请资料、患者信息、项目、临床诊断、申请医生、样本与申请匹配关系等需要补充或更正。
- 待补采/重采待确认：样本量不足、容器错误、破损、泄漏、严重肉眼异常等可能需要重新采样。
- 物流异常待确认：短少、多出、错送、运输超时、运输条件异常等需要进一步确认。
- 人工确认：无法自动归类或需要检验中心人工判断的情况。

关键规则：

- F-004 只生成待处理事项，不直接触发补采/重采任务。
- 待补资料不等于待补采/重采。
- 多个异常原因可关联同一样本。
- 异常来源可以来自物流、核收、质量初筛或人工登记。
- 是否通知 HIS、采样端、物流人员，放入 F-020 消息通知中心。
- 是否对外推送状态，放入 F-005 样本状态全程追踪。

#### 核收更正

- 在样本进入检测或前处理前，允许更正核收结果和质量初筛结果。
- 更正必须登记原因、操作人、时间，并保留原记录。
- 更正后应同步更新样本状态和待处理事项。
- 样本一旦进入检测/前处理或产生结果，F-004 不再直接更正核收状态，应走结果审核、复检、质量异常或人工干预流程。

#### 异常处理

- 样本条码不存在：提示条码不存在或不属于当前核收范围。
- 样本未签收：禁止核收，提示需先完成物流签收。
- 样本存在物流异常：允许进入核收页面查看，但默认不批量通过，需人工确认或单独登记。
- 重复核收：提示当前核收状态；如需修改，走更正流程。
- 批量核收中部分样本异常：正常样本可通过，异常样本单独登记，不影响其他样本。
- 初筛不通过：记录原因，生成待处理事项，样本不进入待检测。
- 申请资料不完整或样本与申请不匹配：生成“待补资料”。
- 样本质量或容器问题：生成“待补采/重采待确认”。
- 物流相关问题：生成“物流异常待确认”。
- 误核收/误判：检测或前处理前允许更正；进入检测/前处理后不允许 F-004 直接更正。

#### 性能与时效要求

- 待核收列表应支持按接收机构、转运箱码、样本条码、签收时间、核收状态、异常类型筛选。
- 转运箱批量核收应实时更新箱内样本核收状态。
- 异常样本登记后应实时生成待处理事项。
- 更正核收或初筛结果后，应实时同步样本状态和待处理事项。
- 具体接口响应时间、并发规模、批量核收数量上限暂存至 `系统实现相关.md`，后续统一讨论。

#### 通知与审计要求

- 平台记录待核收查询、批量核收、异常登记、质量初筛、更正、待处理事项生成等操作。
- 审计至少包含操作人、操作机构、操作时间、操作终端/来源系统、转运箱码、样本条码、核收结果、初筛结果、异常原因、待处理事项、变更前后状态、备注、Trace ID/Request ID。
- 不合格样本、待补资料、待补采/重采待确认是否通知 HIS、采样端或物流人员，放入 F-020 消息通知中心。
- 样本核收状态是否推送 HIS/LIS/采样端，放入 F-005 样本状态全程追踪。
- 患者敏感信息在日志中应脱敏或仅记录摘要。

#### 与 HIS/LIS/仪器/患者端的边界

| 事项 | 平台 | 外部系统/后续模块 |
|------|------|------------------|
| 待核收列表 | 平台提供转运箱和样本查询 | 外部系统可通过接口查询 |
| 核收操作 | 平台记录核收结果 | 现场人员执行实际开箱和核对 |
| 质量初筛 | 平台记录初筛结果和异常原因 | 复杂质量判定可进入后续质量流程 |
| 不合格处置 | 平台生成待处理事项 | 是否补采、重采、拒收由后续流程/人工决定 |
| 待补资料 | 平台记录资料问题 | HIS/申请方负责补齐或更正来源资料 |
| 待补采/重采 | 平台记录待确认事项 | 采样端/HIS/消息中心后续处理 |
| LIS 推送 | F-004 不定义接口细节 | 归 F-005 或 LIS 对接实现 |
| 检测上机 | 不负责 | LIS/仪器/检测流程负责 |
| 结果审核 | 不负责 | 归 F-006/F-007 |

#### 遗留问题与内部待确认

以下事项已登记在 `风险_待内部讨论确定.md`，不作为 F-004 无条件定稿结论：

- 初筛异常原因字典是否由中心统一维护，字典体系归系统架构/平台框架层面处理。
- 批量核收一次最多处理多少样本。
- 是否要求异常样本上传照片或附件。
- 是否允许同一样本关联多个待处理事项。
- “待补资料”如何与 HIS/申请方闭环。
- “待补采/重采待确认”是否需要自动生成采样端任务。
- 核收通过后是否立即推送 LIS，以及推送字段和时机。

以下事项已登记在 `系统实现相关.md`，待需求边界确定后讨论：

- F-004 相关接口性能指标、并发规模、批量核收数量上限。
- F-004 待核收查询、批量核收、异常登记、质量初筛、更正接口形态。

#### 本轮确认结论

F-004 的平台职责收敛为“对已签收样本完成核收与基础质量初筛”。平台以转运箱批量核收为主，异常样本按样本条码单独登记；核收结果和质量初筛结果分开记录。核收通过且质量初筛通过的样本进入“已核收/待检测”。不合格样本不直接触发补采/重采，而是生成待处理事项，并将“待补单”拆分为“待补资料”和“待补采/重采待确认”。F-004 不负责上机检测、结果产生、LIS 推送细节或结果审核。

---

### F-005 样本状态全程追踪

| 属性 | 内容 |
|------|------|
| 功能编号 | F-005 |
| 功能名称 | 样本状态全程追踪 |
| 所属模块 | 业务流程管理 |
| 业务价值 | 将申请、采样、物流、核收、检测、审核、报告阶段状态串成统一状态链，为平台内部用户和外部系统提供进度查询、状态汇总和关键节点状态同步能力。 |
| 核心用户 | HIS/LIS/采样端等外部系统、申请机构、检验中心、平台管理员 |
| 平台入口/接口 | 状态查询、样本状态历史查询、申请状态汇总查询、关键节点状态推送、推送失败队列、人工补推 |
| 当前状态 | 已完成讨论 |

#### 功能定位

F-005 负责把 F-001 到 F-004 已确认的申请、采样、物流、核收状态，与后续检测、审核、报告阶段状态串成统一状态链。它的核心目标是让平台内部用户和外部系统能够按申请或样本条码查询进度，并让 HIS/LIS/采样端等系统及时获得关键节点状态。

平台采用“统一状态机 + 关键节点推送型”：以样本条码为最小追踪粒度，同时维护申请级汇总状态；关键节点支持对外系统推送，人员通知交给 F-020 消息通知中心。是否启用对外推送、推送给哪些系统、推送哪些节点，作为内部待讨论事项，不作为本轮无条件定稿。

#### 平台负责

- 维护申请级和样本级状态链。
- 以样本条码作为最小追踪粒度。
- 根据样本状态汇总申请整体状态。
- 接收平台内部流程产生的状态，例如申请创建、采样、物流、核收。
- 接收 LIS/报告系统或后续模块同步的检测、审核、报告状态。
- 提供按平台申请单号、HIS 申请号、样本条码、患者信息、机构、科室、时间范围、异常状态等条件查询。
- 支持对 HIS/LIS/采样端推送关键节点状态。
- 记录状态变更来源、时间、前后状态和推送审计。
- 处理推送失败、重试、失败队列和人工补推。

#### 平台不负责

- 不负责人员消息通知、短信、企微、站内信、电话提醒等，归入 F-020。
- 不负责报告内容查询，归入 F-008。
- 不负责患者端报告查询，归入 F-008 或患者端相关功能。
- 不负责检测结果产生、报告审核、自动审核规则，归入 LIS/F-006/F-007。
- 不替代各业务模块产生原始业务状态，只负责统一汇总、查询和推送。

#### 触发条件

各业务模块或外部系统产生关键状态事件时，F-005 接收状态事件并更新样本状态、申请汇总状态和状态历史；当状态属于需要同步的关键节点时，平台生成对外状态推送任务。

#### 前置条件

- 平台申请、样本条码等业务对象已存在。
- 状态事件包含可识别的申请或样本标识。
- 状态来源模块或来源系统已接入平台。
- 状态编码、状态流转规则和权限范围已配置。
- 对外推送启用时，目标系统已完成接入配置。

#### 后置结果

状态更新成功后：

- 平台写入状态历史。
- 平台更新样本当前状态。
- 平台根据样本状态更新申请汇总状态。
- 如属于关键节点且启用对外推送，平台生成推送任务。
- 平台记录状态变更审计。

状态更新失败时：

- 平台不更新当前状态。
- 平台记录异常状态事件和失败原因。
- 对可修复问题进入待处理或人工排查。

#### 核心字段/数据对象

| 对象 | 含义 | 关键说明 |
|------|------|----------|
| SpecimenStatus | 样本当前状态 | 以样本条码为最小粒度 |
| OrderStatus | 申请汇总状态 | 根据申请下样本状态和报告状态汇总 |
| StatusEvent | 状态事件 | 各模块或外部系统上报的状态变化 |
| StatusHistory | 状态历史 | 记录前状态、后状态、来源、时间和备注 |
| StatusExceptionFlag | 异常标记 | 表示转运超时、标签异常、待补资料等可并存标记 |
| StatusPushTask | 状态推送任务 | 面向 HIS/LIS/采样端等系统 |
| StatusPushLog | 状态推送日志 | 记录请求、响应、失败原因和重试次数 |
| AuditLog | 审计日志 | 记录状态变更、查询和推送操作 |

状态事件最低必填字段：

- 状态对象类型：申请或样本。
- 平台申请单号或样本条码。
- 状态编码。
- 状态名称。
- 状态发生时间。
- 来源模块或来源系统。
- 操作人或来源账号。
- Trace ID/Request ID。
- 备注。

#### 状态追踪对象

- 样本状态是最小追踪粒度，围绕样本条码展开。
- 申请状态是汇总状态，由申请下样本状态和报告状态汇总得出。
- 患者主索引归 F-016，报告内容查询归 F-008。

#### 样本主状态

- 待采样。
- 已采集/待出库。
- 已出库/转运中。
- 已签收/待核收。
- 已核收/待检测。
- 检测中。
- 结果待审核。
- 报告已发布。
- 报告已回传。
- 已撤销。
- 已作废。
- 异常结束。

#### 申请汇总状态

- 待采样。
- 部分采样。
- 采样已处理。
- 转运中。
- 部分签收。
- 已签收/待核收。
- 部分核收。
- 已核收/待检测。
- 检测中。
- 结果待审核。
- 报告已发布。
- 报告已回传。
- 已撤销。
- 异常结束。

#### 异常状态表达

- 可继续推进的异常用“主状态 + 异常标记/待处理事项”表达，例如“转运中 + 转运超时”、“已签收/待核收 + 标签异常”、“核收不通过 + 待补资料”。
- 终止性异常作为主状态，例如“已作废”、“已撤销”、“异常结束”。
- 异常标记和待处理事项不覆盖主状态，应保留状态发生时的业务位置。

#### 状态来源

- F-001 产生：申请已创建、待采样、已撤销、样本条码已作废。
- F-002 产生：已采集/待出库、未采、暂缓、采集失败、采集更正。
- F-003 产生：已出库/转运中、转运超时、已签收/待核收、物流异常、运输撤回。
- F-004 产生：已核收/待检测、核收不通过、质量初筛不通过、待补资料、待补采/重采待确认。
- LIS/仪器/后续模块产生：检测中、已检测、结果待审核。
- F-006/F-007/F-008 或报告系统产生：审核通过、报告已发布、报告已回传。

#### 主流程

1. 各业务模块在关键动作完成时产生状态事件。
2. F-005 接收状态事件，校验申请、样本、来源系统和状态流转是否合法。
3. 平台写入状态历史，记录前状态、后状态、来源模块、操作人/来源系统、时间和 Trace ID。
4. 平台更新样本当前状态。
5. 平台根据样本状态汇总申请级状态。
6. 如状态属于关键节点且启用对外状态推送，平台生成对外推送任务。
7. 外部系统可按申请、样本、患者、机构、科室、时间范围、异常状态查询状态。
8. 推送失败时进入重试和失败待处理流程，不回滚平台内部状态。

#### 状态查询能力

- 按平台申请单号查询申请和样本状态。
- 按申请机构 + HIS 申请号查询申请和样本状态。
- 按样本条码查询样本全程状态。
- 按患者信息查询患者名下申请/样本状态摘要。
- 按机构、科室、时间范围、状态筛选列表。
- 按异常状态查询待处理样本。
- 支持查看状态历史，包括状态发生时间、来源模块、操作人或来源系统、前后状态、异常标记、备注。

权限边界：

- 申请机构只能查看本机构申请及样本状态。
- 检验中心可查看接收和检测范围内的样本状态。
- 平台管理员可按授权范围查询。
- 患者端查询是否开放、开放哪些字段，放到 F-008 报告查询或患者端相关功能，不在 F-005 定稿。

#### 关键节点推送

P0 支持将以下关键节点作为可推送状态：

- 申请已创建。
- 已采集。
- 已出库/转运中。
- 已签收/待核收。
- 已核收/待检测。
- 核收/初筛异常。
- 已检测/结果待审核。
- 报告已发布。
- 报告已回传。
- 申请撤销。
- 样本作废或异常结束。

推送规则：

- F-005 只负责系统间状态推送，不做人员消息通知。
- 推送对象包括 HIS、LIS、采样端等已接入系统。
- 非关键内部状态、查询动作、纯操作日志不主动推送。
- 推送内容应包含平台申请单号、HIS 申请号、样本条码、状态编码、状态名称、发生时间、来源模块、异常标记、备注、Trace ID/Request ID。
- 是否启用推送、哪些系统接收、哪些节点必须推送，已登记在 `风险_待内部讨论确定.md`。
- 具体推送协议、字段 JSON、重试次数和间隔暂存至 `系统实现相关.md`。

#### 推送失败处理

- 平台记录每次推送请求、响应、失败原因和重试次数。
- 支持自动重试。
- 多次失败后进入“推送失败待处理”队列。
- 支持人工补推或重新触发推送。
- 推送失败不回滚平台内部业务状态。
- 是否通知运维或业务人员，放入 F-020 消息通知中心。

#### 异常处理

- 收到未知申请或未知样本状态事件：拒绝写入当前状态，记录异常事件。
- 状态事件重复上报：按事件唯一标识或业务键去重；无法确认时保留日志但不重复推进状态。
- 状态倒退：默认拒绝，例如已核收样本不能被更新为待采样；允许的更正必须来自对应业务模块的更正事件。
- 状态跳跃：需校验是否允许，例如可从“已签收/待核收”进入“已核收/待检测”，但不能直接从“待采样”到“报告已发布”。
- 外部系统状态晚到：记录到状态历史，但是否更新当前状态需按状态优先级和发生时间判断，具体规则放入实现讨论。
- 申请下部分样本异常：申请状态应体现部分异常或待处理，不直接把整单判为异常结束。
- 推送失败：进入重试和失败待处理，不回滚内部状态。
- 权限不足查询：拒绝返回敏感状态详情，并记录访问审计。

#### 性能与时效要求

- 状态更新应在业务动作完成后实时或准实时写入。
- 状态查询应支持按申请、样本条码、机构、科室、时间范围和状态筛选。
- 状态历史应支持分页查询。
- 关键节点推送应在状态写入后触发。
- 推送失败重试不应阻塞业务主流程。
- 具体查询响应时间、状态事件吞吐量、推送重试策略暂存至 `系统实现相关.md`。

#### 通知与审计要求

- 平台记录所有状态变更历史。
- 审计至少包含状态对象类型、平台申请单号、HIS 申请号、样本条码、前状态、后状态、异常标记、来源模块/来源系统、操作人、发生时间、写入时间、推送结果、Trace ID/Request ID。
- 状态查询应记录访问审计，尤其是跨机构查询、患者信息查询和异常状态查询。
- F-005 不负责人员通知，通知渠道、模板、回执、重试和升级归入 F-020。
- 患者敏感信息在日志中应脱敏或仅记录摘要。

#### 与 HIS/LIS/仪器/患者端的边界

| 事项 | 平台 F-005 | 外部系统/后续模块 |
|------|------------|------------------|
| 申请/样本状态链 | 统一维护和查询 | 各业务模块产生原始状态 |
| 采样/物流/核收状态 | 接收并汇总 | F-002/F-003/F-004 产生 |
| 检测/审核/报告状态 | 接收并汇总 | LIS、仪器、F-006/F-007/F-008 产生 |
| 对外状态推送 | 支持关键节点系统推送 | HIS/LIS/采样端接收处理 |
| 人员通知 | 不负责 | F-020 负责 |
| 报告内容查询 | 不负责 | F-008 负责 |
| 患者端查询 | 不定稿 | F-008 或患者端功能讨论 |
| 状态更正 | 只接收合法更正事件并记录 | 由对应业务模块发起更正 |

#### 遗留问题与内部待确认

以下事项已登记在 `风险_待内部讨论确定.md`，不作为 F-005 无条件定稿结论：

- 是否启用对外状态推送。
- 哪些接入系统需要接收状态推送。
- 哪些关键节点必须推送，哪些节点只提供查询。
- 外部系统是否可以主动查询补偿未收到的状态。
- 状态事件唯一标识和去重规则。
- 状态优先级、状态倒退、状态跳跃、晚到事件处理规则。
- 申请汇总状态计算规则。
- 状态查询性能指标、状态历史保留周期。

以下事项已登记在 `系统实现相关.md`，待需求边界确定后讨论：

- F-005 状态查询、状态事件接收、状态历史查询、推送任务、人工补推接口形态。
- F-005 关键节点推送字段、推送协议和接入系统清单。
- F-005 推送重试次数、间隔、失败队列处理 SLA。
- F-005 状态事件吞吐量、查询响应时间、历史数据保留策略。

#### 本轮确认结论

F-005 的平台职责收敛为“统一维护申请级和样本级状态链，并支持状态查询和关键节点系统推送”。样本条码是最小追踪粒度，申请状态由样本状态和报告状态汇总得出。状态链覆盖从申请创建到报告已发布/已回传，但不负责报告内容查询。采样、物流、核收状态由平台内部模块产生；检测、审核、报告状态由 LIS、报告系统或后续模块同步。人员通知归 F-020，报告查询归 F-008。对外状态推送作为平台能力保留，但是否启用、推送对象和推送节点放入内部待讨论确定。

---

### F-006 结果审核与人工干预

| 属性 | 内容 |
|------|------|
| 功能编号 | F-006 |
| 功能名称 | 结果审核与人工干预 |
| 所属模块 | 业务流程管理 |
| 业务价值 | 承接 LIS/检测系统回传的检验结果和 F-007 自动审核建议，对异常、需复核、需复检结果进行人工把关，确保报告发布前的质量控制、责任留痕和复检闭环。 |
| 核心用户 | 检验技师、审核医师、检验中心管理人员 |
| 平台入口/接口 | 待审核队列、审核详情、人工审核、复检指令、退回复核、补充资料要求、撤回/更正申请登记、审核日志查询 |
| 当前状态 | 已完成讨论 |

#### 功能定位

F-006 负责检验结果发布前的审核承接和人工干预。平台接收 LIS/检测系统回传的检验结果，调用 F-007 自动审核规则形成审核建议；自动审核通过的结果默认可直接发布，自动审核未通过或需人工处理的结果按风险分层进入审核队列。

F-006 不替代 LIS 的检测执行、仪器原始结果管理和复检执行能力。平台负责审核队列、人工判断、复检指令发起、审核结论、报告发布状态和审计留痕；复检执行、重测结果采集和原始结果管理仍由 LIS/实验室流程完成。

#### 平台负责

- 接收 LIS/检测系统回传的检验结果。
- 调用 F-007 自动审核规则，接收自动审核结论和规则失败原因。
- 对自动审核通过的结果默认发布报告。
- 支持自动审核通过结果的抽检复核和人工追回复核能力。
- 对自动审核未通过结果按风险分层进入审核队列。
- 提供人工审核界面，展示当前检验结果、规则失败原因和患者基础信息。
- 支持审核通过、退回复核/重传结果、发起复检、要求补充资料、填写审核备注。
- 发起复检指令并跟踪复检状态。
- 复检结果回流后强制进入人工审核，并展示自动审核建议。
- 人工审核通过后发布报告，并触发 F-008 报告查询/回传能力。
- 记录已发布报告撤回/更正申请。
- 记录完整审核过程和审计日志。

#### 平台不负责

- 不负责仪器检测、上机任务、原始检测结果采集。
- 不直接修改仪器原始结果。
- 不负责复检实际执行。
- 不负责 LIS 内部结果更正流程。
- 不负责完整报告撤回、更正、重新发布审批闭环，相关规则放入内部讨论。
- 不负责危急值通知、接收确认、升级和临床处置闭环，归入 F-010。
- 不负责自动审核规则配置和规则执行细节，归入 F-007。
- 不负责报告内容查询、报告回传细节、患者端报告查询，归入 F-008。
- 不负责人员消息通知渠道和通知模板，归入 F-020。

#### 触发条件

LIS/检测系统产生检验结果并回传平台后，平台调用 F-007 自动审核规则；自动审核通过的结果进入自动发布流程，自动审核未通过、复检回流或人工追回复核的结果进入 F-006 审核流程。

#### 前置条件

- 平台申请和样本条码已存在。
- 样本已完成核收并进入检测流程。
- LIS/检测系统已回传结构化检验结果。
- 检验结果能关联到平台申请、样本、项目和患者。
- F-007 自动审核规则可返回审核结论、规则失败原因或规则执行异常。
- 审核人员具备对应审核角色。

#### 后置结果

自动审核通过且未被拦截时：

- 平台记录自动审核通过结果。
- 平台将结果状态更新为“报告已发布”。
- 平台触发 F-008 报告查询/回传能力。

自动审核未通过时：

- 平台记录规则失败原因。
- 结果按风险分层进入对应人工审核队列。
- 样本或申请状态更新为“结果待审核”或对应待处理状态。

人工审核通过时：

- 平台记录审核结论、审核人、审核时间和备注。
- 平台将结果状态更新为“报告已发布”。
- 平台触发 F-008 报告查询/回传能力。

发起复检时：

- 平台记录复检指令、复检原因、发起人、时间、目标样本和项目。
- 样本或项目进入“待复检/复检中”状态。
- LIS/实验室执行复检后回传复检结果。
- 复检结果回流后必须进入人工审核。

#### 核心字段/数据对象

| 对象 | 含义 | 关键说明 |
|------|------|----------|
| Result | 检验结果 | 来自 LIS/检测系统，关联申请、样本、项目和患者 |
| AutoReviewResult | 自动审核结果 | 来自 F-007，包含通过、失败、警示、规则失败原因 |
| ReviewTask | 审核任务 | 按风险队列分配和处理 |
| ReviewQueue | 审核队列 | 包括待人工审核、疑似危急值、需复检确认、资料不完整/条件不足 |
| ManualReviewRecord | 人工审核记录 | 记录人工动作、结论、备注、审核人和时间 |
| RetestInstruction | 复检指令 | 平台发起，LIS/实验室执行 |
| ReportPublishRecord | 报告发布记录 | 记录报告发布状态、发布时间和触发来源 |
| ReportCorrectionRequest | 报告撤回/更正申请 | 仅记录申请，不在 F-006 定稿完整审批闭环 |
| AuditLog | 审计日志 | 记录自动审核、人工审核、复检、发布、撤回/更正申请全过程 |

人工审核最低展示信息：

- 患者基础信息。
- 平台申请单号。
- 样本条码。
- 检验项目。
- 当前检验结果。
- 参考范围或结果标识。
- 自动审核结论。
- 规则失败原因。
- 当前审核队列。
- 历史审核/复检记录摘要。

#### 审核队列

自动审核未通过后，平台按原因和风险分层进入不同队列：

- 待人工审核：一般规则未通过、结果异常但未触发危急值或复检条件。
- 疑似危急值：结果触及危急值相关规则或边界，需要审核阶段优先处理；危急值通知闭环归 F-010。
- 需复检确认：规则提示结果可疑、前后矛盾、样本或检测条件可能影响结果。
- 资料不完整/条件不足：患者信息、申请信息、样本条件或结果上下文不足以完成审核。

#### 人工审核动作

人工审核支持以下动作：

- 审核通过：确认结果可发布，平台发布报告。
- 退回复核/重传结果：要求 LIS/实验室复核或重新传输结果。
- 发起复检：生成复检指令并跟踪状态。
- 要求补充资料：标记资料不完整或条件不足，等待申请方、LIS 或相关人员补充。
- 填写审核备注：记录判断依据、异常说明或处理意见。

平台不允许审核人员直接修改仪器原始结果。需要结果更正时，应由 LIS/实验室结果流程产生新结果或修订结果后再回传平台审核。

#### 复检规则

- 复检由平台审核人员发起指令。
- 复检指令应明确复检原因、目标样本、目标项目、发起人、发起时间和备注。
- 复检执行和结果采集由 LIS/实验室流程完成。
- 平台跟踪复检状态，但不管理仪器上机任务。
- 复检结果回流后必须进入人工审核。
- 复检结果可展示 F-007 自动审核建议，但不能仅凭自动审核直接发布。

#### 主流程

1. LIS/检测系统完成检测并回传检验结果。
2. 平台关联申请、样本、项目和患者。
3. 平台调用 F-007 自动审核规则。
4. 自动审核通过时，平台默认发布报告，并记录自动审核通过日志。
5. 自动审核未通过时，平台记录规则失败原因，并按风险分层进入审核队列。
6. 审核人员查看待审核队列和审核详情。
7. 平台展示当前检验结果、规则失败原因和患者基础信息。
8. 审核人员选择审核通过、退回复核/重传结果、发起复检、要求补充资料或填写备注。
9. 审核通过后，平台发布报告并触发 F-008。
10. 发起复检时，平台生成复检指令并等待 LIS/实验室回传复检结果。
11. 复检结果回流后，平台重新生成自动审核建议，但必须进入人工审核。
12. 全流程写入审核日志和状态历史。

#### 发布后撤回/更正申请

- 已发布报告发现问题时，平台可记录撤回/更正申请。
- 申请记录应包含报告标识、申请原因、申请人、申请时间、关联申请、关联样本、关联项目和备注。
- 是否允许撤回、是否需要审批链、是否重新发布、是否通知 HIS/患者端，放入 `风险_待内部讨论确定.md`。
- F-006 不在 P0 中定稿完整报告撤回、更正、重新发布闭环。

#### 异常处理

- 结果无法关联申请或样本：拒绝进入发布流程，进入异常待处理，并记录原因。
- 结果字段缺失或格式异常：进入资料不完整/条件不足队列。
- 自动审核规则执行失败：进入待人工审核队列，并记录规则执行异常。
- 疑似危急值：进入疑似危急值队列，通知闭环由 F-010 处理。
- 审核人员权限不足：禁止操作并记录访问审计。
- 复检指令发送失败：记录失败原因，进入复检指令待处理。
- 复检结果晚到或重复回传：记录事件并按实现规则去重和状态校验。
- 已发布报告发现问题：仅记录撤回/更正申请，不直接撤回或改写报告。

#### 性能与时效要求

- 自动审核通过后的发布应实时或准实时完成。
- 自动审核未通过结果应及时进入人工审核队列。
- 疑似危急值队列应优先展示和处理。
- 复检指令发起后应及时同步给 LIS/实验室流程。
- 审核队列应支持按机构、科室、项目、风险队列、申请时间、结果时间、加急标识等条件筛选。
- 具体响应时间、队列刷新频率、自动发布时效和复检指令同步时效暂存至 `系统实现相关.md`。

#### 通知与审计要求

- 平台记录完整审核过程。
- 审计至少包含平台申请单号、样本条码、项目、患者标识摘要、结果摘要、自动审核结论、规则失败原因、审核队列、人工动作、复检指令、审核备注、操作人、操作角色、操作时间、前后状态、来源系统、Trace ID/Request ID。
- 自动审核通过直接发布也必须记录自动审核和发布日志。
- 复检指令、复检回流、人工审核通过、退回复核、补充资料、撤回/更正申请均需留痕。
- 患者敏感信息在日志中应脱敏或仅记录摘要。
- 人员通知、短信、站内信、企微、电话提醒等由 F-020 统一讨论。

#### 与 HIS/LIS/仪器/患者端的边界

| 事项 | 平台 F-006 | 外部系统/后续模块 |
|------|------------|------------------|
| 检验结果接收 | 接收并进入审核流程 | LIS/检测系统产生并回传 |
| 自动审核规则 | 调用并展示结论 | F-007 负责规则配置和执行细节 |
| 人工审核 | 平台提供队列、详情、动作和留痕 | 审核人员做专业判断 |
| 原始结果修改 | 不负责 | LIS/实验室结果流程负责 |
| 复检发起 | 平台生成复检指令并跟踪 | LIS/实验室执行复检并回传结果 |
| 报告发布 | 审核通过后触发发布状态 | F-008 负责报告查询/回传 |
| 危急值闭环 | 识别并标记疑似危急值 | F-010 负责通知、确认、升级和闭环 |
| 报告撤回/更正 | 记录申请 | 具体审批、撤回、重发规则待内部确认 |
| 患者端展示 | 不负责 | F-008 或患者端能力讨论 |

#### 遗留问题与内部待确认

以下事项已登记在 `风险_待内部讨论确定.md`，不作为 F-006 无条件定稿结论：

- 自动审核通过后的抽检是否启用、抽检比例和抽检范围。
- 历史趋势、关联项目分析是否作为后续增强。
- 发布后报告撤回/更正是否允许、审批链、重新发布和通知范围。
- 审核角色是否需要进一步扩展为角色 + 机构/项目范围授权。
- 疑似危急值队列与 F-010 危急值闭环的触发边界。

以下事项已登记在 `系统实现相关.md`，待需求边界确定后讨论：

- F-006 审核队列、人工审核、复检指令、报告发布、撤回/更正申请接口形态。
- F-006 审核状态机、复检状态、报告发布状态与 F-005 状态追踪的衔接规则。
- F-006 自动发布、队列刷新、复检同步和审核操作性能指标。
- F-006 审核日志、结果摘要和敏感信息脱敏实现规则。

#### 本轮确认结论

F-006 的平台职责收敛为“承接检验结果和自动审核建议，并对异常结果进行人工审核、复检发起、发布控制和全过程留痕”。自动审核通过的结果默认可直接发布，同时支持抽检和追回复核能力；自动审核未通过的结果按风险分层进入审核队列。平台不直接修改仪器原始结果，复检执行和结果采集由 LIS/实验室流程完成。复检结果回流后必须进入人工审核。人工审核通过即发布报告并触发 F-008。已发布报告的问题处理仅记录撤回/更正申请，具体规则放入内部讨论确定。

---

### F-007 自动审核规则引擎

| 属性 | 内容 |
|------|------|
| 功能编号 | F-007 |
| 功能名称 | 自动审核规则引擎 |
| 所属模块 | 业务流程管理 |
| 业务价值 | 为 F-006 结果审核提供可配置、可追溯的自动审核规则能力，减少常规结果人工审核工作量，并将高风险、异常或条件不足结果准确分流到人工审核队列。 |
| 核心用户 | 检验中心规则维护人员、检验中心质量管理人员、F-006 审核流程 |
| 平台入口/接口 | 规则配置、规则草稿、规则发布/启用、规则停用、规则试运行、规则执行、规则执行日志查询 |
| 当前状态 | 已完成讨论 |

#### 功能定位

F-007 是平台内部自动审核规则能力，为 F-006 提供规则执行结果和审核建议。平台提供可配置规则库，支持规则配置、启停、发布、生效版本、停用、历史版本保留、单条结果试运行和执行日志查询。

规则由平台/检验中心统一维护，以平台标准项目为主绑定。机构可以使用规则执行结果，但不能自行新增、修改、禁用或维护自动审核规则。F-007 不做复杂脚本编程、高级规则编排、批量历史回放或命中率分析。

#### 平台负责

- 维护可配置自动审核规则库。
- 按平台标准项目绑定规则。
- 支持规则草稿、发布启用、生效版本、停用和历史版本保留。
- 支持结果范围、危急值范围、结果格式/缺失、患者性别/年龄适用性、项目间逻辑关系、样本状态/质量前置条件等基础规则类型。
- 按规则优先级执行规则。
- 支持高风险或阻断类规则命中后中断后续低优先级规则。
- 输出自动审核结论、命中/失败规则、失败原因和建议动作。
- 规则执行异常、配置异常、必要数据缺失或无法判断时，不允许自动通过。
- 支持单条样本或单份检验结果试运行。
- 记录规则配置、发布、停用、试运行和执行日志。

#### 平台不负责

- 不负责 F-006 人工审核队列和人工审核动作。
- 不负责报告发布、报告查询、报告回传，归入 F-006/F-008。
- 不负责危急值通知、确认、升级和闭环，归入 F-010。
- 不负责机构自行维护规则。
- 不做复杂脚本编程或高级规则编排。
- 不做批量历史结果回放和命中率分析。
- 不把历史趋势、跨机构关联趋势作为 P0 强制规则。
- 不在 F-007 中定义具体权限角色，规则配置权限由 F-018 用户权限与审计管理统一讨论。

#### 触发条件

F-006 在接收到 LIS/检测系统回传的检验结果后，调用 F-007 执行自动审核规则；规则维护人员在规则配置、试运行、发布、停用时，也会触发 F-007 的规则管理和日志记录能力。

#### 前置条件

- 平台标准项目已存在且启用。
- 规则所需的项目、样本、患者、申请、结果字段可被平台识别。
- 对应项目已配置并发布生效的自动审核规则。
- F-006 调用规则引擎时传入可关联的结果、样本、申请和患者上下文。
- 规则配置、发布、停用、试运行等操作受平台权限框架控制。

#### 后置结果

规则执行成功时：

- F-007 生成自动审核结论。
- F-007 返回命中/失败规则、失败原因和建议动作。
- F-007 记录规则执行日志。
- F-006 根据建议动作进入自动发布、人工审核、疑似危急值、需复检建议或资料/条件不足流程。

规则执行失败或条件不足时：

- F-007 不输出自动通过。
- F-007 输出待人工审核或条件不足。
- F-007 记录失败原因和可追溯日志。

规则发布成功时：

- 新规则版本成为生效版本。
- 旧版本进入历史版本。
- 平台记录发布人、发布时间和变更内容。

#### 核心字段/数据对象

| 对象 | 含义 | 关键说明 |
|------|------|----------|
| AutoReviewRule | 自动审核规则 | 绑定平台标准项目和规则类型 |
| RuleVersion | 规则版本 | 支持草稿、生效、停用、历史版本 |
| RuleCondition | 规则条件 | 表示范围、缺失、性别年龄、项目间关系、样本前置条件等判断条件 |
| RulePriority | 规则优先级 | 决定执行顺序和中断策略 |
| RuleAction | 建议动作 | 自动通过、待人工审核、疑似危急值、需复检建议、资料/条件不足 |
| RuleExecutionResult | 规则执行结果 | 返回给 F-006 的自动审核结论 |
| RuleExecutionLog | 规则执行日志 | 记录最终结论、已执行规则、命中/失败原因、中断原因 |
| RuleTrialRun | 规则试运行记录 | 记录单条样本/单份结果的试运行输入和输出 |
| AuditLog | 审计日志 | 记录规则配置、发布、停用、试运行等操作 |

规则配置最低字段：

- 规则名称。
- 平台标准项目编码。
- 规则类型。
- 规则条件。
- 规则优先级。
- 是否阻断后续规则。
- 建议动作。
- 启停状态。
- 规则版本。
- 创建人、创建时间。
- 发布人、发布时间。
- 变更说明。

#### P0 规则类型

F-007 P0 支持以下基础规则类型：

- 结果范围规则：判断结果是否在设定范围内。
- 危急值范围规则：判断结果是否触及危急值相关阈值。
- 结果格式/缺失规则：判断结果是否缺失、格式异常、单位或结果表达不符合要求。
- 患者性别/年龄适用性规则：判断项目或结果是否与患者基础信息冲突。
- 项目间逻辑关系规则：判断同一次结果中多个项目之间是否存在明显逻辑矛盾。
- 样本状态/质量前置条件规则：判断样本核收、质量初筛、复检等前置状态是否满足自动审核条件。

历史趋势、跨机构关联趋势、复杂临床资料关联、批量历史回放和命中率分析不纳入 F-007 P0 强制范围。

#### 规则适用范围

- 规则以平台标准项目为主绑定。
- 规则由平台/检验中心统一维护。
- 机构不允许单独新增规则。
- 机构不允许单独修改规则。
- 机构不允许单独禁用规则。
- 机构不维护本机构独立规则库。
- 机构差异如确需支持，作为后续内部讨论事项，不作为 F-007 P0 默认能力。

#### 规则执行结果

F-007 输出给 F-006 的结果至少包括：

- 自动审核结论。
- 已执行规则。
- 命中规则。
- 失败规则。
- 失败原因。
- 中断原因。
- 建议动作。
- 规则版本。
- 执行时间。
- Trace ID/Request ID。

建议动作包括：

- 自动通过。
- 待人工审核。
- 疑似危急值。
- 需复检建议。
- 资料/条件不足。

#### 规则优先级与中断策略

- 规则按优先级顺序执行。
- 危急值、样本质量不合格、资料缺失等高风险或阻断类规则可配置为命中后中断。
- 高风险或阻断类规则命中后，F-007 可直接输出对应结论并停止后续低优先级规则。
- 规则执行日志应记录已执行规则和中断原因。
- 未执行规则不作为最终判断依据，但应能通过日志解释是因高优先级规则中断而未执行。

#### 规则版本与发布

- 规则编辑后先保存为草稿。
- 草稿规则不直接影响生产审核。
- 规则发布/启用后才成为生效版本。
- 生效版本用于 F-006 自动审核。
- 停用规则不参与后续自动审核。
- 历史版本保留用于追溯既往审核结果。
- 发布人、发布时间、变更内容必须留痕。
- P0 不强制完整审批流，是否双人复核或审批后发布放入内部讨论。

#### 规则试运行

- 支持用单条样本或单份检验结果进行试运行。
- 试运行可查看命中规则、失败原因、输出结论和建议动作。
- 试运行结果不改变真实审核状态。
- 试运行结果不触发报告发布。
- P0 不支持批量历史结果回放和命中率分析。

#### 主流程

1. 规则维护人员基于平台标准项目创建或编辑规则草稿。
2. 规则维护人员配置规则类型、条件、优先级、是否阻断和建议动作。
3. 如需验证，选择单条样本或单份结果进行试运行。
4. 试运行返回命中规则、失败原因、输出结论和建议动作。
5. 规则确认后发布/启用，生成生效版本。
6. F-006 接收检验结果后调用 F-007。
7. F-007 加载对应平台标准项目的生效规则。
8. F-007 按优先级执行规则。
9. 如高风险或阻断类规则命中，停止后续低优先级规则。
10. F-007 输出自动审核结论、命中/失败规则、失败原因和建议动作。
11. F-007 写入规则执行日志。
12. F-006 根据 F-007 输出进入自动发布或人工审核分流。

#### 异常处理

- 未找到生效规则：不允许自动通过，输出待人工审核或条件不足。
- 规则配置异常：不允许自动通过，记录配置异常原因。
- 必要数据缺失：不允许自动通过，输出资料/条件不足。
- 规则执行异常：不允许自动通过，输出待人工审核。
- 规则版本不存在或已停用：不参与执行，并记录异常。
- 规则试运行输入无效：拒绝试运行并提示缺失字段。
- 规则发布时配置不完整：禁止发布，并提示缺失项。
- F-006 调用超时或失败：由 F-006 进入人工审核兜底，F-007 记录调用异常。

#### 性能与时效要求

- F-007 应支持 F-006 在结果回传后实时或准实时调用。
- 规则执行失败不应导致结果自动发布。
- 规则执行日志应可按申请、样本、项目、规则、结论、时间范围查询。
- 规则试运行应返回可解释结果。
- 具体规则执行响应时间、规则数量上限、日志保留周期和查询性能暂存至 `系统实现相关.md`。

#### 通知与审计要求

- 规则配置、发布、停用、试运行和执行均需留痕。
- 规则配置权限由 F-018 用户权限与审计管理统一讨论。
- F-007 只要求相关操作必须受权限控制，不在本功能中展开角色细节。
- 执行日志至少包含平台申请单号、样本条码、项目、规则版本、已执行规则、命中/失败原因、中断原因、最终结论、建议动作、执行时间、调用来源、Trace ID/Request ID。
- 配置审计至少包含规则名称、项目、规则类型、变更前后内容、操作人、操作时间、发布状态、变更说明。
- 患者敏感信息在日志中应脱敏或仅记录摘要。

#### 与 HIS/LIS/仪器/患者端的边界

| 事项 | 平台 F-007 | 外部系统/后续模块 |
|------|------------|------------------|
| 规则配置 | 平台/检验中心统一维护 | 机构不单独维护 |
| 规则执行 | F-007 执行并输出建议 | F-006 调用并使用结果 |
| 结果产生 | 不负责 | LIS/检测系统负责 |
| 人工审核 | 不负责 | F-006 负责 |
| 报告发布 | 不负责 | F-006/F-008 负责 |
| 危急值闭环 | 只输出疑似危急值建议 | F-010 负责通知和闭环 |
| 权限角色 | 不展开 | F-018 统一定义 |
| 患者端展示 | 不负责 | F-008 或患者端能力讨论 |

#### 遗留问题与内部待确认

以下事项已登记在 `风险_待内部讨论确定.md`，不作为 F-007 无条件定稿结论：

- 是否需要规则发布审批或双人复核。
- 是否需要机构差异化规则例外。
- 历史趋势、跨机构关联趋势是否进入后续增强。
- 是否需要批量历史结果回放和命中率分析。
- 自动审核规则的医学责任归属和变更管理制度。

以下事项已登记在 `系统实现相关.md`，待需求边界确定后讨论：

- F-007 规则配置模型、规则条件表达方式和规则版本数据结构。
- F-007 规则执行接口、执行结果结构和错误码。
- F-007 优先级、中断策略和规则执行性能指标。
- F-007 执行日志、配置审计、试运行日志的存储和查询方案。

#### 本轮确认结论

F-007 的平台职责收敛为“提供由平台/检验中心统一维护的可配置自动审核规则库，并为 F-006 输出可解释的自动审核建议”。规则以平台标准项目为主绑定，机构不可单独新增、修改、禁用或维护规则。P0 支持结果范围、危急值范围、结果格式/缺失、患者性别/年龄适用性、项目间逻辑关系、样本状态/质量前置条件等基础规则；不做复杂脚本编程、高级规则编排、历史趋势、批量回放或命中率分析。规则按优先级执行，高风险或阻断类规则可中断后续规则。规则异常、配置异常或条件不足时不允许自动通过。权限角色归 F-018 统一讨论，F-007 仅要求配置、发布、停用、试运行和执行均受控并留痕。

---

### F-008 多渠道报告查询

| 属性 | 内容 |
|------|------|
| 功能编号 | F-008 |
| 功能名称 | 多渠道报告查询 |
| 所属模块 | 业务流程管理 |
| 业务价值 | 承接 F-006 报告发布结果，为平台内授权用户提供报告检索、查看、下载、打印和版本追溯能力，支撑检验结果交付、历史报告调阅和区域检验互认。 |
| 核心用户 | 检验中心人员、接入机构授权人员 |
| 平台入口/接口 | 报告列表查询、患者历史报告查询、申请/样本/报告精确查询、报告详情、版式文件查看、下载、打印、报告版本历史 |
| 当前状态 | 已完成讨论 |

#### 功能定位

F-008 当前收敛为平台内报告查询能力。平台在 F-006 审核通过并发布报告后，提供结构化报告结果查询、正式报告版式文件展示、下载、打印、历史版本追溯和访问审计。

本轮不把 HIS 回传、患者端查询、小程序、短信/微信通知、患者身份认证和患者端运营主体作为 F-008 无条件定稿能力。是否回传 HIS、是否开放患者端、患者端如何认证和运营，放入内部讨论或后续版本确认。

#### 平台负责

- 接收并保存 F-006 发布后的报告状态。
- 展示结构化报告结果。
- 接收并展示 LIS/报告系统提供的报告版式文件引用或文件本身。
- 支持版式文件缺失时仍展示已发布结构化结果。
- 支持按患者信息查询历史报告。
- 支持按平台申请单号、HIS 申请号、样本条码、报告编号精确查询。
- 支持检验中心和接入机构授权人员查询平台内报告。
- 支持接入机构授权人员按患者信息查询全域报告。
- 支持报告在线查看、下载和打印。
- 支持报告撤回、更正、重新发布后的版本历史查看。
- 记录报告查询、查看、下载、打印和历史版本访问审计。

#### 平台不负责

- 不负责报告模板设计。
- 不负责电子签章、签名图片或 PDF 生成。
- 不负责 LIS/报告系统内部报告生成逻辑。
- 不负责患者端查询页面、小程序、公众号或 H5。
- 不负责短信、微信、电话等患者通知，归入 F-020 或后续患者端能力。
- 不在本轮定稿 HIS 报告回传是否启用、回传字段和回传失败处理。
- 不在本轮定稿患者授权、诊疗关系限制等更细粒度跨机构查询规则。
- 不负责报告撤回、更正审批流程，相关边界已在 F-006 和风险文档中登记。

#### 触发条件

F-006 审核通过并发布报告后，平台生成或接收报告发布状态，F-008 允许授权用户查询报告。若 LIS/报告系统提供版式文件，平台接收文件引用或文件本身并在报告详情中展示。

#### 前置条件

- 平台申请、样本、患者、报告对象已存在。
- F-006 已完成审核并发布报告。
- 结构化报告结果已写入平台或可由平台查询。
- 用户已登录平台并具备报告查询权限。
- 跨机构查询时，用户属于接入机构授权人员。
- 如需展示版式文件，LIS/报告系统已提供文件引用或文件本身。

#### 后置结果

报告查询成功后：

- 平台展示报告列表或报告详情。
- 平台展示结构化结果。
- 如存在版式文件，平台展示版式文件。
- 平台记录查询或查看审计。

下载或打印成功后：

- 平台输出报告版式文件或报告页面。
- 平台记录下载或打印审计。

报告发生撤回、更正或重新发布后：

- 平台默认展示最新有效版本。
- 平台保留历史版本入口。
- 授权人员可查看旧版、撤回/更正原因、版本时间和操作记录。

#### 核心字段/数据对象

| 对象 | 含义 | 关键说明 |
|------|------|----------|
| Report | 报告 | 关联平台申请、样本、患者和检验项目 |
| StructuredResult | 结构化结果 | 项目明细、结果值、单位、参考范围、异常标识等 |
| ReportFile | 报告版式文件 | LIS/报告系统生成，平台接收引用或文件本身，非必填 |
| ReportStatus | 报告主状态 | 未发布、已发布、已撤回、已更正 |
| ReportFlag | 报告附加标记 | 版式文件缺失、HIS 回传待确认、回传失败、已生成新版等 |
| ReportVersion | 报告版本 | 保存当前有效版本和历史版本 |
| ReportAccessLog | 报告访问日志 | 查询、查看、下载、打印、历史版本访问审计 |
| ReportQueryCondition | 查询条件 | 患者信息、申请号、样本条码、报告编号等 |

报告最低展示字段：

- 患者姓名。
- 性别。
- 年龄或出生日期。
- 平台申请单号。
- HIS 申请号。
- 样本条码。
- 报告编号。
- 申请机构。
- 申请科室。
- 申请医生。
- 检验项目。
- 结构化结果明细。
- 报告发布时间。
- 报告主状态。
- 报告附加标记。
- 报告版本。

#### 查询入口

平台报告查询支持两类入口：

- 患者维度查询：按患者姓名、证件号、手机号、患者 ID 等信息查询历史报告。
- 精确标识查询：按平台申请单号、HIS 申请号、样本条码、报告编号查询报告。

所有查询都需要权限校验和访问审计。

#### 查询范围

- 检验中心授权人员可按授权范围查询平台报告。
- 接入机构授权人员可按患者信息查询全域报告。
- 接入机构授权人员也可按申请、样本、报告标识精确查询报告。
- 当前方案查询效率高，但隐私和越权风险较高，患者授权、诊疗关系限制、查询理由强制填写等细化规则放入内部讨论确定。

#### 报告内容展示

- 平台展示结构化报告结果。
- 平台支持展示正式报告版式文件。
- 报告版式文件由 LIS/报告系统生成。
- 平台不负责报告模板、签章或 PDF 生成。
- 版式文件为非必填。
- 报告审核通过后，平台即可展示结构化报告结果。
- 如果 LIS/报告系统提供版式文件，平台同时展示版式文件。
- 版式文件缺失不阻塞报告查询。

#### 报告状态与版本

报告主状态包括：

- 未发布。
- 已发布。
- 已撤回。
- 已更正。

报告附加标记包括：

- 版式文件缺失。
- HIS 回传待确认。
- 回传失败。
- 已生成新版。

报告撤回、更正或重新发布后：

- 默认展示最新有效版本。
- 保留历史版本入口。
- 历史版本应展示版本号、发布时间、撤回/更正原因、操作记录和版本状态。
- 授权人员可查看历史版本。
- 普通查询结果应避免把旧版本与当前有效版本并列展示，防止误用。

#### 下载与打印

- 授权用户可在线查看报告。
- 授权用户可下载报告。
- 授权用户可打印报告。
- 下载和打印必须留痕。
- 下载/打印审计至少记录用户、机构、时间、报告编号、报告版本、渠道、操作类型和客户端信息。
- 是否加水印、是否限制批量下载，放入内部讨论确定。

#### 主流程

1. F-006 审核通过并发布报告。
2. 平台写入报告主状态为“已发布”。
3. 平台保存结构化报告结果。
4. 如 LIS/报告系统提供版式文件，平台保存文件引用或文件本身。
5. 授权用户进入平台报告查询。
6. 用户按患者信息或精确标识查询报告。
7. 平台执行权限校验并记录查询审计。
8. 平台返回报告列表或报告详情。
9. 用户查看结构化结果和版式文件。
10. 用户下载或打印报告时，平台记录下载/打印审计。
11. 报告撤回、更正或重新发布后，平台默认展示最新有效版本，并保留历史版本入口。

#### 异常处理

- 报告未发布：不展示为可用报告。
- 版式文件缺失：展示结构化结果，并标记“版式文件缺失”。
- 版式文件无法打开：提示文件不可用，保留结构化结果展示并记录异常。
- 患者信息匹配多名患者：返回可区分的患者列表或要求进一步筛选，避免直接混合展示。
- 查询条件过宽：要求补充查询条件或分页返回。
- 权限不足：拒绝查询并记录访问审计。
- 历史版本被访问：记录历史版本访问审计。
- 下载/打印失败：提示失败原因并记录操作日志。
- 报告已撤回：默认不作为有效报告展示，但可在历史版本或撤回记录中查看。

#### 性能与时效要求

- F-006 审核通过后，结构化报告结果应实时或准实时可查。
- 版式文件缺失不影响结构化报告查询。
- 报告列表应支持分页和条件筛选。
- 历史报告查询应支持按时间范围筛选。
- 下载和打印不应阻塞报告查询主流程。
- 具体查询响应时间、报告查询 QPS、文件访问性能、历史报告保留周期暂存至 `系统实现相关.md`。

#### 通知与审计要求

- 平台记录报告查询、查看、下载、打印、历史版本访问操作。
- 审计至少包含用户、机构、角色、查询条件摘要、报告编号、报告版本、患者标识摘要、操作类型、操作时间、客户端信息、Trace ID/Request ID。
- 跨机构全域查询必须记录访问审计。
- 下载和打印必须记录审计。
- 患者敏感信息在日志中应脱敏或仅记录摘要。
- 患者通知、短信、微信推送、站内信等归入 F-020 或后续患者端能力。

#### 与 HIS/LIS/仪器/患者端的边界

| 事项 | 平台 F-008 | 外部系统/后续模块 |
|------|------------|------------------|
| 平台内报告查询 | 负责 | 授权用户使用 |
| 结构化报告结果 | 展示和查询 | LIS/报告系统或 F-006 提供 |
| 报告版式文件 | 接收并展示，非必填 | LIS/报告系统生成 |
| 报告模板/签章/PDF 生成 | 不负责 | LIS/报告系统负责 |
| HIS 报告回传 | 本轮不定稿 | 是否启用放内部讨论 |
| 患者端查询 | P0 不包含 | 后续患者端或运营主体讨论 |
| 患者通知 | 不负责 | F-020 或后续患者端能力 |
| 跨机构全域查询授权细则 | 本轮按授权人员可查并强审计 | 更细规则放内部讨论/F-018/F-016 |

#### 遗留问题与内部待确认

以下事项已登记在 `风险_待内部讨论确定.md`，不作为 F-008 无条件定稿结论：

- 是否启用 HIS 报告回传。
- 患者端查询、小程序、H5、短信/微信通知是否进入后续版本。
- 接入机构授权人员全域查询患者报告的隐私和越权风险。
- 是否要求按诊疗关系、本次就诊或患者授权限制跨机构查询。
- 下载/打印是否加水印、是否限制批量下载。
- 版式文件缺失时是否影响外部交付或归档。

以下事项已登记在 `系统实现相关.md`，待需求边界确定后讨论：

- F-008 报告查询接口、报告详情接口、历史版本接口、下载/打印接口形态。
- F-008 结构化结果与版式文件存储方式。
- F-008 查询性能、文件访问性能、历史报告保留周期。
- F-008 报告访问审计、跨机构查询审计和敏感信息脱敏实现规则。

#### 本轮确认结论

F-008 的平台职责收敛为“平台内报告查询、展示、下载、打印和版本追溯”。报告审核通过后，平台即可展示结构化报告结果；报告版式文件由 LIS/报告系统生成并作为可选数据接入，缺失时不阻塞平台查询。平台支持检验中心和接入机构授权人员查询报告，接入机构授权人员可按患者信息查询全域报告；该能力必须完整留痕，患者授权和诊疗关系限制放入内部讨论。报告默认展示最新有效版本，并保留历史版本入口。HIS 回传、患者端查询、患者通知和患者端运营主体不作为本轮无条件定稿能力。

---

### F-010 危急值闭环管理

| 属性 | 内容 |
|------|------|
| 功能编号 | F-010 |
| 功能名称 | 危急值闭环管理 |
| 所属模块 | 质量与安全管控 |
| 业务价值 | 对危及患者安全的检验结果建立从识别、通知、确认、处置记录、超时升级到统计分析的闭环，保障危急值通报及时性和可追溯性。 |
| 核心用户 | 检验中心人员、申请医生、申请科室授权人员、质量管理人员 |
| 平台入口/接口 | 危急值事件列表、危急值详情、通知任务、确认回执、处理记录、超时升级、事件关闭、危急值基础统计 |
| 当前状态 | 已完成讨论 |

#### 功能定位

F-010 采用“平台闭环管理 + 通知能力调用”模式。平台负责危急值事件、通知任务、确认回执、处理记录、超时升级、事件状态和基础统计；短信、语音、站内信、HIS 通知通道等由 F-020 消息通知中心或外部系统承载。

F-010 不自建完整多渠道通知系统，不替代临床 EMR，不记录完整临床病程。它的核心是保证危急值事件能被及时生成、通知、确认、记录处理摘要、超时升级并可统计追溯。

#### 平台负责

- 接收 F-007/F-006 识别出的疑似危急值。
- 生成危急值事件。
- 生成危急值通知任务，并调用 F-020 或外部系统通知能力。
- 维护危急值事件主状态和附加标记。
- 支持申请医生或申请科室授权人员确认危急值。
- 记录确认人、机构、科室、确认时间、确认来源和关联通知记录。
- 记录处理措施摘要和备注。
- 支持按机构、危急值等级、项目类型配置确认时限、闭环时限、升级对象和升级次数。
- 支持超时升级。
- 支持人工确认、修正或关闭疑似危急值事件。
- 记录全过程审计。
- 提供危急值基础统计。

#### 平台不负责

- 不负责短信、语音、站内信、HIS 弹窗等具体通知通道实现，归入 F-020 或外部系统。
- 不负责 HIS 工作站原生提醒界面。
- 不负责完整临床处置过程记录。
- 不替代 EMR 或病历系统。
- 不在 F-010 中定义具体权限角色，归入 F-018。
- 不处理复杂值班表、动态联系人、节假日排班策略，放入内部讨论。
- 不允许机构自行维护危急值项目、阈值和等级。
- 不提供复杂质量仪表盘，复杂统计分析放入后续统计分析能力。

#### 触发条件

F-007 自动审核规则或 F-006 审核流程识别到疑似危急值时，平台立即生成危急值事件并启动通知流程。后续人工审核可对事件进行确认、修正或关闭。

#### 前置条件

- 平台已存在申请、样本、患者和检验结果。
- 结果已能关联到平台标准项目。
- 危急值项目、阈值、等级和升级策略已由检验中心/平台统一配置。
- F-007/F-006 已输出疑似危急值建议或标记。
- F-020 或外部通知能力已具备可调用通道。
- 申请医生、申请科室或兜底通知对象具备可识别的组织人员信息。

#### 后置结果

疑似危急值触发后：

- 平台生成危急值事件。
- 平台生成通知任务。
- 平台调用 F-020 或外部通知通道。
- 危急值事件进入“疑似危急值”或“已通知”状态。
- 平台记录触发来源、触发时间和通知任务。

确认后：

- 平台记录确认人、机构、科室、确认时间、确认来源和关联通知记录。
- 危急值事件进入“已确认”状态。
- 平台要求记录处理措施摘要或备注。

闭环后：

- 平台记录处理措施摘要、闭环人、闭环时间和备注。
- 危急值事件进入“已闭环”状态。

超时未确认或未闭环时：

- 平台按配置触发升级。
- 事件附加“超时”或“已升级”标记。
- 平台记录升级记录。

#### 核心字段/数据对象

| 对象 | 含义 | 关键说明 |
|------|------|----------|
| CriticalValueEvent | 危急值事件 | 关联患者、申请、样本、项目、结果和触发来源 |
| CriticalValueRule | 危急值标准 | 项目、阈值、等级、适用范围，由检验中心统一维护 |
| CriticalNotificationTask | 危急值通知任务 | 调用 F-020 或外部通知能力 |
| CriticalAcknowledgement | 危急值确认记录 | 记录确认人、机构、科室、时间和来源 |
| CriticalHandlingRecord | 处理措施摘要 | 字典 + 备注，不替代 EMR |
| CriticalEscalationPolicy | 升级策略 | 确认时限、闭环时限、升级对象和升级次数 |
| CriticalEscalationLog | 升级记录 | 记录超时和升级过程 |
| CriticalStatus | 危急值主状态 | 疑似危急值、已通知、已确认、已闭环、已关闭 |
| CriticalFlag | 附加标记 | 已升级、通知失败、超时、人工修正、误报关闭等 |
| AuditLog | 审计日志 | 记录触发、通知、确认、处理、升级、关闭全过程 |

危急值事件最低字段：

- 危急值事件编号。
- 平台申请单号。
- 样本条码。
- 报告编号或结果编号。
- 患者基础信息摘要。
- 申请机构。
- 申请科室。
- 申请医生。
- 平台标准项目。
- 结果值。
- 单位。
- 危急值阈值。
- 危急值等级。
- 触发来源：F-007 或 F-006。
- 触发时间。
- 主状态。
- 附加标记。

#### 触发与识别

- F-007/F-006 识别为疑似危急值时，平台先生成危急值事件并启动通知。
- 人工审核可对疑似危急值事件进行确认、修正或关闭。
- 疑似危急值事件被关闭时，必须登记原因。
- 危急值识别阈值由检验中心/平台统一维护，机构不自行维护。
- 危急值触发点、人工确认点和报告发布点之间的精确关系放入内部讨论。

#### 通知对象

F-010 通知对象采用“申请医生 + 申请科室兜底”策略：

- 优先通知申请医生。
- 同时或超时后通知申请科室、护士站或值班人员。
- 申请医生或申请科室授权人员可确认。
- 联系人来源、科室值班表、住院患者转科后的通知对象、节假日策略放入内部讨论确定。

#### 确认与处理记录

- 危急值可由申请医生确认。
- 危急值也可由申请科室授权人员确认。
- 平台必须记录确认人、确认机构、确认科室、确认时间、确认来源和关联通知记录。
- 闭环至少记录确认动作和处理措施摘要。
- 处理措施采用字典 + 备注方式。

P0 处理措施字典包括：

- 已通知患者。
- 已复查。
- 已转上级。
- 已处置。
- 与临床不符待复核。
- 其他。

平台不记录完整临床病程，不替代 EMR。

#### 超时升级

- F-010 支持可配置升级策略。
- 可按机构、危急值等级、项目类型配置确认时限。
- 可按机构、危急值等级、项目类型配置闭环时限。
- 可配置升级对象。
- 可配置升级次数。
- 复杂值班表、动态联系人、节假日策略放入内部讨论。

#### 事件状态

危急值主状态包括：

- 疑似危急值。
- 已通知。
- 已确认。
- 已闭环。
- 已关闭。

附加标记包括：

- 已升级。
- 通知失败。
- 超时。
- 人工修正。
- 误报关闭。

主状态用于表达闭环进度，附加标记用于表达异常、升级和人工修正情况。

#### 配置维护

- 危急值项目由检验中心/平台统一维护。
- 危急值阈值由检验中心/平台统一维护。
- 危急值等级由检验中心/平台统一维护。
- 确认时限、闭环时限和升级策略由检验中心/平台统一维护。
- 机构不自行维护危急值标准。
- 具体维护权限归入 F-018 用户权限与审计管理。

#### 统计指标

F-010 P0 提供基础统计：

- 危急值事件数。
- 通知及时率。
- 确认及时率。
- 闭环率。
- 超时未确认数量。
- 升级次数。
- 按机构、科室、项目、危急值等级、时间范围筛选统计。

复杂质量仪表盘、跨质量指标分析和运营大屏放入后续统计分析能力。

#### 主流程

1. F-007/F-006 识别到疑似危急值。
2. F-010 生成危急值事件。
3. 平台根据危急值配置确定等级、确认时限和闭环时限。
4. 平台生成通知任务。
5. 平台调用 F-020 或外部通知能力通知申请医生和兜底对象。
6. 平台记录通知任务和发送结果。
7. 申请医生或申请科室授权人员确认危急值。
8. 平台记录确认信息。
9. 确认人填写或选择处理措施摘要和备注。
10. 平台将事件更新为已闭环。
11. 如超时未确认或未闭环，平台按策略升级并记录升级日志。
12. 质量管理人员可查询事件列表、详情和基础统计。

#### 异常处理

- 疑似危急值无法关联申请或样本：生成异常待处理记录，不进入自动闭环。
- 通知对象缺失：记录异常并使用兜底对象或进入待人工处理。
- 通知发送失败：标记通知失败，并按策略重试或升级。
- 超时未确认：标记超时，并按策略升级。
- 超时未闭环：标记超时，并按策略升级。
- 确认人权限不足：拒绝确认并记录访问审计。
- 误报关闭：必须登记关闭原因、关闭人和关闭时间。
- 人工修正危急值等级或状态：必须登记原因并留痕。
- 重复触发同一结果危急值：按事件去重规则处理，具体规则放入系统实现讨论。

#### 性能与时效要求

- 疑似危急值识别后，应实时或准实时生成危急值事件。
- 危急值事件生成后，应及时生成通知任务。
- 通知发送不应阻塞危急值事件写入。
- 危急值列表应支持按机构、科室、项目、等级、状态、超时标记、时间范围筛选。
- 基础统计应支持按时间范围和机构维度查询。
- 具体事件生成时效、通知任务触发时效、升级调度频率和统计性能暂存至 `系统实现相关.md`。

#### 通知与审计要求

- F-010 负责生成通知任务和跟踪闭环状态。
- F-020 或外部系统负责具体通知通道发送。
- 平台记录危急值触发、通知任务、发送结果、确认、处理摘要、超时升级、关闭和人工修正全过程。
- 审计至少包含危急值事件编号、平台申请单号、样本条码、报告编号、患者标识摘要、项目、结果值、阈值、等级、通知对象、通知通道、确认人、处理摘要、前后状态、操作人、操作时间、Trace ID/Request ID。
- 患者敏感信息在日志中应脱敏或仅记录摘要。

#### 与 HIS/LIS/仪器/患者端的边界

| 事项 | 平台 F-010 | 外部系统/后续模块 |
|------|------------|------------------|
| 危急值事件 | 负责生成和闭环管理 | F-007/F-006 提供疑似危急值触发 |
| 危急值标准 | 检验中心/平台统一维护 | 机构不自行维护 |
| 通知任务 | F-010 生成并跟踪 | F-020 或外部系统执行发送 |
| HIS 弹窗/语音提醒 | 不负责界面实现 | HIS/F-020/外部系统承载 |
| 确认回执 | 平台记录 | HIS 或平台页面可回传/提交 |
| 临床处置详情 | 不负责完整病程 | EMR/临床系统负责 |
| 权限角色 | 不展开 | F-018 统一定义 |
| 患者通知 | 不作为 F-010 P0 | 后续患者端/F-020 讨论 |

#### 遗留问题与内部待确认

以下事项已登记在 `风险_待内部讨论确定.md`，不作为 F-010 无条件定稿结论：

- 联系人来源、科室值班表、住院患者转科后的通知对象。
- 节假日、夜间、值班人员等动态联系人策略。
- 危急值触发点、人工确认点和报告发布点的精确关系。
- 危急值项目、阈值和等级的维护制度与医学责任归属。
- 是否要求确认前或闭环前必须同步 HIS。

以下事项已登记在 `系统实现相关.md`，待需求边界确定后讨论：

- F-010 危急值事件、通知任务、确认回执、处理记录、升级记录接口形态。
- F-010 危急值事件状态机、去重规则、超时升级调度机制。
- F-010 与 F-020 消息通知中心、HIS 回执、F-005 状态追踪的衔接。
- F-010 基础统计指标口径和查询性能。

#### 本轮确认结论

F-010 的平台职责收敛为“危急值事件闭环管理”。平台在 F-007/F-006 识别疑似危急值后先生成事件并启动通知，后续人工审核可确认、修正或关闭。通知对象采用申请医生 + 申请科室兜底；确认人可以是申请医生或申请科室授权人员。闭环至少记录确认动作和处理措施摘要，不替代 EMR。升级策略支持按机构、等级、项目类型配置。危急值标准由检验中心/平台统一维护，机构不自行维护。F-010 提供基础统计，复杂质量仪表盘放后续统计分析能力；具体通知通道由 F-020 或外部系统承载。

---

### F-012 室内质控（IQC）管理

| 属性 | 内容 |
|------|------|
| 功能编号 | F-012 |
| 功能名称 | 室内质控（IQC）管理 |
| 所属模块 | 质量与安全管控 |
| 当前状态 | 整体转入内部讨论确定 |

#### 功能主要作用

F-012 的主要作用是对检验中心日常室内质控进行统一管理，用于证明检测系统在控、及时发现检测系统漂移或失控风险，并为后续 F-013 失控处理与追踪提供触发依据。

该功能通常覆盖质控品信息、质控批号、靶值、标准差、质控检测结果、质控图展示、质控规则判断和质控状态记录。通过持续记录和分析室内质控数据，平台可帮助质控管理员判断某一项目、仪器、质控品水平在指定时间范围内是否稳定，是否存在警告或失控风险。

#### 暂不展开原因

室内质控涉及 LIS/仪器数据来源、质控品批号管理、靶值和标准差维护、L-J 图展示、Westgard 规则组合、失控触发口径、质控报告导出等较多专业细节。不同检验中心、不同仪器和不同 LIS 的质控数据结构差异较大，若在当前阶段直接定稿，容易导致需求边界过重或与实际检验科质控制度不一致。

因此，F-012 本轮不继续细化 P0 范围，整体登记到 `风险_待内部讨论确定.md`，待检验中心、质控负责人、LIS/仪器接口方共同确认后再定稿。

#### 本轮确认结论

F-012 暂不作为当前逐项 PRD 讨论中的详细定稿功能。当前仅确认其业务定位：用于室内质控数据管理、质控状态判断、质控图展示和失控风险发现，并为 F-013 失控处理提供触发依据。质控数据来源、质控对象粒度、L-J 图、Westgard 规则、质控品批号、靶值/标准差维护和质控报告等内容全部转入内部讨论确定。

---

### F-013 失控处理与追踪

| 属性 | 内容 |
|------|------|
| 功能编号 | F-013 |
| 功能名称 | 失控处理与追踪 |
| 所属模块 | 质量与安全管控 |
| 当前状态 | 整体转入内部讨论确定 |

#### 功能主要作用

F-013 的主要作用是承接 F-012 或人工登记产生的质控失控事件，对失控事件进行处理闭环管理，确保每一次失控都有原因分析、纠正措施、效果验证和关闭记录。

该功能通常覆盖失控事件登记、失控原因分类、责任人分派、纠正措施记录、复测或再验证结果记录、处理过程追踪、关闭确认、处理记录查询和统计。其核心目标不是判断“是否失控”，而是在失控已经发生或已被登记后，保证处理过程可追踪、可复盘、可审计，降低同类问题重复发生风险。

#### 暂不展开原因

F-013 强依赖 F-012 的质控数据来源、规则判断、失控触发口径和质控对象粒度。当前 F-012 已整体转入内部讨论，因此 F-013 暂不继续细化失控流程、原因分类、纠正措施模板和效果验证标准，避免在上游质控机制未定稿前固化处理流程。

#### 本轮确认结论

F-013 暂不作为当前逐项 PRD 讨论中的详细定稿功能。当前仅确认其业务定位：用于对已确认或已登记的质控失控事件进行原因分析、纠正措施记录、效果验证、关闭确认和追踪审计。失控触发来源、处理流程、原因分类、纠正措施字典、效果验证标准、关闭条件和统计口径全部转入内部讨论确定。

---

### F-016 患者主索引（EMPI）

| 属性 | 内容 |
|------|------|
| 功能编号 | F-016 |
| 功能名称 | 患者主索引（EMPI） |
| 所属模块 | 平台基础能力 |
| 业务价值 | 为跨机构检验申请、报告查询和结果互认提供患者身份统一能力，在保留各机构来源患者信息的基础上生成全局患者 ID，降低重复患者、误合并和历史报告误查风险。 |
| 核心用户 | 平台管理员、EMPI 管理人员、检验中心管理人员、平台内部业务模块 |
| 平台入口/接口 | 患者索引查询、患者来源记录查询、疑似重复队列、人工合并、人工排除、暂不处理、人工拆分、合并/拆分审计、受控患者索引查询接口 |
| 当前状态 | 已完成讨论 |

#### 功能定位

F-016 提供基础 EMPI 能力，不建设完整主数据治理平台。平台为患者生成全局患者 ID，保留各机构来源患者 ID 和来源患者信息，支持确定匹配自动关联、疑似重复队列、人工合并、人工排除、暂不处理、人工拆分和完整审计。

F-016 承接 F-001 中“患者不要求预先存在平台”的边界。创建申请时，平台可以先创建或关联患者记录，不因疑似重复阻断申请创建；后续由 EMPI 队列处理疑似重复。F-016 也支撑 F-008 中按患者维度汇总历史报告。

#### 平台负责

- 生成平台全局患者 ID。
- 保留各机构来源患者 ID。
- 保留各机构来源患者原始字段。
- 根据患者身份信息执行匹配判断。
- 对确定匹配自动关联已有全局患者。
- 对疑似重复患者进入人工处理队列。
- 对未匹配患者创建新的全局患者 ID。
- 支持疑似重复队列人工合并。
- 支持疑似重复队列人工排除。
- 支持疑似重复队列暂不处理。
- 支持误合并后的人工拆分。
- 生成展示用患者主档。
- 保留申请/报告原始来源患者 ID。
- 通过全局患者 ID 汇总历史申请和报告。
- 提供受控患者索引查询接口。
- 记录患者匹配、合并、排除、拆分和查询审计。

#### 平台不负责

- 不建设完整区域主数据治理平台。
- 不做全量历史患者数据清洗和治理定稿。
- 不在 P0 中定稿复杂概率匹配模型。
- 不在 P0 中定稿匹配字段权重、相似度算法和阈值。
- 不覆盖来源机构原始患者字段。
- 不把患者授权、隐私授权流程放在 F-016 中定稿。
- 不在本轮定稿是否向 HIS 主动同步全局患者主档。
- 不替代 HIS 的本地患者档案管理。

#### 触发条件

F-001 创建平台申请时，HIS 传入患者基础信息和来源患者标识，平台调用 F-016 进行患者匹配、创建或关联。平台管理员或 EMPI 管理人员处理疑似重复队列时，也会触发 F-016 的合并、排除、暂不处理或拆分能力。

#### 前置条件

- HIS 或外部系统传入患者姓名、性别、年龄或出生日期等最低身份字段。
- 如有身份证号、医保号、手机号、来源患者 ID 等字段，应随申请或患者信息传入。
- 平台已维护来源机构编码。
- 平台已具备患者索引存储能力。
- 进行人工合并、排除、拆分时，操作人员具备相应权限。

#### 后置结果

确定匹配时：

- 平台关联已有全局患者 ID。
- 平台新增或更新来源患者记录与全局患者 ID 的关系。
- 平台保留来源患者原始字段。

疑似重复时：

- 平台不阻断申请创建。
- 平台先创建或关联临时患者记录。
- 平台生成疑似重复队列任务。
- 后续由人工处理。

未匹配时：

- 平台创建新的全局患者 ID。
- 平台保存来源患者记录。

人工合并后：

- 多个来源患者记录归并到同一全局患者 ID。
- 历史申请和报告可通过全局患者 ID 汇总查询。
- 平台记录合并审计。

人工拆分后：

- 被误合并的来源患者记录从原全局患者 ID 中拆出。
- 平台生成新的全局患者 ID 或关联到其他正确全局患者 ID。
- 平台记录拆分审计和拆分前后关系。

#### 核心字段/数据对象

| 对象 | 含义 | 关键说明 |
|------|------|----------|
| GlobalPatient | 全局患者 | 平台生成的全局患者 ID |
| SourcePatient | 来源患者记录 | 某机构或某 HIS 中的患者记录 |
| PatientIdentity | 患者身份标识 | 身份证号、医保号、手机号等 |
| PatientProfile | 展示用患者主档 | 平台展示用，不覆盖来源原始信息 |
| MatchCandidate | 匹配候选 | 匹配过程中发现的候选患者 |
| DuplicateQueueItem | 疑似重复队列任务 | 待人工确认的疑似重复患者 |
| MergeRecord | 合并记录 | 记录合并前后关系、原因、操作人 |
| SplitRecord | 拆分记录 | 记录拆分前后关系、原因、操作人 |
| ExclusionRecord | 排除记录 | 记录确认不是同一人的判断 |
| AuditLog | 审计日志 | 匹配、查询、合并、拆分、排除全过程留痕 |

来源患者最低字段：

- 来源机构编码。
- 来源系统编码。
- 来源患者 ID。
- 患者姓名。
- 性别。
- 年龄或出生日期。
- 证件类型和证件号码，如有。
- 医保号，如有。
- 手机号，如有。
- 来源记录创建或更新时间。

#### 匹配结果

F-016 P0 明确三类匹配结果：

- 确定匹配：自动关联已有全局患者。
- 疑似重复：进入人工队列，不阻断申请创建。
- 未匹配：创建新的全局患者 ID。

#### 自动关联规则

自动关联不能只看单一字段。P0 规则为：

- 身份证号、医保号、平台全局患者 ID 等强标识一致。
- 姓名、性别、出生日期等基本信息不存在明显冲突。
- 满足上述条件时，可自动关联已有全局患者。
- 强标识一致但姓名、性别、出生日期等存在明显冲突时，进入疑似重复队列。

具体匹配权重、相似度算法、阈值、字段标准化和清洗规则放入 `风险_待内部讨论确定.md`。

#### 疑似重复队列

疑似重复队列 P0 支持以下操作：

- 合并：确认候选记录属于同一人，合并到同一全局患者 ID。
- 排除：确认候选记录不是同一人，记录排除关系，后续避免重复提示。
- 暂不处理：暂时保留队列任务，等待更多信息。

人工处理时应展示来源机构、来源患者 ID、姓名、性别、出生日期、证件号摘要、手机号摘要、历史申请/报告摘要、匹配原因等信息。

#### 人工拆分

- F-016 P0 支持人工拆分。
- 授权人员可将误合并的患者来源记录从原全局患者 ID 中拆出。
- 拆出后可形成新的全局患者 ID。
- 拆分必须登记原因。
- 平台保留拆分前后关系、操作人、操作时间和影响范围。
- 拆分后历史申请/报告仍保留原始来源患者 ID，可根据来源关系恢复归属。

#### 申请与报告归属

- 每条申请保留原始来源患者 ID 和来源机构患者 ID。
- 每份报告保留原始来源患者 ID 和来源机构患者 ID。
- EMPI 合并的是来源患者记录与全局患者 ID 的关系。
- 历史报告查询时，通过全局患者 ID 汇总展示。
- 拆分时可根据来源患者记录恢复申请和报告归属。
- 不直接把历史申请/报告物理改写为目标全局患者 ID。

#### 患者主档

- 平台保留各机构来源患者信息。
- 平台不覆盖来源原始字段。
- 平台为全局患者生成展示用主档。
- 展示用主档用于平台查询、报告汇总和人工核对。
- 展示用主档字段来源规则或人工维护规则放入内部讨论。

#### 对外查询能力

F-016 提供受控患者索引查询接口：

- 按来源机构患者 ID 查询患者索引关系。
- 按证件号查询患者索引关系。
- 按平台全局患者 ID 查询患者索引关系。

是否向 HIS 主动同步全局患者主档、同步哪些字段、是否提供全量/增量同步，放入内部讨论确定。

#### 主流程

1. HIS 调用 F-001 创建申请并传入患者基础信息。
2. 平台调用 F-016 执行患者匹配。
3. 若确定匹配，平台关联已有全局患者 ID。
4. 若未匹配，平台创建新的全局患者 ID。
5. 若疑似重复，平台不阻断申请创建，先创建或关联临时患者记录，并生成疑似重复队列任务。
6. EMPI 管理人员查看疑似重复队列。
7. 管理人员选择合并、排除或暂不处理。
8. 如发现误合并，管理人员发起人工拆分。
9. 平台记录合并、排除、拆分和查询审计。
10. F-008 查询历史报告时，通过全局患者 ID 汇总来源患者记录下的报告。

#### 异常处理

- 患者最低身份字段缺失：由 F-001 拒绝申请创建，F-016 不创建索引。
- 强标识一致但基础信息冲突：进入疑似重复队列。
- 多个候选匹配且无法确定：进入疑似重复队列。
- 来源患者 ID 重复但基础信息冲突：进入疑似重复队列或异常待处理。
- 人工合并权限不足：拒绝操作并记录审计。
- 人工拆分权限不足：拒绝操作并记录审计。
- 合并后发现误合并：通过人工拆分处理。
- 查询命中多名患者：返回候选列表或要求进一步筛选，避免直接混合展示报告。

#### 性能与时效要求

- F-001 创建申请时的患者匹配不应明显阻塞申请创建。
- 疑似重复不阻断申请创建。
- 疑似重复队列应支持按机构、时间、匹配原因、处理状态筛选。
- 合并、排除、拆分后，应实时或准实时影响 F-008 历史报告汇总查询。
- 具体匹配响应时间、疑似队列查询性能、合并/拆分影响范围计算暂存至 `系统实现相关.md`。

#### 通知与审计要求

- 患者匹配、自动关联、创建新全局患者、疑似重复入队、合并、排除、暂不处理、拆分、查询均需留痕。
- 审计至少包含来源机构、来源系统、来源患者 ID、全局患者 ID、匹配结果、匹配原因、人工处理动作、处理人、处理时间、变更前后关系、Trace ID/Request ID。
- 患者敏感信息在日志中应脱敏或仅记录摘要。
- 疑似重复队列是否通知管理人员，归入 F-020 或内部运营流程讨论。

#### 与 HIS/LIS/仪器/患者端的边界

| 事项 | 平台 F-016 | 外部系统/后续模块 |
|------|------------|------------------|
| 全局患者 ID | 平台生成和维护 | 外部系统可受控查询 |
| 来源患者 ID | 平台保留 | HIS/外部系统提供 |
| 患者原始字段 | 平台保留，不覆盖 | 来源系统负责本地患者档案 |
| 患者匹配 | 平台执行基础匹配 | 具体算法阈值待内部确认 |
| 申请创建 | 不因疑似重复阻断 | F-001 继续创建申请 |
| 历史报告汇总 | 提供患者索引基础 | F-008 展示报告 |
| 权限角色 | 不展开 | F-018 统一定义 |
| 患者授权 | 不在本轮定稿 | 后续隐私授权/患者端能力讨论 |
| 主动同步 HIS | 不在本轮定稿 | 是否同步放内部讨论 |

#### 遗留问题与内部待确认

以下事项已登记在 `风险_待内部讨论确定.md`，不作为 F-016 无条件定稿结论：

- 匹配权重、相似度算法、阈值和字段清洗规则。
- 展示用患者主档字段来源规则和人工维护规则。
- 是否向 HIS 主动同步全局患者 ID 或患者主档。
- 患者授权、诊疗关系限制和跨机构隐私合规规则。
- 历史数据批量清洗和批量重算是否纳入后续范围。

以下事项已登记在 `系统实现相关.md`，待需求边界确定后讨论：

- F-016 患者匹配接口、索引查询接口、疑似重复队列接口形态。
- F-016 全局患者、来源患者、合并关系、排除关系和拆分关系数据模型。
- F-016 匹配性能、队列查询性能、合并/拆分后索引刷新机制。
- F-016 审计日志、患者敏感信息脱敏和访问控制实现规则。

#### 本轮确认结论

F-016 的平台职责收敛为“基础 EMPI 能力”。平台生成全局患者 ID，保留各机构来源患者 ID 和原始字段；确定匹配自动关联，疑似重复不阻断申请创建并进入人工队列，未匹配创建新患者。疑似重复队列支持合并、排除、暂不处理，P0 支持人工拆分。申请和报告保留原始来源患者 ID，通过全局患者 ID 汇总展示历史报告，拆分时可根据来源关系恢复。平台保留来源字段并生成展示用主档；匹配权重、阈值、主档字段来源规则和 HIS 同步放入内部讨论确定。

---

### F-017 检验项目标准化管理

| 属性 | 内容 |
|------|------|
| 功能编号 | F-017 |
| 功能名称 | 检验项目标准化管理 |
| 所属模块 | 平台基础能力 |
| 业务价值 | 为检验申请、结果回传、报告展示、自动审核、危急值和后续互认提供统一项目语义基础，确保平台内部使用同一套区域/检验中心标准项目库。 |
| 核心用户 | 检验中心管理员、平台管理员、HIS/机构接口对接人员 |
| 平台入口/接口 | 标准项目维护、组合-明细关系维护、项目版本管理、项目启停用、标准项目查询/下载接口 |
| 当前状态 | 已完成讨论 |

#### 功能定位

F-017 P0 只维护区域/检验中心标准项目库，不维护 HIS 本地项目映射。HIS/机构自行维护本地项目到平台标准项目的映射，调用 F-001 创建申请时必须传平台标准项目编码。

平台负责提供标准项目查询和下载能力，作为 HIS/机构维护本地映射的数据源。平台标准项目编码是平台业务主键；LOINC、国家标准编码、地方标准编码等外部标准编码作为可选字段逐步完善。

#### 平台负责

- 维护区域/检验中心标准项目库。
- 维护平台标准项目编码和项目名称。
- 维护项目基础字段和业务属性。
- 支持组合项目/套餐项目和明细项目。
- 维护组合项目包含哪些明细项目。
- 支持组合项目作为申请项目传入平台。
- 要求结果回传落到明细项目编码。
- 支持项目主数据版本管理。
- 支持项目启用、停用。
- 支持标准项目查询/下载接口。
- 为 F-001、F-006、F-007、F-008、F-010、F-022 等模块提供统一项目基础数据。

#### 平台不负责

- 不维护 HIS 本地项目到平台标准项目的映射。
- 不根据 HIS 本地项目编码自行查找映射关系。
- 不承担 HIS 本地项目维护责任。
- 不维护收费价格、医保属性、退费规则。
- 不负责医生开单目录管理。
- 不在 F-017 中维护容器、采集量、共管/排斥等采样规则，归入 F-022。
- 不在 P0 中建设完整检验知识库。
- 不要求上线时完成全部 LOINC 或国家标准编码映射。

#### 触发条件

平台上线前或项目变更时，检验中心/平台管理员维护标准项目库。HIS/机构需要维护本地映射时，可通过 F-017 查询或下载当前启用的标准项目清单。F-001 创建申请、F-006/F-007 审核、F-008 报告查询、F-010 危急值管理等流程引用平台标准项目。

#### 前置条件

- 检验中心已确认区域标准项目目录。
- 平台已定义标准项目编码规则。
- 需要接入的 HIS/机构已知本地项目映射由自身维护。
- 项目维护人员具备对应权限。

#### 后置结果

项目启用后：

- F-001 可接收该平台标准项目编码。
- HIS/机构可查询或下载该标准项目用于本地映射。
- 后续模块可按该项目建立审核、报告、危急值、采样规则等关联配置。

项目停用后：

- F-001 不允许新申请继续使用该项目。
- 历史申请、报告、统计和审计仍可按当时项目版本查询。
- 停用不删除历史数据。

项目关键字段变更后：

- 生成新的项目版本。
- 新申请使用当前启用版本。
- 历史申请/报告保留当时项目版本和快照。

#### 核心字段/数据对象

| 对象 | 含义 | 关键说明 |
|------|------|----------|
| StandardItem | 标准项目主对象 | 稳定主 ID，表示项目概念 |
| StandardItemVersion | 标准项目版本 | 记录某一时期的项目名称、属性、组合关系等 |
| ItemComponentRelation | 组合-明细关系 | 组合项目包含哪些明细项目 |
| ExternalStandardCode | 外部标准编码 | LOINC、国家标准、地方标准等可选字段 |
| ItemStatus | 项目状态 | 启用、停用 |
| ItemSnapshot | 项目快照 | 申请/报告保存当时版本信息 |
| AuditLog | 审计日志 | 记录项目新增、修改、版本、启停用等操作 |

P0 标准项目字段包括：

- 项目主 ID。
- 项目版本号。
- 平台标准项目编码。
- 项目名称。
- 英文名或简称。
- 项目分类。
- 项目类型：组合项目/明细项目。
- 结果类型。
- 单位。
- 是否可申请。
- 是否可报告。
- 启停用状态。
- 外部标准编码：LOINC、国家标准编码、地方标准编码，可选。
- 版本生效时间。
- 版本停用时间。
- 备注。

#### 组合项目与明细项目

F-017 必须支持组合项目/套餐项目和明细项目层级关系。

- 组合项目/套餐项目：用于申请、开单、采样计划和对外展示。
- 明细项目/检验指标：用于结果回传、报告展示、参考范围、危急值、自动审核规则等。
- 一个组合项目可以包含多个明细项目。
- P0 只维护组合包含哪些明细项目。
- P0 不做复杂互斥、替代、条件展开。

示例：

- 组合项目：凝血四项。
- 明细项目：凝血酶原时间、活化部分凝血活酶时间、凝血酶时间、纤维蛋白原定量。

#### 申请与结果规则

- HIS 调用 F-001 时，可以传平台标准组合项目编码作为申请项目。
- 平台保留申请项目为组合项目。
- 平台根据组合-明细关系识别明细项目。
- 结果回传必须按明细项目编码返回。
- 平台通过明细项目编码支撑参考范围、危急值、自动审核和报告展示。
- 平台不要求 HIS 在申请时必须展开为明细项目。

#### 采样规则边界

- F-017 不维护容器、采集量、共管、排斥等采样规则。
- F-017 可维护项目基础分类、项目类型、是否可申请、是否可报告等基础属性。
- 样本类型、容器、采集量、合管/排斥规则归入 F-022。
- F-001 生成采样计划时应结合 F-017 标准项目和 F-022 采样规则。

#### 外部标准编码

- P0 以平台标准项目编码作为业务主键。
- LOINC 编码作为可选字段。
- 国家标准编码作为可选字段。
- 地方标准编码作为可选字段。
- 不要求上线时全部完成外部标准编码映射。
- 外部标准编码完善进度和责任归属放入内部讨论。

#### 项目版本管理

项目主数据支持版本管理：

- 项目主 ID 稳定不变。
- 关键业务字段变更生成新版本。
- 当前只有一个启用版本用于新申请。
- 历史申请/报告保留当时项目版本和快照。
- 历史数据不因后续项目变更被改写。

需要生成新版本的变更包括：

- 项目名称。
- 组合-明细关系。
- 结果类型。
- 单位。
- 是否可申请。
- 是否可报告。
- 外部标准编码。
- 其他影响业务解释的字段。

不一定需要生成新版本的变更包括：

- 备注。
- 拼音码。
- 排序号。
- 内部显示样式。
- 其他不影响业务含义的字段。

#### 启停用规则

- 项目启用后可被新申请使用。
- 项目停用后不允许新申请使用。
- 项目停用后历史申请仍可查询。
- 项目停用后历史报告仍可查询。
- 项目停用后历史统计和审计仍保留。
- 停用不删除历史数据。

#### 标准项目查询/下载

- 平台提供受控标准项目查询接口。
- 平台提供受控标准项目下载能力。
- HIS/机构可查询或下载当前启用标准项目清单。
- 查询/下载结果用于 HIS/机构维护本地项目映射。
- 平台不主动维护 HIS 本地映射。
- 是否主动推送项目变更给 HIS，放入内部讨论确定。

#### 主流程

1. 平台管理员或检验中心管理员维护标准项目。
2. 新增项目时，平台生成项目主 ID 和初始版本。
3. 管理员配置项目基础字段和业务属性。
4. 如为组合项目，管理员维护其包含的明细项目。
5. 管理员启用项目版本。
6. HIS/机构通过查询/下载接口获取当前启用标准项目清单。
7. HIS/机构在本地维护本地项目到平台标准项目的映射。
8. HIS 调用 F-001 创建申请时传平台标准项目编码。
9. 平台后续流程按标准项目和项目版本处理。
10. 项目关键字段变更时生成新版本，历史申请/报告保留旧版本快照。
11. 项目停用后不再允许新申请使用，但历史数据可查。

#### 异常处理

- HIS 传入不存在的平台标准项目编码：F-001 整单拒绝。
- HIS 传入已停用项目编码：F-001 拒绝使用该项目。
- 组合项目缺少明细关系：禁止启用或进入待配置状态。
- 明细项目被停用但仍被组合项目引用：禁止启用组合版本或提示配置冲突。
- 结果回传使用组合项目编码：拒绝或进入异常待处理，要求按明细项目编码回传。
- 项目关键字段变更未生成版本：禁止保存或提示需创建新版本。
- 历史版本被误删：不允许删除历史版本，仅允许停用。

#### 性能与时效要求

- 标准项目查询应支持按编码、名称、分类、项目类型、启停用状态筛选。
- 标准项目下载应支持当前启用版本清单。
- 项目变更后，新申请应使用当前启用版本。
- 历史申请/报告查询应能展示当时项目版本快照。
- 具体查询性能、下载格式、项目版本快照字段暂存至 `系统实现相关.md`。

#### 通知与审计要求

- 平台记录标准项目新增、修改、版本发布、启用、停用、组合关系调整、查询和下载操作。
- 审计至少包含项目主 ID、项目版本、平台标准项目编码、变更内容、操作人、操作时间、前后状态、Trace ID/Request ID。
- 项目变更是否通知 HIS/机构，放入内部讨论确定。
- 项目维护权限归 F-018 用户权限与审计管理统一讨论。

#### 与 HIS/LIS/仪器/患者端的边界

| 事项 | 平台 F-017 | 外部系统/后续模块 |
|------|------------|------------------|
| 区域标准项目库 | 平台维护 | HIS/机构使用 |
| 本地项目映射 | 不维护 | HIS/机构维护 |
| 平台标准项目查询 | 平台提供 | HIS/机构查询或下载 |
| 组合项目申请 | 支持 | HIS 可传组合项目编码 |
| 结果明细项目 | 要求使用明细项目编码 | LIS/结果系统按明细回传 |
| 采样规则 | 不负责 | F-022 负责 |
| 自动审核规则 | 不负责 | F-007 绑定明细项目 |
| 危急值规则 | 不负责 | F-010/F-007 绑定明细项目 |
| 报告展示 | 提供项目语义基础 | F-008 展示报告 |

#### 遗留问题与内部待确认

以下事项已登记在 `风险_待内部讨论确定.md`，不作为 F-017 无条件定稿结论：

- 外部标准编码维护范围、责任和完成节奏。
- 项目版本变更审批、发布和通知机制。
- 是否允许同一明细项目被多个组合项目复用。
- 是否主动推送项目变更给 HIS/机构。
- HIS 本地映射质量如何校验或验收。

以下事项已登记在 `系统实现相关.md`，待需求边界确定后讨论：

- F-017 标准项目、项目版本、组合-明细关系数据模型。
- F-017 标准项目查询/下载接口形态和下载格式。
- F-017 项目版本快照、历史引用和停用校验实现。
- F-017 项目变更审计和变更影响范围分析。

#### 本轮确认结论

F-017 的平台职责收敛为“维护区域/检验中心标准项目库”。平台不维护 HIS 本地项目映射，HIS/机构自行维护映射并在 F-001 调用时传平台标准项目编码。F-017 支持组合项目和明细项目，组合项目可用于申请，结果必须按明细项目编码回传。F-017 不维护采样规则，容器、采集量、共管/排斥归 F-022。平台标准项目编码是业务主键，LOINC、国家标准、地方标准编码为可选字段。项目主数据支持版本管理，历史申请/报告保留当时版本和快照；项目停用后新申请不可用但历史可查。平台提供受控标准项目查询/下载接口，作为 HIS/机构维护本地映射的数据源。

---

### F-018 用户权限与审计管理

| 属性 | 内容 |
|------|------|
| 功能编号 | F-018 |
| 功能名称 | 用户权限与审计管理 |
| 所属模块 | 平台基础能力 |
| 业务价值 | 为检验协同平台提供业务操作授权、数据访问控制和审计追溯基础，降低跨机构协同中的越权访问、误操作和责任不清风险。 |
| 核心用户 | 平台管理员、审计/管理人员、各业务模块授权用户、外部接入系统 |
| 平台入口/接口 | 权限校验能力、基础审计查询页面、接口/API 调用审计、业务操作审计 |
| 当前状态 | 已完成讨论 |

#### 功能定位

F-018 不在本 PRD 中展开账号体系、角色配置、菜单权限、按钮权限和登录认证方式等通用框架能力。具体角色、菜单和权限配置由平台框架权限模块或内部技术方案统一实现。

本功能只定义检验协同业务必须满足的权限控制点和审计要求：平台所有关键业务动作必须先完成授权校验，所有关键写操作、敏感查询、下载、打印和接口/API 调用必须留痕。

#### 平台负责

- 对关键业务动作执行权限校验。
- 按机构范围、数据范围控制用户可操作对象。
- 对页面操作和接口/API 调用记录审计日志。
- 对新增、修改、删除、审核、发布、撤回、更正、确认、关闭等写操作全量留痕。
- 对患者、报告、跨机构查询、历史版本、EMPI、规则配置、危急值等敏感查询留痕。
- 对报告下载、打印、历史版本访问等敏感访问留痕。
- 提供基础审计查询页面。
- 控制审计查询入口本身的访问权限。
- 保证审计日志业务上不可修改、不可删除。

#### 平台不负责

- 不在本 PRD 中定义固定角色清单。
- 不在本 PRD 中展开账号注册、用户生命周期、密码策略、单点登录等认证能力。
- 不在本 PRD 中定义菜单、按钮和页面级权限配置实现方式。
- 不在本 PRD 中强制高风险操作审批流。
- 不在本 PRD 中建设复杂审计分析、异常行为预警和风控模型。
- 不默认记录完整敏感请求/响应原文。

#### 触发条件

- 用户访问平台页面、执行业务操作、查询敏感数据、下载或打印报告。
- HIS、LIS、外部采样端、外部物流端等系统调用平台接口。
- 平台内部流程触发状态变更、审核、发布、撤回、更正、规则执行、危急值闭环等关键动作。
- 审计/管理人员进入审计查询页面检索审计记录。

#### 前置条件

- 用户或外部系统已完成认证。
- 平台已完成角色、菜单、操作权限和数据范围配置。
- 业务对象具备机构、来源系统、患者、申请、样本、报告、项目等可用于授权判断的归属信息。
- 审计日志存储、查询和保留策略已启用。

#### 后置结果

权限校验通过时：

- 平台允许执行对应业务动作。
- 平台记录操作审计或接口审计。
- 业务对象按对应流程发生状态或数据变化。

权限校验失败时：

- 平台拒绝执行对应动作。
- 平台返回权限不足提示或结构化错误。
- 平台记录失败审计。

审计查询执行后：

- 平台按权限和查询条件返回审计记录。
- 平台记录审计查询行为。

#### 核心字段/数据对象

| 对象 | 含义 | 关键说明 |
|------|------|----------|
| PermissionDomain | 权限域 | 表示申请、采样、物流、核收、审核、报告、危急值、EMPI、项目库、规则配置、系统配置、审计查询等业务域 |
| BusinessAction | 业务动作 | 表示创建、修改、撤销、确认、审核、发布、查询、下载、打印、停用、关闭等动作 |
| DataScope | 数据范围 | 表示用户可访问的机构、科室、项目、患者、样本、报告等范围 |
| AuditLog | 审计日志 | 记录页面操作、业务动作、敏感访问和接口/API 调用 |
| ApiAuditLog | 接口审计日志 | 记录外部系统请求、响应结果、失败原因和 Trace ID/Request ID |

审计日志至少包含：

- 操作人或调用系统。
- 操作人所属机构。
- 操作角色或授权上下文。
- 来源系统或客户端信息。
- 操作类型。
- 权限域。
- 业务对象类型。
- 业务对象标识。
- 操作时间。
- 操作结果。
- 失败原因。
- 变更前后状态摘要。
- 查询条件摘要。
- 患者标识摘要。
- Trace ID/Request ID。

#### 权限控制范围

权限控制采用“业务动作 + 机构/数据范围”。

P0 至少覆盖以下权限域：

- 申请：创建、查询、撤销、状态查看。
- 采样：标签打印、重打换码、采集确认、采集更正。
- 物流：建箱、装箱、出库、签收、异常登记。
- 核收：样本核收、质量初筛、异常登记、更正。
- 审核：人工审核、复检指令、报告发布、撤回/更正。
- 报告：查询、查看、下载、打印、历史版本访问、跨机构查询。
- 危急值：事件查看、通知记录、确认、处理、关闭。
- EMPI：患者索引查询、疑似重复处理、合并、排除、暂不处理、拆分。
- 项目库：标准项目新增、修改、版本发布、启用、停用、查询、下载。
- 规则配置：规则新增、修改、试运行、发布、停用、执行日志查看。
- 系统配置：机构、科室、字典、参数、系统开关等配置维护。
- 审计查询：审计日志查询和导出能力。

具体角色名称不固化，由实施时通过角色、菜单和权限配置完成。

#### 审计范围

审计范围采用“写操作全记录，敏感查询/下载/打印全记录”。

必须记录的写操作包括：

- 新增、修改、删除或停用。
- 撤销、作废、更正、恢复。
- 审核、发布、退回、复检指令。
- 危急值确认、处理、升级、关闭。
- EMPI 合并、拆分、排除、暂不处理。
- 规则发布、停用、试运行。
- 项目版本发布、停用、组合关系调整。
- 系统配置变更。

必须记录的敏感访问包括：

- 患者信息查询。
- EMPI 查询。
- 报告查询、查看、下载、打印。
- 历史报告版本访问。
- 跨机构全域报告查询。
- 规则配置和规则执行日志查看。
- 危急值事件查看。
- 审计日志查询。

页面操作和接口/API 调用均纳入审计范围，成功和失败请求都应记录。

#### 主流程

1. 用户或外部系统发起业务请求。
2. 平台识别请求来源、操作主体、权限域、业务动作和业务对象。
3. 平台根据角色授权、业务动作和机构/数据范围进行权限校验。
4. 权限通过后，平台执行对应业务流程。
5. 权限不通过时，平台拒绝操作并返回权限不足。
6. 平台记录操作审计或接口审计。
7. 审计/管理人员通过受控入口查询审计日志。
8. 平台对审计查询行为本身记录审计。

#### 异常处理

- 未认证用户访问：拒绝访问，并记录安全审计。
- 外部系统认证失败：拒绝接口调用，并记录接口审计。
- 权限不足：拒绝操作，并记录失败审计。
- 数据范围不匹配：拒绝访问对应业务对象，并记录失败审计。
- 敏感查询条件过宽：可拒绝查询或要求补充查询条件，并记录审计。
- 审计日志写入失败：业务是否阻断放入系统实现细节讨论，但必须有补偿和告警机制。
- 审计查询权限不足：拒绝进入或拒绝查询，并记录访问审计。

#### 性能与时效要求

- 权限校验不得明显影响采样、物流、核收、审核等高频操作响应。
- 审计写入应支持页面操作和接口/API 调用的高并发场景。
- 基础审计查询页面支持按时间、用户、机构、业务对象、操作类型、操作结果等条件查询。
- 审计日志在线保留不少于 1 年，支持配置延长。
- 审计日志业务上不可修改、不可删除，不提供业务删除、修改、归档入口。
- 具体审计日志存储结构、索引、归档、容量和性能指标暂存至 `系统实现相关.md`。

#### 通知与审计要求

- F-018 本身不强制产生业务通知。
- 所有权限失败、关键写操作、敏感查询、下载、打印、接口/API 调用必须记录审计。
- 审计日志记录关键字段、摘要、结果和 Trace ID/Request ID。
- 敏感字段应脱敏或摘要化。
- 不默认记录完整敏感请求/响应原文。
- 高风险操作 P0 只做权限校验和审计追溯，不强制二次确认或审批流。

#### 与 HIS/LIS/仪器/患者端的边界

| 事项 | 平台 F-018 | 外部系统/后续模块 |
|------|------------|------------------|
| 平台用户权限 | 提供业务权限控制要求 | 框架权限模块实现具体配置 |
| 登录认证方式 | 不固定 | 本地账号、SSO、统一身份认证等放内部讨论 |
| HIS/LIS/API 调用审计 | 平台记录接口审计 | 外部系统提供调用方身份和请求标识 |
| 机构/数据范围 | 平台按业务对象归属控制 | 外部系统传入准确机构、科室、人员、业务对象信息 |
| 菜单/按钮权限 | 不在 PRD 展开 | 框架权限配置实现 |
| 审批流 | P0 不强制 | 如需审批流，放内部讨论或后续增强 |

#### 遗留问题与内部待确认

以下事项已登记在 `风险_待内部讨论确定.md`，不作为 F-018 无条件定稿结论：

- 登录认证方式采用本地账号、SSO、统一身份认证或混合模式。
- 高风险操作是否需要二次确认、双人复核或审批流。
- 审计日志是否允许技术归档、冷存储或长期在线保留的具体策略。
- 审计查询是否需要导出、脱敏导出和导出审批。
- 异常访问识别、审计复核和行为预警是否纳入后续建设。

以下事项已登记在 `系统实现相关.md`，待需求边界确定后讨论：

- F-018 权限域、业务动作和数据范围模型。
- F-018 审计日志和接口审计日志数据结构。
- F-018 权限校验中间件、接口鉴权和失败审计机制。
- F-018 审计日志存储、索引、查询性能和容量策略。
- F-018 敏感字段脱敏、摘要化和 Trace ID/Request ID 贯通机制。

#### 本轮确认结论

F-018 在本 PRD 中不展开账号、角色、菜单、按钮、登录认证等通用框架能力，只定义检验协同业务权限控制点和审计要求。权限控制采用“业务动作 + 机构/数据范围”，具体角色由框架权限配置实现。审计范围为写操作全记录，敏感查询、下载、打印全记录；页面操作和接口/API 调用都纳入审计。P0 提供基础审计查询页面，入口通过角色菜单权限控制。审计日志在线保留不少于 1 年，支持配置延长；业务上不可修改、不可删除，不提供业务删除、修改、归档入口。审计日志记录关键字段、摘要、结果和 Trace ID/Request ID，敏感字段脱敏或摘要化，不默认记录完整敏感请求/响应原文。高风险操作 P0 只做权限校验和审计追溯，不强制二次确认或审批流。

---

### F-019 系统配置管理

| 属性 | 内容 |
|------|------|
| 功能编号 | F-019 |
| 功能名称 | 系统配置管理 |
| 所属模块 | 平台基础能力 |
| 业务价值 | 为平台运行配置、全局开关和后续扩展配置提供统一承载方向，但本轮不展开具体功能细节。 |
| 核心用户 | 平台管理员、平台运维人员 |
| 平台入口/接口 | 暂不定稿 |
| 当前状态 | 等待内部讨论 |

#### 功能定位

F-019 是平台运行配置能力，主要用于管理全局功能开关、少量运行参数，以及后续可能的机构级差异配置。

本轮不继续展开 F-019 的 P0 详细功能，不定义页面、字段、参数清单、配置分类、导入导出、权限角色和实现方式。该功能整体转入内部讨论确定。

#### 当前确认边界

- F-019 不负责用户、角色、菜单、按钮和权限配置，这些归 F-018/平台框架权限能力。
- F-019 不负责检验项目主数据，检验项目标准化归 F-017。
- F-019 不负责采样规则、容器、采集量、合管/排斥规则，这些归 F-022。
- F-019 不负责系统字典体系，系统字典放到系统架构/平台框架层面，不在本 PRD 中讨论。
- F-019 不负责机构、科室、人员主数据的手工维护；外部主数据同步、基础数据来源和失败处理转入内部讨论。
- F-019 不负责接口限流、重试次数、超时时间、任务调度等技术运行参数，这些归系统实现相关。
- F-019 不在本轮承诺机构级差异配置能力，机构级差异配置转入内部讨论。
- F-019 不在本轮承诺配置导入/导出能力；如后续需要，倾向优先只支持导出、不支持导入。

#### 暂不展开内容

以下内容本轮不作为 F-019 定稿能力：

- 全局功能开关清单。
- 业务运行参数清单。
- 节假日/工作时间配置。
- 机构级差异配置。
- 配置导入/导出。
- 配置草稿、发布、审批和回滚。
- 系统字典管理。
- 机构、科室、人员主数据维护。
- 技术运行参数维护。
- 配置权限角色。

#### 与其他模块的边界

| 事项 | F-019 当前边界 | 归属 |
|------|----------------|------|
| 用户权限 | 不展开 | F-018/平台框架 |
| 检验项目 | 不负责 | F-017 |
| 采样规则 | 不负责 | F-022 |
| 系统字典 | 不在 PRD 讨论 | 系统架构/平台框架 |
| 机构/科室/人员主数据 | 不手工维护 | 外部主数据/内部讨论 |
| 技术运行参数 | 不作为业务功能 | 系统实现相关 |
| 机构级差异配置 | 不定稿 | 内部讨论 |
| 节假日/工作时间 | 不定稿 | 内部讨论 |

#### 本轮确认结论

F-019 系统配置管理整体转入内部讨论。当前仅确认其定位：用于承载平台运行配置、全局功能开关、少量运行参数以及后续可能的机构级差异配置。本 PRD 不展开其页面、字段、参数清单、字典体系、机构/科室/人员主数据维护、技术运行参数、导入导出、权限角色等内容。系统字典归系统架构/平台框架层面处理；权限归 F-018/框架权限控制；检验项目归 F-017；采样规则归 F-022；技术参数归系统实现相关。

---

### F-020 消息通知中心

| 属性 | 内容 |
|------|------|
| 功能编号 | F-020 |
| 功能名称 | 消息通知中心 |
| 所属模块 | 平台基础能力 |
| 业务价值 | 为危急值、样本异常、状态推送、报告发布等业务事件提供后续通知能力承载方向。 |
| 核心用户 | 平台管理员、业务接收人员、外部系统 |
| 平台入口/接口 | 暂不定稿 |
| 当前状态 | 等待内部讨论 |

#### 功能定位

F-020 消息通知中心的建设方案后续内部讨论确定。本 PRD 当前不展开通知渠道、模板、回执、重试、升级、发送记录、站内信、短信、语音、微信、HIS 原生提醒等具体能力。

#### 当前确认边界

- 已完成业务模块中涉及“是否通知、通知谁、通过什么渠道通知、失败如何重试”等内容，均不在对应业务功能内展开。
- F-010 危急值闭环可以生成通知任务并调用 F-020 或外部通知能力，但具体通道建设不在 F-010 中定稿。
- F-005 状态推送与人员通知分离；人员通知归 F-020 或外部通知能力。
- F-020 是否作为平台内建能力、对接外部消息平台，还是由 HIS/第三方系统承载，转入内部讨论。

#### 本轮确认结论

F-020 消息通知中心整体转入内部讨论。当前仅确认其作为后续通知能力承载方向，不在本 PRD 中继续拆解通知渠道、模板、回执、重试、升级和发送记录等建设方案。

---

### F-021 数据备份与恢复

| 属性 | 内容 |
|------|------|
| 功能编号 | F-021 |
| 功能名称 | 数据备份与恢复 |
| 所属模块 | 平台基础能力 |
| 业务价值 | 保障平台数据安全和灾难恢复能力，但作为框架/基础设施能力处理。 |
| 核心用户 | 平台运维人员 |
| 平台入口/接口 | 不在本 PRD 中定义 |
| 当前状态 | 等待内部讨论 |

#### 功能定位

F-021 数据备份与恢复放到框架/基础设施能力中，不在本 PRD 中继续讨论。本 PRD 不定义备份页面、备份策略、恢复演练、备份文件管理、恢复流程、备份权限和运维操作细节。

#### 当前确认边界

- 业务 PRD 只要求平台数据具备备份与恢复保障。
- 具体备份范围、频率、保留周期、恢复点目标、恢复时间目标、演练流程和运维责任归框架/基础设施方案。
- 各业务模块不单独定义备份恢复功能。

#### 本轮确认结论

F-021 不作为当前业务 PRD 的功能继续展开，归入框架/基础设施能力处理。

---

### F-022 样本采集规则与合管规则配置

| 属性 | 内容 |
|------|------|
| 功能编号 | F-022 |
| 功能名称 | 样本采集规则与合管规则配置 |
| 所属模块 | 平台基础能力/采样管理 |
| 业务价值 | 为 F-001 自动生成采样计划、样本条码和 F-002 采样提示提供规则依据，保证跨机构采样标准一致。 |
| 核心用户 | 检验中心规则维护人员、平台管理员、采样人员 |
| 平台入口/接口 | 采样规则维护、规则启停用、采样计划试算、规则历史查询 |
| 当前状态 | 已完成讨论 |

#### 功能定位

F-022 用于维护平台统一的样本采集规则和基础合管/排斥规则，支撑 F-001 根据申请项目自动生成采样计划，并按“一支物理样本容器一个条码”生成样本条码。

F-022 不建设完整采样规则引擎，不处理复杂脚本、复杂优先级、机构差异、特殊人群动态规则或批量历史回放。

#### 平台负责

- 维护明细项目默认采样要求。
- 支持组合项目配置覆盖采样规则。
- 维护样本类型、容器/抗凝剂、建议采集量、最低采集量。
- 维护基础合管规则组。
- 维护必须单管规则。
- 维护不可与指定项目或规则组合管的排斥规则。
- 维护文本型采集注意事项。
- 维护基础采集顺序/显示顺序。
- 支持规则版本管理。
- 支持单次采样计划试算。
- 为 F-001 输出可执行采样计划和规则快照。
- 记录规则维护、启停用、试算和历史查看审计。

#### 平台不负责

- 不支持机构自行维护采样规则。
- 不支持机构差异化采样规则。
- 不支持特殊人群采样规则，相关事项转入内部讨论。
- 不将采样时间窗、稳定性要求作为结构化规则参与计算。
- 不支持复杂实验室规则引擎、脚本规则和批量历史申请回放。
- 不允许采样人员普通手工拆管或合管。
- 不在 F-022 中定义规则维护角色，权限归 F-018/框架权限控制。

#### 触发条件

- F-001 创建平台申请时，根据平台标准项目和 F-022 规则生成采样计划。
- 规则维护人员新增、修改、启用、停用采样规则。
- 规则维护人员输入一组平台标准项目进行采样计划试算。
- F-002 采样页面展示样本类型、容器、采集量、采集顺序和注意事项。

#### 前置条件

- F-017 中平台标准项目已存在并启用。
- 明细项目具备默认采样规则。
- 如组合项目需要特殊处理，已配置组合项目覆盖规则。
- 样本类型、容器/抗凝剂等基础字典已由系统架构/平台框架提供。
- 规则维护人员具备对应权限。

#### 后置结果

规则完整时：

- F-001 生成采样计划。
- 平台按物理容器生成样本条码。
- 每个样本条码关联对应项目、样本类型、容器、采集量和规则快照。
- F-002 可展示采样要求。

核心规则缺失时：

- F-001 整单拒绝。
- 平台返回缺失规则、影响项目和失败原因。

非核心信息缺失时：

- F-001 可创建申请。
- 平台提示配置不完整。
- 注意事项等非核心提示可为空。

#### 核心字段/数据对象

| 对象 | 含义 | 关键说明 |
|------|------|----------|
| SamplingRule | 采样规则 | 明细项目或组合项目覆盖规则的采样要求 |
| SamplingRuleVersion | 采样规则版本 | 记录规则生效期间的配置内容 |
| TubeRuleGroup | 合管规则组 | 表示哪些项目可按同类规则合并到同一容器 |
| ExclusionRule | 排斥规则 | 表示必须单管或不可与指定项目/规则组合管 |
| SamplingPlan | 采样计划 | F-001 创建申请时生成的样本管计划 |
| SamplingRuleSnapshot | 规则快照 | 申请创建时保留的规则版本和关键字段 |
| SamplingSimulation | 采样计划试算 | 输入项目后输出预计管数和采样要求 |

P0 采样规则字段包括：

- 平台标准项目编码。
- 项目类型：明细项目/组合项目覆盖。
- 样本类型。
- 容器类型。
- 抗凝剂或容器特征。
- 建议采集量。
- 最低采集量。
- 合管规则组。
- 是否必须单管。
- 不可合管项目或不可合管规则组。
- 基础采集顺序/显示顺序。
- 文本型采集注意事项。
- 规则版本。
- 启停用状态。

#### 规则绑定方式

- 采样规则以明细项目为基础。
- 明细项目维护默认采样要求。
- 组合项目可配置覆盖规则。
- 未配置组合项目覆盖规则时，平台按组合项目包含的明细项目默认规则计算采样计划。
- 配置组合项目覆盖规则时，平台优先使用组合覆盖规则参与采样计划计算。

#### 合管与排斥规则

合管判断 P0 按以下条件：

- 样本类型一致。
- 容器/抗凝剂一致。
- 属于允许合管的规则组。
- 未命中必须单管规则。
- 未命中不可与指定项目或规则组合管的排斥规则。

排斥规则支持：

- 必须单管：命中后独立生成一支样本条码。
- 不可与指定项目合管：指定项目同时存在时拆管。
- 不可与指定规则组合管：指定规则组同时存在时拆管。

#### 采集量与注意事项

- 采集量配置建议采集量和最低采集量。
- F-002 采样页面优先展示建议采集量。
- F-004 样本核收与质量初筛可参考最低采集量判断样本量不足。
- 采集注意事项采用文本方式维护和展示。
- 采样时间窗、稳定性要求、避光、立即送检、立即离心等先作为文本注意事项，不参与采样计划计算。

#### 规则版本管理

- 采样规则支持版本管理。
- 新申请使用当前启用版本。
- 历史申请保留创建时规则快照。
- 历史样本条码不因规则变更被改写。
- 规则变更不影响已创建申请。
- 已创建申请、采样计划和样本条码继续使用创建时规则快照。

#### 采样计划试算

F-022 P0 支持单次采样计划试算：

- 输入一组平台标准项目。
- 平台按当前启用规则计算预计样本管数。
- 输出样本类型、容器/抗凝剂、项目归属、建议采集量、最低采集量、注意事项和采集顺序。
- 试算结果不生成正式申请。
- 试算结果不生成样本条码。
- 试算操作需要记录审计。

#### 主流程

1. 规则维护人员维护明细项目默认采样规则。
2. 如组合项目存在特殊采样要求，维护组合项目覆盖规则。
3. 规则维护人员配置合管规则组、必须单管和排斥规则。
4. 规则维护人员配置建议采集量、最低采集量、注意事项和显示顺序。
5. 规则维护人员使用试算能力验证一组项目的采样计划。
6. 规则启用后，F-001 创建申请时读取当前启用规则。
7. 平台计算采样计划并生成样本条码。
8. 平台保存采样规则快照。
9. F-002 按采样计划展示采样要求并执行采样。

#### 异常处理

- 明细项目缺少核心采样规则：F-001 整单拒绝。
- 组合覆盖规则配置不完整：禁止启用或提示配置冲突。
- 合管规则冲突：按排斥规则优先，无法判断时拆管或拒绝启用冲突规则。
- 必须单管与合管规则冲突：必须单管优先。
- 注意事项缺失：允许创建申请，但提示配置不完整。
- 试算项目不存在或停用：试算失败并提示项目无效。
- 采样计划特殊情况需调整：普通人工不可调整，走受控异常流程并审计。

#### 性能与时效要求

- F-001 创建申请时采样规则计算不得明显影响接口响应。
- 试算应快速返回预计采样计划。
- 采样规则查询应支持按项目、样本类型、容器、启停用状态筛选。
- 具体规则计算算法、冲突检测、缓存和性能指标暂存至 `系统实现相关.md`。

#### 通知与审计要求

- 规则新增、修改、启用、停用、试算、历史查看必须记录审计。
- 审计至少包含规则编号、规则版本、项目编码、变更内容、操作人、操作时间、前后状态、Trace ID/Request ID。
- F-022 不定义人员通知。
- 规则维护权限归 F-018/框架权限控制。

#### 与 HIS/LIS/仪器/患者端的边界

| 事项 | 平台 F-022 | 外部系统/后续模块 |
|------|------------|------------------|
| 采样规则维护 | 平台/检验中心统一维护 | 机构不自行维护 |
| HIS 创建申请 | 不直接参与 HIS 开单 | HIS 通过 F-001 传平台标准项目 |
| 采样计划生成 | 为 F-001 提供规则依据 | F-001 生成正式采样计划 |
| 采样页面展示 | 提供规则结果 | F-002 展示并执行采样 |
| 样本质量判断 | 提供最低采集量参考 | F-004 执行核收和质量初筛 |
| 检验项目主数据 | 不维护 | F-017 维护 |
| 系统字典 | 不展开 | 系统架构/平台框架 |

#### 遗留问题与内部待确认

以下事项已登记在 `风险_待内部讨论确定.md`，不作为 F-022 无条件定稿结论：

- 特殊人群采样规则是否需要支持。
- 采样时间窗、稳定性要求是否后续结构化并参与物流/质量判断。
- 是否需要批量历史申请回放验证规则变更影响。
- 合管冲突处理细则。
- 受控异常流程如何触发采样计划重算或补采/重采。

以下事项已登记在 `系统实现相关.md`，待需求边界确定后讨论：

- F-022 采样规则、规则版本、规则快照、合管/排斥规则数据模型。
- F-022 采样计划计算算法和冲突检测。
- F-022 单次试算接口和性能指标。
- F-022 与 F-001/F-002/F-004/F-017 的数据衔接。

#### 本轮确认结论

F-022 用于支撑 F-001 自动生成采样计划和样本条码。平台维护单项目采样要求和基础合管/排斥规则，不建设完整采样规则引擎。规则以明细项目为基础，可对组合项目配置覆盖规则。合管按样本类型、容器/抗凝剂和规则组判断；排斥规则支持必须单管和不可与指定项目/规则组合管。采集量配置建议采集量和最低采集量；采集注意事项、采样时间窗和稳定性要求以文本方式展示，不参与复杂计算。核心规则缺失时 F-001 整单拒绝，非核心信息缺失允许创建但提示配置不完整。采样计划由规则自动生成，普通人工不可调整；特殊情况走受控异常流程。规则支持版本，历史申请保留创建时规则快照，新规则只影响之后新创建的申请。P0 不支持机构差异化采样规则。支持单次采样计划试算和基础采集顺序/显示顺序。特殊人群采样规则转内部讨论。权限角色不在 F-022 展开，归 F-018/框架权限控制。

---

