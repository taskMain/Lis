# 检验中心协同平台 — 讨论稿2

> 来源：`版本1/PRD_定稿1.md`、`版本2/新需求.txt`、版本2后续讨论结论
>
> 用途：记录 PRD_定稿2 生成前的讨论结论、边界调整和待内部确认事项。本文不是最终 PRD，后续用于整理写入 `版本2/PRD_定稿2.md`。

---

## 1. 本轮核心调整方向

相较 `PRD_定稿1.md`，本轮需求有以下方向性调整：

- 平台从“生成条码、生成采样计划、维护采样规则/合管规则”的模式，调整为“机构自行生成条码并传给平台，平台保存并校验基础关系”的模式。
- 项目对照方式暂不强行选择单一方案，后续内容需要同时兼容“机构传中心编码”和“机构传本地编码、中心转换”两种方式。
- 多中心医院场景纳入本轮讨论，每张申请必须明确一个接收中心。
- 当前不在讨论稿2中固化完整样本状态机，样本流转深度转入内部讨论确定。
- 当前不在讨论稿2中最终固化标准项目维护维度，标准项目是否只维护大项目转入内部讨论确定，并需明确后续影响。
- PRD2 需要按“平台上完成的操作和功能 / 平台开放接口能力 / 医院系统需实现的对接能力”拆分职责边界。

---

## 2. 已确认结论

### 2.1 检验项目对照方式：双模式兼容

当前不强行选择单一项目对照模式，讨论稿2后续内容需兼容两种方式：

- 模式 A：机构自行维护本地项目与目标中心项目编码的对照，提交申请时直接传目标中心项目编码。
- 模式 B：机构提交本机构项目编码，由平台或中心根据“发起机构 + 接收中心 + 机构项目编码”转换为目标中心项目编码。

后续 PRD2 中，申请创建、签收扫码、信息查询、结果上传、报告展示均需满足两种项目编码来源。最终默认采用哪种方式，或是否允许不同中心/机构采用不同模式，放入内部讨论确定。

### 2.2 多中心医院：固定接收中心模式

采用固定接收中心模式：

- 每张申请必须明确一个接收中心医院。
- 发起机构可以是乡镇医院，也可以是另一个中心医院。
- 接收中心决定本次申请使用的项目编码体系、签收核收、检测结果上传和报告归属。
- 平台当前不做自动分诊、自动调度、按规则分配中心。
- 如后续需要平台统一调度，另行放入内部讨论或后续版本。

### 2.3 条码生成与打印：机构负责，平台做基础校验

采用“机构自行生成条码，平台保存并做基础校验”的方式：

- 平台不负责生成条码。
- 平台不负责条码打印、打印模板、打印机配置。
- 平台不维护条码生成规则、条码前缀、条码长度、码制和打印格式。
- 平台不负责采样规则、合管规则、自动拆管或自动合管。
- 机构创建申请时需把条码号传给平台。
- 平台保存条码，并建立“申请单 - 样本条码 - 项目 - 接收中心”的关系。
- 平台校验条码不能为空。
- 平台校验同一接收中心范围内条码不可重复绑定到不同申请或不同样本。
- 条码打印错误、格式不一致、现场贴错标签等由机构侧负责；平台仅在扫码、签收、查询时发现并记录异常。

### 2.4 PRD2 结构：业务流程与三类职责结合

PRD2 采用“两者结合”的结构：

- 主体仍按业务流程/功能模块组织，便于理解实际流程。
- 每个核心模块下拆成三段：
  - 平台上完成的操作和功能。
  - 平台需要开放的接口能力。
  - 医院 HIS/LIS/检验系统需要实现的对接能力。
- 文档后部汇总：
  - 平台开放接口清单。
  - 医院系统需改造/对接清单。
  - 暂不开发或待内部讨论事项。

### 2.5 创建申请：机构必须同时传条码和样本信息

机构创建申请时必须同时传条码和样本信息：

- 机构调用创建申请接口时，需同时提交患者信息、申请机构/科室/医生信息、接收中心、项目编码、条码号、样本信息。
- 平台创建申请单时同步建立申请单、项目、样本条码之间的关系。
- 平台返回平台申请单号。
- 平台不根据项目自动拆管、合管或生成采样计划。
- 如果条码或必要样本信息缺失，平台拒绝创建申请。
- 后续送出、签收、查询、结果上传均基于创建时登记的条码进行匹配。

### 2.6 一个申请允许多个条码/样本

一个申请允许多个条码/样本：

- 一张平台申请可包含一个或多个样本条码。
- 每个样本条码可关联一个或多个大项目。
- 机构侧自行决定拆几管、每个条码对应哪些项目。
- 平台只保存机构传入的条码、样本、项目关系，不负责根据采样规则重新拆分或合并。
- 平台需要保证同一接收中心范围内条码唯一，并保证条码、样本、项目关系可查询、可追踪。

### 2.7 样本送出与接收：平台页面和接口都支持

样本送出与接收采用“页面 + 接口都支持”的方式：

- 发起机构送出样本时，可在平台页面通过扫码或勾选确认哪些样本已送出。
- 发起机构也可通过 HIS、LIS 或院内检验系统调用平台接口确认样本送出。
- 接收中心接收样本时，可在平台页面通过扫码或勾选确认哪些样本已接收。
- 接收中心也可通过 LIS 或检验系统调用平台接口确认样本接收。
- 平台负责记录送出、接收的样本条码、操作时间、操作人或调用系统、发起机构、接收中心和处理结果。
- 页面操作和接口操作应遵守同一套校验规则，避免同一条码被重复送出、重复接收或跨中心误接收。
- 现场系统对接能力不足时，可先使用平台页面；系统对接成熟后，可切换或并行使用接口。

### 2.8 样本异常处理：异常登记 + 处理结果闭环

样本异常处理采用“异常登记 + 处理结果闭环”的方式：

- 平台支持在送出、接收、签收、检测前等环节登记样本异常。
- 异常登记至少记录样本条码、申请单号、发现环节、异常类型、异常描述、发现机构、发现人或调用系统、发现时间。
- 异常类型可包括条码无法识别、条码重复、样本未收到、样本多出、样本破损、样本泄漏、样本量不足、容器不符、申请信息不匹配、项目不匹配、其他异常。
- 平台支持记录责任方或待处理方，例如发起机构、接收中心、物流方、接口调用方或待人工确认。
- 平台支持记录处理意见、处理结果、处理人、处理时间和关闭时间。
- 平台不自动生成补采任务，不修改 HIS 医嘱，不处理收费、退费或重新开单。
- 是否需要超时升级、消息通知、审批流、自动补采任务，放入后续版本或内部讨论。

### 2.9 平台查询能力：业务常用查询

平台查询能力采用业务常用查询范围：

- 平台提供查询页面和查询接口。
- 支持按平台申请单号查询患者、项目、样本、送出、接收、异常和报告相关信息。
- 支持按样本条码号查询患者、项目、样本、送出、接收、异常和报告相关信息。
- 支持按患者信息查询相关申请和报告，具体可用字段可包括姓名、证件号、就诊号、患者 ID 等。
- 支持按发起机构、接收中心、申请时间、送出时间、接收时间、样本处理状态、异常处理状态等条件查询。
- 查询结果需要体现发起机构、接收中心、平台申请单号、机构申请单号、条码号、项目、患者摘要、样本信息、当前处理状态和异常标记。
- 平台查询不等同于统计分析；TAT 分析、异常率、机构排行、项目统计等暂不作为当前查询范围。
- 查询页面和查询接口都应受权限和数据范围控制，并记录必要审计。

### 2.10 检验结果上传：结构化结果 + 可选报告文件

LIS/检验系统上传结果采用“结构化结果 + 可选报告文件”的方式：

- 接收中心完成检验后，通过接口将检验结果上传平台。
- 上传结果必须关联平台申请单号或样本条码号，确保平台可匹配到申请、患者、项目、样本和接收中心。
- LIS/检验系统需上传结构化结果明细，平台保存并用于报告查询展示。
- 当前不强制每个结果明细项映射到平台标准明细项目编码，可先保存 LIS 原始结果项编码、名称、结果值、单位、参考范围、异常标记等。
- 报告 PDF、图片、HTML 或其他版式文件作为可选上传内容；LIS 厂商不提供版式文件时，不阻塞平台结构化报告查询。
- 平台不负责仪器原始结果采集，不替代 LIS 完成检测、审核和报告生成流程。
- 结果更正、报告撤回、危急值触发边界和通知机制后续继续讨论或转入内部确认。

### 2.11 报告与危急值：报告查询 + 危急值记录，通知机制待内部讨论

报告与危急值当前采用“平台做报告查询 + 危急值记录，通知机制待内部讨论”的方式：

- 平台保存 LIS/检验系统上传的结构化检验结果。
- 平台支持报告查询页面和报告查询接口。
- 平台支持按平台申请单号、样本条码号、患者信息等查询报告。
- 平台支持展示报告最新结果；历史版本、报告更正和撤回规则后续内部确认。
- 平台可保存 LIS/检验系统上传的危急值标记或危急值事件信息。
- 平台支持危急值记录查询和审计。
- 平台当前不直接定稿危急值通知通道、通知对象、确认人、升级策略和闭环统计。
- 报告发布通知、危急值主动通知、通知回执、超时升级和消息中心建设继续放入内部讨论确定。

### 2.12 平台开放接口清单

PRD2 当前按功能边界提供以下平台开放接口，字段级设计后续在接口文档中展开：

- 创建检验申请接口：接收患者信息、申请机构/科室/医生、接收中心、项目编码、样本信息和机构生成的条码号，返回平台申请单号。
- 查询申请/样本/报告接口：支持按平台申请单号、样本条码号、患者信息、机构、接收中心等条件查询。
- 样本送出确认接口：由发起机构或其系统确认哪些样本已送出。
- 样本接收确认接口：由接收中心或其 LIS/检验系统确认哪些样本已接收。
- 样本异常登记/处理接口：支持登记异常、更新处理意见、记录处理结果和关闭异常。
- 检验结果上传接口：由接收中心 LIS/检验系统上传结构化检验结果和可选报告文件。
- 危急值记录上传或同步接口：由 LIS/检验系统上传危急值标记或危急值事件信息。
- 基础项目目录查询/下载接口：支持机构查询或下载中心项目目录，并兼容“机构传中心编码”和“机构传本地编码、中心转换”两种对照模式。

### 2.13 医院系统需实现的对接能力

医院 HIS/LIS/检验系统需要按以下边界配合：

- 机构系统负责生成样本条码并完成本地打印。
- 机构创建申请时，需向平台传患者信息、申请机构/科室/医生、接收中心、项目编码、样本信息和条码号。
- 机构需维护本地项目与中心项目的对照，或按平台/中心要求传本地项目编码，由中心侧转换。
- 发起机构系统可调用样本送出确认接口；若暂不具备接口能力，人员可在平台页面确认。
- 接收中心 LIS/检验系统可调用样本接收确认接口；若暂不具备接口能力，人员可在平台页面确认。
- 接收中心 LIS/检验系统负责完成检测、审核和报告生成，并向平台上传结构化结果和可选报告文件。
- 如需在平台留存危急值记录，LIS/检验系统需上传危急值标记或危急值事件信息。
- 医院系统侧负责本地条码打印错误、贴签错误、项目映射错误、接口重试和本地操作培训等事项。

---

## 3. 转入内部讨论确定的事项

### 3.1 样本流转深度

样本流转深度暂不在讨论稿2中定稿为“仅转出/接收”或“完整状态链”，整体转入内部讨论确定。

讨论稿2后续只保留必要边界：

- 平台至少需要支持基于申请单号或条码号查询患者、项目、样本信息。
- 机构送出样本时，需要在平台或通过接口确认哪些样本已送出。
- 接收中心签收样本时，需要在平台或通过接口确认哪些样本已接收。
- 样本异常需要有异常登记和处理记录。
- 是否扩展为完整状态链，另行内部确认。

### 3.2 标准项目维护维度

标准项目维护维度暂不在讨论稿2中最终定稿为“仅维护大项目”或“维护大项目 + 明细项目”，整体转入内部讨论确定。

讨论稿2后续如需引用项目库范围，只按以下方式表述：

- 当前倾向简化为大项目管理，明细项目标准化暂不展开。
- 大项目示例：血常规、尿常规、肝功四项、凝血四项等。
- LIS 上传的报告明细可作为报告内容展示，但平台是否维护明细项目标准编码另行内部确认。

只维护大项目会影响后续能力：

- 自动审核规则难以直接绑定到具体明细结果项。
- 危急值规则可能只能粗粒度绑定到大项目，或依赖 LIS 侧判断。
- 跨机构结果互认、历史结果对比、趋势图、统计分析和质控分析会缺少统一明细项目口径。
- 报告查询可展示 LIS 上传的明细结果，但平台不能保证不同中心或不同 LIS 的明细名称、单位、参考范围完全一致。
- 后续升级到明细项目标准化时，需要补充大项目与明细项目关系、历史数据兼容、接口字段扩展和项目版本迁移规则。

### 3.3 报告与危急值通知机制

出报告和危急值的通知机制尚未讨论完成，转入后续讨论。

待讨论点包括：

- 报告发布后是否主动通知申请机构、申请医生或科室。
- 危急值是否由平台直接通知，还是由中心 LIS/HIS/第三方系统通知。
- 通知通道采用站内信、HIS 原生提醒、短信、电话、微信/企微，还是只提供接口事件。
- 通知成功、送达、确认、闭环的责任系统分别是谁。
- 报告通知和危急值通知是否统一由消息通知中心承载。

---

## 4. 本轮讨论状态

本轮围绕 `版本2/新需求.txt` 的主要边界已完成讨论，并已记录到本文。

后续可基于本文整理 `版本2/PRD_定稿2.md`。仍需保留为内部讨论的事项包括：

- 检验项目对照方式最终采用哪种模式，或是否长期支持双模式。
- 样本流转深度是仅保留关键节点，还是建设完整状态链。
- 标准项目维护维度是否只维护大项目，以及何时扩展明细项目标准化。
- 报告发布通知、危急值主动通知、通知回执、超时升级和消息中心建设方案。
- 报告历史版本、结果更正、报告撤回和危急值触发边界。
